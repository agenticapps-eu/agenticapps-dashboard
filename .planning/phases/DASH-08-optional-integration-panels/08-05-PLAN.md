---
phase: 08-optional-integration-panels
plan: 05
type: tdd
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - packages/agent/src/routes/linear.ts
  - packages/agent/src/routes/linear.test.ts
  - packages/agent/src/server/app.ts
autonomous: true
requirements: [LINEAR-01, LINEAR-02, LINEAR-03, INV-05, INV-01]
must_haves:
  truths:
    - "GET /api/projects/{id}/linear/issues returns title/status/assignee for issues detected from branch + git log when LINEAR_API_KEY is set"
    - "Issue IDs are detected from the current branch AND recent commit messages, deduped, capped at 3"
    - "Without LINEAR_API_KEY the route returns 404 with a not_configured body"
    - "Linear's HTTP-400 RATELIMITED signal is classified as rate-limited, not unreachable"
    - "Both new routes (sentry, linear) are mounted under /api/projects and reachable through bearer-auth middleware"
  artifacts:
    - path: "packages/agent/src/routes/linear.ts"
      provides: "linearRoute Hono handler for /:id/linear/issues + per-issue cache + evictLinearCacheProject"
      exports: ["linearRoute", "evictLinearCacheProject"]
  key_links:
    - from: "packages/agent/src/routes/linear.ts"
      to: "api.linear.app/graphql"
      via: "POST with raw-key Authorization (no Bearer)"
      pattern: "api.linear.app/graphql"
    - from: "packages/agent/src/server/app.ts"
      to: "packages/agent/src/routes/sentry.ts + linear.ts"
      via: "app.route('/api/projects', sentryRoute) + linearRoute"
      pattern: "app.route\\('/api/projects', (sentry|linear)Route\\)"
---

<objective>
Implement the env-gated Linear data route `GET /api/projects/{id}/linear/issues`: detect issue IDs from the current branch name AND recent commit messages (deduped, capped at 3 per D-08-07), fetch each via the Linear GraphQL API using the human-readable identifier, cache per `projectId:issueId` for 60s with last-good fallback, sanitize errors (including Linear's non-standard HTTP-400 RATELIMITED). Then mount both the Sentry (Plan 03) and Linear routes into app.ts.

Purpose: Delivers LINEAR-01/02/03 and makes both new routes live. app.ts is owned by this plan only (depends on Plan 03's sentry.ts existing) so the single shared file has one writer.
Output: linear.ts route + test, and the two app.ts route mounts + imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-08-optional-integration-panels/08-RESEARCH.md
@.planning/phases/DASH-08-optional-integration-panels/08-PATTERNS.md
@.planning/phases/DASH-08-optional-integration-panels/08-01-SUMMARY.md
@.planning/phases/DASH-08-optional-integration-panels/08-02-SUMMARY.md
@.planning/phases/DASH-08-optional-integration-panels/08-03-SUMMARY.md
@packages/agent/src/routes/integrations.ts

<interfaces>
<!-- Consumed contracts — build against these. -->

From @agenticapps/dashboard-shared (Plan 01): `LinearIssuesResponseSchema`, type `LinearIssuesResponse`, `LinearIssueSchema`.

From packages/agent/src/lib/outboundFetch.ts (Plan 02): `fetchWithTimeout`, `classifyError(err, status?, body?)` (already handles 400+RATELIMITED → rate-limited), `CacheEntry<T>`.

From the codebase:
- packages/agent/src/lib/git.ts: `runAllowedGit('branch', root)` → current branch; `runAllowedGit('log', root)` → `git log --oneline -20` (20-commit cap already baked into ARGV_BY_CMD — Pitfall 6 pre-solved)
- packages/agent/src/lib/registry.ts: `readRegistry(registryFile?)`
- packages/agent/src/server/middleware/errors.ts: `outbound(...)`
- packages/agent/src/routes/sentry.ts (Plan 03): `sentryRoute` (to mount)
- packages/agent/src/server/app.ts: existing mounts `app.route('/api/projects', integrationsRoute)` etc. (~lines 191-192); bearer-auth middleware applies to all

Linear API (08-RESEARCH Finding 3):
- `POST https://api.linear.app/graphql`, `Content-Type: application/json`
- Auth: RAW key, NO Bearer prefix → `Authorization: ${process.env.LINEAR_API_KEY}`
- Query: `query GetIssue($id: String!) { issue(id: $id) { id identifier title url state { name type } assignee { name } } }`, variables `{ id: 'ACME-123' }` (human identifier accepted directly — A3)
- Not found → data.issue is null
- Rate limit → HTTP 400 with errors[0].extensions.code === 'RATELIMITED' (NOT 429)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Linear issue-ID detection (branch + log, deduped, capped 3)</name>
  <files>packages/agent/src/routes/linear.ts, packages/agent/src/routes/linear.test.ts</files>
  <read_first>
    - packages/agent/src/routes/integrations.ts (LINEAR_BRANCH_RE + runAllowedGit('branch') usage)
    - packages/agent/src/lib/git.ts (ARGV_BY_CMD: log = ['log','--oneline','-20'], branch = ['branch','--show-current'])
    - 08-RESEARCH.md §"Research Finding 4" + Pitfall 6 (20-commit cap built in)
    - 08-PATTERNS.md §"packages/agent/src/routes/linear.ts" (branch+log detection + /g flag note)
  </read_first>
  <behavior>
    - detectIssueIds extracts [A-Z]{2,}-\d+ matches from the current branch (e.g. 'donald/ACME-123-fix' → 'ACME-123')
    - detectIssueIds also extracts matches from `git log --oneline -20` output (multiple commit lines)
    - Branch + log matches are deduplicated (ACME-123 appearing in both yields one entry)
    - The deduped list is capped at 3 (D-08-07) preserving first-seen order (branch before log)
    - No matches → returns an empty array (panel renders empty/configure state, not an error)
  </behavior>
  <action>
    Author RED tests first (mock runAllowedGit to return fixture branch + log strings), then implement a `detectIssueIds(root): Promise<string[]>` helper in linear.ts. Use `LINEAR_BRANCH_RE = /[A-Z]{2,}-\d+/g` (note the `/g` flag for matchAll across multi-line log output — divergence from integrations.ts's non-global regex). Run `runAllowedGit('branch', root)` and `runAllowedGit('log', root)`; collect `[...branchOut.matchAll(re)]` then `[...logOut.matchAll(re)]`; dedupe via a Set preserving insertion order; cap at 3. The 20-commit log cap is already enforced by ARGV_BY_CMD (Pitfall 6) — do not pass extra flags.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test linear</automated>
  </verify>
  <acceptance_criteria>
    - linear.test.ts proves branch-only, log-only, and both-source detection with dedup
    - cap-at-3 enforced when >3 distinct IDs are present, branch IDs first
    - empty result when no IDs present
    - `pnpm --filter @agenticapps/dashboard-agent test linear` exits 0
  </acceptance_criteria>
  <done>Issue-ID detection from branch + log with dedup + cap-3 implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: linear/issues route — env-gate, GraphQL fetch, per-issue cache, last-good</name>
  <files>packages/agent/src/routes/linear.ts, packages/agent/src/routes/linear.test.ts</files>
  <read_first>
    - packages/agent/src/routes/integrations.ts (env-gate + project-lookup + Map cache + outbound)
    - 08-RESEARCH.md §"Research Finding 3" (GraphQL query, raw-key auth, 400-RATELIMITED) + §"Pattern 1" + Pitfall 7 (cache key projectId:issueId)
    - 08-PATTERNS.md §"packages/agent/src/routes/linear.ts" (copy-vs-change list)
  </read_first>
  <behavior>
    - LINEAR_API_KEY unset → 404 { ok:false, error:'not_configured', requestId } (LINEAR-03)
    - Unknown project id → 404 error:'project_not_found'
    - Token set: detect up to 3 issue IDs, fetch each via GraphQL, return LinearIssuesResponseSchema with issues[] (identifier/title/url/stateName/stateType/assigneeName) (LINEAR-01)
    - Auth header is the RAW key with NO 'Bearer ' prefix
    - Per-issue cache keyed `${projectId}:${issueId}`; a hit within 60s skips that issue's fetch (Pitfall 7 — no cross-project leakage)
    - Issue not found in Linear (data.issue null) → that entry omitted (not an error for the whole panel)
    - Upstream failure with prior last-good for an issue → that issue served stale-labeled; whole-panel staleReason set when all fail
    - Linear HTTP-400 + RATELIMITED → classified rate-limited (via classifyError); plain 400 → unreachable
    - INV-05: LINEAR_API_KEY never appears in any response body
  </behavior>
  <action>
    Author RED tests for every behavior above (mock global fetch returning GraphQL JSON; assert raw-key header has no 'Bearer'; assert cache key isolation across two project ids; assert the key string never appears in any response), then implement to GREEN. Build `linearRoute = new Hono<Env>()` with `linearRoute.get('/:id/linear/issues', ...)`. Copy from integrations.ts: env-gate (checking LINEAR_API_KEY → not_configured), project-lookup 404 guard, outbound return. Cache is a module Map keyed `${projectId}:${issueId}` with `TTL_MS = 60_000`. Call detectIssueIds (Task 1); for each id not in fresh cache, `POST https://api.linear.app/graphql` via fetchWithTimeout with headers `{ 'Content-Type':'application/json', Authorization: process.env.LINEAR_API_KEY }` (RAW key, no Bearer — A3/Finding 3) and body `{ query, variables:{ id } }`; map data.issue → LinearIssueSchema fields (stateName from state.name, stateType from state.type, assigneeName from assignee?.name ?? null); skip when data.issue is null. On throw/non-2xx per issue: classifyError(err, status, body) (already handles 400+RATELIMITED); serve that issue's lastGood if present else mark it failed; assemble LinearIssuesResponse with per-issue + top-level stale metadata. Wrap the final payload in `outbound(c, LinearIssuesResponseSchema.parse.bind(LinearIssuesResponseSchema), value)`. Export `evictLinearCacheProject(id)` that deletes all `${id}:*` keys. Logging (INV-05): status only, never the key, never raw body to SPA.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test linear</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent/src/routes/linear.ts` exports `linearRoute` and `evictLinearCacheProject`
    - Test: key unset → 404 not_configured; unknown id → 404 project_not_found
    - Test: Authorization header equals the raw key (assert it does NOT start with 'Bearer ')
    - Test: cache keyed projectId:issueId — same issueId across two projects does not leak (separate fetches)
    - Test: HTTP-400 RATELIMITED body → staleReason/category 'rate-limited'
    - Test: `JSON.stringify(responseBody).includes(process.env.LINEAR_API_KEY)` is false in every case (INV-05)
    - Route performs no project-filesystem writes (INV-01)
    - `pnpm --filter @agenticapps/dashboard-agent test linear` exits 0
  </acceptance_criteria>
  <done>linear/issues route implements env-gate, raw-key GraphQL fetch, per-(project,issue) cache, last-good, rate-limit classification, and key non-leakage, tested green.</done>
</task>

<task type="auto">
  <name>Task 3: Mount sentry + linear routes in app.ts</name>
  <files>packages/agent/src/server/app.ts</files>
  <read_first>
    - packages/agent/src/server/app.ts (lines ~174-197 route-mount block; lines ~161-172 bearer-auth middleware)
    - 08-PATTERNS.md §"packages/agent/src/server/app.ts (modify — add route mounts)"
  </read_first>
  <action>
    Add imports near the existing route imports: `import { sentryRoute } from '../routes/sentry.js'` and `import { linearRoute } from '../routes/linear.js'`. In the mount block, after the `integrationsRoute` mount, add `app.route('/api/projects', sentryRoute)` and `app.route('/api/projects', linearRoute)`, matching the exact existing mount style. Both inherit the existing bearer-auth + CORS middleware — add no new auth code (the routes sit behind the same middleware as integrations/secrets).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test && pnpm --filter @agenticapps/dashboard-agent typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "app.route('/api/projects', sentryRoute)" packages/agent/src/server/app.ts` returns 1
    - `grep -c "app.route('/api/projects', linearRoute)" packages/agent/src/server/app.ts` returns 1
    - both route imports present
    - `pnpm --filter @agenticapps/dashboard-agent test` green; `... typecheck` exits 0
  </acceptance_criteria>
  <done>Sentry and Linear routes mounted under bearer-auth; full agent suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→daemon route | bearer-auth'd; :id resolved against registry |
| daemon→api.linear.app | outbound egress carrying the raw API key + human issue identifier |
| upstream error→SPA response | sanitization boundary (D-08-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Information Disclosure | linear/issues response | mitigate | LINEAR_API_KEY never serialized; tested INV-05 substring assertion; only classified category in error metadata |
| T-08-20 | Information Disclosure | daemon logs | mitigate | agentError logs status only; no key / raw body interpolation |
| T-08-21 | Spoofing / SSRF | Linear endpoint URL | mitigate | host hardcoded to api.linear.app/graphql; only the human issue identifier is interpolated into the GraphQL variable, never the URL |
| T-08-22 | Elevation of Privilege | cross-project cache | mitigate | cache keyed `${projectId}:${issueId}` (Pitfall 7); :id resolved via readRegistry().find |
| T-08-23 | Information Disclosure (data boundary) | outbound call | accept (documented) | D-08-10 carve-out: opt-in via LINEAR_API_KEY; payload is the user's own key + issue id to Linear's own API; never project file contents |
| T-08-SC | Tampering | npm installs | accept | zero new packages; Node 22 fetch built-in |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-agent test` green (linear + full suite)
- `pnpm --filter @agenticapps/dashboard-agent typecheck` clean
- INV-05 key-substring assertion present and passing; raw-key (no Bearer) header asserted
</verification>

<success_criteria>
- Route 404s not_configured without key; returns ≤3 detected issues with key
- Branch+log detection deduped/capped; per-(project,issue) cache; last-good fallback
- Linear 400-RATELIMITED classified correctly; raw-key auth (no Bearer)
- Both routes mounted; key never in any response; full agent suite green
</success_criteria>

<output>
Create `.planning/phases/DASH-08-optional-integration-panels/08-05-SUMMARY.md` when done
</output>
