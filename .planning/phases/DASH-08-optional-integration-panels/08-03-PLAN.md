---
phase: 08-optional-integration-panels
plan: 03
type: tdd
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - packages/agent/src/routes/sentry.ts
  - packages/agent/src/routes/sentry.test.ts
autonomous: true
requirements: [SENTRY-01, SENTRY-02, SENTRY-03, INV-05, INV-01]
must_haves:
  truths:
    - "GET /api/projects/{id}/sentry/recent returns the top-5 unresolved issues when SENTRY_AUTH_TOKEN is set"
    - "Without SENTRY_AUTH_TOKEN the route returns 404 with a not_configured body"
    - "A 60s cache hit skips the upstream fetch; on upstream failure the last-good payload is served labeled stale"
    - "No Sentry token ever appears in any SPA-facing JSON response"
    - "Org/project slug resolves automatically from the project's DSN/.sentryclirc with an explicit env-var fallback"
  artifacts:
    - path: "packages/agent/src/routes/sentry.ts"
      provides: "sentryRoute Hono handler for /:id/sentry/recent + evictSentryCacheProject"
      exports: ["sentryRoute", "evictSentryCacheProject"]
  key_links:
    - from: "packages/agent/src/routes/sentry.ts"
      to: "sentry.io/api/0/organizations/{org}/issues"
      via: "fetchWithTimeout with Bearer token"
      pattern: "organizations/.*issues"
    - from: "packages/agent/src/routes/sentry.ts"
      to: "packages/agent/src/lib/outboundFetch.ts"
      via: "fetchWithTimeout + classifyError"
      pattern: "classifyError"
---

<objective>
Implement the env-gated Sentry data route: `GET /api/projects/{id}/sentry/recent`. Returns the top-5 recent unresolved issues for the project, resolving the Sentry org/project slug automatically from the project's DSN / `.sentryclirc` (cached separately at a longer TTL), querying the org-level issues endpoint, caching results 60s, retaining last-good on failure, and sanitizing all errors to fixed categories. Returns 404 `not_configured` when `SENTRY_AUTH_TOKEN` is unset.

Purpose: Delivers SENTRY-01/02/03 and the INV-05 token-safety guarantee at the daemon's first remote-service surface.
Output: sentry.ts route + its mock-fetch test. (Route MOUNTING into app.ts happens in Plan 08-05 to keep app.ts single-owner.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-08-optional-integration-panels/08-RESEARCH.md
@.planning/phases/DASH-08-optional-integration-panels/08-PATTERNS.md
@.planning/phases/DASH-08-optional-integration-panels/08-01-SUMMARY.md
@.planning/phases/DASH-08-optional-integration-panels/08-02-SUMMARY.md
@packages/agent/src/routes/integrations.ts

<interfaces>
<!-- Consumed contracts — build against these, no exploration needed. -->

From @agenticapps/dashboard-shared (Plan 01): `SentryRecentResponseSchema`, type `SentryRecentResponse`, `SentryIssueSchema`.

From packages/agent/src/lib/outboundFetch.ts (Plan 02): `fetchWithTimeout(url, init, timeoutMs=5000)`, `classifyError(err, status?, body?)`, `interface CacheEntry<T>`.

From the codebase:
- packages/agent/src/lib/registry.ts: `readRegistry(registryFile?)` → { projects: [{ id, root, ... }] }
- packages/agent/src/server/middleware/errors.ts: `outbound(c, Schema.parse.bind(Schema), value)` — ALWAYS use this, never bare c.json(Schema.parse(...))
- packages/agent/src/lib/projectMetadataScan.ts: `detectSentryDsnEnv(root)`, `parseSentryClirc(root)` (existence-only today — see action for the minimal INI line-parse needed)
- packages/agent/src/server/app.ts: `type Env` (Hono generics: registryFile + requestId on context)

Sentry endpoints (08-RESEARCH Findings 1 & 2):
- Slug resolution: `GET https://sentry.io/api/0/projects/` with `Authorization: Bearer <token>`; match each project's `id` (string) against the DSN numeric project id; read `slug` + `organization.slug`
- Issues: `GET https://sentry.io/api/0/organizations/{org_slug}/issues/?query=is:unresolved&sort=date&limit=5&project=<numeric_project_id>` with `Authorization: Bearer <token>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentry org/project slug resolution (DSN → API → env fallback)</name>
  <files>packages/agent/src/routes/sentry.ts, packages/agent/src/routes/sentry.test.ts</files>
  <read_first>
    - packages/agent/src/lib/projectMetadataScan.ts (parseSentryClirc lines ~164, detectSentryDsnEnv lines ~199 — current existence-only shape)
    - 08-RESEARCH.md §"Research Finding 1" (3-tier resolution priority + Pitfall 3 string-compare + Pitfall 5 separate slug cache) and §"Open Questions" 2 & 3
    - 08-PATTERNS.md §"packages/agent/src/routes/sentry.ts" (imports + slug cache note)
  </read_first>
  <behavior>
    - Given a DSN with numeric project id 42 and an API /projects/ list containing {id:'42', slug:'web', organization:{slug:'acme'}}, resolveSlugs returns {orgSlug:'acme', projectSlug:'web', projectId:'42'}
    - String comparison: DSN id parsed as number 42 compared as String(42)==='42' against API id '42' (Pitfall 3)
    - When .sentryclirc contains `org=acme` and `project=web` under [defaults], those win without any API call (tier 1)
    - When SENTRY_ORG_SLUG and SENTRY_PROJECT_SLUG env vars are set, they are used as the tier-3 escape hatch
    - When no DSN, no clirc, no env override resolves → resolution returns null and the route surfaces 'unreachable' (not a crash)
    - Resolved slug pair is cached with a 10-min TTL keyed by project id, separate from the 60s issues cache (Pitfall 5)
  </behavior>
  <action>
    Author RED tests first (mock global fetch for the /projects/ list; use fixture .sentryclirc + DSN strings). Implement a `resolveSentrySlugs(root, projectId)` helper inside sentry.ts (or a co-located non-exported function) with the 3-tier priority from Research Finding 1: (1) minimal `.sentryclirc` line-parse — accept only `[header]` lines and `key = value` lines, extract `org` and `project` from the `[defaults]` section, no includes/no code-exec; (2) parse the DSN numeric project id (last path segment), call `GET https://sentry.io/api/0/projects/` via fetchWithTimeout with `Authorization: Bearer ${process.env.SENTRY_AUTH_TOKEN}`, paginate with a max-pages guard (10×100), match `project.id === String(dsnNumericId)`, read `slug` + `organization.slug`; (3) `SENTRY_ORG_SLUG` + `SENTRY_PROJECT_SLUG` env vars. Cache the resolved `{orgSlug, projectSlug, projectId}` in a module-level Map keyed by projectId with a 10-min TTL (separate from the issues cache). On total resolution failure return null; document the explicit env-var fallback in the /help copy (handled in Plan 06). Handle Open-Question 2 (organization may be nested object or organizationId) by reading `organization.slug` if present, else falling back to the DSN-hostname org subdomain resolution path noted in Research.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test sentry</automated>
  </verify>
  <acceptance_criteria>
    - sentry.test.ts proves DSN-id→API-match resolution returns the correct {orgSlug, projectSlug}
    - String-vs-number id comparison is covered (a number-typed DSN id matches a string-typed API id)
    - `.sentryclirc` org/project tier-1 path resolves with zero fetch calls (assert fetch not called)
    - SENTRY_ORG_SLUG/SENTRY_PROJECT_SLUG tier-3 fallback path is covered
    - slug cache is keyed by projectId and not re-resolved within its 10-min TTL (fetch called once across two calls)
    - `pnpm --filter @agenticapps/dashboard-agent test sentry` exits 0
  </acceptance_criteria>
  <done>Slug resolution with the 3-tier priority + separate 10-min cache is implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sentry/recent route — env-gate, 60s cache, last-good fallback, token safety</name>
  <files>packages/agent/src/routes/sentry.ts, packages/agent/src/routes/sentry.test.ts</files>
  <read_first>
    - packages/agent/src/routes/integrations.ts (lines 36-61 env-gate + Map cache + outbound; lines ~104 evict export)
    - packages/agent/src/routes/read.ts (lines 35-43 project-lookup 404 guard)
    - 08-RESEARCH.md §"Pattern 1: Env-Gated Route" + §"Research Finding 5" control-flow + §"Research Finding 6" sanitization
    - 08-PATTERNS.md §"packages/agent/src/routes/sentry.ts" (copy-vs-change list)
  </read_first>
  <behavior>
    - SENTRY_AUTH_TOKEN unset → 404 with body { ok:false, error:'not_configured', requestId } (SENTRY-03)
    - Unknown project id → 404 with body error:'project_not_found'
    - Token set + cache miss → fetches issues, returns SentryRecentResponseSchema-shaped payload of ≤5 issues with title/level/count/lastSeen/permalink/shortId (SENTRY-01)
    - Second call within 60s → cache hit, upstream fetch NOT called again (SENTRY-02)
    - Cache miss + upstream 500/timeout WITH a prior last-good → serves last-good payload with stale:true, staleFrom ISO, staleReason category; never 404/503 (SENTRY-02, D-08-09)
    - Cache miss + upstream failure with NO prior last-good → 503 with sanitized error category, never the raw upstream body
    - INV-05: no SENTRY_AUTH_TOKEN substring appears anywhere in any response body across all the above cases
  </behavior>
  <action>
    Author RED tests for every behavior above (mock global fetch; assert response shapes + that fetch is/ isn't called per cache state; assert the token string never appears in JSON.stringify of any response), then implement to GREEN. Build `sentryRoute = new Hono<Env>()` with `sentryRoute.get('/:id/sentry/recent', ...)`. Copy verbatim from integrations.ts: the env-gate shape (but checking SENTRY_AUTH_TOKEN, returning `not_configured`), the `readRegistry().find(p.id===projectId)` + 404 `project_not_found` guard, the inline `Map` cache structure with `TTL_MS = 60_000` (not 5_000), and the `outbound(c, SentryRecentResponseSchema.parse.bind(SentryRecentResponseSchema), value)` return. Inside the fetch path call resolveSentrySlugs (Task 1), then `GET https://sentry.io/api/0/organizations/{orgSlug}/issues/?query=is:unresolved&sort=date&limit=5&project=${numericProjectId}` via fetchWithTimeout with the Bearer header, map the top-5 issue objects to SentryIssueSchema fields. On any throw/non-2xx: classifyError(err, status, body); if a prior cache entry has lastGood, serve `{...lastGood.value, stale:true, staleFrom, staleReason}` via outbound; else `c.json({ok:false, error:<category>, requestId}, 503)`. Retain lastGood on every success (CacheEntry shape from outboundFetch.ts). Export `evictSentryCacheProject(id)` to clear both the issues cache and the slug cache (mirrors integrations.ts evict export, to be wired into unregister in a follow-up; not blocking this plan). Logging (INV-05): on failure log status only via agentError — never the token, never the raw body to the SPA.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test sentry</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent/src/routes/sentry.ts` exports `sentryRoute` and `evictSentryCacheProject`
    - Test: token unset → 404 not_configured; unknown id → 404 project_not_found
    - Test: cache hit within 60s does not call fetch a second time
    - Test: upstream failure with prior last-good → 200 with stale:true + staleFrom + staleReason; without last-good → 503 sanitized category
    - Test: `JSON.stringify(responseBody).includes(process.env.SENTRY_AUTH_TOKEN)` is false in every case (INV-05)
    - Route reads only API responses + registry; performs no project-filesystem writes (INV-01)
    - `pnpm --filter @agenticapps/dashboard-agent test sentry` exits 0
  </acceptance_criteria>
  <done>sentry/recent route implements env-gate, 60s cache, last-good stale fallback, sanitized errors, and token non-leakage, all tested green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→daemon route | bearer-auth'd (existing middleware); :id resolved against registry only |
| daemon→sentry.io | outbound egress carrying the user's own Bearer token + numeric project id |
| upstream error→SPA response | sanitization boundary (D-08-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | sentry/recent response | mitigate | Token never serialized; tested INV-05 substring assertion; only classified category in error responses (D-08-11) |
| T-08-10 | Information Disclosure | daemon logs | mitigate | agentError logs status only; no token / raw body interpolation |
| T-08-11 | Spoofing / SSRF | Sentry endpoint URL | mitigate | host hardcoded to sentry.io; only org slug + numeric project id interpolated, both daemon-derived; no user-supplied URL |
| T-08-12 | Elevation of Privilege | cross-project cache | mitigate | issues + slug caches keyed by projectId; :id resolved via readRegistry().find guard |
| T-08-13 | Information Disclosure (data boundary) | first outbound call | accept (documented) | D-08-10 carve-out: opt-in via SENTRY_AUTH_TOKEN; payload is the user's own token + project id to Sentry's own API; never project file contents. Auditable here. |
| T-08-SC | Tampering | npm installs | accept | zero new packages; Node 22 fetch built-in |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-agent test sentry` green
- `pnpm --filter @agenticapps/dashboard-agent typecheck` clean
- INV-05 substring assertion present and passing in sentry.test.ts
</verification>

<success_criteria>
- Route 404s not_configured without token; returns ≤5 issues with token
- 60s cache + last-good stale fallback + sanitized categories
- Token never in any response; endpoint host hardcoded; caches keyed by projectId
- Slug resolution 3-tier with separate 10-min cache
</success_criteria>

<output>
Create `.planning/phases/DASH-08-optional-integration-panels/08-03-SUMMARY.md` when done
</output>
