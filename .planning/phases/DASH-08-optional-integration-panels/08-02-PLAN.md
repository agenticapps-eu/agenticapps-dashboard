---
phase: 08-optional-integration-panels
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/agent/src/constants.ts
  - packages/agent/src/lib/outboundFetch.ts
  - packages/agent/src/lib/outboundFetch.test.ts
  - packages/agent/src/lib/envFile.ts
  - packages/agent/src/lib/envFile.test.ts
  - packages/agent/src/lib/auth.ts
autonomous: true
requirements: [INV-02, INV-05, INFI-01, INFI-02]
must_haves:
  truths:
    - "Outbound fetch aborts at ~5s with no retry and never throws past the route boundary"
    - "Upstream errors are classified to exactly unreachable / unauthorized / rate-limited before leaving the daemon"
    - "Last-good response is retained beyond the 60s TTL and served on subsequent failure"
    - "env.json loads at boot and fills only env vars that are unset in process.env (process.env wins)"
    - "env.json is written and read at mode 0600 with symlink rejection, reusing auth.ts primitives"
  artifacts:
    - path: "packages/agent/src/lib/outboundFetch.ts"
      provides: "fetchWithTimeout, classifyError, CacheEntry<T> with lastGood"
      exports: ["fetchWithTimeout", "classifyError", "CacheEntry"]
    - path: "packages/agent/src/lib/envFile.ts"
      provides: "loadEnvFile, writeEnvFile, readEnvFile / listEnvKeys"
      exports: ["loadEnvFile", "writeEnvFile", "readEnvFile"]
    - path: "packages/agent/src/constants.ts"
      provides: "ENV_FILE constant"
      contains: "ENV_FILE"
  key_links:
    - from: "packages/agent/src/lib/envFile.ts"
      to: "packages/agent/src/lib/auth.ts"
      via: "import assertSecurePermissions + ensureConfigDir"
      pattern: "assertSecurePermissions"
    - from: "packages/agent/src/lib/envFile.ts"
      to: "packages/agent/src/lib/atomicWrite.ts"
      via: "atomicWriteFile at 0o600"
      pattern: "atomicWriteFile.*0o600"
---

<objective>
Build the two daemon primitives every Phase 8 route and CLI command needs: `outboundFetch.ts` (the first outbound-HTTP helper in the codebase — 5s timeout, no retry, last-good retention, sanitized error classification) and `envFile.ts` (the env.json store mirroring auth.json's 0600 + symlink-reject + atomic-write discipline). Add the ENV_FILE constant and export `ensureConfigDir` from auth.ts for reuse.

Purpose: These libs encapsulate the two highest-risk new behaviors — outbound network egress (INV-05 token safety, D-08-08/09/11) and secrets-on-disk (INV-02 no native dep, INFI-01/02). Centralizing them means the Wave 3 routes and CLI consume tested helpers instead of re-implementing.
Output: outboundFetch.ts + envFile.ts + their tests, the ENV_FILE constant, and an exported ensureConfigDir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-08-optional-integration-panels/08-RESEARCH.md
@.planning/phases/DASH-08-optional-integration-panels/08-PATTERNS.md
@.planning/phases/DASH-08-optional-integration-panels/08-01-SUMMARY.md

<interfaces>
<!-- Primitives to REUSE (do not re-implement) — from auth.ts / atomicWrite.ts / stateCorruption.ts -->

packages/agent/src/lib/auth.ts:
- `assertSecurePermissions(filePath)` — uses lstatSync to reject symlinks + checks 0600/0700; CALL IT, don't reimplement
- `ensureConfigDir(dir)` — currently PRIVATE; Task 1 exports it (creates CONFIG_DIR at 0700)

packages/agent/src/lib/atomicWrite.ts:
- `atomicWriteFile(path, body, mode)` — tmp+rename, O_EXCL/O_NOFOLLOW, fsync; pass 0o600

packages/agent/src/lib/stateCorruption.ts:
- `parseOrCorrupt(Schema, parsedJson, label)` — throws StateCorruptionError on mismatch

packages/agent/src/lib/logging.ts:
- `agentError(msg)` / `agentLog(msg)` — NEVER interpolate a token value into these

From @agenticapps/dashboard-shared (Wave 1): `EnvFileSchema`, `AllowedEnvKeySchema`, `ALLOWED_ENV_KEYS`, type `EnvFile`

Node 22 globals (INV-02 — no import, no new dep): `fetch`, `AbortController`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENV_FILE constant + export ensureConfigDir</name>
  <files>packages/agent/src/constants.ts, packages/agent/src/lib/auth.ts</files>
  <read_first>
    - packages/agent/src/constants.ts (lines 9-13: CONFIG_DIR / AUTH_FILE / REGISTRY_FILE style)
    - packages/agent/src/lib/auth.ts (locate ensureConfigDir; it is called by ensureAuthFile and writeAuthFile)
    - 08-PATTERNS.md §"packages/agent/src/constants.ts" and the ensureConfigDir note in §"...envFile.ts"
  </read_first>
  <action>
    In constants.ts add `export const ENV_FILE = join(CONFIG_DIR, 'env.json')` immediately after `AUTH_FILE`, matching the exact existing style. In auth.ts, change `ensureConfigDir` from private to `export` (it already exists and is called internally twice) so envFile.ts can reuse the same 0700 dir-creation logic rather than duplicating it. Do not alter ensureConfigDir's behavior.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ENV_FILE = join(CONFIG_DIR, 'env.json')" packages/agent/src/constants.ts` returns 1
    - `grep -c "export function ensureConfigDir\|export const ensureConfigDir" packages/agent/src/lib/auth.ts` returns 1
    - `pnpm --filter @agenticapps/dashboard-agent typecheck` exits 0 (no broken internal callers)
  </acceptance_criteria>
  <done>ENV_FILE defined; ensureConfigDir exported with unchanged behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: outboundFetch.ts — timeout, last-good cache, error classification</name>
  <files>packages/agent/src/lib/outboundFetch.ts, packages/agent/src/lib/outboundFetch.test.ts</files>
  <read_first>
    - packages/agent/src/routes/integrations.ts (lines 36-61: CacheEntry shape + Map cache to extend)
    - packages/agent/src/lib/atomicWrite.ts (try/finally cleanup discipline analog)
    - 08-PATTERNS.md §"packages/agent/src/lib/outboundFetch.ts" (fetchWithTimeout + classifyError excerpts)
    - 08-RESEARCH.md §"Research Finding 5" (control flow) and §"Research Finding 6" (error→category table)
  </read_first>
  <behavior>
    - fetchWithTimeout aborts the request after timeoutMs (default 5000) via AbortController; the AbortError surfaces (D-08-08, no retry)
    - fetchWithTimeout clears its timer in a finally block even when fetch throws
    - classifyError(AbortError) → 'unreachable'
    - classifyError(TypeError 'fetch failed') → 'unreachable' (DNS/ECONNREFUSED)
    - classifyError(_, 401) and (_, 403) → 'unauthorized'
    - classifyError(_, 429) → 'rate-limited' (Sentry standard)
    - classifyError(_, 400, body with errors[0].extensions.code==='RATELIMITED') → 'rate-limited' (Linear non-standard, Pitfall 1)
    - classifyError(_, 404) and (_, 500) and any other non-2xx → 'unreachable'
    - CacheEntry<T> retains a lastGood sub-entry; a helper that, given a prior entry with lastGood, produces a stale-labeled value (stale:true, staleFrom ISO, staleReason category) — proving D-08-09 last-good survives TTL expiry
  </behavior>
  <action>
    Author RED tests in outboundFetch.test.ts covering every behavior above (mock global fetch with vi.stubGlobal / a fake timers setup for the abort path), then implement to GREEN. Export `fetchWithTimeout(url, init, timeoutMs=5000)` using the Node 22 global `AbortController` + `fetch` (no import, INV-02) with `clearTimeout` in `finally`. Export `type OutboundErrorCategory = 'unreachable' | 'unauthorized' | 'rate-limited'` and `classifyError(err, status?, body?)` implementing the full mapping table (check err.name==='AbortError' and err instanceof TypeError first, then status 401/403→unauthorized, 429→rate-limited, 400+isLinearRateLimited(body)→rate-limited, else unreachable). Export `interface CacheEntry<T> { value: T; cachedAtMs: number; lastGood?: { value: T; cachedAtMs: number } }`. Do NOT add retry logic (D-08-08). Logging rule (INV-05): if this helper logs, log status only — never a token value; raw upstream body stays available to callers for daemon-side logging, never returned to the SPA.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test outboundFetch</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent/src/lib/outboundFetch.ts` exports `fetchWithTimeout`, `classifyError`, and the `CacheEntry` type
    - Test asserts `classifyError({name:'AbortError'})==='unreachable'`, `classifyError(null,401)==='unauthorized'`, `classifyError(null,429)==='rate-limited'`, `classifyError(null,400,{errors:[{extensions:{code:'RATELIMITED'}}]})==='rate-limited'`
    - Test proves fetchWithTimeout rejects/aborts at the timeout boundary with no retry attempt (fetch called exactly once)
    - `grep -c "fetch(" packages/agent/src/lib/outboundFetch.ts` shows global fetch used (no new dependency import)
    - `pnpm --filter @agenticapps/dashboard-agent test outboundFetch` exits 0
  </acceptance_criteria>
  <done>outboundFetch helpers exist and are tested for timeout, no-retry, classification, and last-good shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: envFile.ts — boot load (process.env wins) + 0600 write</name>
  <files>packages/agent/src/lib/envFile.ts, packages/agent/src/lib/envFile.test.ts</files>
  <read_first>
    - packages/agent/src/lib/auth.ts (readAuthFile / writeAuthFile / ensureAuthFile + assertSecurePermissions as the exact template)
    - packages/agent/src/lib/atomicWrite.ts (atomicWriteFile signature)
    - packages/agent/src/lib/stateCorruption.ts (parseOrCorrupt)
    - 08-PATTERNS.md §"packages/agent/src/lib/envFile.ts" (loadEnvFile + writeEnvFile excerpts)
    - 08-RESEARCH.md §"Research Finding 7" + Pitfall 4 (corrupt env.json must not crash boot)
  </read_first>
  <behavior>
    - loadEnvFile() with no env.json present returns silently (env.json is optional — INV-03)
    - loadEnvFile() sets process.env[KEY] only for keys NOT already present in process.env (D-08-12: process.env wins → INFI-01)
    - loadEnvFile() leaves a process.env key untouched when it is already set (the infisical run guarantee)
    - loadEnvFile() calls assertSecurePermissions before reading (symlink/loose-mode rejection)
    - writeEnvFile() validates against EnvFileSchema then atomicWriteFile at mode 0o600 (INFI-02)
    - writeEnvFile() / EnvFileSchema rejects an unknown key in vars (allow-list, D-08-13)
    - readEnvFile() returns the parsed EnvFile or null when absent (for env list/status)
  </behavior>
  <action>
    Author RED tests in envFile.test.ts using a tmp dir for the env.json path (pass an explicit filePath override; default is ENV_FILE) and a save/restore of process.env per test, covering every behavior above, then implement to GREEN. Export `loadEnvFile(filePath = ENV_FILE): void` — early-return if !existsSync; call `assertSecurePermissions(filePath)`; read + `parseOrCorrupt(EnvFileSchema, JSON.parse(raw), 'env.json')`; for each [key,value] in data.vars set `process.env[key] = value` ONLY if `!(key in process.env)` (D-08-12). Export `writeEnvFile(data: EnvFile, filePath = ENV_FILE)` — `EnvFileSchema.parse(data)`, `ensureConfigDir(dirname(filePath))` (reused from auth.ts), `atomicWriteFile(filePath, JSON.stringify(validated, null, 2), 0o600)`. Export `readEnvFile(filePath = ENV_FILE): EnvFile | null` for the CLI list command. Reuse auth.ts/atomicWrite.ts/stateCorruption.ts primitives — do NOT reimplement lstat checks or atomic writes (08-RESEARCH §"Don't Hand-Roll").
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test envFile</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent/src/lib/envFile.ts` exports `loadEnvFile`, `writeEnvFile`, `readEnvFile`
    - Test: with `process.env.SENTRY_AUTH_TOKEN='from-process'` and env.json setting it to 'from-file', after loadEnvFile the value remains 'from-process' (D-08-12 / INFI-01)
    - Test: with the key unset, loadEnvFile populates it from env.json
    - Test: writeEnvFile produces a file whose mode is 0600 (assert via fs.statSync mode & 0o777 === 0o600)
    - `grep -c "assertSecurePermissions" packages/agent/src/lib/envFile.ts` returns ≥ 1 (reused, not reimplemented)
    - `grep -v '^[[:space:]]*//' packages/agent/src/lib/envFile.ts | grep -c "lstatSync"` returns 0 (symlink check delegated to auth.ts, not duplicated)
    - `pnpm --filter @agenticapps/dashboard-agent test envFile` exits 0
  </acceptance_criteria>
  <done>envFile load/write/read implemented with process.env-wins merge and 0600 atomic writes, tested green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon→sentry.io / api.linear.app | First outbound egress; carries the user's own token only |
| env.json on disk→daemon process | Plaintext-at-0600 secrets file; read at boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | outboundFetch error path | mitigate | classifyError collapses every upstream error to one of 3 fixed categories; raw body never returned by the helper to a route's SPA response (D-08-11/INV-05) |
| T-08-05 | Information Disclosure | daemon logs | mitigate | logging rule: no token value interpolated into agentError/agentLog; only status codes logged |
| T-08-06 | Tampering | env.json symlink swap | mitigate | loadEnvFile calls assertSecurePermissions (lstatSync rejects symlinks) before any read (reused from auth.ts) |
| T-08-07 | Tampering | env.json partial write / race | mitigate | writeEnvFile uses atomicWriteFile (tmp+rename) at 0o600 — no partial-write exposure |
| T-08-08 | Denial of Service | corrupt env.json crashing boot | mitigate | Plan 08-04 wraps loadEnvFile in try/catch at boot; this plan keeps parseOrCorrupt throwing so the caller can isolate it (Pitfall 4) |
| T-08-SC | Tampering | npm installs | accept | zero new packages; Node 22 fetch/AbortController are built-ins (08-RESEARCH §"Package Legitimacy Audit") |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-agent test` green for outboundFetch + envFile suites
- `pnpm --filter @agenticapps/dashboard-agent typecheck` clean
- No new dependency in packages/agent/package.json (INV-02)
</verification>

<success_criteria>
- fetchWithTimeout aborts at 5s, no retry; classifyError covers all 9 mapping rows incl. Linear 400-RATELIMITED
- CacheEntry carries lastGood for D-08-09
- loadEnvFile honors process.env-wins (INFI-01); writeEnvFile is 0600 + allow-list validated (INFI-02/D-08-13)
- Symlink + atomic-write primitives reused from auth.ts/atomicWrite.ts, not duplicated
- Agent tests green
</success_criteria>

<output>
Create `.planning/phases/DASH-08-optional-integration-panels/08-02-SUMMARY.md` when done
</output>
