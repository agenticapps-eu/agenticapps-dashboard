---
phase: 08-optional-integration-panels
plan: 06
type: tdd
wave: 4
depends_on: ["08-01", "08-03", "08-05"]
files_modified:
  - packages/spa/src/lib/projectQueries.ts
  - packages/spa/src/components/panels/SentryPanel.tsx
  - packages/spa/src/components/panels/SentryPanel.test.tsx
  - packages/spa/src/components/panels/LinearPanel.tsx
  - packages/spa/src/components/panels/LinearPanel.test.tsx
  - packages/spa/src/components/SingleProjectView.tsx
autonomous: true
requirements: [SENTRY-01, SENTRY-02, SENTRY-03, LINEAR-01, LINEAR-02, LINEAR-03, INV-03, INV-04]
must_haves:
  truths:
    - "With no token configured both panels render a static 'Configure {ENV_VAR} to enable' empty state and the rest of the dashboard is unaffected"
    - "With data, the Sentry panel lists up to 5 issues each linking out to its Sentry URL"
    - "With data, the Linear panel lists up to 3 issues with title/status/assignee each linking out to its Linear URL"
    - "On stale data, the panel shows 'unreachable — using cached data from {time}' instead of blanking or crashing"
    - "Both panels are rendered in the single-project Health column"
    - "The pre-existing static Linear surface (IntegrationsHealth row, Phase 5) stays API-free — it never imports/calls useLinearIssues or the /linear/issues route (LINEAR-02 clause b / D-08-06)"
    - "Both new SPA panels validate daemon payloads against the shared SentryRecentResponseSchema / LinearIssuesResponseSchema (INV-04) and surface InlineDrift/schema_drift on mismatch"
  artifacts:
    - path: "packages/spa/src/components/panels/SentryPanel.tsx"
      provides: "SentryPanel — 4-state render + issue list + link-out + stale banner"
      exports: ["SentryPanel"]
    - path: "packages/spa/src/components/panels/LinearPanel.tsx"
      provides: "LinearPanel — 4-state render + issue list + link-out + stale banner"
      exports: ["LinearPanel"]
    - path: "packages/spa/src/lib/projectQueries.ts"
      provides: "useSentryRecent + useLinearIssues hooks"
      exports: ["useSentryRecent", "useLinearIssues"]
  key_links:
    - from: "packages/spa/src/lib/projectQueries.ts"
      to: "/api/projects/{id}/sentry/recent + /linear/issues"
      via: "apiFetch + parseOrDrift against shared schema"
      pattern: "sentry/recent|linear/issues"
    - from: "packages/spa/src/components/SingleProjectView.tsx"
      to: "SentryPanel + LinearPanel"
      via: "rendered in the Health column"
      pattern: "<SentryPanel|<LinearPanel"
---

<objective>
Build the two SPA panels (SentryPanel, LinearPanel) and their TanStack Query hooks, then render them in the single-project Health column. Panels mirror the existing 4-state pattern (schema_drift → InlineDrift; loading; unreachable/no-data; happy path) via PanelContainer, show the SENTRY-02 verbatim cached-data fallback on stale, render a static "Configure {ENV_VAR} to enable" empty state when not configured (T-05-05-Static-Copy-Trust), and link each issue out to its vendor URL.

Purpose: Delivers the SPA half of SENTRY-01/02/03 and LINEAR-01/03, validates the shared schemas at the browser boundary (INV-04), and proves INV-03 (dashboard fully functional with zero integration env vars). Also asserts the LINEAR-02 clause-(b) lock: the pre-existing static Linear surface (IntegrationsHealth) stays API-free (D-08-06).
Output: useSentryRecent + useLinearIssues hooks, SentryPanel + LinearPanel + their RTL tests, and the SingleProjectView wiring. (This is the only frontend-touching plan; the 08-IMPECCABLE.md ≥80 artifact is produced at verify time, not here.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-08-optional-integration-panels/08-RESEARCH.md
@.planning/phases/DASH-08-optional-integration-panels/08-PATTERNS.md
@.planning/phases/DASH-08-optional-integration-panels/08-01-SUMMARY.md
@.planning/phases/DASH-08-optional-integration-panels/08-03-SUMMARY.md
@.planning/phases/DASH-08-optional-integration-panels/08-05-SUMMARY.md
@packages/spa/src/components/panels/ObservabilityHealth.tsx
@packages/spa/src/components/panels/IntegrationsHealth.tsx
@packages/spa/src/components/panels/PanelContainer.tsx
@packages/spa/src/lib/projectQueries.ts

<interfaces>
<!-- Consumed contracts — build against these, no exploration needed. -->

From @agenticapps/dashboard-shared (Plan 01): `SentryRecentResponseSchema`, type `SentryRecentResponse`, `LinearIssuesResponseSchema`, type `LinearIssuesResponse`.

From packages/spa/src/lib/api.ts: `apiFetch(path, Schema)` → `{ ok:true, data } | { ok:false, drift:{ path } }` (bearer wrapper + parseOrDrift). On `!ok` throw `new Error('schema_drift:' + drift.path)`.

projectQueries.ts conventions (lines 57-99): `SKILLS_TTL_MS = 60_000`; hook shape = useQuery({ queryKey:['name', id] as const, queryFn, enabled: id !== null, staleTime, refetchInterval, refetchIntervalInBackground:false }).

PanelContainer props (packages/spa/src/components/panels/PanelContainer.tsx): `panelId`, `title`, `unreachable?`, `stale?` (renders a 'Stale' pill), `defaultCollapsed?`.

4-state guard block + InlineDrift usage: copy verbatim from ObservabilityHealth.tsx (schema_drift → InlineDrift; isLoading → Loading…; error||!data → unreachable). Static empty-state copy: IntegrationsHealth.tsx "not-detected" JSX literal pattern.

IntegrationsHealth.tsx (Phase 5, HEALTH-05) — the pre-existing static Linear surface. Consumes ONLY `useIntegrations` (status detection); has no `<a href>` anchors and makes no Sentry/Linear data calls (header comment: `T-05-05-No-Read-More-Link`). This is the API-free Linear surface locked by LINEAR-02 clause (b) / D-08-06 — leave it untouched.

SingleProjectView.tsx Health column (lines ~73-76) renders SkillHealth / ObservabilityHealth / SecretsHealth / IntegrationsHealth — add the two new panels there alongside, without modifying the existing IntegrationsHealth render or its props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSentryRecent + useLinearIssues query hooks</name>
  <files>packages/spa/src/lib/projectQueries.ts</files>
  <read_first>
    - packages/spa/src/lib/projectQueries.ts (lines 57-99: useIntegrations + SKILLS_TTL_MS + hook invariants)
    - 08-PATTERNS.md §"packages/spa/src/lib/projectQueries.ts" (useSentryRecent + useLinearIssues excerpts + the list-endpoint note)
  </read_first>
  <behavior>
    - useSentryRecent(id) queries /api/projects/{id}/sentry/recent, parses against SentryRecentResponseSchema, throws schema_drift:{path} on drift
    - useLinearIssues(id) queries /api/projects/{id}/linear/issues, parses against LinearIssuesResponseSchema, throws schema_drift on drift
    - both hooks: enabled only when id !== null; queryKey includes id (cross-project cache safety); refetchIntervalInBackground:false; 60s staleTime/refetchInterval
  </behavior>
  <action>
    Add imports for `SentryRecentResponseSchema`, `type SentryRecentResponse`, `LinearIssuesResponseSchema`, `type LinearIssuesResponse` from '@agenticapps/dashboard-shared' to the existing import block. Add `useSentryRecent(id)` and `useLinearIssues(id)` modeled exactly on `useIntegrations` (lines 57-99): queryKey `['sentry-recent', id] as const` / `['linear-issues', id] as const`; queryFn calls `apiFetch` against the matching schema and throws `new Error('schema_drift:' + result.drift.path)` on `!result.ok`; `enabled: id !== null`; `staleTime`/`refetchInterval` = SKILLS_TTL_MS (60_000); `refetchIntervalInBackground: false`. Note the Linear endpoint is the daemon-aggregated LIST route `/linear/issues` (Plan 05), not per-issue — confirmed by the architecture diagram. If RTL tests need a queryClient wrapper, follow the existing projectQueries test harness.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test projectQueries</automated>
  </verify>
  <acceptance_criteria>
    - `packages/spa/src/lib/projectQueries.ts` exports `useSentryRecent` and `useLinearIssues`
    - both hooks include `id` in queryKey and set `refetchIntervalInBackground: false`
    - both hooks parse against the shared schema and throw `schema_drift:` on parse failure (INV-04 at the browser boundary)
    - `grep -c "sentry/recent" packages/spa/src/lib/projectQueries.ts` ≥ 1; `grep -c "linear/issues" packages/spa/src/lib/projectQueries.ts` ≥ 1
    - `pnpm --filter @agenticapps/dashboard-spa test projectQueries` exits 0 (or the spa test suite if no granular filter)
  </acceptance_criteria>
  <done>Both query hooks exist, schema-validating and per-project-keyed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SentryPanel — 4-state + issue list + link-out + stale banner</name>
  <files>packages/spa/src/components/panels/SentryPanel.tsx, packages/spa/src/components/panels/SentryPanel.test.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/ObservabilityHealth.tsx (4-state guard block to copy verbatim)
    - packages/spa/src/components/panels/IntegrationsHealth.tsx (static "configure to enable" JSX literal pattern)
    - packages/spa/src/components/panels/PanelContainer.tsx (stale pill + props)
    - 08-PATTERNS.md §"packages/spa/src/components/panels/SentryPanel.tsx" (skeleton + link-out + stale + empty-state excerpts)
  </read_first>
  <behavior>
    - schema_drift error → renders InlineDrift (copied guard)
    - isLoading → Loading… in PanelContainer
    - error or no data → PanelContainer unreachable
    - data with 0 issues / not-configured → static JSX: "Set SENTRY_AUTH_TOKEN to enable the Sentry panel." with a link to /help (T-05-05 — copy is a literal, never daemon-supplied), defaultCollapsed
    - data with issues → up to 5 rows each: title, level badge, event count (Number(count).toLocaleString()), last-seen relative time, and a link-out to issue.permalink (shortId as link text, ExternalLink icon, rel="noopener noreferrer")
    - data.stale === true → PanelContainer stale pill + a banner "Sentry API unreachable — using cached data from {staleFrom}" (SENTRY-02 verbatim string)
  </behavior>
  <action>
    Author RED RTL tests (render with a mocked useSentryRecent via the existing panel test harness / queryClient wrapper) for: empty/not-configured static copy, happy-path issue list with link hrefs === permalink, and stale banner text. Then implement SentryPanel({projectId}). Copy the 4-state guard block verbatim from ObservabilityHealth.tsx (drift→InlineDrift, loading, unreachable). Render via PanelContainer with `panelId='sentry-panel'`, `title='Sentry'`, `stale={data.stale}`. Empty/not-configured branch: a STATIC JSX literal (no interpolation of any query.data string) telling the user to set SENTRY_AUTH_TOKEN with a /help link, defaultCollapsed. Happy path: map data.issues (already ≤5) to rows with title, a level badge, `Number(issue.count).toLocaleString()`, relative lastSeen, and an `<a href={issue.permalink} target="_blank" rel="noopener noreferrer">` link-out with the ExternalLink icon (D-08-04). When data.staleFrom present, render the verbatim "Sentry API unreachable — using cached data from {time}" banner. Only issue fields (title/level/count/lastSeen/permalink/shortId) come from the daemon (React-escaped); the configure copy stays a literal.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test SentryPanel</automated>
  </verify>
  <acceptance_criteria>
    - `packages/spa/src/components/panels/SentryPanel.tsx` exports `SentryPanel`
    - Test: with not-configured/empty data, the static "SENTRY_AUTH_TOKEN" configure copy renders (and is not sourced from query.data)
    - Test: with issues, exactly the issue rows render and each link href equals issue.permalink with target=_blank + rel includes noopener
    - Test: stale data renders the verbatim "Sentry API unreachable — using cached data from" banner text
    - `grep -c "schema_drift:" packages/spa/src/components/panels/SentryPanel.tsx` ≥ 1 (drift guard copied)
    - `pnpm --filter @agenticapps/dashboard-spa test SentryPanel` exits 0
  </acceptance_criteria>
  <done>SentryPanel renders all four states, links out, and shows the verbatim stale banner; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LinearPanel + wire both panels into SingleProjectView (static Linear surface stays API-free)</name>
  <files>packages/spa/src/components/panels/LinearPanel.tsx, packages/spa/src/components/panels/LinearPanel.test.tsx, packages/spa/src/components/SingleProjectView.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/ObservabilityHealth.tsx (4-state guard block)
    - packages/spa/src/components/panels/IntegrationsHealth.tsx (the Phase 5 static Linear surface — uses useIntegrations only, no useLinearIssues, no /linear/issues; must remain unchanged per LINEAR-02 clause b / D-08-06)
    - packages/spa/src/components/SingleProjectView.tsx (Health column render block ~lines 69-76: SkillHealth / ObservabilityHealth / SecretsHealth / IntegrationsHealth)
    - 08-PATTERNS.md §"packages/spa/src/components/panels/LinearPanel.tsx"
  </read_first>
  <behavior>
    - LinearPanel: same 4-state guard as SentryPanel (drift / loading / unreachable / happy)
    - not-configured/empty → static "Set LINEAR_API_KEY to enable the Linear panel." with /help link, defaultCollapsed
    - data with issues → up to 3 rows: identifier, title, state (stateName), assignee (assigneeName ?? 'Unassigned'), link-out to issue.url
    - data.stale → stale pill + "Linear API unreachable — using cached data from {time}" banner
    - SingleProjectView renders <SentryPanel> and <LinearPanel> in the Health column, alongside (not replacing) the existing IntegrationsHealth panel
    - The new LinearPanel (and its useLinearIssues hook) is the ONLY consumer of /linear/issues; the pre-existing IntegrationsHealth Linear row stays API-free — it neither imports useLinearIssues nor references the /linear/issues route (LINEAR-02 clause b / D-08-06)
  </behavior>
  <action>
    Author RED RTL tests for LinearPanel (empty configure copy, happy-path 3-issue list with hrefs === issue.url, stale banner), then implement LinearPanel({projectId}) mirroring SentryPanel: same copied 4-state guard, PanelContainer `panelId='linear-panel'` / `title='Linear'` / `stale={data.stale}`; static "LINEAR_API_KEY" configure copy with /help link on empty/not-configured (defaultCollapsed, literal copy per T-05-05); happy path maps data.issues (≤3) to rows with identifier, title, stateName, `assigneeName ?? 'Unassigned'`, and an `<a href={issue.url} target="_blank" rel="noopener noreferrer">` link-out with ExternalLink; stale banner "Linear API unreachable — using cached data from {time}" when staleFrom present. Then in SingleProjectView.tsx add `<SentryPanel projectId={projectId} />` and `<LinearPanel projectId={projectId} />` into the Health column section (near ObservabilityHealth / IntegrationsHealth), with their imports — do NOT modify the existing `<IntegrationsHealth projectId={projectId} />` render or touch IntegrationsHealth.tsx (its Linear row must stay API-free per D-08-06). Per memory `feedback_sidebar_section_architecture` and D-08-03/07 these are standalone panels alongside the existing health panels.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `packages/spa/src/components/panels/LinearPanel.tsx` exports `LinearPanel`
    - Test: not-configured renders the static "LINEAR_API_KEY" configure copy
    - Test: with issues, ≤3 rows render and each link href equals issue.url with target=_blank + rel noopener; unassigned shows 'Unassigned'
    - Test: stale renders the verbatim "Linear API unreachable — using cached data from" banner
    - `grep -c "<SentryPanel" packages/spa/src/components/SingleProjectView.tsx` returns 1; `grep -c "<LinearPanel" packages/spa/src/components/SingleProjectView.tsx` returns 1
    - LINEAR-02 clause (b) — static Linear surface stays API-free: `grep -c "useLinearIssues\|linear/issues" packages/spa/src/components/panels/IntegrationsHealth.tsx` returns 0 (the Phase 5 IntegrationsHealth Linear row is unchanged and never imports useLinearIssues or calls the /linear/issues route; D-08-06)
    - `git diff --quiet packages/spa/src/components/panels/IntegrationsHealth.tsx` exits 0 (IntegrationsHealth.tsx is not modified by this plan)
    - `pnpm --filter @agenticapps/dashboard-spa test` exits 0
    - `pnpm lint` exits 0 (CI blocks on eslint errors; SingleProjectView.tsx is the riskiest lint surface — per memory `feedback_run_lint_before_ship`)
  </acceptance_criteria>
  <done>LinearPanel renders all states + links out; both panels live in the single-project Health column; the Phase 5 static Linear surface (IntegrationsHealth) is unchanged and stays API-free; spa suite + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon response→panel render | only schema-validated daemon fields rendered; configure copy is a static literal |
| panel→/help link | static internal route; no daemon-supplied URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-24 | Tampering | configure-to-enable copy | mitigate | T-05-05-Static-Copy-Trust — empty-state copy is a JSX literal, never interpolated from query.data; tested |
| T-08-25 | Information Disclosure | rendered issue fields | accept | only non-secret issue metadata (title/level/count/url/state/assignee) crosses; daemon already stripped tokens (Plans 03/05); React auto-escapes |
| T-08-26 | Cross-site (link-out) | external issue links | mitigate | all link-outs use rel="noopener noreferrer" target="_blank" |
| T-08-27 | Schema drift | panel data parse | mitigate | apiFetch parseOrDrift → schema_drift guard → InlineDrift (no silent mis-render) — validates INV-04 at the browser |
| T-08-28 | Tampering / data boundary | static Linear surface (IntegrationsHealth) | mitigate | LINEAR-02 clause b / D-08-06 — the Phase 5 IntegrationsHealth Linear row stays API-free (no useLinearIssues, no /linear/issues); asserted by grep + git-diff-quiet in Task 3 |
| T-08-SC | Tampering | npm installs | accept | zero new packages |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test` green (hooks + both panels + SingleProjectView)
- `pnpm --filter @agenticapps/dashboard-spa typecheck` clean
- `pnpm lint` clean (CI enforces eslint — per memory `feedback_run_lint_before_ship`)
- INV-03 confirmed: with no env vars the panels show configure copy and the rest of the view renders
- LINEAR-02 clause (b): IntegrationsHealth.tsx unchanged and free of any useLinearIssues / /linear/issues reference (D-08-06)
</verification>

<success_criteria>
- Both panels render all four states with static configure copy + link-outs + verbatim stale banners
- Hooks schema-validate at the browser boundary (INV-04) and are per-project keyed
- Both panels wired into the single-project Health column
- The pre-existing static Linear surface (IntegrationsHealth) stays API-free (LINEAR-02 clause b / D-08-06)
- spa suite + typecheck + lint green
- (08-IMPECCABLE.md ≥80 produced at verify time — not a task here)
</success_criteria>

<output>
Create `.planning/phases/DASH-08-optional-integration-panels/08-06-SUMMARY.md` when done
</output>
