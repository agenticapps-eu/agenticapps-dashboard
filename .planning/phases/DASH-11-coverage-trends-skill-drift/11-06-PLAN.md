---
phase: 11
plan: 06
type: tdd
wave: 2
depends_on: []
files_modified:
  - packages/spa/src/components/ui/PageHeader.tsx
  - packages/spa/src/components/ui/PageHeader.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageRow.tsx
  - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
  - packages/spa/src/routes/coverage.lazy.tsx
autonomous: true
requirements:
  - PLI-01
  - PLI-02
  - PLI-03
tags:
  - spa
  - polish
  - sticky-header
  - opacity
  - tdd

must_haves:
  truths:
    - "PageHeader supports an optional sticky?: boolean prop (default false — backward-compatible)"
    - "When sticky=true, the outer div gains 'sticky top-0 z-10 bg-app-bg' classes; mb-6 24px bottom margin preserved"
    - "Existing PageHeader usages on other routes are unaffected (sticky defaults to false)"
    - "CoverageRow per-row refresh button starts at opacity-30 (was opacity-0); hover/focus still bumps to opacity-100"
    - "/coverage route opts into sticky PageHeader by passing sticky={true}"
    - "Plan 06 runs INDEPENDENTLY of Plans 02-05 (no dependency on schemas/routes/SPA infrastructure changes)"
  artifacts:
    - path: "packages/spa/src/components/ui/PageHeader.tsx"
      provides: "PageHeader with new sticky prop"
      exports: ["PageHeader", "PageHeaderProps"]
    - path: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      provides: "Coverage row with refresh button at opacity-30 default"
      exports: ["CoverageRow"]
    - path: "packages/spa/src/routes/coverage.lazy.tsx"
      provides: "Coverage route opting into sticky=true"
      contains: "sticky={true}"
  key_links:
    - from: "packages/spa/src/routes/coverage.lazy.tsx"
      to: "packages/spa/src/components/ui/PageHeader.tsx"
      via: "<PageHeader title=\"Coverage\" sticky={true} ... />"
      pattern: "sticky=\\{true\\}|sticky={true}"
---

<objective>
Land the two Phase 10.6 polish items folded into Phase 11 (D-11-09 + D-11-10) + the route opt-in for sticky PageHeader (PLI-03):

1. **PLI-01 — PageHeader sticky prop:** Add optional `sticky?: boolean` (default `false`); when `true`, the outer div gains `sticky top-0 z-10 bg-app-bg` Tailwind classes. The existing `mb-6` 24px bottom margin is preserved per CONTEXT §Specifics. Opt-in per-route so no other dashboard route regresses.

2. **PLI-02 — CoverageRow opacity polish:** Change the per-row refresh button's default opacity from `opacity-0` to `opacity-30` for touchpad/keyboard discoverability (Phase 10.6 IMPECCABLE triage carry-over). Hover/focus still bumps to `opacity-100`. One-token swap.

3. **PLI-03 — Route opt-in:** `/coverage` route's `<PageHeader>` invocation gains `sticky={true}`.

Purpose: Closes the "fold into Phase 11.x" items from `.planning/phases/DASH-10-.../10-IMPECCABLE.md` "Additional follow-up" section. Affects every dashboard route only insofar as PageHeader is touched — the default-false ensures backward compatibility on non-Coverage routes.

This plan is **truly independent of the schema/route/SPA hook infrastructure** (Plans 01-05) — it only edits two existing component files + one route file. It can run in any wave; placed in Wave 2 to keep wave count tidy.

Output: 3 modified files + test extensions. Tiny but user-visible polish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/DASH-11-coverage-trends-skill-drift/11-CONTEXT.md
@.planning/phases/DASH-11-coverage-trends-skill-drift/11-RESEARCH.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md

# Existing files this plan modifies — READ END-TO-END FIRST
@packages/spa/src/components/ui/PageHeader.tsx
@packages/spa/src/components/ui/PageHeader.test.tsx
@packages/spa/src/components/panels/coverage/CoverageRow.tsx
@packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
@packages/spa/src/routes/coverage.lazy.tsx

# Token reference
@packages/spa/src/styles/tokens.css
@packages/spa/src/components/AppShellV2.tsx

<interfaces>
From `packages/spa/src/components/AppShellV2.tsx:49` (existing — verified):
```tsx
<main id="main" className="flex-1 overflow-y-auto p-6">
```
The `<main>` is the SCROLL CONTAINER. `sticky top-0` anchors to the nearest scroll ancestor → sticky pins to the top of `<main>` content area, BELOW the TopBar. No wrapper or scroll-container hoist needed.

From `packages/spa/src/styles/tokens.css` (existing — verified):
```css
:root {
  --color-app-bg: #FAFAF7;   /* warm paper */
  --z-sticky: 10;
  --z-overlay: 100;
}
```
Tailwind utilities: `bg-app-bg` (maps to `--color-app-bg`), `z-10` (matches `--z-sticky`).

From `packages/spa/src/components/ui/PageHeader.tsx` (existing):
```typescript
export interface PageHeaderProps {
  title: string
  helper?: string
  actions?: ReactNode
  children?: ReactNode
}
// Phase 11 ADDS: sticky?: boolean (default false)
```

From `packages/spa/src/components/panels/coverage/CoverageRow.tsx:120` (existing — verified):
```tsx
// Per-row refresh button currently has `opacity-0` in its className.
// Phase 11 changes this single token to `opacity-30`.
// hover:opacity-100 + focus:opacity-100 remain unchanged.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageHeader sticky prop + CoverageRow opacity-30 + route opt-in (RED→GREEN combined)</name>
  <files>packages/spa/src/components/ui/PageHeader.tsx, packages/spa/src/components/ui/PageHeader.test.tsx, packages/spa/src/components/panels/coverage/CoverageRow.tsx, packages/spa/src/components/panels/coverage/CoverageRow.test.tsx, packages/spa/src/routes/coverage.lazy.tsx</files>

  <read_first>
    - packages/spa/src/components/ui/PageHeader.tsx (existing — read end-to-end; identify the outer div + its current `mb-6 flex flex-col gap-1` className)
    - packages/spa/src/components/ui/PageHeader.test.tsx (existing — extend, do NOT remove)
    - packages/spa/src/components/panels/coverage/CoverageRow.tsx (existing — find the single line where `opacity-0` appears; verify hover:/focus: states adjacent)
    - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx (existing — extend with opacity assertion)
    - packages/spa/src/routes/coverage.lazy.tsx (existing — find the `<PageHeader title="Coverage" .../>` invocation; add `sticky={true}` prop)
    - packages/spa/src/components/AppShellV2.tsx (read line 49 to confirm `<main>` has `overflow-y-auto` — this is what makes `sticky top-0` work; no plan change here, just verification)
    - packages/spa/src/styles/tokens.css (confirm `--color-app-bg` + `--z-sticky` token names — Tailwind classes `bg-app-bg` + `z-10` map to these)
    - .planning/phases/DASH-11-coverage-trends-skill-drift/11-RESEARCH.md §"Pattern 7: Sticky PageHeader" (lines 524-549) — implementation source
    - .planning/phases/DASH-11-coverage-trends-skill-drift/11-RESEARCH.md §"Pitfall 8: Sticky PageHeader blocked by overflow:hidden ancestor" (line 635) — verify no ancestor regressions
    - .planning/phases/DASH-11-coverage-trends-skill-drift/11-CONTEXT.md §Decisions D-11-09 + D-11-10 + §Specifics (mb-6 24px preserved)
  </read_first>

  <behavior>
    **PageHeader.test.tsx additions:**
    - Test 1: `<PageHeader title="X" />` (no sticky prop) renders with className containing `mb-6 flex flex-col gap-1` BUT NOT containing `sticky` (backward-compat default)
    - Test 2: `<PageHeader title="X" sticky={false} />` matches Test 1 behaviour exactly (explicit false === default)
    - Test 3: `<PageHeader title="X" sticky={true} />` renders with className containing `mb-6 flex flex-col gap-1 sticky top-0 z-10 bg-app-bg` (all in some order; verify each token is present)
    - Test 4: The `mb-6` 24px bottom margin is preserved in BOTH cases (regression guard for CONTEXT §Specifics)
    - Test 5: The existing children/helper/actions render unchanged across all three cases (no regression to existing rendering)

    **CoverageRow.test.tsx additions:**
    - Test 6: The per-row refresh button's className contains `opacity-30` (not `opacity-0`) by default
    - Test 7: The button STILL contains `hover:opacity-100` (or equivalent) — bumping behaviour preserved
    - Test 8: The button STILL contains `focus:opacity-100` (or equivalent — keyboard discoverability)
  </behavior>

  <action>
**Step A — PageHeader sticky prop (RED + GREEN):**

Append tests 1-5 to `PageHeader.test.tsx`. Confirm 1-2 still pass (current default behaviour) and 3 fails (sticky token absent because the prop is not yet supported).

Modify `packages/spa/src/components/ui/PageHeader.tsx`:

```typescript
export interface PageHeaderProps {
  title: string
  helper?: string
  actions?: ReactNode
  children?: ReactNode
  /**
   * Phase 11 PLI-01 / D-11-09 — when true, the header sticks to the top of
   * the AppShellV2 <main> scroll container. Defaults to false to preserve
   * current behavior on every route that has not opted in.
   *
   * Tokens: bg-app-bg matches --color-app-bg (#FAFAF7 warm paper) so scrolled
   * content does not bleed through; z-10 matches --z-sticky (stays below
   * --z-overlay: 100 and --z-modal: 1000). 24px bottom margin (mb-6) is
   * preserved per CONTEXT §Specifics.
   */
  sticky?: boolean
}

export function PageHeader({
  title,
  helper,
  actions,
  children,
  sticky = false,
}: PageHeaderProps): JSX.Element {
  const stickyClasses = sticky ? ' sticky top-0 z-10 bg-app-bg' : ''
  return (
    <div className={`mb-6 flex flex-col gap-1${stickyClasses}`}>
      {/* existing render — UNCHANGED below this line */}
      {/* ... */}
    </div>
  )
}
```

Run tests; all 5 (1-5) pass. Confirm GREEN.

Commit: `test+feat(11-06): add sticky prop to PageHeader (PLI-01 D-11-09)`.

**Step B — CoverageRow opacity polish (RED + GREEN):**

Append tests 6-8 to `CoverageRow.test.tsx`. Confirm test 6 fails (opacity-0 default).

Edit `CoverageRow.tsx`: find the per-row refresh button's className (Plan 10.6 audit at line ≈120 — verify by reading the file). Change the literal `opacity-0` to `opacity-30`. DO NOT change any other class. The `hover:opacity-100` and `focus:opacity-100` (or whichever bumping selectors are present) stay untouched.

Run tests; 6-8 pass.

Commit: `test+feat(11-06): bump CoverageRow refresh button to opacity-30 (PLI-02 D-11-10)`.

**Step C — Coverage route opts into sticky (PLI-03):**

Edit `packages/spa/src/routes/coverage.lazy.tsx`. Find the `<PageHeader title="Coverage" .../>` invocation (or wherever Coverage page's PageHeader is rendered — it might be in `CoveragePage.tsx` instead of the lazy file; verify by reading both). Add `sticky={true}` prop:

```typescript
<PageHeader title="Coverage" sticky={true} /* existing props */ />
```

Add a small test (if a route-level test file exists) OR add an assertion to the existing CoveragePage test verifying the PageHeader is rendered with sticky=true (mock PageHeader OR check the parent's render tree for the prop). If no clean test boundary exists, lean on visual smoke instead: dev server + scroll the page + verify the header pins.

Commit: `feat(11-06): /coverage opts into sticky PageHeader (PLI-03)`.

**Step D — visual smoke (recommended; NOT blocking):**

With `pnpm --filter @agenticapps/dashboard-spa dev` running:
1. Navigate to `/coverage`
2. Scroll the matrix; confirm the PageHeader pins to the top of `<main>` (NOT to the viewport — the TopBar stays above)
3. Confirm scrolled content does NOT visibly pass behind the header (opacity / bg-app-bg working)
4. Navigate to another route (`/`, `/projects/:id`, `/help`, `/settings`); confirm PageHeader is NOT sticky on those routes (default-false preserved)
5. Hover the per-row refresh button on `/coverage`; confirm it goes from opacity-30 to opacity-100 smoothly

If any smoke check fails, debug the Pitfall 8 ancestor-overflow concern (research §"Pitfall 8") before committing.
  </action>

  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/components/ui/PageHeader.test.tsx src/components/panels/coverage/CoverageRow.test.tsx --run 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "sticky?: boolean\|sticky = false" packages/spa/src/components/ui/PageHeader.tsx` returns ≥ 1 (prop exists with default)
    - `grep -c "sticky top-0 z-10 bg-app-bg" packages/spa/src/components/ui/PageHeader.tsx` returns 1 (correct token chain)
    - `grep -c "mb-6" packages/spa/src/components/ui/PageHeader.tsx` returns ≥ 1 (24px bottom margin preserved per CONTEXT §Specifics)
    - `grep -c "opacity-0" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns 0 (old default fully removed from the refresh button line)
    - `grep -c "opacity-30" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns ≥ 1 (new default present)
    - `grep -c "hover:opacity-100\|focus:opacity-100\|group-hover:opacity-100" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns ≥ 1 (bumping preserved)
    - `grep -c "sticky={true}\|sticky=\\{true\\}" packages/spa/src/routes/coverage.lazy.tsx packages/spa/src/components/panels/coverage/CoveragePage.tsx` returns ≥ 1 (route opted in)
    - All ≥8 new tests pass; full SPA suite stays green
    - Visual smoke confirms sticky header on `/coverage` only (other routes unaffected)
    - Manual: `pnpm --filter @agenticapps/dashboard-spa build` succeeds
  </acceptance_criteria>

  <done>
PageHeader sticky prop ships with default-false backward compatibility; CoverageRow opacity-30 default; /coverage opts in; all tests pass; visual smoke clean across all four affected routes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan modifies only SPA presentational props/classes. No daemon routes touched. No data crosses any trust boundary as part of this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | Sticky CSS bypasses an overlay/modal | mitigate | `z-10` matches the existing `--z-sticky` token; modals are at `--z-modal: 1000` and overlays at `--z-overlay: 100`. Sticky header cannot float above either layer. Visual smoke verifies modals open over the header. |
| T-11-06-02 | Information Disclosure | Scrolled content visually bleeds through the sticky header | mitigate | `bg-app-bg` token applied on the outer div when `sticky=true` provides an opaque backstop. Visual smoke verifies. |
| T-11-06-03 | Denial of Service | Sticky pinning fails silently due to ancestor `overflow:hidden` (Pitfall 8) | accept (verified-absent) | Research §Pitfall 8 verified that `<main>` is the scroll ancestor and has no intermediate `overflow:hidden`. If a future change adds one, the sticky behaviour silently degrades to non-sticky (no crash, no security implication). |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test --run` — full SPA suite green
- `pnpm --filter @agenticapps/dashboard-spa typecheck` — clean
- `pnpm --filter @agenticapps/dashboard-spa build` — emits without warnings
- Token invariant: `tokenSourceOfTruth.test.ts` continues to pass
- Visual smoke (dev server running):
  - `/coverage`: sticky header pins on scroll; opacity-30 row-refresh buttons visible without hover; hover/focus bumps to opacity-100
  - `/`, `/projects/:id`, `/help`, `/settings`, `/onboarding`, `/pair`: PageHeader behaviour UNCHANGED (regression guard — non-Coverage routes are NOT sticky)
- `git diff packages/spa/src/components/ui/PageHeader.tsx` shows only ADDITIONS for the new `sticky` prop + className concatenation; no existing-line modifications beyond the className template
- `git diff packages/spa/src/components/panels/coverage/CoverageRow.tsx` shows a one-token swap (`opacity-0` → `opacity-30`) and nothing else
</verification>

<success_criteria>
Plan 06 is complete when:
- [ ] `PageHeader` accepts optional `sticky?: boolean` prop (default `false`)
- [ ] When `sticky=true`, the outer div gains `sticky top-0 z-10 bg-app-bg` classes
- [ ] `mb-6` 24px bottom margin preserved in both sticky and non-sticky modes
- [ ] `CoverageRow` per-row refresh button starts at `opacity-30` (was `opacity-0`)
- [ ] `hover:opacity-100` / `focus:opacity-100` bumping behaviour preserved
- [ ] `/coverage` route passes `sticky={true}` to PageHeader
- [ ] Other routes remain default (non-sticky) — regression-guarded
- [ ] ≥8 new tests pass; full SPA suite stays green
- [ ] Visual smoke confirms sticky behaviour on `/coverage` only
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-11-coverage-trends-skill-drift/11-06-SUMMARY.md` recording:
- 3 source files modified
- Test counts added
- Confirmation that `tokenSourceOfTruth.test.ts` continues to pass (no new tokens or hex literals)
- Visual smoke screenshots (or notes) for `/coverage` sticky behaviour + non-Coverage routes' regression-guard
- Note for the phase-level impeccable critique: `/coverage` should be re-critiqued after this plan (composite delta from sticky header + opacity polish + drift badge from Plan 04 all bundled) — Plan 04 + Plan 06 together change the visible `/coverage` surface
</output>
