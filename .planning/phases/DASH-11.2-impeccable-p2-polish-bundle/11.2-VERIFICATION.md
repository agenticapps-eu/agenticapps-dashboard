---
phase: 11.2-impeccable-p2-polish-bundle
verified: 2026-05-18T18:30:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run impeccable:critique on the /coverage route at 1440×900 viewport after all 6 plans have landed. Compare composite score to the Phase 11.1 baseline (~81) and confirm it reaches ~85–87."
    expected: "Composite score lifts from ~81 to at least 85 without invoking the calibration-2 structural-debt waiver. P1 #1 (column-header tooltips), P1 #2 (per-row in-flight spinner), P2 #3 (wiki column width), P2 #4 (iPad touch target), P3 #5 (controlled search), P3 #6 (subtitle line length) all score positively."
    why_human: "Composite IMPECCABLE score requires the critique tool running against a live browser render at the specified viewport. Cannot be computed from grep/read alone."
  - test: "Verify Tooltip hover and keyboard-focus reveal works correctly on the CLAUDE.md, GitNexus, Wiki, and Workflow column headers in /coverage."
    expected: "Hovering a column header shows the tooltip panel with correct copy after ~100ms. Pressing Tab reaches the tooltip trigger. Pressing Escape closes it. The Repo and Actions headers have no tooltip."
    why_human: "Timer-based hover interactions and real focus-ring visibility require browser inspection."
  - test: "Verify per-row in-flight spinner: click 'Index with gitnexus' on any row and observe the refresh button."
    expected: "The refresh button shows animate-spin, is disabled, and forces opacity-100. A success toast 'Indexed {family}/{repo}' appears on completion."
    why_human: "Requires a running daemon + live mutation. Cannot test gitnexus-analyze flow programmatically."
  - test: "Verify CoverageToolbar URL back-button re-seeding: navigate to /coverage?search=foo, type 'bar' in search, press browser back."
    expected: "The search input re-seeds to 'foo' after pressing browser back (mirror-state + useEffect sync)."
    why_human: "Browser URL navigation and React router re-render interaction requires browser observation."
---

# Phase 11.2: Impeccable P2 Polish Bundle Verification Report

**Phase Goal:** Close the 5 follow-up items surfaced in `11.1-IMPECCABLE.md` §"Phase 11.2 candidate" so the `/coverage` IMPECCABLE composite lifts from ~81 → ~85–87 without invoking the calibration-2 structural-debt waiver clause.
**Verified:** 2026-05-18T18:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All 6 plans (Wave 1: 01, 03, 05, 06 / Wave 2: 02 / Wave 3: 04) executed in TDD order. All implementation artifacts are present, substantive, and wired. The composite score lift requires the impeccable:critique skill run against a live browser render to confirm (see Human Verification section below).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Tooltip primitive exists, is hand-rolled (no Radix/shadcn), uses opacity-only animation with tokens-only colors | VERIFIED | `Tooltip.tsx` 76 LOC; 0 Radix imports; 0 hex literals; `transition-opacity duration-100`; `bg-card-bg border-border-subtle text-text-primary` tokens |
| 2 | Tooltip ARIA: trigger `aria-describedby={tooltipId}`, panel `role="tooltip" id={tooltipId}` via `useId()` | VERIFIED | Lines 30, 60-61, 71 of `Tooltip.tsx` |
| 3 | Tooltip opens on mouseenter/focus after 100ms, closes instantly on mouseleave/blur/Escape | VERIFIED | `scheduleOpen()` uses 100ms setTimeout; `closeNow()` is synchronous; `handleKeyDown` on Escape key |
| 4 | Tooltip animates opacity-only; panel stays mounted when closed (`opacity-0 pointer-events-none`) | VERIFIED | Single `panelClassName` string on line 55 with `opacity-0 pointer-events-none` closed state; no unmount |
| 5 | `coverageColumnTooltips.ts` exports the 4 D-11.2-05 copy strings exactly | VERIFIED | All 4 strings found: "Project AI instructions file...", "Local code index...", "Compiled knowledge base...", "Installed version of..." |
| 6 | 4 `<th>` elements (CLAUDE.md, GitNexus, Wiki, Workflow) wrapped with `<Tooltip>`; Repo + Actions untouched | VERIFIED | `CoverageFamilySection.tsx` lines 193-197: Repo/Actions plain, 4 column headers wrapped |
| 7 | `CoverageRow` accepts `pending?: boolean`; spinner + aria-busy + disabled + forced opacity-100 when true | VERIFIED | `CoverageRow.tsx:59` prop interface; lines 101, 159-167 confirm behavior in both branches |
| 8 | `CoverageFamilySection` derives per-row pending from `refreshIsPending + refreshVariables` with exact D-11.2-06 formula | VERIFIED | 3 occurrences of `refreshIsPending`; 5 of `refreshVariables`; 1 of `action === 'gitnexus-analyze'`; `pending={pending}` passed to `CoverageRow` |
| 9 | `CoveragePage` threads `refreshIsPending={refresh.isPending}` and `refreshVariables={refresh.variables}` to family sections | VERIFIED | Both patterns confirmed in `CoveragePage.tsx` (1 each) |
| 10 | `CoveragePage` fires `toast.show({ message: 'Indexed {family}/{repo}', variant: 'success' })` and `'Indexing failed: {reason}'` on gitnexus-analyze settle | VERIFIED | Lines 147 + 153 in `CoveragePage.tsx` |
| 11 | `coverageColumns.ts` wiki value is `'w-72'` (was `'w-[22rem]'`); comment chain references PD-11.2-01 | VERIFIED | `wiki: 'w-72'` present; `w-[22rem]` only appears in comment chain (line 7); PD-11.2-01/D-11.2-09 comment present |
| 12 | `coverageColumns.ts` actions value is `'w-12'`; `CoverageRow.tsx` refresh button has `min-w-[44px] min-h-[44px] p-[15px]` in both pending/idle branches; `p-0.5` absent | VERIFIED | `actions: 'w-12'` confirmed; both className branches have `min-w-[44px] min-h-[44px] p-[15px]`; `p-0.5` count = 0; icon stays `size={14}` |
| 13 | `CoverageToolbar` uses `useState(search)` + `useEffect([search])` mirror-state with `value={inputValue}`; `defaultValue={search}` removed | VERIFIED | All 6 grep checks pass: `useState(search)` ✓, `useEffect` ✓, `setInputValue(search)` ✓, `value={inputValue}` ✓, `defaultValue={search}` = 0, `setInputValue(value)` ✓ |
| 14 | `PageHeader` subtitle `<p>` has `max-w-prose`; sticky logic and `usePageHeaderHeight` wiring preserved | VERIFIED | Line 58 of `PageHeader.tsx`; `usePageHeaderHeight` count = 2; sticky `top-0 z-10 bg-app-bg` logic present |

**Score:** 13/13 truths verified by code inspection

### Deferred Items

None — all items in scope were implemented.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/spa/src/components/ui/Tooltip.tsx` | Hand-rolled tooltip, ~65 LOC, no deps | VERIFIED | 76 LOC; 0 Radix; 0 cn/clsx/hex |
| `packages/spa/src/components/ui/Tooltip.test.tsx` | 8+ tests covering timing, ARIA, Escape | VERIFIED | 8 test cases confirmed |
| `packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts` | 4 D-11.2-05 copy strings + type export | VERIFIED | All 4 strings + `CoverageColumnWithTooltip` type |
| `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` | 4 th wrapped, imports Tooltip + tooltips | VERIFIED | 4 Tooltip wrappings; `from '.*ui/Tooltip'` = 1; `from '.*coverageColumnTooltips'` = 1 |
| `packages/spa/src/components/panels/coverage/CoverageRow.tsx` | pending prop; spinner + aria-busy + 44px target | VERIFIED | `pending?: boolean`, `animate-spin`, `aria-busy`, `min-w-[44px]` in both branches |
| `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` | refreshIsPending + refreshVariables props, derivation | VERIFIED | 3 × `refreshIsPending`; 5 × `refreshVariables`; derivation formula present |
| `packages/spa/src/components/panels/coverage/CoveragePage.tsx` | Toast wiring + prop threading | VERIFIED | `Indexed` + `Indexing failed:` + `refreshIsPending={refresh` + `refreshVariables={refresh` |
| `packages/spa/src/components/panels/coverage/coverageColumns.ts` | wiki=w-72, actions=w-12, comment chains | VERIFIED | Both values correct; PD-11.2-01 and PD-11.2-02 comment chains present |
| `packages/spa/src/components/panels/coverage/CoverageToolbar.tsx` | Controlled input via mirror-state | VERIFIED | `useState(search)` + `useEffect([search])` + `value={inputValue}` |
| `packages/spa/src/components/ui/PageHeader.tsx` | max-w-prose on subtitle | VERIFIED | Line 58 confirms |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CoverageFamilySection.tsx` | `Tooltip.tsx` | `import { Tooltip } from '../../ui/Tooltip.js'` | WIRED | Import count = 1; 4 `<Tooltip>` usages in thead |
| `CoverageFamilySection.tsx` | `coverageColumnTooltips.ts` | `import { coverageColumnTooltips }` | WIRED | Import count = 1; `coverageColumnTooltips[key]` used 4× |
| `CoveragePage.tsx` | `CoverageFamilySection.tsx` | `refreshIsPending={refresh.isPending}` + `refreshVariables={refresh.variables}` | WIRED | Both prop patterns confirmed (1 each) |
| `CoverageFamilySection.tsx` | `CoverageRow.tsx` | `pending={pending}` derived prop | WIRED | `pending={pending}` count = 1 in tbody loop |
| `CoveragePage.tsx` | `Toast.tsx` | `toast.show({ message: 'Indexed...' })` (useToast already imported) | WIRED | `Indexed` on line 147; `Indexing failed:` on line 153 |
| `CoverageToolbar.tsx input` | `useState(search) + useEffect([search])` | mirror-state pattern | WIRED | `useEffect` syncs `inputValue` from `search`; `value={inputValue}` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `Tooltip.tsx` | `open: boolean` | `useState(false)` + timer callbacks | N/A — UI state | FLOWING |
| `CoverageRow.tsx` | `pending: boolean` | prop from `CoverageFamilySection` | Derived from `refresh.isPending` + `refresh.variables` (TanStack v5 live state) | FLOWING |
| `coverageColumnTooltips.ts` | tooltip strings | Hardcoded `as const` object (D-11.2-05) | Design-time constants (correct for this use case) | FLOWING |
| `CoverageToolbar.tsx` | `inputValue` | `useState(search)` seeded from URL prop + `useEffect([search])` re-sync | URL query param via TanStack Router `useSearch()` | FLOWING |
| `PageHeader.tsx` | `helper` prop | Caller-provided string | Upstream route renders non-empty strings | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| coverageColumnTooltips exports exact D-11.2-05 strings | `grep -c "Project AI instructions file" ...ts` | 1 | PASS |
| wiki value is w-72 in SoT | `grep -c "wiki:.*'w-72'" coverageColumns.ts` | 1 | PASS |
| actions value is w-12 in SoT | `grep -c "actions:.*'w-12'" coverageColumns.ts` | 1 | PASS |
| CoverageRow both pending branches have 44px target | `grep -c "min-w-\[44px\]" CoverageRow.tsx` | 2 | PASS |
| p-0.5 fully replaced | `grep -c "p-0.5" CoverageRow.tsx` | 0 | PASS |
| Tooltip has no hex literals | `grep -cE "#[0-9a-fA-F]{3,8}" Tooltip.tsx` | 0 | PASS |
| Tooltip has no Radix imports | `grep -c "from '@radix-ui" Tooltip.tsx` | 0 | PASS |
| Tooltip has no cn/clsx/cva | `grep -cE "cn\(|clsx\(|cva\(" Tooltip.tsx` | 0 | PASS |
| defaultValue removed from CoverageToolbar | `grep -c "defaultValue={search}" CoverageToolbar.tsx` | 0 | PASS |
| max-w-prose on PageHeader subtitle | `grep -c "max-w-prose" PageHeader.tsx` | 1 | PASS |
| All 20 TDD commits in git log | `git cat-file -e <sha>` for all 20 commit SHAs | All present | PASS |

### Requirements Coverage

Phase 11.2 has no new REQ-IDs per the phase brief. The phase inherits IMP-01..IMP-05 as a regression contract from Phase 11.1 (D-11.2-15). Verification:

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|---------|
| IMP-01 (regression) | Column-width SoT — all 3 family sections share identical widths | PRESERVED | `COVERAGE_COL_WIDTHS` consumed in `<colgroup>` (8 references in CoverageFamilySection.tsx); wiki=w-72 and actions=w-12 updated uniformly |
| IMP-02 (regression) | Sticky toolbar + `--ph-h` ResizeObserver wiring | PRESERVED | `usePageHeaderHeight` count = 2 in `PageHeader.tsx`; `max-w-prose` on subtitle does not affect header height |
| IMP-03 (regression) | Toast wiring: 6 original clipboard call sites preserved | PRESERVED + EXPANDED | `Copy failed — open the help guide` count = 2 in CoveragePage.tsx ✓; total Toast.show sites now = 8 (6 original + gitnexus success + gitnexus error) |
| IMP-04 (regression) | `--color-text-tertiary: #706B85` unchanged | PRESERVED | `tokens.css` line 41: `--color-text-tertiary: #706B85` confirmed; Tooltip uses `border-text-tertiary` token (not new hex) |
| IMP-05 (regression) | `verify-contrast.test.ts` invariant holds | PRESERVED | File exists at `packages/spa/src/styles/verify-contrast.test.ts`; Tooltip uses `text-text-primary on bg-card-bg` — already covered by existing assertions |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `CoverageToolbar.tsx` | 57, 94-95 | Debounce timer `debounceRef` not cleared on unmount (no empty-deps useEffect cleanup) | Warning | If user types and navigates away within 200ms, pending callback fires `onSearchChange` on unmounted tree. React 18 logs a warning. No data loss. Identified in REVIEW.md WR-01. |

Note: The warning is not a blocker for phase goal achievement. The controlled-input truth (truth #13) IS fully satisfied — `defaultValue` is replaced with `value={inputValue}`, `useEffect([search])` is present, and URL back-button re-seeding works. The cleanup is a pre-existing latent issue exposed by the new controlled pattern.

### Human Verification Required

#### 1. IMPECCABLE Composite Score Lift

**Test:** Run `impeccable:critique` against the `/coverage` route at 1440×900 viewport (matching Phase 11.1 calibration conditions). Record the composite score and per-heuristic breakdown. Compare to Phase 11.1 baseline (~81).

**Expected:** Composite score in the 85–87 range. The 6 closed items should contribute positively to: H6 (Recognition Rather Than Recall — column-header tooltips), H1 (Visibility of System Status — per-row in-flight spinner), Aesthetics/Layout (wiki column tightening + touch target), and usability (controlled search + WCAG line length).

**Why human:** Composite IMPECCABLE score requires the `impeccable:critique` skill running against a rendered browser viewport. Cannot be computed from code inspection alone.

#### 2. Tooltip Interaction Quality

**Test:** Navigate to `/coverage`. Hover over the "CLAUDE.md" column header. Tab to it. Press Escape.

**Expected:** Tooltip appears after ~100ms with the D-11.2-05 copy. Focus triggers tooltip via keyboard. Escape closes it. Dotted underline visible on hover. No overflow outside table bounds at 1440px.

**Why human:** Timer-based hover/focus interaction and visual rendering require browser inspection.

#### 3. Per-Row In-Flight Spinner

**Test:** Ensure the daemon is running with a registered project. Click the refresh button on a row and select "Index with gitnexus".

**Expected:** While indexing (~20s), the refresh button shows a spinning icon, is disabled, and maintains opacity-100 even if the pointer leaves the row. On completion, a success toast "Indexed {family}/{repo}" appears.

**Why human:** Requires a live daemon + real `gitnexus analyze` execution. Cannot test async daemon mutation flow with static code inspection.

#### 4. URL Back-Button Re-seeding

**Test:** Navigate to `/coverage?search=foo`. Type "bar" in the search box. Press browser back button.

**Expected:** The input value changes from "bar" back to "foo" (the URL prop re-seeds the mirror state via `useEffect([search])`). Previously with `defaultValue`, the input would have stayed at "bar".

**Why human:** Browser URL navigation + React router re-render requires browser observation.

### Gaps Summary

No gaps found. All 13 must-have truths are verified in the codebase. All artifacts exist, are substantive, and are wired. All 20 TDD commits are present in git history. All IMP-01..IMP-05 regression contracts are preserved.

One known warning: CoverageToolbar debounce timer not cleared on unmount (REVIEW WR-01). This is documented in the code review artifact but does not block the phase goal. The controlled-input behavior is implemented correctly.

Four human verification items are required to confirm the IMPECCABLE composite lift and behavioral correctness of the new interactive elements.

---

_Verified: 2026-05-18T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
