---
phase: 11.2
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/spa/src/components/panels/coverage/coverageColumns.ts
  - packages/spa/src/components/panels/coverage/coverageColumns.test.ts
autonomous: true
requirements: []
tags:
  - coverage
  - layout
  - sot
  - tdd
  - p2

must_haves:
  truths:
    - "coverageColumns.ts wiki entry value is 'w-72' (was 'w-[22rem]')"
    - "coverageColumns.ts wiki entry has a new comment chain line referencing PD-11.2-01 + 11.1-IMPECCABLE.md P2 #3 + the 22rem → w-72 transition (D-11.2-10)"
    - "All other entries in COVERAGE_COL_WIDTHS (repo, claudeMd, gitNexus, workflow, actions) are unchanged in this plan — Plan 04 owns the actions widening"
    - "coverageColumns.test.ts has been updated to assert the new wiki value AND to assert the regression contract that repo/claudeMd/gitNexus/workflow remain unchanged"
    - "Regression: CoverageFamilySection's <colgroup> renders the new w-72 class via the SoT — no per-call-site changes needed; IMP-01 contract holds (3 family sections render identical widths)"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      provides: "Updated wiki width to w-72 (288px), comment chain entry"
      contains: "wiki:     'w-72'"
    - path: "packages/spa/src/components/panels/coverage/coverageColumns.test.ts"
      provides: "Test asserting wiki === 'w-72' and the other 5 entries unchanged"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/coverageColumns.test.ts"
      to: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      via: "import { COVERAGE_COL_WIDTHS }"
      pattern: "COVERAGE_COL_WIDTHS\\.wiki"
---

<objective>
Tighten the wiki column width from `w-[22rem]` (352px) to `w-72` (288px) — a single edit to the COVERAGE_COL_WIDTHS source-of-truth — per D-11.2-09. The new width matches the repo column for bilateral visual rhythm and frees 64px of horizontal table space (which Plan 04 partially reclaims to widen the actions column for the 44×44px touch target).

Purpose: Close P2 #3 from `11.1-IMPECCABLE.md`. The 352px wiki column over-allocates whitespace against the common case (`<sha> · <relative-time>` rarely exceeds ~150px). `w-72` (288px) provides 138px of breathing room without truncation.

Output: 1 modified file (coverageColumns.ts — 1 string value + comment chain) + 1 modified test file. Because `coverageColumns.ts` is the single source of truth consumed by both `<colgroup>` and `<td>` (per IMP-01 contract), no per-call-site changes are needed. The CoverageFamilySection `<colgroup>` automatically picks up the new value.

Implements: D-11.2-09 (wiki: w-[22rem] → w-72), D-11.2-10 (comment chain history pattern).

Wave 1 parallel-safe: no file overlap with Plan 01 (Tooltip), Plan 05 (CoverageToolbar), or Plan 06 (PageHeader). Plan 04 depends on this plan because both write to `coverageColumns.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md

# Existing files this plan modifies
@packages/spa/src/components/panels/coverage/coverageColumns.ts
@packages/spa/src/components/panels/coverage/coverageColumns.test.ts

<interfaces>
Current state of `packages/spa/src/components/panels/coverage/coverageColumns.ts`:

```typescript
/** Single source of truth for CoveragePanel column widths consumed by colgroup + CoverageRow. */

export const COVERAGE_COL_WIDTHS = Object.freeze({
  repo:     'w-72',       // 288px — repo names (max measured 280)
  claudeMd: 'w-32',       // 128px — 4-state freshness + subtext
  gitNexus: 'w-36',       // 144px — max measured 131px > 124px threshold (PD-11.1-06: bumped from w-32)
  wiki:     'w-[22rem]',  // 352px — sha + timestamp (max measured 364, truncates cleanly)
  workflow: 'w-32',       // 128px
  actions:  'w-8',        //  32px — refresh icon only
} as const)

export type CoverageColumnKey = keyof typeof COVERAGE_COL_WIDTHS
```

This plan touches ONLY the `wiki` line + adds a comment chain entry. The `actions` line is owned by Plan 04 (Wave 3).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update coverageColumns.test.ts to assert new wiki value + lock regression (RED then GREEN)</name>
  <files>packages/spa/src/components/panels/coverage/coverageColumns.test.ts, packages/spa/src/components/panels/coverage/coverageColumns.ts</files>

  <read_first>
    - `packages/spa/src/components/panels/coverage/coverageColumns.ts` (current SoT — confirm the 6 entries match the interfaces block above)
    - `packages/spa/src/components/panels/coverage/coverageColumns.test.ts` (existing test — find the assertion on `wiki` and update it; also keep/strengthen the assertions on the other 5 entries)
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md` §D-11.2-09, D-11.2-10 — exact transition value + comment chain requirement
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md` §"4. Wiki column width" — exact comment chain text
  </read_first>

  <behavior>
    **Step 1a (RED) — update coverageColumns.test.ts:**

    The existing test file already asserts the 6 column widths. Modify the assertion for `wiki` from `'w-[22rem]'` (or whatever it currently asserts) to `'w-72'`. ALSO add (if not already present) explicit assertions that the other 5 entries are unchanged, to serve as a regression lock against accidental edits:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { COVERAGE_COL_WIDTHS } from './coverageColumns.js'

    describe('COVERAGE_COL_WIDTHS', () => {
      // Regression lock — these 5 entries are unchanged in Phase 11.2 Plan 03
      it('repo column width is w-72 (288px)', () => {
        expect(COVERAGE_COL_WIDTHS.repo).toBe('w-72')
      })
      it('claudeMd column width is w-32 (128px)', () => {
        expect(COVERAGE_COL_WIDTHS.claudeMd).toBe('w-32')
      })
      it('gitNexus column width is w-36 (144px)', () => {
        expect(COVERAGE_COL_WIDTHS.gitNexus).toBe('w-36')
      })
      it('workflow column width is w-32 (128px)', () => {
        expect(COVERAGE_COL_WIDTHS.workflow).toBe('w-32')
      })
      // Note: actions column is owned by Plan 04 — it stays at 'w-8' (32px) until that plan lands.
      it('actions column width is w-8 (32px) — Plan 04 will widen to w-12', () => {
        expect(COVERAGE_COL_WIDTHS.actions).toBe('w-8')
      })

      // Plan 03 (D-11.2-09): wiki tightening
      it('wiki column width is w-72 (288px) — tightened from w-[22rem] per D-11.2-09', () => {
        expect(COVERAGE_COL_WIDTHS.wiki).toBe('w-72')
      })
    })
    ```

    Preserve any pre-existing tests (e.g., type-level assertions on `CoverageColumnKey`) — append/replace only the value assertions.

    Run the test — confirm the wiki assertion FAILS (`w-[22rem]` !== `w-72`). The other 5 should still pass. Commit:
    `test(11.2): RED — coverageColumns wiki width regression lock (w-[22rem] → w-72)`

    **Step 1b (GREEN) — modify coverageColumns.ts:**

    Change ONLY the `wiki` line + add a comment chain entry per D-11.2-10:

    BEFORE:
    ```typescript
    export const COVERAGE_COL_WIDTHS = Object.freeze({
      repo:     'w-72',       // 288px — repo names (max measured 280)
      claudeMd: 'w-32',       // 128px — 4-state freshness + subtext
      gitNexus: 'w-36',       // 144px — max measured 131px > 124px threshold (PD-11.1-06: bumped from w-32)
      wiki:     'w-[22rem]',  // 352px — sha + timestamp (max measured 364, truncates cleanly)
      workflow: 'w-32',       // 128px
      actions:  'w-8',        //  32px — refresh icon only
    } as const)
    ```

    AFTER:
    ```typescript
    export const COVERAGE_COL_WIDTHS = Object.freeze({
      repo:     'w-72',       // 288px — repo names (max measured 280)
      claudeMd: 'w-32',       // 128px — 4-state freshness + subtext
      gitNexus: 'w-36',       // 144px — max measured 131px > 124px threshold (PD-11.1-06: bumped from w-32)
      // PD-11.2-01: w-[22rem] (352px) → w-72 (288px) — 11.1-IMPECCABLE.md P2 #3, D-11.2-09.
      //   Over-allocated: max measured content 150px (sha+timestamp); 288px = 138px breathing room.
      //   Matches repo column (w-72) for visual rhythm. Net table delta vs Plan 02: −64px.
      wiki:     'w-72',       // 288px — sha + timestamp (max measured 150)
      workflow: 'w-32',       // 128px
      actions:  'w-8',        //  32px — refresh icon only (Plan 04 widens to w-12 for HIG 44px touch target)
    } as const)
    ```

    Keep the file header docstring unchanged. The `actions` line gets a one-line forward-reference comment noting Plan 04 ownership (kept terse so Plan 04 can replace it cleanly).

    Run tests — all 6 width assertions pass. Run the full SPA suite to confirm CoverageFamilySection/CoverageRow tests are unaffected (they consume COVERAGE_COL_WIDTHS.wiki via the SoT — the new value flows through automatically). Commit:
    `feat(11.2): GREEN — wiki column w-[22rem] → w-72 (PD-11.2-01)`
  </behavior>

  <action>
    Execute Step 1a (RED — update test) → confirm wiki assertion fails → execute Step 1b (GREEN — single string swap + comment chain) → confirm full suite green. Two atomic commits.

    Do NOT touch the `actions` line value in this plan. Plan 04 owns that change. The forward-reference comment is acceptable scaffolding but is optional — the executor may keep `actions:  'w-8',        //  32px — refresh icon only` unchanged if cleaner.
  </action>

  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/coverageColumns.test.ts 2>&1 | tail -15</automated>
    All 6 width assertions pass.
    <automated>pnpm --filter @agenticapps/dashboard-spa test 2>&1 | tail -10</automated>
    Full SPA suite green (CoverageFamilySection's `<colgroup>` automatically picks up the new wiki class via the SoT).
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck 2>&1 | tail -5</automated>
    Typecheck clean.
  </verify>

  <acceptance_criteria>
    - `grep -c "wiki:     'w-72'\\|wiki: 'w-72'\\|wiki:.*'w-72'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - `grep -c "w-\\[22rem\\]" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 0
    - `grep -c "PD-11.2-01\\|D-11.2-09\\|11.1-IMPECCABLE.md P2 #3" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns >= 1
    - `grep -c "repo:.*'w-72'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1 (repo unchanged)
    - `grep -c "claudeMd:.*'w-32'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - `grep -c "gitNexus:.*'w-36'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - `grep -c "workflow:.*'w-32'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - `grep -c "actions:.*'w-8'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1 (untouched — Plan 04 will edit)
    - `grep -c "COVERAGE_COL_WIDTHS.wiki" packages/spa/src/components/panels/coverage/coverageColumns.test.ts` returns >= 1
    - `pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/coverageColumns.test.ts` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa test` exits 0
    - Two commits landed in TDD order: `test(11.2): RED — coverageColumns wiki width` then `feat(11.2): GREEN — wiki column w-[22rem] → w-72`
  </acceptance_criteria>

  <done>coverageColumns.ts wiki value swapped to 'w-72' with comment chain entry; test file locks all 6 widths (including the regression contract on the other 5 entries); full SPA suite green; two TDD commits landed.</done>
</task>

</tasks>

<threat_model>
- New attack surface: none. This plan changes a single Tailwind className string in a frozen const object. No new code paths, no new data flow, no new dependencies.
- Data flows touched: none.
- Trust boundaries crossed: none.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-05 | N/A | coverageColumns.ts | accept | No security surface. CSS class value change only. |

Mitigations: existing CSP / bearer-token auth / read-only filesystem constraints preserved unchanged.
</threat_model>

<verification>
- coverageColumns.test.ts 6 assertions pass.
- Full SPA suite still green (CoverageFamilySection's `<colgroup>` and CoverageRow's `<td>` consume the SoT — no per-site update needed).
- Regression: IMP-01 column-width-identity contract holds — `<colgroup>` and `<td>` consume the same constant, so all 3 family sections render identical widths.
- Regression: typecheck clean.
- The forward-reference note in the `actions` comment is benign scaffolding for Plan 04.
</verification>

<success_criteria>
- Plan 03 closes D-11.2-09 + D-11.2-10 fully per the decision-coverage matrix.
- Two commits landed.
- Wave 1 parallel-safe: file ownership is exclusive (coverageColumns.ts touched by no other Wave 1 plan).
- Plan 04 (Wave 3) inherits the new wiki value and then edits the `actions` line.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-03-SUMMARY.md` matching the Phase 11.1 plan-summary convention. Confirm the 6 column width assertions and the comment chain entry.
</output>
