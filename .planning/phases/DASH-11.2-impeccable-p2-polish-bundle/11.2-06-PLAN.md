---
phase: 11.2
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/spa/src/components/ui/PageHeader.tsx
  - packages/spa/src/components/ui/PageHeader.test.tsx
autonomous: true
requirements: []
tags:
  - page-header
  - typography
  - wcag-line-length
  - tdd
  - p3

must_haves:
  truths:
    - "PageHeader subtitle <p> at line 58 includes the Tailwind class `max-w-prose` (65ch)"
    - "All other className tokens on that <p> are unchanged (mt-1, text-sm, text-text-tertiary)"
    - "The change applies retroactively to all 4 routes consuming PageHeader (Coverage, Skill drift, Pair, Home) with zero prop API change"
    - "Regression: D-11-09 sticky behaviour preserved (no edit to the outer <div> sticky logic); D-11.1-05 usePageHeaderHeight ResizeObserver still publishes --ph-h correctly (max-w-prose causes no height change at common viewport widths)"
    - "Regression: existing PageHeader.test.tsx assertions for title, helper, actions slot, children slot, sticky/non-sticky all still pass"
  artifacts:
    - path: "packages/spa/src/components/ui/PageHeader.tsx"
      provides: "Subtitle <p> gains max-w-prose for line-length discipline"
      contains: "max-w-prose"
  key_links:
    - from: "packages/spa/src/components/ui/PageHeader.tsx"
      to: "Tailwind's max-w-prose token (65ch)"
      via: "className utility"
      pattern: "max-w-prose"
---

<objective>
Add `max-w-prose` (Tailwind's 65ch utility) to the subtitle paragraph in `PageHeader.tsx` so long helper text wraps at the WCAG-recommended 45–75ch reading band. One-line edit; applies retroactively to all 4 routes using PageHeader.

Purpose: Close P3 #6 from `11.1-IMPECCABLE.md`. The detector flagged the helper paragraph as unconstrained — long subtitles can extend to the full content width (700+ characters at 1440px), violating the 65–80ch optimal reading band.

Output: 1 modified line in PageHeader.tsx + 1 new test assertion. Zero prop API change. Zero downstream call-site updates.

Implements: D-11.2-14 (max-w-prose on subtitle <p>).

Wave 1 parallel-safe: `PageHeader.tsx` is exclusively owned by this plan — no other Phase 11.2 plan touches it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md

# Existing files this plan modifies
@packages/spa/src/components/ui/PageHeader.tsx
@packages/spa/src/components/ui/PageHeader.test.tsx

<interfaces>
Current PageHeader.tsx line 58 (the subtitle paragraph):

```tsx
{helper && (
  <p className="mt-1 text-sm text-text-tertiary">{helper}</p>
)}
```

Target shape (D-11.2-14):

```tsx
{helper && (
  <p className="mt-1 text-sm text-text-tertiary max-w-prose">{helper}</p>
)}
```

One token added (`max-w-prose`). All other code in PageHeader.tsx is unchanged.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append failing test for max-w-prose on subtitle (RED) + apply the edit (GREEN)</name>
  <files>packages/spa/src/components/ui/PageHeader.test.tsx, packages/spa/src/components/ui/PageHeader.tsx</files>

  <read_first>
    - `packages/spa/src/components/ui/PageHeader.tsx` (entire file — confirm line 58 matches the interfaces block above)
    - `packages/spa/src/components/ui/PageHeader.test.tsx` (existing — find the existing helper-rendering tests; the new assertion will sit alongside them)
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md` §D-11.2-14 — one-line edit rationale
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md` §"7. PageHeader subtitle line length" — exact target
  </read_first>

  <behavior>
    **Step 1a (RED) — append test to PageHeader.test.tsx:**

    Add a new test under the existing helper-rendering describe (or a new `describe('subtitle line length (D-11.2-14)')` block):

    1. `subtitle paragraph applies max-w-prose (65ch line-length cap)`:
       - Render `<PageHeader title="Coverage" helper="Per-repo knowledge-layer freshness across agenticapps, factiv, and neuroflash families" />`
       - Query the helper text: `const helperEl = screen.getByText(/Per-repo knowledge-layer freshness/i)`
       - Assert `helperEl.className.includes('max-w-prose') === true`

    2. (Optional but recommended) `subtitle paragraph preserves existing classes (mt-1, text-sm, text-text-tertiary)`:
       - Same render
       - Assert `helperEl.className.includes('mt-1') && .includes('text-sm') && .includes('text-text-tertiary')`
       - This is a regression lock against future edits that might inadvertently drop existing classes when adding new ones.

    Run tests — confirm test 1 fails (max-w-prose not yet applied). Commit:
    `test(11.2): RED — PageHeader subtitle adopts max-w-prose (WCAG 45–75ch reading band)`

    **Step 1b (GREEN) — modify PageHeader.tsx:**

    Apply the single token addition at line 58:

    BEFORE:
    ```tsx
    {helper && (
      <p className="mt-1 text-sm text-text-tertiary">{helper}</p>
    )}
    ```

    AFTER:
    ```tsx
    {helper && (
      <p className="mt-1 text-sm text-text-tertiary max-w-prose">{helper}</p>
    )}
    ```

    Optionally add a comment line above the JSDoc/component note documenting the D-11.2-14 decision — but a comment is OPTIONAL. The grep on `max-w-prose` is the load-bearing acceptance check.

    Run tests — confirm 1-2 new tests pass + all existing PageHeader tests still pass (existing tests cover: title rendering, helper rendering, actions slot, children slot, sticky vs non-sticky modes, usePageHeaderHeight integration). Run the full SPA suite to confirm none of the 4 PageHeader-consuming routes regress visually. Commit:
    `feat(11.2): GREEN — PageHeader subtitle max-w-prose (D-11.2-14, WCAG line-length)`
  </behavior>

  <action>
    Execute the RED test append → confirm failure → apply the GREEN one-line edit → confirm pass. Two atomic commits. The change is a single token addition; the executor MUST NOT touch any other line in PageHeader.tsx.

    Specifically DO NOT modify the `sticky` logic, the `usePageHeaderHeight` integration, the test-id (`page-header` / `page-header-sticky`), the children slot, or any other className. The Phase 11.1 sticky-stack invariant (--ph-h ResizeObserver) is regression-critical.
  </action>

  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/ui/PageHeader.test.tsx 2>&1 | tail -15</automated>
    All PageHeader tests pass (existing + 1-2 new).
    <automated>pnpm --filter @agenticapps/dashboard-spa test 2>&1 | tail -10</automated>
    Full SPA suite green — including CoveragePage.test.tsx (sticky integration) and any other PageHeader-consuming route tests.
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck 2>&1 | tail -5</automated>
    Typecheck clean.
  </verify>

  <acceptance_criteria>
    - `grep -c "max-w-prose" packages/spa/src/components/ui/PageHeader.tsx` returns 1
    - `grep -c "mt-1 text-sm text-text-tertiary max-w-prose\\|max-w-prose.*text-text-tertiary\\|text-text-tertiary.*max-w-prose" packages/spa/src/components/ui/PageHeader.tsx` returns 1
    - `grep -c "sticky top-\\[-1.5rem\\] z-10 bg-app-bg" packages/spa/src/components/ui/PageHeader.tsx` returns 1 (Phase 11 D-11-09 sticky logic preserved)
    - `grep -c "usePageHeaderHeight" packages/spa/src/components/ui/PageHeader.tsx` returns >= 1 (Phase 11.1 D-11.1-05 ResizeObserver wiring preserved)
    - `grep -c "max-w-prose" packages/spa/src/components/ui/PageHeader.test.tsx` returns >= 1
    - `pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/ui/PageHeader.test.tsx` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa test` exits 0 (full SPA suite green — confirms no regression on Coverage/Pair/Home/SkillDrift routes that consume PageHeader)
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0
    - Two commits landed in TDD order: `test(11.2): RED — PageHeader subtitle max-w-prose` then `feat(11.2): GREEN — PageHeader subtitle max-w-prose`
  </acceptance_criteria>

  <done>PageHeader subtitle <p> gains max-w-prose; all PageHeader tests pass; full SPA suite green; sticky behaviour + ResizeObserver wiring preserved; two TDD commits landed.</done>
</task>

</tasks>

<threat_model>
- New attack surface: none. CSS class addition only. No new code paths, no new prop API, no new data flow.
- Data flows touched: none.
- Trust boundaries crossed: none.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-08 | N/A | PageHeader.tsx subtitle | accept | No security surface. CSS class addition only. |

Mitigations: existing CSP / bearer-token auth / read-only filesystem constraints preserved unchanged.
</threat_model>

<verification>
- 1-2 new PageHeader tests pass.
- All existing PageHeader tests (~10+) still pass.
- Full SPA suite green — including CoveragePage.test.tsx (which exercises PageHeader's sticky + children slot integration).
- Typecheck clean.
- Regression: D-11-09 sticky behaviour (Phase 11 PLI-01) — outer <div>'s sticky className unchanged.
- Regression: D-11.1-05 usePageHeaderHeight (Phase 11.1 IMP-02) — ResizeObserver wiring untouched. `max-w-prose` on the subtitle does not change PageHeader's height at common viewport widths (the subtitle remains single-line; only wraps once content exceeds 65ch). Even if it wraps, the ResizeObserver picks up the new height and republishes `--ph-h` correctly.
- Regression: D-11.2-15 §IMP-02 — sticky-stack math holds.
</verification>

<success_criteria>
- Plan 06 closes D-11.2-14 fully per the decision-coverage matrix.
- Two commits landed.
- Wave 1 parallel-safe: exclusive ownership of PageHeader.tsx + PageHeader.test.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-06-SUMMARY.md` matching the Phase 11.1 plan-summary convention.
</output>
