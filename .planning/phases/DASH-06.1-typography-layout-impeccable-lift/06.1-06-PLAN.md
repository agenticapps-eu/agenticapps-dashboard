---
phase: 06.1
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 6 v1.0 user-touched routes (`/`, `/projects/:id`, `/settings`, `/help`, `/onboarding`, `/pair`) measured fresh with the impeccable critique workflow at 1440x900 (desktop, per D-6-21)."
    - "Each route's composite score is recorded; per-sub-score deltas vs the post-06-06 baseline are computed."
    - "`node scripts/check-impeccable-score.mjs /tmp/imp-*.json` exits 0 against the captured JSONs (the gate passes)."
    - "If any route is below 90, the deltas are documented and a follow-up plan is recommended; the developer's checkpoint decides whether to ship 06.1 with the gap or extend the phase."
    - "The post-061-impeccable.md reference is committed under `refs/` so future phases have the same diff baseline as 06's `refs/baseline-impeccable.md` and `refs/post-polish-impeccable.md`."
  artifacts:
    - path: ".planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md"
      provides: "Post-06.1 impeccable scores for all 6 routes at 1440x900, per-sub-score deltas vs 06-06 post-polish baseline, gate verdict (PASS or REMAINING DELTA), and recommendation for 06-07 closing-ritual PR."
      contains: "post-polish-impeccable.md"
      min_lines: 80
    - path: "/tmp/imp-*.json"
      provides: "6 raw impeccable JSON captures (one per route); sourced for the markdown summary; not committed to repo."
  key_links:
    - from: "Live SPA + agent (developer's local stack)"
      to: "/tmp/imp-{route-slug}.json captures"
      via: "Playwright MCP browser automation following the impeccable critique workflow"
      pattern: "1440x900"
    - from: "/tmp/imp-*.json"
      to: "scripts/check-impeccable-score.mjs"
      via: "node CLI invocation"
      pattern: "node scripts/check-impeccable-score\\.mjs"
    - from: "Captures + script verdict"
      to: "post-061-impeccable.md committed reference"
      via: "developer-authored markdown summary"
      pattern: "Composite"
---

<objective>
Re-measure the impeccable composite + sub-scores against the live SPA + agent on the developer's local stack after Plans 01–05 have shipped, and verify the ≥ 90 gate (D-6-09 / D-6-21) now PASSES on all 6 v1.0 user-touched routes at 1440x900.

This is the goal-backward verification step for Phase 06.1. If the gate passes, Phase 6's 06-07 closing-ritual PR can land. If any route is still below 90, this plan documents the remaining delta and the developer decides whether to add a 06.1-07 closure plan or accept the gap.

`autonomous: false` — this plan REQUIRES the developer's local stack to be running (agent + SPA on localhost) and includes checkpoint tasks because the impeccable critique workflow is interactive.

Output: `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md` committed; the gate verdict known; next step decided.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-CONTEXT.md
@.planning/phases/06-polish-service-install-acceptance/06-CONTEXT.md
@.planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md
@.planning/phases/06-polish-service-install-acceptance/refs/post-polish-impeccable.md
@scripts/check-impeccable-score.mjs
@.github/workflows/impeccable.yml

<interfaces>
<!-- The 6 v1.0 user-touched routes (per D-6-10): -->

  /                     — Multi-project home
  /projects/:projectId  — Single-project view (use the registered fixture project — agenticapps-dashboard)
  /settings             — Pair settings (will exercise the Plan 05 MaskedToken integration)
  /help                 — Help page (will exercise Plan 01 line-length cap)
  /onboarding           — Onboarding hero (will exercise Plan 01 line-length cap)
  /pair?agent=...&token=...  — Pair flow happy path (will exercise Plan 04 numbered-steps empty canvas if the apiFetch is intercepted to remain pending)

<!-- Gate parameters (per D-6-09 / D-6-21): -->

  Threshold: composite >= 90
  Breakpoint: 1440x900 (desktop only — sm/md captured for diagnostic, not gate)
  Methodology: impeccable critique skill (Assessment A — LLM heuristic scoring per route via
    Playwright browser inspection; produces JSON with `routes` array of {route, breakpoint, score, sub_scores})

<!-- Score parser (already committed in 06-06): -->

  scripts/check-impeccable-score.mjs:
    - Reads report JSON (file path or stdin).
    - Filters to breakpoint === '1440x900'.
    - Exit 0 if all gate routes >= threshold; exit 1 if any route <90; exit 2 on malformed JSON.

<!-- Pre-06.1 baseline (post-polish, captured 2026-05-10): -->

  Route                  Composite  Color  Typography  Layout  Spacing  Hierarchy  Accessibility
  /                      86         90     82          88      86       87         82
  /projects/:id          81         84     80          84      80       79         78
  /settings              86         92     84          88      87       85         82
  /help                  86         86     84          88      85       84         86
  /onboarding            87         86     86          90      88       88         85
  /pair (error)          83         85     83          80      84       85         83

  Gap to 90 composite: /=−4, /projects/:id=−9, /settings=−4, /help=−4, /onboarding=−3, /pair=−7

<!-- Expected impact of Plans 01–05 on each route: -->

  /         — Plan 04 (TopBar aria-live, sidebar aria-current) lifts Accessibility +5..8.
  /projects/:id — Plan 03 (PanelContainer disclosure) is the BIG unlock: 5 panels collapsed when empty, removes "spread thin" verdict; Hierarchy +8..12 expected. Plan 04 (sidebar aria-current) +2..3.
  /settings — Plan 05 (MaskedToken integration) closes "token in plaintext"; Accessibility +6..10. Plan 01 (page-level prose cap) +2..3 to Typography.
  /help     — Plan 01 (line-length cap) +6..8 to Typography. Plan 04 (sidebar aria-current) marginal.
  /onboarding — Plan 01 (line-length cap) +4..6 to Typography.
  /pair     — Plan 04 (numbered-steps empty canvas + max-w-[75ch]) lifts Layout +8..12 and Typography +5..7.

  Predicted post-06.1 composites (rough):
    /=92, /projects/:id=91, /settings=92, /help=93, /onboarding=92, /pair=92
  All predicted to clear 90.

<!-- Pre-flight requirements: -->

  - Plans 01-05 merged into the worktree (worktree must show all 5 SUMMARY.md files committed under
    `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/`).
  - `pnpm -r typecheck && pnpm -r test && pnpm -r build` all green.
  - SPA running at localhost:5174 with Plans 01-05 changes live (`pnpm --filter @agenticapps/dashboard-spa dev`).
  - Agent running at 127.0.0.1:5193 with the dashboard project registered (`agentic-dashboard start`).
  - Browser paired with the agent (visit /pair URL once or use existing localStorage pairing).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight — confirm Plans 01–05 are merged and the live stack is running</name>
  <files>.planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-{01,02,03,04,05}-SUMMARY.md (read-only verification)</files>
  <read_first>
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/ (confirm SUMMARY.md exists for plans 01, 02, 03, 04, 05)
  </read_first>
  <action>
    Run the verification commands listed in `<how-to-verify>` and confirm all pass before proceeding to Task 2. This is a read-only checkpoint — no files are written.

    The exact commands to run, in order:

    1. Confirm SUMMARY files exist:
       ```bash
       ls .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-{01,02,03,04,05}-SUMMARY.md
       ```

    2. Run the workspace gate locally:
       ```bash
       pnpm -r typecheck && pnpm -r test && pnpm -r build
       ```

    3. Start the agent (in one terminal):
       ```bash
       agentic-dashboard start
       ```

    4. Start the SPA dev server (in another terminal):
       ```bash
       pnpm --filter @agenticapps/dashboard-spa dev
       ```

    5. Pair the browser if not already (visit the printed pair URL).

    6. Manually navigate to each of the 6 routes and visually confirm.

    7. Type "ready" to continue.
  </action>
  <what-built>
    Plans 01–05 implementations are merged into the working tree. The live developer stack (agent + SPA) is running with the new code so impeccable can measure the actual rendered UI.
  </what-built>
  <how-to-verify>
    1. `ls .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-{01,02,03,04,05}-SUMMARY.md` — expect 5 files listed.
    2. `pnpm -r typecheck && pnpm -r test && pnpm -r build` — expect all green.
    3. `agentic-dashboard start` boots; pair URL is available.
    4. `pnpm --filter @agenticapps/dashboard-spa dev` boots Vite at http://localhost:5174.
    5. Browser paired with the agent.
    6. Visual check across the 6 routes:
       - / renders with the sidebar's active state visible.
       - /projects/agenticapps-dashboard renders with the 5 right-column panels collapsed-on-empty (chevron + header).
       - /settings shows the masked token + Reveal/Copy/Edit triplet.
       - /help and /onboarding show prose wrapping at ~75ch.
       - /pair?agent=...&token=... shows the 3-step numbered list during the pairing state.
  </how-to-verify>
  <verify>
    <automated>ls .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-01-SUMMARY.md .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-02-SUMMARY.md .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-03-SUMMARY.md .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-04-SUMMARY.md .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-05-SUMMARY.md && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 5 SUMMARY files exist.
    - Workspace typecheck/test/build all green.
    - Agent + SPA running.
    - User confirms readiness with "ready" or describes blockers.
  </acceptance_criteria>
  <done>
    User has typed "ready" after confirming all 6 verification points succeed.
  </done>
  <resume-signal>Type "ready" to continue, or describe any blockers (failing tests, missing SUMMARY files, agent/SPA not booting).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run impeccable critique on all 6 routes at 1440x900 and capture JSONs</name>
  <files>/tmp/imp-home.json, /tmp/imp-project.json, /tmp/imp-settings.json, /tmp/imp-help.json, /tmp/imp-onboarding.json, /tmp/imp-pair.json, /tmp/imp-all-061.json</files>
  <read_first>
    - ~/.claude/skills/impeccable/SKILL.md (impeccable critique workflow — Assessment A scoring + Playwright invocation pattern)
    - ~/.claude/skills/impeccable/reference/critique.md (LLM design review workflow)
    - .planning/phases/06-polish-service-install-acceptance/refs/post-polish-impeccable.md (the previous capture's exact format and route list — replicate for diff-friendliness)
    - .github/workflows/impeccable.yml (CI invocation flags — gives the canonical `impeccable critique --url ... --viewport 1440x900 --json` invocation shape; replicate locally)
  </read_first>
  <action>
    Run the impeccable critique workflow against the live SPA. Use the canonical pattern (mirror `.github/workflows/impeccable.yml`, but locally pointed at `http://localhost:5174` instead of the CI preview URL).

    For each of the 6 routes listed in `<how-to-verify>`, run the impeccable critique skill (`/impeccable:critique` or the equivalent Playwright + Assessment-A invocation per the skill SKILL.md). All captures at viewport 1440x900 (desktop). sm/md captures are OPTIONAL — not gate-relevant.

    Then build a combined report JSON for the score parser:

    ```bash
    node -e "
    const fs = require('fs');
    const routes = ['home', 'project', 'settings', 'help', 'onboarding', 'pair']
      .map(r => JSON.parse(fs.readFileSync('/tmp/imp-' + r + '.json', 'utf8')))
      .flatMap(report => report.routes ?? [report]);
    fs.writeFileSync('/tmp/imp-all-061.json', JSON.stringify({ routes }, null, 2));
    console.log('wrote', routes.length, 'routes to /tmp/imp-all-061.json');
    "
    ```

    Confirm all 7 files exist and are non-empty:
    ```bash
    ls -lh /tmp/imp-{home,project,settings,help,onboarding,pair,all-061}.json
    ```

    Type "captured" to continue, or describe issues.
  </action>
  <what-built>
    Six fresh impeccable critique JSON captures, one per route, each at 1440x900 desktop breakpoint, written to `/tmp/imp-{route-slug}.json`, plus a combined `/tmp/imp-all-061.json` for the gate parser.
  </what-built>
  <how-to-verify>
    Per-route capture targets:

    | Route | URL | Output JSON |
    |-------|-----|-------------|
    | / | http://localhost:5174/ | /tmp/imp-home.json |
    | /projects/:id | http://localhost:5174/projects/agenticapps-dashboard | /tmp/imp-project.json |
    | /settings | http://localhost:5174/settings | /tmp/imp-settings.json |
    | /help | http://localhost:5174/help | /tmp/imp-help.json |
    | /onboarding | http://localhost:5174/onboarding | /tmp/imp-onboarding.json |
    | /pair | http://localhost:5174/pair?agent=http%3A%2F%2F127.0.0.1%3A5193&token=invalid-token | /tmp/imp-pair.json |

    After running the impeccable critique on each, run the combiner script in the action and confirm `/tmp/imp-all-061.json` exists with 6 routes.
  </how-to-verify>
  <verify>
    <automated>ls -lh /tmp/imp-home.json /tmp/imp-project.json /tmp/imp-settings.json /tmp/imp-help.json /tmp/imp-onboarding.json /tmp/imp-pair.json /tmp/imp-all-061.json</automated>
  </verify>
  <acceptance_criteria>
    - 6 per-route JSONs exist at `/tmp/imp-*.json`.
    - 1 combined `/tmp/imp-all-061.json` exists with a `routes` array of length >= 6.
    - Each route entry has the required keys: `route`, `breakpoint === '1440x900'`, `score` (numeric), and per-sub-score keys (color/typography/layout/spacing/hierarchy/accessibility).
    - User types "captured" to continue.
  </acceptance_criteria>
  <done>
    All 7 JSON files present at /tmp; user types "captured".
  </done>
  <resume-signal>Type "captured" to continue, or describe issues (impeccable error, agent unreachable, SPA route 404).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run check-impeccable-score.mjs against the combined report and write post-061-impeccable.md</name>
  <files>
    .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md
  </files>
  <read_first>
    - /tmp/imp-all-061.json (the combined captures from Task 2)
    - scripts/check-impeccable-score.mjs (the gate parser)
    - .planning/phases/06-polish-service-install-acceptance/refs/post-polish-impeccable.md (the format template — replicate the section structure: Assessment A per route + composite table + D-6-19 verdict + gate result + recommendation)
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-CONTEXT.md (D-6.1-* expected impact for the recommendation section)
  </read_first>
  <action>
    1. Run the gate parser:
       ```bash
       node scripts/check-impeccable-score.mjs /tmp/imp-all-061.json
       echo "Exit code: $?"
       ```
       Capture the full output (summary table + exit code). Note the exit code — 0 = PASS, 1 = REMAINING DELTA, 2 = malformed.

    2. Create `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/` directory if it does not exist:
       ```bash
       mkdir -p .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/
       ```

    3. Author `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md`. Use the EXACT structure of `06-06's post-polish-impeccable.md` as the template (read it first if not already in context). Required sections:

       - **Header**: Measurement date, branch, methodology line ("Assessment A via Playwright browser inspection + Assessment B `npx impeccable packages/spa/src --json` deterministic detector"), breakpoint audited (1440x900 per D-6-21), pre-06.1 baseline reference (`refs/post-polish-impeccable.md`).

       - **Assessment B (deterministic detector)**: run `npx impeccable packages/spa/src --json` and report exit code + finding count + true-positive/false-positive split. Expected outcome (predicted): exit 0, 0 true positives (Plans 01–05 don't add deterministic violations).

       - **Assessment A — Post-06.1 scores @ 1440x900 (per route)**: 6 sections (one per route), each with:
         - Polish applied (which Plan changed which surface).
         - Sub-scores table (post-06.1 vs post-06.06 vs delta).
         - Composite (post-06.1 vs post-06.06 vs delta).
         - Pass (>= 90) verdict.

       - **Composite scores table** (markdown table mirroring the post-polish-impeccable.md table on lines 134-144):
         | Route | Pre-06.1 | Post-06.1 | Delta | Pass (>= 90)? |
         (one row per route, populated from the JSON captures.)

       - **D-6.1 verdict**: PASS (all 6 routes >= 90) OR REMAINING DELTA (table of which routes still under, with sub-score blockers and recommended follow-up).

       - **Gate result (D-6-09)**: copy the script's summary text + exit code; one line: "Gate threshold: 90 (composite at 1440x900); Result: PASS|FAIL".

       - **Recommendation**:
         - If PASS: "Phase 06.1 closes successfully. Phase 6's 06-07 closing-ritual PR can now land — the impeccable CI gate (.github/workflows/impeccable.yml) will pass on the v1.0 closure PR."
         - If REMAINING DELTA: list the routes still under 90 + suggested next steps (extend phase 06.1 with a 06.1-07 closure plan; OR re-discuss with /gsd-discuss-phase to widen scope; OR accept and document as a known limitation in v1.0 README).

    4. Commit the new file:
       ```bash
       git add .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md
       git commit -m "docs(06.1-06): post-06.1 impeccable scores — gate verdict {PASS|FAIL}"
       ```
  </action>
  <verify>
    <automated>node scripts/check-impeccable-score.mjs /tmp/imp-all-061.json; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md` exists.
    - File contains the 6-route composite table with header `| Route | Pre-06.1 | Post-06.1 | Delta | Pass`.
    - File contains the gate-result line with explicit PASS or REMAINING DELTA verdict.
    - File contains a recommendation section appropriate to the verdict (06-07 ready vs 06.1-07 needed).
    - Script `node scripts/check-impeccable-score.mjs /tmp/imp-all-061.json` ran and its exit code is recorded in the file (PASS = 0, REMAINING DELTA = 1).
    - File is committed (visible in `git log -1 --name-only`).
  </acceptance_criteria>
  <done>
    post-061-impeccable.md exists, the gate verdict is captured (PASS or REMAINING DELTA), and the recommendation matches the verdict.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Decide next action based on gate verdict</name>
  <files>(no file modifications — decision is recorded in the plan summary)</files>
  <read_first>
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md (the verdict from Task 3)
  </read_first>
  <action>
    Read the gate verdict written by Task 3, then choose ONE of the three options listed in `<options>`. Justify the choice in 1-2 sentences so the orchestrator can route correctly:

    - If PASS: select `close-phase`. Phase 06.1 is done; orchestrator proceeds to Phase 6's 06-07 closing-ritual PR.
    - If REMAINING DELTA and the gap is fixable in another focused plan: select `extend-phase` and recommend the 06.1-07 plan content (which routes still need lift, which sub-scores, which D-decisions remain unfulfilled).
    - If REMAINING DELTA but the developer chooses to ship anyway: select `accept-and-document` — note that this requires explicit override of D-6-09 (the locked ≥ 90 gate decision); the developer must confirm the override in writing.
  </action>
  <decision>Whether Phase 06.1 closes (gate PASS) or extends (gate REMAINING DELTA).</decision>
  <context>
    Plans 01–05 have shipped, Task 3 has measured the gate. The next action depends on the verdict in `refs/post-061-impeccable.md`.
  </context>
  <options>
    <option id="close-phase">
      <name>CLOSE — Gate PASS, Phase 06.1 done</name>
      <pros>v1.0 closure PR (Phase 6 plan 06-07) can now land; CI gate will pass; no additional polish work needed.</pros>
      <cons>None — this is the happy path.</cons>
    </option>
    <option id="extend-phase">
      <name>EXTEND — Gate REMAINING DELTA, add 06.1-07 closure plan</name>
      <pros>Phase 06.1 closes the actual gap measured rather than shipping a partial fix; v1.0 quality bar preserved.</pros>
      <cons>Additional planning + execution time before 06-07 can proceed.</cons>
    </option>
    <option id="accept-and-document">
      <name>ACCEPT — Gate REMAINING DELTA but acceptable; document in v1.0 README</name>
      <pros>Ships v1.0 sooner; alpha audience explicitly accepts known UI imperfections.</pros>
      <cons>D-6-09 says "≥ 90 gate before merge"; accepting requires explicit user override of the locked decision (not a planner-discretionary choice — needs the user's call).</cons>
    </option>
  </options>
  <verify>
    <automated>grep -E 'PASS|REMAINING DELTA' .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md</automated>
  </verify>
  <acceptance_criteria>
    - User selects exactly one of: close-phase, extend-phase, accept-and-document.
    - Justification recorded in 1-2 sentences (printed to chat or appended to the SUMMARY).
  </acceptance_criteria>
  <done>
    Decision recorded; orchestrator has the routing signal to proceed to 06-07 (close), open a follow-up plan (extend), or document the override (accept).
  </done>
  <resume-signal>Select: close-phase, extend-phase, or accept-and-document. Justify briefly so the orchestrator can route correctly.</resume-signal>
</task>

</tasks>

<verification>
1. `ls .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md` returns the file path (non-empty file).
2. `git log -1 --pretty='%s' .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md` shows a commit with subject containing `docs(06.1-06)`.
3. `node scripts/check-impeccable-score.mjs /tmp/imp-all-061.json` exits 0 (PASS) — OR — exits 1 with the REMAINING DELTA documented in the markdown.
4. `grep -E 'PASS|REMAINING DELTA' .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md` matches at least once.
5. The user's checkpoint:decision is recorded (close-phase / extend-phase / accept-and-document) and the orchestrator routes accordingly.
</verification>

<success_criteria>
- All 4 tasks complete (3 checkpoint:* + 1 auto).
- 6 fresh impeccable JSONs captured (`/tmp/imp-*.json`).
- Combined report parsed via `check-impeccable-score.mjs` with exit code recorded.
- `refs/post-061-impeccable.md` exists, committed, follows the post-polish-impeccable.md template.
- Gate verdict explicit (PASS or REMAINING DELTA).
- Next-step decision made by the user (close / extend / accept).
- If PASS: Phase 06.1 ready to close, 06-07 unblocked.
- If REMAINING DELTA + extend: a follow-up 06.1-07 plan recommendation is in the post-061 file.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live SPA → Playwright browser → impeccable JSON | impeccable runs against the developer's local stack, not against production. JSONs are written to /tmp (developer-machine local). |
| /tmp/imp-*.json → committed markdown | The markdown summary is a hand-curated transcription of the JSON; the raw JSONs are NOT committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-06-01 | I (Information disclosure) | Impeccable JSON captures may include screenshot data URIs containing the developer's actual project names, file lists, or registry contents | accept | The JSONs live in /tmp (not committed). The summary markdown is hand-authored to exclude PII (project names allowed; file paths summarized). The 06's post-polish-impeccable.md set the precedent — same pattern. |
| T-06.1-06-02 | T (Tampering) | Developer could mis-transcribe scores from JSON to markdown | mitigate | The check-impeccable-score.mjs script's exit code is the source of truth for the PASS/FAIL verdict. The markdown is a human-readable transcription, but the gate decision derives from the script, not the markdown. |
| T-06.1-06-03 | D (Denial of service) | Running impeccable against 6 routes could exhaust developer-machine resources | accept | Each route capture takes ~30-60s; total ~5 minutes. No real DoS surface. |
| T-06.1-06-04 | E (Elevation of privilege) | The impeccable workflow has read-only access to the SPA — no surface for elevation. | accept | Read-only browser inspection; no side effects on the SPA or agent. |
</threat_model>

<output>
After completion, create `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-06-SUMMARY.md` recording:
- Gate result (PASS / REMAINING DELTA) + exit code from `check-impeccable-score.mjs`.
- Composite scores table (per-route, post-06.1 vs post-06.06 baseline).
- D-6.1-01..04 effectiveness matrix (which decision lifted which sub-score on which route).
- The user's checkpoint:decision (close-phase / extend-phase / accept-and-document) and rationale.
- If PASS: confirmation that Phase 06.1 is closed and Phase 6's 06-07 can proceed.
- If extending: pointer to the recommended 06.1-07 follow-up plan content.
</output>
