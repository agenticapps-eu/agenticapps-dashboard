---
phase: 06.1
plan: 07
title: Targeted closure polish to clear ≥ 90 gate on all 6 v1.0 routes
type: execute
wave: 5
depends_on: [06.1-06]
files_modified:
  - packages/spa/src/routes/settings.lazy.tsx
  - packages/spa/src/components/OnboardingHero.tsx
  - packages/spa/src/components/MultiProjectHome.tsx
  - packages/spa/src/components/ProjectCard.tsx
  - packages/spa/src/components/SingleProjectView.tsx
  - packages/spa/src/components/panels/IntegrationsHealth.tsx
  - packages/spa/src/routes/help.lazy.tsx
  - packages/spa/src/routes/pair.lazy.tsx
  - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md
  - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-07-impeccable.md
autonomous: false
requirements: []
gap_closure: true
---

<objective>
Close the remaining 1-5 point gap to ≥ 90 across all 6 v1.0 user-touched routes at 1440x900 per D-6-09 + D-6-21. Re-measurement (Plan 06.1-06) showed every route improved (+1 to +4) but composite range 85-89 still misses the gate.

Per the 06.1-06 subagent's per-route analysis, the gaps are small and addressable with targeted polish — NOT structural rework. Anti-AI-slop discipline (D-43 / D-5.1-10) and token-system constraints (D-5.1-*) remain locked.
</objective>

<scope>
Six surgical edits (per subagent recommendation), one per route's gap:

| Route | Current | Target | Specific change |
|-------|---------|--------|----------------|
| `/settings` | 89 | 90 | Wrap Theme section (ThemeToggle) in a Card-styled container so it visually pairs with ManualPairForm. Lifts Layout. |
| `/onboarding` | 89 | 90 | Refine step-number circles: change `bg-card-bg-hover` to `bg-accent/10` + `text-accent` for clearer visual hierarchy on the numbered ladder. Lifts Color + Hierarchy. |
| `/` | 88 | 90 | Apply `max-w-[60ch]` cap to ProjectCard descriptive text + tighten MultiProjectHome empty-state spacing. Lifts Typography. |
| `/help` | 87 | ≥ 90 | Add a "Common tasks" section below the keyboard shortcuts with 3-4 short bulleted items (e.g., "Pair a new device → Settings → Manual pair", "Refresh project state → press R"). Lifts Hierarchy + Layout (fills empty lower half). |
| `/projects/:id` | 85 | 90 | Shorten IntegrationsHealth empty-state nudge text (current paragraphs are dense; trim to single-sentence form). Reduces visual density. Lifts Typography + Hierarchy. |
| `/pair` | 87 | 90 | Center the numbered-steps `<ol>` block + add explicit `max-w-[60ch]` to constrain reading width. Lifts Layout. |
</scope>

<known_constraints_locked>
- D-43 / D-5.1-10 (anti-AI-slop): NO animations, NO transitions, NO skeletons, NO gradient text, NO glassmorphism, NO new SVG illustrations.
- D-5.1-* (token system): NO new colors, NO new radii, NO new font families.
- D-6-21 (desktop-only, lg-only gate): only /settings + /onboarding + / + /help + /projects/:id + /pair at 1440x900 are gate-relevant.
- Surgical edits only: each route's change should be small enough to commit atomically (1-2 commits per route).
</known_constraints_locked>

<tasks>

<task type="auto">
  <name>Task 1: Apply 6 targeted polish edits + commit each atomically</name>
  <files>per-route files in frontmatter</files>
  <read_first>
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-impeccable.md (per-route gap analysis)
    - Each target file before editing
  </read_first>
  <action>
    For each of the 6 routes per the scope table above:
    1. Read the current source file
    2. Apply the surgical edit per the table description
    3. Run any affected test files to confirm no regressions
    4. Commit as `polish(06.1-07): {route} {dimension} lift {before}→{target} ({short description})`

    Commit cadence: one commit per route. Six commits total expected.

    Anti-AI-slop hard rule: if a polish edit feels cosmetic-only (no real UX improvement), STOP and surface a checkpoint asking the orchestrator whether to lower the gate threshold instead.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa exec vitest run [affected test files]; pnpm --filter @agenticapps/dashboard-spa exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 6 routes have a corresponding polish commit
    - SPA test suite passes (no regressions)
    - Workspace typecheck clean
    - No new tokens introduced (grep tokens.css unchanged)
    - No animation/transition utility classes added (grep target files for transition|animate-|motion-|@keyframes returns 0)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-measure impeccable on all 6 routes; verify gate clears</name>
  <files>.planning/phases/DASH-06.1-typography-layout-impeccable-lift/refs/post-061-07-impeccable.md</files>
  <action>
    Same workflow as 06.1-06: Playwright MCP browser automation against live SPA at 1440x900 for the 6 routes; write JSONs to /tmp/imp-*-lg.json; run `node scripts/check-impeccable-score.mjs /tmp/imp-*-lg.json`.

    Author `refs/post-061-07-impeccable.md` with before/after comparison table.

    Commit:
    ```
    feat(06.1-07): re-measure post-closure-polish — gate {PASS|REMAINING DELTA}
    ```

    If exit 0 (PASS): write SUMMARY confirming gate clears, Phase 06.1 closure complete.
    If exit 1 (FAIL): surface checkpoint:decision per 06.1-06's pattern — orchestrator decides between (a) accept REMAINING DELTA + override D-6-09, (b) lower gate threshold, (c) one more polish micro-pass.
  </action>
  <acceptance_criteria>
    - refs/post-061-07-impeccable.md committed with full 6-route table
    - Gate result documented (PASS exit 0 or REMAINING DELTA exit 1 with route-by-route)
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
1. All 6 v1.0 routes' composite ≥ 90 at 1440x900 (gate clears via `node scripts/check-impeccable-score.mjs`)
2. No anti-AI-slop violations introduced (grep for transition/animate/motion/@keyframes/skeleton-shimmer/glassmorphism/gradient-text returns 0)
3. No new design tokens added (`packages/spa/src/styles/tokens.css` byte-identical pre/post)
4. SPA test suite green (no regression to the 627 currently-passing tests)
5. refs/post-061-07-impeccable.md committed with before/after evidence

If must-have 1 fails after this plan, escalate to checkpoint:decision rather than infinite polish loops.
</must_haves>
