---
phase: 04-single-project-view-discipline-phase-progress
plan: 06
type: execute
wave: 5
depends_on:
  - 04
  - 05
files_modified:
  - packages/spa/src/components/panels/PhaseProgress.tsx
  - packages/spa/src/components/panels/PhaseProgress.test.tsx
  - packages/spa/src/components/panels/ExecutionTimeline.tsx
  - packages/spa/src/components/panels/ExecutionTimeline.test.tsx
  - packages/spa/src/components/panels/ReviewStatus.tsx
  - packages/spa/src/components/panels/ReviewStatus.test.tsx
  - packages/spa/src/components/panels/SecurityStatus.tsx
  - packages/spa/src/components/panels/SecurityStatus.test.tsx
  - packages/spa/src/components/panels/VerificationStatus.tsx
  - packages/spa/src/components/panels/VerificationStatus.test.tsx
  - packages/spa/src/components/panels/InlineDrift.tsx
  - packages/spa/src/components/panels/InlineDrift.test.tsx
  - packages/spa/src/components/SingleProjectView.tsx
  - packages/spa/src/__tests__/projects-detail-e2e.test.tsx
autonomous: true
requirements:
  - PHASE-01
  - PHASE-02
  - PHASE-03
  - PHASE-04
  - PHASE-05
user_setup: []
must_haves:
  truths:
    - "PhaseProgress panel renders the file-by-file checklist in canonical order from PhaseProgressResponse.files (PHASE-01)"
    - "ExecutionTimeline panel groups TDD red/green commit pairs per task ID with RED dot + GREEN dot from PhaseProgressResponse.tdd.timeline (PHASE-02)"
    - "ReviewStatus panel renders Stage 1 + Stage 2 lines with 4-bucket severity glyphs (🔴 critical · 🟠 high · 🟡 medium · ⚪ low) from PhaseProgressResponse.review (PHASE-03)"
    - "SecurityStatus panel renders /cso content + DB-Sentinel content from SecurityResponse, each clamped to ~8 lines (max-h-32) (PHASE-04)"
    - "VerificationStatus panel renders progress summary + per-item rows from PhaseProgressResponse.verification (PHASE-05)"
    - "All 5 panels use a SHARED usePhaseProgress(projectId) call — only SecurityStatus uses useSecurity (separate route per D-4-01)"
    - "InlineDrift extracted into a shared component (consolidates the duplication noted in Plan 05); used by all 8 Phase 4 panels"
    - "SingleProjectView's 5 phase-progress-column slots are replaced with the real panel components"
    - "End-to-end test (projects-detail-e2e.test.tsx) renders the complete /projects/{id} route with mocked daemon responses and asserts every panel mounts + ROADMAP success criterion 1 (header + left + center columns) holds"
  artifacts:
    - path: "packages/spa/src/components/panels/InlineDrift.tsx"
      provides: "Shared inline schema-drift surface used by all 8 panels (Phase 5 extension target)"
      exports: ["InlineDrift"]
    - path: "packages/spa/src/components/panels/PhaseProgress.tsx"
      provides: "PHASE-01 panel — file-by-file checklist"
      exports: ["PhaseProgress"]
    - path: "packages/spa/src/components/panels/ExecutionTimeline.tsx"
      provides: "PHASE-02 panel — grouped RED/GREEN commit pairs"
      exports: ["ExecutionTimeline"]
    - path: "packages/spa/src/components/panels/ReviewStatus.tsx"
      provides: "PHASE-03 panel — Stage 1/2 finding-count rows"
      exports: ["ReviewStatus"]
    - path: "packages/spa/src/components/panels/SecurityStatus.tsx"
      provides: "PHASE-04 panel — /cso + DB-Sentinel content"
      exports: ["SecurityStatus"]
    - path: "packages/spa/src/components/panels/VerificationStatus.tsx"
      provides: "PHASE-05 panel — must_haves vs evidence + per-item rows"
      exports: ["VerificationStatus"]
    - path: "packages/spa/src/__tests__/projects-detail-e2e.test.tsx"
      provides: "Full route render with all 8 panels mocked, asserts ROADMAP success criterion 1"
    - path: "packages/spa/src/components/SingleProjectView.tsx"
      provides: "Phase-progress-column slots filled with the 5 panel components"
      contains: "<PhaseProgress projectId="
  key_links:
    - from: "packages/spa/src/components/panels/PhaseProgress.tsx"
      to: "packages/spa/src/lib/projectQueries.ts"
      via: "usePhaseProgress(projectId)"
      pattern: "usePhaseProgress"
    - from: "packages/spa/src/components/panels/ExecutionTimeline.tsx"
      to: "packages/spa/src/lib/projectQueries.ts"
      via: "usePhaseProgress(projectId) — reads .tdd.timeline"
      pattern: "usePhaseProgress"
    - from: "packages/spa/src/components/panels/ReviewStatus.tsx"
      to: "packages/spa/src/lib/projectQueries.ts"
      via: "usePhaseProgress(projectId) — reads .review"
      pattern: "usePhaseProgress"
    - from: "packages/spa/src/components/panels/SecurityStatus.tsx"
      to: "packages/spa/src/lib/projectQueries.ts"
      via: "useSecurity(projectId)"
      pattern: "useSecurity"
    - from: "packages/spa/src/components/panels/VerificationStatus.tsx"
      to: "packages/spa/src/lib/projectQueries.ts"
      via: "usePhaseProgress(projectId) — reads .verification"
      pattern: "usePhaseProgress"
    - from: "packages/spa/src/components/SingleProjectView.tsx"
      to: "packages/spa/src/components/panels/PhaseProgress.tsx"
      via: "<PhaseProgress projectId={projectId} />"
      pattern: "<PhaseProgress"
    - from: "packages/spa/src/components/panels/CommitmentBlock.tsx"
      to: "packages/spa/src/components/panels/InlineDrift.tsx"
      via: "extract: replace local InlineDrift helper with shared import"
      pattern: "import { InlineDrift }"
---

<objective>
Wave 4 center-column panels — Phase Progress. Replace the five phase-progress-column slots in SingleProjectView with real panels driven by `usePhaseProgress` (4 panels) and `useSecurity` (1 panel — the only standalone route in this column per D-4-01). Extract the duplicated `InlineDrift` helper from Plan 05 into a shared component. Add an end-to-end route render test that proves all 8 panels mount and asserts ROADMAP success criterion 1 (clicking a card on `/` navigates to `/projects/{id}` and renders header + left + center columns).

Purpose: Deliver PHASE-01..05. After this plan, the entire `/projects/{id}` route is rendered end-to-end. Phase 4 ships.

Output: 5 new panel components + 1 shared InlineDrift extraction + 1 e2e route test + final SingleProjectView edit.

This plan can run in PARALLEL with Plan 05 (Discipline column). The shared SingleProjectView.tsx file is the only conflict — sequence the SingleProjectView edits via depends_on if running parallel; or, if running sequentially, Plan 06 lands after Plan 05 and rebases trivially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-single-project-view-discipline-phase-progress/04-CONTEXT.md
@.planning/phases/04-single-project-view-discipline-phase-progress/04-RESEARCH.md
@.planning/phases/04-single-project-view-discipline-phase-progress/04-UI-SPEC.md
@packages/spa/src/lib/projectQueries.ts
@packages/spa/src/components/panels/PanelContainer.tsx
@packages/spa/src/components/panels/CommitmentBlock.tsx
@packages/spa/src/components/panels/HookFirings.tsx
@packages/spa/src/components/SingleProjectView.tsx
@packages/spa/src/lib/relativeTime.ts
@packages/spa/src/components/CodeBlock.tsx

<interfaces>
From packages/spa/src/lib/projectQueries.ts (Plan 04):
```typescript
export function usePhaseProgress(id: string | null): UseQueryResult<PhaseProgressResponse, Error>
export function useSecurity(id: string | null): UseQueryResult<SecurityResponse, Error>
```

From @agenticapps/dashboard-shared:
```typescript
export type PhaseFileStatus = { name: string; present: boolean; mtimeIso: string | null }
export type ExecutionTimelineEntry = {
  taskId: string
  redCommit: { sha: string; subject: string; isoDate: string } | null
  greenCommit: { sha: string; subject: string; isoDate: string } | null
}
export type ReviewFindingCounts = { critical: number; high: number; medium: number; low: number }
export type ReviewStatusPayload = {
  stage1: { present: boolean; findings: ReviewFindingCounts } | null
  stage2: { present: boolean; findings: ReviewFindingCounts } | null
}
export type VerificationStatusPayload = {
  mustHavesTotal: number
  mustHavesEvidenced: number
  items: { text: string; evidenced: boolean }[]
}
export type PhaseProgressResponse = {
  phase: string | null
  paddedPhase: string | null
  files: PhaseFileStatus[]
  tdd: { greenPairs: number; totalTasks: number; timeline: ExecutionTimelineEntry[] }
  review: ReviewStatusPayload
  verification: VerificationStatusPayload
}
export type SecurityResponse = {
  cso: { fileName: string; content: string } | null
  dbSentinel: { fileName: string; content: string } | null
}
```

From Plan 05:
- `panels/PanelContainer.tsx` — wraps every panel.
- `panels/CommitmentBlock.tsx` / `HookFirings.tsx` / `RationalizationFires.tsx` each contain a local `InlineDrift` helper. This plan extracts that helper into `panels/InlineDrift.tsx` and replaces the 3 local copies with imports.
- `lib/relativeTime.ts` — used by ExecutionTimeline.

UI-SPEC severity glyph contract (from "Color" + "ReviewStatus" sections):
```
critical → 🔴
high     → 🟠
medium   → 🟡
low      → ⚪
```
Render as inline `<span aria-hidden="true">🔴</span><span class="font-mono">2</span>` with the parent row carrying an `aria-label="2 critical, 1 high, 4 medium, 7 low"` for screen readers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared InlineDrift component + refactor Plan 05 panels (RED then GREEN)</name>
  <files>
    packages/spa/src/components/panels/InlineDrift.tsx (NEW),
    packages/spa/src/components/panels/InlineDrift.test.tsx (NEW),
    packages/spa/src/components/panels/CommitmentBlock.tsx (MODIFY: replace local helper with import),
    packages/spa/src/components/panels/HookFirings.tsx (MODIFY: replace local helper with import),
    packages/spa/src/components/panels/RationalizationFires.tsx (MODIFY: replace local helper with import)
  </files>
  <read_first>
    - packages/spa/src/components/panels/CommitmentBlock.tsx (current InlineDrift helper at the bottom of the file — copy verbatim into the new shared file)
    - packages/spa/src/components/panels/HookFirings.tsx (same helper — verify it's identical)
    - packages/spa/src/components/panels/RationalizationFires.tsx (same helper)
    - packages/spa/src/components/panels/PanelContainer.tsx (the InlineDrift uses this — keep the import)
    - packages/spa/src/components/SchemaDriftState.tsx (full drift state — InlineDrift is the simplified per-panel variant)
  </read_first>
  <behavior>
    - Test ID1: `<InlineDrift panelId="x" title="Foo" path="bar" onRetry={fn} />` renders a section with `aria-labelledby="x-title"` containing the title `Schema drift — Foo`.
    - Test ID2: the field path `bar` is visible in `<p>field: bar</p>` styled mono.
    - Test ID3: clicking the Retry button invokes `onRetry`.
    - Test ID4: AlertTriangle icon is `aria-hidden="true"` and styled `text-[--danger]`.
    - Test ID5 (refactor): after refactor, `CommitmentBlock.tsx` no longer contains a local `function InlineDrift` definition (verified by grep), but does `import { InlineDrift } from './InlineDrift.js'`. Same for HookFirings and RationalizationFires.
    - Test ID6 (regression): all Plan 05 panel tests still pass after the refactor (drift state still rendered correctly).
  </behavior>
  <action>
    1. Create `packages/spa/src/components/panels/InlineDrift.tsx`:
       ```typescript
       import { AlertTriangle } from 'lucide-react'

       import { PanelContainer } from './PanelContainer.js'

       export interface InlineDriftProps {
         panelId: string
         title: string
         path: string
         onRetry: () => void
       }

       /**
        * Per-panel inline schema-drift state. Simplified vs SchemaDriftState
        * (which surfaces the full Zod issue tree) because the query hook only
        * exposes `Error('schema_drift:<path>')` — the full issue array stays in
        * console.error from parseOrDrift's debug log.
        *
        * Used by all 8 Phase 4 panels.
        */
       export function InlineDrift({ panelId, title, path, onRetry }: InlineDriftProps): React.JSX.Element {
         return (
           <PanelContainer panelId={panelId} title={`Schema drift — ${title}`}>
             <div className="flex items-start gap-2 text-sm">
               <AlertTriangle size={14} aria-hidden="true" className="mt-0.5 text-[--danger]" />
               <div>
                 <p className="text-[--text]">The agent and dashboard disagree on the response shape.</p>
                 <p className="mt-1 font-mono text-xs text-[--text-muted]">field: {path}</p>
                 <button
                   type="button"
                   onClick={onRetry}
                   className="mt-2 rounded border border-[--border-strong] bg-[--surface-elevated] px-3 py-1 text-xs font-semibold hover:bg-[--border] focus:outline-none focus-visible:ring-2 focus-visible:ring-[--ring]"
                 >
                   Retry
                 </button>
               </div>
             </div>
           </PanelContainer>
         )
       }
       ```

    2. Create `packages/spa/src/components/panels/InlineDrift.test.tsx` covering ID1–ID4.

    3. Refactor each of the three Plan 05 panel files:
       - Add `import { InlineDrift } from './InlineDrift.js'` near the top.
       - Remove the local `function InlineDrift(...)` definition at the bottom of the file.
       - The call sites (`<InlineDrift panelId="..." title="..." />`) remain unchanged — same props, same component.

    4. RED → GREEN: write `InlineDrift.test.tsx` first, run, fails. Commit `test(04-06): add failing tests for shared InlineDrift (RED)`. Implement, rerun, commit `feat(04-06): extract shared InlineDrift (GREEN)`.

    5. After implementing the shared component, refactor the 3 Plan 05 panels in a SINGLE commit. Run all panel tests including the existing CommitmentBlock/HookFirings/RationalizationFires tests; they should all still pass (the refactor is behavior-preserving). Commit `refactor(04-06): replace local InlineDrift with shared component`.

    Notes:
      - This is a deliberate three-commit sequence: test+feat for the new shared component (TDD), then a separate refactor commit. The refactor commit must have ZERO test failures — if any drift-state assertion changes, you've broken behavior.
      - The 5 new Phase 4 panels in Tasks 2-3 will use the shared InlineDrift directly — no per-file duplication.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run panels</automated>
  </verify>
  <done>
    - `panels/InlineDrift.tsx` exists with paired tests.
    - 4+ tests passing.
    - 3 Plan 05 panels refactored — no local `function InlineDrift` definitions remain.
    - All Plan 05 panel tests still pass.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
  </done>
  <acceptance_criteria>
    - File `packages/spa/src/components/panels/InlineDrift.tsx` exists.
    - `grep -c "export function InlineDrift" packages/spa/src/components/panels/InlineDrift.tsx` returns 1.
    - `grep -c "Schema drift — \\${title}" packages/spa/src/components/panels/InlineDrift.tsx` returns 1.
    - `grep -c "function InlineDrift" packages/spa/src/components/panels/CommitmentBlock.tsx` returns 0 (local helper removed).
    - `grep -c "function InlineDrift" packages/spa/src/components/panels/HookFirings.tsx` returns 0.
    - `grep -c "function InlineDrift" packages/spa/src/components/panels/RationalizationFires.tsx` returns 0.
    - `grep -c "import { InlineDrift } from './InlineDrift.js'" packages/spa/src/components/panels/CommitmentBlock.tsx` returns 1.
    - `grep -c "import { InlineDrift } from './InlineDrift.js'" packages/spa/src/components/panels/HookFirings.tsx` returns 1.
    - `grep -c "import { InlineDrift } from './InlineDrift.js'" packages/spa/src/components/panels/RationalizationFires.tsx` returns 1.
    - File `packages/spa/src/components/panels/InlineDrift.test.tsx` has ≥ 4 `it(` blocks.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
    - `git log --oneline -5` shows `test(04-06):` then `feat(04-06):` then `refactor(04-06):` for InlineDrift.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PhaseProgress + ExecutionTimeline + ReviewStatus panels (RED then GREEN)</name>
  <files>
    packages/spa/src/components/panels/PhaseProgress.tsx (NEW),
    packages/spa/src/components/panels/PhaseProgress.test.tsx (NEW),
    packages/spa/src/components/panels/ExecutionTimeline.tsx (NEW),
    packages/spa/src/components/panels/ExecutionTimeline.test.tsx (NEW),
    packages/spa/src/components/panels/ReviewStatus.tsx (NEW),
    packages/spa/src/components/panels/ReviewStatus.test.tsx (NEW)
  </files>
  <read_first>
    - packages/spa/src/components/panels/PanelContainer.tsx + InlineDrift.tsx (reuse)
    - packages/spa/src/components/panels/CommitmentBlock.tsx (mirror for state-handling structure)
    - packages/spa/src/lib/projectQueries.ts (usePhaseProgress)
    - packages/spa/src/lib/relativeTime.ts (used by PhaseProgress mtime + ExecutionTimeline isoDate)
    - .planning/phases/04-single-project-view-discipline-phase-progress/04-UI-SPEC.md sections "PhaseProgress", "ExecutionTimeline", "ReviewStatus" (verbatim render contracts)
    - .planning/phases/04-single-project-view-discipline-phase-progress/04-CONTEXT.md D-4-13 (always expanded), D-4-14 (per-panel empty states), D-4-16 (review file selection)
  </read_first>
  <behavior>
    PhaseProgress (PHASE-01):
    - Test PP1: with `files: [{ name: 'CONTEXT.md', present: true, mtimeIso: '2026-05-06T10:00:00Z' }, { name: 'UI-SPEC.md', present: false, mtimeIso: null }]`, renders 2 rows.
    - Test PP2: present rows show CheckCircle2 icon (text-[--success]) + filename in font-mono + relative mtime label.
    - Test PP3: missing rows show Minus icon (text-[--text-subtle]) + filename in text-[--text-muted] (NO mtime label).
    - Test PP4: when `phase` is null and files is `[]`, renders empty-state copy `No phase work yet. Run /gsd-discuss-phase or /gsd-plan-phase to start.`.
    - Test PP5: phase header line above the list shows `Phase {paddedPhase} — {phase}` in `text-xs uppercase tracking-wide font-mono text-[--text-muted]`.
    - Test PP6: panel title is `Phase Progress`. Loading + drift + unreachable behave like CommitmentBlock.

    ExecutionTimeline (PHASE-02):
    - Test ET1: with `tdd.timeline: [{ taskId: '04-01', redCommit: { sha, subject, isoDate }, greenCommit: { sha, subject, isoDate } }]`, renders one task group with header `Task 04-01` and 2 commit rows (RED + GREEN).
    - Test ET2: each commit row shows the colored dot (RED→`text-[--danger]`, GREEN→`text-[--success]`) + commit subject in font-mono + relative timestamp.
    - Test ET3: incomplete pair (`greenCommit: null`) renders only the RED row, with the subject in `text-[--text-muted]` to indicate pending.
    - Test ET4: when `timeline: []`, renders empty-state copy `No TDD commits yet for this phase.`.
    - Test ET5: dot is `aria-hidden="true"`; row has `<span class="sr-only">RED:</span>` (or GREEN:) for screen readers.
    - Test ET6: panel title is `Execution Timeline`.

    ReviewStatus (PHASE-03):
    - Test RS1: with `review: { stage1: { present: true, findings: { critical: 2, high: 1, medium: 4, low: 7 } }, stage2: null }`, renders 2 lines (Stage 1 + Stage 2).
    - Test RS2: Stage 1 line shows `Stage 1` label + `present` status + glyphs `🔴 2 · 🟠 1 · 🟡 4 · ⚪ 7`.
    - Test RS3: Stage 2 line shows `Stage 2` label + `not run` status + no glyph row.
    - Test RS4: the glyph row's parent has `aria-label="Stage 1 findings: 2 critical, 1 high, 4 medium, 7 low"`.
    - Test RS5: when both stages are null, renders empty-state copy `No review run yet — try /review or /gsd-code-review.`.
    - Test RS6: glyphs are emoji literals; classes ensure glyphs are `aria-hidden="true"` and the count is `font-mono text-sm text-[--text]`.
    - Test RS7: panel title is `Review Status`.
  </behavior>
  <action>
    1. Create `packages/spa/src/components/panels/PhaseProgress.tsx`:
       ```typescript
       import { CheckCircle2, Minus } from 'lucide-react'

       import { usePhaseProgress } from '../../lib/projectQueries.js'
       import { formatRelativeTime } from '../../lib/relativeTime.js'

       import { PanelContainer } from './PanelContainer.js'
       import { InlineDrift } from './InlineDrift.js'

       export interface PhaseProgressProps { projectId: string }

       export function PhaseProgress({ projectId }: PhaseProgressProps): React.JSX.Element {
         const query = usePhaseProgress(projectId)
         if (query.error) {
           const msg = query.error.message
           if (msg.startsWith('schema_drift:')) {
             return <InlineDrift panelId="phase-progress" title="Phase Progress" path={msg.slice('schema_drift:'.length)} onRetry={() => query.refetch()} />
           }
           return <PanelContainer panelId="phase-progress" title="Phase Progress" unreachable>{null}</PanelContainer>
         }
         if (query.isLoading || !query.data) {
           return (
             <PanelContainer panelId="phase-progress" title="Phase Progress">
               <p className="text-sm text-[--text-muted]">Loading...</p>
             </PanelContainer>
           )
         }
         const { phase, paddedPhase, files } = query.data
         if (phase === null && files.length === 0) {
           return (
             <PanelContainer panelId="phase-progress" title="Phase Progress">
               <p className="text-base leading-relaxed text-[--text-muted]">
                 No phase work yet. Run /gsd-discuss-phase or /gsd-plan-phase to start.
               </p>
             </PanelContainer>
           )
         }
         return (
           <PanelContainer panelId="phase-progress" title="Phase Progress">
             {phase && (
               <p className="font-mono text-xs uppercase tracking-wide text-[--text-muted]">
                 Phase {paddedPhase ?? '??'} — {phase}
               </p>
             )}
             <ul className="flex flex-col gap-1">
               {files.map((f) => (
                 <li key={f.name} className="flex items-baseline gap-3">
                   {f.present ? (
                     <CheckCircle2 size={14} aria-hidden="true" className="text-[--success]" />
                   ) : (
                     <Minus size={14} aria-hidden="true" className="text-[--text-subtle]" />
                   )}
                   <code className={`font-mono text-sm ${f.present ? 'text-[--text]' : 'text-[--text-muted]'}`}>
                     {f.name}
                   </code>
                   {f.present && f.mtimeIso && (
                     <time dateTime={f.mtimeIso} className="font-mono text-xs text-[--text-muted]">
                       {formatRelativeTime(f.mtimeIso)}
                     </time>
                   )}
                 </li>
               ))}
             </ul>
           </PanelContainer>
         )
       }
       ```

    2. Create `packages/spa/src/components/panels/ExecutionTimeline.tsx`:
       ```typescript
       import { usePhaseProgress } from '../../lib/projectQueries.js'
       import { formatRelativeTime } from '../../lib/relativeTime.js'

       import { PanelContainer } from './PanelContainer.js'
       import { InlineDrift } from './InlineDrift.js'

       export interface ExecutionTimelineProps { projectId: string }

       export function ExecutionTimeline({ projectId }: ExecutionTimelineProps): React.JSX.Element {
         const query = usePhaseProgress(projectId)
         if (query.error) {
           const msg = query.error.message
           if (msg.startsWith('schema_drift:')) {
             return <InlineDrift panelId="execution-timeline" title="Execution Timeline" path={msg.slice('schema_drift:'.length)} onRetry={() => query.refetch()} />
           }
           return <PanelContainer panelId="execution-timeline" title="Execution Timeline" unreachable>{null}</PanelContainer>
         }
         if (query.isLoading || !query.data) {
           return (
             <PanelContainer panelId="execution-timeline" title="Execution Timeline">
               <p className="text-sm text-[--text-muted]">Loading...</p>
             </PanelContainer>
           )
         }
         const { timeline } = query.data.tdd
         if (timeline.length === 0) {
           return (
             <PanelContainer panelId="execution-timeline" title="Execution Timeline">
               <p className="text-base leading-relaxed text-[--text-muted]">
                 No TDD commits yet for this phase.
               </p>
             </PanelContainer>
           )
         }
         return (
           <PanelContainer panelId="execution-timeline" title="Execution Timeline">
             <ol className="flex flex-col gap-3">
               {timeline.map((t) => (
                 <li key={t.taskId} className="flex flex-col gap-1">
                   <p className="font-mono text-xs font-semibold uppercase tracking-wide text-[--text-muted]">
                     Task {t.taskId}
                   </p>
                   {t.redCommit && (
                     <CommitRow kind="RED" sha={t.redCommit.sha} subject={t.redCommit.subject} isoDate={t.redCommit.isoDate} pending={t.greenCommit === null} />
                   )}
                   {t.greenCommit && (
                     <CommitRow kind="GREEN" sha={t.greenCommit.sha} subject={t.greenCommit.subject} isoDate={t.greenCommit.isoDate} pending={false} />
                   )}
                 </li>
               ))}
             </ol>
           </PanelContainer>
         )
       }

       function CommitRow({ kind, sha, subject, isoDate, pending }: { kind: 'RED' | 'GREEN'; sha: string; subject: string; isoDate: string; pending: boolean }): React.JSX.Element {
         const dotClass = kind === 'RED' ? 'text-[--danger]' : 'text-[--success]'
         const subjectClass = pending ? 'font-mono text-sm text-[--text-muted]' : 'font-mono text-sm text-[--text]'
         return (
           <div className="flex items-baseline gap-2 pl-3">
             <span aria-hidden="true" className={dotClass}>●</span>
             <span className="sr-only">{kind}:</span>
             <code className={subjectClass} title={sha}>{subject}</code>
             <time dateTime={isoDate} className="font-mono text-xs text-[--text-muted]">
               {formatRelativeTime(isoDate)}
             </time>
           </div>
         )
       }
       ```

    3. Create `packages/spa/src/components/panels/ReviewStatus.tsx`:
       ```typescript
       import type { ReviewStatusPayload, ReviewFindingCounts } from '@agenticapps/dashboard-shared'

       import { usePhaseProgress } from '../../lib/projectQueries.js'

       import { PanelContainer } from './PanelContainer.js'
       import { InlineDrift } from './InlineDrift.js'

       export interface ReviewStatusProps { projectId: string }

       const SEVERITY_GLYPH: Record<keyof ReviewFindingCounts, string> = {
         critical: '🔴',
         high: '🟠',
         medium: '🟡',
         low: '⚪',
       }

       export function ReviewStatus({ projectId }: ReviewStatusProps): React.JSX.Element {
         const query = usePhaseProgress(projectId)
         if (query.error) {
           const msg = query.error.message
           if (msg.startsWith('schema_drift:')) {
             return <InlineDrift panelId="review-status" title="Review Status" path={msg.slice('schema_drift:'.length)} onRetry={() => query.refetch()} />
           }
           return <PanelContainer panelId="review-status" title="Review Status" unreachable>{null}</PanelContainer>
         }
         if (query.isLoading || !query.data) {
           return (
             <PanelContainer panelId="review-status" title="Review Status">
               <p className="text-sm text-[--text-muted]">Loading...</p>
             </PanelContainer>
           )
         }
         const { stage1, stage2 } = query.data.review
         if (stage1 === null && stage2 === null) {
           return (
             <PanelContainer panelId="review-status" title="Review Status">
               <p className="text-base leading-relaxed text-[--text-muted]">
                 No review run yet — try /review or /gsd-code-review.
               </p>
             </PanelContainer>
           )
         }
         return (
           <PanelContainer panelId="review-status" title="Review Status">
             <StageRow label="Stage 1" stage={stage1} />
             <StageRow label="Stage 2" stage={stage2} />
           </PanelContainer>
         )
       }

       function StageRow({ label, stage }: { label: string; stage: ReviewStatusPayload['stage1'] }): React.JSX.Element {
         if (stage === null) {
           return (
             <div className="flex items-baseline gap-3">
               <span className="text-sm font-semibold text-[--text]">{label}</span>
               <span className="text-sm text-[--text-muted]">not run</span>
             </div>
           )
         }
         const f = stage.findings
         const ariaLabel = `${label} findings: ${f.critical} critical, ${f.high} high, ${f.medium} medium, ${f.low} low`
         return (
           <div className="flex flex-wrap items-baseline gap-3">
             <span className="text-sm font-semibold text-[--text]">{label}</span>
             <span className="text-sm text-[--text-muted]">present</span>
             <span aria-label={ariaLabel} className="inline-flex items-baseline gap-2 font-mono text-sm">
               {(['critical', 'high', 'medium', 'low'] as const).map((sev) => (
                 <span key={sev} className="inline-flex items-baseline gap-1">
                   <span aria-hidden="true">{SEVERITY_GLYPH[sev]}</span>
                   <span className="text-[--text]">{f[sev]}</span>
                 </span>
               ))}
             </span>
           </div>
         )
       }
       ```

    4. Create the three test files covering PP1–PP6, ET1–ET6, RS1–RS7. Use the same `vi.mock('../../lib/projectQueries.js', ...)` pattern as Plan 05.

    5. RED → GREEN: write all three test files first, run, ~19 tests fail, commit `test(04-06): add failing tests for PhaseProgress + ExecutionTimeline + ReviewStatus (RED)`. Implement, rerun, commit `feat(04-06): implement PhaseProgress + ExecutionTimeline + ReviewStatus (GREEN)`.

    Notes:
      - **ExecutionTimeline emoji glyphs** are literal `●` (U+25CF). Color comes from the className. The `<span class="sr-only">RED:</span>` provides screen-reader semantics independent of color.
      - **Severity emojis** in ReviewStatus are emoji literals (🔴🟠🟡⚪). The aria-label on the parent provides screen-reader text (color is decorative).
      - **PhaseProgress mtime** uses `formatRelativeTime` (Plan 05). For files with `present: false`, no mtime is shown.
      - **All 3 panels share a single useQuery call** (usePhaseProgress) — TanStack Query dedupes identical queryKeys, so 3 panels mounting concurrently produce 1 network request per 5s window. Verify by mocking apiFetch and asserting it was called once.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run panels/PhaseProgress panels/ExecutionTimeline panels/ReviewStatus</automated>
  </verify>
  <done>
    - 3 panel files + 3 test files.
    - 19+ tests passing.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
  </done>
  <acceptance_criteria>
    - File `packages/spa/src/components/panels/PhaseProgress.tsx` exists.
    - `grep -c "usePhaseProgress(projectId)" packages/spa/src/components/panels/PhaseProgress.tsx` returns 1.
    - `grep -c "CheckCircle2" packages/spa/src/components/panels/PhaseProgress.tsx` returns ≥ 1.
    - `grep -c "Minus" packages/spa/src/components/panels/PhaseProgress.tsx` returns ≥ 1.
    - `grep -c "No phase work yet. Run /gsd-discuss-phase or /gsd-plan-phase to start." packages/spa/src/components/panels/PhaseProgress.tsx` returns 1.
    - File `packages/spa/src/components/panels/ExecutionTimeline.tsx` exists.
    - `grep -c "usePhaseProgress(projectId)" packages/spa/src/components/panels/ExecutionTimeline.tsx` returns 1.
    - `grep -c "Task {t.taskId}" packages/spa/src/components/panels/ExecutionTimeline.tsx` returns 1.
    - `grep -c "No TDD commits yet for this phase." packages/spa/src/components/panels/ExecutionTimeline.tsx` returns 1.
    - `grep -c "sr-only" packages/spa/src/components/panels/ExecutionTimeline.tsx` returns ≥ 1 (RED:/GREEN: prefix).
    - File `packages/spa/src/components/panels/ReviewStatus.tsx` exists.
    - `grep -c "usePhaseProgress(projectId)" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - `grep -c "🔴" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - `grep -c "🟠" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - `grep -c "🟡" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - `grep -c "⚪" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - `grep -c "No review run yet" packages/spa/src/components/panels/ReviewStatus.tsx` returns 1.
    - File `packages/spa/src/components/panels/PhaseProgress.test.tsx` has ≥ 6 `it(` blocks.
    - File `packages/spa/src/components/panels/ExecutionTimeline.test.tsx` has ≥ 6 `it(` blocks.
    - File `packages/spa/src/components/panels/ReviewStatus.test.tsx` has ≥ 7 `it(` blocks.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
    - `git log --oneline -3` shows `test(04-06):` then `feat(04-06):` for these panels.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SecurityStatus + VerificationStatus panels (RED then GREEN)</name>
  <files>
    packages/spa/src/components/panels/SecurityStatus.tsx (NEW),
    packages/spa/src/components/panels/SecurityStatus.test.tsx (NEW),
    packages/spa/src/components/panels/VerificationStatus.tsx (NEW),
    packages/spa/src/components/panels/VerificationStatus.test.tsx (NEW)
  </files>
  <read_first>
    - packages/spa/src/components/panels/PhaseProgress.tsx (just created — reuse the same query structure)
    - packages/spa/src/lib/projectQueries.ts (useSecurity is the dedicated hook for SecurityStatus; usePhaseProgress feeds VerificationStatus)
    - .planning/phases/04-single-project-view-discipline-phase-progress/04-UI-SPEC.md sections "SecurityStatus" + "VerificationStatus" (verbatim render contracts)
  </read_first>
  <behavior>
    SecurityStatus (PHASE-04):
    - Test SS1: with `cso: { fileName: '04-SECURITY.md', content: 'audit content' }, dbSentinel: null`, renders the cso section with heading `/cso audit (04-SECURITY.md)` and the content in a `<pre>` clamped to `max-h-32 overflow-y-auto whitespace-pre-wrap`.
    - Test SS2: also renders the `Database Sentinel` section showing `not detected` in `text-[--text-muted]`.
    - Test SS3: when both cso and dbSentinel are populated, both sections render their content.
    - Test SS4: when both are null → empty-state copy `No /cso audit yet for this phase.`.
    - Test SS5: panel title is `Security Status`. Loading + drift + unreachable behave like PhaseProgress.

    VerificationStatus (PHASE-05):
    - Test VS1: with `mustHavesTotal: 9, mustHavesEvidenced: 7, items: [...3 items, 2 evidenced]`, renders summary `7 / 9 must-haves evidenced` in `text-sm text-[--text]`.
    - Test VS2: renders 3 item rows: evidenced rows get CheckCircle2 + text in `text-[--text]`; unevidenced get Minus + text.
    - Test VS3: when `mustHavesEvidenced === mustHavesTotal && mustHavesTotal > 0`, the summary line gets class `text-[--success] font-semibold`.
    - Test VS4: when `mustHavesTotal === 0`, renders empty-state copy `No verification run yet — try /gsd-verify-work.`.
    - Test VS5: panel title is `Verification Status`.
  </behavior>
  <action>
    1. Create `packages/spa/src/components/panels/SecurityStatus.tsx`:
       ```typescript
       import { useSecurity } from '../../lib/projectQueries.js'

       import { PanelContainer } from './PanelContainer.js'
       import { InlineDrift } from './InlineDrift.js'

       export interface SecurityStatusProps { projectId: string }

       export function SecurityStatus({ projectId }: SecurityStatusProps): React.JSX.Element {
         const query = useSecurity(projectId)
         if (query.error) {
           const msg = query.error.message
           if (msg.startsWith('schema_drift:')) {
             return <InlineDrift panelId="security-status" title="Security Status" path={msg.slice('schema_drift:'.length)} onRetry={() => query.refetch()} />
           }
           return <PanelContainer panelId="security-status" title="Security Status" unreachable>{null}</PanelContainer>
         }
         if (query.isLoading || !query.data) {
           return (
             <PanelContainer panelId="security-status" title="Security Status">
               <p className="text-sm text-[--text-muted]">Loading...</p>
             </PanelContainer>
           )
         }
         const { cso, dbSentinel } = query.data
         if (cso === null && dbSentinel === null) {
           return (
             <PanelContainer panelId="security-status" title="Security Status">
               <p className="text-base leading-relaxed text-[--text-muted]">
                 No /cso audit yet for this phase.
               </p>
             </PanelContainer>
           )
         }
         return (
           <PanelContainer panelId="security-status" title="Security Status">
             <section className="flex flex-col gap-1">
               <h3 className="text-sm font-semibold text-[--text]">
                 /cso audit{cso ? ` (${cso.fileName})` : ''}
               </h3>
               {cso ? (
                 <pre className="max-h-32 overflow-y-auto whitespace-pre-wrap rounded bg-[--surface-elevated] p-3 font-mono text-xs leading-relaxed text-[--text-muted]">
                   {cso.content}
                 </pre>
               ) : (
                 <p className="text-sm text-[--text-muted]">not detected</p>
               )}
             </section>
             <section className="flex flex-col gap-1">
               <h3 className="text-sm font-semibold text-[--text]">Database Sentinel</h3>
               {dbSentinel ? (
                 <pre className="max-h-32 overflow-y-auto whitespace-pre-wrap rounded bg-[--surface-elevated] p-3 font-mono text-xs leading-relaxed text-[--text-muted]">
                   {dbSentinel.content}
                 </pre>
               ) : (
                 <p className="text-sm text-[--text-muted]">not detected</p>
               )}
             </section>
           </PanelContainer>
         )
       }
       ```

    2. Create `packages/spa/src/components/panels/VerificationStatus.tsx`:
       ```typescript
       import { CheckCircle2, Minus } from 'lucide-react'

       import { usePhaseProgress } from '../../lib/projectQueries.js'

       import { PanelContainer } from './PanelContainer.js'
       import { InlineDrift } from './InlineDrift.js'

       export interface VerificationStatusProps { projectId: string }

       export function VerificationStatus({ projectId }: VerificationStatusProps): React.JSX.Element {
         const query = usePhaseProgress(projectId)
         if (query.error) {
           const msg = query.error.message
           if (msg.startsWith('schema_drift:')) {
             return <InlineDrift panelId="verification-status" title="Verification Status" path={msg.slice('schema_drift:'.length)} onRetry={() => query.refetch()} />
           }
           return <PanelContainer panelId="verification-status" title="Verification Status" unreachable>{null}</PanelContainer>
         }
         if (query.isLoading || !query.data) {
           return (
             <PanelContainer panelId="verification-status" title="Verification Status">
               <p className="text-sm text-[--text-muted]">Loading...</p>
             </PanelContainer>
           )
         }
         const { mustHavesTotal, mustHavesEvidenced, items } = query.data.verification
         if (mustHavesTotal === 0) {
           return (
             <PanelContainer panelId="verification-status" title="Verification Status">
               <p className="text-base leading-relaxed text-[--text-muted]">
                 No verification run yet — try /gsd-verify-work.
               </p>
             </PanelContainer>
           )
         }
         const allEvidenced = mustHavesEvidenced === mustHavesTotal
         const summaryClass = allEvidenced
           ? 'text-sm font-semibold text-[--success]'
           : 'text-sm text-[--text]'
         return (
           <PanelContainer panelId="verification-status" title="Verification Status">
             <p className={summaryClass}>
               {mustHavesEvidenced} / {mustHavesTotal} must-haves evidenced
             </p>
             <ul className="flex flex-col gap-1">
               {items.map((item, i) => (
                 <li key={`${item.text}-${i}`} className="flex items-baseline gap-2">
                   {item.evidenced ? (
                     <CheckCircle2 size={14} aria-hidden="true" className="text-[--success]" />
                   ) : (
                     <Minus size={14} aria-hidden="true" className="text-[--text-subtle]" />
                   )}
                   <span className={`text-sm ${item.evidenced ? 'text-[--text]' : 'text-[--text-muted]'}`}>
                     {item.text}
                   </span>
                 </li>
               ))}
             </ul>
           </PanelContainer>
         )
       }
       ```

    3. Create the two test files covering SS1–SS5, VS1–VS5.

    4. RED → GREEN: write tests, run, ~10 tests fail, commit `test(04-06): add failing tests for SecurityStatus + VerificationStatus (RED)`. Implement, rerun, commit `feat(04-06): implement SecurityStatus + VerificationStatus (GREEN)`.

    Notes:
      - `max-h-32` (128px) clamps the cso/dbSentinel content per UI-SPEC. Overflow scrolls vertically.
      - VerificationStatus uses `usePhaseProgress`, NOT a separate `useVerification`. The /phase-progress route bundles verification per D-4-01 design.
      - SecurityStatus is the ONLY Phase 4 panel using `useSecurity` — separate route per D-4-01 to keep the audit content cache key independent of the bulkier phase-progress payload.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run panels/SecurityStatus panels/VerificationStatus</automated>
  </verify>
  <done>
    - 2 panel files + 2 test files.
    - 10+ tests passing.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
  </done>
  <acceptance_criteria>
    - File `packages/spa/src/components/panels/SecurityStatus.tsx` exists.
    - `grep -c "useSecurity(projectId)" packages/spa/src/components/panels/SecurityStatus.tsx` returns 1.
    - `grep -c "/cso audit" packages/spa/src/components/panels/SecurityStatus.tsx` returns 1.
    - `grep -c "Database Sentinel" packages/spa/src/components/panels/SecurityStatus.tsx` returns 1.
    - `grep -c "max-h-32" packages/spa/src/components/panels/SecurityStatus.tsx` returns ≥ 1.
    - `grep -c "No /cso audit yet" packages/spa/src/components/panels/SecurityStatus.tsx` returns 1.
    - File `packages/spa/src/components/panels/VerificationStatus.tsx` exists.
    - `grep -c "usePhaseProgress(projectId)" packages/spa/src/components/panels/VerificationStatus.tsx` returns 1.
    - `grep -c "must-haves evidenced" packages/spa/src/components/panels/VerificationStatus.tsx` returns 1.
    - `grep -c "No verification run yet" packages/spa/src/components/panels/VerificationStatus.tsx` returns 1.
    - File `packages/spa/src/components/panels/SecurityStatus.test.tsx` has ≥ 5 `it(` blocks.
    - File `packages/spa/src/components/panels/VerificationStatus.test.tsx` has ≥ 5 `it(` blocks.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
    - `git log --oneline -3` shows `test(04-06):` then `feat(04-06):` for these panels.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Mount the 5 Phase Progress panels in SingleProjectView + e2e route test</name>
  <files>
    packages/spa/src/components/SingleProjectView.tsx (MODIFY: replace 5 data-slot divs with real panel components),
    packages/spa/src/components/SingleProjectView.test.tsx (MODIFY: assert all 8 panels mounted),
    packages/spa/src/__tests__/projects-detail-e2e.test.tsx (NEW)
  </files>
  <read_first>
    - packages/spa/src/components/SingleProjectView.tsx (after Plan 05: discipline-column has real components, phase-progress-column has 5 `<div data-slot="..." />` placeholders)
    - packages/spa/src/components/SingleProjectView.test.tsx (existing tests; update to assert all 8 panel regions exist)
    - packages/spa/src/__tests__/register-optimistic.test.ts (Phase 3 e2e-style integration test — replicate its setup pattern: QueryClient, MemoryHistory, RouterProvider, fetch mock)
    - .planning/ROADMAP.md Phase 4 success criteria 1–5 (the e2e test must assert criterion 1 explicitly)
  </read_first>
  <action>
    1. In `packages/spa/src/components/SingleProjectView.tsx`:
       - Add 5 imports next to the Plan 05 imports:
         ```typescript
         import { PhaseProgress } from './panels/PhaseProgress.js'
         import { ExecutionTimeline } from './panels/ExecutionTimeline.js'
         import { ReviewStatus } from './panels/ReviewStatus.js'
         import { SecurityStatus } from './panels/SecurityStatus.js'
         import { VerificationStatus } from './panels/VerificationStatus.js'
         ```
       - Replace the 5 `<div data-slot="..." />` placeholders in the phase-progress-column with the real components:
         ```tsx
         <PhaseProgress projectId={projectId} />
         <ExecutionTimeline projectId={projectId} />
         <ReviewStatus projectId={projectId} />
         <SecurityStatus projectId={projectId} />
         <VerificationStatus projectId={projectId} />
         ```

    2. In `packages/spa/src/components/SingleProjectView.test.tsx`:
       - Update the existing tests so the no-more-slot-divs assertions cover all 8 panels.
       - Add a test that asserts all 8 panel `<region>` elements (one per PanelContainer) are present after mock-loading.

    3. Create `packages/spa/src/__tests__/projects-detail-e2e.test.tsx`:
       - Setup: QueryClient with retry:false, MemoryHistory at `/projects/acme`, mock `fetch` to return canned daemon responses for `/api/registry`, `/api/projects/acme/overview`, `/api/projects/acme/commitment`, `/api/projects/acme/observations/recent?limit=20`, `/api/projects/acme/discipline`, `/api/projects/acme/phase-progress`, `/api/projects/acme/security`. Stub `getPairing` to return a fixed `{ agentUrl, token }`.
       - Use the SAME router structure as in `routes.tsx` (or import the route declarations from there). Render `<RouterProvider router={...} />` inside `<QueryClientProvider />`.
       - Test E2E1 (ROADMAP success criterion 1): `await screen.findByRole('navigation', { name: 'Project breadcrumb' })` proves the header rendered.
       - Test E2E2: `await screen.findByRole('region', { name: 'Commitment' })` and the 7 other panel titles all appear (Hook Firings, Rationalization Fires, Phase Progress, Execution Timeline, Review Status, Security Status, Verification Status).
       - Test E2E3 (ROADMAP success criterion 2): when commitment response includes a markdown block, the panel renders the markdown.
       - Test E2E4 (ROADMAP success criterion 3): when phase-progress response includes a TDD timeline entry with red+green, ExecutionTimeline renders both.
       - Test E2E5 (ROADMAP success criterion 4): when phase-progress response includes Stage 1 with finding counts, ReviewStatus renders all 4 severity glyphs.
       - Test E2E6 (ROADMAP success criterion 5): when phase-progress response includes verification, VerificationStatus shows `7 / 9 must-haves evidenced`.
       - Test E2E7 (cross-project leakage guard): mount with `/projects/acme`, then re-render with `/projects/beta`; assert that the queryClient cache has SEPARATE entries (`getQueryData(['commitment', 'acme'])` !== `getQueryData(['commitment', 'beta'])`).

    4. NOT TDD per task — this is integration verification. Commit message: `feat(04-06): mount Phase Progress panels in SingleProjectView + e2e route test`.

    Notes:
      - The e2e test file lives in `packages/spa/src/__tests__/` (alongside Phase 3's `register-optimistic.test.ts`) — that directory is the convention for cross-component tests.
      - The e2e test does NOT spawn a real daemon. It mocks `fetch` at the module level. The pairing helper is mocked via `vi.mock('../lib/pairing.js', ...)`.
      - Test E2E7 uses `qc.getQueryData(...)` after `await screen.findByRole(...)` to ensure both fetches resolved before asserting.
      - The 5 ROADMAP success criteria from the planning_context are explicitly verified one-per-test (E2E1..E2E6 cover criteria 1-5; E2E7 is a bonus security check on cross-project leakage).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run SingleProjectView projects-detail-e2e</automated>
  </verify>
  <done>
    - SingleProjectView mounts all 8 panels.
    - SingleProjectView.test.tsx asserts all 8 panel regions.
    - projects-detail-e2e.test.tsx covers ROADMAP success criteria 1-5 + cross-project leakage guard.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
    - `pnpm -r typecheck` exits 0.
    - `pnpm -r build` exits 0.
    - `pnpm lint` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "<PhaseProgress projectId=" packages/spa/src/components/SingleProjectView.tsx` returns 1.
    - `grep -c "<ExecutionTimeline projectId=" packages/spa/src/components/SingleProjectView.tsx` returns 1.
    - `grep -c "<ReviewStatus projectId=" packages/spa/src/components/SingleProjectView.tsx` returns 1.
    - `grep -c "<SecurityStatus projectId=" packages/spa/src/components/SingleProjectView.tsx` returns 1.
    - `grep -c "<VerificationStatus projectId=" packages/spa/src/components/SingleProjectView.tsx` returns 1.
    - `grep -c "data-slot=\"phase-progress\"" packages/spa/src/components/SingleProjectView.tsx` returns 0.
    - `grep -c "data-slot=\"execution-timeline\"" packages/spa/src/components/SingleProjectView.tsx` returns 0.
    - `grep -c "data-slot=\"review-status\"" packages/spa/src/components/SingleProjectView.tsx` returns 0.
    - `grep -c "data-slot=\"security-status\"" packages/spa/src/components/SingleProjectView.tsx` returns 0.
    - `grep -c "data-slot=\"verification-status\"" packages/spa/src/components/SingleProjectView.tsx` returns 0.
    - `grep -c "data-slot=" packages/spa/src/components/SingleProjectView.tsx` returns 0 (all 8 slots replaced).
    - File `packages/spa/src/__tests__/projects-detail-e2e.test.tsx` exists.
    - `grep -c "Project breadcrumb" packages/spa/src/__tests__/projects-detail-e2e.test.tsx` returns ≥ 1 (header assertion).
    - `grep -c "Commitment" packages/spa/src/__tests__/projects-detail-e2e.test.tsx` returns ≥ 1.
    - `grep -c "must-haves evidenced" packages/spa/src/__tests__/projects-detail-e2e.test.tsx` returns ≥ 1.
    - `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0.
    - `pnpm -r typecheck` exits 0.
    - `pnpm -r build` exits 0.
    - `pnpm lint` exits 0.
    - `pnpm -r test` exits 0 (full workspace green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon → SPA panel | Same as Plan 05 (text interpolation auto-escaping). Phase Progress panels render: filenames, commit subjects, audit content. None of these reach a raw-HTML sink. |
| Raw audit content → `<pre>` | SecurityStatus renders cso/dbSentinel content in `<pre>`. React text interpolation escapes HTML chars. No XSS surface. |
| Cross-project leakage | E2E test E2E7 explicitly asserts that switching projects produces independent cache entries — no leakage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | T (Tampering) | Hostile content in cso `content` field | mitigate | Daemon caps content at 4096 chars (Plan 02). React text interpolation escapes HTML. `<pre>` preserves whitespace but does not interpret HTML. |
| T-04-06-02 | T (Tampering) | Hostile commit subject in ExecutionTimeline | mitigate | React text interpolation. Commit subjects are bounded by git's 72-char-by-convention; even longer subjects render safely. |
| T-04-06-03 | I (Information Disclosure) | Cross-project cache leak when navigating /projects/acme → /projects/beta | mitigate | E2E test E2E7 asserts two query cache entries are independent. queryKey includes projectId (Plan 04 T-04-04-01). |
| T-04-06-04 | D (Denial of Service) | Massive PhaseProgress.files array overwhelming the panel | accept | Daemon-side bounded: phase directory has < 30 files in practice. PhaseProgress renders without virtualisation. UI-SPEC explicitly waived virtualisation. |
| T-04-06-05 | D (Denial of Service) | ExecutionTimeline with hundreds of task groups | accept | Phase prefix filter (Plan 02) bounds entries to commits matching `04-NN`. A 100-task phase is the realistic ceiling. Panel renders without virtualisation. |
| T-04-06-06 | T (Tampering) | The shared InlineDrift refactor — Plan 05 panels' tests must still pass after extraction | mitigate | Refactor preserves call sites verbatim. Plan 05 tests run after the refactor as a regression check. |
| T-04-06-07 | I (Information Disclosure) | document.title set in SingleProjectView includes projectId | accept | Browser-local; no network exposure. projectId is the user's own slug. |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test --run` exits 0 with ≥ 40 new tests across 7 new files + 2 modified.
- `pnpm -r typecheck` exits 0.
- `pnpm -r build` exits 0.
- `pnpm lint` exits 0.
- `pnpm -r test` exits 0 (FULL workspace green — all phases).
- TDD evidence: `git log --oneline | grep -E "^[a-f0-9]+ (test|feat|refactor)\(04-06\):"` returns ≥ 8 lines.
- Manual: `pnpm --filter @agenticapps/dashboard-spa dev` + daemon. Click any project. Detail page shows all 8 panels. Resize the window — left column reflows below center on narrow viewports (D-4-09 grid behavior).
- ROADMAP success criteria 1-5 verified by E2E test cases E2E1–E2E6.
- pre-PR checklist (per CLAUDE.md memory): `pnpm -r typecheck && pnpm -r test && pnpm -r build && pnpm lint` all green.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion 1 (clicking a card navigates to /projects/{id} and renders header + left + center columns) — verified by E2E1.
- ROADMAP success criterion 2 (CommitmentBlock surfaces the most recent `## Workflow commitment` block) — verified by E2E3.
- ROADMAP success criterion 3 (ExecutionTimeline parses test(RED)/feat(GREEN) commit pairs grouped per task) — verified by E2E4.
- ROADMAP success criterion 4 (ReviewStatus parses `<finding severity="...">` and renders by severity) — verified by E2E5.
- ROADMAP success criterion 5 (VerificationStatus shows must_haves count vs evidence count) — verified by E2E6.
- All 9 Phase 4 requirement IDs (DISC-01..04, PHASE-01..05) addressed across Plans 01–06.
- Cross-project leakage prevented (E2E7).
- No new dependencies. No native deps. No raw-HTML sinks introduced.
- Phase 4 ships.
</success_criteria>

<output>
After completion, create `.planning/phases/04-single-project-view-discipline-phase-progress/04-06-SUMMARY.md` documenting:
- Each panel's render states (~5 per panel × 5 panels = 25 paths) — confirm tests cover all.
- Empty-state copy verbatim — confirm matches UI-SPEC.
- The shared InlineDrift extraction with refactor commit + Plan 05 panel cleanup.
- TDD commit pairs: list each `test(04-06):` → `feat(04-06):` pair with shas; plus the `refactor(04-06):` extraction commit.
- The 5 ROADMAP success criteria → which test in `projects-detail-e2e.test.tsx` verifies each.
- Cross-project leakage guard (E2E7) result.
- Phase 4 ship checklist:
  - [ ] All 6 plans land
  - [ ] Full workspace test green: `pnpm -r test`
  - [ ] Typecheck green: `pnpm -r typecheck`
  - [ ] Build green: `pnpm -r build`
  - [ ] Lint green: `pnpm lint`
  - [ ] /review (Stage 1) ran
  - [ ] superpowers:requesting-code-review (Stage 2) ran
  - [ ] /cso ran (5 new HTTP read routes added)
  - [ ] /qa ran (dev server smoke)
  - [ ] HUMAN-UAT.md created and items 1-7 from RESEARCH §UAT acknowledged
- Phase 5 (Skills + Health Panels) prerequisites confirmed: SingleProjectView's grid is ready to widen to `grid-cols-[1fr_1.5fr_1fr]`; the 8 panel components and the shared PanelContainer + InlineDrift are reusable.
</output>
