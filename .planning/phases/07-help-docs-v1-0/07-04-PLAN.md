---
phase: 07
plan: 04
type: execute
wave: 2
title: "Wave 2 page content: 5 anchor MDX + reference/shortcuts MDX + content validation"
depends_on: ["07-01", "07-02", "07-03"]
autonomous: true
requirements:
  - HELP-01
  - HELP-06
files_modified:
  - packages/spa/src/help/pages/landing.mdx
  - packages/spa/src/help/pages/workflow/overview.mdx
  - packages/spa/src/help/pages/repos/overview.mdx
  - packages/spa/src/help/pages/observability/overview.mdx
  - packages/spa/src/help/pages/operations/install.mdx
  - packages/spa/src/help/pages/reference/shortcuts.mdx
  - packages/spa/src/help/__tests__/anchor-pages.test.tsx
  - packages/spa/src/help/__tests__/frontmatter.test.tsx
  - packages/spa/src/help/__tests__/mermaid-syntax.test.ts
  - packages/spa/src/help/__tests__/reference-shortcuts.test.tsx
must_haves:
  truths:
    - "5 anchor .mdx files exist at packages/spa/src/help/pages/{landing,workflow/overview,repos/overview,observability/overview,operations/install}.mdx"
    - "reference/shortcuts.mdx exists with frontmatter and renders the KbdHint keyboard-shortcuts table"
    - "Each MDX file's frontmatter has the {slug, title, order, section} shape and types check against mdx.d.ts"
    - "Every triple-backtick mermaid fence has been converted to <MermaidBlock code={`...`} /> JSX inside the MDX"
    - "Every <HelpWidget name=\"...\" /> JSX references one of the 8 stub names; unknown widget references fail typecheck"
    - "mermaid.parse() validates every Mermaid block at test time (no syntax errors)"
  artifacts:
    - path: "packages/spa/src/help/pages/landing.mdx"
      provides: "Welcome page with intro + Mermaid flowchart + RepoTopologyMap widget + section links"
      contains: "frontmatter"
    - path: "packages/spa/src/help/pages/workflow/overview.mdx"
      provides: "Workflow overview with 1 Mermaid block"
    - path: "packages/spa/src/help/pages/repos/overview.mdx"
      provides: "Repos overview with 1 RepoTopologyMap widget + 1 Mermaid"
    - path: "packages/spa/src/help/pages/observability/overview.mdx"
      provides: "Observability overview with 2 Mermaid blocks + 1 ScanReportPlayground widget"
    - path: "packages/spa/src/help/pages/operations/install.mdx"
      provides: "Operations install with 1 MigrationDryRun widget"
    - path: "packages/spa/src/help/pages/reference/shortcuts.mdx"
      provides: "HELP-06: keyboard shortcuts table using <KbdHint> via explicit MDX import"
  key_links:
    - from: "packages/spa/src/help/pages/repos/overview.mdx"
      to: "HelpWidget name='RepoTopologyMap'"
      via: "MDX JSX (resolved by MDXProvider at render)"
      pattern: "<HelpWidget name=\"RepoTopologyMap\""
    - from: "packages/spa/src/help/pages/observability/overview.mdx"
      to: "HelpWidget name='ScanReportPlayground'"
      via: "MDX JSX"
      pattern: "<HelpWidget name=\"ScanReportPlayground\""
    - from: "packages/spa/src/help/pages/operations/install.mdx"
      to: "HelpWidget name='MigrationDryRun'"
      via: "MDX JSX"
      pattern: "<HelpWidget name=\"MigrationDryRun\""
---

<objective>
Copy the 5 anchor `.md` source pages from `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/{landing,workflow/overview,repos/overview,observability/overview,operations/install}.md` to `packages/spa/src/help/pages/**/*.mdx`, preserving frontmatter and content. **Convert every triple-backtick `mermaid` fence to `<MermaidBlock code={...}/>` JSX** (because the `pre`-mapping in mdxComponents lands in Plan 07-05, not here — keeping content authoring decoupled from route wiring). Author the new `reference/shortcuts.mdx` from the existing `routes/help.lazy.tsx` content (KbdHint table + common tasks) with HELP-06 frontmatter. Validate every MDX file: frontmatter shape, Mermaid syntax (via `mermaid.parse()`), MDX module imports successfully, and `<HelpWidget>` JSX names resolve to known stubs at typecheck time.

Purpose: HELP-01 (5 anchor pages) and HELP-06 (shortcuts content migrated) satisfied at the content level. Plan 07-05 wires routes that import these MDX modules.

Output: 6 MDX files + 4 vitest test files. Estimated context cost: 35-40% (content copy is bulk; validation is logical).

**MDX import strategy for Mermaid:** Plan 07-04 explicitly uses `<MermaidBlock code={...} />` JSX instead of relying on triple-backtick `mermaid` fences. Rationale: MDX fences default to `<pre><code className="language-mermaid">{...}</code></pre>`. Wiring a custom `pre` MDX component to detect `language-mermaid` and render `<MermaidBlock>` is Plan 07-05's responsibility (when finalising `mdxComponents.ts`). Plan 07-04 chooses the simpler path — JSX inline — to keep this plan focused on content (not pipeline wiring). The trade-off: source .md files have to be hand-edited at copy time to extract code-fenced mermaid into JSX. This is a 5-fold cost (5 mermaid blocks across 4 anchor MDX files); acceptable given the alternative complicates Plan 07-05.
</objective>

<read_first>
Plan-level reference list:

- `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` — D-7-01 (HELP-06 shortcuts content), D-7-15 (content tests), D-7-17 (verification evidence).
- `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §Pitfall 6 (Mermaid syntax validation), §Phase Requirements (HELP-01 covers all 5 anchors + GFM + Mermaid).
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/landing.md` — anchor 1 source.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/workflow/overview.md` — anchor 2 source.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/repos/overview.md` — anchor 3 source.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/observability/overview.md` — anchor 4 source.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/operations/install.md` — anchor 5 source.
- `packages/spa/src/routes/help.lazy.tsx` — current shortcuts content (to be moved into MDX).
- `packages/spa/src/components/ui/KbdHint.tsx` — referenced from reference/shortcuts.mdx via JSX import.
- `packages/spa/src/help/mdxComponents.ts` — has HelpWidget, HelpHook, ComingSoon, MermaidBlock (Plan 07-02). Plan 07-05 adds KbdHint.
- `packages/spa/src/help/mdx.d.ts` (Plan 07-01) — frontmatter shape contract.
</read_first>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/agenticapps-workflow/skill/SKILL.md
</execution_context>

<context>
@.planning/phases/07-help-docs-v1-0/07-CONTEXT.md
@.planning/phases/07-help-docs-v1-0/07-RESEARCH.md
@packages/spa/src/routes/help.lazy.tsx
@packages/spa/src/components/ui/KbdHint.tsx
</context>

<tasks>

<task id="07-04-T1" type="auto" tdd="false">
  <name>Task 1: Create directory tree + copy 5 anchor MDX files with frontmatter + JSX-converted Mermaid</name>
  <files>packages/spa/src/help/pages/landing.mdx, packages/spa/src/help/pages/workflow/overview.mdx, packages/spa/src/help/pages/repos/overview.mdx, packages/spa/src/help/pages/observability/overview.mdx, packages/spa/src/help/pages/operations/install.mdx</files>
  <read_first>
    - All 5 source `.md` files (full paths above).
    - The frontmatter in each (every source already has `{ slug, title, order, section }`).
  </read_first>
  <behavior>
    - Directory tree: `packages/spa/src/help/pages/{workflow,repos,observability,operations,reference}/` created.
    - Each `.md` source copied to `.mdx` at the corresponding subpath:
      - `landing.md` → `pages/landing.mdx`
      - `workflow/overview.md` → `pages/workflow/overview.mdx`
      - `repos/overview.md` → `pages/repos/overview.mdx`
      - `observability/overview.md` → `pages/observability/overview.mdx`
      - `operations/install.md` → `pages/operations/install.mdx`
    - Frontmatter preserved verbatim. (remark-mdx-frontmatter parses YAML frontmatter from MDX with leading `---`.)
    - Every triple-backtick `mermaid` fence block is REPLACED with `<MermaidBlock code={...} />` JSX:
      - Use a TEMPLATE LITERAL string for the `code` prop so multi-line mermaid source survives without escaping.
      - **`HelpWidget` JSX is already MDX-compatible — leave it untouched.** Source: `<HelpWidget name="RepoTopologyMap" />`.
    - All inline links `[Workflow Overview](/help/workflow/overview)` etc. stay as-is (MDX GFM resolves them to `<a>` tags by default; that's fine for v1.0 because the SPA router will catch the navigation event at click and route via History API. If we observe full-page reloads in Plan 07-05 e2e, we'll add `a: ({ href, children }) => <Link to={href}>{children}</Link>` to mdxComponents in 07-05. Not in scope for this plan.)
  </behavior>
  <action>
**Step 1: Create the directory tree.**

```bash
mkdir -p packages/spa/src/help/pages/workflow
mkdir -p packages/spa/src/help/pages/repos
mkdir -p packages/spa/src/help/pages/observability
mkdir -p packages/spa/src/help/pages/operations
mkdir -p packages/spa/src/help/pages/reference
```

**Step 2: Copy each .md to its .mdx target.** For each of the 5 anchor files, use the Read tool to inspect the source, then the Write tool to create the .mdx. Do NOT use a shell copy because we have to transform Mermaid fences inline.

**Anchor 1: landing.mdx**

Read `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/landing.md`.

The source contains one Mermaid block (`flowchart LR` with the Idea → Brainstorm → Plan → Execute → Verify → Review → Ship pipeline + 4 gate nodes + classDef styles).

Write `packages/spa/src/help/pages/landing.mdx` with:
- The verbatim frontmatter from the source.
- The verbatim prose content.
- The Mermaid block REPLACED with `<MermaidBlock code={...} />` JSX (use ES2015 template literal for the code prop value).
- The existing `<HelpWidget name="RepoTopologyMap" />` JSX preserved.

**Anchor 2: workflow/overview.mdx**

Read `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/workflow/overview.md`. Has 1 mermaid block. Transform per above. Write to `packages/spa/src/help/pages/workflow/overview.mdx`.

**Anchor 3: repos/overview.mdx**

Read `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/repos/overview.md`. Has 1 mermaid block AND 1 `<HelpWidget name="RepoTopologyMap" />` JSX. Transform mermaid; preserve HelpWidget. Write.

**Anchor 4: observability/overview.mdx**

Read `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/observability/overview.md`. Has 2 mermaid blocks AND 1 `<HelpWidget name="ScanReportPlayground" />`. Transform both mermaid blocks; preserve HelpWidget. Write.

**Anchor 5: operations/install.mdx**

Read `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/operations/install.md`. Has 0 mermaid blocks AND 1 `<HelpWidget name="MigrationDryRun" />`. Preserve HelpWidget. Write.

**Note on backticks in template literals:** if any mermaid block contains a literal backtick (none of the 5 blocks do — verified by grep), the executor must escape it. Otherwise template literal works as-is.

**Commit:** `feat(07-04): 5 anchor MDX pages with JSX-converted Mermaid blocks`.
  </action>
  <verify>
    <automated>for f in landing.mdx workflow/overview.mdx repos/overview.mdx observability/overview.mdx operations/install.mdx; do test -f "packages/spa/src/help/pages/${f}" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 .mdx files exist at the documented paths
    - Each has the YAML frontmatter starting with `---` on line 1 and a `slug:` line
    - Zero triple-backtick mermaid fences remain (all converted to JSX): `! grep -rF 'mermaid' packages/spa/src/help/pages/ | grep -F 'language-mermaid'` exit 0 AND `! grep -l '^.*mermaid$' packages/spa/src/help/pages/*.mdx packages/spa/src/help/pages/**/*.mdx | head -1` (no fence syntax remains)
    - The 3 HelpWidget JSX references are present at the right files:
      - `grep -q '<HelpWidget name="RepoTopologyMap"' packages/spa/src/help/pages/landing.mdx` OR same on `repos/overview.mdx` exit 0
      - `grep -q '<HelpWidget name="ScanReportPlayground"' packages/spa/src/help/pages/observability/overview.mdx` exit 0
      - `grep -q '<HelpWidget name="MigrationDryRun"' packages/spa/src/help/pages/operations/install.mdx` exit 0
    - `MermaidBlock` JSX appears in the 4 mermaid-bearing files: `grep -l '<MermaidBlock' packages/spa/src/help/pages/landing.mdx packages/spa/src/help/pages/workflow/overview.mdx packages/spa/src/help/pages/repos/overview.mdx packages/spa/src/help/pages/observability/overview.mdx | wc -l` returns 4
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exit 0 (proves mdx.d.ts ambient declaration + remark-mdx-frontmatter shape match)
    - `pnpm --filter @agenticapps/dashboard-spa build` exit 0 (proves MDX modules compile end-to-end)
  </acceptance_criteria>
  <done>5 anchor MDX files in place; Mermaid converted to JSX; HelpWidget references preserved; build green.</done>
</task>

<task id="07-04-T2" type="auto" tdd="false">
  <name>Task 2: Author reference/shortcuts.mdx from existing /help route content (HELP-06)</name>
  <files>packages/spa/src/help/pages/reference/shortcuts.mdx</files>
  <read_first>
    - `packages/spa/src/routes/help.lazy.tsx` — current keyboard shortcuts content (lines 17-62 of help.lazy.tsx).
    - `packages/spa/src/components/ui/KbdHint.tsx` — the `<KbdHint keys="..." />` component.
    - D-7-15 — reference/shortcuts test spec.
  </read_first>
  <behavior>
    - File: `packages/spa/src/help/pages/reference/shortcuts.mdx`.
    - Frontmatter: `{ slug: '/help/reference/shortcuts', title: 'Keyboard shortcuts', order: 0, section: 'reference' }`.
    - Body: introductory paragraph + the keyboard shortcuts table + the "Common tasks" list, with `<KbdHint keys="..." />` JSX for each shortcut.
    - **Imports:** at the top of the MDX (after frontmatter), import KbdHint explicitly: `import { KbdHint } from '../../../components/ui/KbdHint.js'`. This is a defensive measure — Plan 07-05 finalises mdxComponents with `KbdHint` so the explicit import becomes redundant; until then the explicit import keeps the page renderable in isolation tests.
    - MDX-side: tables use GFM syntax (since remark-gfm is in the chain); `<KbdHint>` JSX is inline inside table cells.
  </behavior>
  <action>
Create `packages/spa/src/help/pages/reference/shortcuts.mdx` with the following content (substitute `===YAML===` with `---` when writing the actual file — the marker is used here to avoid confusing the planner's frontmatter parser):

```mdx
===YAML===
slug: /help/reference/shortcuts
title: Keyboard shortcuts
order: 0
section: reference
===YAML===

import { KbdHint } from '../../../components/ui/KbdHint.js'

# Keyboard shortcuts

Reference for every keyboard shortcut wired into the dashboard. The `?` shortcut from anywhere on a paired dashboard route lands you on this docs site's landing page.

## Global shortcuts

| Keys | Action |
|------|--------|
| <KbdHint keys="R" /> | Refresh the current view's data |
| <KbdHint keys="?" /> | Open the docs landing page (`/help`) |
| <KbdHint keys="/" /> | Focus the search box (home page only) |
| <KbdHint keys="Cmd" /> + <KbdHint keys="K" /> | Open the command palette |

## Common tasks

- **Pair a new device** — Settings → Manual pair
- **Refresh project state** — press <KbdHint keys="R" />
- **View keyboard shortcuts** — press <KbdHint keys="?" /> (lands on docs landing; click "Keyboard shortcuts" in the Reference sidebar)
- **Open the command palette** — press <KbdHint keys="Cmd" /> + <KbdHint keys="K" />

## When shortcuts don't fire

- **You're inside an input field.** Shortcut hook skips when the focused element is `<input>`, `<textarea>`, `<select>`, or contenteditable — typing "r" in the search box doesn't refresh.
- **A modifier is held.** Cmd-R / Ctrl-R always reloads the browser; the shortcut hook bails on `metaKey`/`ctrlKey`/`altKey`/`shiftKey`.
- **You're on `/help/*` itself.** The docs site has its own chrome (separate layout); the global shortcut hook is mounted on the dashboard chrome only. Inside `/help/*`, no global shortcuts fire — use the in-page links instead. (v1.1 may add `?` to scroll-to-top within `/help/*`.)
```

Commit: `feat(07-04): reference/shortcuts.mdx (HELP-06 — migrated keyboard shortcuts from legacy /help)`.
  </action>
  <verify>
    <automated>test -f packages/spa/src/help/pages/reference/shortcuts.mdx && pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `head -6 packages/spa/src/help/pages/reference/shortcuts.mdx | grep -q "slug: /help/reference/shortcuts"` exit 0
    - `head -6 packages/spa/src/help/pages/reference/shortcuts.mdx | grep -q "section: reference"` exit 0
    - `head -6 packages/spa/src/help/pages/reference/shortcuts.mdx | grep -q "order: 0"` exit 0
    - `grep -q "import { KbdHint }" packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `grep -q "<KbdHint keys=\"R\"" packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `grep -q "<KbdHint keys=\"?\"" packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `grep -q "<KbdHint keys=\"/\"" packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `grep -q "<KbdHint keys=\"Cmd\"" packages/spa/src/help/pages/reference/shortcuts.mdx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa build` exit 0
  </acceptance_criteria>
  <done>HELP-06 content migrated; KbdHint imported; MDX builds clean.</done>
</task>

<task id="07-04-T3" type="auto" tdd="true">
  <name>Task 3: Frontmatter shape validation test (table-driven across 6 MDX files)</name>
  <files>packages/spa/src/help/__tests__/frontmatter.test.tsx</files>
  <read_first>
    - All 6 MDX files just authored.
    - `packages/spa/src/help/mdx.d.ts` — frontmatter shape contract.
  </read_first>
  <behavior>
    - Test imports `frontmatter` named export from each of the 6 MDX files.
    - Asserts each `frontmatter.slug` / `.title` / `.order` / `.section` match expected values from the source.
    - Covers HELP-01 (5 anchor pages) + HELP-06 (shortcuts page).
  </behavior>
  <action>
Create `packages/spa/src/help/__tests__/frontmatter.test.tsx`:

```tsx
/**
 * Plan 07-04 Task 3 — frontmatter shape validation.
 *
 * Imports the named `frontmatter` export from each anchor MDX + shortcuts MDX
 * and asserts the shape matches what HelpLayout's breadcrumb and document.title
 * code (Plan 07-05) will read.
 *
 * @see .planning/phases/07-help-docs-v1-0/07-CONTEXT.md D-7-04 (remark-mdx-frontmatter)
 */
import { describe, it, expect } from 'vitest'

import { frontmatter as landingFm } from '../pages/landing.mdx'
import { frontmatter as workflowFm } from '../pages/workflow/overview.mdx'
import { frontmatter as reposFm } from '../pages/repos/overview.mdx'
import { frontmatter as obsFm } from '../pages/observability/overview.mdx'
import { frontmatter as opsFm } from '../pages/operations/install.mdx'
import { frontmatter as shortcutsFm } from '../pages/reference/shortcuts.mdx'

const CASES = [
  ['landing', landingFm, '/help', 'landing'],
  ['workflow/overview', workflowFm, '/help/workflow/overview', 'workflow'],
  ['repos/overview', reposFm, '/help/repos/overview', 'repos'],
  ['observability/overview', obsFm, '/help/observability/overview', 'observability'],
  ['operations/install', opsFm, '/help/operations/install', 'operations'],
  ['reference/shortcuts', shortcutsFm, '/help/reference/shortcuts', 'reference'],
] as const

describe('MDX frontmatter shape', () => {
  it.each(CASES)('%s frontmatter has slug=%s section=%s and a non-empty title', (_name, fm, expectedSlug, expectedSection) => {
    expect(fm.slug).toBe(expectedSlug)
    expect(fm.section).toBe(expectedSection)
    expect(typeof fm.title).toBe('string')
    expect(fm.title.length).toBeGreaterThan(0)
    expect(typeof fm.order).toBe('number')
  })

  it('reference/shortcuts frontmatter title is "Keyboard shortcuts" (HELP-06)', () => {
    expect(shortcutsFm.title).toBe('Keyboard shortcuts')
  })
})
```

Run: `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/frontmatter.test.tsx`. Expect green (the MDX modules already exist from Tasks 1 + 2). Commit `test(07-04): frontmatter shape validation (6 MDX files)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/frontmatter.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/__tests__/frontmatter.test.tsx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/frontmatter.test.tsx` exit 0, reports 7 cases pass (6 table rows + 1 named test)
  </acceptance_criteria>
  <done>Frontmatter shape validated across all 6 MDX files.</done>
</task>

<task id="07-04-T4" type="auto" tdd="true">
  <name>Task 4: Mermaid syntax validation test — node-side, mermaid.parse() over all 4 mermaid-bearing files</name>
  <files>packages/spa/src/help/__tests__/mermaid-syntax.test.ts</files>
  <read_first>
    - `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §Pitfall 6 (Mermaid syntax error crashes "no console errors" invariant).
    - The 4 mermaid-bearing MDX files: `landing.mdx`, `workflow/overview.mdx`, `repos/overview.mdx`, `observability/overview.mdx`.
  </read_first>
  <behavior>
    - Test reads each `.mdx` file from disk, extracts every `<MermaidBlock code={...} />` JSX block via regex, and calls `mermaid.parse(code)` on each.
    - Asserts every `mermaid.parse()` resolves (truthy result).
    - Test runs in node environment (not jsdom — mermaid.parse is a pure syntax check that works in node when called without DOM).
    - Skips files without mermaid (operations/install.mdx, reference/shortcuts.mdx).
  </behavior>
  <action>
Create `packages/spa/src/help/__tests__/mermaid-syntax.test.ts`:

```ts
// @vitest-environment node
/**
 * Plan 07-04 Task 4 — Mermaid syntax validation.
 *
 * Reads each .mdx file from disk, extracts <MermaidBlock code={...} /> JSX
 * blocks, and validates each via `mermaid.parse()`. Catches typos at commit
 * time — without this, Plan 07-05 e2e would catch them only at browser render.
 *
 * @see .planning/phases/07-help-docs-v1-0/07-RESEARCH.md §Pitfall 6
 */
import { readFileSync } from 'node:fs'
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'

import mermaid from 'mermaid'
import { describe, it, expect } from 'vitest'

const HERE = dirname(fileURLToPath(import.meta.url))
const PAGES_DIR = resolve(HERE, '..', 'pages')

const MERMAID_FILES = [
  'landing.mdx',
  'workflow/overview.mdx',
  'repos/overview.mdx',
  'observability/overview.mdx',
] as const

/**
 * Extract every `<MermaidBlock code={` ... `} />` block's code string from MDX source.
 * Captures the contents of the template literal that follows `code={`.
 * Handles single-line and multi-line template literals.
 */
function extractMermaidBlocks(mdxSource: string): string[] {
  const blocks: string[] = []
  const re = /<MermaidBlock\s+code=\{`([\s\S]*?)`\}\s*\/>/g
  let m: RegExpExecArray | null
  while ((m = re.exec(mdxSource)) !== null) {
    blocks.push(m[1])
  }
  return blocks
}

describe('Mermaid syntax validation (Plan 07-04 anchor MDX)', () => {
  mermaid.initialize({ startOnLoad: false })

  it.each(MERMAID_FILES)('%s mermaid blocks all parse without syntax errors', async (relPath) => {
    const filePath = resolve(PAGES_DIR, relPath)
    const source = readFileSync(filePath, 'utf8')
    const blocks = extractMermaidBlocks(source)
    expect(blocks.length).toBeGreaterThan(0)
    for (const code of blocks) {
      await expect(mermaid.parse(code, { suppressErrors: false })).resolves.toBeTruthy()
    }
  })

  it('operations/install.mdx has zero Mermaid blocks (sanity)', () => {
    const source = readFileSync(resolve(PAGES_DIR, 'operations/install.mdx'), 'utf8')
    expect(extractMermaidBlocks(source).length).toBe(0)
  })

  it('total mermaid block count is 5: landing 1 + workflow 1 + repos 1 + observability 2', () => {
    let total = 0
    for (const rel of MERMAID_FILES) {
      const source = readFileSync(resolve(PAGES_DIR, rel), 'utf8')
      total += extractMermaidBlocks(source).length
    }
    expect(total).toBe(5)
  })
})
```

NOTE: `mermaid.parse` in jsdom env may fail because mermaid expects DOM. The `// @vitest-environment node` directive at the top forces this test to run in pure node — `mermaid` v11 supports `parse()` in node-only mode (per mermaid docs).

Run test: should pass. If a parse fails, fix the source mermaid in the corresponding MDX file. Commit `test(07-04): mermaid syntax validation across 4 anchor MDX files (5 blocks total)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/mermaid-syntax.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/__tests__/mermaid-syntax.test.ts` exit 0
    - `grep -q "@vitest-environment node" packages/spa/src/help/__tests__/mermaid-syntax.test.ts` exit 0
    - `grep -q "mermaid.parse" packages/spa/src/help/__tests__/mermaid-syntax.test.ts` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/mermaid-syntax.test.ts` exit 0, reports 6 cases pass (4 mermaid files + 1 zero-count + 1 total-count)
  </acceptance_criteria>
  <done>All Mermaid blocks proven syntactically valid; protects against typos at commit time.</done>
</task>

<task id="07-04-T5" type="auto" tdd="true">
  <name>Task 5: Anchor pages MDX render smoke test — RTL + MDXProvider</name>
  <files>packages/spa/src/help/__tests__/anchor-pages.test.tsx</files>
  <read_first>
    - All 5 anchor MDX files.
    - `packages/spa/src/help/mdxComponents.ts` — provides HelpWidget, HelpHook, ComingSoon, MermaidBlock.
  </read_first>
  <behavior>
    - Test imports each anchor MDX (default export) + the mdxComponents map.
    - For each, renders the MDX inside `<MDXProvider components={mdxComponents}>` and asserts:
      - A heading appears (every anchor has at least one h1 or h2).
      - For files that reference `<HelpWidget>`: the resolved stub appears (we mock the lazy imports per Plan 07-02 pattern).
      - For files that reference `<MermaidBlock>`: the `<pre class="mermaid">` slot appears.
    - Skip Mermaid SVG assertion (deferred to Plan 07-05 Playwright).
  </behavior>
  <action>
Create `packages/spa/src/help/__tests__/anchor-pages.test.tsx`:

```tsx
/**
 * Plan 07-04 Task 5 — anchor MDX render smoke test.
 *
 * Imports each anchor + shortcuts MDX, renders via MDXProvider, asserts
 * structural elements (heading + widget Suspense + Mermaid slot) appear.
 *
 * NOTE: Mock the 3 referenced widget stubs so we don't depend on Plan 07-03's
 * widgets dir being co-resident. (The actual integration is verified in Plan
 * 07-03 stubs-smoke + Plan 07-05 e2e.)
 */
import { render, screen } from '@testing-library/react'
import { MDXProvider } from '@mdx-js/react'
import { Suspense } from 'react'
import { describe, it, expect, vi } from 'vitest'

// Mock useNavigate for HelpHook (anchor pages don't use HelpHook today, but defensive).
vi.mock('@tanstack/react-router', async (importActual) => {
  const actual = await importActual<typeof import('@tanstack/react-router')>()
  return {
    ...actual,
    useNavigate: () => () => Promise.resolve(),
    Link: ({ to, children }: { to: string; children: React.ReactNode }) => <a href={to}>{children}</a>,
  }
})

// Mock the 3 widget stubs referenced by anchor MDX so we don't lazy-load them.
vi.mock('../widgets/RepoTopologyMap.stub', () => ({
  default: () => <div data-testid="stub-RepoTopologyMap">RepoTopologyMap stub</div>,
}))
vi.mock('../widgets/ScanReportPlayground.stub', () => ({
  default: () => <div data-testid="stub-ScanReportPlayground">ScanReportPlayground stub</div>,
}))
vi.mock('../widgets/MigrationDryRun.stub', () => ({
  default: () => <div data-testid="stub-MigrationDryRun">MigrationDryRun stub</div>,
}))

// Mock mermaid runtime (Plan 07-02 MermaidBlock dynamic imports).
vi.mock('mermaid', () => ({
  default: { initialize: vi.fn(), run: vi.fn().mockResolvedValue(undefined) },
}))

import { mdxComponents } from '../mdxComponents'
import LandingPage from '../pages/landing.mdx'
import WorkflowOverview from '../pages/workflow/overview.mdx'
import ReposOverview from '../pages/repos/overview.mdx'
import ObservabilityOverview from '../pages/observability/overview.mdx'
import OperationsInstall from '../pages/operations/install.mdx'

function renderMdx(Component: React.ComponentType): ReturnType<typeof render> {
  return render(
    <MDXProvider components={mdxComponents}>
      <Suspense fallback={<div data-testid="suspense-fallback">Loading…</div>}>
        <article className="prose">
          <Component />
        </article>
      </Suspense>
    </MDXProvider>,
  )
}

describe('Anchor MDX pages render smoke', () => {
  it('landing.mdx renders an h1 heading + at least one MermaidBlock slot', () => {
    renderMdx(LandingPage)
    expect(screen.getAllByRole('heading', { level: 1 }).length).toBeGreaterThan(0)
    expect(document.querySelector('pre.mermaid')).toBeInTheDocument()
  })

  it('workflow/overview.mdx renders + has a Mermaid slot', () => {
    renderMdx(WorkflowOverview)
    expect(screen.getAllByRole('heading').length).toBeGreaterThan(0)
    expect(document.querySelector('pre.mermaid')).toBeInTheDocument()
  })

  it('repos/overview.mdx renders + dispatches RepoTopologyMap via HelpWidget', async () => {
    renderMdx(ReposOverview)
    expect(screen.getAllByRole('heading').length).toBeGreaterThan(0)
    expect(document.querySelector('pre.mermaid')).toBeInTheDocument()
    expect(await screen.findByTestId('stub-RepoTopologyMap')).toBeInTheDocument()
  })

  it('observability/overview.mdx renders 2 Mermaid slots + ScanReportPlayground widget', async () => {
    renderMdx(ObservabilityOverview)
    expect(screen.getAllByRole('heading').length).toBeGreaterThan(0)
    expect(document.querySelectorAll('pre.mermaid').length).toBe(2)
    expect(await screen.findByTestId('stub-ScanReportPlayground')).toBeInTheDocument()
  })

  it('operations/install.mdx renders + dispatches MigrationDryRun (no Mermaid)', async () => {
    renderMdx(OperationsInstall)
    expect(screen.getAllByRole('heading').length).toBeGreaterThan(0)
    expect(document.querySelectorAll('pre.mermaid').length).toBe(0)
    expect(await screen.findByTestId('stub-MigrationDryRun')).toBeInTheDocument()
  })
})
```

Run: 5 cases pass. Commit `test(07-04): anchor MDX render smoke (5 cases, 3 widget stubs mocked, mermaid mocked)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/anchor-pages.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/__tests__/anchor-pages.test.tsx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/anchor-pages.test.tsx` exit 0, reports 5 cases pass
  </acceptance_criteria>
  <done>All 5 anchor MDX pages proven to render + widget references resolve + Mermaid slots appear.</done>
</task>

<task id="07-04-T6" type="auto" tdd="true">
  <name>Task 6: reference/shortcuts.mdx render test — verify KbdHint chips appear (HELP-06)</name>
  <files>packages/spa/src/help/__tests__/reference-shortcuts.test.tsx</files>
  <read_first>
    - `packages/spa/src/help/pages/reference/shortcuts.mdx` (Task 2).
    - `packages/spa/src/components/ui/KbdHint.tsx`.
  </read_first>
  <behavior>
    - Test imports `shortcuts.mdx` default export and renders.
    - Asserts at least 4 KbdHint chips appear: `R`, `?`, `/`, `Cmd`/`K` (the visible text of the chips).
    - Asserts the table rows contain the action descriptions.
  </behavior>
  <action>
Create `packages/spa/src/help/__tests__/reference-shortcuts.test.tsx`:

```tsx
/**
 * Plan 07-04 Task 6 — reference/shortcuts.mdx render test.
 *
 * HELP-06: the old /help keyboard-shortcuts content lives at this MDX page,
 * rendering the KbdHint chips. Validate the migration content-for-content.
 */
import { render, screen } from '@testing-library/react'
import { MDXProvider } from '@mdx-js/react'
import { describe, it, expect } from 'vitest'

import { mdxComponents } from '../mdxComponents'
import ShortcutsPage from '../pages/reference/shortcuts.mdx'

describe('reference/shortcuts.mdx (HELP-06)', () => {
  it('renders the page heading', () => {
    render(
      <MDXProvider components={mdxComponents}>
        <article className="prose">
          <ShortcutsPage />
        </article>
      </MDXProvider>,
    )
    expect(screen.getByRole('heading', { name: /Keyboard shortcuts/i, level: 1 })).toBeInTheDocument()
  })

  it('renders all four canonical shortcut keys (R, ?, /, Cmd+K)', () => {
    render(
      <MDXProvider components={mdxComponents}>
        <article className="prose">
          <ShortcutsPage />
        </article>
      </MDXProvider>,
    )
    expect(screen.getAllByText('R').length).toBeGreaterThan(0)
    expect(screen.getAllByText('?').length).toBeGreaterThan(0)
    expect(screen.getAllByText('/').length).toBeGreaterThan(0)
    expect(screen.getAllByText('Cmd').length).toBeGreaterThan(0)
    expect(screen.getAllByText('K').length).toBeGreaterThan(0)
  })

  it('renders the "Refresh" action description for the R shortcut', () => {
    render(
      <MDXProvider components={mdxComponents}>
        <article className="prose">
          <ShortcutsPage />
        </article>
      </MDXProvider>,
    )
    expect(screen.getByText(/refresh the current view/i)).toBeInTheDocument()
  })

  it('renders the GFM table (remark-gfm proves operational)', () => {
    const { container } = render(
      <MDXProvider components={mdxComponents}>
        <article className="prose">
          <ShortcutsPage />
        </article>
      </MDXProvider>,
    )
    expect(container.querySelector('table')).toBeInTheDocument()
  })
})
```

Run: 4 cases pass. Commit `test(07-04): reference/shortcuts.mdx render + KbdHint integration (HELP-06)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/reference-shortcuts.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/__tests__/reference-shortcuts.test.tsx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/reference-shortcuts.test.tsx` exit 0, reports 4 cases pass
  </acceptance_criteria>
  <done>HELP-06 evidence: shortcuts MDX renders KbdHint chips + GFM table; proves the migration.</done>
</task>

<task id="07-04-T7" type="auto" tdd="false">
  <name>Task 7: Final verification — full SPA suite + build + lint</name>
  <files>(verification only)</files>
  <read_first>
    - All files from this plan.
  </read_first>
  <behavior>
    - Full SPA test suite green.
    - SPA build emits MDX chunks (one per anchor + shortcuts = 6 lazy chunks expected once Plan 07-05 wires lazy routes; for now they bundle eagerly because nothing imports them yet — still no build failures).
    - SPA typecheck green.
    - Workspace tests green.
  </behavior>
  <action>
1. `pnpm --filter @agenticapps/dashboard-spa test` — full SPA suite. Expect green.
2. `pnpm --filter @agenticapps/dashboard-spa typecheck` — exit 0.
3. `pnpm --filter @agenticapps/dashboard-spa build` — exit 0. Inspect chunk report; mermaid should NOT be in the main chunk (lazy in MermaidBlock).
4. `pnpm -r typecheck && pnpm -r test && pnpm lint` — workspace-wide. Exit 0.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test && pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - SPA test suite exit 0; reports ~70+ tests (existing baseline + Phase 7 additions).
    - SPA typecheck exit 0.
    - SPA build exit 0; `dist/` contains MDX-derived chunks.
    - `pnpm -r typecheck && pnpm -r test && pnpm lint` exit 0.
  </acceptance_criteria>
  <done>Wave 2 content shipped; ready for Plan 07-05 routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX content → React render | Content is in-repo authored by us (translated from migration source files). MDX evaluates JSX expressions; content authoring is the trust boundary. |
| Hyperlinks in MDX → SPA navigation | External links (github.com) use `target="_blank" rel="noopener noreferrer"` in `<ComingSoon>` (Plan 07-02). Internal links default to `<a href>` which the SPA router can intercept; full-page reloads acceptable for v1.0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | XSS | MDX content with arbitrary JSX | mitigate | All MDX content originates from `.planning/`-tracked sources or this repo's PR-reviewed additions. No user-supplied content is ever rendered as MDX. `dangerouslySetInnerHTML` is NOT used anywhere in the help system. |
| T-07-04-02 | Open Redirect | MDX `<a href>` external links | accept | All external links in the 6 MDX files point to either github.com or agenticapps.eu/capitalparx.com — verified by grep. No user-supplied URLs. |
| T-07-04-03 | Information Disclosure | Anchor MDX references projects (cparx) | accept | All project mentions are public (open-source repos, public landing page); no internal/secret info. |
| T-07-04-04 | DoS | Mermaid syntax error → infinite-render loop | mitigate | Plan 07-04 Task 4 validates every Mermaid block at test time via `mermaid.parse()`; bad syntax blocks the commit. |
| T-07-04-05 | Tampering | KbdHint import path in reference/shortcuts.mdx | mitigate | Import path `'../../../components/ui/KbdHint.js'` is a relative path to a controlled file; no dynamic resolution. |

**Block-on policy:** `high` severity blocks. None of T-07-04-01..05 are `high`.
</threat_model>

<verification>
- 6 MDX files exist at the documented paths with valid frontmatter.
- Zero triple-backtick mermaid fences in any MDX file (all converted to JSX).
- 4 vitest test files exist; all green; total ~22 new tests.
- Mermaid syntax validation passes for all 5 mermaid blocks.
- HELP-01 evidence: 5 anchor pages render with frontmatter + Mermaid slots + GFM tables + widget references.
- HELP-06 evidence: shortcuts MDX renders KbdHint chips for R / ? / / / Cmd+K.
- SPA build succeeds; chunks committed.
</verification>

<success_criteria>
- HELP-01 evidence: each anchor MDX imports and renders successfully (anchor-pages test green).
- HELP-06 evidence: reference/shortcuts.mdx renders the KbdHint table (reference-shortcuts test green).
- Mermaid syntax validated for all 4 anchor mermaid-bearing files (5 total blocks).
- Frontmatter shape contract holds for all 6 MDX modules.
- Zero build errors; full SPA test suite green.
- Plan 07-05 can import and lazy-route each MDX module without further content authoring.
</success_criteria>

<output>
After completion, create `.planning/phases/07-help-docs-v1-0/07-04-SUMMARY.md` describing:
- File-by-file delta (10 files: 6 MDX + 4 tests).
- Mermaid block count per file (landing 1, workflow 1, repos 1, observability 2, operations 0, shortcuts 0 = 5 total).
- HelpWidget reference count: RepoTopologyMap, ScanReportPlayground, MigrationDryRun. Optionally: landing.mdx also references RepoTopologyMap — check the source.
- Test counts: 7 frontmatter + 6 mermaid-syntax + 5 anchor-render + 4 shortcuts-render = 22 new tests.
- HELP-01 + HELP-06 coverage closed (content level — routing in 07-05).
- Coverage matrix: D-7-15 ✓ (content tests), HELP-01 ✓, HELP-06 ✓.
</output>