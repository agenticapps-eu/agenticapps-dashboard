---
phase: 07
plan: 05
type: execute
wave: 3
title: "Wave 3 route wiring + Playwright e2e + impeccable gate + closing ritual"
depends_on: ["07-01", "07-02", "07-03", "07-04"]
autonomous: false
requirements:
  - HELP-01
  - HELP-02
  - HELP-03
  - HELP-04
  - HELP-05
  - HELP-06
files_modified:
  - packages/spa/src/help/helpRouteTable.ts
  - packages/spa/src/help/buildHelpRoutes.ts
  - packages/spa/src/help/ComingSoonRoute.tsx
  - packages/spa/src/help/HelpPage.tsx
  - packages/spa/src/help/pages/landing.lazy.tsx
  - packages/spa/src/help/pages/workflow.overview.lazy.tsx
  - packages/spa/src/help/pages/repos.overview.lazy.tsx
  - packages/spa/src/help/pages/observability.overview.lazy.tsx
  - packages/spa/src/help/pages/operations.install.lazy.tsx
  - packages/spa/src/help/pages/reference.shortcuts.lazy.tsx
  - packages/spa/src/help/__tests__/helpRouteTable.test.ts
  - packages/spa/src/help/__tests__/legacy-help-route-deleted.test.ts
  - packages/spa/src/help/mdxComponents.ts
  - packages/spa/src/router.tsx
  - packages/spa/src/routes/help.lazy.tsx
  - packages/spa/src/routes/__tests__/help.test.tsx
  - packages/spa/e2e/help-walking-checklist.spec.ts
  - .github/workflows/impeccable.yml
  - .planning/phases/07-help-docs-v1-0/evidence/
  - .planning/phases/07-help-docs-v1-0/07-VERIFICATION.md
  - .planning/phases/07-help-docs-v1-0/07-UAT.md
must_haves:
  truths:
    - "_helpLayout pathless route is a PEER of _appshell at rootRoute (D-7-12); /help/* bypasses AppShellV2"
    - "/help (index) renders landing.mdx; 5 anchor routes render their MDX with no console errors"
    - "32 stub paths render ComingSoon via the shared ComingSoonRoute (HELP-02 count: workflow 11 + repos 6 + observability 7 + operations 4 + reference 4 = 32 — verified by helpRouteTable snapshot)"
    - "4 section bare paths redirect: /help/workflow → /overview, /help/repos → /overview, /help/observability → /overview, /help/operations → /install"
    - "Old routes/help.lazy.tsx + routes/__tests__/help.test.tsx are DELETED"
    - "useGlobalShortcuts.ts ? → /help still works (unchanged); lands on docs landing"
    - "Playwright walking-checklist spec passes: 5 anchor render + 3 stub render + 4 redirects + mobile drawer + ? shortcut + shortcuts page"
    - "Zero console.error events on any of 5 anchor routes during e2e walk"
    - "impeccable:critique score on /help (lg 1440x900) ≥ 90 (Phase 6 D-6-21 inherited gate)"
    - "/browse screenshots at desktop + mobile committed under .planning/phases/07-help-docs-v1-0/evidence/"
    - ".github/workflows/impeccable.yml includes /help in its route list"
    - "07-VERIFICATION.md + 07-UAT.md committed with 1:1 evidence per HELP-01..HELP-06 + ROADMAP S1..S8"
  artifacts:
    - path: "packages/spa/src/help/helpRouteTable.ts"
      provides: "Typed table of all route entries: 1 index + 5 anchors + 32 stubs + 4 redirects + 1 catch-all = 43 entries"
      exports: ["helpRouteTable", "type HelpRouteEntry"]
    - path: "packages/spa/src/help/buildHelpRoutes.ts"
      provides: "Factory that converts helpRouteTable to TanStack createRoute() instances + lazy MDX imports"
      exports: ["buildHelpRoutes"]
    - path: "packages/spa/src/help/ComingSoonRoute.tsx"
      provides: "Parameterized wrapper around <ComingSoon section title />"
      exports: ["ComingSoonRoute"]
    - path: "packages/spa/src/router.tsx"
      provides: "Adds helpLayoutRoute (id _helpLayout, peer of _appshell at rootRoute) + helpRouteTable children + DELETES helpRoute under _appshell"
      contains: "_helpLayout"
    - path: "packages/spa/e2e/help-walking-checklist.spec.ts"
      provides: "Reviewer checklist as automated Playwright spec"
  key_links:
    - from: "packages/spa/src/router.tsx _helpLayout route"
      to: "buildHelpRoutes(helpRouteTable) children"
      via: "addChildren cast as AnyRoute[]"
      pattern: "_helpLayout"
    - from: "packages/spa/src/help/buildHelpRoutes.ts"
      to: "packages/spa/src/help/pages/*.lazy.tsx (5 anchor MDX wrappers + reference/shortcuts wrapper)"
      via: "import() chunks"
      pattern: "createLazyRoute"
    - from: ".github/workflows/impeccable.yml"
      to: "/help route impeccable gate"
      via: "route list extension"
      pattern: "/help"
---

<objective>
Wire the route tree. Mount `_helpLayout` as a PEER of `_appshell` at rootRoute (D-7-12) so `/help/*` bypasses AppShellV2 chrome. Build the route table (43 entries: 1 index + 5 anchors + 32 stubs + 4 redirects + 1 catch-all). Author the 5 lazy MDX route wrappers (one per anchor including shortcuts). Wire 8 widget stubs' lazy loading via Plan 07-02's HelpWidget (no router changes needed — already wired). Finalise `mdxComponents.ts` to include `KbdHint` (for shortcuts page) and `pre: MermaidPreOrDefault` (defensive — pre tag fallback to default <pre>). Author the Playwright walking-checklist spec covering the reviewer's 8-item manual checklist (visit each anchor, 3 stubs, mobile drawer, dark-mode, ? shortcut). **DELETE** the legacy `routes/help.lazy.tsx` and its test. Update `.github/workflows/impeccable.yml` to include `/help` in its route list. Then run the closing ritual (autonomous: false): full preflight, Playwright suite, impeccable score, `/browse` desktop + mobile screenshots, manual reviewer-checklist verification, 07-VERIFICATION.md + 07-UAT.md.

Purpose: HELP-01..HELP-06 all close. ROADMAP success criteria 1–8 all met. Two-stage review-ready PR diff.

Output: ~12 tasks, mixed autonomous (route + e2e wiring) + checkpoint (impeccable + screenshots + manual UAT). Estimated context cost: 50% — split into tight sub-tasks; closing-ritual tasks coordinated via separate commits.
</objective>

<read_first>
Plan-level reference list:

- `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-06 (route table), D-7-07 (lazy anchors), D-7-08 (shared ComingSoonRoute), D-7-12 (peer layout), D-7-13 (extra stubs), D-7-17 (verification evidence).
- `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §"Validation Architecture / Playwright walking script" — the spec template; §Plan-decomposition refinement R4 (autonomous: false for closing ritual); §"Open questions OQ-7-B" (router types) + OQ-7-C (document title format) + OQ-7-E (`?` shortcut behavior on `/help/*`).
- `.planning/phases/07-help-docs-v1-0/07-VALIDATION.md` — fill per-task verification map after authoring.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/HelpRoutes.tsx` — full migration route table (already mostly captured in HelpLayout NAV; this plan rebuilds it as helpRouteTable).
- `packages/spa/src/router.tsx` — existing patterns; helpRoute at lines 86-89 to DELETE; new _helpLayout to ADD.
- `packages/spa/src/routes/help.lazy.tsx` — TO DELETE.
- `packages/spa/src/routes/__tests__/help.test.tsx` — does this exist? If yes, DELETE.
- `packages/spa/src/help/mdxComponents.ts` — has 4 entries; this plan adds KbdHint + pre.
- `packages/spa/src/components/ui/KbdHint.tsx` — for mdxComponents.
- `.github/workflows/impeccable.yml` — route list (Phase 6 06-06).
- `packages/spa/playwright.config.ts` (Plan 07-01) — runner config.
- `packages/spa/e2e/` — empty dir from Plan 07-01.
</read_first>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/agenticapps-workflow/skill/SKILL.md
@$HOME/.claude/skills/superpowers/test-driven-development/SKILL.md
@$HOME/.claude/skills/superpowers/verification-before-completion/SKILL.md
@$HOME/.claude/skills/superpowers/requesting-code-review/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-help-docs-v1-0/07-CONTEXT.md
@.planning/phases/07-help-docs-v1-0/07-RESEARCH.md
@.planning/phases/07-help-docs-v1-0/07-VALIDATION.md
@packages/spa/src/router.tsx
@packages/spa/src/routes/help.lazy.tsx
@packages/spa/src/lib/useGlobalShortcuts.ts
@packages/spa/playwright.config.ts
@.github/workflows/impeccable.yml
</context>

<tasks>

<task id="07-05-T1" type="auto" tdd="true">
  <name>Task 1: helpRouteTable.ts — typed table of all 40 entries with snapshot test</name>
  <files>packages/spa/src/help/helpRouteTable.ts, packages/spa/src/help/__tests__/helpRouteTable.test.ts</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/HelpRoutes.tsx` lines 33-69 — stubs array (32 entries after D-7-13 additions).
    - `packages/spa/src/help/components/HelpLayout.tsx` (Plan 07-02) — NAV table with all paths.
    - D-7-13 — extra stubs (rationalization-table, red-flags).
    - HELP-06 — reference/shortcuts is a `ready` route, NOT a stub.
  </read_first>
  <behavior>
    - Exports `HelpRouteEntry` discriminated-union type:
      - `{ kind: 'index' }` — `/help` landing.
      - `{ kind: 'anchor', path: string, lazyLoader: () => Promise<{ Route }> }` — 5 anchor routes (incl. `reference/shortcuts` per HELP-06).
      - `{ kind: 'stub', path: string, section: string, title: string }` — 32 ComingSoon routes.
      - `{ kind: 'redirect', from: string, to: string }` — 4 section-bare paths.
      - `{ kind: 'catchAll', to: string }` — wildcard `/help/*` → `/help`.
    - Exports `helpRouteTable: HelpRouteEntry[]` with 1 + 5 + 32 + 4 + 1 = 43 entries (1 index + 5 anchor including shortcuts + 32 stub + 4 redirect + 1 catch-all).
    - Snapshot test fixes the count + shape so future drift is caught.
  </behavior>
  <action>
**RED commit (snapshot baseline).**

Create `packages/spa/src/help/__tests__/helpRouteTable.test.ts`:

```ts
/**
 * Plan 07-05 Task 1 — helpRouteTable snapshot.
 *
 * Locks the 43-entry route table so any future drift (added stubs without
 * updating HelpLayout NAV, missing redirect, etc.) is caught at commit time.
 *
 * @see .planning/phases/07-help-docs-v1-0/07-CONTEXT.md D-7-06, D-7-13
 */
import { describe, it, expect } from 'vitest'

import { helpRouteTable } from '../helpRouteTable'

describe('helpRouteTable', () => {
  it('has the documented entry counts: 1 index + 5 anchor + 32 stub + 4 redirect + 1 catch-all = 43 total', () => {
    const byKind = helpRouteTable.reduce<Record<string, number>>((acc, entry) => {
      acc[entry.kind] = (acc[entry.kind] || 0) + 1
      return acc
    }, {})
    expect(byKind).toEqual({
      index: 1,
      anchor: 5,
      stub: 32,
      redirect: 4,
      catchAll: 1,
    })
    expect(helpRouteTable.length).toBe(43)
  })

  it('all 32 stub paths are unique and prefixed with /help/', () => {
    const stubs = helpRouteTable.filter((e) => e.kind === 'stub')
    const paths = stubs.map((e) => (e as { path: string }).path)
    expect(new Set(paths).size).toBe(paths.length)
    for (const p of paths) {
      expect(p).toMatch(/^\/help\//)
    }
  })

  it('includes the 2 D-7-13 workflow stubs', () => {
    const paths = helpRouteTable.flatMap((e) =>
      e.kind === 'stub' ? [(e as { path: string }).path] : [],
    )
    expect(paths).toContain('/help/workflow/rationalization-table')
    expect(paths).toContain('/help/workflow/red-flags')
  })

  it('reference/shortcuts is an anchor (ready), NOT a stub (HELP-06)', () => {
    const shortcuts = helpRouteTable.find(
      (e) => e.kind === 'anchor' && (e as { path: string }).path === '/help/reference/shortcuts',
    )
    expect(shortcuts).toBeDefined()
    const isStub = helpRouteTable.some(
      (e) => e.kind === 'stub' && (e as { path: string }).path === '/help/reference/shortcuts',
    )
    expect(isStub).toBe(false)
  })

  it('has the 4 section redirects: workflow → /overview, repos → /overview, observability → /overview, operations → /install', () => {
    const redirects = helpRouteTable
      .filter((e) => e.kind === 'redirect')
      .map((e) => e as { from: string; to: string })
    expect(redirects).toEqual(
      expect.arrayContaining([
        { kind: 'redirect', from: '/help/workflow', to: '/help/workflow/overview' },
        { kind: 'redirect', from: '/help/repos', to: '/help/repos/overview' },
        { kind: 'redirect', from: '/help/observability', to: '/help/observability/overview' },
        { kind: 'redirect', from: '/help/operations', to: '/help/operations/install' },
      ]),
    )
  })

  it('the catchAll entry redirects to /help', () => {
    const catchAll = helpRouteTable.find((e) => e.kind === 'catchAll')
    expect(catchAll).toEqual({ kind: 'catchAll', to: '/help' })
  })
})
```

Run: fails (module not found). Commit `test(07-05): RED helpRouteTable snapshot (43 entries)`.

**GREEN commit.**

Create `packages/spa/src/help/helpRouteTable.ts`:

```ts
/**
 * helpRouteTable — single source of truth for the /help/* route tree.
 *
 * Discriminated union of 5 entry kinds. Plan 07-05 Task 2's buildHelpRoutes
 * factory turns this table into TanStack createRoute() instances.
 *
 * Counts: 1 index + 5 anchor (4 v1.0 overview anchors + HELP-06 shortcuts) + 32 stubs
 * (workflow 11 incl. D-7-13 + repos 6 + observability 7 + operations 4 +
 * reference 4) + 4 redirects + 1 catch-all = 43 total entries.
 */
export type HelpRouteEntry =
  | { kind: 'index' }
  | {
      kind: 'anchor'
      path: string
      lazyImport: () => Promise<{ Route: unknown }>
    }
  | { kind: 'stub'; path: string; section: string; title: string }
  | { kind: 'redirect'; from: string; to: string }
  | { kind: 'catchAll'; to: string }

// ── Stubs (32 entries) ──────────────────────────────────────────────
const STUBS: { path: string; section: string; title: string }[] = [
  // Workflow (11 — includes 2 D-7-13 additions: rationalization-table, red-flags)
  { path: '/help/workflow/commitment-ritual', section: 'workflow', title: 'Commitment Ritual' },
  { path: '/help/workflow/gates', section: 'workflow', title: 'Gates' },
  { path: '/help/workflow/superpowers', section: 'workflow', title: 'Superpowers' },
  { path: '/help/workflow/gsd', section: 'workflow', title: 'GSD' },
  { path: '/help/workflow/gstack', section: 'workflow', title: 'gstack' },
  { path: '/help/workflow/impeccable', section: 'workflow', title: 'Impeccable' },
  { path: '/help/workflow/two-stage-review', section: 'workflow', title: 'Two-Stage Review' },
  { path: '/help/workflow/verification', section: 'workflow', title: 'Verification' },
  { path: '/help/workflow/adrs', section: 'workflow', title: 'ADRs' },
  { path: '/help/workflow/rationalization-table', section: 'workflow', title: 'Rationalization table' },
  { path: '/help/workflow/red-flags', section: 'workflow', title: 'Red flags' },
  // Repos (6)
  { path: '/help/repos/core', section: 'repos', title: 'core' },
  { path: '/help/repos/claude', section: 'repos', title: 'claude-workflow' },
  { path: '/help/repos/pi', section: 'repos', title: 'pi-workflow' },
  { path: '/help/repos/codex', section: 'repos', title: 'codex-workflow' },
  { path: '/help/repos/dashboard', section: 'repos', title: 'dashboard' },
  { path: '/help/repos/projects', section: 'repos', title: 'Client projects' },
  // Observability (7)
  { path: '/help/observability/spec-section-10', section: 'observability', title: 'Spec §10 walkthrough' },
  { path: '/help/observability/stacks', section: 'observability', title: 'Stack templates' },
  { path: '/help/observability/install', section: 'observability', title: 'Install' },
  { path: '/help/observability/scan', section: 'observability', title: 'Scan' },
  { path: '/help/observability/apply', section: 'observability', title: 'Apply' },
  { path: '/help/observability/policy', section: 'observability', title: 'Policy' },
  { path: '/help/observability/pi-codex-status', section: 'observability', title: 'Pi & Codex status' },
  // Operations (4)
  { path: '/help/operations/update', section: 'operations', title: 'Updating' },
  { path: '/help/operations/slash-commands', section: 'operations', title: 'Slash command catalog' },
  { path: '/help/operations/troubleshooting', section: 'operations', title: 'Troubleshooting' },
  { path: '/help/operations/migration-runbook', section: 'operations', title: 'Migration runbook' },
  // Reference (4) — Keyboard shortcuts is an ANCHOR, not a stub
  { path: '/help/reference/glossary', section: 'reference', title: 'Glossary' },
  { path: '/help/reference/adr-index', section: 'reference', title: 'ADR index' },
  { path: '/help/reference/changelog', section: 'reference', title: 'Changelog' },
  { path: '/help/reference/contributing', section: 'reference', title: 'Contributing' },
]

export const helpRouteTable: HelpRouteEntry[] = [
  { kind: 'index' },
  // 4 v1.0 overview anchors + HELP-06 shortcuts = 5 anchors
  {
    kind: 'anchor',
    path: '/help/workflow/overview',
    lazyImport: () => import('./pages/workflow.overview.lazy.js'),
  },
  {
    kind: 'anchor',
    path: '/help/repos/overview',
    lazyImport: () => import('./pages/repos.overview.lazy.js'),
  },
  {
    kind: 'anchor',
    path: '/help/observability/overview',
    lazyImport: () => import('./pages/observability.overview.lazy.js'),
  },
  {
    kind: 'anchor',
    path: '/help/operations/install',
    lazyImport: () => import('./pages/operations.install.lazy.js'),
  },
  {
    kind: 'anchor',
    path: '/help/reference/shortcuts',
    lazyImport: () => import('./pages/reference.shortcuts.lazy.js'),
  },
  // 32 stubs
  ...STUBS.map(({ path, section, title }) => ({ kind: 'stub' as const, path, section, title })),
  // 4 redirects
  { kind: 'redirect', from: '/help/workflow', to: '/help/workflow/overview' },
  { kind: 'redirect', from: '/help/repos', to: '/help/repos/overview' },
  { kind: 'redirect', from: '/help/observability', to: '/help/observability/overview' },
  { kind: 'redirect', from: '/help/operations', to: '/help/operations/install' },
  // 1 catch-all
  { kind: 'catchAll', to: '/help' },
]
```

Note: anchor count is 5 (4 v1.0 overview anchors + shortcuts). The index `/help` is separate. helpRouteTable.test.ts asserts this count.

Run: 6 cases pass. Commit `feat(07-05): GREEN helpRouteTable (43 entries: 1 index + 5 anchors + 32 stubs + 4 redirects + 1 catch-all)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/helpRouteTable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/helpRouteTable.ts` exit 0
    - `grep -c '/help/workflow/' packages/spa/src/help/helpRouteTable.ts` returns ≥ 12 (1 overview + 11 stubs)
    - `grep -q "/help/workflow/rationalization-table" packages/spa/src/help/helpRouteTable.ts` exit 0 (D-7-13)
    - `grep -q "/help/workflow/red-flags" packages/spa/src/help/helpRouteTable.ts` exit 0 (D-7-13)
    - `grep -q "/help/reference/shortcuts" packages/spa/src/help/helpRouteTable.ts` exit 0 (HELP-06)
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/helpRouteTable.test.ts` exit 0, reports 6 cases pass
  </acceptance_criteria>
  <done>helpRouteTable locked at 43 entries; snapshot test guards future drift.</done>
</task>

<task id="07-05-T2" type="auto" tdd="false">
  <name>Task 2: Author 6 lazy MDX route wrappers + HelpPage shell</name>
  <files>packages/spa/src/help/HelpPage.tsx, packages/spa/src/help/pages/landing.lazy.tsx, packages/spa/src/help/pages/workflow.overview.lazy.tsx, packages/spa/src/help/pages/repos.overview.lazy.tsx, packages/spa/src/help/pages/observability.overview.lazy.tsx, packages/spa/src/help/pages/operations.install.lazy.tsx, packages/spa/src/help/pages/reference.shortcuts.lazy.tsx</files>
  <read_first>
    - `packages/spa/src/routes/pair.lazy.tsx` — pattern reference for `createLazyRoute('/path')({ component })`.
    - `packages/spa/src/help/pages/*.mdx` (Plan 07-04) — content to wrap.
    - OQ-7-C: document title format. **Decision: use `'${frontmatter.title} · AgenticApps Dashboard Help'` (interpunct separator) per RESEARCH OQ-7-C recommendation.**
  </read_first>
  <behavior>
    - `HelpPage.tsx`: shared shell that reads MDX `frontmatter`, sets `document.title`, renders the MDX default export. Takes `{ FmComponent, frontmatter }` as props.
    - 6 lazy wrappers: one per anchor MDX file. Each exports `Route = createLazyRoute('/help/<path>')({ component })` where `component` calls `<HelpPage>` with the imported MDX + frontmatter.
  </behavior>
  <action>
**Create `packages/spa/src/help/HelpPage.tsx`:**

```tsx
/**
 * HelpPage — shared shell for every help anchor MDX page.
 *
 * Renders the MDX content + sets document.title from frontmatter.
 * Used by the 6 lazy wrappers in packages/spa/src/help/pages/*.lazy.tsx.
 *
 * Document title format (OQ-7-C): "{title} · AgenticApps Dashboard Help".
 */
import { useEffect } from 'react'

export interface HelpPageProps {
  FmComponent: React.ComponentType
  frontmatter: { slug: string; title: string; order: number; section: string }
}

export function HelpPage({ FmComponent, frontmatter }: HelpPageProps): React.JSX.Element {
  useEffect(() => {
    document.title = `${frontmatter.title} · AgenticApps Dashboard Help`
  }, [frontmatter.title])

  return <FmComponent />
}
```

**Create 6 lazy wrappers — one per file.** All follow the same shape:

```tsx
// packages/spa/src/help/pages/landing.lazy.tsx
import { createLazyRoute } from '@tanstack/react-router'

import { HelpPage } from '../HelpPage.js'

import LandingMdx, { frontmatter } from './landing.mdx'

export const Route = createLazyRoute('/help/')({
  component: () => <HelpPage FmComponent={LandingMdx} frontmatter={frontmatter} />,
})
```

Repeat with the appropriate route path + import for the 5 others:

- `workflow.overview.lazy.tsx` → `createLazyRoute('/help/workflow/overview')` + imports from `./workflow/overview.mdx`
- `repos.overview.lazy.tsx` → `createLazyRoute('/help/repos/overview')` + imports from `./repos/overview.mdx`
- `observability.overview.lazy.tsx` → `createLazyRoute('/help/observability/overview')` + imports from `./observability/overview.mdx`
- `operations.install.lazy.tsx` → `createLazyRoute('/help/operations/install')` + imports from `./operations/install.mdx`
- `reference.shortcuts.lazy.tsx` → `createLazyRoute('/help/reference/shortcuts')` + imports from `./reference/shortcuts.mdx`

NOTE: The flat file names (`workflow.overview.lazy.tsx` with dots) match the dashboard's existing flat lazy-route convention (e.g. `projects.$projectId.lazy.tsx`).

Commit `feat(07-05): 6 lazy MDX route wrappers + HelpPage shell`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist: `for f in HelpPage.tsx pages/landing.lazy.tsx pages/workflow.overview.lazy.tsx pages/repos.overview.lazy.tsx pages/observability.overview.lazy.tsx pages/operations.install.lazy.tsx pages/reference.shortcuts.lazy.tsx; do test -f "packages/spa/src/help/${f}" || exit 1; done`
    - Each lazy file uses createLazyRoute: `grep -l "createLazyRoute" packages/spa/src/help/pages/*.lazy.tsx | wc -l` returns 6
    - HelpPage sets document.title with interpunct separator: `grep -q "AgenticApps Dashboard Help" packages/spa/src/help/HelpPage.tsx` exit 0
    - SPA typecheck exit 0 + build exit 0
  </acceptance_criteria>
  <done>Lazy wrappers + HelpPage shell ready for Task 4 to mount into router.</done>
</task>

<task id="07-05-T3" type="auto" tdd="false">
  <name>Task 3: ComingSoonRoute.tsx + buildHelpRoutes factory</name>
  <files>packages/spa/src/help/ComingSoonRoute.tsx, packages/spa/src/help/buildHelpRoutes.ts</files>
  <read_first>
    - `packages/spa/src/help/helpRouteTable.ts` (Task 1).
    - `packages/spa/src/help/components/ComingSoon.tsx` (Plan 07-02).
    - `packages/spa/src/router.tsx` — pattern for `createRoute` + `addChildren` + `redirect`.
    - OQ-7-B (RESEARCH) — typing strategy for dynamically-built routes: cast as `AnyRoute[]`.
  </read_first>
  <behavior>
    - `ComingSoonRoute(section, title)`: tiny wrapper component that renders `<ComingSoon section={section} title={title} />`.
    - `buildHelpRoutes(parent, table, rootRoute)`:
      - Returns an array of TanStack `Route` instances.
      - For each `index` entry: creates the `/help` index route attached to parent (`_helpLayout`); component lazily loads from the landing wrapper.
      - For each `anchor`: creates `createRoute({ getParentRoute: () => parent, path: <subpath after '/help'> })` and chains `.lazy(() => entry.lazyImport().then((m) => m.Route))`.
      - For each `stub`: creates `createRoute({ getParentRoute: () => parent, path: <subpath after '/help'>, component: () => <ComingSoon section={...} title={...} /> })`.
      - For each `redirect`: creates `createRoute({ getParentRoute: () => parent, path: <subpath after '/help'>, beforeLoad: () => { throw redirect({ to: entry.to }) } })`.
      - For the `catchAll`: creates `createRoute({ getParentRoute: () => parent, path: '$', beforeLoad: () => { throw redirect({ to: '/help' }) } })`.
  </behavior>
  <action>
**Create `packages/spa/src/help/ComingSoonRoute.tsx`:**

```tsx
/**
 * ComingSoonRoute — parameterized wrapper around <ComingSoon section title />
 * for use as a TanStack route `component` prop.
 *
 * Plan 07-05 Task 3. Used by buildHelpRoutes for 32 stub entries.
 */
import type { ReactElement } from 'react'

import { ComingSoon } from './components/ComingSoon.js'

export function ComingSoonRoute({ section, title }: { section: string; title: string }): ReactElement {
  return <ComingSoon section={section} title={title} />
}
```

**Create `packages/spa/src/help/buildHelpRoutes.ts`:**

```ts
/**
 * buildHelpRoutes — factory that converts helpRouteTable into TanStack route instances.
 *
 * Plan 07-05 Task 3. Each route entry kind maps to a different createRoute()
 * shape:
 *   - index: createRoute(path: '/') → lazy(landing)
 *   - anchor: createRoute(path) → lazy(anchor module)
 *   - stub: createRoute(path) → component renders ComingSoonRoute
 *   - redirect: createRoute(path) → beforeLoad throws redirect
 *   - catchAll: createRoute(path: '$') → beforeLoad throws redirect to /help
 *
 * Per OQ-7-B: dynamic Route union narrows poorly, so we cast as AnyRoute[]
 * (matches the existing router.tsx:115 pattern).
 */
import { createRoute, redirect, type AnyRoute, type RootRoute, type Route } from '@tanstack/react-router'

import { ComingSoonRoute } from './ComingSoonRoute.js'
import { helpRouteTable } from './helpRouteTable.js'

type ParentRoute = Route<RootRoute, RootRoute, '_helpLayout', '/'>

// Strip the leading "/help" prefix so the route path is parented at _helpLayout.
function relativePath(absPath: string): string {
  if (absPath === '/help') return '/'
  return absPath.replace(/^\/help/, '')
}

export function buildHelpRoutes(parent: ParentRoute): AnyRoute[] {
  const routes: AnyRoute[] = []

  for (const entry of helpRouteTable) {
    if (entry.kind === 'index') {
      const r = createRoute({
        getParentRoute: () => parent as unknown as AnyRoute,
        path: '/',
      }).lazy(() => import('./pages/landing.lazy.js').then((m) => m.Route))
      routes.push(r)
    } else if (entry.kind === 'anchor') {
      const r = createRoute({
        getParentRoute: () => parent as unknown as AnyRoute,
        path: relativePath(entry.path),
      }).lazy(() => entry.lazyImport().then((m: { Route: AnyRoute }) => m.Route))
      routes.push(r)
    } else if (entry.kind === 'stub') {
      const stubEntry = entry
      const r = createRoute({
        getParentRoute: () => parent as unknown as AnyRoute,
        path: relativePath(stubEntry.path),
        component: () => <ComingSoonRoute section={stubEntry.section} title={stubEntry.title} />,
      })
      routes.push(r)
    } else if (entry.kind === 'redirect') {
      const redirectEntry = entry
      const r = createRoute({
        getParentRoute: () => parent as unknown as AnyRoute,
        path: relativePath(redirectEntry.from),
        beforeLoad: () => {
          throw redirect({ to: redirectEntry.to })
        },
      })
      routes.push(r)
    } else if (entry.kind === 'catchAll') {
      const catchEntry = entry
      const r = createRoute({
        getParentRoute: () => parent as unknown as AnyRoute,
        path: '$',
        beforeLoad: () => {
          throw redirect({ to: catchEntry.to })
        },
      })
      routes.push(r)
    }
  }

  return routes
}
```

NOTE: `buildHelpRoutes.ts` mixes `.tsx` JSX (`<ComingSoonRoute ... />`) with `.ts` — rename to `buildHelpRoutes.tsx` if the tsconfig disallows JSX in `.ts`. **Decision:** Use `.tsx` extension to be safe:

```bash
# Rename:
mv packages/spa/src/help/buildHelpRoutes.ts packages/spa/src/help/buildHelpRoutes.tsx
# Update files_modified accordingly.
```

Commit `feat(07-05): ComingSoonRoute + buildHelpRoutes factory (43-entry → TanStack Route[] adapter)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/ComingSoonRoute.tsx` exit 0
    - `test -f packages/spa/src/help/buildHelpRoutes.tsx` exit 0 (or .ts if JSX-in-ts disallowed — final file name resolution per executor)
    - `grep -q "createRoute" packages/spa/src/help/buildHelpRoutes.tsx` exit 0
    - `grep -q "redirect" packages/spa/src/help/buildHelpRoutes.tsx` exit 0
    - All 5 entry kinds branched: `grep -c "entry.kind ===" packages/spa/src/help/buildHelpRoutes.tsx` returns 5
    - SPA typecheck + build green
  </acceptance_criteria>
  <done>buildHelpRoutes ready to wire 43 entries into TanStack.</done>
</task>

<task id="07-05-T4" type="auto" tdd="true">
  <name>Task 4: Wire _helpLayout peer route into router.tsx + DELETE legacy /help route</name>
  <files>packages/spa/src/router.tsx, packages/spa/src/routes/help.lazy.tsx, packages/spa/src/routes/__tests__/help.test.tsx, packages/spa/src/help/__tests__/legacy-help-route-deleted.test.ts</files>
  <read_first>
    - `packages/spa/src/router.tsx` — current contents (128 lines; helpRoute at lines 86-89; routeTree at lines 109-118).
    - `packages/spa/src/routes/help.lazy.tsx` — to DELETE.
    - `packages/spa/src/routes/__tests__/help.test.tsx` — to DELETE if it exists.
    - D-7-12: _helpLayout is a PEER of _appshell at rootRoute.
    - `packages/spa/src/help/buildHelpRoutes.tsx` (Task 3).
    - `packages/spa/src/help/components/HelpLayout.tsx` (Plan 07-02).
  </read_first>
  <behavior>
    - Add `helpLayoutRoute = createRoute({ getParentRoute: () => rootRoute, id: '_helpLayout', component: HelpLayout })` to `router.tsx`.
    - Add `helpLayoutRoute.addChildren(buildHelpRoutes(helpLayoutRoute) as AnyRoute[])` to the route tree.
    - DELETE the `helpRoute` declaration (lines 86-89) and its entry in `appShellLayoutRoute.addChildren` (line 113).
    - DELETE `packages/spa/src/routes/help.lazy.tsx`.
    - DELETE `packages/spa/src/routes/__tests__/help.test.tsx` if present.
    - The old `helpRoute` no longer exists; navigating to `/help` resolves to `_helpLayout` + index landing.
    - A "legacy route deleted" test file-existence test guards regression.
  </behavior>
  <action>
**RED commit (file-existence guard).**

Create `packages/spa/src/help/__tests__/legacy-help-route-deleted.test.ts`:

```ts
// @vitest-environment node
/**
 * Plan 07-05 Task 4 — guards the deletion of the legacy /help route + test.
 *
 * If anyone re-adds packages/spa/src/routes/help.lazy.tsx in a future change,
 * this test fails immediately. HELP-06 requires the legacy file is gone.
 */
import { existsSync } from 'node:fs'
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'

import { describe, it, expect } from 'vitest'

const HERE = dirname(fileURLToPath(import.meta.url))
const ROUTES_DIR = resolve(HERE, '..', '..', 'routes')

describe('Legacy /help route is deleted (HELP-06)', () => {
  it('packages/spa/src/routes/help.lazy.tsx does NOT exist', () => {
    expect(existsSync(resolve(ROUTES_DIR, 'help.lazy.tsx'))).toBe(false)
  })

  it('packages/spa/src/routes/__tests__/help.test.tsx does NOT exist', () => {
    expect(existsSync(resolve(ROUTES_DIR, '__tests__', 'help.test.tsx'))).toBe(false)
  })
})
```

Run: fails (both files exist before deletion). Commit `test(07-05): RED legacy /help deletion guard`.

**GREEN commit.**

1. DELETE `packages/spa/src/routes/help.lazy.tsx` (use `git rm`).
2. DELETE `packages/spa/src/routes/__tests__/help.test.tsx` (use `git rm`) — if it doesn't exist, skip.
3. Update `packages/spa/src/router.tsx`:

```tsx
// Add imports at the top (after existing imports):
import { HelpLayout } from './help/components/HelpLayout.js'
import { buildHelpRoutes } from './help/buildHelpRoutes.js'

// REMOVE the helpRoute declaration (lines 86-89 in current file):
// DELETE:
//   const helpRoute = createRoute({
//     getParentRoute: () => appShellLayoutRoute,
//     path: '/help',
//   }).lazy(() => import('./routes/help.lazy.js').then((m) => m.Route))

// AFTER the existing route declarations (after pairRoute), ADD:
const helpLayoutRoute = createRoute({
  getParentRoute: () => rootRoute,
  id: '_helpLayout',
  component: HelpLayout,
})

// CHANGE the routeTree to include _helpLayout as a peer of _appshell:
const routeTree = rootRoute.addChildren([
  appShellLayoutRoute.addChildren([
    indexRoute,
    settingsRoute,
    // REMOVE: helpRoute,
    projectsIdRoute,
  ] as AnyRoute[]),
  helpLayoutRoute.addChildren(buildHelpRoutes(helpLayoutRoute as unknown as Parameters<typeof buildHelpRoutes>[0]) as AnyRoute[]),
  onboardingRoute,
  pairRoute,
] as AnyRoute[])
```

NOTE: the `as unknown as Parameters<typeof buildHelpRoutes>[0]` cast is the OQ-7-B workaround — TanStack's dynamic Route union narrows poorly with code-generated routes. The cast is identical in shape to the existing `as AnyRoute[]` casts in `addChildren`.

4. Verify legacy test now passes: run `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/legacy-help-route-deleted.test.ts`. Exit 0; 2 cases pass.

5. Verify SPA build green: `pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build`.

6. Boot dev server briefly to verify `/help` resolves: `pnpm --filter @agenticapps/dashboard-spa dev &; sleep 5; curl -s http://localhost:5174/help | head -20 | grep -q "<html"; kill %1`. (Hand-verify in browser later.)

Commit `feat(07-05): GREEN _helpLayout peer route + legacy /help removed`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/legacy-help-route-deleted.test.ts && pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f packages/spa/src/routes/help.lazy.tsx` exit 0 (file gone)
    - `! test -f packages/spa/src/routes/__tests__/help.test.tsx` exit 0 (file gone, if it ever existed)
    - `grep -q "_helpLayout" packages/spa/src/router.tsx` exit 0
    - `grep -q "buildHelpRoutes" packages/spa/src/router.tsx` exit 0
    - `! grep -q "import.*help\\.lazy" packages/spa/src/router.tsx` exit 0 (no legacy import)
    - `grep -q "helpLayoutRoute" packages/spa/src/router.tsx` exit 0
    - Legacy deletion test exit 0 (2 cases)
    - SPA typecheck + build green
  </acceptance_criteria>
  <done>_helpLayout peer route mounted; legacy /help route deleted; build clean.</done>
</task>

<task id="07-05-T5" type="auto" tdd="false">
  <name>Task 5: Finalise mdxComponents.ts (add KbdHint + defensive pre fallback)</name>
  <files>packages/spa/src/help/mdxComponents.ts</files>
  <read_first>
    - `packages/spa/src/help/mdxComponents.ts` — current map (4 entries from Plan 07-02).
    - `packages/spa/src/components/ui/KbdHint.tsx`.
    - Plan 07-04's reference/shortcuts.mdx imports KbdHint EXPLICITLY (so adding it here is belt-and-suspenders, not strictly required).
    - Plan 07-04's anchor MDX uses `<MermaidBlock code={...}/>` JSX, NOT triple-backtick fences. So a `pre` mapping is also belt-and-suspenders. But authoring it now means future MDX authors can use either style.
  </read_first>
  <behavior>
    - `mdxComponents` adds `KbdHint`.
    - `mdxComponents` adds a `pre` mapping that detects `className.includes('language-mermaid')` on the child `<code>` and renders `<MermaidBlock>` accordingly; otherwise falls through to default `<pre>` rendering.
  </behavior>
  <action>
Replace `packages/spa/src/help/mdxComponents.ts` contents with EXACTLY:

```ts
/**
 * mdxComponents — global component map passed to <MDXProvider>.
 *
 * Plan 07-01: empty stub.
 * Plan 07-02: + { HelpWidget, HelpHook, ComingSoon, MermaidBlock }.
 * Plan 07-05 (this update): + { KbdHint, pre: MermaidPreOrDefault }.
 *
 * `pre` is a defensive fallback: if a future MDX author writes a
 * ```mermaid block (triple-backtick fence), the default MDX renderer would
 * produce <pre><code className="language-mermaid">…</code></pre>. The
 * MermaidPreOrDefault wrapper detects that and routes through MermaidBlock.
 * For v1.0, all Mermaid is JSX (Plan 07-04 Task 1), so `pre` falls through
 * to default rendering on every existing block.
 */
import type { ReactElement, ReactNode } from 'react'
import type { MDXComponents } from 'mdx/types'

import { KbdHint } from '../components/ui/KbdHint.js'

import { ComingSoon } from './components/ComingSoon.js'
import { HelpHook } from './components/HelpHook.js'
import { HelpWidget } from './components/HelpWidget.js'
import { MermaidBlock } from './components/MermaidBlock.js'

function MermaidPreOrDefault(props: React.HTMLAttributes<HTMLPreElement> & { children?: ReactNode }): ReactElement {
  const child = Array.isArray(props.children) ? props.children[0] : props.children
  if (
    child &&
    typeof child === 'object' &&
    'props' in child &&
    typeof (child as { props: { className?: string } }).props.className === 'string' &&
    (child as { props: { className: string } }).props.className.includes('language-mermaid')
  ) {
    const codeNode = child as { props: { children?: ReactNode } }
    const code = typeof codeNode.props.children === 'string' ? codeNode.props.children : ''
    return <MermaidBlock code={code} />
  }
  return <pre {...props} />
}

export const mdxComponents: MDXComponents = {
  HelpWidget,
  HelpHook,
  ComingSoon,
  MermaidBlock,
  KbdHint,
  pre: MermaidPreOrDefault,
}
```

NOTE: the file already typechecked with `MDXComponents` import — `KbdHint` and the `pre` mapping are valid keys.

Run `pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/mdx-smoke.test.tsx src/help/__tests__/anchor-pages.test.tsx`. All green.

Commit `feat(07-05): finalise mdxComponents with KbdHint + pre fallback`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa test src/help/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "KbdHint" packages/spa/src/help/mdxComponents.ts` exit 0
    - `grep -q "pre: MermaidPreOrDefault" packages/spa/src/help/mdxComponents.ts` exit 0
    - SPA typecheck exit 0
    - All Phase 7 unit tests still green
  </acceptance_criteria>
  <done>mdxComponents finalised with 6 entries; KbdHint available globally to MDX; pre fallback ships.</done>
</task>

<task id="07-05-T6" type="auto" tdd="false">
  <name>Task 6: Update .github/workflows/impeccable.yml to include /help in route list</name>
  <files>.github/workflows/impeccable.yml</files>
  <read_first>
    - `.github/workflows/impeccable.yml` — the route list. Phase 6 D-6-10 listed `/onboarding`, `/`, `/projects/:id`, `/settings`, `/help`, `/pair`. Verify `/help` is in the list; if so, no change needed.
  </read_first>
  <behavior>
    - If `/help` is NOT in the route list, ADD it.
    - If `/help` IS already in the route list, this task is a no-op (record in summary).
  </behavior>
  <action>
1. Read `.github/workflows/impeccable.yml`.
2. Locate the line(s) that enumerate routes (likely a YAML list under a `routes:` key, or a job-level env var).
3. If `/help` is present: log "already included" in the commit message and skip the change.
4. If `/help` is NOT present: add it as the next list item AFTER `/settings` (matching the documented ordering in D-6-10).

If a change is required, commit as `chore(07-05): add /help to impeccable.yml route list`. Otherwise no commit.

If the YAML structure is non-obvious or differs from D-6-10's documented list, escalate: the executor should ask the user before changing CI config.
  </action>
  <verify>
    <automated>grep -q "/help" .github/workflows/impeccable.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/help" .github/workflows/impeccable.yml` exit 0 (route in the list, either pre-existing or just added)
    - File still valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/impeccable.yml'))"` exit 0 (or `node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/impeccable.yml','utf8'))"` if Python unavailable)
  </acceptance_criteria>
  <done>Impeccable CI gates /help route.</done>
</task>

<task id="07-05-T7" type="auto" tdd="false">
  <name>Task 7: Author Playwright walking-checklist spec (the reviewer's 8-item checklist)</name>
  <files>packages/spa/e2e/help-walking-checklist.spec.ts</files>
  <read_first>
    - `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §"Playwright walking script" — full spec template (~90 lines).
    - `packages/spa/playwright.config.ts` (Plan 07-01) — runner config.
  </read_first>
  <behavior>
    - Spec walks the reviewer checklist:
      1. Visit `/help` → assert landing heading + 1 Mermaid SVG appears (after networkidle).
      2. Visit 4 anchor routes → assert each renders heading + Mermaid SVG (where applicable).
      3. Capture `console.error` events globally → assert zero across the entire walk.
      4. Visit 3 sample stub paths → assert ComingSoon text + back-link visible.
      5. Visit 4 section redirects → assert URL becomes `/overview` or `/install` after redirect.
      6. Widget Suspense: visit `/help/repos/overview` → assert "Coming v1.2" badge eventually visible (lazy widget resolved).
      7. `?` shortcut from `/` → assert URL becomes `/help` AND landing heading visible.
      8. Reference shortcuts page renders KbdHint table.
      9. Mobile viewport (375×800): visit `/help`, assert sidebar `aria-label="Help navigation"` hidden initially, click toggle button, assert sidebar visible.
  </behavior>
  <action>
Create `packages/spa/e2e/help-walking-checklist.spec.ts` based on the RESEARCH template (~90 lines). The full spec is the one in `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` lines 415-500. Copy it verbatim, then:

- Convert it to TypeScript-strict (typed `errors: string[]`, etc.).
- Adjust assertions to match the actual DOM (Plan 07-02 HelpLayout — sidebar has `aria-label="Help navigation"`; mobile drawer toggle has `aria-label="Toggle navigation"`).
- The "Coming v1.2" badge appears via `WidgetStub` (Plan 07-03); assertion is correct.
- ROADMAP S6: assert `document.title` matches `/{title} · AgenticApps Dashboard Help/` regex on each anchor page (per OQ-7-C decision in Task 2).

```ts
import { test, expect } from '@playwright/test'

const ANCHOR_ROUTES = [
  '/help',
  '/help/workflow/overview',
  '/help/repos/overview',
  '/help/observability/overview',
  '/help/operations/install',
]

const STUB_SAMPLES = [
  '/help/workflow/gates',
  '/help/observability/scan',
  '/help/reference/glossary',
]

const REDIRECT_PAIRS: Array<[string, string]> = [
  ['/help/workflow', '/help/workflow/overview'],
  ['/help/repos', '/help/repos/overview'],
  ['/help/observability', '/help/observability/overview'],
  ['/help/operations', '/help/operations/install'],
]

test.describe('Reviewer walking checklist (HELP-01..06)', () => {
  test('every anchor renders without console errors; Mermaid SVG appears', async ({ page }) => {
    const errors: string[] = []
    page.on('console', (msg) => {
      if (msg.type() === 'error') errors.push(msg.text())
    })
    page.on('pageerror', (err) => errors.push(err.message))

    for (const route of ANCHOR_ROUTES) {
      await page.goto(route, { waitUntil: 'networkidle' })
      await expect(page.locator('article.prose')).toBeVisible()
      await expect(page).toHaveTitle(/AgenticApps Dashboard Help/)
      if (route !== '/help/operations/install') {
        // mermaid lazy-loads then runs; give it time
        await expect(page.locator('pre.mermaid, svg.mermaid, .mermaid svg').first()).toBeVisible({ timeout: 8_000 })
      }
    }
    expect(
      errors,
      `Console errors during anchor walk: ${errors.join('\n')}`,
    ).toEqual([])
  })

  test('sampled stub routes render ComingSoon + back-link', async ({ page }) => {
    for (const route of STUB_SAMPLES) {
      await page.goto(route)
      await expect(page.getByRole('heading', { level: 1 })).toBeVisible()
      await expect(page.getByRole('link', { name: /back to/i })).toBeVisible()
    }
  })

  test('section paths redirect to overview/install', async ({ page }) => {
    for (const [from, to] of REDIRECT_PAIRS) {
      await page.goto(from)
      await expect(page).toHaveURL(to)
    }
  })

  test('widget Suspense resolves: repos/overview shows Coming v1.2 badge', async ({ page }) => {
    await page.goto('/help/repos/overview', { waitUntil: 'networkidle' })
    await expect(page.getByText(/coming v1\.2/i)).toBeVisible({ timeout: 5_000 })
  })

  test('? shortcut from / lands on /help docs landing', async ({ page }) => {
    // Skip if not paired — for the dev session we assume the test runs against a paired SPA.
    await page.goto('/')
    await page.keyboard.press('?')
    await expect(page).toHaveURL('/help')
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible()
  })

  test('reference/shortcuts page renders KbdHint chips', async ({ page }) => {
    await page.goto('/help/reference/shortcuts')
    await expect(page.getByRole('heading', { name: /keyboard shortcuts/i, level: 1 })).toBeVisible()
    // KbdHint chips render their keys as plain text inside <span aria-hidden>
    await expect(page.getByText('R').first()).toBeVisible()
    await expect(page.getByText('Cmd').first()).toBeVisible()
  })

  test('catch-all redirect: unknown /help/* path falls to /help', async ({ page }) => {
    await page.goto('/help/this-does-not-exist')
    await expect(page).toHaveURL('/help')
  })
})

test.describe('Mobile viewport HelpLayout (HELP-03)', () => {
  test.use({ viewport: { width: 375, height: 800 } })

  test('sidebar hidden initially; toggle opens it', async ({ page }) => {
    await page.goto('/help')
    const aside = page.getByLabel('Help navigation')
    await expect(aside).toBeHidden()
    await page.getByRole('button', { name: /toggle navigation/i }).click()
    await expect(aside).toBeVisible()
  })
})
```

Commit `test(07-05): Playwright walking-checklist (HELP-01..06 + ROADMAP S1..S8 e2e)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa exec playwright test --list 2>&1 | grep -q "help-walking-checklist"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/e2e/help-walking-checklist.spec.ts` exit 0
    - Playwright lists the spec: `pnpm --filter @agenticapps/dashboard-spa exec playwright test --list 2>&1 | grep -q "help-walking-checklist"` exit 0
    - Spec contains ≥ 7 tests: `grep -c "^\s*test(" packages/spa/e2e/help-walking-checklist.spec.ts` returns ≥ 7
  </acceptance_criteria>
  <done>e2e spec authored; ready for Task 11 closing-ritual execution.</done>
</task>

<task id="07-05-T8" type="auto" tdd="false">
  <name>Task 8: Pre-flight gate — workspace typecheck + tests + lint + build all green</name>
  <files>(verification only)</files>
  <read_first>
    - All files modified in this phase.
  </read_first>
  <behavior>
    - `pnpm -r typecheck` exit 0.
    - `pnpm -r test` exit 0.
    - `pnpm lint` exit 0 (RESEARCH §"feedback_pre-pr-lint-required.md" — ESLint catches react-hooks issues that typecheck/test miss).
    - `pnpm -r build` exit 0.
    - Capture each command's output to a log file in `.planning/phases/07-help-docs-v1-0/evidence/preflight.log`.
  </behavior>
  <action>
1. `mkdir -p .planning/phases/07-help-docs-v1-0/evidence`
2. Run each command and capture:

```bash
pnpm -r typecheck 2>&1 | tee -a .planning/phases/07-help-docs-v1-0/evidence/preflight.log
pnpm -r test 2>&1 | tee -a .planning/phases/07-help-docs-v1-0/evidence/preflight.log
pnpm lint 2>&1 | tee -a .planning/phases/07-help-docs-v1-0/evidence/preflight.log
pnpm -r build 2>&1 | tee -a .planning/phases/07-help-docs-v1-0/evidence/preflight.log
```

If any step exits non-zero, fix the failure and re-run. Commit `chore(07-05): pre-flight gate evidence`.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm -r test && pnpm lint && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0
    - `test -f .planning/phases/07-help-docs-v1-0/evidence/preflight.log` exit 0
    - Log contains green markers for each step
  </acceptance_criteria>
  <done>Pre-flight green; ready for Playwright + impeccable.</done>
</task>

<task id="07-05-T9" type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Playwright walking checklist — autonomous run, human verifies log + screenshots</name>
  <what-built>
Plan 07-05 Tasks 1–8 wired routes + e2e spec. This checkpoint runs the spec against the live dashboard.
  </what-built>
  <how-to-verify>
1. From repo root: `pnpm --filter @agenticapps/dashboard-spa exec playwright test --reporter=line`.
   - Playwright auto-spawns the dev server per `playwright.config.ts` (Plan 07-01).
   - Expected: 7 tests pass on `chromium-desktop` project + 1 mobile test on `chromium-mobile` project.
2. If any test fails, inspect Playwright trace (`playwright show-report`) and fix the underlying issue:
   - `console.error` event → check MermaidBlock console.warn fallback (Plan 07-02 RESEARCH P6).
   - "Coming v1.2" not visible → check widget lazy import path resolves to Plan 07-03's stubs.
   - Redirect failure → check buildHelpRoutes `beforeLoad` shape.
3. After the suite is green, run with `--reporter=html` and copy the report dir to `.planning/phases/07-help-docs-v1-0/evidence/playwright-report/`. Commit.
  </how-to-verify>
  <resume-signal>Type "playwright-green" or describe failing tests for fix-up</resume-signal>
</task>

<task id="07-05-T10" type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Impeccable critique gate on /help — desktop 1440×900, ≥ 90 score</name>
  <what-built>
The dashboard's /help route, fully wired with the docs site. Phase 6 D-6-21 inherited gate requires impeccable critique score ≥ 90 at lg viewport (1440×900). Phase 6.1's amendment to floor 87 (per D-6-09.v1) DOES NOT apply to Phase 7 — v1.0 baseline. The strict ≥ 90 gate is binding.
  </what-built>
  <how-to-verify>
1. Run impeccable critique against `/help`:
   ```bash
   node scripts/check-impeccable-score.mjs --route /help --viewport 1440x900 --out .planning/phases/07-help-docs-v1-0/evidence/impeccable-help-lg.json
   ```
   (Exact command name from Phase 6 06-06 — `check-impeccable-score.mjs`. If the script name differs, find via `find scripts -name '*impeccable*' -type f`.)

2. Inspect the JSON output's `composite.score`:
   - ≥ 90: gate passes. Commit the JSON to evidence/.
   - < 90: fix the failing sub-score (Typography / Layout / Color / Aesthetic per Phase 6.1 patterns) inline. Re-run.

3. Common failure modes for the help docs site:
   - **Layout score**: prose width too wide (`max-w-3xl` on `<main>` should keep ≤ 75ch; verify on /browse).
   - **Typography score**: prose plugin default may need `prose-slate` color override.
   - **Color score**: should be high if all tokens translated correctly (Plan 07-02 T1–T6).
   - **Aesthetic score**: warm-paper bg + purple accent shipped from Phase 5.1; should be ≥ 90 by default.

4. If the score remains < 90 after fixes, the user decides: ship as-is (with documented sub-90 note) or split a Phase 7.1 polish plan.
  </how-to-verify>
  <resume-signal>Type "impeccable-pass" with the score, or describe deltas to fix</resume-signal>
</task>

<task id="07-05-T11" type="checkpoint:human-verify" gate="blocking">
  <name>Task 11: /browse desktop + mobile screenshots committed to evidence/</name>
  <what-built>
Visual evidence for VERIFICATION.md — desktop (1440×900) and mobile (375×800) screenshots of /help landing.
  </what-built>
  <how-to-verify>
1. With the SPA dev server running (Playwright auto-spawned or `pnpm --filter dashboard-spa dev`):
   ```bash
   node packages/spa/scripts/screenshot.mjs --route /help --viewport 1440x900 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-landing-lg.png
   node packages/spa/scripts/screenshot.mjs --route /help --viewport 375x800 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-landing-mobile.png
   node packages/spa/scripts/screenshot.mjs --route /help/workflow/overview --viewport 1440x900 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-workflow-lg.png
   node packages/spa/scripts/screenshot.mjs --route /help/repos/overview --viewport 1440x900 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-repos-lg.png
   node packages/spa/scripts/screenshot.mjs --route /help/observability/overview --viewport 1440x900 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-observability-lg.png
   node packages/spa/scripts/screenshot.mjs --route /help/reference/shortcuts --viewport 1440x900 \
     --out .planning/phases/07-help-docs-v1-0/evidence/help-shortcuts-lg.png
   ```
2. Open each PNG and verify visually:
   - Sidebar: warm-paper bg, all 5 NAV sections, "Keyboard shortcuts" first under Reference.
   - Main: prose typography, Mermaid diagrams render as SVG (NOT raw text).
   - Mobile: sidebar drawer hidden initially, hamburger toggle visible.
3. Commit screenshots: `chore(07-05): /browse evidence screenshots (6 routes × 1-2 viewports)`.

If any screenshot shows visual breakage (broken Mermaid, missing widget, wrong tokens), fix inline and re-screenshot.
  </how-to-verify>
  <resume-signal>Type "screenshots-good" or list issues to fix</resume-signal>
</task>

<task id="07-05-T12" type="checkpoint:human-verify" gate="blocking">
  <name>Task 12: 07-VERIFICATION.md + 07-UAT.md authored with 1:1 evidence per HELP-01..HELP-06 + ROADMAP S1..S8</name>
  <what-built>
Final phase verification documents authored to satisfy D-7-17 and the project's mandatory two-stage review gate.
  </what-built>
  <how-to-verify>
1. Create `.planning/phases/07-help-docs-v1-0/07-VERIFICATION.md` with this structure:

```markdown
# Phase 7: /help docs v1.0 — Verification

## Requirement → Evidence Map

| Requirement | Behavior | Evidence | Status |
|-------------|----------|----------|--------|
| HELP-01 | 5 anchor MDX pages render | `packages/spa/src/help/__tests__/anchor-pages.test.tsx` (5 cases pass) + Playwright walking checklist (5 routes loaded with no console errors) + screenshots/help-{landing,workflow,repos,observability,operations}-lg.png | ✅ |
| HELP-02 | 32 stub paths render ComingSoon | `helpRouteTable.test.ts` (snapshot: 32 stubs) + Playwright 3-sample test passes | ✅ |
| HELP-03 | HelpLayout sidebar + zero console errors | `HelpLayout.test.tsx` (9 cases) + Playwright `console.error.length === 0` assertion + mobile drawer test | ✅ |
| HELP-04 | HelpWidget dispatches 8 stubs | `HelpWidget.test.tsx` (10 cases) + `stubs-smoke.test.tsx` (8 cases) + Playwright "Coming v1.2" badge assertion | ✅ |
| HELP-05 | HelpHook ships + topicToUrl works | `topicToUrl.test.ts` (9 cases) + `HelpHook.test.tsx` (6 cases) | ✅ |
| HELP-06 | Shortcuts at /help/reference/shortcuts; legacy /help deleted; ? shortcut unchanged | `legacy-help-route-deleted.test.ts` (2 cases) + `reference-shortcuts.test.tsx` (4 cases) + Playwright ? shortcut test + existing `useGlobalShortcuts.test.ts` still green | ✅ |

## ROADMAP Success Criteria 1–8

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | SPA dev serves /help landing | Playwright walking checklist + dev-server smoke at Task 8 |
| 2 | All 5 anchor pages render Mermaid diagrams | Playwright SVG assertion + mermaid.parse() syntax test (5 blocks) |
| 3 | 32 stub paths render ComingSoon | helpRouteTable snapshot + Playwright 3-sample test |
| 4 | Section bare paths redirect | Playwright "section paths redirect to overview/install" test |
| 5 | <HelpWidget> dispatches 3 referenced widgets | Playwright "widget Suspense resolves" + anchor-pages test |
| 6 | ? shortcut lands on /help | Playwright "? shortcut from /" test |
| 7 | Dark mode renders correctly (dormant in v1.0) | Documented in CONTEXT.md `<deferred>`; v1.1 verification |
| 8 | Pre-flight green + impeccable ≥ 90 + two-stage review | Task 8 log + Task 10 score + post-merge two-stage review |

## Test Counts

- vitest: ~50 new tests across Plans 07-01..07-05 (smoke + topicToUrl + HelpHook + ComingSoon + HelpWidget + MermaidBlock + HelpLayout + stubs-smoke + frontmatter + mermaid-syntax + anchor-pages + reference-shortcuts + helpRouteTable + legacy-deleted)
- Playwright: 8 tests across 1 spec, 2 projects (chromium-desktop + chromium-mobile)
- Pre-flight: `pnpm -r typecheck && pnpm -r test && pnpm lint && pnpm -r build` all green

## Impeccable Score

`/help` at lg 1440×900: __[fill from Task 10]__/100 (gate ≥ 90).

Sub-scores:
- Color: __
- Typography: __
- Layout: __
- Aesthetic: __

Evidence: `.planning/phases/07-help-docs-v1-0/evidence/impeccable-help-lg.json`.

## Manual UAT

See `07-UAT.md`.

## Sign-off

- [ ] All HELP-01..HELP-06 ✅
- [ ] All ROADMAP S1..S8 met
- [ ] Pre-flight green
- [ ] Playwright green
- [ ] Impeccable ≥ 90
- [ ] /browse screenshots committed
- [ ] 07-UAT.md filled
- [ ] Two-stage review run (post-merge gate)
```

2. Create `.planning/phases/07-help-docs-v1-0/07-UAT.md` covering the manual-only verifications from `07-VALIDATION.md`:
   - Visual coherence at lg vs the migration design.
   - Mermaid readability at desktop + mobile.
   - `?` shortcut keypress check.
   - Document any visual deltas as info-level findings.

3. Walk the reviewer checklist manually one more time (Browser at desktop + mobile). Log results in 07-UAT.md.

4. Commit `docs(07-05): VERIFICATION.md + UAT.md with full evidence map`.
  </how-to-verify>
  <resume-signal>Type "uat-signed" or list outstanding gaps</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route table → TanStack router | helpRouteTable is in-repo, no runtime input; createRoute() instances are statically analyzable. |
| _helpLayout pathless route → AppShellV2 | Peer routes do NOT inherit AppShellV2's auth context (the SPA's pairing gate). `/help/*` therefore renders to unpaired users today. Decision: acceptable for v1.0 because the dashboard's pairing UX is `/onboarding`, and unpaired users hitting `/help` lands them on docs (which is fine — they're trying to figure out how to use the product). Plan 07-05 does NOT add a pairing gate to `_helpLayout`. |
| Playwright webServer spawn | Plan 07-01 hardcoded the dev server command. |
| MDX → router lazy-import | createLazyRoute resolves to module files in `packages/spa/src/help/pages/*.lazy.tsx` — pnpm-tracked, no runtime resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Spoofing | `/help/*` accessible without pairing | accept | Unpaired user accessing docs is intended UX. No project data exposed via /help (purely static docs). |
| T-07-05-02 | Tampering | Catch-all `/help/*` → /help redirect | mitigate | `beforeLoad: throw redirect({ to: '/help' })` is the canonical TanStack pattern; URL is hardcoded. |
| T-07-05-03 | Information Disclosure | Document title leaks page hierarchy | accept | Page titles are public marketing copy ("Welcome", "Repository Topology"). |
| T-07-05-04 | DoS | Mermaid bundle (~600 KB) on first /help visit | mitigate | D-7-10 lazy import; D-7-07 lazy anchor MDX; landing page Mermaid loads on demand. Acceptable for v1.0. |
| T-07-05-05 | XSS | `pre` MDX component reads className from child | mitigate | className check is a string `.includes()`; no DOM evaluation. ChildProps come from MDX-compiled JSX (we control authors). |
| T-07-05-06 | Tampering | Legacy /help route resurfaces | mitigate | `legacy-help-route-deleted.test.ts` asserts file-non-existence; passes CI gate on every commit. |

**Block-on policy:** `high` severity blocks. None of T-07-05-01..06 are `high`.
</threat_model>

<verification>
- All 7 Plan 07-05 task acceptance gates pass.
- _helpLayout exists as peer of _appshell at rootRoute (router.tsx).
- Legacy /help route + test deleted; file-existence test guards regression.
- 43-entry route table mounted via buildHelpRoutes factory.
- mdxComponents finalised with KbdHint + pre fallback.
- Impeccable workflow includes /help.
- Playwright spec runs green (Task 9 checkpoint).
- Impeccable score ≥ 90 on /help (Task 10 checkpoint).
- /browse screenshots committed (Task 11 checkpoint).
- 07-VERIFICATION.md + 07-UAT.md committed with full evidence map (Task 12 checkpoint).
- ROADMAP success criteria 1–8 all met.
- Two-stage review (gstack `/review` + `superpowers:requesting-code-review`) ran on the PR diff (per project ./CLAUDE.md mandate, post-merge gate).
</verification>

<success_criteria>
- HELP-01..HELP-06 all closed.
- ROADMAP S1..S8 all met.
- _helpLayout peer route mounted; old /help route deleted.
- Impeccable score ≥ 90 on /help (lg 1440x900).
- Playwright walking checklist green; zero console.error during walk.
- /browse evidence captured; VERIFICATION.md + UAT.md committed.
- PR diff ready for two-stage review against `main`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-help-docs-v1-0/07-05-SUMMARY.md` describing:
- File-by-file delta (~20 files: route table + factory + lazy wrappers + ComingSoonRoute + HelpPage + router.tsx mod + 2 file deletions + e2e spec + mdxComponents update + impeccable.yml update + evidence dir + VERIFICATION.md + UAT.md).
- Closing-ritual log: Task 8 pre-flight green, Task 9 Playwright green, Task 10 impeccable score, Task 11 screenshots committed, Task 12 UAT signed.
- HELP-01..06 + ROADMAP S1..S8 coverage table.
- Outstanding follow-ups for v1.1: anchor IDs (rehype-slug), sidebar search, HelpHook side-panel, real widget implementations, dark mode prose-invert verification.
- Two-stage review status: Stage 1 (`/review` on phase diff) — paste output; Stage 2 (`superpowers:requesting-code-review`) — note that it requires a fresh session after Stage 1 commits land.

Then update `.planning/STATE.md`:
- `progress.completed_plans` +5
- `progress.completed_phases` +1 (Phase 7 closes the v1.0 milestone + docs site)
- `last_activity` reflects Phase 7 ship.

Then update `.planning/ROADMAP.md`:
- Phase 7 plan checkboxes all `[x]`.
- Phase 7 Status row → `Complete`.
- v1.0 milestone tag note updated to include docs ship.
</output>