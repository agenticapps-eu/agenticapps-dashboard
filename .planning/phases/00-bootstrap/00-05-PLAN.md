---
phase: 00-bootstrap
plan: 05
type: execute
wave: 3
depends_on: [00-01, 00-02, 00-03]
files_modified:
  - README.md
  - docs/deploy/cloudflare-pages-setup.md
autonomous: true
requirements: [BOOT-03, BOOT-05]
requirements_addressed: [BOOT-03, BOOT-05]
threat_refs: [T-00-13]

must_haves:
  truths:
    - "Repo-root README.md opens with an explicit alpha notice"
    - "README contains the three-command install snippet from CONTEXT.md verbatim"
    - "README links to docs/spec/dashboard-prompt.md and ROADMAP.md"
    - "docs/deploy/cloudflare-pages-setup.md documents the (manual) CF Pages dashboard configuration that BOOT-03 depends on"
    - "CF Pages docs cover both preview Access policy AND production Access policy (the easy-to-miss split per RESEARCH Pitfall 7)"
  artifacts:
    - path: "README.md"
      provides: "Repo-root readme with alpha notice + install snippet + spec link"
      contains: "alpha"
    - path: "docs/deploy/cloudflare-pages-setup.md"
      provides: "Reproducible CF Pages dashboard configuration steps"
      contains: "agenticapps-dashboard"
  key_links:
    - from: "README.md"
      to: "docs/spec/dashboard-prompt.md"
      via: "markdown link"
      pattern: "docs/spec/dashboard-prompt.md"
    - from: "README.md"
      to: "@agenticapps/dashboard-agent"
      via: "npx install snippet"
      pattern: "npx @agenticapps/dashboard-agent"
    - from: "docs/deploy/cloudflare-pages-setup.md"
      to: "packages/spa/dist"
      via: "publish dir directive"
      pattern: "packages/spa/dist"
---

<objective>
Write the repo-root `README.md` (BOOT-05) and `docs/deploy/cloudflare-pages-setup.md` (BOOT-03 reproducibility). README opens with an alpha notice, includes the spec-canonical three-command install snippet, points to the spec + roadmap, and stays under one screen of useful content. CF Pages doc captures the human-only dashboard steps that BOOT-03 depends on (build command, publish dir, env vars, Access policies for both preview and production) so the deployment is reproducible even though the CF Pages UI itself is manual.

Purpose: Phase 0 success criteria 5 (README) and reproducibility for criterion 2 (CF Pages preview deploy). After this plan the repo is presentable to a fresh reader and the deployment is documented end-to-end.

Output: Two markdown files. No code; pure docs. Both committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/agenticapps-workflow/skill/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-bootstrap/00-CONTEXT.md
@.planning/phases/00-bootstrap/00-RESEARCH.md
@.planning/phases/00-bootstrap/00-01-PLAN.md
@.planning/phases/00-bootstrap/00-02-PLAN.md
@.planning/phases/00-bootstrap/00-03-PLAN.md
@.planning/phases/00-bootstrap/00-04-PLAN.md
@docs/spec/dashboard-prompt.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-root README.md (alpha notice + install snippet + spec link)</name>
  <files>
    README.md
  </files>
  <read_first>
    - .planning/phases/00-bootstrap/00-CONTEXT.md (§"Specifics" — exact three-command install snippet to use verbatim; §"Claude's Discretion" — README copy guidance "keep to one screen")
    - .planning/REQUIREMENTS.md (BOOT-05 acceptance criteria)
    - .planning/ROADMAP.md (Phase 0 §"Success Criteria" item 5)
    - docs/spec/dashboard-prompt.md (§"CLI surface" — confirms `register` and `start` commands referenced in install snippet; §"Goal" — pulls one-line description for README intro)
    - README.md (current contents — placeholder per CLAUDE.md; replace entirely)
    - CLAUDE.md (confirms repo state at Phase 0, target architecture summary for README intro)
  </read_first>
  <action>
Replace `README.md` entirely with the following content. The three-command install snippet matches CONTEXT.md §"Specifics" verbatim — it references `register` and `start` (Phase 1 commands) so the README + binary message stay coherent during the placeholder period:

````markdown
# AgenticApps Pipeline Dashboard

> **Alpha — placeholder release.** Phases 0–6 ship a complete, useful dashboard with zero third-party service dependencies. We are at Phase 0 (bootstrap). Daemon and SPA logic land in Phase 1 onward. Don't run this against anything you care about yet.

A registry-based, multi-project dashboard that visualizes the running state of the AgenticApps Superpowers + GSD + gstack pipeline across every registered project — from any device, while keeping all data on your own machine.

## Architecture

- **`packages/spa`** — Vite + React + Tailwind static SPA, hosted on Cloudflare Pages, no data stored cloud-side.
- **`packages/agent`** — Node 20+ local daemon (Hono), reads `.planning/`, `.claude/`, `git log` per registered project. Loopback default; bearer-token auth; `0600` config files; no native dependencies.
- **`packages/shared`** — Zod schemas + TS types, single source of truth for daemon ↔ SPA wire shapes.

## Install (alpha)

```bash
npx @agenticapps/dashboard-agent register ~/Sourcecode/your-first-project
npx @agenticapps/dashboard-agent start
# click the printed pair URL
```

Phase 0 publishes `@agenticapps/dashboard-agent@0.0.1-alpha.0` as a placeholder: `--version` prints the version and `start` prints a friendly notice that the real daemon arrives in Phase 1. Use this to verify the install path works against your shell environment before the daemon ships.

## Development

This is a pnpm workspace. Requirements: Node 20+, pnpm 9.5+.

```bash
pnpm install
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

Per-package commands use `--filter`:

```bash
pnpm --filter @agenticapps/dashboard-spa dev      # SPA on http://localhost:5174
pnpm --filter @agenticapps/dashboard-agent build  # Build the CLI bundle
pnpm --filter @agenticapps/dashboard-agent test   # Run agent tests only
```

CI runs the same five gates (install + lint + typecheck + test + build) on push and pull request — see `.github/workflows/ci.yml`. Releases trigger on `v*` tag push — see `.github/workflows/release.yml`.

## Deployment

- **SPA** — Cloudflare Pages (`agenticapps-dashboard`), production URL `https://agenticapps-dashboard.pages.dev`. Custom domain `dashboard.agenticapps.eu` is deferred to a later phase. Setup: [`docs/deploy/cloudflare-pages-setup.md`](docs/deploy/cloudflare-pages-setup.md).
- **Agent** — npm scope `@agenticapps`. Published from a tagged release; consumed via `npx`.

## Documentation

- **Spec (binding):** [`docs/spec/dashboard-prompt.md`](docs/spec/dashboard-prompt.md) — the source of truth that drives every phase. When intuition disagrees with the spec, the spec wins.
- **Roadmap:** [`.planning/ROADMAP.md`](.planning/ROADMAP.md) — nine phases, Phases 0–6 are v1.
- **Requirements:** [`.planning/REQUIREMENTS.md`](.planning/REQUIREMENTS.md) — every requirement traced to a phase.
- **Workflow:** [`.claude/skills/agenticapps-workflow/skill/SKILL.md`](.claude/skills/agenticapps-workflow/skill/SKILL.md) — Superpowers + GSD + gstack contract.

## License

Currently UNLICENSED (no LICENSE file). The repo is private through Phase 6; an MIT LICENSE lands at Phase 8 with the public-readiness flip.
````

Constraints honored:
- "Alpha" notice in the FIRST h1-adjacent block (BOOT-05 success criterion — `grep -q "alpha" README.md` passes via the blockquote line).
- Install snippet matches CONTEXT.md §"Specifics" verbatim (BOOT-05 + spec coherence).
- Spec link uses an explicit relative path so `grep -q "docs/spec/dashboard-prompt.md" README.md` succeeds.
- Length is roughly one screen (~80 lines including code blocks) per CONTEXT §"Claude's Discretion".
- No FAQ / troubleshooting sections — those land in Phase 6 polish per REQUIREMENTS.md POLISH-06.
- License section accurately reflects D-13 ("UNLICENSED" / no LICENSE file in Phase 0; MIT at Phase 8).
- Do NOT mention `dashboard.agenticapps.eu` as a working URL (custom domain deferred per CONTEXT pre-flight).
- Do NOT add badges (CI status, npm version) yet — npm version doesn't exist until first publish; CI badge can be added post-publish in a Phase 6 polish task.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps-dashboard && test -f README.md && grep -qF "alpha" README.md && grep -qF "@agenticapps/dashboard-agent" README.md && grep -qF "npx @agenticapps/dashboard-agent register" README.md && grep -qF "npx @agenticapps/dashboard-agent start" README.md && grep -qF "docs/spec/dashboard-prompt.md" README.md && grep -qF ".planning/ROADMAP.md" README.md && grep -qF "agenticapps-dashboard.pages.dev" README.md && grep -qF "docs/deploy/cloudflare-pages-setup.md" README.md && [ "$(wc -l < README.md)" -lt 200 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `README.md`.
    - File contains the literal string `alpha` (BOOT-05).
    - File contains all three install commands verbatim from CONTEXT.md §"Specifics":
      - `npx @agenticapps/dashboard-agent register ~/Sourcecode/your-first-project`
      - `npx @agenticapps/dashboard-agent start`
      - `# click the printed pair URL`
      Each verifiable with `grep -qF`.
    - File contains a markdown link to `docs/spec/dashboard-prompt.md` (BOOT-05).
    - File contains a markdown link to `.planning/ROADMAP.md`.
    - File contains a markdown link to `docs/deploy/cloudflare-pages-setup.md` (cross-reference to Task 2 output).
    - File mentions `agenticapps-dashboard.pages.dev` (production URL — sets correct expectation).
    - File DOES NOT claim `dashboard.agenticapps.eu` is currently live (custom domain deferred).
    - File mentions UNLICENSED (matches package.json `license: "UNLICENSED"` from Plan 04).
    - File length is under 200 lines (one-screen rule).
    - File has the three primary h2 sections: `## Architecture`, `## Install`, `## Development`. Verify with `grep -c "^## " README.md` returns ≥ 5 (architecture, install, development, deployment, documentation, license).
  </acceptance_criteria>
  <done>
README.md replaces the placeholder; all BOOT-05 acceptance strings + cross-references present; one-screen length.
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/deploy/cloudflare-pages-setup.md (manual CF dashboard reproducibility)</name>
  <files>
    docs/deploy/cloudflare-pages-setup.md
  </files>
  <read_first>
    - .planning/phases/00-bootstrap/00-RESEARCH.md (§"Cloudflare Pages Configuration (Required Human Steps)" — verbatim source for build command, publish dir, env vars; §Pitfall 1 — monorepo root pitfall; §Pitfall 7 — preview vs production Access split)
    - .planning/phases/00-bootstrap/00-CONTEXT.md (§"Code Context" §"Integration Points" — confirms CF Pages project name `agenticapps-dashboard`, secrets present, default URL)
    - .planning/REQUIREMENTS.md (BOOT-03 acceptance criteria)
    - docs/spec/dashboard-prompt.md (§"Cloudflare Access" — confirms email-only policy)
    - Confirm `docs/deploy/` does not exist yet (`ls docs/`); create the directory.
  </read_first>
  <action>
Create `docs/deploy/cloudflare-pages-setup.md`. This documents the manual CF dashboard steps that BOOT-03 depends on. The repo cannot perform these via code (D-11 — no `cloudflare/pages-action` in Phase 0), so we capture them as reproducible instructions.

Source EVERYTHING from RESEARCH.md §"Cloudflare Pages Configuration" — copy values verbatim, do not invent.

```markdown
# Cloudflare Pages — Manual Setup

> **Status:** Pre-flight steps already completed by the user (per `.planning/phases/00-bootstrap/00-CONTEXT.md` §"Code Context"). This document is the reproducibility record — if the Pages project is ever reset or recreated, these are the exact steps to restore it.

## Project

- **Cloudflare Pages project name:** `agenticapps-dashboard`
- **Production URL:** `https://agenticapps-dashboard.pages.dev`
- **Custom domain (`dashboard.agenticapps.eu`):** deferred. Production URL stays `*.pages.dev` through v1.
- **Git integration:** connected to GitHub repo `agenticapps-eu/agenticapps-dashboard`. Push to any branch triggers a preview deploy; push to `main` triggers a production deploy.

## Build configuration

Set in **Cloudflare Pages dashboard → Project → Settings → Builds & deployments → Build configurations**:

| Setting | Value |
|---|---|
| Build command | `pnpm --filter @agenticapps/dashboard-spa build` |
| Build output directory | `packages/spa/dist` |
| Root directory | `/` (repo root, NOT `packages/spa`) |

The root directory MUST stay at `/`. pnpm needs to resolve the workspace root to follow `workspace:*` references. Setting root directory to `packages/spa` would break `@agenticapps/dashboard-shared` resolution. (See `.planning/phases/00-bootstrap/00-RESEARCH.md` §"Common Pitfalls" Pitfall 1.)

## Environment variables

Set in **Settings → Environment variables → Production AND Preview** (apply to both):

| Variable | Value | Why |
|---|---|---|
| `NODE_VERSION` | `20` | Override CF Pages build image default; matches `.nvmrc`. |
| `PNPM_VERSION` | `10` | Match local pnpm version; ensures catalog feature is available (catalog requires pnpm ≥ 9.5; we run 10). |

CF Pages v3 build image defaults to Node 22 / pnpm 10.11.1. Setting these explicitly is belt-and-suspenders against build-image upgrades that change defaults.

## Cloudflare Access — preview deployments

CF Access policies set on a Pages project via **Settings → Enable access policy** protect **only preview deployments** by default. To gate preview URLs:

1. Pages project → Settings → "Enable access policy"
2. Toggle on
3. Configure: **Email-only**, allow your own email address
4. Apply

This covers `<hash>.agenticapps-dashboard.pages.dev` and `<branch>.agenticapps-dashboard.pages.dev`.

## Cloudflare Access — production deployment (separate!)

The production URL `agenticapps-dashboard.pages.dev` is NOT covered by the preview policy above. (See RESEARCH §Pitfall 7.) Configure separately:

1. Cloudflare Zero Trust → Access → Applications → Add application
2. **Self-hosted application**, hostname `agenticapps-dashboard.pages.dev`
3. Policy: **Allow**, **Email**, set to your own email address
4. Save

After both policies are in place, an unauthenticated browser visit to either preview or production URL should show the CF Access email-OTP gate.

## Verification (run after any CF dashboard change)

1. Push a feature branch:
   ```bash
   git push origin feat/cf-pages-smoke-test
   ```
2. Wait ~60 seconds for CF Pages build.
3. Open the PR for the feature branch on GitHub. The "Cloudflare Pages" bot should comment with the preview URL within 1–2 minutes.
4. Click the preview URL. You should hit the CF Access email gate. After authenticating, the SPA should load and show "AgenticApps Dashboard — alpha".

## Triage when builds fail

| Symptom | Likely cause | Fix |
|---|---|---|
| `Cannot find module '@agenticapps/dashboard-shared'` | Root directory is set to `packages/spa` instead of `/` | Reset root directory to `/` in build config |
| `pnpm-workspace.yaml not found` | Same as above | Same fix |
| `ERR_PNPM_FROZEN_LOCKFILE` | Lockfile out of date with `pnpm-workspace.yaml` (catalog drift) | Run `pnpm install` locally and commit the updated lockfile |
| Build fails with Node module errors | Build image using older Node version | Confirm `NODE_VERSION=20` env var is set on BOTH Production and Preview environments |
| Preview URL returns 200 but no CF Access prompt | Preview Access policy not enabled | Settings → Enable access policy → toggle on |

## Reference

- Source-of-truth research: [`.planning/phases/00-bootstrap/00-RESEARCH.md`](../../.planning/phases/00-bootstrap/00-RESEARCH.md) §"Cloudflare Pages Configuration"
- Cloudflare Pages docs: <https://developers.cloudflare.com/pages/configuration/preview-deployments/>
- Decision: [`.planning/phases/00-bootstrap/00-CONTEXT.md`](../../.planning/phases/00-bootstrap/00-CONTEXT.md) D-11 (no `cloudflare/pages-action` in v1)
```

Notes for executor:
- Create the directory `docs/deploy/` if it does not exist.
- Use ASCII (no smart quotes) so CI tooling never trips on encoding.
- Keep the file under 150 lines; we don't need a tutorial — we need a checklist.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps-dashboard && test -f docs/deploy/cloudflare-pages-setup.md && grep -qF "agenticapps-dashboard" docs/deploy/cloudflare-pages-setup.md && grep -qF "pnpm --filter @agenticapps/dashboard-spa build" docs/deploy/cloudflare-pages-setup.md && grep -qF "packages/spa/dist" docs/deploy/cloudflare-pages-setup.md && grep -qF "NODE_VERSION" docs/deploy/cloudflare-pages-setup.md && grep -qF "PNPM_VERSION" docs/deploy/cloudflare-pages-setup.md && grep -qF "Email-only" docs/deploy/cloudflare-pages-setup.md && grep -qF "preview" docs/deploy/cloudflare-pages-setup.md && grep -qF "production" docs/deploy/cloudflare-pages-setup.md && [ "$(wc -l < docs/deploy/cloudflare-pages-setup.md)" -lt 200 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `docs/deploy/cloudflare-pages-setup.md`.
    - File contains the project name `agenticapps-dashboard`.
    - File contains the build command `pnpm --filter @agenticapps/dashboard-spa build` (verbatim).
    - File contains the publish directory `packages/spa/dist` (verbatim).
    - File specifies root directory `/` (NOT `packages/spa`) — guards against RESEARCH Pitfall 1.
    - File contains both `NODE_VERSION` (value 20) AND `PNPM_VERSION` (value 10) env vars.
    - File documents BOTH preview Access policy AND production Access policy as SEPARATE configurations (RESEARCH Pitfall 7) — verifiable with `grep -c "Access" docs/deploy/cloudflare-pages-setup.md` returning ≥ 4.
    - File includes a triage table for build failures (matches RESEARCH §Common Pitfalls).
    - File length under 200 lines.
    - File references the source RESEARCH.md (cross-link).
    - No invented facts — all values trace to RESEARCH §"Cloudflare Pages Configuration" or CONTEXT §"Code Context" §"Integration Points".
  </acceptance_criteria>
  <done>
CF Pages reproducibility doc lives at `docs/deploy/cloudflare-pages-setup.md`; covers build config, env vars, preview + production Access policies, and triage.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → public readers | Repo is private through Phase 6; once flipped public at Phase 8, README is the first thing readers see. Phase 0 README must accurately set expectations (alpha, placeholder). |
| docs/deploy → operators | Anyone with CF dashboard access can mis-apply these instructions; document carefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-13 | I (Information Disclosure) | README revealing internal infrastructure prematurely | accept | Repo is private through Phase 6 (D-13). README mentions production URL (`agenticapps-dashboard.pages.dev`) but that URL is gated by CF Access (email-only). No secrets, internal paths, or sensitive infrastructure are documented. Re-evaluate at Phase 8 public flip. |
| T-00-14 | T (Tampering) | Misconfigured CF Pages root directory leading to wrong build artifact | mitigate | docs/deploy/cloudflare-pages-setup.md explicitly calls out the `packages/spa` root-directory anti-pattern in the triage table; `pnpm-workspace.yaml` resolution failure surfaces as a build error, not a silent wrong-build. |

</threat_model>

<verification>
- README contains all five required strings: `alpha`, the install snippet (3 lines verbatim), spec link, ROADMAP link, deploy doc link.
- docs/deploy/cloudflare-pages-setup.md covers build config + env vars + both Access policies.
- Both files committed; phase verify-work step manually visits a CF Pages preview URL after pushing the phase branch.
</verification>

<success_criteria>
1. README.md opens with alpha notice and install snippet matching CONTEXT.md §"Specifics" verbatim. (BOOT-05)
2. README links to spec, roadmap, and deploy doc. (BOOT-05)
3. docs/deploy/cloudflare-pages-setup.md captures the manual CF dashboard config so BOOT-03 is reproducible. (BOOT-03)
4. CF Pages doc covers BOTH preview AND production Access policies separately (RESEARCH Pitfall 7).
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap/00-05-SUMMARY.md` with:
- README final length (line count) and grep results for required strings.
- CF Pages doc final length and grep results.
- Note: actual CF Pages preview deploy verification happens at phase verify-work (push branch → click PR comment → confirm Access gate + SPA loads).
</output>
