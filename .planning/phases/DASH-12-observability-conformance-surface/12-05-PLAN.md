---
phase: 12
plan: 05
type: tdd
wave: 5
depends_on:
  - 12-00-PLAN.md
files_modified:
  - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx
autonomous: true
requirements:
  - REQ-12-RVP-02
  - REQ-12-RVP-03
requirements_addressed:
  - REQ-12-RVP-02
  - REQ-12-RVP-03
objectives: >
  Coverage page responsive collapse below 768px (D-12-23). Card-per-row layout when
  useViewportBreakpoint() === 'xs'. Bounded scope (D-12-24) — applies to /coverage
  only; /observability/conformance is desktop-first for v1.2.0. Phase 11.1 IMP-01
  <colgroup> width contract is preserved by wrapping the existing table-based render
  in a viewport guard.
notes: >
  Pitfall 9 mitigation: Phase 11.1 IMP-01 asserted identical pixel widths across 3
  table sections via <colgroup>. This plan preserves that invariant by branching on
  useViewportBreakpoint() === 'xs' BEFORE rendering — the desktop branch is unchanged
  (table + colgroup); the mobile branch renders cards. Phase 11.1 test must still pass
  in the desktop branch.
  Plan 12-05 is INDEPENDENT of Wave 2/3/4 (touches only coverage panel files) — could
  run parallel to Wave 2/3 in execution but is gated on Wave 0's useViewportBreakpoint
  hook only.

must_haves:
  truths:
    - "CoverageFamilySection renders the existing table layout when viewport >= sm (default)"
    - "CoverageFamilySection renders card-per-row layout when viewport === xs (<640px per Tailwind 4)"
    - "Mobile card preserves all 4 column states (claudeMd / gitNexus / wiki / workflowVersion) as status pills"
    - "Mobile card preserves the 44×44 touch target for refresh button (Phase 11.2 D-11.2-11)"
    - "Phase 11.1 IMP-01 <colgroup> invariant still passes in desktop branch (regression contract)"
    - "Bounded scope (D-12-24): only /coverage page is responsive; /observability/conformance unchanged"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx"
      provides: "Card-per-row layout for xs viewport"
      contains: "export function CoverageFamilySectionMobile"
    - path: "packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx"
      provides: "Viewport-aware branch between table and mobile card layout"
      contains: "useViewportBreakpoint"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx"
      to: "packages/spa/src/lib/useViewportBreakpoint.ts"
      via: "named import"
      pattern: "useViewportBreakpoint"
    - from: "packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx"
      to: "packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx"
      via: "named import + conditional render"
      pattern: "CoverageFamilySectionMobile"
---

<objective>
Wave 5 — Coverage page responsive collapse below 768px. Single behaviour change in CoverageFamilySection.tsx (branch on viewport) + one new sibling component (CoverageFamilySectionMobile.tsx) that renders the mobile card-per-row layout.

The desktop branch (the existing <table> + <colgroup> + 3-section render) is COMPLETELY UNCHANGED. Phase 11.1 IMP-01 + 11.2 D-11.2-09/10/11/12 invariants are preserved — only the branch-on-viewport adds the mobile path.

The mobile layout per card per repo:
- Repo name + override chip (top-left)
- 4 column status pills (claudeMd / gitNexus / wiki / workflowVersion) arranged in a 2×2 or single-row grid
- Actions row at the bottom (refresh button with 44×44 touch target preserved)

Bounded scope: `/observability/conformance` is NOT responsive in v1.2.0 (D-12-24). Only `/coverage` gets the responsive collapse.
Output: 1 new component + 1 test file + 1 edit to existing component + 1 test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md
@.planning/phases/DASH-12-observability-conformance-surface/12-00-PLAN.md
@packages/spa/src/lib/useViewportBreakpoint.ts
@packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx
@packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx
@packages/spa/src/components/panels/coverage/CoverageRow.tsx
@packages/spa/src/components/panels/coverage/CoverageCell.tsx
@packages/spa/src/components/panels/coverage/coverageColumns.ts
@packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts
@packages/shared/src/schemas/coverage.ts

<interfaces>
<!-- Existing patterns Plan 12-05 must preserve. -->

From packages/spa/src/lib/useViewportBreakpoint.ts (Wave 0):
- export function useViewportBreakpoint(): 'xs' | 'sm' | 'md' | 'lg' | 'xl'
- Returns 'xs' when no min-width matches (i.e. < 640px Tailwind 4 default)

From packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (current):
- Props: { family: CoverageFamily; rows: CoverageRow[]; gitNexusInstallState: GitNexusInstallState; onRefresh: (...) => void; refreshState: ... }
- Renders: family header + install hint (when applicable) + <table> with <colgroup> + <thead> with sticky <th> + <tbody> with CoverageRow components

From packages/spa/src/components/panels/coverage/CoverageRow.tsx (Phase 11.2):
- Renders 4 <CoverageCell> components + actions <td> with refresh button
- Refresh button has min-w/h-[44px] + p-[15px] (Phase 11.2 D-11.2-11)

From packages/spa/src/components/panels/coverage/CoverageCell.tsx (Phase 10 + 11):
- Props: { state: CoverageState; subtext?: string; drift?: ... }
- Renders status icon (fresh ✓ / stale ⚠ / missing ✕ / not-applicable ⚪) + subtext

From packages/spa/src/components/panels/coverage/coverageColumns.ts (Phase 11.1 + 11.2):
- Column width SoT — exported as COVERAGE_COL_WIDTHS for <colgroup> consumers

From packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts (Phase 11.2):
- coverageColumnTooltips: Record<column, string>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoverageFamilySectionMobile.tsx — card-per-row layout (RED to GREEN)</name>
  <files>
    packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx (NEW)
    packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx (NEW)
  </files>
  <read_first>
    packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx (verify NEW)
    packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (full source — to extract the props shape + family-header / install-hint logic)
    packages/spa/src/components/panels/coverage/CoverageRow.tsx (Phase 11.2 — refresh button 44×44 touch target spec)
    packages/spa/src/components/panels/coverage/CoverageCell.tsx (existing cell rendering — reused inside cards)
    .planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md §D-12-23 (locked: card-per-row, 44×44 touch targets preserved)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §Pitfall 9 (don't break Phase 11.1 colgroup invariant)
  </read_first>
  <behavior>
    - Props: identical to CoverageFamilySection.tsx (same family, rows, gitNexusInstallState, refresh callback, inFlightRefreshes Set).
    - Renders: family header (preserved from desktop) + install hint (preserved when applicable) + a list of <article> cards (one per row).
    - Per-card layout (Tailwind classes):
      - Outer: `<article className="rounded-lg border border-border-subtle bg-card-bg p-4 flex flex-col gap-3 mb-3">`
      - Top row: `<header className="flex items-center justify-between gap-2"><h3 className="text-base font-semibold text-text-primary truncate">{repo.name}</h3>{overrideChip}</header>`
      - 4 column status pills in a 2-column grid: `<div className="grid grid-cols-2 gap-2">` each cell is `<div className="flex flex-col gap-1"><span className="text-xs uppercase tracking-wide text-text-tertiary">{coverageColumnTooltips[col].shortLabel ?? col}</span><CoverageCell state={...} subtext={...} drift={...} /></div>`
      - Actions row: `<div className="flex justify-end"><button className="min-w-[44px] min-h-[44px] p-[15px] ...">{refresh icon or spinner}</button></div>`
    - The refresh button preserves the Phase 11.2 min-w/h-[44px] + p-[15px] touch-target invariant.
    - The card layout reuses CoverageCell verbatim (no new state-pill primitive).
    - aria-busy + disabled on the refresh button when row is in inFlightRefreshes Set.
  </behavior>
  <action>
    1. Write packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx FIRST (RED).

    Test cases:
    - it('renders family header text identical to CoverageFamilySection')
    - it('renders install hint when gitNexusInstallState === "not-installed"')
    - it('renders one <article> card per row')
    - it('card header shows repo name')
    - it('card body shows all 4 column states as <CoverageCell> children')
    - it('card body uses a 2-column grid for the 4 cells')
    - it('refresh button has min-w-[44px] AND min-h-[44px] AND p-[15px] (Phase 11.2 invariant)')
    - it('refresh button is disabled + aria-busy when row is in inFlightRefreshes')
    - it('clicking refresh button invokes onRefresh with the correct row')
    - it('no <table> element rendered in mobile layout')
    - it('preserves OverrideChip in card header when overrides > 0')

    2. Run pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySectionMobile --run — confirm RED.

    3. Write packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx. Imports:
    - React
    - type CoverageRow, type CoverageFamily, type GitNexusInstallState from @agenticapps/dashboard-shared
    - CoverageCell from ./CoverageCell
    - OverrideChip from ./OverrideChip (existing component)
    - coverageColumnTooltips from ./coverageColumnTooltips
    - InstallGitNexusButton from ./InstallGitNexusButton (existing component used by desktop section)
    - Loader2, RefreshCw from lucide-react

    Component signature: same props as CoverageFamilySection. Extract the family-header and install-hint logic VERBATIM from CoverageFamilySection.tsx (do NOT introduce a divergent header/install-hint copy — if practical, extract those into a shared sub-component, but that's a secondary refactor — defer if it complicates the diff).

    JSX skeleton:
    ```tsx
    <section aria-labelledby={`family-${family}`}>
      <header className="..."> ... family aggregate counts + install hint ... </header>
      <div className="flex flex-col gap-3">
        {rows.map((row) => {
          const id = `${family}/${row.repo}`
          const isInFlight = inFlightRefreshes.has(id)
          return (
            <article key={id} className="rounded-lg border border-border-subtle bg-card-bg p-4 flex flex-col gap-3">
              <header className="flex items-center justify-between gap-2">
                <h3 className="text-base font-semibold text-text-primary truncate">{row.repo}</h3>
                {row.overrides > 0 && <OverrideChip count={row.overrides} />}
              </header>
              <div className="grid grid-cols-2 gap-3" role="list" aria-label={`Coverage states for ${row.repo}`}>
                {(['claudeMd', 'gitNexus', 'wiki', 'workflowVersion'] as const).map((col) => (
                  <div key={col} role="listitem" className="flex flex-col gap-1">
                    <span className="text-xs uppercase tracking-wide text-text-tertiary">{col}</span>
                    <CoverageCell state={row[col].state} subtext={row[col].subtext} drift={row[col].drift} />
                  </div>
                ))}
              </div>
              <div className="flex justify-end">
                <button
                  type="button"
                  onClick={() => onRefresh({ family, repo: row.repo, action: 'gitnexus-analyze' })}
                  disabled={isInFlight}
                  aria-busy={isInFlight}
                  aria-label={`Refresh GitNexus index for ${row.repo}`}
                  className="min-w-[44px] min-h-[44px] p-[15px] rounded-md bg-card-bg border border-border-subtle text-text-secondary hover:text-text-primary disabled:opacity-50"
                >
                  {isInFlight ? <Loader2 size={14} className="animate-spin" /> : <RefreshCw size={14} />}
                </button>
              </div>
            </article>
          )
        })}
      </div>
    </section>
    ```

    Note: read the actual `CoverageRow` interface to get the correct field names for overrides + subtext + drift — the snippet above is a guide; adapt to real types.

    4. Run pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySectionMobile --run — confirm GREEN.

    5. Commit: test(12-05): add failing tests for CoverageFamilySectionMobile then feat(12-05): mobile card-per-row layout for /coverage <768px (D-12-23).

    Anti-patterns to avoid:
    - DO NOT change touch-target sizes (Phase 11.2 D-11.2-11 invariant).
    - DO NOT render a <table> element in the mobile branch.
    - DO NOT duplicate state-pill rendering — reuse CoverageCell.
    - DO NOT skip aria-label / aria-busy on the refresh button (a11y regression).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySectionMobile --run</automated>
  </verify>
  <done>
    - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx exists with CoverageFamilySectionMobile export.
    - All 11 tests pass.
    - `grep -c "min-w-\[44px\]\|min-h-\[44px\]" packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx` returns at least 2 (Phase 11.2 invariant).
    - `grep -c "<table" packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx` returns 0 (no table in mobile).
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch CoverageFamilySection on useViewportBreakpoint (RED to GREEN)</name>
  <files>
    packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (EDIT — add viewport branch)
    packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx (EDIT — extend with viewport tests)
  </files>
  <read_first>
    packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (current — full file, esp. the top of the component where the render begins)
    packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx (current — Phase 11.1 IMP-01 <colgroup> assertions live here)
    packages/spa/src/lib/useViewportBreakpoint.ts (Wave 0)
    packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx (Task 1)
  </read_first>
  <behavior>
    - CoverageFamilySection.tsx imports `useViewportBreakpoint` from lib AND `CoverageFamilySectionMobile` from the new sibling file.
    - At the top of the render function (after props destructure but BEFORE any JSX): `const bp = useViewportBreakpoint(); if (bp === 'xs') return <CoverageFamilySectionMobile {...allProps} />`
    - The existing desktop render (table + colgroup + tbody) is UNCHANGED below this branch.
    - Phase 11.1 IMP-01 test (asserting `<col>` className equality across 3 sections) continues to pass UNCHANGED — because at jsdom default viewport (1024px), useViewportBreakpoint returns 'lg' (NOT 'xs'), so the desktop branch renders.
    - New tests cover BOTH branches: at jsdom default → desktop table renders; with matchMedia mock returning xs → mobile cards render.
  </behavior>
  <action>
    1. Extend packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx with NEW test cases (do NOT remove or alter the existing Phase 11.1 IMP-01 tests — they MUST continue to pass).

    NEW test cases (RED first):
    - it('renders desktop <table> layout at jsdom default viewport (Phase 11.1 regression contract)')
    - it('renders mobile card layout when matchMedia mock returns xs') — mock window.matchMedia to return matches:false for all min-width queries, forcing useViewportBreakpoint to return 'xs'
    - it('renders <colgroup> in desktop branch (Phase 11.1 IMP-01 regression)') — same as existing test, re-confirmed
    - it('does NOT render <table> in mobile branch')

    2. Run pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySection --run — confirm RED on new tests, existing tests still GREEN.

    3. Edit packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx:

    Add import at the top (near existing component imports):
    ```typescript
    import { useViewportBreakpoint } from '../../../lib/useViewportBreakpoint.js'
    import { CoverageFamilySectionMobile } from './CoverageFamilySectionMobile.js'
    ```

    Inside the function body, AFTER props destructure but BEFORE any other hooks/JSX, INSERT:
    ```typescript
    const breakpoint = useViewportBreakpoint()
    if (breakpoint === 'xs') {
      return (
        <CoverageFamilySectionMobile
          family={family}
          rows={rows}
          gitNexusInstallState={gitNexusInstallState}
          inFlightRefreshes={inFlightRefreshes}
          onRefresh={onRefresh}
          // ... pass through ALL props identically
        />
      )
    }
    ```

    Do NOT touch any of the desktop-branch JSX below this point.

    4. Run pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySection --run — confirm GREEN (new + existing tests).

    5. Run full SPA suite: pnpm --filter @agenticapps/dashboard-spa test --run — verify no regression elsewhere (esp. Phase 11.1 verify-contrast.test.ts + coverageColumns.test.ts).

    6. Commit: test(12-05): add viewport-branch tests to CoverageFamilySection then feat(12-05): branch CoverageFamilySection on viewport <768px → mobile cards (D-12-23, D-12-24).

    Anti-patterns to avoid:
    - DO NOT touch any of the desktop-branch JSX (Phase 11.1 IMP-01 + 11.2 invariants).
    - DO NOT add the viewport branch INSIDE the table render — it MUST be a top-level early return.
    - DO NOT call useViewportBreakpoint conditionally (Rules of Hooks).
    - DO NOT change the props interface — pass through verbatim.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test CoverageFamilySection --run</automated>
  </verify>
  <done>
    - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx contains `useViewportBreakpoint` import + viewport branch.
    - All existing Phase 11.1 + 11.2 tests still pass.
    - New viewport-branch tests pass.
    - `grep -c "useViewportBreakpoint" packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` returns at least 1.
    - `grep -c "CoverageFamilySectionMobile" packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` returns at least 1.
    - Full SPA suite green (no regression).
    - 2 atomic commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewport size → render path | Untrusted client signal — but only switches between two pre-built render paths. No data validation impact. Defence-in-depth: both branches consume identically-shaped props from validated daemon response (parseOrDrift upstream). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verified by Task |
|-----------|----------|-----------|----------|-------------|------------|------------------|
| T-12-RESPONSIVE-DRIFT | Tampering | mobile branch divergence | low | mitigate | Both branches consume the same CoverageRow type from @agenticapps/dashboard-shared. Tests assert all 4 column states render in mobile branch. | Tasks 1 + 2 |
| T-12-RESPONSIVE-A11Y | Information disclosure | a11y regression on mobile | medium | mitigate | aria-label + aria-busy on refresh button; role="list" + role="listitem" on the 4-column grid. Test: "refresh button has aria-label" + "no console errors in a11y axe check". | Task 1 |
| T-12-TOUCH-TARGET | Availability | mobile touch target shrinkage | medium | mitigate | min-w-[44px] + min-h-[44px] + p-[15px] preserved per Phase 11.2 D-11.2-11. Test: "refresh button has min-w-[44px] AND min-h-[44px] AND p-[15px]". | Task 1 |
| T-12-COLGROUP-REGRESSION | Tampering | Phase 11.1 invariant break | high | mitigate | Phase 11.1 <colgroup> assertion runs at jsdom default viewport (1024px → 'lg' → desktop branch). The viewport branch is a top-level early-return BEFORE any of the desktop JSX. Test: "renders <colgroup> in desktop branch (Phase 11.1 IMP-01 regression)" passes. | Task 2 |
</threat_model>

<verification>
- pnpm --filter @agenticapps/dashboard-spa test --run exits 0 (full SPA suite — no Phase 11.1/11.2 regression).
- pnpm --filter @agenticapps/dashboard-spa typecheck exits 0.
- pnpm --filter @agenticapps/dashboard-spa build exits 0.
- Manual smoke (folded into Wave 6 /qa): resize browser to 600px width on /coverage → cards render; resize back to 1280px → table renders.
- 4 atomic commits exist (2 per task).
</verification>

<success_criteria>
Wave 5 complete when:
1. CoverageFamilySectionMobile renders a card-per-row layout with all 4 column states + 44×44 touch target.
2. CoverageFamilySection branches on useViewportBreakpoint() === 'xs' → mobile component.
3. Phase 11.1 IMP-01 <colgroup> invariant still passes (verified by existing tests).
4. Phase 11.2 D-11.2-11 44×44 touch target invariant still passes (re-asserted in mobile tests).
5. Bounded scope honoured: /observability/conformance not touched.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-12-observability-conformance-surface/12-05-SUMMARY.md` with:
- Files created/modified (list)
- Test counts per task
- Confirmation: Phase 11.1 IMP-01 + Phase 11.2 D-11.2-11/12 invariants preserved
- Confirmation: bounded scope honoured (no touch on /observability/conformance files)
</output>
