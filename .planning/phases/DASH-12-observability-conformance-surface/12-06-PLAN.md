---
phase: 12
plan: 06
type: execute
wave: 6
depends_on:
  - 12-00-PLAN.md
  - 12-01-PLAN.md
  - 12-02-PLAN.md
  - 12-03-PLAN.md
  - 12-04-PLAN.md
  - 12-05-PLAN.md
files_modified:
  - .planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md
  - .planning/phases/DASH-12-observability-conformance-surface/12-SECURITY.md
  - .planning/phases/DASH-12-observability-conformance-surface/12-IMPECCABLE.md
  - .planning/phases/DASH-12-observability-conformance-surface/12-HUMAN-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REQ-12-IMP-01
requirements_addressed:
  - REQ-12-IMP-01
objectives: >
  Wave 6 — post-phase gates. Sequential, mostly manual. Stage 1 `/review` of the full
  phase diff, Stage 2 `superpowers:requesting-code-review`, `/cso` audit on the new
  daemon write surface (REQUIRED per D-12-26), `/qa` walkthrough covering trend chart +
  family cards + fix-path flow + responsive collapse, `Skill: impeccable critique` at
  1440×900 → `12-IMPECCABLE.md` (calibration data point #5 for D-10.5-03). Closes
  REQUIREMENTS.md REQ-12-NN entries.
notes: >
  This wave is autonomous: false — every task is a checkpoint:human-verify or
  checkpoint:decision. Sequential within the wave (each gate's verdict informs the next).
  /qa walkthrough also folds the Coverage <768px screenshot pass (Open Question 5
  resolution) into the same IMPECCABLE artifact opportunistically — if time permits;
  else punted to Phase 12.x.

must_haves:
  truths:
    - "12-REVIEW.md exists with both Stage 1 (/review) and Stage 2 (superpowers:requesting-code-review) verdicts captured"
    - "12-SECURITY.md exists from /cso audit; every threat in Plan 12-02 threat model has a verdict (mitigated / accepted / transferred)"
    - "12-IMPECCABLE.md exists with composite + per-heuristic scores + findings + persona red flags at 1440×900"
    - "12-HUMAN-UAT.md exists with /qa walkthrough findings on /observability/conformance + /coverage responsive"
    - "REQUIREMENTS.md REQ-12-NN entries marked Complete (or Partial with explicit reason)"
  artifacts:
    - path: ".planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md"
      provides: "Two-stage review verdict"
    - path: ".planning/phases/DASH-12-observability-conformance-surface/12-SECURITY.md"
      provides: "/cso threat-model verdict"
    - path: ".planning/phases/DASH-12-observability-conformance-surface/12-IMPECCABLE.md"
      provides: "Calibration data point #5"
    - path: ".planning/phases/DASH-12-observability-conformance-surface/12-HUMAN-UAT.md"
      provides: "/qa walkthrough findings"
  key_links:
    - from: ".planning/phases/DASH-12-observability-conformance-surface/12-SECURITY.md"
      to: "Plan 12-02 threat model (T-12-PATH-TRAVERSAL et al)"
      via: "Verdict per threat ID"
      pattern: "T-12-"
    - from: ".planning/phases/DASH-12-observability-conformance-surface/12-IMPECCABLE.md"
      to: "D-10.5-03 calibration table"
      via: "calibration data point #5"
      pattern: "calibration"
---

<objective>
Wave 6 closes Phase 12 with the canonical 5-gate sequence. Six checkpoint tasks. Sequential — each task waits on the previous one's verdict.

Sequence:
1. **Stage 1 /review** — run /review on the full phase 12 diff. Output: REVIEW.md "Stage 1" section.
2. **/cso audit** — audit the new daemon write surface (POST /api/admin/registry/fix-path + drift detector). Output: SECURITY.md with verdict per threat ID.
3. **Stage 2 superpowers:requesting-code-review** — fresh-context code review (separate from Stage 1). Output: REVIEW.md "Stage 2" section.
4. **/qa walkthrough** — Live walkthrough on /observability/conformance + /coverage at 767/768/1024px viewports. Output: HUMAN-UAT.md.
5. **Skill: impeccable critique** at 1440×900 on /observability/conformance — capture composite + per-heuristic + findings. Bonus: 5-min /coverage <768px screenshot pass (Open Question 5). Output: 12-IMPECCABLE.md (calibration data point #5).
6. **REQUIREMENTS.md tick-off** — mark all REQ-12-NN entries Complete (or Partial with reason).

Purpose: Ship Phase 12 with full quality discipline. Calibration data point #5 settles D-10.5-03 floor recalibration (was provisional pending data point #5).
Output: 4 new artifact .md files in the phase dir + 1 edit to REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md
@.planning/phases/DASH-12-observability-conformance-surface/12-02-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/DASH-10.5-impeccable-skill-driven-gate/10.5-DECISIONS.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md
@.planning/phases/DASH-11-coverage-trends-skill-drift/11-IMPECCABLE.md
@.planning/phases/DASH-11.1-impeccable-p1-polish-bundle/11.1-IMPECCABLE.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-IMPECCABLE.md

<interfaces>
<!-- Existing process artifacts Plan 12-06 produces. -->

12-REVIEW.md structure (from Phase 10/11/11.1/11.2 precedent):
- ## Stage 1 — /review (gstack)
  - Findings table: severity / file:line / description / status
  - Verdict: PASS / FAIL with N errors / M warnings / K info
- ## Stage 2 — superpowers:requesting-code-review (fresh-context)
  - <finding severity="..."> blocks
  - Verdict: Ready to merge / Needs changes

12-SECURITY.md structure (from Phase 10 10-SECURITY.md precedent):
- Threat IDs from Plan 12-02 + Plan 12-03 + any /cso surfaces additional
- Per threat: ASVS category / Component / Severity / Disposition / Mitigation evidence / Verdict
- Overall verdict: SHIP / HOLD

12-IMPECCABLE.md structure (from Phase 11.1/11.2 precedent):
- Composite score (0-100)
- Per-heuristic scores: Typography / Color / Layout / Hierarchy / Affordance / Motion / Feedback / Empty states / Errors / Loading / Density / a11y / Microcopy / Brand / Performance
- Findings: P1 / P2 / P3 + carry-over Phase 12.x candidates
- Personas: red flags for Director (Drew), Maker (Marie), Editor (Eli) personas
- Calibration data point #5 for D-10.5-03

12-HUMAN-UAT.md structure (from Phase 10 10-HUMAN-UAT.md precedent):
- Walkthrough script with N scenarios
- Per scenario: steps + expected + actual + verdict
- D-12-28 coverage: trend chart hover/focus/keyboard reveal, family card tier at 90/89/70/69, fix-path flow happy + error paths, responsive collapse at 767/768/1024px
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stage 1 /review on the full Phase 12 diff</name>
  <files>
    .planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md (NEW — Stage 1 section)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user will run /review against the Phase 12 branch in a fresh-context session and report verdict. The executor MUST NOT proceed past this task without an explicit user signal matching <resume-signal> below. After the user reports the verdict, the executor writes 12-REVIEW.md (Stage 1 section) capturing what the user reported.</action>
  <what-built>
    Phase 12 ships:
    - 2 new daemon routes (/api/observability/conformance, /api/admin/registry/fix-path)
    - 5 new daemon libs (conformanceCache, conformanceScore, snapshotFleetReader, conformanceScan, registryPathDrift)
    - 1 new shared schema (conformance.ts with ConformanceResponseSchema + tierOf)
    - 4 new SPA components (FleetTrendChart, FamilyCard, PathDriftPanel, ConformancePage) + 1 mobile sibling (CoverageFamilySectionMobile)
    - 1 new SPA hook (useViewportBreakpoint)
    - 1 new lazy route (/observability/conformance)
    - 1 new query module (conformanceQueries with useConformance + useRegistryFixPath)
    - 2 edits (Sidebar.tsx 3rd entry, snapshotPaths.ts retention bump)
    - 1 router edit (conformanceRoute registration)
    Phase requirement IDs minted (REQ-12-NN) addressed across all plans.
  </what-built>
  <how-to-verify>
    1. Switch to a clean fresh-context Claude session.
    2. Run `/review` against the Phase 12 branch (HEAD vs main).
    3. Capture verdict + finding count breakdown in 12-REVIEW.md "Stage 1" section. Format mirrors prior phases (10/11/11.1/11.2 REVIEW.md).
    4. Required sections:
       - Files reviewed (count + paths)
       - Findings: severity / file:line / description / status
       - Verdict: PASS / FAIL with N errors / M warnings / K info
    5. If FAIL with high-severity errors → STOP. Surface the gating findings to the user; fold the fix into a small follow-up plan (e.g. 12-07 if needed) before proceeding to Stage 2.
    6. If PASS or only low-severity findings → proceed to Task 2 (/cso).
  </how-to-verify>
  <resume-signal>Type "stage-1-pass" + paste verdict line, OR describe blocking findings.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md</automated>
  </verify>
  <done>
    12-REVIEW.md exists with Stage 1 section + verdict.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: /cso audit on new daemon write surface (REQUIRED per D-12-26)</name>
  <files>
    .planning/phases/DASH-12-observability-conformance-surface/12-SECURITY.md (NEW)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user will run /cso against the Phase 12 branch (focus: new daemon write surface POST /api/admin/registry/fix-path + drift detector). The executor MUST NOT proceed past this task without a "cso-pass" signal. After the user reports the verdict, the executor writes 12-SECURITY.md capturing per-threat verdicts mapped against the Plan 12-02 threat model (T-12-PATH-TRAVERSAL through T-12-CACHE-STALE) plus any additional threats /cso surfaced.</action>
  <what-built>
    The Phase 12 new write surface: POST /api/admin/registry/fix-path. Threat model captured inline in Plan 12-02 covering 11 threats (T-12-PATH-TRAVERSAL, T-12-SYMLINK-ESCAPE, T-12-CONCURRENT-WRITE, T-12-CSRF, T-12-AUTH, T-12-INFO-DISCLOSURE, T-12-REGISTRY-CORRUPTION, T-12-DENIAL-OF-SERVICE, T-12-SUPPLY-CHAIN, T-12-IDEMPOTENT-FAIL, T-12-CACHE-STALE). All have implementation defences shipped + tests asserting each defence.

    Read-only surfaces also added: GET /api/observability/conformance (no write surface — bearer auth inherited). Plus registryPathDrift detector reads .git/config inside family roots (read-only).
  </what-built>
  <how-to-verify>
    1. Run `/cso` audit against the Phase 12 diff.
    2. For EACH threat ID in Plan 12-02 threat model: verify the implementation matches the documented mitigation. Read the code (registryFixPath.ts, registryPathDrift.ts) and confirm:
       - canonicaliseRoot is called BEFORE family-root containment check (Pitfall 7 ordering)
       - assertRegistrationAllowed blocklist invoked
       - realpath of family roots is awaited inside the route handler (not cached)
       - rate-limiter is invoked BEFORE Zod body parse (rejected fast to avoid wasted compute)
       - error responses use structured codes only (no FS path leakage)
       - writeRegistry → atomicWriteFile O_NOFOLLOW + O_EXCL chain intact
       - invalidateConformanceCache + invalidateCoverageCache called AFTER successful write
    3. Spot-check additional surfaces that /cso may flag:
       - Daemon does NOT touch any registered project's filesystem (INV-01)
       - registryPathDrift uses fs.readFile + regex for .git/config (no subprocess invocation)
       - PathDriftPanel maxLength={4096} on manual paste input
    4. Write 12-SECURITY.md with per-threat verdict:
       - Threat ID / ASVS category / Severity / Disposition / Mitigation evidence (file:line) / Verdict (Mitigated / Accepted / Transferred / Not Applicable)
    5. Overall verdict: SHIP / HOLD.
    6. If HOLD with high-severity gap → STOP. Fold the fix into a follow-up plan before proceeding.
  </how-to-verify>
  <resume-signal>Type "cso-pass" + paste overall verdict, OR describe blocking gaps.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/DASH-12-observability-conformance-surface/12-SECURITY.md</automated>
  </verify>
  <done>
    12-SECURITY.md exists with verdict for each T-12-NN threat ID + overall SHIP/HOLD.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Stage 2 superpowers:requesting-code-review (fresh-context)</name>
  <files>
    .planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md (EDIT — append Stage 2 section)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user runs superpowers:requesting-code-review in a fresh-context session. The executor MUST NOT proceed without a "stage-2-ready" signal. After the verdict, the executor appends the Stage 2 section to 12-REVIEW.md including the &lt;finding severity&gt; blocks the user pastes.</action>
  <what-built>
    Same surface as Task 1 (full Phase 12 diff), reviewed under the Stage 2 contract: fresh-context Claude session uses `superpowers:requesting-code-review` to produce <finding severity="..."> blocks. This is distinct from Stage 1 (/review) — they must NOT collapse (D-12-27 workflow contract).
  </what-built>
  <how-to-verify>
    1. Open a fresh-context Claude session (or after /clear).
    2. Run `superpowers:requesting-code-review` against the Phase 12 branch.
    3. Capture <finding severity="..."> blocks in 12-REVIEW.md "Stage 2" section.
    4. Required sections:
       - Files reviewed
       - Findings: <finding severity="critical|high|medium|low|info"><file>...</file><line>...</line><description>...</description></finding>
       - Verdict: Ready to merge / Needs changes
    5. If Needs changes → STOP. Address findings + re-run Stage 2.
    6. If Ready to merge → proceed to Task 4 (/qa walkthrough).
  </how-to-verify>
  <resume-signal>Type "stage-2-ready" + paste verdict, OR describe blocking findings.</resume-signal>
  <verify>
    <automated>grep -c "Stage 2" .planning/phases/DASH-12-observability-conformance-surface/12-REVIEW.md</automated>
  </verify>
  <done>
    12-REVIEW.md has both Stage 1 + Stage 2 sections with verdicts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: /qa walkthrough (D-12-28 coverage)</name>
  <files>
    .planning/phases/DASH-12-observability-conformance-surface/12-HUMAN-UAT.md (NEW)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user runs the 5-scenario walkthrough described under &lt;how-to-verify&gt; (Conformance trend chart reveals + Family card tiers + Fix-path happy + Fix-path errors + Coverage responsive collapse). The executor MUST NOT proceed without a "qa-pass" signal. After the user reports scenario-by-scenario verdicts, the executor writes 12-HUMAN-UAT.md.</action>
  <what-built>
    Two routes: /observability/conformance (new) and /coverage (responsive collapse added). Plus POST /api/admin/registry/fix-path mutation.
  </what-built>
  <how-to-verify>
    1. Start the dev environment:
       - pnpm --filter @agenticapps/dashboard-agent build && pnpm --filter @agenticapps/dashboard-agent start
       - pnpm --filter @agenticapps/dashboard-spa dev (in another terminal)
       - Pair the SPA against the daemon if not already paired
    2. Walkthrough scenarios (D-12-28 — minimum coverage):

       Scenario 1: Conformance trend chart hover/focus/keyboard reveal
       - Navigate to /observability/conformance
       - Hover over the chart on a specific day → breakdown panel appears top-right with date + 5 numbers
       - Tab into the chart from the family cards → focus moves through invisible per-day rects → breakdown updates
       - Press Escape → breakdown closes
       - On iPad-portrait via Tailscale: tap a day-tick → breakdown reveals (no hover-only)

       Scenario 2: Family card tier coloring at boundaries
       - With seeded data: agenticapps score 91 → green tier; factiv score 89 → amber tier; neuroflash score 70 → amber tier; (verify 69 case if possible) → red tier
       - Tier pill background + text use the correct status tokens

       Scenario 3: Fix-path flow — happy path
       - Open registry editor (or simulate via daemon CLI): rename a registered project's directory on disk (or symlink it to a new location)
       - Refresh /observability/conformance → drifted entry appears in PathDriftPanel
       - If suggestedPath inferred: click "Fix path" → toast "Fixed registry path for {id}" → drifted entry disappears
       - If suggestedPath null: paste the corrected path → button enables → click → toast → drift cleared

       Scenario 4: Fix-path flow — error paths
       - Try fixing with newPath /etc → 422 newPath_blocked → red toast "Path is blocked"
       - Try fixing with newPath ~/Documents/foo (outside family roots) → 422 newPath_outside_family_roots → red toast "Path is outside the family roots"
       - Send 11 fix-path requests in rapid succession (or use rl test harness) → 11th gets 429 rate_limited → red toast

       Scenario 5: Coverage responsive collapse
       - Resize browser to 767px width → CoverageFamilySection re-renders as cards
       - Resize to 768px → CoverageFamilySection re-renders as table (Phase 11.1 colgroup intact)
       - Resize to 1024px → table renders with all sticky-header / column-width invariants
       - On iPad-portrait via Tailscale: /coverage renders cards; refresh button is 44×44

    3. Per scenario record: steps + expected + actual + verdict (PASS / FAIL / OBSERVED-ISSUE).
    4. If any FAIL: STOP and fix before proceeding to impeccable critique.
    5. If all PASS: proceed to Task 5.
  </how-to-verify>
  <resume-signal>Type "qa-pass" + paste scenario counts, OR describe failing scenarios.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/DASH-12-observability-conformance-surface/12-HUMAN-UAT.md</automated>
  </verify>
  <done>
    12-HUMAN-UAT.md exists with ≥5 scenarios + per-scenario verdicts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Skill: impeccable critique at 1440×900 — calibration data point #5</name>
  <files>
    .planning/phases/DASH-12-observability-conformance-surface/12-IMPECCABLE.md (NEW)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user runs the impeccable:critique skill at 1440×900 on /observability/conformance. The executor MUST NOT proceed without an "impeccable-pass {score}" signal. After the user reports the composite + per-heuristic + findings, the executor writes 12-IMPECCABLE.md and updates the D-10.5-03 calibration table with data point #5.</action>
  <what-built>
    /observability/conformance page (primary target — desktop-first per D-12-24). Bonus opportunistic: /coverage at <768px (responsive collapse — Open Question 5 fold).
  </what-built>
  <how-to-verify>
    1. Ensure the dev server is running and /observability/conformance renders with seeded data (≥14 days of NDJSON, ≥3 family scores, ≥1 drifted entry for the panel).
    2. Run the `impeccable:critique` skill against:
       - PRIMARY: /observability/conformance at 1440×900
       - BONUS (if time permits): /coverage at <768px (capture a single screenshot at 600px width; do NOT run full critique — that's potential Phase 12.x scope)
    3. Capture in 12-IMPECCABLE.md:
       - Composite score (0-100 integer)
       - Per-heuristic scores: Typography / Color / Layout / Hierarchy / Affordance / Motion / Feedback / Empty states / Errors / Loading / Density / a11y / Microcopy / Brand / Performance
       - Findings: P1 (must-fix) / P2 (should-fix) / P3 (nice-to-have)
       - Persona red flags (Director / Maker / Editor)
       - Phase 12.x candidate carry-overs
       - Calibration data point #5 for D-10.5-03 — append to the calibration table in 10.5-DECISIONS.md or reference it
    4. Threshold:
       - Composite ≥ 87 → PASS (clears the legacy floor)
       - Composite < 87 but ≥ Phase 11.x calibration band (74-76 or recalibrated value) → ACCEPT with structural-debt waiver (D-10.5-03.calibration-2 clause if active)
       - Composite catastrophically low (< 60) → HOLD: fold P1s into 12-07 polish plan before merge
    5. If P1 findings exist that block ship → STOP. Decide between fold-into-12-07 vs accept-with-deferred.
    6. If composite ≥ 87 OR accepted with documented waiver → proceed to Task 6.
  </how-to-verify>
  <resume-signal>Type "impeccable-pass {composite_score}" + paste P1/P2/P3 counts, OR describe blocking P1s.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/DASH-12-observability-conformance-surface/12-IMPECCABLE.md</automated>
  </verify>
  <done>
    12-IMPECCABLE.md exists with composite + per-heuristic scores + findings + persona red flags.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: REQUIREMENTS.md tick-off + ROADMAP update</name>
  <files>
    .planning/REQUIREMENTS.md (EDIT — add Phase 12 section + tick-off table entries)
    .planning/ROADMAP.md (EDIT — mark Phase 12 as Complete + PR reference)
  </files>
  <action>Pause execution and present the user with a checkpoint. The user confirms whether all REQ-12-NN entries should be marked Complete (or Partial with explicit reason). The executor MUST NOT proceed without a "ticked-off" signal. After confirmation, the executor edits .planning/REQUIREMENTS.md (mark traceability entries from Pending → Complete or Partial), updates .planning/ROADMAP.md (Phase 12 Progress row, PR reference if available), and updates .planning/STATE.md (move Current Position past Phase 12, add Decisions, add Performance Metrics row, update milestone status).</action>
  <what-built>
    All 5 prior plans (12-00 through 12-05) shipped. Phase 12 requirement IDs minted during planning need to be added to REQUIREMENTS.md under a new section and ticked off.
  </what-built>
  <how-to-verify>
    1. Open .planning/REQUIREMENTS.md and add a new section `## Phase 12 — Observability Conformance Surface` after the Phase 11.1 section. Include all minted REQ-12-NN entries with full descriptions matching the working stems in this plan set:
       - REQ-12-CON-01 (shared schema)
       - REQ-12-CON-02 (computeConformanceScores)
       - REQ-12-CON-03 (tierOf mapping)
       - REQ-12-CON-04 (GET /api/observability/conformance route)
       - REQ-12-CON-05 (conformanceScan aggregator)
       - REQ-12-FCH-01..05 (FleetTrendChart fields)
       - REQ-12-RPD-01..04 (registry path drift detector + inference + route + panel)
       - REQ-12-RVP-01..03 (useViewportBreakpoint + CoverageFamilySection responsive + bounded scope)
       - REQ-12-PAGE-01..02 (route + page composition)
       - REQ-12-NAV-01 (sidebar 3rd entry)
       - REQ-12-IMP-01 (impeccable artifact)
       - REQ-12-FOUNDATION-01 (RETENTION_DAYS bump)
    2. Append the REQ-12-NN entries to the Traceability table at the bottom of REQUIREMENTS.md with status "Complete" (or "Partial" with explicit reason).
    3. Open .planning/ROADMAP.md and update the Phase 12 entry:
       - Mark as [x] Complete on the milestone line
       - Update the Progress table row for Phase 12 with "N/N Complete" and the merge date
       - If a PR exists, reference it
    4. Verify gsd-tools recognises Phase 12 as complete:
       - node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" status (or equivalent verification command)
    5. Update STATE.md:
       - Move Current Position from Phase 12 to whatever's next (Phase 13 if planned, else "v1.2 close-out")
       - Add Decisions for Phase 12 (D-12-01 through D-12-29 ratified + any PD-12-NN from execution)
       - Add Performance Metrics row for Phase 12 plans
       - Update milestone: v1.2 — Fleet conformance & drift visibility — [opened/in-progress/complete] per actual state
    6. Final commit on the phase branch covering REQUIREMENTS.md + ROADMAP.md + STATE.md updates.
  </how-to-verify>
  <resume-signal>Type "ticked-off" + paste the REQ count, OR describe issues.</resume-signal>
  <verify>
    <automated>grep -c "REQ-12-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md has ≥ 18 REQ-12-NN entries (the count above). ROADMAP.md Phase 12 entry marked Complete. STATE.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 12 code diff → reviewers | Two independent reviews (Stage 1 + Stage 2) + /cso audit + /qa walkthrough. Each is an independent verification surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verified by Task |
|-----------|----------|-----------|----------|-------------|------------|------------------|
| T-12-REVIEW-COLLAPSE | Process integrity | Two-stage review | high | mitigate | D-12-27 workflow contract — Stage 1 and Stage 2 MUST NOT collapse. This plan structures Tasks 1 + 3 as separate checkpoints with distinct verdicts. | Tasks 1 + 3 |
| T-12-CSO-SKIP | Process integrity | /cso gate | high | mitigate | D-12-26 — /cso REQUIRED. This plan structures Task 2 as a blocking checkpoint. | Task 2 |
| T-12-IMPECCABLE-SKIP | Process integrity | Impeccable artifact | high | mitigate | D-12-29 — artifact MUST exist post-merge. Task 5 produces it. | Task 5 |
</threat_model>

<verification>
- All 6 tasks complete with their respective artifacts written.
- pnpm -r test --run exits 0 (full regression suite green at merge time).
- pnpm -r typecheck exits 0.
- pnpm -r build exits 0.
- All 4 new .md artifacts exist in .planning/phases/DASH-12-observability-conformance-surface/.
- REQUIREMENTS.md has ≥ 18 REQ-12-NN entries (depending on final REQ count).
</verification>

<success_criteria>
Wave 6 complete when:
1. Stage 1 + Stage 2 reviews both PASS (or all findings are non-blocking + documented).
2. /cso verdict is SHIP with every Plan 12-02 threat ID marked Mitigated/Accepted.
3. /qa walkthrough scenarios all PASS.
4. /impeccable critique composite ≥ 87 (OR accepted with structural-debt waiver per D-10.5-03 calibration policy).
5. REQUIREMENTS.md updated; ROADMAP.md updated; STATE.md updated.
6. Phase 12 merge-ready — branch can be PR'd against main.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-12-observability-conformance-surface/12-06-SUMMARY.md` with:
- Stage 1 + Stage 2 verdict summary
- /cso verdict
- /qa scenario count + verdict
- Impeccable composite + per-heuristic + calibration data point note
- REQ-12-NN count added to REQUIREMENTS.md
- ROADMAP.md update note
- One-sentence "v1.2 milestone status" reflection
</output>
