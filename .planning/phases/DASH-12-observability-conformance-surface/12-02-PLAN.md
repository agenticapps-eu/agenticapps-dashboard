---
phase: 12
plan: 02
type: tdd
wave: 2
depends_on:
  - 12-00-PLAN.md
  - 12-01-PLAN.md
files_modified:
  - packages/agent/src/lib/conformanceCache.ts
  - packages/agent/src/lib/conformanceCache.test.ts
  - packages/agent/src/lib/registryPathDrift.ts
  - packages/agent/src/lib/registryPathDrift.test.ts
  - packages/agent/src/lib/conformanceScan.ts
  - packages/agent/src/lib/conformanceScan.test.ts
  - packages/agent/src/routes/conformance.ts
  - packages/agent/src/routes/conformance.test.ts
  - packages/agent/src/routes/registryFixPath.ts
  - packages/agent/src/routes/registryFixPath.test.ts
  - packages/agent/src/server/app.ts
autonomous: true
requirements:
  - REQ-12-CON-04
  - REQ-12-CON-05
  - REQ-12-RPD-01
  - REQ-12-RPD-02
  - REQ-12-RPD-03
requirements_addressed:
  - REQ-12-CON-04
  - REQ-12-CON-05
  - REQ-12-RPD-01
  - REQ-12-RPD-02
  - REQ-12-RPD-03
objectives: >
  Daemon wire surface for Phase 12. Composes Wave 1 primitives (conformanceScore +
  snapshotFleetReader) into two new routes — GET /api/observability/conformance
  (bulk-per-family payload, D-12-14/16/17) and POST /api/admin/registry/fix-path
  (registry mutation route, D-12-19 — the new security surface that /cso must audit).
  Plus three new daemon libs: conformanceCache (30s TTL), registryPathDrift (detector
  + suggested-path inference), conformanceScan (orchestrator).
notes: >
  The fix-path route is THE Phase 12 threat surface. Every threat in <threat_model> below
  must be covered by an explicit test. /cso (Wave 6) will audit; this plan ships the
  defences inline so /cso has something to verify.
  A4 ratified: atomicWriteFile (via writeRegistry) + rate-limiter (10/10s/token) bounds
  concurrent fix-path races; no proper-lockfile needed.
  A7 ratified: existing assertRegistrationAllowed blocklist (18 system roots + 9 secret
  dirs + CONFIG_DIR) is sufficient for newPath validation.
  Implementation note: .git/config origin URL parsing uses direct fs.readFile + regex
  (NO subprocess) — RESEARCH §Environment availability fallback. No new runtime deps.

must_haves:
  truths:
    - "GET /api/observability/conformance returns ConformanceResponse-shaped payload with bearer auth"
    - "POST /api/admin/registry/fix-path rejects newPath with .., absolute outside family roots, symlink escape, missing project, malformed body"
    - "POST /api/admin/registry/fix-path mutates registry.json atomically (writeRegistry → atomicWriteFile O_NOFOLLOW + O_EXCL)"
    - "POST /api/admin/registry/fix-path is rate-limited via rlConsume(tokHash) — 429 on burst"
    - "POST /api/admin/registry/fix-path invalidates conformanceCache + coverageCache on success"
    - "registryPathDrift detector flags missing / symlink-target-changed / git-remote-changed"
    - "suggested-path inference reads .git/config remote.origin.url via fs.readFile + regex; returns null on failure"
    - "conformanceCache is 30s TTL singleton (matches Phase 10/11 cache cadence)"
    - "App mounts conformanceRoute + registryFixPathRoute under bearer-auth + CORS lock"
  artifacts:
    - path: "packages/agent/src/lib/conformanceCache.ts"
      provides: "30s TTL singleton cache for ConformanceResponse"
      contains: "export const TTL_MS = 30_000"
    - path: "packages/agent/src/lib/registryPathDrift.ts"
      provides: "Drift detector + suggested-path inference"
      contains: "export async function detectPathDrift"
    - path: "packages/agent/src/lib/conformanceScan.ts"
      provides: "Orchestrator composing coverageScan + conformanceScore + snapshotFleetReader + drift detector"
      contains: "export async function scanConformance"
    - path: "packages/agent/src/routes/conformance.ts"
      provides: "GET /api/observability/conformance"
      contains: "conformanceRoute.get('/observability/conformance'"
    - path: "packages/agent/src/routes/registryFixPath.ts"
      provides: "POST /api/admin/registry/fix-path"
      contains: "registryFixPathRoute.post('/registry/fix-path'"
  key_links:
    - from: "packages/agent/src/server/app.ts"
      to: "conformanceRoute + registryFixPathRoute"
      via: "app.route('/api', ...) + app.route('/api/admin', ...)"
      pattern: "app.route\\('/api'"
    - from: "packages/agent/src/routes/registryFixPath.ts"
      to: "atomicWriteFile via writeRegistry"
      via: "import { writeRegistry } from '../lib/registry.js'"
      pattern: "writeRegistry"
    - from: "packages/agent/src/routes/registryFixPath.ts"
      to: "rate limiter"
      via: "import { consume as rlConsume, tokenHashOf }"
      pattern: "rlConsume"
---

<objective>
Wave 2 daemon wire surface. Five new daemon files + 1 app.ts wiring edit:

1. **conformanceCache.ts** — 30s TTL singleton (mirrors `coverageCache.ts` verbatim). D-12-17.
2. **registryPathDrift.ts** — Detects entries where (a) `existsSync` fails, (b) realpath differs from stored path, (c) `.git/config` origin no longer matches the family root tree. Suggested-path inference scans family roots for a dir whose `.git/config` carries the same origin. D-12-18, D-12-21.
3. **conformanceScan.ts** — Orchestrator: calls `scanCoverageInternal()`, `detectPathDrift()`, `computeConformanceScores()`, `readDailySeriesForFleet()`. Composes the bulk-per-family wire payload. D-12-25.
4. **routes/conformance.ts** — `GET /api/observability/conformance` returning `ConformanceResponse`. Bearer-auth + CORS + outbound() schema-drift defence inherited. D-12-14.
5. **routes/registryFixPath.ts** — `POST /api/admin/registry/fix-path` with body `{ id, newPath }`. The new threat surface. Defences: rate-limiter + Zod validation + `canonicaliseRoot` (realpath) + `assertRegistrationAllowed` (blocklist) + family-root containment (Pitfall 7) + `writeRegistry` (atomic O_NOFOLLOW + O_EXCL) + cache invalidation. D-12-19, D-12-26.

Purpose: Lock the wire shape (under .strict() schema), exercise every defence in tests, and have a fully working `/api/observability/conformance` and `/api/admin/registry/fix-path` ready for Wave 3's SPA consumers.
Output: 5 new TS files + 5 test files + 1 app.ts edit. Full agent suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md
@.planning/phases/DASH-12-observability-conformance-surface/12-00-PLAN.md
@.planning/phases/DASH-12-observability-conformance-surface/12-01-PLAN.md
@CLAUDE.md
@packages/shared/src/schemas/conformance.ts
@packages/agent/src/lib/coverageCache.ts
@packages/agent/src/lib/coverageScan.ts
@packages/agent/src/lib/registry.ts
@packages/agent/src/lib/atomicWrite.ts
@packages/agent/src/lib/paths.ts
@packages/agent/src/lib/rateLimiter.ts
@packages/agent/src/routes/coverageHistory.ts
@packages/agent/src/routes/registry.ts
@packages/agent/src/server/app.ts
@packages/agent/src/server/middleware/errors.ts

<interfaces>
<!-- Existing daemon-side APIs Wave 2 composes. -->

From packages/agent/src/lib/coverageCache.ts (template — copy/rename/retype):
- export const TTL_MS = 30_000
- export function getCoverageCache(now?: number): CoverageResponse | null
- export function setCoverageCache(value: CoverageResponse, now?: number): void
- export function invalidateCoverageCache(): void
- export function _resetCoverageCacheForTests(): void

From packages/agent/src/lib/coverageScan.ts:
- export async function scanCoverageInternal(): Promise<{ response: CoverageResponse; warnings: string[] }>

From packages/agent/src/lib/registry.ts:
- export function readRegistry(registryFile?: string): RegistryFile
- export function writeRegistry(reg: RegistryFile, registryFile?: string): void  // atomic, 0o600
- export function canonicaliseRoot(pathArg: string): string  // realpath + abs
- export function assertRegistrationAllowed(canonicalRoot: string): void  // throws RegistrationPathBlocked
- export class RegistrationPathBlocked extends Error { reason: string }

From packages/agent/src/lib/paths.ts:
- export const COVERAGE_ROOTS = { gitnexus, agenticapps, factiv, neuroflash } : Record<...,() => string>

From packages/agent/src/lib/rateLimiter.ts:
- export function consume(tokHash: string): { allowed: boolean; retryAfter: number }
- export function tokenHashOf(token: string): string

From packages/agent/src/server/middleware/errors.ts:
- export function outbound<T>(c, parser, body): Response

From @agenticapps/dashboard-shared (Wave 0):
- ConformanceResponseSchema, RegistryFixPathRequestSchema (.strict), PathDriftEntrySchema, RegistryEntrySchema

From packages/agent/src/server/app.ts (current mount surface):
- app.route('/api', coverageRoute)              // line 135
- app.route('/api', coverageHistoryRoute)        // line 136
- app.route('/api', skillDriftRoute)             // line 137
- Phase 12: ADD HERE — conformanceRoute under /api, registryFixPathRoute under /api/admin
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conformanceCache.ts — 30s TTL singleton (RED to GREEN)</name>
  <files>
    packages/agent/src/lib/conformanceCache.ts (NEW)
    packages/agent/src/lib/conformanceCache.test.ts (NEW)
  </files>
  <read_first>
    packages/agent/src/lib/conformanceCache.ts (verify NEW)
    packages/agent/src/lib/coverageCache.ts (verbatim template — copy/rename/retype)
    packages/agent/src/lib/coverageCache.test.ts (verbatim test template)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Pattern 2" lines 236-266 (verbatim source)
  </read_first>
  <behavior>
    - TTL_MS = 30_000 (matches coverageCache + D-12-17).
    - getConformanceCache(now?) returns cached value if not expired, else null.
    - setConformanceCache(value, now?) stores with expiresAt = now + TTL_MS.
    - invalidateConformanceCache() clears the cache (called by fix-path route after mutation).
    - _resetConformanceCacheForTests() clears cache (called by test setup).
  </behavior>
  <action>
    1. Write packages/agent/src/lib/conformanceCache.test.ts FIRST (RED). Mirror coverageCache.test.ts structure verbatim — swap types CoverageResponse to ConformanceResponse.

    Test cases:
    - it('returns null on cold cache')
    - it('returns set value before TTL expires')
    - it('returns null after TTL expires')
    - it('invalidateConformanceCache clears the cache')
    - it('_resetConformanceCacheForTests clears the cache (test affordance)')
    - it('setConformanceCache uses Date.now() default for now arg')

    Fixture: minimal valid ConformanceResponse from @agenticapps/dashboard-shared with schemaVersion: 1, today: { asOf: '2026-05-19T12:00:00.000Z', fleet: 50, agenticapps: 50, factiv: 50, neuroflash: 50 }, delta14d zeros, series: [], drifted: [].

    2. Run pnpm --filter @agenticapps/dashboard-agent test conformanceCache --run — confirm RED.

    3. Write packages/agent/src/lib/conformanceCache.ts per RESEARCH lines 244-263 (verbatim — swap types). Module-scoped `let cache: CacheEntry | null = null`. Five named exports: TTL_MS, getConformanceCache, setConformanceCache, invalidateConformanceCache, _resetConformanceCacheForTests.

    4. Run pnpm --filter @agenticapps/dashboard-agent test conformanceCache --run — confirm GREEN.

    5. Commit: test(12-02): add failing tests for conformanceCache then feat(12-02): conformanceCache 30s TTL singleton (D-12-17).

    Anti-patterns: Same as coverageCache — NO Map keying (singleton only); NO Symbol or class.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test conformanceCache --run</automated>
  </verify>
  <done>
    - packages/agent/src/lib/conformanceCache.ts exists with 5 named exports.
    - `grep -c "TTL_MS = 30_000" packages/agent/src/lib/conformanceCache.ts` returns 1.
    - All 6 tests pass.
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: registryPathDrift.ts — detector + suggested-path inference (RED to GREEN)</name>
  <files>
    packages/agent/src/lib/registryPathDrift.ts (NEW)
    packages/agent/src/lib/registryPathDrift.test.ts (NEW)
  </files>
  <read_first>
    packages/agent/src/lib/registryPathDrift.ts (verify NEW)
    packages/agent/src/lib/registry.ts (readRegistry, canonicaliseRoot, RegistryEntry shape)
    packages/agent/src/lib/paths.ts:140-152 (COVERAGE_ROOTS factory)
    packages/agent/src/lib/coverageResolver.ts (canonicalising paths; existing realpath pattern)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §Pitfall 7 (symlink escape) + §"Daemon route (Phase 12 routes/registryFixPath.ts)"
    packages/shared/src/schemas/conformance.ts (PathDriftEntrySchema shape Wave 0 minted)
  </read_first>
  <behavior>
    - detectPathDrift(opts?): Promise<PathDriftEntry[]> iterates readRegistry().projects.
    - For each entry:
      1. existsSync(entry.root) === false → emit reason: 'missing'.
      2. realpath(entry.root) differs from canonicaliseRoot stored OR throws ENOENT → emit reason: 'symlink-target-changed'.
      3. .git/config remote.origin.url cannot find a matching family-root candidate → emit reason: 'git-remote-changed'.
    - inferSuggestedPath(originUrl: string): Promise<string | null> (best-effort):
      1. For each family in COVERAGE_ROOTS (agenticapps/factiv/neuroflash): realpath, readdirSync one level deep.
      2. For each candidate dir: fs.readFile(join(candidate, '.git/config'), 'utf8'); parse `[remote "origin"]` url field via regex.
      3. If URL matches input originUrl: return realpath(candidate).
      4. If nothing matches: return null.
    - All errors caught defensively — function NEVER throws. Drift detection returns empty array on registry-read failure.
    - Result shape matches PathDriftEntrySchema: { id, storedPath, suggestedPath, reason }.
    - NO subprocess — uses fs.readFile + regex parse only (RESEARCH §Environment availability fallback).
  </behavior>
  <action>
    1. Write packages/agent/src/lib/registryPathDrift.test.ts FIRST (RED). Use mkdtempSync for fixture directories.

    Test cases (RED first):
    - it('returns [] when registry is empty')
    - it('emits reason: missing when entry.root does not exist on disk')
    - it('emits reason: symlink-target-changed when realpath differs from stored root') — fixture creates a symlink, points it elsewhere, asserts detection
    - it('emits reason: git-remote-changed when .git/config origin no longer matches stored prefix') — fixture-based; mock the origin URL
    - it('returns suggestedPath: null when no family-root dir matches origin') — drifted entry, no inference possible
    - it('inferSuggestedPath finds matching dir by .git/config remote.origin.url') — fixture creates two dirs under a temp family-root with different .git/config URLs
    - it('inferSuggestedPath returns null when .git/config is unreadable')
    - it('detectPathDrift never throws on partial-failure (registry-read error returns empty array)')
    - it('detectPathDrift returns entries shaped as PathDriftEntrySchema (validates round-trip)') — explicit PathDriftEntrySchema.parse(result[0]) assertion
    - it('SECURITY: inferSuggestedPath reads ONLY .git/config inside family roots')
    - it('SECURITY: detector does NOT throw on symlink loops in entry.root') — fixture-based

    2. Run pnpm --filter @agenticapps/dashboard-agent test registryPathDrift --run — confirm RED.

    3. Write packages/agent/src/lib/registryPathDrift.ts. Imports: node:fs (existsSync, readdirSync), node:fs/promises (readFile, realpath), node:path (join). NO node:child_process. NO subprocess.

    Code structure:
    - Constant: `const REMOTE_ORIGIN_URL_RE = /\[remote "origin"\][\s\S]*?url\s*=\s*([^\s]+)/`
    - Helper `parseOriginUrl(configText: string): string | null` — regex match the .git/config text.
    - Helper `readGitOrigin(rootPath: string): Promise<string | null>` — fs.readFile(join(rootPath, '.git', 'config'), 'utf8') wrapped in try/catch returning null on any throw.
    - export `inferSuggestedPath(originUrl: string): Promise<string | null>`:
      - Iterate families ['agenticapps', 'factiv', 'neuroflash'] as const
      - For each: realpath the COVERAGE_ROOTS[family]() inside try/catch (skip on throw)
      - readdirSync(familyRoot) inside try/catch (skip on throw)
      - For each entry: join(familyRoot, entry); readGitOrigin(candidate); if matches input originUrl: return realpath(candidate) inside try/catch (return candidate as fallback on throw)
      - Return null when no match
    - export `detectPathDrift(opts?: { registryFile?: string }): Promise<PathDriftEntry[]>`:
      - try { readRegistry(opts.registryFile).projects } catch { return [] }
      - For each project: try { probeEntry(p.id, p.root); if drifted → infer suggestedPath } catch { continue }
      - Internal helper `probeEntry(id, storedPath)`:
        - if !existsSync(storedPath) → return { reason: 'missing', originUrl: null }
        - try { real = await realpath(storedPath); canonical = canonicaliseRoot(storedPath); if (real !== canonical) → return symlink-target-changed with readGitOrigin(real) } catch { /* fall through */ }
        - originUrl = await readGitOrigin(storedPath); if originUrl AND storedPath is not under any of (realpath of agenticapps/factiv/neuroflash) — return git-remote-changed with originUrl
        - else return null (not drifted)

    Output shape per entry: `{ id, storedPath, suggestedPath, reason }` matching PathDriftEntrySchema.

    4. Run pnpm --filter @agenticapps/dashboard-agent test registryPathDrift --run — confirm GREEN.

    5. Run pnpm --filter @agenticapps/dashboard-agent typecheck — confirm green.

    6. Commit: test(12-02): add failing tests for registryPathDrift then feat(12-02): registryPathDrift detector + inferSuggestedPath (D-12-18, D-12-21).

    Anti-patterns to avoid:
    - NO subprocess for git operations — use fs.readFile + regex (RESEARCH §Environment availability fallback).
    - NO recursive readdir of family roots — one level deep only (matches Phase 10 repo discovery depth).
    - NO throw on .git/config malformed lines — best-effort silent skip.
    - NO widening of trust boundary: only reads .git/config (single allow-listed file under family roots).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test registryPathDrift --run</automated>
  </verify>
  <done>
    - packages/agent/src/lib/registryPathDrift.ts exists with detectPathDrift, inferSuggestedPath exports.
    - packages/agent/src/lib/registryPathDrift.test.ts exists with ≥11 tests; all pass.
    - `grep -c "remote.*origin" packages/agent/src/lib/registryPathDrift.ts` returns at least 1 (regex parse).
    - `grep -c "child_process\|require.*child" packages/agent/src/lib/registryPathDrift.ts` returns 0 (no subprocess).
    - `grep -c "throw " packages/agent/src/lib/registryPathDrift.ts` returns 0 (defensive: never throws).
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: conformanceScan.ts orchestrator (RED to GREEN)</name>
  <files>
    packages/agent/src/lib/conformanceScan.ts (NEW)
    packages/agent/src/lib/conformanceScan.test.ts (NEW)
  </files>
  <read_first>
    packages/agent/src/lib/conformanceScan.ts (verify NEW)
    packages/agent/src/lib/coverageScan.ts (orchestrator template — scanCoverageInternal pattern)
    packages/agent/src/lib/conformanceScore.ts (Wave 1 — computeConformanceScores)
    packages/agent/src/lib/snapshots/snapshotFleetReader.ts (Wave 1 — readDailySeriesForFleet)
    packages/agent/src/lib/registryPathDrift.ts (Task 2 — detectPathDrift)
    packages/shared/src/schemas/conformance.ts (Wave 0 — ConformanceResponse shape)
  </read_first>
  <behavior>
    - scanConformance(opts?: { now?: Date }): Promise<ConformanceResponse> orchestrates:
      1. Promise.allSettled([scanCoverageInternal(), detectPathDrift()]) — parallelise independent reads.
      2. Build driftedRepoIds: Set<string> from drifted entries — for each drifted entry, compute `${family}/${repo}` from the stored path via family-root prefix matching. Skip entries that can't be family-bucketed.
      3. If coverage scan failed → return defensive payload (empty today/delta/series, drifted from whatever the detector returned).
      4. computeConformanceScores(coverage, driftedRepoIds) → today scores per family + fleet.
      5. readDailySeriesForFleet({ driftedRepoIds, now }) → 90-day series (wrapped in try/catch returning [] on throw).
      6. Compute delta14d: for each family + fleet, today's score minus the score from series[length-15] (14 days before the last entry). If series.length < 15, delta = 0.
      7. Return { schemaVersion: 1, today: { asOf, fleet, agenticapps, factiv, neuroflash }, delta14d, series, drifted }.
    - Failure isolation: if any sub-step throws, return a defensive payload with the available fields so the daemon never 500s on a partial-failure.
  </behavior>
  <action>
    1. Write packages/agent/src/lib/conformanceScan.test.ts FIRST. Use vi.mock for scanCoverageInternal + detectPathDrift + readDailySeriesForFleet.

    Test cases (RED first):
    - it('returns ConformanceResponse-shaped payload (parses via ConformanceResponseSchema)')
    - it('today scores come from computeConformanceScores(coverage, driftedSet)')
    - it('series is the 90-day per-day result from readDailySeriesForFleet')
    - it('drifted is the result of detectPathDrift')
    - it('delta14d = today - series[length-15] when 15+ entries exist') — fixture: series.length === 15, today's fleet 80, series[0] fleet 75 → delta14d.fleet = 5
    - it('delta14d = 0 when series.length < 15 (window not built yet)')
    - it('schemaVersion = 1 and asOf is an ISO datetime')
    - it('drifted repo IDs flow into computeConformanceScores')
    - it('partial failure: scanCoverageInternal throws → returns defensive empty payload (does not 500)')
    - it('integration: full payload parses ConformanceResponseSchema.strict()')

    2. Run pnpm --filter @agenticapps/dashboard-agent test conformanceScan --run — confirm RED.

    3. Write packages/agent/src/lib/conformanceScan.ts:
    - Imports: ConformanceResponse + CoverageFamily + PathDriftEntry from @agenticapps/dashboard-shared; scanCoverageInternal from coverageScan; computeConformanceScores from conformanceScore; detectPathDrift from registryPathDrift; readDailySeriesForFleet from snapshots/snapshotFleetReader; COVERAGE_ROOTS from paths.
    - Helper `pathToRepoId(storedPath: string): string | null`:
      - For family in ['agenticapps','factiv','neuroflash']: root = COVERAGE_ROOTS[family](); if storedPath === root or storedPath.startsWith(root + '/') → relativeSegment = storedPath.slice(root.length + 1).split('/')[0]; if relativeSegment → return `${family}/${relativeSegment}`
      - Return null
    - export `scanConformance({ now }: { now?: Date } = {}): Promise<ConformanceResponse>`:
      - now = opts.now ?? new Date()
      - const [coverageResult, drifted] = await Promise.allSettled([scanCoverageInternal(), detectPathDrift()])
      - driftedEntries = drifted.status === 'fulfilled' ? drifted.value : []
      - driftedRepoIds = new Set<string>(); for each driftedEntries: id = pathToRepoId(d.storedPath); if id → add to set
      - If coverageResult.status !== 'fulfilled' → return { schemaVersion: 1, today: { asOf, fleet:0, agenticapps:0, factiv:0, neuroflash:0 }, delta14d: zeros, series: [], drifted: driftedEntries }
      - coverage = coverageResult.value.response
      - scores = computeConformanceScores(coverage, driftedRepoIds)
      - today = { asOf: now.toISOString(), fleet: scores.fleet.score, agenticapps: scores.agenticapps.score, factiv: scores.factiv.score, neuroflash: scores.neuroflash.score }
      - try { series = await readDailySeriesForFleet({ driftedRepoIds, now }) } catch { series = [] }
      - deltaIndex = series.length - 15; baseline = deltaIndex >= 0 ? series[deltaIndex] : null
      - delta14d = baseline ? { fleet: today.fleet - baseline.fleet, ... per family } : zeros
      - return { schemaVersion: 1 as const, today, delta14d, series, drifted: driftedEntries }

    4. Run pnpm --filter @agenticapps/dashboard-agent test conformanceScan --run — confirm GREEN.

    5. Commit: test(12-02): add failing tests for conformanceScan orchestrator then feat(12-02): conformanceScan composer + delta14d (D-12-16, D-12-25).

    Anti-patterns to avoid:
    - DO NOT compute scores per-day differently from today's scores — both use computeConformanceScores or its NDJSON-record-equivalent (Pitfall 3 consistency).
    - DO NOT throw on partial failures — defensive payload with available data.
    - DO NOT recurse into family roots — pathToRepoId only looks at the first segment after the family root.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test conformanceScan --run</automated>
  </verify>
  <done>
    - packages/agent/src/lib/conformanceScan.ts exists with scanConformance export.
    - packages/agent/src/lib/conformanceScan.test.ts exists with ≥10 tests; all pass.
    - `grep -c "ConformanceResponseSchema.parse" packages/agent/src/lib/conformanceScan.test.ts` returns at least 1.
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: routes/conformance.ts — GET /api/observability/conformance (RED to GREEN)</name>
  <files>
    packages/agent/src/routes/conformance.ts (NEW)
    packages/agent/src/routes/conformance.test.ts (NEW)
    packages/agent/src/server/app.ts (EDIT — mount the route)
  </files>
  <read_first>
    packages/agent/src/routes/conformance.ts (verify NEW)
    packages/agent/src/routes/coverageHistory.ts (sibling-endpoint template)
    packages/agent/src/server/app.ts:135-138 (current mount surface; add line for conformance)
    packages/agent/src/server/middleware/errors.ts (outbound() wrapper)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Daemon route (Phase 12 routes/conformance.ts)" lines 686-708 (verbatim source)
  </read_first>
  <behavior>
    - GET /api/observability/conformance with NO query params.
    - Bearer-auth + CORS inherited from app.ts middleware (no per-route auth check).
    - 30s cache short-circuit: getConformanceCache() → if hit, return cached; else call scanConformance() + setConformanceCache().
    - Response goes through outbound(c, ConformanceResponseSchema.parse.bind(...), data) — schema drift surfaces as 500.
    - Route mounted via app.route('/api', conformanceRoute) so the final URL is /api/observability/conformance.
  </behavior>
  <action>
    1. Write packages/agent/src/routes/conformance.test.ts FIRST. Use the in-process Hono test pattern from coverageHistory.test.ts (createApp → app.request).

    Test cases (RED first):
    - it('returns 401 without bearer token')
    - it('returns 200 + ConformanceResponse-shaped JSON with bearer token')
    - it('response parses ConformanceResponseSchema.strict() successfully')
    - it('30s cache: second call within TTL returns cached value (does not re-scan)') — spy on scanConformance
    - it('cache miss after TTL: third call after 31s re-scans')
    - it('CORS: missing Origin header still allowed (no CORS preflight)') — matches Phase 10/11 route patterns
    - it('OPTIONS preflight returns CORS headers for PROD_ORIGIN + DEV_ORIGIN')
    - it('schema drift: monkey-patched scanConformance returning malformed data returns 500 schema_drift')

    2. Run pnpm --filter @agenticapps/dashboard-agent test conformance.route --run — confirm RED.

    3. Write packages/agent/src/routes/conformance.ts per RESEARCH lines 686-708:
    - Imports: Hono; ConformanceResponseSchema from @agenticapps/dashboard-shared; scanConformance from lib/conformanceScan; getConformanceCache + setConformanceCache from lib/conformanceCache; outbound from server/middleware/errors; Env type from server/app.
    - export `conformanceRoute = new Hono<Env>()`
    - conformanceRoute.get('/observability/conformance', async (c) => { cached = getConformanceCache(); data = cached ?? await scanConformance(); if (!cached) setConformanceCache(data); return outbound(c, ConformanceResponseSchema.parse.bind(ConformanceResponseSchema), data) })

    4. Edit packages/agent/src/server/app.ts — INSERT after `app.route('/api', skillDriftRoute)` line 137:
    - `app.route('/api', conformanceRoute) // Phase 12 D-12-14: GET /api/observability/conformance`
    - Add import at the top: `import { conformanceRoute } from '../routes/conformance.js'`

    5. Run pnpm --filter @agenticapps/dashboard-agent test conformance.route --run — confirm GREEN.

    6. Commit: test(12-02): add failing tests for conformance route then feat(12-02): GET /api/observability/conformance route + app mount (D-12-14, D-12-16, D-12-17).

    Anti-patterns to avoid:
    - DO NOT add per-route bearer-auth check (inherited from middleware).
    - DO NOT add per-route CORS (inherited).
    - DO NOT skip outbound() wrap (Pitfall: schema drift would leak as 200).
    - DO NOT add query params (D-12-16 is bulk-per-family, single fetch shape).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test conformance.route --run</automated>
  </verify>
  <done>
    - packages/agent/src/routes/conformance.ts exists with conformanceRoute export.
    - packages/agent/src/server/app.ts mounts conformanceRoute under /api.
    - `grep -c "conformanceRoute" packages/agent/src/server/app.ts` returns at least 2 (import + mount).
    - All 8 route tests pass.
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: routes/registryFixPath.ts — POST /api/admin/registry/fix-path (RED to GREEN)</name>
  <files>
    packages/agent/src/routes/registryFixPath.ts (NEW)
    packages/agent/src/routes/registryFixPath.test.ts (NEW)
    packages/agent/src/server/app.ts (EDIT — mount the admin route)
  </files>
  <read_first>
    packages/agent/src/routes/registryFixPath.ts (verify NEW)
    packages/agent/src/routes/registry.ts (template for rate-limiting + zValidator + atomic write pattern)
    packages/agent/src/lib/registry.ts:144-164 (assertRegistrationAllowed blocklist)
    packages/agent/src/lib/registry.ts:63-78 (canonicaliseRoot realpath)
    packages/agent/src/lib/atomicWrite.ts (atomicWriteFile O_NOFOLLOW + O_EXCL)
    packages/agent/src/lib/paths.ts:140-152 (COVERAGE_ROOTS factory)
    packages/agent/src/lib/rateLimiter.ts (consume + tokenHashOf)
    packages/agent/src/server/app.ts:104-115 (bearer-auth wiring + Env shape)
    packages/agent/src/server/app.ts:120 (existing /api/admin mount surface)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Daemon route (Phase 12 routes/registryFixPath.ts)" lines 710-805 (verbatim source — THIS IS LOAD-BEARING)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Pitfall 7" (symlink escape) + §"Pitfall 6" (concurrent write race)
  </read_first>
  <behavior>
    - POST /api/admin/registry/fix-path with body { id: string, newPath: string } validated via zValidator + RegistryFixPathRequestSchema.strict().
    - Bearer-auth inherited.
    - Rate-limited per token-hash (rlConsume(tokHash) — 10 req/10s).
    - Validation pipeline (ALL must pass; first failure short-circuits):
      1. Zod body parse → 422 'invalid_request' on failure.
      2. Rate limit → 429 'rate_limited' with Retry-After header on burst.
      3. canonicaliseRoot(newPath) (realpath + abs) — try/catch returns 422 'newPath_unresolvable' on ENOENT.
      4. assertRegistrationAllowed(canonical) (blocklist) — returns 422 'newPath_blocked' on RegistrationPathBlocked.
      5. Family-root containment: realpath of each COVERAGE_ROOT, check canonical === r || canonical.startsWith(r + sep) for at least one. Else 422 'newPath_outside_family_roots'.
      6. readRegistry(registryFile) → find entry by id. 404 'project_not_found' on miss.
      7. Mutate entry.root = canonical. writeRegistry(reg, registryFile) (atomic).
      8. invalidateConformanceCache() + invalidateCoverageCache().
      9. Return outbound(c, RegistryEntrySchema.parse, updatedEntry).
    - Mounted at app.route('/api/admin', registryFixPathRoute). Route path is /registry/fix-path so the URL is /api/admin/registry/fix-path.
  </behavior>
  <action>
    1. Write packages/agent/src/routes/registryFixPath.test.ts FIRST. Use the in-process Hono pattern + isolated HOME fixture (mkdtempSync) to host registry.json + family-root fixtures.

    Test cases (RED first — EVERY threat in the threat model gets a test):
    - it('returns 401 without bearer token')
    - it('returns 422 on missing body fields (id OR newPath empty/missing)')
    - it('returns 422 on extra body fields (.strict rejection)')
    - it('returns 422 newPath_blocked when newPath is /etc (system root blocklist)')
    - it('returns 422 newPath_blocked when newPath is ~/.ssh (secret-dir blocklist)')
    - it('returns 422 newPath_outside_family_roots when newPath is ~/Documents/foo')
    - it('returns 422 newPath_outside_family_roots when newPath uses ../.. to escape') — path traversal
    - it('returns 422 newPath_outside_family_roots when newPath is a symlink pointing outside family roots') — Pitfall 7 symlink escape, MUST resolve realpath BEFORE containment check
    - it('returns 404 project_not_found when id is not in registry')
    - it('returns 200 + updated RegistryEntry on happy path (newPath under ~/Sourcecode/agenticapps)')
    - it('writes registry.json atomically (file content matches updated entry)')
    - it('registry.json mode is 0o600 after write')
    - it('rate-limit: 11th request within 10s window returns 429 with Retry-After header')
    - it('invalidates conformanceCache + coverageCache on successful mutation')
    - it('error responses do NOT leak filesystem details (just structured error code)') — info-disclosure defence
    - it('SECURITY: idempotent — calling fix-path twice with same payload yields same final state')
    - it('outbound parse: monkey-patched writeRegistry returning malformed entry returns 500 schema_drift')

    Fixture helper: withFamilyRootFixture creates homeDir via mkdtempSync, sets up ~/Sourcecode/agenticapps/real-project/.git/config with a known origin URL, writes registry with stale path. Cleans up via rmSync after callback. Override COVERAGE_ROOTS via vi.mock or pass via env var (verify pattern in paths.test.ts).

    2. Run pnpm --filter @agenticapps/dashboard-agent test registryFixPath --run — confirm RED.

    3. Write packages/agent/src/routes/registryFixPath.ts per RESEARCH lines 710-805 (verbatim with light edits):
    - Imports: realpath from node:fs/promises; sep from node:path; Hono + Context type; zValidator from @hono/zod-validator; RegistryEntrySchema + RegistryFixPathRequestSchema from @agenticapps/dashboard-shared; readRegistry, writeRegistry, canonicaliseRoot, assertRegistrationAllowed, RegistrationPathBlocked from lib/registry; consume as rlConsume + tokenHashOf from lib/rateLimiter; COVERAGE_ROOTS from lib/paths; invalidateConformanceCache from lib/conformanceCache; invalidateCoverageCache from lib/coverageCache; outbound from server/middleware/errors; Env from server/app.
    - Helper tokenFromAuthHeader(c) returns the trimmed bearer token after "Bearer " or null.
    - export `registryFixPathRoute = new Hono<Env>()`
    - registryFixPathRoute.post('/registry/fix-path', zValidator('json', RegistryFixPathRequestSchema, (result, c) => { if (!result.success) return c.json({ ok: false, error: 'invalid_request' }, 422) }), async (c) => { ... }).
    - Handler body (in order):
      a. requestId = c.get('requestId') ?? 'unknown'; token = tokenFromAuthHeader(c); tokHash = token ? tokenHashOf(token) : 'no-token'
      b. rl = rlConsume(tokHash); if (!rl.allowed) return c.json({ ok: false, error: 'rate_limited', requestId }, 429, { 'Retry-After': String(rl.retryAfter) })
      c. body = c.req.valid('json'); registryFile = c.get('registryFile')
      d. try { canonical = canonicaliseRoot(body.newPath) } catch { return 422 'newPath_unresolvable' }
      e. try { assertRegistrationAllowed(canonical) } catch (err) { if RegistrationPathBlocked return 422 'newPath_blocked'; else throw }
      f. familyRoots = await Promise.all(['agenticapps','factiv','neuroflash'] mapped to realpath(COVERAGE_ROOTS[f]()) inside try/catch returning null on throw)
      g. insideFamily = familyRoots.some(r => r !== null && (canonical === r || canonical.startsWith(r + sep))); if (!insideFamily) return 422 'newPath_outside_family_roots'
      h. reg = readRegistry(registryFile); entry = reg.projects.find(p => p.id === body.id); if (!entry) return 404 'project_not_found'
      i. entry.root = canonical; writeRegistry(reg, registryFile)
      j. invalidateConformanceCache(); invalidateCoverageCache()
      k. return outbound(c, RegistryEntrySchema.parse.bind(RegistryEntrySchema), entry)

    4. Edit packages/agent/src/server/app.ts — INSERT after the line that mounts conformanceRoute (from Task 4):
    - `app.route('/api/admin', registryFixPathRoute)  // Phase 12 D-12-19, D-12-26`
    - Add import: `import { registryFixPathRoute } from '../routes/registryFixPath.js'`

    5. Run pnpm --filter @agenticapps/dashboard-agent test registryFixPath --run — confirm GREEN.

    6. Run full agent suite: pnpm --filter @agenticapps/dashboard-agent test --run. ALL existing tests must remain green (no regression on Phase 1 registry tests, Phase 10 coverage tests, Phase 11 trends/skill-drift tests).

    7. Commit: test(12-02): add failing tests for registryFixPath route + threat coverage then feat(12-02): POST /api/admin/registry/fix-path (D-12-19, D-12-26 + 9 threat mitigations).

    Anti-patterns to avoid:
    - DO NOT skip canonicaliseRoot before containment check (Pitfall 7 — TOCTOU window).
    - DO NOT add a custom symlink-traversal helper (RESEARCH anti-pattern — use realpath everywhere).
    - DO NOT hand-roll a lockfile or in-process Mutex (A4 ratified — rate-limiter + atomicWriteFile suffice).
    - DO NOT leak filesystem paths in error responses (T-12-INFO-DISCLOSURE — structured codes only).
    - DO NOT return error: <FS path> — only structured codes (newPath_blocked, project_not_found, etc.).
    - DO NOT log canonical or storedPath at info-level — keep diagnostic context bounded.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test registryFixPath --run</automated>
  </verify>
  <done>
    - packages/agent/src/routes/registryFixPath.ts exists with registryFixPathRoute export.
    - packages/agent/src/server/app.ts mounts registryFixPathRoute under /api/admin.
    - `grep -c "registryFixPathRoute" packages/agent/src/server/app.ts` returns at least 2 (import + mount).
    - All 17 threat-model tests pass.
    - `grep -c "newPath_outside_family_roots\|newPath_blocked\|newPath_unresolvable\|project_not_found\|rate_limited\|invalid_request" packages/agent/src/routes/registryFixPath.ts` returns at least 6 (every threat surfaces as a distinct error code).
    - `grep -c "writeRegistry" packages/agent/src/routes/registryFixPath.ts` returns at least 1 (atomic write path).
    - Full agent suite pnpm --filter @agenticapps/dashboard-agent test --run exits 0 (no regression).
    - 2 atomic commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA / curl → `POST /api/admin/registry/fix-path` | Untrusted body crosses here — Zod `.strict()` rejects shape drift; bearer auth required; rate-limited per token. |
| `newPath` user input → registry.json | User-supplied path becomes a stored canonical path. Defences: canonicaliseRoot (realpath), blocklist, family-root containment. |
| `.git/config` of registered project → daemon | Read-only; ONLY `[remote "origin"] url` field parsed via fs.readFile + regex (NO subprocess). |
| `~/.agenticapps/dashboard/registry.json` write | atomicWriteFile O_NOFOLLOW + O_EXCL + fsync + rename. Concurrent writes serialize. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verified by Task |
|-----------|----------|-----------|----------|-------------|------------|------------------|
| T-12-PATH-TRAVERSAL | Tampering | `POST /api/admin/registry/fix-path` body `newPath` | high | mitigate | canonicaliseRoot follows realpath + makes absolute; family-root containment via canonical.startsWith(realRoot + sep). Test: "returns 422 newPath_outside_family_roots when newPath uses ../.." | Task 5 |
| T-12-SYMLINK-ESCAPE | Tampering | `POST /api/admin/registry/fix-path` (planted symlink) | high | mitigate | realpath family roots + canonical comparison BEFORE registry mutation. Test: "newPath is a symlink pointing outside family roots → 422". Defence-in-depth: atomicWriteFile O_NOFOLLOW refuses planted tmp symlinks. | Task 5 |
| T-12-CONCURRENT-WRITE | Tampering / DoS | registry.json mutation race | medium | mitigate | atomicWriteFile POSIX rename atomicity + rate-limiter (10/10s/token). Concurrent fix-path from two tabs serializes via rate-limit; last-write-wins semantically. A4 ratified — no Mutex/lockfile needed. | Task 5 |
| T-12-CSRF | Spoofing | `POST /api/admin/registry/fix-path` | medium | mitigate | CORS lock to PROD_ORIGIN + DEV_ORIGIN inherited; bearer auth (NOT cookie auth) → no CSRF surface. Test: "OPTIONS preflight returns CORS headers". | Task 5 |
| T-12-AUTH | Spoofing | both routes | high | mitigate | Bearer middleware (app.ts:104-115) constant-time compare; refuses empty tokens. Test: "returns 401 without bearer token" on both routes. | Tasks 4 + 5 |
| T-12-INFO-DISCLOSURE | Info disclosure | error response bodies | medium | mitigate | Structured error codes only (newPath_blocked, project_not_found, etc.) — no FS path leakage. requestId only. Test: "error responses do NOT leak filesystem details". | Task 5 |
| T-12-REGISTRY-CORRUPTION | Tampering | registry.json mode regression | high | mitigate | writeRegistry refuses to start daemon if existing mode is looser than 0o600 (Phase 1 INV-02). atomicWriteFile recreates with 0o600. Test: "registry.json mode is 0o600 after write". | Task 5 |
| T-12-DENIAL-OF-SERVICE | Availability | fix-path repeated calls | low | mitigate | rate-limiter sliding 10s/cap 10 per token-hash. Test: "11th request within 10s returns 429 with Retry-After header". | Task 5 |
| T-12-SUPPLY-CHAIN | Tampering | new runtime deps | high | mitigate | Plan 12-02 adds ZERO runtime deps. All defences use node:fs/promises + node:path + existing workspace deps (hono, zod, @hono/zod-validator). NO subprocess in registryPathDrift (fs.readFile + regex). | Plan-wide |
| T-12-IDEMPOTENT-FAIL | Tampering | fix-path replay | low | accept | Idempotent by design — calling fix-path twice with same body produces same final registry state. Test: "calling fix-path twice yields same final state". | Task 5 |
| T-12-CACHE-STALE | Tampering | conformance cache after fix | medium | mitigate | invalidateConformanceCache() + invalidateCoverageCache() called inside the route after writeRegistry success. Test: "invalidates conformanceCache + coverageCache on successful mutation". | Task 5 |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-agent test --run` exits 0 (full agent suite — no regression).
- `pnpm --filter @agenticapps/dashboard-agent typecheck` exits 0.
- `curl -s -H "Authorization: Bearer $TOKEN" http://127.0.0.1:5193/api/observability/conformance | jq -e '.schemaVersion'` returns `1`.
- `curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"id":"foo","newPath":"/etc"}' http://127.0.0.1:5193/api/admin/registry/fix-path` returns 422 with `newPath_blocked`.
- 10 atomic commits exist (2 per task × 5 tasks).
</verification>

<success_criteria>
Wave 2 complete when:
1. `GET /api/observability/conformance` returns a valid ConformanceResponse (Schema.strict() round-trip).
2. `POST /api/admin/registry/fix-path` is reachable, rejects all 9+ threat vectors with distinct error codes, mutates registry.json atomically on happy path, invalidates caches.
3. No new runtime deps in packages/agent (verified by `git diff -- packages/agent/package.json`).
4. The cache TTL math works: cold call → re-scan; warm call within 30s → cached; cold call after 31s → re-scan.
5. Wave 3 SPA can `fetch('/api/observability/conformance', { headers: { Authorization: 'Bearer ...' }})` and consume.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-12-observability-conformance-surface/12-02-SUMMARY.md` with:
- Files created/modified (list)
- Test counts per task
- Confirmation: all 9 threat IDs have at least one passing test
- Confirmation: no new runtime deps added (no T-12-SUPPLY-CHAIN)
- Note for /cso (Wave 6): registryFixPath threat model is captured inline; audit should verify implementation matches.
</output>
