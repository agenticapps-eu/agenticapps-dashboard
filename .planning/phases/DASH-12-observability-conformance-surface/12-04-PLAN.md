---
phase: 12
plan: 04
type: tdd
wave: 4
depends_on:
  - 12-00-PLAN.md
  - 12-02-PLAN.md
  - 12-03-PLAN.md
files_modified:
  - packages/spa/src/components/panels/conformance/ConformancePage.tsx
  - packages/spa/src/components/panels/conformance/ConformancePage.test.tsx
  - packages/spa/src/routes/observability.conformance.lazy.tsx
  - packages/spa/src/router.tsx
  - packages/spa/src/components/ui/Sidebar.tsx
autonomous: true
requirements:
  - REQ-12-PAGE-01
  - REQ-12-PAGE-02
  - REQ-12-NAV-01
requirements_addressed:
  - REQ-12-PAGE-01
  - REQ-12-PAGE-02
  - REQ-12-NAV-01
objectives: >
  Page composition + router + sidebar. ConformancePage.tsx composes the Wave 3 primitives
  (sticky PageHeader + PathDriftPanel + 3-up FamilyCard row + FleetTrendChart) under
  loading/error/empty/schemaDrift branches. observability.conformance.lazy.tsx wires the
  lazy route. router.tsx registers conformanceRoute under _appshell. Sidebar.tsx adds
  Conformance as the 3rd entry in the Observability section (D-12-01 — Coverage / Skill
  drift / Conformance order per Open Question 3 resolution).
notes: >
  Open Question 3 resolved: sidebar order is Coverage → Skill drift → Conformance.
  Rationale: existing Phase 11 order (Coverage → Skill drift) is preserved; Conformance
  added as the 3rd entry. Why not Coverage → Conformance → Skill drift (headline-then-
  detail per RESEARCH OQ3)? Because changing the EXISTING Phase 11 D-11-08 order would
  cause a UX surprise for users already trained on it. Additive growth (3rd entry) is
  the lower-friction choice — graduating the section without disturbing existing IA.
  This deviation from RESEARCH OQ3 is documented in the SUMMARY.

must_haves:
  truths:
    - "ConformancePage composes PageHeader (sticky) + PathDriftPanel + 3 FamilyCard + FleetTrendChart"
    - "ConformancePage handles loading / error / schemaDrift / empty states explicitly"
    - "/observability/conformance route renders under _appshell layout"
    - "Sidebar Observability section has 3 entries: Coverage / Skill drift / Conformance (order preserves existing)"
    - "Conformance icon is visually distinct from Coverage + Skill drift (lucide-react choice: TrendingUp or Gauge)"
  artifacts:
    - path: "packages/spa/src/components/panels/conformance/ConformancePage.tsx"
      provides: "ConformancePage composition"
      contains: "export function ConformancePage"
    - path: "packages/spa/src/routes/observability.conformance.lazy.tsx"
      provides: "Lazy route wrapper"
      contains: "export const Route"
    - path: "packages/spa/src/router.tsx"
      provides: "conformanceRoute registration under _appshell"
      contains: "observability/conformance"
    - path: "packages/spa/src/components/ui/Sidebar.tsx"
      provides: "Observability section with 3rd entry Conformance"
      contains: "to=\"/observability/conformance\""
  key_links:
    - from: "packages/spa/src/router.tsx"
      to: "packages/spa/src/routes/observability.conformance.lazy.tsx"
      via: "lazy import"
      pattern: "observability.conformance.lazy"
    - from: "packages/spa/src/components/ui/Sidebar.tsx"
      to: "/observability/conformance"
      via: "SidebarItem to= prop"
      pattern: "/observability/conformance"
---

<objective>
Wave 4 — wire it all together into a routable page that ships.

1. **ConformancePage.tsx** — Composes Wave 3 primitives. Branches on useConformance result:
   - isLoading → render loading skeleton
   - isError → render generic error state (no FS path leakage)
   - data has schemaDriftAt → render SchemaDriftState (existing primitive — CoveragePage:252-260)
   - data.series empty AND no drifted → render empty state
   - happy path → sticky PageHeader + PathDriftPanel (if drifted) + 3 FamilyCard row + FleetTrendChart
   Reuses sticky PageHeader (Phase 11.1) so the chart hover panel works under scroll.

2. **observability.conformance.lazy.tsx** — Lazy route wrapper. Mirrors `observability.skill-drift.lazy.tsx` pattern.

3. **router.tsx** — Register `conformanceRoute` under `_appshell` layout. Path `/observability/conformance`. NO validateSearch for v1.2.0 (no `?date=` deep-link per RESEARCH §REQ-12-PAGE-01 deferral).

4. **Sidebar.tsx** — Add 3rd `SidebarItem` to the Observability section. Order: Coverage / Skill drift / Conformance (preserves existing Phase 11 order; documented deviation from RESEARCH OQ3 in the SUMMARY).

Purpose: Ship the routable surface. After this plan, users can click "Conformance" in the sidebar and see a working page that fetches `/api/observability/conformance` + renders the trend chart + family cards + drift panel.
Output: 1 new page component + 1 new lazy route + 2 file edits. SPA suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md
@.planning/phases/DASH-12-observability-conformance-surface/12-03-PLAN.md
@packages/spa/src/components/panels/coverage/CoveragePage.tsx
@packages/spa/src/components/panels/conformance/FleetTrendChart.tsx
@packages/spa/src/components/panels/conformance/FamilyCard.tsx
@packages/spa/src/components/panels/conformance/PathDriftPanel.tsx
@packages/spa/src/lib/conformanceQueries.ts
@packages/spa/src/components/ui/PageHeader.tsx
@packages/spa/src/components/ui/Sidebar.tsx
@packages/spa/src/router.tsx
@packages/spa/src/routes/coverage.lazy.tsx
@packages/spa/src/routes/observability.skill-drift.lazy.tsx

<interfaces>
<!-- Existing patterns Plan 12-04 mirrors. -->

From packages/spa/src/components/ui/PageHeader.tsx (Phase 11.1):
- <PageHeader title="..." helper="..." sticky={true}>{children?}</PageHeader>

From packages/spa/src/components/panels/coverage/CoveragePage.tsx (composition template):
- isLoading branch → loading skeleton
- isError branch → ErrorState
- 'schemaDriftAt' in data branch → <SchemaDriftState path={data.schemaDriftAt} />
- empty branch → <CoverageEmptyState />
- happy path → <PageHeader sticky={true}>{toolbar}</PageHeader> + <CoverageFamilySection /> rows

From packages/spa/src/components/ui/Sidebar.tsx (Phase 11 — 2 entries currently):
- <SidebarSection label="Observability">
  - <SidebarItem to="/coverage" icon={<Activity />} label="Coverage" />
  - <SidebarItem to="/observability/skill-drift" icon={<Layers />} label="Skill drift" />

From packages/spa/src/router.tsx (mount surface):
- Current line ~125: observabilitySkillDriftRoute under _appshell layout
- Phase 12: add conformanceRoute under _appshell layout

From lucide-react (existing dep):
- Available icons: TrendingUp, Gauge, BarChart3, Activity, Layers, Cog, HelpCircle, FolderKanban, etc.

From @agenticapps/dashboard-shared (Wave 0):
- ConformanceResponse, PathDriftEntry, tierOf
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConformancePage.tsx — composition (RED to GREEN)</name>
  <files>
    packages/spa/src/components/panels/conformance/ConformancePage.tsx (NEW)
    packages/spa/src/components/panels/conformance/ConformancePage.test.tsx (NEW)
  </files>
  <read_first>
    packages/spa/src/components/panels/conformance/ConformancePage.tsx (verify NEW)
    packages/spa/src/components/panels/coverage/CoveragePage.tsx (composition template — loading/error/drift/empty/happy branches; lines 1-362)
    packages/spa/src/components/panels/conformance/FleetTrendChart.tsx (Plan 12-03 — child component)
    packages/spa/src/components/panels/conformance/FamilyCard.tsx (Plan 12-03 — child component)
    packages/spa/src/components/panels/conformance/PathDriftPanel.tsx (Plan 12-03 — child component)
    packages/spa/src/lib/conformanceQueries.ts (Plan 12-03 — useConformance hook)
    packages/spa/src/components/ui/PageHeader.tsx (Phase 11.1 — sticky variant)
  </read_first>
  <behavior>
    - Component name: ConformancePage. Default export + named export.
    - Calls useConformance() → switch on (isLoading, isError, data shape).
    - Loading branch: render <main><PageHeader title="Fleet conformance" helper="..." sticky /><LoadingSkeleton /></main> — LoadingSkeleton is a placeholder grid (3 family card skeletons + 240px chart skeleton).
    - Error branch: render <main><PageHeader sticky /><ErrorState message="Could not load conformance data." retry={() => refetch()} /></main>. Generic message — NO FS path leakage.
    - Schema-drift branch (`'schemaDriftAt' in data`): render <main><PageHeader sticky /><SchemaDriftState path={data.schemaDriftAt} /></main>. SchemaDriftState is the existing primitive — reuse from CoveragePage.tsx:252-260.
    - Happy path: render <main className="flex flex-col gap-6">
      <PageHeader title="Fleet conformance" helper="How conformant every registered project is to the AgenticApps standard." sticky />
      {data.drifted.length > 0 && <PathDriftPanel drifted={data.drifted} />}
      <section aria-label="Family conformance scores" className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <FamilyCard family="agenticapps" score={data.today.agenticapps} delta14d={data.delta14d.agenticapps} />
        <FamilyCard family="factiv" score={data.today.factiv} delta14d={data.delta14d.factiv} />
        <FamilyCard family="neuroflash" score={data.today.neuroflash} delta14d={data.delta14d.neuroflash} />
      </section>
      <section aria-labelledby="trend-heading" className="rounded-lg border border-border-subtle bg-card-bg p-4">
        <h2 id="trend-heading" className="text-base font-semibold text-text-primary mb-2">90-day fleet trend</h2>
        <p className="text-sm text-text-tertiary mb-4">Daily conformance score per family + fleet aggregate.</p>
        <FleetTrendChart series={data.series} ariaLabel="Daily fleet conformance scores over the last 90 days" />
      </section>
    </main>
    - Pure composition — no fetching beyond useConformance; no own state besides what's in children.
  </behavior>
  <action>
    1. Write packages/spa/src/components/panels/conformance/ConformancePage.test.tsx FIRST (RED). Use TestQueryClientProvider with msw or fetch mock.

    Test cases:
    - it('renders loading skeleton while isLoading')
    - it('renders ErrorState on isError with generic message (no FS path leakage)')
    - it('renders SchemaDriftState when data has schemaDriftAt')
    - it('renders PathDriftPanel only when data.drifted.length > 0')
    - it('renders 3 FamilyCards (agenticapps, factiv, neuroflash) on happy path')
    - it('passes today.{family} as score prop to each FamilyCard')
    - it('passes delta14d.{family} as delta14d prop to each FamilyCard')
    - it('renders FleetTrendChart with data.series')
    - it('renders sticky PageHeader with "Fleet conformance" title')
    - it('renders the trend section with aria-labelledby for SR navigation')
    - it('SECURITY: ErrorState message does NOT include error.message or stack (generic only)')
    - it('SECURITY: schema-drift error path renders SchemaDriftState, never raw data')

    2. Run pnpm --filter @agenticapps/dashboard-spa test ConformancePage --run — confirm RED.

    3. Write packages/spa/src/components/panels/conformance/ConformancePage.tsx:
    - Imports: React; useConformance from lib/conformanceQueries; PageHeader from components/ui/PageHeader; SchemaDriftState (reuse existing import from CoveragePage); ErrorState (reuse existing or inline a minimal one); FleetTrendChart from ./FleetTrendChart; FamilyCard from ./FamilyCard; PathDriftPanel from ./PathDriftPanel.
    - LoadingSkeleton inline: 3 div skeletons of size matching FamilyCard + 1 div skeleton of height 240 matching the chart.
    - Function body: switch on query state per the behavior spec above. Use early-return pattern for non-happy branches.

    4. Run pnpm --filter @agenticapps/dashboard-spa test ConformancePage --run — confirm GREEN.

    5. Commit: test(12-04): add failing tests for ConformancePage composition then feat(12-04): ConformancePage composes FamilyCard/FleetTrendChart/PathDriftPanel (REQ-12-PAGE-02).

    Anti-patterns to avoid:
    - DO NOT leak error.message into ErrorState UI (T-12-INFO-DISCLOSURE).
    - DO NOT render schema-drift data as if it were valid (always SchemaDriftState branch).
    - DO NOT render PathDriftPanel when drifted is empty (panel auto-hides per Plan 12-03, but the conditional render here is defence-in-depth).
    - DO NOT compose own data-fetching logic — the page is presentational composition.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test ConformancePage --run</automated>
  </verify>
  <done>
    - packages/spa/src/components/panels/conformance/ConformancePage.tsx exists with ConformancePage export.
    - All 12 tests pass.
    - `grep -c "FleetTrendChart\|FamilyCard\|PathDriftPanel" packages/spa/src/components/panels/conformance/ConformancePage.tsx` returns at least 3.
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy route + router wiring (RED to GREEN)</name>
  <files>
    packages/spa/src/routes/observability.conformance.lazy.tsx (NEW)
    packages/spa/src/router.tsx (EDIT — register conformanceRoute under _appshell)
  </files>
  <read_first>
    packages/spa/src/routes/observability.conformance.lazy.tsx (verify NEW)
    packages/spa/src/routes/observability.skill-drift.lazy.tsx (Phase 11 — verbatim template)
    packages/spa/src/router.tsx (current registration patterns; lines 111-128 for coverage + skill-drift)
    packages/spa/src/components/panels/conformance/ConformancePage.tsx (Task 1 — the route renders this)
  </read_first>
  <behavior>
    - observability.conformance.lazy.tsx: exports `Route` via `createLazyRoute(...)` pattern matching Phase 11's skill-drift lazy route. Component is ConformancePage from panels/conformance.
    - router.tsx: ADD a new const `conformanceRoute = createRoute({ getParentRoute: () => appShellLayoutRoute, path: '/observability/conformance' }).lazy(...)`. Insert it in the routeTree.addChildren array under appShellLayoutRoute, right after `observabilitySkillDriftRoute`.
    - NO validateSearch (v1.2.0 ships without `?date=` query param per RESEARCH §REQ-12-PAGE-01 deferral).
  </behavior>
  <action>
    1. There are no automated tests for the lazy file itself (router smoke is tested via integration in ConformancePage.test.tsx + sidebar nav test in Task 3). Write the file directly.

    2. Write packages/spa/src/routes/observability.conformance.lazy.tsx — mirror observability.skill-drift.lazy.tsx structure:
    ```tsx
    import { createLazyRoute } from '@tanstack/react-router'
    import { ConformancePage } from '../components/panels/conformance/ConformancePage.js'

    export const Route = createLazyRoute('/observability/conformance')({
      component: ConformancePage,
    })
    ```

    3. Edit packages/spa/src/router.tsx — INSERT after the observabilitySkillDriftRoute block (currently lines 119-128):
    ```typescript
    /**
     * conformanceRoute — /observability/conformance page under _appshell.
     * Phase 12 D-12-01: Observability sidebar section gains 3rd entry.
     * D-12-02: sibling route (not widening Phase 5 /observability aggregator).
     * No validateSearch in v1.2.0 — deep-link to specific day deferred.
     */
    const conformanceRoute = createRoute({
      getParentRoute: () => appShellLayoutRoute,
      path: '/observability/conformance',
    }).lazy(() => import('./routes/observability.conformance.lazy.js').then((m) => m.Route))
    ```

    4. Edit packages/spa/src/router.tsx routeTree.addChildren array (currently lines 166-178) — ADD conformanceRoute to the appShellLayoutRoute.addChildren list:
    ```typescript
    appShellLayoutRoute.addChildren([
      indexRoute,
      settingsRoute,
      projectsIdRoute,
      coverageRoute,
      observabilitySkillDriftRoute,
      conformanceRoute,    // Phase 12 D-12-01
    ] as AnyRoute[]),
    ```

    5. Run pnpm --filter @agenticapps/dashboard-spa typecheck — confirm green.

    6. Run pnpm --filter @agenticapps/dashboard-spa test router --run — confirm router tests still pass (existing router tests should be tolerant of additive routes).

    7. Build smoke: pnpm --filter @agenticapps/dashboard-spa build — confirm route bundle emits.

    8. Commit: feat(12-04): wire /observability/conformance route under _appshell (REQ-12-PAGE-01).

    Anti-patterns to avoid:
    - DO NOT add validateSearch (v1.2.0 deferral).
    - DO NOT eager-import ConformancePage in router.tsx (lazy chunk must split).
    - DO NOT touch coverageRoute, observabilitySkillDriftRoute, or any other existing route definition.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <done>
    - packages/spa/src/routes/observability.conformance.lazy.tsx exists with Route export.
    - packages/spa/src/router.tsx contains `conformanceRoute` const + entry in routeTree.
    - `grep -c "/observability/conformance" packages/spa/src/router.tsx` returns at least 2 (path + lazy import).
    - `pnpm --filter @agenticapps/dashboard-spa build` exits 0; output includes the conformance route bundle (verify via build output paths).
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0.
    - 1 atomic commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sidebar 3rd entry — Conformance (RED to GREEN)</name>
  <files>
    packages/spa/src/components/ui/Sidebar.tsx (EDIT — add 3rd entry)
    packages/spa/src/components/ui/Sidebar.test.tsx (EDIT — extend existing or NEW if missing)
  </files>
  <read_first>
    packages/spa/src/components/ui/Sidebar.tsx (current — lines 71-82 show Observability section)
    packages/spa/src/components/ui/Sidebar.test.tsx (verify exists — if not, create with both Phase 11 + Phase 12 entries)
    packages/spa/src/components/ui/SidebarItem.tsx (peer primitive — same as Coverage + Skill drift entries use)
    .planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md §D-12-01 (locked: Conformance is the 3rd entry)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Open Question 3" (Coverage → Conformance → Skill drift suggested; planner picks ORDER below)
  </read_first>
  <behavior>
    - Sidebar.tsx Observability section gains a 3rd <SidebarItem> entry.
    - Order: Coverage (top) → Skill drift (middle) → Conformance (bottom).
    - Icon for Conformance: lucide-react `TrendingUp` (visually distinct from Activity-for-Coverage and Layers-for-Skill-drift).
    - Path: `/observability/conformance`.
    - Label: `Conformance`.
    - Comment updated to document graduation from 2 → 3 entries (Phase 12 D-12-01).
  </behavior>
  <action>
    1. Write/extend packages/spa/src/components/ui/Sidebar.test.tsx FIRST. If the file does NOT exist, create it with full coverage. If it exists, add the new test cases.

    Test cases (RED first):
    - it('Observability section contains 3 entries in order: Coverage, Skill drift, Conformance')
    - it('Conformance entry routes to /observability/conformance')
    - it('Conformance entry uses TrendingUp icon (visually distinct from Activity + Layers)')
    - it('Conformance entry uses SidebarItem peer primitive (NOT SidebarSubItem) — matches Phase 11 D-11-08')

    Render Sidebar via TestQueryClientProvider + MemoryRouter (existing test infrastructure — verify against existing Sidebar tests if any).

    2. Run pnpm --filter @agenticapps/dashboard-spa test Sidebar --run — confirm RED.

    3. Edit packages/spa/src/components/ui/Sidebar.tsx:

    Update the lucide-react import line:
    `import { Activity, Cog, HelpCircle, FolderKanban, Layers, TrendingUp } from 'lucide-react'`

    Update the Observability section block (currently lines 65-82). REPLACE the section with:
    ```tsx
    {/* Observability section — Phase 10 D-10-08 introduced Coverage; Phase 11 D-11-08
        added Skill drift as a peer; Phase 12 D-12-01 graduates the section to 3 peer
        entries: Coverage / Skill drift / Conformance. Order preserves existing IA
        (user-memory feedback_sidebar_section_architecture: additive growth over reorder).
        All three use the SidebarItem peer primitive (NOT SidebarSubItem). */}
    <SidebarSection label="Observability">
      <SidebarItem
        to="/coverage"
        icon={<Activity size={16} aria-hidden="true" />}
        label="Coverage"
      />
      <SidebarItem
        to="/observability/skill-drift"
        icon={<Layers size={16} aria-hidden="true" />}
        label="Skill drift"
      />
      <SidebarItem
        to="/observability/conformance"
        icon={<TrendingUp size={16} aria-hidden="true" />}
        label="Conformance"
      />
    </SidebarSection>
    ```

    4. Run pnpm --filter @agenticapps/dashboard-spa test Sidebar --run — confirm GREEN.

    5. Run full SPA suite: pnpm --filter @agenticapps/dashboard-spa test --run — verify no regression.

    6. Commit: test(12-04): assert sidebar has 3 Observability entries in order then feat(12-04): graduate Observability to 3 peer entries — add Conformance (D-12-01, REQ-12-NAV-01).

    Anti-patterns to avoid:
    - DO NOT change the order of Coverage or Skill drift (preserves existing UX).
    - DO NOT use SidebarSubItem for Conformance (D-12-01 + D-11-08 lock: SidebarItem peer pattern).
    - DO NOT pick an icon already used in the same section (visual collision).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test Sidebar --run</automated>
  </verify>
  <done>
    - packages/spa/src/components/ui/Sidebar.tsx contains 3 SidebarItem entries under SidebarSection label="Observability".
    - The order in the JSX is Coverage → Skill drift → Conformance.
    - `grep -c "/observability/conformance" packages/spa/src/components/ui/Sidebar.tsx` returns 1.
    - `grep -c "TrendingUp" packages/spa/src/components/ui/Sidebar.tsx` returns at least 2 (import + usage).
    - All 4 new sidebar tests pass.
    - 2 atomic commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /observability/conformance route → ConformancePage | Authenticated SPA-internal route under _appshell (pairing check inherited from indexRoute precedent). No new auth boundary. |
| ConformancePage useConformance → daemon | All defences inherited from Wave 3 (parseOrDrift + 30s staleTime). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verified by Task |
|-----------|----------|-----------|----------|-------------|------------|------------------|
| T-12-PAGE-ERROR-LEAK | Info disclosure | ConformancePage ErrorState branch | low | mitigate | Generic message "Could not load conformance data." — no error.message, no stack. Test: "ErrorState message does NOT include error.message or stack". | Task 1 |
| T-12-PAGE-DRIFT-RENDER | Tampering | ConformancePage schema-drift branch | medium | mitigate | When `'schemaDriftAt' in data`, route to SchemaDriftState (existing primitive) — NEVER render unvalidated data. Test: "schema-drift error path renders SchemaDriftState, never raw data". | Task 1 |
| T-12-SIDEBAR-INJECTION | Tampering | Sidebar label/path | none | accept | Label + path are compile-time literals in Sidebar.tsx; no user input involved. No render-path injection surface. | Task 3 |
| T-12-ROUTE-AUTH-BYPASS | Spoofing | /observability/conformance route | medium | mitigate | Route is under _appshell layout which inherits the pairing check via the index route's beforeLoad — same pattern as /coverage and /observability/skill-drift. Bearer auth on the daemon side is the load-bearing defence (Plan 12-02). | Task 2 |
</threat_model>

<verification>
- pnpm --filter @agenticapps/dashboard-spa test --run exits 0 (full SPA suite — no regression).
- pnpm --filter @agenticapps/dashboard-spa typecheck exits 0.
- pnpm --filter @agenticapps/dashboard-spa build exits 0; output contains the lazy chunk for /observability/conformance.
- After running `pnpm --filter @agenticapps/dashboard-spa dev`, navigating to `http://localhost:5174/observability/conformance` renders ConformancePage (manual smoke — fold into /qa Wave 6).
- 5 atomic commits exist (2 + 1 + 2).
</verification>

<success_criteria>
Wave 4 complete when:
1. /observability/conformance route exists in router.tsx + lazy file + ConformancePage component.
2. Sidebar Observability section has 3 entries: Coverage → Skill drift → Conformance.
3. ConformancePage handles loading + error + schema-drift + happy-path branches (≥ 12 tests).
4. Wave 5 can begin (Coverage responsive collapse — independent of this plan's files).
5. Wave 6 /qa walkthrough has a working surface to verify against.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-12-observability-conformance-surface/12-04-SUMMARY.md` with:
- Files created/modified (list)
- Test counts per task
- Confirmation: sidebar order chosen is Coverage → Skill drift → Conformance (documented deviation from RESEARCH OQ3 suggestion)
- One-line note on what's left for Wave 5 (Coverage responsive collapse) and Wave 6 (gates)
</output>
