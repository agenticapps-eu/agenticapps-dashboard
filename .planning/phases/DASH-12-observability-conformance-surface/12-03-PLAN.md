---
phase: 12
plan: 03
type: tdd
wave: 3
depends_on:
  - 12-00-PLAN.md
  - 12-02-PLAN.md
files_modified:
  - packages/spa/src/lib/conformanceQueries.ts
  - packages/spa/src/lib/conformanceQueries.test.ts
  - packages/spa/src/components/panels/conformance/FleetTrendChart.tsx
  - packages/spa/src/components/panels/conformance/FleetTrendChart.test.tsx
  - packages/spa/src/components/panels/conformance/FamilyCard.tsx
  - packages/spa/src/components/panels/conformance/FamilyCard.test.tsx
  - packages/spa/src/components/panels/conformance/PathDriftPanel.tsx
  - packages/spa/src/components/panels/conformance/PathDriftPanel.test.tsx
autonomous: true
requirements:
  - REQ-12-FCH-01
  - REQ-12-FCH-02
  - REQ-12-FCH-03
  - REQ-12-FCH-04
  - REQ-12-FCH-05
  - REQ-12-RPD-04
  - REQ-12-CON-04
requirements_addressed:
  - REQ-12-FCH-01
  - REQ-12-FCH-02
  - REQ-12-FCH-03
  - REQ-12-FCH-04
  - REQ-12-FCH-05
  - REQ-12-RPD-04
  - REQ-12-CON-04
objectives: >
  SPA primitives that compose into Plan 12-04's ConformancePage. Three new components
  + one query hook — FleetTrendChart.tsx (pure SVG ≤120 LOC; 4 polylines; 90-day x-axis;
  hover+focus+keyboard reveal; SR-only mirror table), FamilyCard.tsx (score + 14d delta
  + tier pill via tierOf), PathDriftPanel.tsx (collapsible panel with Fix-path affordance
  via useToast feedback), useConformance() TanStack Query hook (30s staleTime matches
  daemon cache).
notes: >
  T-12-XSS mitigation: all user-visible strings (registry paths, suggestedPath) rendered
  as text via JSX expression interpolation. The test suite explicitly bans the dangerous-
  inner-html prop (grep returns 0) in this plan's files. React's default escaping is the
  defence.
  Pitfall 5 (touch-compatible hover) mitigation: per-day reveal wires hover + focus +
  pointerdown + Escape via invisible rect tabIndex 0 (Tooltip.tsx:111-126 pattern).
  Pitfall 8 (SR users): sibling visually-hidden table.sr-only mirrors the SVG data.
  Pitfall 11 (TanStack stale-time): useConformance staleTime = 30_000 to match daemon
  cache.

must_haves:
  truths:
    - "FleetTrendChart renders 4 polylines (3 family + 1 fleet aggregate, heavier stroke)"
    - "FleetTrendChart renders y-axis gridlines at 0/25/50/75/100; threshold rules at 70 and 90 (dashed)"
    - "FleetTrendChart renders daily tick marks; date labels every 14 days (≤7 labels)"
    - "FleetTrendChart renders an empty-state message when series.length < 14 (D-12-13)"
    - "FleetTrendChart per-day reveal works on hover AND focus AND keyboard (Pitfall 5)"
    - "FleetTrendChart includes sr-only table mirror (Pitfall 8)"
    - "FleetTrendChart LOC ≤ 120 (D-12-08 — verified by line count test)"
    - "FamilyCard shows score + 14d delta + tier-coloured pill via tierOf()"
    - "PathDriftPanel lists drifted entries; Fix-path button POSTs to /api/admin/registry/fix-path"
    - "PathDriftPanel shows success/error toast via useToast on fix-path settle"
    - "PathDriftPanel uses inFlightRefreshes Set pattern (Phase 11.2) for concurrent click safety"
    - "useConformance hook has staleTime 30_000 + parseOrDrift schema-drift defence"
  artifacts:
    - path: "packages/spa/src/lib/conformanceQueries.ts"
      provides: "useConformance + useRegistryFixPath TanStack Query hooks"
      contains: "export function useConformance"
    - path: "packages/spa/src/components/panels/conformance/FleetTrendChart.tsx"
      provides: "Pure-SVG 90-day fleet trend chart"
      contains: "export function FleetTrendChart"
      min_lines: 80
    - path: "packages/spa/src/components/panels/conformance/FamilyCard.tsx"
      provides: "Per-family conformance card (score + 14d delta + tier pill)"
      contains: "export function FamilyCard"
    - path: "packages/spa/src/components/panels/conformance/PathDriftPanel.tsx"
      provides: "Drift entries + Fix-path affordance"
      contains: "export function PathDriftPanel"
  key_links:
    - from: "packages/spa/src/lib/conformanceQueries.ts"
      to: "@agenticapps/dashboard-shared ConformanceResponseSchema + tierOf"
      via: "import + parseOrDrift schema validation"
      pattern: "ConformanceResponseSchema"
    - from: "packages/spa/src/components/panels/conformance/PathDriftPanel.tsx"
      to: "POST /api/admin/registry/fix-path"
      via: "useMutation + apiFetch"
      pattern: "/api/admin/registry/fix-path"
    - from: "packages/spa/src/components/panels/conformance/FamilyCard.tsx"
      to: "tierOf from @agenticapps/dashboard-shared"
      via: "named import"
      pattern: "tierOf\\("
---

<objective>
Wave 3 SPA presentation primitives. Four files (3 components + 1 query hook) that compose
into Plan 12-04's ConformancePage:

1. **conformanceQueries.ts** — useConformance() hook (30s staleTime per Pitfall 11) +
   useRegistryFixPath() mutation hook. Both use apiFetch + parseOrDrift schema defence
   (INV-04). Mirror coverageQueries.ts pattern.

2. **FleetTrendChart.tsx** — Pure-SVG ≤120 LOC primitive. 4 polylines (3 family + 1
   heavy-stroke fleet aggregate). 90-day x-axis with daily tick marks + 14-day label
   cadence. Y-axis 0-100 with gridlines at 0/25/50/75/100 + threshold dashed rules at
   70 (amber) and 90 (green). Hover + focus + keyboard + Escape reveal via invisible
   rect tabIndex 0 overlays. Per-day breakdown panel anchored top-right within chart
   bounds. SR-only table mirror (Pitfall 8). Empty/building state when series.length
   < 14 (D-12-13).

3. **FamilyCard.tsx** — Single family's conformance card. Score (0-100 integer),
   14d delta with up/down glyph, tier pill via tierOf() mapping to status tokens.
   Reuses Phase 5.1 text-status-success/-warning/-error tokens.

4. **PathDriftPanel.tsx** — Collapsible panel above family cards. Lists drifted entries
   from ConformanceResponse.drifted. Per-entry: project name + stored path + suggested
   path (when present) + Fix path button. Manual paste input when suggestedPath ===
   null. Wires useRegistryFixPath mutation; success → green toast, error → red toast.
   Concurrent-click safety via inFlightRefreshes ReadonlySet (Phase 11.2 pattern).

Purpose: Lock the rendering primitives. Plan 12-04 composes them. Plan 12-05 reuses
useViewportBreakpoint (already Wave 0) for the Coverage responsive collapse.
Output: 4 new files + 4 test files. SPA suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-CONTEXT.md
@.planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md
@.planning/phases/DASH-12-observability-conformance-surface/12-00-PLAN.md
@.planning/phases/DASH-12-observability-conformance-surface/12-02-PLAN.md
@packages/shared/src/schemas/conformance.ts
@packages/spa/src/lib/coverageQueries.ts
@packages/spa/src/lib/api.ts
@packages/spa/src/components/ui/Toast.tsx
@packages/spa/src/components/ui/Tooltip.tsx
@packages/spa/src/components/panels/coverage/CoverageDriftBadge.tsx
@packages/spa/src/components/panels/coverage/CoveragePage.tsx
@packages/spa/src/styles/tokens.css

<interfaces>
<!-- Existing SPA APIs Plan 12-03 composes. -->

From @agenticapps/dashboard-shared (Wave 0 + existing):
- ConformanceResponse, ConformanceDayPoint, PathDriftEntry, ConformanceTier, tierOf, ConformanceResponseSchema, RegistryFixPathRequest, RegistryEntry, RegistryEntrySchema

From packages/spa/src/lib/api.ts (existing):
- export function apiFetch<T>(url: string, Schema: z.ZodSchema<T>, options?: ApiOptions): Promise<ApiResult<T>>
- ApiResult = { ok: true, data: T } | { ok: false, drift: { path: string } } | { ok: false, error: ApiError }

From packages/spa/src/lib/coverageQueries.ts (template pattern):
- export const COVERAGE_STALE_TIME_MS = 30_000
- export function useCoverage(): UseQueryResult<CoverageResponse>
- export function useCoverageRefresh(): UseMutationResult<...>

From packages/spa/src/components/ui/Toast.tsx (Phase 11.1):
- export function useToast(): { show: (opts: { message: string; variant: 'success' | 'error' }) => void }

From packages/spa/src/components/ui/Tooltip.tsx (Phase 11.2):
- <Tooltip content="...">{children}</Tooltip>

From Phase 5.1 tokens (tokens.css):
- text-status-success (green) / text-status-warning (amber) / text-status-error (red)
- bg-status-success / bg-status-warning / bg-status-error
- text-text-primary, text-text-secondary, text-text-tertiary (post-Phase 11.1)
- bg-card-bg, border-border-subtle
- z-overlay = 100 (for hover panel)

From packages/spa/src/components/panels/coverage/CoveragePage.tsx (Phase 11.2 inFlightRefreshes pattern):
- const [inFlightRefreshes, setInFlightRefreshes] = useState<ReadonlySet<string>>(new Set())
- Wraps mutation onMutate/onSettled to add/remove entries from the Set
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conformanceQueries.ts — useConformance + useRegistryFixPath (RED to GREEN)</name>
  <files>
    packages/spa/src/lib/conformanceQueries.ts (NEW)
    packages/spa/src/lib/conformanceQueries.test.ts (NEW)
  </files>
  <read_first>
    packages/spa/src/lib/conformanceQueries.ts (verify NEW)
    packages/spa/src/lib/coverageQueries.ts (verbatim template — staleTime + parseOrDrift pattern)
    packages/spa/src/lib/coverageQueries.test.ts (verbatim test template)
    packages/spa/src/lib/api.ts (apiFetch + ApiResult + parseOrDrift shape)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §Pitfall 11 (TanStack stale-time)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Validation Architecture" REQ-12-PAGE-02
  </read_first>
  <behavior>
    - export const CONFORMANCE_STALE_TIME_MS = 30_000 (matches daemon cache TTL per Pitfall 11).
    - export function useConformance(): UseQueryResult<ConformanceResponse | { schemaDriftAt: string }>
      - queryKey: ['conformance']
      - queryFn: apiFetch('/api/observability/conformance', ConformanceResponseSchema)
      - On drift: return { schemaDriftAt: drift.path }
      - On success: return data
      - staleTime: CONFORMANCE_STALE_TIME_MS
      - refetchOnWindowFocus: false
    - export interface UseRegistryFixPathVariables { id: string; newPath: string }
    - export function useRegistryFixPath(): UseMutationResult<RegistryEntry, ApiError, UseRegistryFixPathVariables>
      - mutationFn: apiFetch('/api/admin/registry/fix-path', RegistryEntrySchema, { method: 'POST', body: JSON.stringify({ id, newPath }), headers: { 'Content-Type': 'application/json' } })
      - onSuccess: queryClient.invalidateQueries({ queryKey: ['conformance'] }) + queryClient.invalidateQueries({ queryKey: ['coverage'] })
  </behavior>
  <action>
    1. Write packages/spa/src/lib/conformanceQueries.test.ts FIRST (RED). Mirror coverageQueries.test.ts.

    Test cases:
    - it('useConformance fetches /api/observability/conformance and parses the response')
    - it('useConformance returns schemaDriftAt when parseOrDrift fails')
    - it('useConformance staleTime is 30_000 (matches daemon cache)')
    - it('useConformance does NOT refetch on window focus')
    - it('useRegistryFixPath POSTs { id, newPath } to /api/admin/registry/fix-path')
    - it('useRegistryFixPath onSuccess invalidates conformance + coverage queries')
    - it('useRegistryFixPath surfaces 422 errors as ApiError to onError handler')

    2. Run pnpm --filter @agenticapps/dashboard-spa test conformanceQueries --run — confirm RED.

    3. Write packages/spa/src/lib/conformanceQueries.ts:
    - Imports: useQuery, useMutation, useQueryClient, type UseQueryResult, type UseMutationResult from @tanstack/react-query; ConformanceResponseSchema, RegistryEntrySchema, type ConformanceResponse, type RegistryEntry from @agenticapps/dashboard-shared; apiFetch, type ApiError from lib/api
    - export const CONFORMANCE_STALE_TIME_MS = 30_000
    - export type ConformanceQueryResult = ConformanceResponse | { schemaDriftAt: string }
    - export function useConformance — use the queryFn pattern from coverageQueries: try apiFetch, check r.ok, if !r.ok and 'drift' in r return { schemaDriftAt: r.drift.path }, if !r.ok throw r.error, else return r.data. staleTime + refetchOnWindowFocus: false.
    - export interface UseRegistryFixPathVariables { id: string; newPath: string }
    - export function useRegistryFixPath — useMutation with mutationFn calling apiFetch with method POST + JSON body + Content-Type header; onSuccess invalidates both query keys.

    4. Run pnpm --filter @agenticapps/dashboard-spa test conformanceQueries --run — confirm GREEN.

    5. Commit: test(12-03): add failing tests for conformanceQueries then feat(12-03): useConformance + useRegistryFixPath hooks (D-12-17, Pitfall 11).

    Anti-patterns to avoid:
    - DO NOT set staleTime: 0 (Pitfall 11 — refetch storm).
    - DO NOT use refetchOnWindowFocus: true (over-fetches; daemon cache already handles freshness).
    - DO NOT skip parseOrDrift handling (INV-04 schema-drift contract).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test conformanceQueries --run</automated>
  </verify>
  <done>
    - packages/spa/src/lib/conformanceQueries.ts exists with useConformance + useRegistryFixPath + CONFORMANCE_STALE_TIME_MS exports.
    - All 7 tests pass.
    - `grep -c "staleTime: 30_000\|staleTime: CONFORMANCE_STALE_TIME_MS" packages/spa/src/lib/conformanceQueries.ts` returns at least 1.
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FleetTrendChart.tsx — pure-SVG ≤120 LOC primitive (RED to GREEN)</name>
  <files>
    packages/spa/src/components/panels/conformance/FleetTrendChart.tsx (NEW)
    packages/spa/src/components/panels/conformance/FleetTrendChart.test.tsx (NEW)
  </files>
  <read_first>
    packages/spa/src/components/panels/conformance/FleetTrendChart.tsx (verify NEW)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"FleetTrendChart sketch" lines 851-948 (verbatim source — VERY load-bearing)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §Pitfall 4 (tick density), §Pitfall 5 (touch-compatible hover), §Pitfall 8 (SR table mirror)
    packages/spa/src/components/panels/coverage/CoverageDriftBadge.tsx (Phase 11 — pure-SVG primitive precedent)
    packages/spa/src/styles/tokens.css (text-status-info/-warning/-error tokens + border-border-subtle)
    packages/shared/src/schemas/conformance.ts (ConformanceDayPoint shape)
  </read_first>
  <behavior>
    - Props: { series: ConformanceDayPoint[]; ariaLabel: string }
    - Empty state when series.length === 0 → renders a text-tertiary div with "No history yet — building 90-day trend".
    - Building state when series.length > 0 AND series.length < 14 → renders text-tertiary div with `Building 90-day trend — ${14 - series.length} more day(s) of data needed` (D-12-13).
    - When series.length >= 14: render the full SVG chart.
    - SVG viewBox "0 0 720 240"; PAD top:20, right:16, bottom:32, left:36.
    - 5 horizontal gridlines at y(0)/y(25)/y(50)/y(75)/y(100). Threshold rules at y(70) and y(90) use strokeDasharray="4 4" and stroke-border-subtle (full opacity); others use stroke-border-subtle/50.
    - 3 family polylines with stroke widths 1.5, distinct tokens: agenticapps → stroke-status-info, factiv → stroke-status-warning, neuroflash → stroke-accent. (Planner discretion — these chosen because they're visually distinct and live in Phase 5.1 palette.)
    - 1 fleet polyline stroke-text-primary, strokeWidth 2.5 (heavier — D-12-10).
    - Daily tick marks (90 small lines, ~4px tall, every day position) + text labels every 14 days (≈7 labels max). Anchor 14-day cadence to most-recent date so today is the rightmost label.
    - Per-day invisible <rect tabIndex={0}> overlay per data point — wires onMouseEnter, onMouseLeave, onFocus, onBlur, onPointerDown, onKeyDown (Escape closes). Pitfall 5.
    - Per-day breakdown panel: when hoverIdx !== null, render absolute-positioned top-right div with date + 4 family scores + fleet score (D-12-11). Anchored top-right within chart bounds; visible on viewport <900px without overflow (Open Question 2 resolution).
    - SR-only <table className="sr-only"> mirror: caption + one <tr> per day with Date / Fleet / agenticapps / factiv / neuroflash columns. role="img" + aria-label on the chart wrapper (Pitfall 8).
    - Y-axis text labels at 0, 50, 100 (left of plot, font-size 10, fill-text-tertiary).
    - LOC budget ≤ 120 (D-12-08) — verified via a test that reads the file via fs.readFileSync and counts non-blank non-comment lines.
  </behavior>
  <action>
    1. Write packages/spa/src/components/panels/conformance/FleetTrendChart.test.tsx FIRST. Use @testing-library/react + jsdom.

    Test cases (RED first):
    - it('empty state when series is empty')
    - it('building state when series.length < 14 (shows "N more days needed")')
    - it('renders 4 polylines (3 family + 1 fleet) when series.length >= 14')
    - it('fleet polyline has heavier stroke (strokeWidth 2.5) than family polylines (1.5)')
    - it('renders 5 gridlines at y(0), y(25), y(50), y(75), y(100)')
    - it('threshold rules at y(70) and y(90) use strokeDasharray')
    - it('renders 90 daily tick marks when series.length === 90')
    - it('renders ≤ 7 date labels (every 14 days)')
    - it('hover reveal: onMouseEnter on a per-day rect shows breakdown panel with date + 5 numbers')
    - it('focus reveal: onFocus on a per-day rect shows breakdown panel (keyboard nav)')
    - it('pointerdown reveal: onPointerDown shows breakdown panel (touch device)')
    - it('Escape key on focused rect closes the panel')
    - it('SR-only table mirrors SVG data with one row per day')
    - it('role="img" + aria-label is set on the chart wrapper')
    - it('LOC budget: file has ≤ 120 non-blank non-comment lines') — fs.readFileSync the file, split on \n, filter out empty + comment-only lines, expect ≤ 120
    - it('SECURITY: file does NOT contain the dangerous-inner-html prop name') — `expect(text.includes("dangerouslySetInnerHTML")).toBe(false)`

    2. Run pnpm --filter @agenticapps/dashboard-spa test FleetTrendChart --run — confirm RED.

    3. Write packages/spa/src/components/panels/conformance/FleetTrendChart.tsx VERBATIM from RESEARCH.md lines 851-948, with these specific edits:

    - Add the building-state branch before the empty state:
      if (series.length === 0) return <div className="text-text-tertiary text-sm">No history yet — building 90-day trend.</div>
      if (series.length < 14) return <div className="text-text-tertiary text-sm">Building 90-day trend — {14 - series.length} more day(s) of data needed.</div>

    - Add `onPointerDown={() => setHoverIdx(i)}` to the invisible rect overlay (Pitfall 5 touch).
    - Add `onKeyDown={(e) => { if (e.key === 'Escape') setHoverIdx(null) }}` to the rect (Pitfall 5 keyboard close).
    - Keep the verbatim SVG structure for the rest of the file.
    - The breakdown panel uses `bg-card-bg border border-border-subtle rounded-md p-3 shadow-card text-sm` (Phase 5.1 tokens).
    - LOC discipline: collapse multi-line JSX where possible; the verbatim sketch is ~105 LOC. Stay under 120 even with the additional branches.

    4. Run pnpm --filter @agenticapps/dashboard-spa test FleetTrendChart --run — confirm GREEN.

    5. If LOC > 120 after implementation: extract the breakdown panel into a sibling component (FleetTrendBreakdown.tsx) before re-running tests. Document the extraction in the SUMMARY.

    6. Commit: test(12-03): add failing tests for FleetTrendChart (incl. LOC + a11y + touch) then feat(12-03): FleetTrendChart pure-SVG primitive (D-12-08/09/10/11/12/13 + Pitfalls 4/5/8).

    Anti-patterns to avoid:
    - DO NOT use Recharts/Chart.js/D3 (D-12-08).
    - DO NOT use the dangerous-inner-html prop anywhere (T-12-XSS).
    - DO NOT rely on hover-only events (Pitfall 5 — touch breaks).
    - DO NOT skip the SR-only table (Pitfall 8 — a11y).
    - DO NOT exceed 120 LOC (D-12-08).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test FleetTrendChart --run</automated>
  </verify>
  <done>
    - packages/spa/src/components/panels/conformance/FleetTrendChart.tsx exists with default export FleetTrendChart.
    - All 16 tests pass.
    - `wc -l packages/spa/src/components/panels/conformance/FleetTrendChart.tsx` returns ≤ 130 (file lines; LOC after stripping comments ≤ 120 per the test).
    - The LOC + dangerous-inner-html assertion tests both pass.
    - `grep -c "polyline\|<line\|<rect\|<text" packages/spa/src/components/panels/conformance/FleetTrendChart.tsx` returns at least 8 (real SVG drawing).
    - 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FamilyCard.tsx + PathDriftPanel.tsx (RED to GREEN, parallel within task — different files)</name>
  <files>
    packages/spa/src/components/panels/conformance/FamilyCard.tsx (NEW)
    packages/spa/src/components/panels/conformance/FamilyCard.test.tsx (NEW)
    packages/spa/src/components/panels/conformance/PathDriftPanel.tsx (NEW)
    packages/spa/src/components/panels/conformance/PathDriftPanel.test.tsx (NEW)
  </files>
  <read_first>
    packages/spa/src/components/panels/conformance/FamilyCard.tsx (verify NEW)
    packages/spa/src/components/panels/conformance/PathDriftPanel.tsx (verify NEW)
    packages/spa/src/components/panels/coverage/CoveragePage.tsx (inFlightRefreshes Set pattern — Phase 11.2)
    packages/spa/src/components/ui/Toast.tsx (useToast API)
    packages/spa/src/lib/conformanceQueries.ts (Task 1 — useRegistryFixPath mutation hook)
    packages/shared/src/schemas/conformance.ts (tierOf + PathDriftEntry shape)
    .planning/phases/DASH-12-observability-conformance-surface/12-RESEARCH.md §"Validation Architecture" REQ-12-RPD-04
  </read_first>
  <behavior>
    FamilyCard:
    - Props: { family: 'agenticapps' | 'factiv' | 'neuroflash'; score: number; delta14d: number }
    - Renders: family name + score (large MetricNumeric — Phase 5.1 primitive if available, else <span className="text-4xl font-bold">) + 14d delta with up/down glyph + tier pill (StatusPill primitive if available, else inline span with bg-status-*).
    - tier = tierOf(score) → green → bg-status-success/text-status-success; amber → bg-status-warning; red → bg-status-error.
    - delta14d 0 → render "—" (no glyph); positive → up-glyph N (text-status-success); negative → down-glyph |N| (text-status-error).
    - Phase 5.1 token-only — NO hex literals (verify-contrast.test.ts enforces).

    PathDriftPanel:
    - Props: { drifted: PathDriftEntry[] }
    - Returns null when drifted.length === 0 (hide entirely when empty).
    - Otherwise: collapsible panel with chevron toggle (Phase 5.1 pattern) + count `${N} drifted registr${y/ies}`.
    - Per-entry row: project name (from entry.id) + storedPath (text-text-secondary, monospace, truncate-with-ellipsis) + arrow → suggestedPath OR a controlled text input + Fix path button.
    - Fix path button: when suggestedPath !== null, calls useRegistryFixPath.mutate({ id, newPath: suggestedPath }); when null, button is disabled until user pastes a non-empty path into the input.
    - Per-row in-flight state via inFlightRefreshes: ReadonlySet<string> (Phase 11.2 pattern) — keyed by entry.id. While in-flight: button disabled + aria-busy="true" + spinner inline.
    - On mutation settle: useToast.show({ message, variant }) — success: `Fixed registry path for ${id}` (green); error: `Failed to fix path: ${errorCodeToMessage(code)}` (red). Error codes mapped to user-friendly strings.
    - SECURITY: storedPath + suggestedPath rendered as JSX text via expression interpolation (React escaping defence). Manual paste input value clamped via maxLength={4096} (defence-in-depth; .strict() rejects extra chars but the UI input matches).
  </behavior>
  <action>
    1. Write packages/spa/src/components/panels/conformance/FamilyCard.test.tsx + PathDriftPanel.test.tsx FIRST (RED).

    FamilyCard test cases:
    - it('renders family name capitalised')
    - it('renders score as a number (0-100 integer)')
    - it('renders tier pill with green class when score >= 90')
    - it('renders tier pill with amber class when 70 <= score < 90')
    - it('renders tier pill with red class when score < 70')
    - it('renders up-arrow + N for positive delta14d (text-status-success)')
    - it('renders down-arrow + N for negative delta14d (text-status-error)')
    - it('renders em-dash for delta14d 0 (no glyph)')
    - it('SECURITY: no hex literals — uses Phase 5.1 status tokens') — assert `grep -c "#" file.tsx` returns 0
    - it('SECURITY: file does NOT contain the dangerous-inner-html prop name')

    PathDriftPanel test cases:
    - it('renders null when drifted is empty')
    - it('renders count when drifted has entries')
    - it('collapsible: clicking chevron toggles entries visibility')
    - it('per-entry: shows project name + storedPath as text')
    - it('per-entry with suggestedPath: Fix path button is enabled')
    - it('per-entry without suggestedPath: shows controlled input, button disabled until non-empty')
    - it('clicking Fix path: calls useRegistryFixPath.mutate({ id, newPath })')
    - it('successful mutation: shows green toast via useToast')
    - it('error mutation: shows red toast with mapped error code')
    - it('in-flight: button is disabled + aria-busy=true + spinner shown for that row')
    - it('concurrent: two simultaneous clicks on different rows produce independent in-flight states (inFlightRefreshes Set)')
    - it('SECURITY: storedPath rendered as JSX text — file does NOT contain the dangerous-inner-html prop name')
    - it('SECURITY: paste input maxLength=4096')

    Use TestQueryClientProvider + ToastProvider wrappers (existing test helpers — see CoveragePage.test.tsx for pattern).

    2. Run pnpm --filter @agenticapps/dashboard-spa test FamilyCard PathDriftPanel --run — confirm RED.

    3. Write packages/spa/src/components/panels/conformance/FamilyCard.tsx:
    - Imports: React; tierOf, type ConformanceTier from @agenticapps/dashboard-shared.
    - Props interface: { family: 'agenticapps' | 'factiv' | 'neuroflash'; score: number; delta14d: number }
    - Helper getTierClasses(tier): green → 'bg-status-success/10 text-status-success border-status-success/30'; amber → 'bg-status-warning/10 text-status-warning border-status-warning/30'; red → 'bg-status-error/10 text-status-error border-status-error/30'
    - Helper deltaGlyph(d): d > 0 → { glyph: '▲', cls: 'text-status-success', label: `Up ${d} points` }; d < 0 → { glyph: '▼', cls: 'text-status-error', label: `Down ${Math.abs(d)} points` }; d === 0 → { glyph: '—', cls: 'text-text-tertiary', label: 'No change' }
    - JSX: <article className="rounded-lg border border-border-subtle bg-card-bg p-4 flex flex-col gap-2"> with header (family name) + score + delta + tier pill. Use aria-label on the delta glyph for SR.

    4. Write packages/spa/src/components/panels/conformance/PathDriftPanel.tsx:
    - Imports: React (useState); useRegistryFixPath from lib/conformanceQueries; useToast from components/ui/Toast; type PathDriftEntry from @agenticapps/dashboard-shared; ChevronDown, ChevronRight, Loader2 from lucide-react.
    - State: expanded: boolean (default true when entries present); manualPaths: Record<string, string> (per-row input state when suggestedPath null); inFlightRefreshes: ReadonlySet<string> (Phase 11.2 pattern).
    - Helper errorCodeToMessage(code): newPath_outside_family_roots → "Path is outside the family roots"; newPath_blocked → "Path is blocked (system or secret dir)"; newPath_unresolvable → "Path does not exist on disk"; project_not_found → "Project no longer in registry"; rate_limited → "Too many requests — try again in a few seconds"; default → "Fix failed".
    - On click Fix path: call mutation.mutate({ id, newPath: suggestedPath ?? manualPaths[id] }). onMutate: setInFlightRefreshes(prev => new Set([...prev, id])). onSettled: setInFlightRefreshes(prev => { const next = new Set(prev); next.delete(id); return next }). onSuccess: toast.show({ message: `Fixed registry path for ${id}`, variant: 'success' }). onError: toast.show({ message: `Failed to fix path: ${errorCodeToMessage(err.code)}`, variant: 'error' }).
    - JSX: collapsible <section> with header + entries list. Each entry is a <div className="flex items-center gap-2 py-2 border-b border-border-subtle"> with project name + arrow + storedPath (truncate) + (suggestedPath as JSX text OR controlled <input maxLength={4096} value={manualPaths[id] ?? ''} onChange={(e) => setManualPaths({...manualPaths, [id]: e.target.value})} />) + <button disabled={isInFlight || (!suggestedPath && !manualPaths[id])} aria-busy={isInFlight}>Fix path</button>.

    5. Run pnpm --filter @agenticapps/dashboard-spa test FamilyCard PathDriftPanel --run — confirm GREEN.

    6. Commit: test(12-03): add failing tests for FamilyCard + PathDriftPanel then feat(12-03): FamilyCard + PathDriftPanel components (D-12-04, D-12-20, D-12-21).

    Anti-patterns to avoid:
    - DO NOT use hex literals (Phase 11.1 verify-contrast.test.ts enforces).
    - DO NOT use the dangerous-inner-html prop on any element (T-12-XSS).
    - DO NOT make Fix path button auto-confirm without user click (D-12-21).
    - DO NOT swallow mutation errors — every error must produce a toast.
    - DO NOT skip the inFlightRefreshes Set pattern (Phase 11.2 concurrent-click safety).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test FamilyCard PathDriftPanel --run</automated>
  </verify>
  <done>
    - 4 new files exist (2 components + 2 tests).
    - FamilyCard test count ≥ 10; PathDriftPanel test count ≥ 13.
    - The dangerous-inner-html grep assertion tests both pass.
    - `grep -cE "#[0-9a-fA-F]{3,}" packages/spa/src/components/panels/conformance/FamilyCard.tsx packages/spa/src/components/panels/conformance/PathDriftPanel.tsx` returns 0 (no hex literals).
    - `grep -c "inFlightRefreshes" packages/spa/src/components/panels/conformance/PathDriftPanel.tsx` returns at least 2.
    - `grep -c "tierOf" packages/spa/src/components/panels/conformance/FamilyCard.tsx` returns at least 1.
    - 2 atomic commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon → SPA via /api/observability/conformance | Untrusted wire shape — parseOrDrift validates against ConformanceResponseSchema; drift surfaces as schemaDriftAt, never renders. |
| SPA user input (manual paste) → /api/admin/registry/fix-path | maxLength=4096 + Zod .strict() rejection; daemon defences (Plan 12-02) are the load-bearing guards. |
| PathDriftEntry.storedPath / suggestedPath → DOM render | React JSX text escaping is the XSS defence — text content rendered via JSX expression interpolation, never via the dangerous-inner-html prop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verified by Task |
|-----------|----------|-----------|----------|-------------|------------|------------------|
| T-12-XSS | Tampering / Info disclosure | FleetTrendChart + FamilyCard + PathDriftPanel | high | mitigate | React JSX default escaping. Tests assert the dangerous-inner-html prop name does NOT appear in any of the three component files. PathDriftEntry strings rendered as JSX expression interpolations only. | Tasks 2 + 3 |
| T-12-SCHEMA-DRIFT-RENDER | Tampering | useConformance fetch | medium | mitigate | parseOrDrift in apiFetch returns { ok: false, drift: { path } }. useConformance maps to { schemaDriftAt }; ConformancePage (Plan 12-04) renders SchemaDriftState component. No render path consumes unvalidated data. | Task 1 |
| T-12-INPUT-OVERFLOW | DoS | PathDriftPanel manual paste input | low | mitigate | <input maxLength={4096}> caps input length. Zod .min(1) rejects empty; daemon rate-limiter caps mutation rate. | Task 3 |
| T-12-CONCURRENT-FIX | Tampering | PathDriftPanel double-click | low | mitigate | inFlightRefreshes Set (Phase 11.2 pattern) — per-row button disabled while mutation pending; Phase 11.2 regression class verified. | Task 3 |
| T-12-TOAST-LEAK | Info disclosure | useToast error message | low | mitigate | errorCodeToMessage maps daemon error codes to user-friendly strings — NEVER includes raw error.message (which could leak FS paths or stack). | Task 3 |
</threat_model>

<verification>
- pnpm --filter @agenticapps/dashboard-spa test --run exits 0.
- pnpm --filter @agenticapps/dashboard-spa typecheck exits 0.
- Tests asserting the dangerous-inner-html string is absent pass across all Phase 12 SPA files.
- wc -l packages/spa/src/components/panels/conformance/FleetTrendChart.tsx shows ≤ 130 raw lines (LOC after stripping comments ≤ 120 per test).
- 6 atomic commits exist (2 per task × 3 tasks).
</verification>

<success_criteria>
Wave 3 complete when:
1. FleetTrendChart renders 4 polylines + gridlines + threshold rules + touch-compatible per-day reveal + SR-only mirror; LOC budget ≤ 120 verified.
2. FamilyCard renders score + 14d delta + tier pill using Phase 5.1 tokens only (no hex literals).
3. PathDriftPanel lists drifted entries + Fix-path affordance with toast feedback + concurrent-click safety.
4. useConformance hook fetches with 30s staleTime + parseOrDrift defence.
5. All ≥ 36 tests across 3 tasks pass; no SPA regression.
6. Wave 4 (ConformancePage composition) can import all 4 from this plan and compose.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-12-observability-conformance-surface/12-03-SUMMARY.md` with:
- Files created (list)
- Test counts per task
- LOC budget verdict for FleetTrendChart (actual LOC + whether extraction was needed)
- Confirmation: T-12-XSS mitigated (dangerous-inner-html prop name absent from all three component files)
- Confirmation: Phase 11.1 verify-contrast.test.ts still passes (no hex literals introduced)
</output>
