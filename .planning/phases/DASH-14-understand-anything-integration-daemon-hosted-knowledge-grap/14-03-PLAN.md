---
phase: 14-understand-anything-integration-daemon-hosted-knowledge-grap
plan: 03
type: tdd
wave: 2
depends_on: [14-01]
files_modified:
  - packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx
  - packages/spa/src/components/panels/coverage/UnderstandCopyPill.test.tsx
  - packages/spa/src/components/panels/coverage/coverageColumns.ts
  - packages/spa/src/components/panels/coverage/coverageColumns.test.ts
  - packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts
  - packages/spa/src/components/panels/coverage/CoverageRow.tsx
  - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx
  - packages/spa/src/components/panels/coverage/CoveragePage.tsx
  - packages/spa/src/components/panels/coverage/CoveragePage.test.tsx
autonomous: true
requirements: [D-14-06, D-14-07, D-14-08, D-14-10]

must_haves:
  truths:
    - "The Coverage matrix shows an Understand column per repo: fresh rows render a ✓ that doubles as a new-tab viewer link; stale rows render the stale state WITH a viewer link AND a copy pill; missing rows render a copy pill (D-14-06/D-14-08/D-14-10)"
    - "Clicking the copy pill puts `cd ~/Sourcecode/{family}/{repo} && claude \"/understand\"` on the clipboard with toast feedback (D-14-10)"
    - "Viewer links open in a new tab at {agentUrl}/understand/{repoId}/?token={row viewerToken} — never the main bearer token (D-14-07, D-14-03)"
    - "Stale/fresh cells expose lastAnalyzedAt via tooltip (D-14-08)"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx"
      provides: "Copy pill + viewer link composite cell content"
      min_lines: 40
    - path: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      provides: "COVERAGE_COL_WIDTHS.understand entry"
      contains: "understand"
    - path: "packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts"
      provides: "understand column-header tooltip copy"
      contains: "understand"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx"
      to: "packages/shared/src/clipboard.ts"
      via: "buildUnderstandCommand import from @agenticapps/dashboard-shared"
      pattern: "buildUnderstandCommand"
    - from: "packages/spa/src/components/panels/coverage/CoveragePage.tsx"
      to: "row.understand.viewerToken"
      via: "viewer URL construction passed down to CoverageRow"
      pattern: "/understand/"
---

<objective>
Add the Understand column to the Coverage matrix: 3-state cell (fresh ✓-link / stale link+pill / missing pill), copy-command pill with toast feedback, and viewer-URL construction from the per-row scoped token.

Purpose: D-14-06's "Understand-cell ✓ doubles as a link", D-14-08 staleness display, D-14-10 copy pill.
Output: new UnderstandCopyPill component, column wiring across desktop + mobile coverage tables, CoveragePage URL plumbing.

NOTE: until the daemon plans (14-05/14-06) ship, `row.understand` is undefined at runtime — the column must render an empty/em-dash cell for undefined without crashing (optional-field back-compat in both directions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-CONTEXT.md
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-PATTERNS.md

<interfaces>
From packages/shared (plan 14-01 — read the shipped source, not the plan):
- `buildUnderstandCommand(family, repo): { string, argv }` exported from @agenticapps/dashboard-shared
- `CoverageRow.understand?: { kind: 'basic', state: 'fresh'|'stale'|'missing'|'not-applicable', lastAnalyzedAt?, analyzedCommit?, analyzedFiles?, viewerToken?, degraded?, degradedReason? }` — state is 'fresh', NOT 'present'

From packages/spa (verified to exist):
- `COVERAGE_COL_WIDTHS` in coverageColumns.ts (repo w-72, gitNexus w-36, wiki w-72, workflow w-32, actions w-12)
- `coverageColumnTooltips.ts` tooltip SoT (D-11.2-05)
- `InstallGitNexusButton.tsx` — writeToClipboard + useToast pattern donor
- `CoverageCell.tsx` — 3-state presentational cell
- `Tooltip` primitive in packages/spa/src/components/ui/Tooltip.tsx (D-11.2-01)
- `getPairing()` in packages/spa/src/lib/pairing.ts → { agentUrl, token } (use agentUrl ONLY; never pairing.token in viewer URLs)
- D-5.1-10 constraint: NO cn()/clsx/CVA, NO hex literals, NO shadcn aliases in any new component
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UnderstandCopyPill component + column width + header tooltip</name>
  <files>packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx, packages/spa/src/components/panels/coverage/UnderstandCopyPill.test.tsx, packages/spa/src/components/panels/coverage/coverageColumns.ts, packages/spa/src/components/panels/coverage/coverageColumns.test.ts, packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/InstallGitNexusButton.tsx (the entire pattern donor: writeToClipboard, useToast, aria-labels, Tailwind token classes)
    - packages/spa/src/components/panels/coverage/coverageColumns.ts + coverageColumns.test.ts (width SoT + regression-lock test style, D-11.2-10 comment-chain convention)
    - packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts
    - packages/spa/src/lib/clipboardCompat.ts (writeToClipboard signature)
  </read_first>
  <behavior>
    - Test 1: state='missing' renders the copy pill (button labelled /understand) and NO viewer link
    - Test 2: state='stale' renders BOTH the viewer link (when viewerUrl prop set) AND the copy pill (D-14-10: "stale row keeps its link too" — deviation from the 14-PATTERNS.md sketch which linked only on present/fresh)
    - Test 3: state='fresh' renders the viewer link only, target="_blank" rel="noopener noreferrer" (D-14-07), and NO pill
    - Test 4: click on pill calls writeToClipboard with the exact buildUnderstandCommand string and shows a success toast; clipboard failure shows error toast
    - Test 5: viewer link href equals the viewerUrl prop verbatim (URL construction is the parent's job)
  </behavior>
  <action>
    Create UnderstandCopyPill.tsx with props `{ family: string; repo: string; viewerUrl?: string; state: 'fresh' | 'stale' | 'missing' }`. Render rules: link iff (state==='fresh' || state==='stale') && viewerUrl; pill iff state==='stale' || state==='missing'. Use lucide `ExternalLink` + `Copy` icons sized per InstallGitNexusButton; aria-labels "Open knowledge graph for {repo} in new tab" and "Copy understand command for {repo} to clipboard". Add `understand: 'w-36'` to COVERAGE_COL_WIDTHS (before actions key, with D-11.2-10-style comment "Phase 14 — same density as gitNexus: link + pill"), extend coverageColumns.test.ts regression lock. Add tooltip entry `understand: 'Understand-anything knowledge graph. Built by /understand; stored under <repo>/.understand-anything/.'` to coverageColumnTooltips.ts.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- --run src/components/panels/coverage/UnderstandCopyPill.test.tsx src/components/panels/coverage/coverageColumns.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behavior tests green
    - `grep -c "noopener noreferrer" packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx` ≥ 1
    - `grep -cE "clsx|cn\(|#[0-9a-fA-F]{3,6}" packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx` returns 0 (D-5.1-10)
    - COVERAGE_COL_WIDTHS regression test includes the understand key
  </acceptance_criteria>
  <done>Pill/link composite renders correctly for all 3 states with clipboard + toast behavior proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Understand cell in CoverageRow + desktop/mobile table sections</name>
  <files>packages/spa/src/components/panels/coverage/CoverageRow.tsx, packages/spa/src/components/panels/coverage/CoverageRow.test.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/CoverageRow.tsx (gitNexus cell composition ~lines 148-173 — the structural template; COVERAGE_COL_WIDTHS usage; prop threading)
    - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (colgroup + th anatomy, Tooltip-wrapped headers per D-11.2-05)
    - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx (mobile card cell anatomy)
  </read_first>
  <behavior>
    - Test 1: row with understand.state='fresh' + viewerUrl renders the viewer link inside the understand cell; tooltip/title on the cell contains lastAnalyzedAt when provided (D-14-08 tooltip)
    - Test 2: row with understand.state='stale' renders link + pill; state='missing' renders pill only
    - Test 3: row with `understand === undefined` (pre-Phase-14 daemon) renders an inert placeholder (em-dash or empty cell) without crashing
    - Test 4: desktop section renders the new <col> + <th> with the Tooltip-wrapped "Understand" header; column count assertions updated
    - Test 5: mobile section renders the understand cell in the card layout
  </behavior>
  <action>
    Thread a new optional prop `understandViewerUrl?: string` (the fully built per-repo URL) through CoverageFamilySection → CoverageRow, derived per-row by the parent (Task 3). In CoverageRow add a `<td className={COVERAGE_COL_WIDTHS.understand + ' px-2 py-2'}>` cell after workflowVersion and before actions, rendering UnderstandCopyPill when row.understand is defined with state fresh/stale/missing, wrapped (or titled) with lastAnalyzedAt tooltip text when present; render '—' for undefined or 'not-applicable'. In CoverageFamilySection add the matching `<col>` + Tooltip-wrapped `<th>` using coverageColumnTooltips.understand. Mirror in CoverageFamilySectionMobile with the existing card-row label/value pattern. Update every existing column-count/snapshot-adjacent test the new column breaks (expect CoverageFamilySection.test.tsx and CoveragePage.test.tsx assertions on header counts).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- --run src/components/panels/coverage/CoverageRow.test.tsx src/components/panels/coverage/CoverageFamilySection.test.tsx src/components/panels/coverage/CoverageFamilySectionMobile.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behavior tests green; pre-existing coverage suites green (no orphaned column-count assertions)
    - Undefined-understand row renders without throwing (Test 3 explicitly)
    - Header `<th>` for Understand wrapped in Tooltip with the SoT copy
  </acceptance_criteria>
  <done>Understand column live in desktop + mobile tables with all 3 states + back-compat placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CoveragePage viewer-URL construction from per-row token</name>
  <files>packages/spa/src/components/panels/coverage/CoveragePage.tsx, packages/spa/src/components/panels/coverage/CoveragePage.test.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/CoveragePage.tsx (how gitnexusInstalled/canScan health-derived props flow to sections today)
    - packages/spa/src/lib/pairing.ts (getPairing → agentUrl)
    - packages/spa/src/lib/healthQueries.ts (useHealth hook shape)
  </read_first>
  <behavior>
    - Test 1: for a row with understand.viewerToken='v1.abc.def' and repo id 'agenticapps/claude-workflow', the URL passed down is `{agentUrl}/understand/agenticapps/claude-workflow/?token=v1.abc.def` with the token URI-encoded via encodeURIComponent
    - Test 2: row without viewerToken (or undefined understand) gets `undefined` viewerUrl — no link rendered
    - Test 3: the main pairing token NEVER appears in any constructed viewer URL (assert constructed href does not contain the pairing token fixture value) — D-14-03
  </behavior>
  <action>
    In CoveragePage, build per-row viewer URLs: `buildViewerUrl(agentUrl, family, repo, viewerToken)` returning `${agentUrl}/understand/${family}/${repo}/?token=${encodeURIComponent(viewerToken)}` (repoId is `family/repo` — two path segments, matching the daemon route in plan 14-05). Pass down through CoverageFamilySection as the per-row `understandViewerUrl`. Source agentUrl from getPairing(); when unpaired or token absent, pass undefined. Keep the helper local to the coverage panel (single consumer family; the Code Intelligence page in 14-04 builds its own from the same row data — if the executor prefers, lift the helper to packages/spa/src/lib/understandViewerUrl.ts and import from both; that lift is approved but then 14-04's executor must import rather than redefine — coordinate via SUMMARY).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- --run src/components/panels/coverage/CoveragePage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Constructed URL exactly matches `^https?://.+/understand/[a-z0-9-]+/[a-z0-9][a-z0-9-_.]*/\?token=` in tests
    - Negative assertion: pairing.token fixture string absent from every rendered href
    - `pnpm --filter @agenticapps/dashboard-spa test` fully green and `pnpm --filter @agenticapps/dashboard-spa typecheck` green
  </acceptance_criteria>
  <done>Coverage rows link to the daemon-hosted viewer with the scoped token only; bearer token provably absent from URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → browser history/new tab | Viewer URLs (with scoped token) enter browser history when tabs open |
| clipboard | Copy pill writes a shell command the user will paste into a terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information Disclosure (token leakage in URLs) | viewer URL construction | mitigate (residual accepted per D-14-03) | Only the scoped read-only viewer token enters URLs; Task 3 Test 3 proves the bearer token never does; scoped token is repo-bound + read-only + rotates with bearer rotation |
| T-14-03-02 | Tampering | reverse-tabnabbing via viewer link | mitigate | rel="noopener noreferrer" on every viewer link (Task 1 acceptance grep) |
| T-14-03-03 | Tampering (command injection via clipboard) | UnderstandCopyPill | mitigate | Command string built ONLY by shared buildUnderstandCommand from daemon-validated family/repo row fields; family/repo originate from the daemon's allow-listed discovery, and the user reviews before pasting |
| T-14-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test` green (996+ existing + new)
- `pnpm --filter @agenticapps/dashboard-spa typecheck` green
- Manual (deferred to plan 14-08 checkpoint): column visible at /coverage against a live daemon
</verification>

<success_criteria>
- Understand column renders all 3 states on desktop + mobile, with link-on-stale preserved per D-14-10
- Copy pill copies the exact D-14-10 string with toast feedback
- Viewer URLs carry only the scoped token
</success_criteria>

<output>
Create `.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-03-SUMMARY.md` when done.
</output>
