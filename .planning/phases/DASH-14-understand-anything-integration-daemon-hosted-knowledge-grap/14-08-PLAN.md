---
phase: 14-understand-anything-integration-daemon-hosted-knowledge-grap
plan: 08
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05, 14-06, 14-07]
files_modified:
  - .planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-IMPECCABLE.md
autonomous: false
requirements: [D-14-01, D-14-02, D-14-03, D-14-04, D-14-05, D-14-06, D-14-07, D-14-08, D-14-09, D-14-10]

must_haves:
  truths:
    - "The live click-through works: install viewer via CLI, start daemon, open /coverage, click a fresh repo's Understand link, the knowledge-graph viewer renders the claude-workflow graph in a new tab including source preview (D-14-01/03/05/06/07)"
    - "14-IMPECCABLE.md exists with composite + per-heuristic scores for /coverage and /code-intelligence at 1440×900 (D-10.5-02 gate)"
    - "The post-phase ritual is executed: two-stage review + /cso audit covering the D-14-05 ratified exception and the scoped-token surface + /qa"
  artifacts:
    - path: ".planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-IMPECCABLE.md"
      provides: "Impeccable critique artifact for the two affected routes"
      min_lines: 40
  key_links:
    - from: "14-IMPECCABLE.md"
      to: "/coverage and /code-intelligence routes"
      via: "impeccable:critique skill at 1440×900"
      pattern: "composite"
---

<objective>
Close Phase 14: full-suite gate, live end-to-end verification of the viewer pipeline (the three Manual-Only items from 14-VALIDATION.md), the mandatory 14-IMPECCABLE.md artifact, and the post-phase review ritual with the /cso audit the D-14-05 ratified exception requires.

Purpose: Phase 14 is frontend-touching (IMPECCABLE gate) and adds a new auth surface + a ratified read-scope exception (/cso mandatory).
Output: green full suite, verified live viewer, 14-IMPECCABLE.md, ritual evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-CONTEXT.md
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-VALIDATION.md
@.planning/phases/DASH-10.5-impeccable-skill-driven-gate/10.5-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite phase gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - 14-01..14-07 SUMMARY files in this phase directory (what shipped, any deviations to verify against)
  </read_first>
  <action>
    Run the workspace gate: `pnpm -r test && pnpm -r typecheck && pnpm -r build`. Cross-check the 14-VALIDATION.md Per-Task Verification Map: every behavior family (D-14-03 token gate, D-14-05 traversal suite, D-14-05b sanitization, D-14-08 staleness, D-14-09 resolution, D-14-10 builder, D-14-06 sidebar/page, coverage column states) has a green automated test — name the covering test file per family in the SUMMARY. Known-pending exception: the 2 migration-0008 smoke failures documented in STATE.md are pre-existing and do not block (note them explicitly if present).
  </action>
  <verify>
    <automated>pnpm -r test && pnpm -r typecheck && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (modulo the 2 documented pre-existing migration-0008 smoke failures, called out by name if they appear)
    - Verification-map cross-check table written into the SUMMARY with one test-file citation per behavior family
  </acceptance_criteria>
  <done>All automated phase evidence green and mapped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end viewer verification (Manual-Only items)</name>
  <what-built>Daemon-hosted understand-anything viewer pipeline: CLI installer (14-07), viewer route + 6 data endpoints (14-05), coverage column + Code Intelligence page (14-03/14-04), status detection (14-06).</what-built>
  <how-to-verify>
    1. Build + install the viewer for real: `pnpm --filter @agenticapps/dashboard-agent build && node packages/agent/dist/cli.js install-understand-viewer` — expect success output and a populated `~/.agenticapps/dashboard/understand-viewer/2.7.6/` containing index.html (14-VALIDATION Manual item 2).
    2. Start the daemon (`agentic-dashboard start` or the dev equivalent) and the SPA dev server (`pnpm --filter @agenticapps/dashboard-spa dev`), open http://localhost:5174/coverage.
    3. Confirm the Understand column: claude-workflow row shows fresh ✓-link or stale link+pill depending on current HEAD vs its 2026-06-06 analysis; an unanalyzed repo shows the copy pill; click the pill and paste — clipboard must hold `cd ~/Sourcecode/agenticapps/claude-workflow && claude "/understand"` form for that row (D-14-10).
    4. Click the viewer link: a NEW TAB opens at `{agentUrl}/understand/agenticapps/claude-workflow/?token=v1.…` and the knowledge graph renders (graph canvas, 110 analyzed files) WITHOUT a token prompt; open a node's source preview to confirm file-content.json works (D-14-05, 14-VALIDATION Manual item 1).
    5. Negative check: edit the ?token= value in the URL bar and reload — the viewer's TokenGate must appear / data must 403; verify the URL never contains your main bearer token (D-14-03).
    6. Visit http://localhost:5174/code-intelligence — listing shows claude-workflow with metadata + viewer link; if the plugin cache gains a newer version than installed, the update hint names `agentic-dashboard install-understand-viewer` (D-14-02/06).
    7. OPTIONAL (infra-dependent, same gap as Phase 13 UAT Test 10): from a Tailscale device, open the viewer link against the tailscale bind — assets + data must serve (D-14-04). Skip with a note if no second device is at hand.
  </how-to-verify>
  <files>(none — live verification only)</files>
  <action>Prepare the environment for the user (build agent, run the real install-understand-viewer, start daemon + SPA dev server), then pause and walk the user through the 7 numbered steps above. Record outcomes per step in the SUMMARY, including the Tailscale skip-note if step 7 is skipped.</action>
  <verify>
    <automated>test -d "$HOME/.agenticapps/dashboard/understand-viewer" && ls "$HOME/.agenticapps/dashboard/understand-viewer"</automated>
    <human-check>User confirms steps 1-6 pass (step 7 optional with note)</human-check>
  </verify>
  <done>User typed "approved"; per-step outcomes recorded; any failures triaged into gap items before proceeding.</done>
  <resume-signal>Type "approved" or describe issues per step number</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 14-IMPECCABLE.md artifact</name>
  <files>.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-IMPECCABLE.md</files>
  <read_first>
    - .planning/phases/DASH-10.5-impeccable-skill-driven-gate/10.5-DECISIONS.md (artifact format, composite floor D-10.5-03 + calibration status — this is calibration data point per the open calibration-2 question)
    - .planning/phases/DASH-11-coverage-trends-skill-drift/11-IMPECCABLE.md or the 11.1 equivalent (artifact precedent shape)
  </read_first>
  <action>
    With the dev server from Task 2 still running, run the `impeccable:critique` skill against the two affected routes — `/coverage` (Understand column added) and `/code-intelligence` (new page) — at 1440×900. Record composite + per-heuristic scores, findings, and persona red flags into 14-IMPECCABLE.md following the established artifact format. Composite floor ≥ 87 per D-6-09.v1, calibration provisional per D-10.5-03 — if below floor, record the score, triage P1/P2 findings into follow-up candidates (Phase 11.1/11.2 precedent), and note the calibration-2 context rather than silently passing.
  </action>
  <verify>
    <automated>test -f .planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-IMPECCABLE.md && grep -ci "composite" .planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-IMPECCABLE.md</automated>
  </verify>
  <acceptance_criteria>
    - 14-IMPECCABLE.md contains composite scores for BOTH routes at 1440×900, per-heuristic breakdowns, and a findings section
    - Floor comparison stated explicitly with the D-10.5-03 calibration caveat
  </acceptance_criteria>
  <done>Frontend gate artifact committed (D-10.5-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Post-phase ritual — two-stage review, /cso, /qa</name>
  <what-built>The full Phase 14 diff across packages/shared, packages/agent, packages/spa.</what-built>
  <how-to-verify>
    Run the mandatory agenticapps-workflow post-phase ritual (do NOT collapse stages):
    1. Stage 1: `/review` on the phase diff.
    2. Stage 2: `superpowers:requesting-code-review`.
    3. `/cso` — MANDATORY (auth + API touched). Named audit targets: (a) the D-14-05 ratified exception — `file-content.json` serving graph-listed files beyond the .planning/.claude allow-list, all 12 guards + symlink containment; (b) the scoped viewer-token system (HMAC binding, rotation, 0600 secret); (c) the pre-bearerAuth mount order in app.ts; (d) D-14-04 all-bind-modes serving posture.
    4. `/qa` against the running dev server (coverage + code-intelligence + viewer routes).
    Record verdicts; any HIGH finding blocks merge per security_block_on: high.
  </how-to-verify>
  <files>(none — review ritual; findings may spawn fix-up commits)</files>
  <action>Execute the four ritual steps above in order (stages never collapse), capturing each verdict. For /cso, present the four named audit targets explicitly. Address or formally accept every finding; HIGH findings block merge per security_block_on: high.</action>
  <verify>
    <automated>git log --oneline -5</automated>
    <human-check>User confirms /review, stage-2 review, /cso (4 named targets), and /qa verdicts recorded with no unaddressed HIGH findings</human-check>
  </verify>
  <done>All four ritual steps executed with verdicts in the SUMMARY; zero unaddressed HIGH findings.</done>
  <resume-signal>Type "approved" with review/cso/qa verdicts, or describe blocking findings</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase diff → main | Merge gate; the ritual is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08-01 | All categories (residual) | entire phase surface | mitigate | /cso audit explicitly scoped to the four named targets in Task 4; two-stage review; block-on-high policy |
| T-14-08-SC | Tampering | npm installs | accept | No new packages introduced this phase (verified across 14-01..14-07) |
</threat_model>

<verification>
- Full workspace suite + typecheck + build green
- Live e2e approved by the user; 14-IMPECCABLE.md committed; ritual verdicts recorded in the SUMMARY
</verification>

<success_criteria>
- Phase goal demonstrably true end-to-end: link click → daemon-served viewer rendering the live claude-workflow graph; coverage status + copy pill verified; all gates and audits complete
</success_criteria>

<output>
Create `.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-08-SUMMARY.md` when done.
</output>
