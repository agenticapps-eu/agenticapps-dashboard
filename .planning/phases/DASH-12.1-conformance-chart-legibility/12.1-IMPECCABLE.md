---
phase: DASH-12.1-conformance-chart-legibility
artifact: IMPECCABLE
critique_date: 2026-06-10
route: /observability/conformance
viewport: 1440x900
composite_before: 80
composite_after: 84
nielsen_before: 27
nielsen_after: 28
cognitive_load_before: 2
cognitive_load_after: 0
floor: 80
verdict: PASS (above floor; both 12-IMPECCABLE P1s resolved)
requirement: IMPV-12.1-01
---

# 12.1-IMPECCABLE.md — Phase 12.1 (Conformance Chart Legibility)

Verification re-critique after fixing the two P1 chart-legibility findings from `12-IMPECCABLE.md`. Single-fix polish phase (req IMPV-12.1-01).

## Result

**Composite 80 → 84** (above the ratified ≥80 floor, D-10.5-03.calibration-2). Both P1s fully resolved, no regression introduced.

| Metric | Before (Phase 12) | After (Phase 12.1) | Δ |
|--------|-------------------|--------------------|---|
| Composite | 80 | **84** | +4 |
| Nielsen total | 27/40 | **28/40** | +1 |
| Cognitive load failures | 2/8 (moderate) | **0/8 (low)** | −2 |
| Deterministic scan | source-clean | source-clean | = |

## P1 findings — both RESOLVED

### P1 #1 — Persistent chart legend (was: family identity hover-only)
**RESOLVED.** A `<ul aria-label="Chart legend">` renders above the chart with all four series (agenticapps, factiv, neuroflash, fleet), each with a colored swatch. Swatch colors are an **exact computed-RGB match** to the line strokes (verified live):
- agenticapps `rgb(91,111,168)` (status-info), factiv `rgb(194,128,43)` (status-warning), neuroflash `rgb(107,70,193)` (accent), fleet `rgb(31,27,46)` (text-primary, the heavy 2.5px line).
Family→line identity is now resolvable without hovering. Legend sits in normal flow above the `role="img"` chart (accessible, not presentational); single row, no overflow at 1440px.

### P1 #2 — Threshold line labels (was: anonymous gridlines)
**RESOLVED.** The 70 and 90 dashed rules are labeled in-chart, right-anchored: **"70 floor"** and **"90 target"**, distinguishing them from the solid base gridlines (0/25/50/75/100). The conformance reference lines now self-explain.

## Nielsen movers

| # | Heuristic | Before | After | Why |
|---|-----------|--------|-------|-----|
| 1 | Visibility of status | 3 | 3→(+ context) | Thresholds named; per-day detail still hover-gated (held at 3). |
| 6 | Recognition rather than recall | 2 | **3** | Legend removes the hover-to-recall burden for line identity — the core lift. |
| 10 | Help & documentation | 2 | **3** | In-chart "floor"/"target" labels act as inline contextual help for the dashed rules. |

(H1 absorbed the threshold-labeling benefit; net Nielsen +1 to 28/40. H7 unchanged — the legend is static, not an interactive series filter.)

## Cognitive load — 2 → 0 failures

Both prior failures were the unlabeled multi-line chart (no line-identity key; anonymous threshold lines). Both resolved. Now 0/8 (low band).

## New issues introduced

None material. No legend overflow, no swatch/stroke color mismatch (verified identical RGB), no layout shift (chart `viewBox` unchanged), no console errors. The code change also closed code-review finding **IN-06** (dead `isThreshold` branch in the base-gridline loop removed) while staying under the ≤120 LOC budget (D-12-08; now 119).

## Remaining (non-blocking, future enhancement)

- **P3:** legend is non-interactive — a future enhancement could let it toggle/isolate a series, which would also move H7 (flexibility). Not a regression; out of scope for this P1 fix.

## Calibration note (D-10.5-03)

| Phase | Surface | Nielsen | Composite |
|-------|---------|---------|-----------|
| 12 | Conformance (as shipped) | 27/40 | 80 |
| **12.1** | **Conformance (legibility fix)** | **28/40** | **84** |

Confirms the 12-IMPECCABLE projection: fixing the two chart-legibility P1s lifts the surface from at-floor (80) into the 83–85 band, matching the trajectory Coverage surfaces followed after their P-bundles (11→11.1→11.2: 76→81→83).

---
*Method: focused verification re-critique (live browser inspection at 1440×900 + computed-color verification). Deterministic scan unchanged (source-clean; legend is a `<ul>` + swatches, threshold labels are right-anchored SVG `<text>` — no slop patterns). TDD: S17 (legend) + S18 (threshold labels) RED→GREEN; full spa suite 1207 green.*
