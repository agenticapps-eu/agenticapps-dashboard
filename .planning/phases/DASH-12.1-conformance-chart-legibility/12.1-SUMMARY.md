---
phase: DASH-12.1-conformance-chart-legibility
type: execute
completed: 2026-06-10
requirement: IMPV-12.1-01
tdd: true
key_files:
  modified:
    - packages/spa/src/components/panels/conformance/FleetTrendChart.tsx
    - packages/spa/src/components/panels/conformance/FleetTrendChart.test.tsx
  created:
    - .planning/phases/DASH-12.1-conformance-chart-legibility/12.1-IMPECCABLE.md
verdict: PASS
---

# 12.1 SUMMARY — Conformance chart legibility

Fixes the two P1 findings from `12-IMPECCABLE.md`. Single-file SPA change, TDD.

## What changed
- **Persistent legend** (`<ul aria-label="Chart legend">`) above the chart, outside the `role="img"` element: agenticapps/factiv/neuroflash/fleet → colored swatch (exact stroke-color parity). Family identity is now readable without hover.
- **Threshold labels**: the 70 (floor) / 90 (target) dashed rules are labeled in-chart, right-anchored.
- **Cleanup**: removed the dead `isThreshold` branch from the base-gridline loop (70/90 never in `[0,25,50,75,100]`) — closes code-review **IN-06** and keeps the file at 119 LOC (≤120 budget, D-12-08).

## TDD
- `test(12.1): RED` — S17 (legend) + S18 (threshold labels) fail against the unfixed chart.
- `feat(12.1): GREEN` — implementation; S17/S18 pass. Full spa suite **1207 green**; typecheck + eslint clean.

## Result
IMPECCABLE composite **80 → 84** (Nielsen 27→28/40; cognitive load 2→0). Above the ≥80 floor; both P1s resolved with no regression. See `12.1-IMPECCABLE.md`.

## Next
Phase 13 close-out (gate ritual), then Phase 14.1, then Phase 8.
