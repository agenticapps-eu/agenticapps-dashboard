---
phase: 06
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md
  - .planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png
  - .planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png
  - packages/spa/scripts/screenshot.mjs
autonomous: false
gap_closure: false
requirements:
  - POLISH-04
user_setup: []
must_haves:
  truths:
    - "Fresh impeccable composite score captured for / and /projects/:id at 1440x900 against the live Phase 5.1 sidebar shell (warm paper / aubergine / accent purple)"
    - "Baseline screenshots committed under refs/ for downstream visual diff and the closure ritual"
    - "Phase 5.1 HUMAN-UAT item AC-08 (after-shell.png re-capture) is satisfied with a real sidebar-shell screenshot at /projects/:id"
    - "The screenshot.mjs script can be parameterized by route + viewport so Plan 06 (CI gate) and Plan 07 (README) can reuse it without duplicating browser-launch code"
  artifacts:
    - path: ".planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md"
      provides: "Composite score + sub-scores + below-90 deltas for /, /projects/:id, /settings, /help, /onboarding, /pair at sm/md/lg breakpoints"
      contains: "## Composite scores"
    - path: ".planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png"
      provides: "Multi-project home reference at 1440x900 with sidebar-shell visible"
    - path: ".planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png"
      provides: "Single-project view reference at 1440x900 with 3-column grid + sidebar-shell visible"
    - path: ".planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png"
      provides: "Phase 5.1 HUMAN-UAT AC-08 closure — sidebar-shell capture at /projects/agenticapps-dashboard"
  key_links:
    - from: "packages/spa/scripts/screenshot.mjs"
      to: "Playwright chromium 1.59.1"
      via: "import { chromium } from 'playwright'"
      pattern: "playwright"
    - from: "06-01 baseline capture"
      to: "06-06 impeccable CI gate (Wave 2)"
      via: "deltas in baseline-impeccable.md drive the gate threshold + targeted polish"
      pattern: "baseline-impeccable.md"
---

<objective>
Capture fresh impeccable:critique baselines and screenshot references against the LIVE Phase 5.1 sidebar shell BEFORE any polish or gate work begins. RESEARCH key finding (Pitfall 3): the Phase 3 sub-scores (Color 76, Typography 78, Layout 84) were measured against the deleted dark-theme AppShell. Phase 5.1's warm-paper + aubergine + accent purple + Inter likely already lifted them past 90. Without a fresh baseline, the planner would either over-polish problems that no longer exist, or under-polish problems that emerged from the redesign.

Also closes Phase 5.1 HUMAN-UAT item AC-08 (replace `refs/after-shell.png` — currently captured against `/onboarding` instead of `/projects/:id`) by extending screenshot.mjs to accept route + viewport flags and writing the real sidebar-shell capture.

Purpose: Establish the empirical truth that Plan 06 (CI gate) enforces against, and unblock Plan 07/08 by parameterizing the screenshot script.

Output:
- `refs/baseline-impeccable.md` — composite + sub-scores per audited route per breakpoint, with below-90 deltas called out
- `refs/baseline-home.png` + `refs/baseline-project.png` at 1440x900
- `refs/after-shell.png` (in Phase 5.1 dir) re-captured against `/projects/agenticapps-dashboard`
- `packages/spa/scripts/screenshot.mjs` extended to accept `--route` and `--viewport WxH` (back-compat env-var fallback preserved)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-service-install-acceptance/06-CONTEXT.md
@.planning/phases/06-polish-service-install-acceptance/06-RESEARCH.md
@.planning/phases/06-polish-service-install-acceptance/06-VALIDATION.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-HUMAN-UAT.md
@packages/spa/scripts/screenshot.mjs
@packages/spa/src/components/AppShellV2.tsx

<interfaces>
<!-- Existing screenshot.mjs script — extend, don't replace -->

From packages/spa/scripts/screenshot.mjs:
```javascript
import { chromium } from 'playwright'
const URL = process.env.SCREENSHOT_URL || 'http://localhost:5174/projects/agenticapps-dashboard'
const OUT = process.env.SCREENSHOT_OUT || '.planning/phases/DASH-05.1-.../refs/after-shell.png'
const browser = await chromium.launch()
const ctx = await browser.newContext({ viewport: { width: 1440, height: 900 } })
const page = await ctx.newPage()
await page.goto(URL, { waitUntil: 'networkidle' })
await page.screenshot({ path: OUT, fullPage: false })
await browser.close()
```

Phase 5.1 design tokens that should now drive the score (UI-SPEC §2):
- `--bg-app: #FAFAF7` warm paper (NOT pure white, NOT dark)
- `--text-primary: #1F1B2E` deep aubergine (NOT pure black)
- `--accent: #6B46C1` purple (NOT Tailwind blue-500)
- `--font-sans: 'Inter'` as PRIMARY, not fallback
- `--shadow-card: 0 4px 12px rgba(0,0,0,0.04)` subtle, no glassmorphism
- 12px card radius, 24px outer padding, 60px top bar, 240px sidebar

Routes audited per D-6-10:
- `/onboarding` (standalone shell — no AppShellV2)
- `/` (MultiProjectHome under AppShellV2)
- `/projects/:id` (SingleProjectView under AppShellV2)
- `/settings` (under AppShellV2)
- `/help` (under AppShellV2)
- `/pair?agent=...&token=...` (validation success path; standalone shell)

Breakpoints per D-6-10 (Claude's Discretion in CONTEXT.md):
- sm: 390x844 (mobile)
- md: 768x1024 (tablet)
- lg: 1440x900 (desktop) — primary target per UI-SPEC AC-06
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend screenshot.mjs with --route and --viewport flags + named-export browser launch</name>
  <files>packages/spa/scripts/screenshot.mjs, packages/spa/scripts/screenshot.test.mjs</files>
  <read_first>
    - packages/spa/scripts/screenshot.mjs (current implementation — preserve env-var fallback for back-compat with Phase 5.1 invocations)
    - packages/spa/package.json (verify Playwright is reachable via the workspace; it currently is NOT a devDependency — Plan 06 adds it formally; for now invoke via `npx --package=playwright -- node ...` as the existing script docstring shows)
  </read_first>
  <action>
    Rewrite `packages/spa/scripts/screenshot.mjs` to:

    1. Parse argv flags `--route <path>` (default `/projects/agenticapps-dashboard`), `--viewport <WxH>` (default `1440x900`), `--out <path>` (default `.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png`), `--base-url <url>` (default `http://localhost:5174`). Use a tiny manual parser (no minimist/yargs — INV-05 spirit applies to scripts too).
    2. Preserve the existing env vars `SCREENSHOT_URL`, `SCREENSHOT_OUT` as fallback overrides (env wins over flag default; flag wins over env).
    3. Export a named function `captureScreenshot({ route, viewport, out, baseUrl })` that returns the path written. Keep the top-level run gated on `import.meta.url === \`file://\${process.argv[1]}\`` so importing from the test does not auto-launch chromium.
    4. Keep `await page.goto(url, { waitUntil: 'networkidle' })` and `fullPage: false` (D-6-16 mandates real reproducible captures — networkidle prevents capturing during a 5s polling refetch flicker).
    5. Add a `console.error` and `process.exit(2)` if route does not start with `/`, to fail-fast on bad invocations from CI.

    Concrete viewport parsing:
    ```javascript
    function parseViewport(s) {
      const m = /^(\d+)x(\d+)$/.exec(s)
      if (!m) throw new Error(`Invalid --viewport: ${s} (expected WxH e.g. 1440x900)`)
      return { width: Number(m[1]), height: Number(m[2]) }
    }
    ```

    Then create `packages/spa/scripts/screenshot.test.mjs` (vitest) that:
    - Imports `parseViewport` (also export it) and asserts `parseViewport('1440x900')` returns `{width: 1440, height: 900}`.
    - Asserts `parseViewport('bad')` throws.
    - Imports `captureScreenshot` (do NOT call it — just verify the export exists, since calling it requires chromium which CI has not yet installed at this wave).

    This task does NOT add Playwright as a formal devDependency — Plan 06 does that. The script remains invocable via `npx --package=playwright -- node packages/spa/scripts/screenshot.mjs ...` as the existing docstring already documents.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa exec vitest run scripts/screenshot.test.mjs</automated>
    Manual: `node packages/spa/scripts/screenshot.mjs --route /onboarding --viewport 768x1024 --out /tmp/test.png` (with dev server running) writes a 768x1024 PNG.
  </verify>
  <acceptance_criteria>
    - `grep -c "parseViewport\|captureScreenshot\|--route\|--viewport" packages/spa/scripts/screenshot.mjs` returns >= 4
    - `grep "import.meta.url" packages/spa/scripts/screenshot.mjs` matches (top-level run is gated)
    - `grep "process.env.SCREENSHOT_URL\|process.env.SCREENSHOT_OUT" packages/spa/scripts/screenshot.mjs` matches BOTH (env-var fallback preserved)
    - `pnpm --filter @agenticapps/dashboard-spa exec vitest run scripts/screenshot.test.mjs` exits 0 with at least 3 passing assertions
    - File `packages/spa/scripts/screenshot.mjs` does NOT import minimist, yargs, commander, or any other CLI parser (manual parsing only)
  </acceptance_criteria>
  <done>screenshot.mjs accepts --route/--viewport/--out/--base-url with env-var fallback; exports captureScreenshot + parseViewport; vitest covers parseViewport + export shape; manual smoke against the dev server writes a PNG at the requested viewport.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture fresh impeccable baselines + reference screenshots (HUMAN — requires live dev server + agent)</name>
  <files>
    .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md,
    .planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png,
    .planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png,
    .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png
  </files>
  <read_first>
    - .planning/phases/06-polish-service-install-acceptance/06-RESEARCH.md (Pitfall 3 — Phase 3 sub-scores are stale, fresh baseline is mandatory)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md (the design tokens that drive the new scores)
    - ~/.claude/skills/impeccable/SKILL.md (impeccable critique CLI flags + JSON output shape)
  </read_first>
  <action>See <how-to-verify> below — this is a human-driven multi-step capture process. Run the impeccable scans + screenshot captures + author the baseline-impeccable.md, then commit all four artifacts.</action>
  <verify><automated>test -f .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md && test -f .planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png && test -f .planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png</automated></verify>
  <done>baseline-impeccable.md filled with real scores; all three PNGs committed; after-shell.png re-captured against /projects/:id; D-6-19 verdict recorded (PASS or REMAINING DELTA).</done>
  <what-built>
    Extended screenshot.mjs (Task 1) is ready. The fresh baseline must be captured against the LIVE running stack — not a mock — because the impeccable scoring depends on real DOM rendered by the live SPA pulling real data from the live agent.
  </what-built>
  <how-to-verify>
    Pre-flight (in three terminals):
    1. Build shared types: `pnpm --filter @agenticapps/dashboard-shared build`
    2. Terminal A — start agent:
       `pnpm --filter @agenticapps/dashboard-agent build && node packages/agent/dist/cli.js start`
       Confirm `127.0.0.1:5193` is reachable: `curl -s http://127.0.0.1:5193/health`
    3. Terminal B — start SPA:
       `pnpm --filter @agenticapps/dashboard-spa dev`
       Confirm `http://localhost:5174` returns the SPA shell.
    4. Pair the SPA: open the printed pair URL in Chrome, confirm `/` renders the multi-project home with at least one registered project.

    Capture step (in Terminal C — repeat for each entry below):

    For impeccable scoring per D-6-10:
    ```
    npx impeccable critique --route http://localhost:5174/ --viewport 1440x900 --json > /tmp/imp-home-lg.json
    npx impeccable critique --route http://localhost:5174/ --viewport 768x1024 --json > /tmp/imp-home-md.json
    npx impeccable critique --route http://localhost:5174/ --viewport 390x844 --json > /tmp/imp-home-sm.json
    npx impeccable critique --route http://localhost:5174/projects/agenticapps-dashboard --viewport 1440x900 --json > /tmp/imp-project-lg.json
    npx impeccable critique --route http://localhost:5174/settings --viewport 1440x900 --json > /tmp/imp-settings-lg.json
    npx impeccable critique --route http://localhost:5174/help --viewport 1440x900 --json > /tmp/imp-help-lg.json
    npx impeccable critique --route http://localhost:5174/onboarding --viewport 1440x900 --json > /tmp/imp-onboarding-lg.json
    npx impeccable critique --route 'http://localhost:5174/pair?agent=http%3A%2F%2F127.0.0.1%3A5193&token=fake-but-format-valid-71-char-token-XX-YY' --viewport 1440x900 --json > /tmp/imp-pair-lg.json
    ```
    (If `npx impeccable` does NOT take `--route` directly, follow the actual CLI per `~/.claude/skills/impeccable/SKILL.md` — the goal is one JSON per route per breakpoint, with composite + sub-scores.)

    For reference screenshots:
    ```
    node packages/spa/scripts/screenshot.mjs --route / --viewport 1440x900 --out .planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png
    node packages/spa/scripts/screenshot.mjs --route /projects/agenticapps-dashboard --viewport 1440x900 --out .planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png
    node packages/spa/scripts/screenshot.mjs --route /projects/agenticapps-dashboard --viewport 1440x900 --out .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png
    ```

    Then write `.planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` with this template:

    ```markdown
    # Phase 6 — Impeccable Baseline (captured 2026-05-10)

    **Captured against:** Phase 5.1 sidebar shell (warm paper / aubergine / accent purple / Inter)
    **Live stack:** agent on 127.0.0.1:5193, SPA on localhost:5174, paired with project `agenticapps-dashboard`

    ## Composite scores

    | Route | Breakpoint | Composite | Color | Typography | Layout | Spacing | Hierarchy | Accessibility | Pass (>= 90)? |
    |-------|-----------|-----------|-------|------------|--------|---------|-----------|---------------|--------------|
    | / | 1440x900 | XX | XX | XX | XX | XX | XX | XX | yes/no |
    | / | 768x1024 | ... | ... | ... | ... | ... | ... | ... | ... |
    | / | 390x844 | ... | ... | ... | ... | ... | ... | ... | ... |
    | /projects/:id | 1440x900 | ... | ... | ... | ... | ... | ... | ... | ... |
    | /settings | 1440x900 | ... | ... | ... | ... | ... | ... | ... | ... |
    | /help | 1440x900 | ... | ... | ... | ... | ... | ... | ... | ... |
    | /onboarding | 1440x900 | ... | ... | ... | ... | ... | ... | ... | ... |
    | /pair | 1440x900 | ... | ... | ... | ... | ... | ... | ... | ... |

    ## Below-90 deltas (the polish backlog for Plan 06)

    For each cell where Pass = no, list:
    - Route + breakpoint + sub-score
    - Findings text from the JSON (verbatim)
    - File:line evidence (e.g., `packages/spa/src/components/HomeToolbar.tsx:101`)
    - Proposed fix (one line)

    Example:
    - `/` 1440x900 Color 84 — "purple-tinted hover bg #F2EBFA on white card has insufficient contrast for 13px helper text" — `packages/spa/src/components/ui/Card.tsx:NN` — switch helper text on hover from `--text-tertiary` to `--text-secondary`.

    ## D-6-19 carry-forward verification

    Phase 3 baseline (against deleted dark AppShell, captured 2026-05-05):
    - Color 76, Typography 78, Layout 84 on `/`

    Fresh baseline (against Phase 5.1 sidebar shell):
    - Color XX, Typography XX, Layout XX on `/`

    Verdict: [PASS — Phase 5.1 lifted these past 90, no carry-forward polish needed] OR [REMAINING DELTA — see Below-90 table]
    ```

    Commit all four artifacts (3 PNGs + 1 markdown) plus the extended screenshot.mjs from Task 1.
  </how-to-verify>
  <resume-signal>Type "approved" once `refs/baseline-impeccable.md` is filled in with real numbers, all three PNGs are committed, and the after-shell.png is the new sidebar capture (not the old onboarding shot).</resume-signal>
  <acceptance_criteria>
    - `ls .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` returns the file
    - `ls .planning/phases/06-polish-service-install-acceptance/refs/baseline-home.png` returns the file
    - `ls .planning/phases/06-polish-service-install-acceptance/refs/baseline-project.png` returns the file
    - `ls .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png` returns a file with mtime > 2026-05-10T00:45:00Z (re-captured, not the old onboarding shot)
    - `grep -c "PASS\|REMAINING DELTA" .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` returns >= 1
    - `grep -c "Composite" .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` returns >= 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server -> headless browser | Local-only; baseline capture runs against `localhost:5174` over loopback only |
| screenshot.mjs argv -> playwright launch | User-controlled flags; route prefix-validated to `/`; viewport regex-validated as WxH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | screenshot.mjs --route flag | mitigate | Reject any route value that does not start with `/`; exit code 2 with clear message (prevents `--route http://attacker/exfil` capturing arbitrary URLs) |
| T-06-01-02 | Information Disclosure | baseline PNGs committed to repo | accept | Repo is private through Phase 6 (PROJECT.md). Screenshots show registered project names + phase data which are non-secret. Same disclosure surface as the Phase 5.1 after-shell.png already committed |
| T-06-01-03 | Denial of Service | playwright spawn left running | mitigate | `await browser.close()` is in a finally-equivalent path; subprocess test (Task 1) does not call captureScreenshot to avoid leaking processes in CI |
</threat_model>

<verification>
1. `pnpm --filter @agenticapps/dashboard-spa exec vitest run scripts/screenshot.test.mjs` exits 0
2. `ls .planning/phases/06-polish-service-install-acceptance/refs/baseline-{home,project}.png` shows both files exist
3. `ls .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/refs/after-shell.png` shows the file exists and is a NEW capture (file mtime > Phase 5.1 close commit timestamp 2026-05-10T00:45:00Z)
4. `wc -l .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` returns >= 30 (the table is filled in, not stub)
5. `grep -c "PASS\|REMAINING DELTA" .planning/phases/06-polish-service-install-acceptance/refs/baseline-impeccable.md` returns >= 1 (D-6-19 verdict recorded)
</verification>

<success_criteria>
- screenshot.mjs is parameterized (route + viewport + out + base-url) with env-var fallback preserved
- baseline-impeccable.md has a filled-in scores table for all 6 routes (sm/md/lg where applicable per D-6-10) with below-90 deltas enumerated as the polish backlog Plan 06 will close
- Phase 5.1 HUMAN-UAT AC-08 closed: after-shell.png is now a real sidebar-shell capture at /projects/:id
- baseline-home.png + baseline-project.png exist at 1440x900 for the closure ritual visual diff
- D-6-19 verdict recorded (carry-forward Color/Typography/Layout sub-scores either confirmed PASS by Phase 5.1 redesign, or enumerated as remaining deltas for Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-service-install-acceptance/06-01-SUMMARY.md` documenting:
- Fresh composite scores per route per breakpoint
- D-6-19 verdict (carry-forward closed by Phase 5.1, or remaining work for Plan 06)
- Whether Plan 06's gate threshold (>= 90) is currently met across all audited routes
- Any unexpected regressions discovered (e.g., a route that scored lower than Phase 3)
</output>
