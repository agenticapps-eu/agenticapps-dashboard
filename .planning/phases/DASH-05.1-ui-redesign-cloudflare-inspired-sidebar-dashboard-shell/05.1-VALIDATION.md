---
phase: 05.1
slug: ui-redesign-cloudflare-inspired-sidebar-dashboard-shell
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-09
revised: 2026-05-09
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from RESEARCH.md §"Validation Architecture" + each plan's `<task>` `<automated>` blocks.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.5 + @testing-library/react 16.3.2 + jsdom 29.1.1 |
| **Config file** | `packages/spa/vitest.config.ts` (existing — Phase 0) |
| **Quick run command** | `pnpm --filter @agenticapps/dashboard-spa test --run <path>` |
| **Full suite command** | `pnpm -r test` |
| **Estimated runtime** | quick: ~3–10 s · full: ~45–60 s (1117-test baseline + new V2 tests) |
| **Workspace package** | `packages/spa` (the SPA is the only package this phase touches) |

---

## Sampling Rate

- **After every task commit:** Run the task's `<automated>` command (typically `pnpm --filter @agenticapps/dashboard-spa test --run <focused-path>`).
- **After every plan wave:** Run `pnpm -r typecheck && pnpm -r test && pnpm lint` (full workspace, ~60 s — same as the existing pre-PR check).
- **Before `/gsd-verify-work`:** Full suite must be green.
- **Phase gate (Plan 06):** Full suite green + `/impeccable:critique` ≥ 90 manual run + `refs/after-shell.png` committed.
- **Max feedback latency:** ≤ 60 s for full workspace, ≤ 10 s for focused task run.

---

## Per-Task Verification Map

> Mirrors each plan's `<task>` `<automated>` block. Status starts as ⬜ pending; flips to ✅ green when the task closes.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-01-01 | 01 | 1 | AC-04 (no-orange invariant) + AC-05 prep | T-05.1-01-02 (supply chain) · T-05.1-01-03 (font fallback) | Inter font loads as first import; tokens.css is sole locked-palette source; legacy `.dark` block preserved (OQ-5) | unit + smoke | `pnpm --filter @agenticapps/dashboard-spa test --run src/styles/noOrange.test.ts` | ❌ W0 (created by this task) | ⬜ pending |
| 05.1-01-02 | 01 | 1 | AC-07 (UI primitives have tests) | T-05.1-01-01 (token info-disclosure — N/A: public values) | 7 primitives ship with vitest tests; no `cn()` / `clsx` / CVA / transitions | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/ui/` | ❌ W0 (created by this task) | ⬜ pending |
| 05.1-01-03 | 01 | 1 | AC-05 (tokens = single source of truth) | — | Hex literals only allowed inside tokens.css; production component files are hex-free | unit (invariant scan) | `pnpm --filter @agenticapps/dashboard-spa test --run src/styles/tokenSourceOfTruth.test.ts` | ❌ W0 (created by this task — added in revision) | ⬜ pending |
| 05.1-02-01 | 02 | 2 | AC-01 (paired routes under AppShellV2) + UI-SPEC §5/§6/§7 | T-05.1-02-02 (route-shape regression) | Sidebar/TopBar/Breadcrumb/PageHeader render with correct geometry; TopBar tags + status pill render only on `/projects/:id` (Warning 3 fix); SidebarItemDisabled covers OBSERVE placeholders (Info 8 fix) | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/ui/` | ❌ W1 | ⬜ pending |
| 05.1-02-02 | 02 | 2 | AC-01 / AC-02 (shell-on / shell-off route discrimination) + Pitfall 4 | T-05.1-02-01 (env-flag bleed) · T-05.1-02-03 (RepairBanner placement) · T-05.1-02-04 (Pitfall 4 strict-equality) | `VITE_APPSHELL_V2 === '1'` selects V2 tree; any other value selects legacy; onboarding/pair correctly skip the shell | integration | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/AppShellV2.test.tsx src/router.test.tsx` | ❌ W1 | ⬜ pending |
| 05.1-03-01 | 03 | 3 | UI-SPEC §1 (multi-project home in V2) | T-05.1-03-01 (visual-regression mitigation) | MultiProjectHome wraps content in PageHeader on V2; legacy mode unchanged; empty-state repaletted | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/MultiProjectHome.test.tsx` | ✅ existing (assertions extended) | ⬜ pending |
| 05.1-03-02 | 03 | 3 | UI-SPEC §8 (Card chrome) + AC-05 prep | T-05.1-03-02 (no data-shape change) | HomeToolbar / ProjectCard / RegisterButtonCard repaletted to namespaced tokens; pre-flight grep confirms zero legacy aliases in these 3 files | unit (existing tests preserve behavior) | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/HomeToolbar.test.tsx src/components/ProjectCard.test.tsx src/components/RegisterButtonCard.test.tsx` | ✅ existing (RegisterButtonCard.test.tsx pre-exists — verified pre-revision) | ⬜ pending |
| 05.1-04-01 | 04 | 4 | UI-SPEC §8 (PanelContainer chrome) + Discipline column | T-05.1-04-03 (InlineDrift cascade) | PanelContainer + InlineDrift + 3 Discipline panels repaletted; no legacy aliases in 5 files | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/PanelContainer.test.tsx src/components/panels/InlineDrift.test.tsx src/components/panels/CommitmentBlock.test.tsx src/components/panels/HookFirings.test.tsx src/components/panels/RationalizationFires.test.tsx` | ✅ existing | ⬜ pending |
| 05.1-04-02 | 04 | 4 | Phase Progress column | — | 5 Phase Progress panels repaletted | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/PhaseProgress.test.tsx src/components/panels/ExecutionTimeline.test.tsx src/components/panels/ReviewStatus.test.tsx src/components/panels/SecurityStatus.test.tsx src/components/panels/VerificationStatus.test.tsx` | ✅ existing | ⬜ pending |
| 05.1-04-03 | 04 | 4 | Skills+Health column + UI-SPEC §1 (3-col grid + Pitfall 8 gap-6) | T-05.1-04-01 (D-5-20 verbatim copy preserved) · T-05.1-04-02 (SkillHealth 8-state) | 5 Skills+Health panels repaletted; SkillHealth's 8 states verified; SingleProjectView wraps in PageHeader on V2; column inner gap normalized to gap-6 | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/InstalledSkills.test.tsx src/components/panels/SkillHealth.test.tsx src/components/panels/ObservabilityHealth.test.tsx src/components/panels/SecretsHealth.test.tsx src/components/panels/IntegrationsHealth.test.tsx src/components/SingleProjectView.test.tsx src/components/ProjectHeader.test.tsx` | ✅ existing | ⬜ pending |
| 05.1-05-01 | 05 | 5 | /settings + /help in V2 | T-05.1-05-01 (ManualPairForm 8-state) | Settings + Help wrap content in PageHeader on V2; ManualPairForm / ThemeToggle / ThemeChip / CodeBlock alias-free | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/ManualPairForm.test.tsx src/components/ThemeToggle.test.tsx src/components/ThemeChip.test.tsx src/components/CodeBlock.test.tsx` | ✅ existing | ⬜ pending |
| 05.1-05-02 | 05 | 5 | Workspace alias-free precondition for Wave 5 | T-05.1-05-02 (RegisterModal field rendering) · T-05.1-05-03 (RepairBanner animation removal — D-5.1-10) | 8 shell-adjacent components alias-free; RepairBanner motion-safe animation removed; workspace-wide grep returns zero `[--*]` matches | unit + global-grep audit | `pnpm --filter @agenticapps/dashboard-spa test --run src/components/RepairBanner.test.tsx src/components/CommandPalette.test.tsx src/components/SchemaDriftState.test.tsx src/components/DaemonUnreachableState.test.tsx src/components/CardContextMenu.test.tsx src/components/RegisterModal.test.tsx src/components/RenameTagsForms.test.tsx src/components/Header.test.tsx` | ✅ existing | ⬜ pending |
| 05.1-06-01 | 06 | 6 | Flag flip + V2-default conversion | T-05.1-06-01 (deleted-import sanity) | router uses single AppShellV2 tree; conditional V2 gates collapsed; `.env.example` deleted/stub | unit | `pnpm --filter @agenticapps/dashboard-spa test --run src/router.test.tsx src/components/MultiProjectHome.test.tsx src/components/SingleProjectView.test.tsx src/routes/projects.$projectId.test.tsx` | ✅ existing (assertions revised) | ⬜ pending |
| 05.1-06-02 | 06 | 6 | Legacy shell deletion + alias `:root` + `.dark` block deletion + AC-05 still green | T-05.1-06-01 (importer audit) · T-05.1-06-03 (intentional dark-mode regression — RESEARCH OQ-5) | AppShell/Header/HomeLayout/ProjectLayout/appShellWidth/ProjectHeader deleted; alias `:root` + `.dark` block deleted; `tokenSourceOfTruth.test.ts` still green; `noOrange.test.ts` allow-list relaxed (no `#d97706` anywhere) | unit + global-grep audit | `pnpm -r test && pnpm -r typecheck && pnpm lint && pnpm --filter @agenticapps/dashboard-spa test --run src/styles/tokenSourceOfTruth.test.ts src/styles/noOrange.test.ts` | ✅/❌ mix (12 deletions + global.css edit) | ⬜ pending |
| 05.1-06-03 | 06 | 6 | AC-06 preview gate + AC-08 screenshot | T-05.1-06-02 (screenshot data sensitivity) | `refs/after-shell.png` committed; both impeccable:critique scores documented in 05.1-06-SUMMARY.md (Phase ships even if score < 90 — Phase 6 enforces hard gate per Info 9 fix) | manual / smoke | manual: `npx --package=playwright -- node packages/spa/scripts/screenshot.mjs` + `/impeccable:critique http://localhost:5174/ 1440x900` + `/impeccable:critique http://localhost:5174/projects/agenticapps-dashboard 1440x900` | ❌ W5 (created in this task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Validation Architecture (copied from RESEARCH §"Validation Architecture")

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| AC-01 | Paired routes render under AppShellV2 | integration | `pnpm --filter @agenticapps/dashboard-spa test --run AppShellV2.test.tsx` | ❌ Wave 1 (Plan 02) |
| AC-02 | `/onboarding` and `/pair` skip AppShellV2 | integration | same suite, asserts no `[data-testid="appshell-v2"]` on those routes | ❌ Wave 1 (Plan 02) |
| AC-03 | Existing 1117-test suite still passes | regression | `pnpm -r test` | ✅ existing |
| AC-04 | No `#FFA500` / no `orange` literal in any `.tsx`/`.css` under `packages/spa/src` | unit | `pnpm --filter @agenticapps/dashboard-spa test --run noOrange.test.ts` | ❌ Wave 0 (Plan 01 Task 1) |
| AC-05 | No hardcoded hex (`/#[0-9a-fA-F]{3,8}\b/`) in any `.tsx`/`.ts` under `packages/spa/src/components/` | unit | `pnpm --filter @agenticapps/dashboard-spa test --run tokenSourceOfTruth.test.ts` | ❌ Wave 0 (Plan 01 Task 3 — added in revision to close Blocker 1) |
| AC-06 | `impeccable:critique` ≥ 90 on `/` and `/projects/:id` (PREVIEW gate per Info 9 fix; Phase 6 POLISH-04 enforces hard gate) | manual | `/impeccable:critique http://localhost:5174/...` | manual-only |
| AC-07 | Each new UI primitive has component test | unit | `pnpm --filter @agenticapps/dashboard-spa test --run components/ui/` | ❌ Wave 0 (Plan 01 Task 2) |
| AC-08 | Screenshot lands in `refs/after-shell.png` | smoke | `node packages/spa/scripts/screenshot.mjs` | ❌ Wave 5 (Plan 06 Task 3) |

### Sampling Rate

- **Per task commit:** `pnpm --filter @agenticapps/dashboard-spa test` (SPA-only, ~10 s).
- **Per wave merge:** `pnpm -r typecheck && pnpm -r test && pnpm lint` (full workspace, ~60 s).
- **Phase gate:** Full suite green + `/impeccable:critique` scores documented in 05.1-06-SUMMARY.md + `refs/after-shell.png` committed.

---

## Wave 0 Requirements (Plan 01)

These files do NOT exist before Wave 1. Plan 01 Tasks 1–3 create them.

- [ ] `packages/spa/src/styles/tokens.css` (NEW — Plan 01 Task 1)
- [ ] `packages/spa/src/styles/noOrange.test.ts` (NEW — Plan 01 Task 1; AC-04 invariant)
- [ ] `packages/spa/src/styles/tokenSourceOfTruth.test.ts` (NEW — Plan 01 Task 3; AC-05 invariant — added in revision)
- [ ] `packages/spa/src/components/ui/Card.tsx` + `Card.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/CardHeader.tsx` + `CardHeader.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/Pill.tsx` + `Pill.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/StatusPill.tsx` + `StatusPill.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/EmptyState.tsx` + `EmptyState.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/MetricNumeric.tsx` + `MetricNumeric.test.tsx` (Plan 01 Task 2)
- [ ] `packages/spa/src/components/ui/KbdHint.tsx` + `KbdHint.test.tsx` (Plan 01 Task 2)

*Framework install: NONE — vitest already in place from Phase 0.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Anti-AI-slop visual checklist (no transitions on route entry, no skeleton-shimmer, no emoji as iconography, tabular-nums on metrics, sidebar 240px, TopBar 60px, card gap 24px, no border on cards, focus rings purple) | UI-SPEC §13 + D-5.1-10 | Visual judgment requires human eye | Plan 06 Task 3 `<how-to-verify>` step 3 |
| `/impeccable:critique` score ≥ 90 (PREVIEW) | AC-06 | Critique skill is invoked from a Claude Code chat, not CI; LLM-driven evaluation | Plan 06 Task 3 — invoke twice; document outputs in 05.1-06-SUMMARY.md regardless of pass/fail. Phase ships at any score because Phase 6 POLISH-04 enforces the hard gate. |
| Visual smoke screenshot at `refs/after-shell.png` | AC-08 | Playwright capture against live dev server | Plan 06 Task 3 — `npx --package=playwright -- node packages/spa/scripts/screenshot.mjs` |

---

## Wave 0 Gaps

Files / tests that MUST exist before Wave 1 starts (i.e., before any plan with `wave: 2+` runs):

- [ ] `packages/spa/src/styles/tokens.css` (created by Plan 01 Task 1)
- [ ] `packages/spa/src/styles/noOrange.test.ts` (created by Plan 01 Task 1)
- [ ] `packages/spa/src/styles/tokenSourceOfTruth.test.ts` (created by Plan 01 Task 3 — revision blocker fix)
- [ ] All 7 UI primitives + their `.test.tsx` siblings (created by Plan 01 Task 2)
- [ ] `@fontsource-variable/inter` in `pnpm-workspace.yaml` catalog + `packages/spa/package.json` (Plan 01 Task 1)

After Plan 01 closes, set `wave_0_complete: true` in this file's frontmatter.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (verified per-row above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has a focused vitest command)
- [x] Wave 0 covers all MISSING references (token files + 7 primitives + 2 invariant tests)
- [x] No watch-mode flags (every command uses `--run` for CI determinism)
- [x] Feedback latency < 60 s for full workspace; < 10 s for focused task runs
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-09 (planner) — pending checker re-review after revision.
