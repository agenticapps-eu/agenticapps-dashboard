---
phase: 05.1
plan: 04
type: execute
wave: 4
depends_on: [05.1-03]
files_modified:
  - packages/spa/src/components/SingleProjectView.tsx
  - packages/spa/src/components/SingleProjectView.test.tsx
  - packages/spa/src/components/ProjectHeader.tsx
  - packages/spa/src/components/panels/PanelContainer.tsx
  - packages/spa/src/components/panels/PanelContainer.test.tsx
  - packages/spa/src/components/panels/InlineDrift.tsx
  - packages/spa/src/components/panels/CommitmentBlock.tsx
  - packages/spa/src/components/panels/HookFirings.tsx
  - packages/spa/src/components/panels/RationalizationFires.tsx
  - packages/spa/src/components/panels/PhaseProgress.tsx
  - packages/spa/src/components/panels/ExecutionTimeline.tsx
  - packages/spa/src/components/panels/ReviewStatus.tsx
  - packages/spa/src/components/panels/SecurityStatus.tsx
  - packages/spa/src/components/panels/VerificationStatus.tsx
  - packages/spa/src/components/panels/InstalledSkills.tsx
  - packages/spa/src/components/panels/SkillHealth.tsx
  - packages/spa/src/components/panels/ObservabilityHealth.tsx
  - packages/spa/src/components/panels/SecretsHealth.tsx
  - packages/spa/src/components/panels/IntegrationsHealth.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 13 production panels (3 Discipline + 5 Phase Progress + 5 Skills+Health) render under the new shell with bg-card-bg + shadow-card + rounded-card chrome"
    - "PanelContainer.tsx is REPALETTED IN PLACE to use Tailwind 4 namespaced tokens (bg-card-bg, border-border-subtle, text-text-primary, etc.); its API and a11y contract (panelId, title, stale, unreachable, aria-labelledby={`${panelId}-title`}) UNCHANGED"
    - "SkillHealth.tsx (252 LOC, 8 states) — ALL EIGHT states render correctly post-migration: schema_drift, isLoading, error/no-data unreachable, kind:not-installed, kind:timeout, kind:error, kind:unparseable, kind:ok with row-expand"
    - "InlineDrift.tsx (used by 13 panels for schema-drift state) repaletted to bg-card-bg + text-status-error + border-border-subtle"
    - "SingleProjectView.tsx wraps content in PageHeader (V2-mode only); ProjectHeader breadcrumb is REPLACED by Wave 1's Breadcrumb in the TopBar — ProjectHeader becomes vestigial in V2 mode (rendered only in legacy mode for back-compat); 3-column grid stays exactly as-is (1fr 1.5fr 1fr per Phase 5 D-5-01); column gap stays gap-6 (24px); inner-column gap-4 → gap-6 (RESEARCH Pitfall 8)"
    - "Existing 13 panel test files pass UNCHANGED — assertions match on text content + aria attributes, not on specific Tailwind class strings"
    - "`grep -E '\\\\[--text\\\\b|\\\\[--surface|\\\\[--border|\\\\[--accent|\\\\[--ring|\\\\[--danger|\\\\[--warning|\\\\[--success|\\\\[--text-muted|\\\\[--text-subtle' packages/spa/src/components/panels/*.tsx` returns ZERO matches (alias removal precondition for Wave 5)"
    - "1117-test baseline holds; pnpm -r test passes"
  artifacts:
    - path: "packages/spa/src/components/panels/PanelContainer.tsx"
      provides: "Updated chrome — bg-card-bg, shadow-card, rounded-card; preserves panelId/title/stale/unreachable API"
      contains: "bg-card-bg"
    - path: "packages/spa/src/components/panels/InlineDrift.tsx"
      provides: "Repaletted schema-drift inline state"
      contains: "text-status-error"
    - path: "packages/spa/src/components/SingleProjectView.tsx"
      provides: "PageHeader wrap (V2-gated) + 3-col grid with gap-6 inner+outer"
      contains: "PageHeader"
  key_links:
    - from: "packages/spa/src/components/SingleProjectView.tsx"
      to: "packages/spa/src/components/ui/PageHeader.tsx"
      via: "PageHeader title={projectId} on V2 mode"
      pattern: "PageHeader.*title="
    - from: "packages/spa/src/components/panels/*.tsx"
      to: "packages/spa/src/components/panels/PanelContainer.tsx"
      via: "All 13 panels still wrap their content in PanelContainer (no DOM structure swap)"
      pattern: "<PanelContainer"
---

<objective>
Wave 3 — Migrate `/projects/:id` to render inside AppShellV2 with PageHeader + repalette PanelContainer + InlineDrift + all 13 panels to use Tailwind 4 namespaced tokens. The migration is MECHANICAL: PanelContainer.tsx is the shared wrapper for all 13 panels (verified 2026-05-09 in RESEARCH "Per-Panel Migration Cost"), so updating PanelContainer's chrome propagates to every panel. Per-panel work is then ONLY token-class rewrites in panel-specific code (e.g., inline expanded-row blocks in SkillHealth, install-hint blocks in HookFirings, configure-to-enable blocks in IntegrationsHealth).

Purpose: This is the largest panel-touching wave. Three parallel tasks cover the three columns. Once green, only Wave 4 (settings/help) and Wave 5 (alias deletion + flag flip) remain.

Output: PanelContainer + InlineDrift repaletted at the source; 13 panel files audited; SingleProjectView gets PageHeader; column gap normalized to 24px (Pitfall 8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-CONTEXT.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-03-SUMMARY.md
@.planning/phases/04-single-project-view-discipline-phase-progress/04-CONTEXT.md
@.planning/phases/05-skills-health-panels/05-CONTEXT.md
@CLAUDE.md
@packages/spa/src/components/SingleProjectView.tsx
@packages/spa/src/components/ProjectHeader.tsx
@packages/spa/src/components/panels/PanelContainer.tsx
@packages/spa/src/components/panels/InlineDrift.tsx
@packages/spa/src/components/panels/SkillHealth.tsx
@packages/spa/src/components/ui/PageHeader.tsx

<token_translation_table>
RESEARCH "Per-Panel Migration Cost" — apply this table to every .tsx file in packages/spa/src/components/panels/:

| Legacy alias usage          | Namespaced replacement       |
|------------------------------|------------------------------|
| `bg-[--surface]`             | `bg-card-bg`                 |
| `bg-[--surface-elevated]`    | `bg-card-bg-hover`           |
| `border-[--border]`          | `border-border-subtle`       |
| `border-[--border-strong]`   | `border-border-subtle`       |
| `text-[--text]`              | `text-text-primary`          |
| `text-[--text-muted]`        | `text-text-secondary`        |
| `text-[--text-subtle]`       | `text-text-tertiary`         |
| `bg-[--accent]`              | `bg-accent`                  |
| `bg-[--accent-hover]`        | `bg-accent-hover`            |
| `text-[--accent]`            | `text-accent`                |
| `text-[--accent-fg]`         | `text-white`                 |
| `bg-[--ring]`                | `bg-accent`                  |
| `focus-visible:ring-[--ring]`| `focus-visible:ring-accent`  |
| `text-[--danger]`            | `text-status-error`          |
| `bg-[--danger-surface]`      | `bg-card-bg-hover` (or remove if just background) |
| `text-[--warning]`           | `text-status-warning`        |
| `bg-[--warning-surface]`     | `bg-card-bg-hover`           |
| `text-[--success]`           | `text-status-success`        |
| `text-[--bg]`                | `text-app-bg`                |
| `bg-[--bg]`                  | `bg-app-bg`                  |
| `divide-[--border]`          | `divide-border-subtle`       |
| `rounded-md` on top-level <section> wrappers (PanelContainer only) | `rounded-card` |

Tabular-nums (UI-SPEC §3): for any `tabular-nums` className already present, leave unchanged — Tailwind utility is theme-independent.

ZERO behavior changes — only className strings change.
</token_translation_table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repalette PanelContainer + InlineDrift + Discipline column panels (CommitmentBlock, HookFirings, RationalizationFires)</name>
  <files>
    packages/spa/src/components/panels/PanelContainer.tsx,
    packages/spa/src/components/panels/PanelContainer.test.tsx,
    packages/spa/src/components/panels/InlineDrift.tsx,
    packages/spa/src/components/panels/CommitmentBlock.tsx,
    packages/spa/src/components/panels/HookFirings.tsx,
    packages/spa/src/components/panels/RationalizationFires.tsx
  </files>
  <read_first>
    - packages/spa/src/components/panels/PanelContainer.tsx (current chrome — `flex flex-col gap-4 rounded-md border border-[--border] bg-[--surface] p-6`)
    - packages/spa/src/components/panels/InlineDrift.tsx (used by 13+ panels for schema_drift error state)
    - packages/spa/src/components/panels/CommitmentBlock.tsx (Discipline panel — empty/loading/data states)
    - packages/spa/src/components/panels/HookFirings.tsx (DISC-04 install-hint state with CodeBlock — verify CodeBlock renders inside Card body)
    - packages/spa/src/components/panels/RationalizationFires.tsx
    - packages/spa/src/components/panels/PanelContainer.test.tsx (assert API: panelId labels heading, stale pill, unreachable inline message)
    - packages/spa/src/components/CodeBlock.tsx (used inside HookFirings; ensure its tokens migrate too, OR confirm it stays alias-driven for one more wave — Wave 4 will catch it)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md "Per-Panel Migration Cost" + Pitfall 7 (a11y contract preservation)
  </read_first>
  <behavior>
    PanelContainer.test.tsx existing tests MUST pass without modification:
    - aria-labelledby is `${panelId}-title`
    - The h2 has id `${panelId}-title` and the title text
    - When stale=true, "Stale" pill renders right side
    - When unreachable=true, "Agent unreachable — retrying..." appears below the title

    OPTIONAL added test in PanelContainer.test.tsx: snapshot the className (or assert presence of `bg-card-bg`, `shadow-card`, `rounded-card`).

    Existing CommitmentBlock.test.tsx, HookFirings.test.tsx, RationalizationFires.test.tsx MUST pass unchanged.
  </behavior>
  <action>
    Step 1 — packages/spa/src/components/panels/PanelContainer.tsx — replace the section className:

    BEFORE: `className="flex flex-col gap-4 rounded-md border border-[--border] bg-[--surface] p-6"`

    AFTER: `className="flex flex-col gap-4 rounded-card bg-card-bg p-6 shadow-card"`

    Note: removing the `border` is intentional — UI-SPEC §8 says "no border" on cards (shadow + radius do the visual work). The `border-border-subtle` translation table entry was for elements that NEED a hairline; PanelContainer is a card surface and gets shadow-only.

    Update the stale pill className: `className="rounded-md bg-card-bg-hover px-2 py-0.5 text-xs font-semibold text-status-warning"` (was `bg-[--warning-surface] text-[--warning]`).

    Update the unreachable line: `className="flex items-center gap-2 text-sm text-status-warning"` (was `text-[--warning]`).

    Update the heading className: `className="text-lg font-semibold leading-snug text-text-primary"` — UI-SPEC §8 says CardHeader is `text-lg` semibold; the existing `text-xl` is too big for the new design. Note: UI-SPEC §8 says "Card Header" is `text-lg`; the existing PanelContainer uses `text-xl` which conflicts. Migrate to `text-lg` per UI-SPEC.

    NOTE on h2 size change: PanelContainer existing tests assert text content + aria attributes, not class names. The font-size visual change is intentional (UI-SPEC §8). Verify no test breaks; if any test uses `getByRole('heading', { level: 2 })` and asserts a specific class, update accordingly.

    Step 2 — packages/spa/src/components/panels/InlineDrift.tsx — apply <token_translation_table> to every className. InlineDrift is a wrapper used by all 13 panels for schema_drift errors; currently uses `bg-[--danger-surface] text-[--danger] border-[--danger]/20` etc. New shape:
    ```typescript
    // Within InlineDrift's section:
    className="flex flex-col gap-2 rounded-card bg-card-bg p-6 shadow-card"
    // Heading line:
    className="text-lg font-semibold leading-snug text-status-error"
    // Body text:
    className="text-sm text-text-secondary"
    // Retry button:
    className="border border-border-subtle rounded px-3 py-2 text-sm text-text-primary focus-visible:ring-2 focus-visible:ring-accent min-w-[44px] min-h-[44px]"
    ```

    Step 3 — packages/spa/src/components/panels/CommitmentBlock.tsx — apply <token_translation_table> to every legacy `[--*]` arbitrary-value classname. The component is small (~80 LOC); a search-replace pass covers it.

    Step 4 — packages/spa/src/components/panels/HookFirings.tsx — apply translation. Pay attention to:
    - The DISC-04 install-hint state: `<CodeBlock>` child stays unchanged (CodeBlock migrates in a separate plan); but the surrounding text uses tokens.
    - The empty-state message text colors: `text-[--text-muted]` → `text-text-secondary`.
    - The row-list wrapper: `divide-[--border]` → `divide-border-subtle`.

    Step 5 — packages/spa/src/components/panels/RationalizationFires.tsx — apply translation. Counter chip uses `bg-card-bg-hover text-text-secondary`.

    Step 6 — Pre-commit audit:
    ```bash
    grep -nE '\[--text\b|\[--surface|\[--border|\[--accent|\[--ring|\[--danger|\[--warning|\[--success|\[--text-muted|\[--text-subtle|\[--bg\b' \
      packages/spa/src/components/panels/PanelContainer.tsx \
      packages/spa/src/components/panels/InlineDrift.tsx \
      packages/spa/src/components/panels/CommitmentBlock.tsx \
      packages/spa/src/components/panels/HookFirings.tsx \
      packages/spa/src/components/panels/RationalizationFires.tsx
    ```
    Output must be EMPTY. If any line remains, investigate (likely an unmapped token).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/PanelContainer.test.tsx src/components/panels/InlineDrift.test.tsx src/components/panels/CommitmentBlock.test.tsx src/components/panels/HookFirings.test.tsx src/components/panels/RationalizationFires.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/components/panels/PanelContainer.tsx packages/spa/src/components/panels/InlineDrift.tsx packages/spa/src/components/panels/CommitmentBlock.tsx packages/spa/src/components/panels/HookFirings.tsx packages/spa/src/components/panels/RationalizationFires.tsx` returns NO matches
    - `grep -c "bg-card-bg" packages/spa/src/components/panels/PanelContainer.tsx` returns ≥ 1
    - `grep -c "rounded-card" packages/spa/src/components/panels/PanelContainer.tsx` returns ≥ 1
    - `grep -c "shadow-card" packages/spa/src/components/panels/PanelContainer.tsx` returns ≥ 1
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/` exits 0
    - `pnpm -r test` exits 0
  </acceptance_criteria>
  <done>
    PanelContainer is the new card; InlineDrift uses status-error; 3 Discipline panels migrated. No legacy aliases remain in these 5 files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repalette Phase Progress column panels (PhaseProgress, ExecutionTimeline, ReviewStatus, SecurityStatus, VerificationStatus)</name>
  <files>
    packages/spa/src/components/panels/PhaseProgress.tsx,
    packages/spa/src/components/panels/ExecutionTimeline.tsx,
    packages/spa/src/components/panels/ReviewStatus.tsx,
    packages/spa/src/components/panels/SecurityStatus.tsx,
    packages/spa/src/components/panels/VerificationStatus.tsx
  </files>
  <read_first>
    - packages/spa/src/components/panels/PhaseProgress.tsx (file-by-file checklist with green/yellow/red dots)
    - packages/spa/src/components/panels/ExecutionTimeline.tsx (TDD red/green commit pairs from git log)
    - packages/spa/src/components/panels/ReviewStatus.tsx (Stage 1/2 finding counts by severity)
    - packages/spa/src/components/panels/SecurityStatus.tsx (cso + database-sentinel summary)
    - packages/spa/src/components/panels/VerificationStatus.tsx (must_haves vs evidence count)
    - Their .test.tsx files (verify token swap doesn't break assertions)
    - <token_translation_table> in <context>
  </read_first>
  <behavior>
    All 5 panel test files MUST pass without modification.
  </behavior>
  <action>
    For each of the 5 files, apply <token_translation_table> mechanically:
    - Search every `[--text]`, `[--text-muted]`, `[--text-subtle]`, `[--surface]`, `[--surface-elevated]`, `[--border]`, `[--border-strong]`, `[--accent]`, `[--accent-fg]`, `[--ring]`, `[--danger]`, `[--warning]`, `[--success]`, `[--bg]` arbitrary-value class
    - Replace with the namespaced utility per the translation table
    - Preserve any tabular-nums, font-mono, font-semibold, leading-* utilities unchanged

    Specific notes:
    - PhaseProgress.tsx — checklist with green ✓ / red ✗ glyphs: glyph color comes from text-status-success / text-status-error
    - ExecutionTimeline.tsx — green/red commit pair indicators: same pattern
    - ReviewStatus.tsx — finding-count chips use Pill (consider migrating to the Wave 0 Pill primitive, but only if it's a 1:1 swap; otherwise leave the inline span and just retoken)
    - SecurityStatus.tsx — severity glyphs (3-of-4 per D-5-16) keep their existing string content; just update text colors
    - VerificationStatus.tsx — must_haves vs evidence delta count uses tabular-nums; preserve

    Pre-commit audit per Task 1 Step 6 (run grep on these 5 files) must return empty.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/PhaseProgress.test.tsx src/components/panels/ExecutionTimeline.test.tsx src/components/panels/ReviewStatus.test.tsx src/components/panels/SecurityStatus.test.tsx src/components/panels/VerificationStatus.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/components/panels/PhaseProgress.tsx packages/spa/src/components/panels/ExecutionTimeline.tsx packages/spa/src/components/panels/ReviewStatus.tsx packages/spa/src/components/panels/SecurityStatus.tsx packages/spa/src/components/panels/VerificationStatus.tsx` returns NO matches
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/PhaseProgress.test.tsx src/components/panels/ExecutionTimeline.test.tsx src/components/panels/ReviewStatus.test.tsx src/components/panels/SecurityStatus.test.tsx src/components/panels/VerificationStatus.test.tsx` exits 0
    - `pnpm -r test` exits 0
  </acceptance_criteria>
  <done>
    5 Phase Progress panels migrated. No legacy aliases remain. All tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repalette Skills+Health column panels (InstalledSkills, SkillHealth, ObservabilityHealth, SecretsHealth, IntegrationsHealth) + SingleProjectView PageHeader integration + ProjectHeader V2-gating + grid gap normalization</name>
  <files>
    packages/spa/src/components/panels/InstalledSkills.tsx,
    packages/spa/src/components/panels/SkillHealth.tsx,
    packages/spa/src/components/panels/ObservabilityHealth.tsx,
    packages/spa/src/components/panels/SecretsHealth.tsx,
    packages/spa/src/components/panels/IntegrationsHealth.tsx,
    packages/spa/src/components/SingleProjectView.tsx,
    packages/spa/src/components/SingleProjectView.test.tsx,
    packages/spa/src/components/ProjectHeader.tsx
  </files>
  <read_first>
    - packages/spa/src/components/panels/InstalledSkills.tsx
    - packages/spa/src/components/panels/SkillHealth.tsx (252 LOC, 8 states — RESEARCH "Per-Panel Migration Cost" calls out this one as needing careful state-by-state review)
    - packages/spa/src/components/panels/SkillHealth.test.tsx (8-state coverage)
    - packages/spa/src/components/panels/ObservabilityHealth.tsx
    - packages/spa/src/components/panels/SecretsHealth.tsx
    - packages/spa/src/components/panels/IntegrationsHealth.tsx (175 LOC; configure-to-enable inline paragraphs with verbatim D-5-20 copy — preserve copy exactly, only change className)
    - packages/spa/src/components/SingleProjectView.tsx (3-col grid, gap-6 outer, gap-4 inner — Pitfall 8 says inner gap-4 → gap-6)
    - packages/spa/src/components/SingleProjectView.test.tsx
    - packages/spa/src/components/ProjectHeader.tsx (becomes vestigial in V2 mode — Breadcrumb in TopBar replaces it; legacy mode keeps it)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §1 (single-project view layout with column gap and card gap both 24px), §7 (PageHeader)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md Pitfall 8 (gap normalization)
  </read_first>
  <behavior>
    All 5 panel test files MUST pass without modification.

    SkillHealth — 8 states verified:
    1. schema_drift → InlineDrift (already migrated in Task 1)
    2. isLoading → "Loading..." text-text-secondary
    3. error/no-data → unreachable PanelContainer
    4. kind:'not-installed' → CodeBlock + text-text-primary message
    5. kind:'timeout' → text-text-primary message + "Retry scan" button (44×44 touch target)
    6. kind:'error' → <pre> + text-text-secondary stderr + text-text-tertiary exit-code label
    7. kind:'unparseable' → text-text-primary message
    8. kind:'ok' → score badge (color tier: text-status-success ≥ 90, text-status-warning < 60, text-text-primary otherwise) + per-file rows with row-level expand

    Severity glyph emojis 🔴🟠⚪ stay verbatim (unicode literals; not changed by token migration).

    SingleProjectView.test.tsx adds:
    - Test (NEW): when V2 flag = '1', PageHeader renders with title equal to projectId. ProjectHeader is suppressed (or rendered legacy-mode-only via conditional).
    - Test (NEW): grid uses gap-6 outer AND inner column gap-6 (both 24px) — assert `flex-col gap-6` on column sections (was `flex-col gap-4`).
    - Existing tests (data-testids: single-project-grid, discipline-column, phase-progress-column, health-column) stay unchanged.

    ProjectHeader.tsx is REPALETTED but becomes display-vestigial in V2 mode (legacy mode keeps it; V2 mode replaces it via Breadcrumb in TopBar). The component is NOT deleted in this plan — Wave 5 (Plan 06) decides whether to delete it after the flag flip.
  </behavior>
  <action>
    Step 1 — InstalledSkills.tsx, ObservabilityHealth.tsx, SecretsHealth.tsx, IntegrationsHealth.tsx — apply <token_translation_table> mechanically. Same approach as Task 2.

    Special attention IntegrationsHealth.tsx: the 3 verbatim configure-to-enable paragraphs (D-5-20) MUST stay byte-identical in their text content. Only the wrapping `bg-[--surface-elevated]` etc. classes change. The paragraphs are stored as JSX literals (T-05-05-Static-Copy-Trust) — verify no template-string accidentally touches user content while rewriting className strings.

    Step 2 — SkillHealth.tsx (252 LOC, 8 states) — apply translation but VERIFY each state by reading the corresponding test in SkillHealth.test.tsx. Specific deltas:
    - score color tiers: `text-[--success]` → `text-status-success`; `text-[--warning]` → `text-status-warning`; `text-[--text]` → `text-text-primary`
    - row-level expand: `bg-[--surface-elevated]` → `bg-card-bg-hover`
    - per-finding row: `text-[--text]` → `text-text-primary`; `text-[--text-muted]` → `text-text-secondary`; `text-[--text-subtle]` → `text-text-tertiary`
    - retry button: `border-[--border]` → `border-border-subtle`; `focus-visible:ring-[--ring]` → `focus-visible:ring-accent`
    - error <pre>: `bg-[--surface-elevated] text-[--text-muted]` → `bg-card-bg-hover text-text-secondary`

    Step 3 — Pre-commit audit on all 5 panel files:
    ```bash
    grep -nE '\[--text\b|\[--surface|\[--border|\[--accent|\[--ring|\[--danger|\[--warning|\[--success|\[--text-muted|\[--text-subtle|\[--bg\b' \
      packages/spa/src/components/panels/InstalledSkills.tsx \
      packages/spa/src/components/panels/SkillHealth.tsx \
      packages/spa/src/components/panels/ObservabilityHealth.tsx \
      packages/spa/src/components/panels/SecretsHealth.tsx \
      packages/spa/src/components/panels/IntegrationsHealth.tsx
    ```
    Empty output expected.

    Step 4 — packages/spa/src/components/SingleProjectView.tsx:

    Add imports:
    ```typescript
    import { PageHeader } from './ui/PageHeader.js'
    ```

    Inside the component:
    ```typescript
    const useV2 = import.meta.env.VITE_APPSHELL_V2 === '1'
    ```

    Replace the JSX:
    ```jsx
    return (
      <div>
        {!useV2 && <ProjectHeader projectId={projectId} />}
        {useV2 && <PageHeader title={projectId} />}
        <div
          data-testid="single-project-grid"
          className="grid grid-cols-[1fr_1.5fr_1fr] items-start gap-6"
        >
          <section
            data-testid="discipline-column"
            aria-label="Discipline"
            className="flex min-w-0 flex-col gap-6"
          >
            ... (3 panels)
          </section>
          <section
            data-testid="phase-progress-column"
            aria-label="Phase Progress"
            className="flex min-w-0 flex-col gap-6"
          >
            ... (5 panels)
          </section>
          <section
            data-testid="health-column"
            aria-label="Health"
            className="flex min-w-0 flex-col gap-6"
          >
            ... (5 panels)
          </section>
        </div>
      </div>
    )
    ```

    KEY changes:
    - V2-mode PageHeader replaces ProjectHeader visually (Breadcrumb in TopBar handles the back-nav)
    - Inner column gap goes `gap-4` → `gap-6` (Pitfall 8 — match outer gap-6 for consistent rhythm)

    Step 5 — packages/spa/src/components/ProjectHeader.tsx — apply <token_translation_table> mechanically (it's used in legacy mode). The Sep() helper, the text-[--text-muted] tokens, etc. all migrate. Keep everything else unchanged.

    Step 6 — SingleProjectView.test.tsx — add the two new tests inside a new `describe('VITE_APPSHELL_V2 mode')` block using vi.stubEnv. Existing tests untouched. Existing tests that match on `flex-col gap-4` className strings (if any) MUST be updated to `flex-col gap-6`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/InstalledSkills.test.tsx src/components/panels/SkillHealth.test.tsx src/components/panels/ObservabilityHealth.test.tsx src/components/panels/SecretsHealth.test.tsx src/components/panels/IntegrationsHealth.test.tsx src/components/SingleProjectView.test.tsx src/components/ProjectHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/components/panels/InstalledSkills.tsx packages/spa/src/components/panels/SkillHealth.tsx packages/spa/src/components/panels/ObservabilityHealth.tsx packages/spa/src/components/panels/SecretsHealth.tsx packages/spa/src/components/panels/IntegrationsHealth.tsx` returns NO matches
    - `grep -E '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring' packages/spa/src/components/ProjectHeader.tsx` returns NO matches
    - `grep -c "PageHeader" packages/spa/src/components/SingleProjectView.tsx` returns ≥ 1
    - `grep -c "flex-col gap-6" packages/spa/src/components/SingleProjectView.tsx` returns 3 (one per column — Pitfall 8 normalization)
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/panels/ src/components/SingleProjectView.test.tsx src/components/ProjectHeader.test.tsx` exits 0
    - `pnpm -r typecheck` exits 0
    - `pnpm -r test` exits 0
    - `pnpm lint` exits 0
    - GLOBAL grep over all panels: `grep -rEn '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/components/panels/` returns NO matches (across all 13 panels + InlineDrift + PanelContainer)
  </acceptance_criteria>
  <done>
    SkillHealth's 8 states all render with new tokens; SingleProjectView wraps in PageHeader on V2; column gap normalized; all 13 panels free of legacy aliases. Test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 3 is mechanical token migration on /projects/:id panels. No new network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04-01 | T (Tampering) | IntegrationsHealth verbatim configure-to-enable copy (D-5-20) | mitigate | Token migration only touches className strings. JSX text literals (paragraph content) stay byte-identical. T-05-05-Static-Copy-Trust invariant preserved. |
| T-05.1-04-02 | I (Information disclosure) | SkillHealth state migration corruption | mitigate | Each of 8 states verified against existing SkillHealth.test.tsx coverage; no behavior change, only token classNames. |
| T-05.1-04-03 | T (Tampering) | InlineDrift token-rename causing schema-drift state to render incorrectly across 13 panels | mitigate | InlineDrift is migrated once in Task 1; all 13 consumers inherit the migration. Existing 13 panel tests cover schema-drift state. |
</threat_model>

<verification>
- pnpm -r typecheck exits 0
- pnpm -r test exits 0 (1117 baseline + new V2-mode tests)
- pnpm lint exits 0
- Manual smoke (V2 mode): `VITE_APPSHELL_V2=1 pnpm --filter @agenticapps/dashboard-spa dev`. Visit `/projects/agenticapps-dashboard`. Sidebar visible (left); TopBar with breadcrumb "All Projects > agenticapps-dashboard"; PageHeader with project ID; 3-column grid with cards using bg-card-bg + shadow-card; SkillHealth shows the 92/100 score in text-status-success.
- Manual smoke (legacy mode): `pnpm --filter @agenticapps/dashboard-spa dev`. Visit `/projects/...`. Old Header visible; ProjectHeader breadcrumb visible (legacy mode renders it); panels look slightly different colors (because aliases now point to new tokens) but layout/behavior unchanged.
</verification>

<success_criteria>
1. PanelContainer + InlineDrift + 13 panels all use Tailwind 4 namespaced utilities (no legacy `[--*]` aliases).
2. SkillHealth's 8 states verified post-migration; row-expand still works.
3. SingleProjectView V2 mode shows PageHeader instead of ProjectHeader.
4. Inner-column gap normalized to gap-6 (Pitfall 8 fix).
5. ProjectHeader migrated to namespaced tokens (legacy-mode rendering preserved).
6. Audit grep over `packages/spa/src/components/panels/` returns NO legacy aliases.
7. 1117-test baseline + new V2-mode tests pass.
8. `pnpm lint` green.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-04-SUMMARY.md` documenting:
- The exact panels migrated (13 + 2 helpers)
- The Pitfall 8 fix (gap-4 → gap-6 inner columns)
- The ProjectHeader V2-suppression strategy (replaced by Breadcrumb in TopBar — Wave 5 decides delete vs keep)
- Confirmation SkillHealth 8-state coverage exhaustive
- Note for Wave 4: settings/help routes still use legacy alias-based content; CodeBlock + ManualPairForm + ThemeToggle migrate in Plan 05.1-05
</output>
