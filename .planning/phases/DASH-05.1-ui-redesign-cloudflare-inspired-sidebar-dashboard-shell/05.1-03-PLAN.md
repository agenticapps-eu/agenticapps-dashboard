---
phase: 05.1
plan: 03
type: execute
wave: 3
depends_on: [05.1-02]
files_modified:
  - packages/spa/src/components/MultiProjectHome.tsx
  - packages/spa/src/components/MultiProjectHome.test.tsx
  - packages/spa/src/components/HomeToolbar.tsx
  - packages/spa/src/components/HomeToolbar.test.tsx
  - packages/spa/src/components/ProjectCard.tsx
  - packages/spa/src/components/ProjectCard.test.tsx
  - packages/spa/src/components/RegisterButtonCard.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When VITE_APPSHELL_V2='1', `/` renders inside AppShellV2 with a PageHeader (title 'Projects', helper 'N projects · last refresh ...') above the existing card grid"
    - "All existing MultiProjectHome behavior preserved: filter chips, search, sort, kebab menus, Register modal, Cmd+K integration, schema-drift state, daemon-unreachable state, focus-on-newly-added-card (D-29)"
    - "ProjectCard repalettes its chrome to use --color-* tokens (via Tailwind 4 utilities like bg-card-bg, text-text-primary) instead of legacy aliases like text-[--text]"
    - "RegisterButtonCard repalettes to bg-card-bg + dashed border-border-subtle + text-accent on hover"
    - "HomeToolbar repalettes filter chips: selected uses bg-accent + text-white; unselected uses bg-card-bg + text-text-primary + border-border-subtle"
    - "HomeLayout wrapper STILL exists and STILL works in legacy mode (flag-OFF) — Wave 5 deletes it; this plan keeps it functional but bypasses it in V2 mode by NOT calling setAppShellWidth from V2 main content (V2 has fixed grid, no width override needed)"
    - "All existing 1117 tests pass UNCHANGED — repalette is mechanical (className token swap), no behavior change"
    - "MultiProjectHome.test.tsx gets a new test verifying PageHeader renders with title 'Projects' (V2-mode assertion) — flag-OFF mode tests stay unchanged"
  artifacts:
    - path: "packages/spa/src/components/MultiProjectHome.tsx"
      provides: "Wraps content in PageHeader (V2-mode only, gated by import.meta.env.VITE_APPSHELL_V2 === '1') + repaletted toolbar/card/register"
      contains: "PageHeader"
    - path: "packages/spa/src/components/HomeToolbar.tsx"
      provides: "Filter chips, search, sort with new tokens (bg-accent + text-white selected; bg-card-bg + border-border-subtle unselected)"
      contains: "bg-accent"
    - path: "packages/spa/src/components/ProjectCard.tsx"
      provides: "Card chrome migrated to bg-card-bg + shadow-card + rounded-card; text uses text-text-primary/secondary/tertiary"
      contains: "bg-card-bg"
    - path: "packages/spa/src/components/RegisterButtonCard.tsx"
      provides: "Repaletted dashed-border card with text-accent on hover"
      contains: "border-border-subtle"
  key_links:
    - from: "packages/spa/src/components/MultiProjectHome.tsx"
      to: "packages/spa/src/components/ui/PageHeader.tsx"
      via: "import + render PageHeader at top of content (V2-mode gate)"
      pattern: "PageHeader.*title=.Projects"
    - from: "packages/spa/src/components/MultiProjectHome.tsx"
      to: "packages/spa/src/lib/lastRefresh.ts"
      via: "useLastRefresh() for the 'N projects · refreshed Xs ago' helper text"
      pattern: "useLastRefresh"
---

<objective>
Wave 2 — Migrate `/` (multi-project home page) to render inside AppShellV2 when the flag is on. Wrap content in PageHeader. Repalette HomeToolbar, ProjectCard, RegisterButtonCard to use the new --color-* tokens via Tailwind 4 namespaced utilities. ZERO behavior changes — every test that exists for filter/search/sort/modal/kebab continues to pass.

Purpose: The home page is the lowest-risk migration target (single composition, no panels, no per-row state machines). Migrating it first validates the AppShellV2 wiring and the token translation pattern before tackling /projects/:id (which has 13 panels in Wave 3).

Output: Repaletted toolbar + project card + register button card; PageHeader integration in MultiProjectHome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-CONTEXT.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-01-SUMMARY.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-02-SUMMARY.md
@.planning/phases/03-multi-project-home-page/03-CONTEXT.md
@CLAUDE.md
@packages/spa/src/components/MultiProjectHome.tsx
@packages/spa/src/components/HomeToolbar.tsx
@packages/spa/src/components/ProjectCard.tsx
@packages/spa/src/components/RegisterButtonCard.tsx
@packages/spa/src/components/HomeLayout.tsx
@packages/spa/src/lib/lastRefresh.ts
@packages/spa/src/components/ui/PageHeader.tsx
@packages/spa/src/components/ui/Pill.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PageHeader to MultiProjectHome (V2-gated) + repalette empty-state and grid container</name>
  <files>
    packages/spa/src/components/MultiProjectHome.tsx,
    packages/spa/src/components/MultiProjectHome.test.tsx
  </files>
  <read_first>
    - packages/spa/src/components/MultiProjectHome.tsx (lines 1-170 — full current file; understand all 5 states)
    - packages/spa/src/components/MultiProjectHome.test.tsx (current behavior assertions to preserve)
    - packages/spa/src/components/ui/PageHeader.tsx (Wave 1 primitive)
    - packages/spa/src/lib/lastRefresh.ts (useLastRefresh hook contract — provides count + refreshLabel)
    - packages/spa/src/components/HomeLayout.tsx (kept; still wraps; V2 won't change width override behavior since AppShellV2 has fixed grid)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §1 ("Multi-project home"), §7 (PageHeader)
  </read_first>
  <behavior>
    Updated MultiProjectHome.test.tsx adds:
    - Test (NEW): when VITE_APPSHELL_V2 = '1', PageHeader renders with title "Projects" and helper "{count} projects · {refreshLabel}" — assert via screen.getByRole('heading', { level: 1, name: /^Projects$/ })
    - Test (NEW): when VITE_APPSHELL_V2 ≠ '1', PageHeader does NOT render (legacy mode unchanged)
    - All EXISTING tests stay unchanged (filter chips, sort, search, kebab open, schema-drift, unreachable, register-modal-open, focus-on-new-card behavior).

    Empty-state copy preserved: "No projects registered yet." + the agentic-dashboard register snippet — but the surrounding section uses `bg-card-bg` + `border-border-subtle` instead of legacy aliases.
  </behavior>
  <action>
    Step 1 — packages/spa/src/components/MultiProjectHome.tsx top:
    Add imports:
    ```typescript
    import { useLastRefresh } from '../lib/lastRefresh.js'
    import { PageHeader } from './ui/PageHeader.js'
    ```

    Step 2 — Inside the MultiProjectHome function body, before the `if (list.isError)` block, derive PageHeader props:
    ```typescript
    const useV2 = import.meta.env.VITE_APPSHELL_V2 === '1'
    const lastRefresh = useLastRefresh()
    const headerHelper =
      lastRefresh.count !== null
        ? `${lastRefresh.count} ${lastRefresh.count === 1 ? 'project' : 'projects'} · ${lastRefresh.refreshLabel ?? ''}`
        : ''
    ```

    Step 3 — Wrap the main return body in V2 mode with a PageHeader prelude. Inject between `<HomeLayout>` and `<div className="px-6 py-8 md:px-8">`:
    ```jsx
    <HomeLayout>
      {useV2 && <PageHeader title="Projects" helper={headerHelper} />}
      <div className={useV2 ? "" : "px-6 py-8 md:px-8"}>
        ... existing content ...
      </div>
      ...
    </HomeLayout>
    ```

    Note: in V2 mode AppShellV2 already provides `p-6` outer padding via `<main className="p-6">`, so the inner `px-6 py-8 md:px-8` becomes double-padding. Drop it in V2 mode (use empty className).

    Step 4 — Repalette the empty-state block (currently `border border-[--border] bg-[--surface]`):
    ```jsx
    <div className="mt-6 rounded-card border border-border-subtle bg-card-bg p-6 text-center shadow-card">
      <h2 className="text-lg font-semibold text-text-primary">No projects registered yet.</h2>
      <p className="mt-2 text-base text-text-secondary">
        Run{' '}
        <code className="font-mono text-sm">agentic-dashboard register &lt;path&gt;</code>{' '}
        to add one, or use the button above.
      </p>
    </div>
    ```

    Step 5 — Add the new tests in MultiProjectHome.test.tsx using vi.stubEnv('VITE_APPSHELL_V2', '1') in a beforeEach for the new test block; restore in afterEach. Re-render after stubbing because import.meta.env is read inside the component.

    Note on test scaffolding: existing MultiProjectHome.test.tsx already mocks useRegistryList and other hooks. The new tests add a `describe('VITE_APPSHELL_V2 mode', () => {...})` sibling block, NOT modifying the existing assertions. Use `vi.stubEnv` per test, restore via vi.unstubAllEnvs in afterEach.

    Step 6 — Mock useLastRefresh in the test file (not currently mocked) to return `{ count: 3, refreshLabel: 'refreshed 4s ago' }` — assert the header helper text matches "3 projects · refreshed 4s ago".
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/MultiProjectHome.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { PageHeader }" packages/spa/src/components/MultiProjectHome.tsx` returns 1
    - `grep -c "import.meta.env.VITE_APPSHELL_V2 === '1'" packages/spa/src/components/MultiProjectHome.tsx` returns 1
    - `grep -c "title=\"Projects\"" packages/spa/src/components/MultiProjectHome.tsx` returns 1
    - `grep -c "useLastRefresh" packages/spa/src/components/MultiProjectHome.tsx` returns ≥ 1
    - `grep -c "bg-card-bg" packages/spa/src/components/MultiProjectHome.tsx` returns ≥ 1 (empty-state repaletted)
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/MultiProjectHome.test.tsx` exits 0
    - `pnpm -r test` exits 0 (1117 baseline + new V2-mode test cases pass)
  </acceptance_criteria>
  <done>
    MultiProjectHome wraps its content in PageHeader when the V2 flag is set; legacy mode renders identically to before. Empty-state uses new tokens. All other behavior preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repalette HomeToolbar + ProjectCard + RegisterButtonCard to new tokens</name>
  <files>
    packages/spa/src/components/HomeToolbar.tsx,
    packages/spa/src/components/HomeToolbar.test.tsx,
    packages/spa/src/components/ProjectCard.tsx,
    packages/spa/src/components/ProjectCard.test.tsx,
    packages/spa/src/components/RegisterButtonCard.tsx
  </files>
  <read_first>
    - packages/spa/src/components/HomeToolbar.tsx (current chip/search/sort styles — note CHIP_SELECTED, CHIP_UNSELECTED, search input, sort select all use legacy aliases)
    - packages/spa/src/components/HomeToolbar.test.tsx (existing behavior assertions to preserve)
    - packages/spa/src/components/ProjectCard.tsx (current card chrome — uses --surface, --border, --text via legacy aliases)
    - packages/spa/src/components/ProjectCard.test.tsx (existing assertions)
    - packages/spa/src/components/RegisterButtonCard.tsx (current dashed-border card)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §8 (Card anatomy), §4 (spacing/radius)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md "Per-Panel Migration Cost" (mechanical token swap)
  </read_first>
  <behavior>
    All existing tests in HomeToolbar.test.tsx and ProjectCard.test.tsx MUST pass without modification (these are mechanical className token swaps — no DOM structure changes, no behavior changes).

    OPTIONAL new test: HomeToolbar.test.tsx adds one snapshot/className assertion: "selected chip uses bg-accent class". Same for ProjectCard.test.tsx: "card root uses bg-card-bg + shadow-card + rounded-card".
  </behavior>
  <action>
    HomeToolbar.tsx — replace CHIP_SELECTED + CHIP_UNSELECTED constants:
    ```typescript
    const CHIP_SELECTED =
      'bg-accent text-white border border-accent px-3 py-2 rounded-md text-sm font-semibold focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-app-bg'

    const CHIP_UNSELECTED =
      'bg-card-bg text-text-primary border border-border-subtle px-3 py-2 rounded-md text-sm font-semibold hover:bg-card-bg-hover focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-app-bg'
    ```

    Replace the search input className: `bg-card-bg-hover border-border-subtle ... text-text-primary placeholder:text-text-tertiary focus-visible:ring-accent`.

    Replace the sort select className: same token swap.

    Replace the chevron + search icon `text-[--text-muted]` → `text-text-secondary`.

    For overflow chips with `(count)`, change `text-[--text-muted]` → `text-text-tertiary`.

    ProjectCard.tsx — apply the token translation table (mechanical — RESEARCH "Per-Panel Migration Cost" — wrapper-only mostly):
    ```
    bg-[--surface]                  → bg-card-bg
    bg-[--surface-elevated]         → bg-card-bg-hover
    border-[--border]               → border-border-subtle
    border-[--border-strong]        → border-border-subtle
    text-[--text]                   → text-text-primary
    text-[--text-muted]             → text-text-secondary
    text-[--text-subtle]            → text-text-tertiary
    bg-[--accent]                   → bg-accent
    text-[--accent]                 → text-accent
    text-[--accent-fg]              → text-white
    bg-[--ring], focus:ring-[--ring]→ ring-accent
    bg-[--danger-surface]           → bg-card-bg (the danger surface tinting comes via Pill component now if needed)
    text-[--danger]                 → text-status-error
    text-[--warning]                → text-status-warning
    text-[--success]                → text-status-success
    rounded-md (where it's the card itself) → rounded-card
    ```

    For the card's root <article> wrapper, ensure it uses `bg-card-bg shadow-card rounded-card` (UI-SPEC §8 card anatomy). Existing card likely uses `border` — keep it for hover-expand behavior parity, but use `border-border-subtle` instead of `border-[--border]`.

    RegisterButtonCard.tsx — repalette the dashed border + text-color to:
    ```
    border-2 border-dashed border-border-subtle bg-card-bg text-text-secondary hover:border-accent hover:text-accent rounded-card
    ```
    Preserve the click handler + label "+ Register project".

    DO NOT TOUCH: any test file's assertion logic. The token swap is invisible to assertions that match on text content or aria attributes. If any test asserts a specific className string, update the className in BOTH the test and the component to the new token.

    NOTE: `packages/spa/src/components/RegisterButtonCard.test.tsx` ALREADY EXISTS (verified pre-Plan-03). It is intentionally NOT in this plan's `files_modified` because the test stays unchanged — the mechanical className swap on `RegisterButtonCard.tsx` does not affect any test assertion (the existing test verifies onClick behavior + label text, not specific className strings). The acceptance command runs it to confirm it stays green.

    AUDIT pre-flight: before committing, run `grep -E "\\[--text|\\[--surface|\\[--border|\\[--accent|\\[--danger|\\[--warning|\\[--success|\\[--ring|\\[--bg|\\[--text-muted|\\[--text-subtle" packages/spa/src/components/HomeToolbar.tsx packages/spa/src/components/ProjectCard.tsx packages/spa/src/components/RegisterButtonCard.tsx` and confirm output is empty (no remaining legacy alias usages in these three files).

    NOTE: Even though the alias layer means legacy `[--text]` STILL resolves correctly at render time, the AC-05 invariant (hex literals only in tokens.css) PLUS the AC for "use namespaced tokens" means we migrate these three files NOW. Other panel files migrate in Wave 3 (Plan 04). The Wave 5 plan removes the alias layer; if any file is left on legacy aliases at that point, it breaks.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/HomeToolbar.test.tsx src/components/ProjectCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\\[--text|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success" packages/spa/src/components/HomeToolbar.tsx packages/spa/src/components/ProjectCard.tsx packages/spa/src/components/RegisterButtonCard.tsx` returns no matches
    - `grep -c "bg-accent" packages/spa/src/components/HomeToolbar.tsx` returns ≥ 1 (selected chip)
    - `grep -c "bg-card-bg" packages/spa/src/components/ProjectCard.tsx` returns ≥ 1
    - `grep -c "border-border-subtle" packages/spa/src/components/RegisterButtonCard.tsx` returns ≥ 1
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/HomeToolbar.test.tsx src/components/ProjectCard.test.tsx src/components/RegisterButtonCard.test.tsx` exits 0
    - `pnpm -r test` exits 0
    - `pnpm -r typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    HomeToolbar, ProjectCard, RegisterButtonCard render with the new token utilities. Test suite green; no behavior changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 2 is mechanical token migration on `/`. No new network surface, no new daemon routes, no new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03-01 | T (Tampering) | Token migration introducing visual regression | mitigate | Existing 1117 tests cover behavior (filter logic, search filter, sort, kebab, modal, card states); only chrome class strings changed. Pre-flight grep audit confirms zero remaining legacy aliases in the three migrated files. |
| T-05.1-03-02 | I (Information disclosure) | Project card chrome leaking data | accept | No data shape changes; only colors/typography updated. ProjectCard still renders the same fields it did pre-migration. |
</threat_model>

<verification>
- pnpm -r typecheck exits 0
- pnpm -r test exits 0 (1117 baseline)
- pnpm lint exits 0
- Manual smoke (V2 mode): `VITE_APPSHELL_V2=1 pnpm --filter @agenticapps/dashboard-spa dev`. Visit `/`. Sidebar visible (Wave 1 wired); PageHeader "Projects · 3 projects · refreshed 4s ago" visible above the toolbar; filter chips selected state shows purple bg; cards show warm-paper bg with subtle shadow.
- Manual smoke (legacy mode, no flag set): `pnpm --filter @agenticapps/dashboard-spa dev`. Visit `/`. Looks identical to pre-Phase-5.1 (legacy AppShell + repaletted toolbar/cards via aliases — colors will look slightly different because the alias values map to NEW palette, but the layout/behavior is unchanged).
</verification>

<success_criteria>
1. MultiProjectHome adds PageHeader (title "Projects" + helper "N projects · refreshed Xs ago") when V2 flag is on.
2. Legacy mode unchanged — no PageHeader, no behavior regression.
3. HomeToolbar chips, search, sort all use --color-* namespaced Tailwind utilities.
4. ProjectCard root uses bg-card-bg + shadow-card + rounded-card.
5. RegisterButtonCard uses dashed border-border-subtle + text-text-secondary, hover text-accent.
6. No file in this plan contains `[--text]`, `[--surface]`, `[--border]`, `[--accent]`, `[--danger]`, `[--warning]`, `[--success]`, `[--ring]` arbitrary-value classes (audit grep confirms).
7. 1117-test baseline + new V2-mode tests all pass.
8. `pnpm lint` green.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-03-SUMMARY.md` documenting:
- The V2 gate pattern used in MultiProjectHome (`useV2 && <PageHeader />` conditional)
- The token translation table applied to the three files (legacy alias → namespaced utility)
- HomeLayout still active in legacy mode; bypassed in V2 mode (Wave 5 will delete HomeLayout)
- Note for Wave 3 executor: the same translation table applies to the 13 panels
</output>
