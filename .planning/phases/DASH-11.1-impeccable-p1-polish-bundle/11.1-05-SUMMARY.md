---
phase: DASH-11.1-impeccable-p1-polish-bundle
plan: "05"
subsystem: ui
tags: [coverage, sticky, layout, composition, tdd, css-custom-properties]

requires:
  - phase: DASH-11.1-impeccable-p1-polish-bundle/11.1-04
    provides: usePageHeaderHeight hook publishes --ph-h on documentElement; PageHeader gains data-testid

provides:
  - CoverageToolbar rendered inside PageHeader children slot (inherits sticky surface)
  - CoverageFamilySection family-header top: calc(var(--ph-h)-1.5rem) -- hardcoded top-8 removed
  - CoverageFamilySection column-headers top: calc(var(--ph-h)+1.5625rem) -- hardcoded top-[5.0625rem] removed
  - 4 atomic TDD commits (RED+GREEN x2) locked by 4 new test assertions

affects:
  - DASH-11.1-impeccable-p1-polish-bundle/11.1-06
  - future plans touching CoverageFamilySection sticky stack

tech-stack:
  added: []
  patterns:
    - "CSS custom property consumption: sticky top-[calc(var(--ph-h)+...)] instead of magic-number pixel offsets"
    - "PageHeader children slot as composition point for route-specific sticky content"

key-files:
  created: []
  modified:
    - packages/spa/src/components/panels/coverage/CoveragePage.tsx
    - packages/spa/src/components/panels/coverage/CoveragePage.test.tsx
    - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx
    - packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx

key-decisions:
  - "Task 0 was already complete from Plan 04's feat commit (758b63a) -- data-testid and tests both shipped; no additional Task 0 commit needed"
  - "Stale inline comment block inside CoverageFamilySection function body removed (CLAUDE.md: no comments inside function bodies) instead of updated per plan"
  - "CoverageToolbar moved into PageHeader children slot only in the main render branch; loading/error/empty branches remain self-closing PageHeader (no toolbar in those states)"

patterns-established:
  - "var(--ph-h) consumption pattern: any sticky element below the PageHeader uses top-[calc(var(--ph-h)+-offset)] for automatic adaptation"

requirements-completed:
  - IMP-02

duration: 18min
completed: "2026-05-18"
---

# Phase 11.1 Plan 05: Sticky Toolbar Composition + calc(var(--ph-h)) Stack Summary

**CoverageToolbar moved into PageHeader children slot (sticky inheritance) and CoverageFamilySection sticky offsets switched from hardcoded px values to calc(var(--ph-h)) expressions consuming the ResizeObserver-published CSS variable.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-18T12:00:00Z
- **Completed:** 2026-05-18T14:04:00Z
- **Tasks:** 4 TDD tasks (Task 0 pre-completed in Plan 04)
- **Files modified:** 4

## Accomplishments

- CoverageToolbar is now rendered inside `<PageHeader>` via the children slot — it inherits the sticky surface and stays visible at every scrollY on /coverage
- Family-header sticky top switched from hardcoded `top-8` (32px) to `top-[calc(var(--ph-h)-1.5rem)]` — auto-adapts when PageHeader height changes
- Column-headers sticky top switched from hardcoded `top-[5.0625rem]` (81px) to `top-[calc(var(--ph-h)+1.5625rem)]` — applied to all 6 `<th>` elements
- 4 new test assertions locked the structural invariant: toolbar inside testid subtree, calc expressions present, hardcoded values absent
- Full SPA suite: 943 tests across 111 files — all passing

## Task Commits

1. **Task 0: PageHeader data-testid (PD-11.1-07)** — pre-completed in Plan 04 commit `758b63a` (feat); no additional commit needed
2. **Task 1: RED — CoverageToolbar inside PageHeader subtree** — `5c4e2f6` (test)
3. **Task 2: GREEN — CoverageToolbar as PageHeader child** — `cfd43cd` (feat)
4. **Task 3: RED — family/column sticky top consumes var(--ph-h)** — `84ed527` (test)
5. **Task 4: GREEN — CoverageFamilySection calc(var(--ph-h)) stack** — `9e1d2c1` (feat)

## Files Created/Modified

- `packages/spa/src/components/panels/coverage/CoveragePage.tsx` — CoverageToolbar moved from sibling to PageHeader children slot in main render branch
- `packages/spa/src/components/panels/coverage/CoveragePage.test.tsx` — added `within` import + new describe block with 2 structural assertions (toolbar inside testid subtree)
- `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` — family-header top-8 -> calc(var(--ph-h)-1.5rem); all 6 th top-[5.0625rem] -> calc(var(--ph-h)+1.5625rem); stale comment block removed per CLAUDE.md
- `packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx` — new describe block with 2 assertions for calc expressions on header and th elements

## Decisions Made

- **Task 0 skip**: `data-testid` was already added to PageHeader in Plan 04's GREEN commit (`758b63a`), including the corresponding PageHeader.test.tsx assertions. No duplicate commit created; documented here for traceability.
- **Comment removal instead of update**: The plan said to "update the inline comment block" in CoverageFamilySection. The block is inside the function body (JSX return). CLAUDE.md mandates NO comments inside function bodies. The block was removed rather than updated — CLAUDE.md takes precedence over plan instructions.
- **Only main render branch gets toolbar**: The loading, error, and empty-matrix PageHeader elements are self-closing and correctly have no toolbar — those states have no filter data to display.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale comment block removed instead of updated**
- **Found during:** Task 4 (GREEN — CoverageFamilySection)
- **Issue:** Plan said to "update the inline comment block (lines 167-175) to document the new calc-based positioning". The block lives inside the JSX return (function body). CLAUDE.md constraint D-5.1-10: NO comments inside function bodies.
- **Fix:** Comment block removed entirely rather than updated with stale/incorrect information.
- **Files modified:** `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx`
- **Verification:** Tests still pass; no JSX comment nodes in function body.
- **Committed in:** `9e1d2c1` (Task 4 GREEN)

---

**Total deviations:** 1 auto-fixed (Rule 1 — CLAUDE.md constraint takes precedence)
**Impact on plan:** Zero functional impact; comment removal is cosmetic. The calc expressions are self-documenting.

## Issues Encountered

None — plan executed cleanly. All RED tests failed for the correct reason (structural mismatch), all GREEN implementations made them pass.

## Visual Verification Status

Dev server was confirmed reachable at `http://localhost:5174` (HTTP 200). However, Playwright is not installed in this environment (`playwright not found in node_modules/.bin`). Screenshot pass at scrollY 0/200/400/800/1200/1600 (1440x900) and iPad portrait (768x1024) per PD-11.1-08 is deferred to the QA step (`/qa`).

The sticky-stack regression contract is verified structurally by the test suite:
- Family-header top expression: `calc(var(--ph-h)-1.5rem)` confirmed by `CoverageFamilySection.test.tsx`
- Column-header top expression: `calc(var(--ph-h)+1.5625rem)` confirmed by `CoverageFamilySection.test.tsx`
- Toolbar inside PageHeader subtree: confirmed by `CoveragePage.test.tsx` via `within(getByTestId('page-header-sticky'))`

## Known Stubs

None — all wiring is live. The `--ph-h` CSS variable is published by the ResizeObserver in `usePageHeaderHeight` (Plan 04) and consumed by the calc expressions added in this plan.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check

### Files exist:
- `packages/spa/src/components/panels/coverage/CoveragePage.tsx` — FOUND (modified)
- `packages/spa/src/components/panels/coverage/CoveragePage.test.tsx` — FOUND (modified)
- `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` — FOUND (modified)
- `packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx` — FOUND (modified)

### Commits exist:
- `5c4e2f6` — FOUND (test: RED CoverageToolbar)
- `cfd43cd` — FOUND (feat: GREEN CoverageToolbar)
- `84ed527` — FOUND (test: RED calc var)
- `9e1d2c1` — FOUND (feat: GREEN calc var)

### Test results:
- Coverage suite: 105/105 passed
- Full SPA suite: 943/943 passed
- Typecheck: clean
- Build: clean

## Self-Check: PASSED

## Next Phase Readiness

Plan 05 completes IMP-02 (sticky toolbar). The /coverage sticky stack now auto-adapts to PageHeader height via `--ph-h`. Plan 06 (Toast clipboard feedback — IMP-03) can proceed independently.

---
*Phase: DASH-11.1-impeccable-p1-polish-bundle*
*Completed: 2026-05-18*
