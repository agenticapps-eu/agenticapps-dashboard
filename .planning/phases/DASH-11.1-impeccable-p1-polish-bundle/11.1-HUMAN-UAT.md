# Phase 11.1 — Human UAT

**Status:** scaffold (filled during `/qa` post-execution)
**Run conditions:** dev server reachable on `http://localhost:5174`; daemon optional (Coverage page can render against a known-good cache or fixture)

UAT items map 1:1 with the `human_verification` block in `11.1-VERIFICATION.md`. Each item lands as ✅ / ⚠ / ❌ with a one-line note and screenshot path.

---

## UAT-1 — All 6 clipboard call sites surface a toast

**What to do:**

1. `/browse` to `/coverage`
2. Click each of the 6 clipboard surfaces:
   1. `IndexGitNexusButton` (purple CTA top-right when `installState='installed-no-registry'`)
   2. `InstallGitNexusButton` (purple CTA top-right when `installState='not-installed'`)
   3. Family-section "Copy npm install -g gitnexus" link (when `installState='not-installed'`)
   4. Per-row refresh popover → "Copy `/wiki-compile` command" entry
   5. Per-row refresh popover → "Copy workflow update" entry
   6. `CoverageEmptyState` install link (force `no-repos` state via empty registry or fixture)

**Expected per click:**
- Toast appears at `top-16 right-4` within ~200ms of click
- Toast shows the contextual message (CLI → "paste in terminal to ...")
- Toast fades out after 2.4s
- Rapid double-click of the same surface REPLACES the toast (no stack)

**Outcome:** [TBD-on-execute]

**Screenshot evidence:** `screenshots/06-toast-{site}.png` × 6

---

## UAT-2 — Column-width lock visible at 1440×900

**What to do:**

1. `/browse` to `/coverage` at 1440×900 viewport
2. Mentally trace any column edge top-to-bottom across the 3 family sections
3. Take a screenshot at scrollY 0 with all 3 sections visible

**Expected:**
- Repo column 288px wide across all 3 sections
- CLAUDE.md / GitNexus / Workflow columns 128px each
- Wiki column 352px
- Actions column 32px
- Edges align EXACTLY across families (matrix mental model restored)

**Outcome:** [TBD-on-execute]

**Screenshot evidence:** `screenshots/02-col-widths-{0,400,800}.png`

---

## UAT-3 — Sticky stack flush at every scroll position

**What to do:**

1. `/browse` to `/coverage`
2. Scroll to scrollY = 0, then 200, 400, 800, 1200, 1600
3. Take a screenshot at each position
4. Verify the 4-element sticky stack at every position:
   - Row 1: PageHeader title + actions
   - Row 2: PageHeader helper subtitle
   - Row 3: CoverageToolbar (chips + search) — NEW position
   - Row 4: Family-header (sticky)
   - Row 5: Column-headers (sticky)

**Expected per position:**
- All 5 rows flush (no transparent gap between any pair)
- All 5 rows readable
- Scrolling rows pass UNDER the column-header row (z-index correct)
- No row pulls out of position or overlaps

**Outcome:** [TBD-on-execute]

**Regression contract:** This UAT is the Phase 11 post-UAT contract extended. If Plan 05 introduces a gap or overlap at any of the 6 scroll positions, REVERT and surface as PD-11.1.

**Screenshot evidence:** `screenshots/05-sticky-stack-{0,200,400,800,1200,1600}.png`

---

## UAT-4 — Token contrast: visually darker, tier hierarchy preserved

**What to do:**

1. `/browse` to `/coverage`
2. Capture screenshot of the sidebar section labels (WORKSPACE, OBSERVABILITY, ACCOUNT) and the table column headers (Repo, CLAUDE.md, GitNexus, Wiki, Workflow)
3. Open browser devtools, hover the text and read computed color — should be `#75708A`
4. Compare visually to the prior-state screenshot (Phase 11 IMPECCABLE coverage-assessment-b.png) — text should be NOTICEABLY darker

**Expected:**
- Sidebar section labels readable (vs current barely-discernible 3.9:1)
- Tier hierarchy preserved: section labels (tertiary) still LIGHTER than active item label (primary)
- No usage looks "broken-dark" — no chrome text becomes harder to read

**Outcome:** [TBD-on-execute]

---

## UAT-5 — IMPECCABLE re-pass (calibration data point #3)

**What to do:**

1. Confirm `feat/impeccable-p1-polish-bundle` branch HEAD has all 6 plans committed
2. Boot dev server
3. Run `/impeccable critique` (skill from `~/.claude/skills/impeccable/`) against `/coverage` at 1440×900
4. Capture composite + per-heuristic + deterministic detector findings
5. Write `11.1-IMPECCABLE.md` per the Phase 11 artifact convention

**Expected:**
- Composite ≥ 80 (record actual — calibration data point #3)
- Deterministic detector reports ZERO `low-contrast` findings on `text-text-tertiary` (was 3 in Phase 11)
- Nielsen #1 (Visibility of System Status): ≥ 3 (was 2 in Phase 11 — toast closes the silent-clipboard issue)
- Nielsen #4 (Consistency and Standards): ≥ 3 (was 2 in Phase 11 — column-width lock closes the matrix-mental-model issue)
- Phase 11 verification items #1-7 from `11-IMPECCABLE.md` lines 242-249 still all pass

**Outcome:** [TBD-on-execute]

**Decision branch on result:**
- Composite ≥ 85 → opens Option A path on D-10.5-03 (keep 87 floor — Phase 11.1 closes the gap fully)
- 78-84 → confirms Option B path (recalibrate floor to ~80 + waiver clause)
- 75-77 → confirms Option C path (lift-over-prior as the new metric)
- < 75 → confirms Option D path (per-surface waiver list — calibration is structurally broken)

**Artifact:** `11.1-IMPECCABLE.md` (calibration data point #3)

---

## UAT-6 — Regression smoke on non-Coverage routes

**What to do:**

1. Walk every non-Coverage route: `/`, `/projects`, `/help`, `/settings`, `/observability/skill-drift`, `/pair`, `/login`
2. Inspect each for:
   - Token visual regression (text-text-tertiary darker but readable)
   - PageHeader sticky mode unchanged (default sticky=false routes don't suddenly stick)
   - No console errors / warnings on the new ResizeObserver hook
3. Take baseline screenshot of each route at scrollY=0

**Expected:**
- Every route renders without visual regression
- text-text-tertiary appears slightly darker than prior — no usage breaks
- No layout shift introduced by the new `--ph-h` default

**Outcome:** [TBD-on-execute]

---

## UAT outcome summary (filled at end of execute)

- UAT-1 (toast wiring): [pending]
- UAT-2 (column-width lock): [pending]
- UAT-3 (sticky stack): [pending]
- UAT-4 (token contrast): [pending]
- UAT-5 (IMPECCABLE re-pass): [pending]
- UAT-6 (cross-route regression): [pending]

**Overall:** [pending]

**Sign-off:** [TBD-on-execute] by [TBD-on-execute]
