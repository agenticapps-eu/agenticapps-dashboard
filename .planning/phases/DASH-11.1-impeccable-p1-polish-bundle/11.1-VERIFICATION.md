---
phase: 11.1-impeccable-p1-polish-bundle
verified: TBD-on-execute
status: pending
must_haves_score: 0/24
requirements_score: 0/5
human_uat_resolved: TBD-on-execute
human_uat_outcome: pending
re_verification: false
human_verification:
  - test: "All 6 clipboard call sites surface a toast within 200ms of click — visually walk: IndexGitNexusButton, InstallGitNexusButton, CoverageFamilySection install hint, CoverageEmptyState install link, CoveragePage row-refresh wiki-compile entry, CoveragePage row-refresh workflow-update entry"
    expected: "Each click → toast appears at top-16 right-4 with the contextual message (CLI command → 'paste in terminal to ...'); fades out after 2.4s; no toast queue (rapid double-clicks replace)"
    why_human: "Visual feedback timing + per-site wording can only be verified live"

  - test: "Column-width lock at 1440×900 — open /coverage, scroll the matrix, visually trace any column edge top-to-bottom across all 3 family sections; edges must align exactly"
    expected: "Repo column 288px, CLAUDE.md / GitNexus / Workflow 128px each, Wiki 352px, Actions 32px — identical across agenticapps, factiv, neuroflash"
    why_human: "Visual column-edge alignment cannot be programmatically asserted at the pixel level without a browser engine; structural assertion confirms <col> classes match"

  - test: "Sticky toolbar at scrollY = 0, 200, 400, 800, 1200, 1600 — PageHeader + CoverageToolbar (chips + search) + family-header + column-headers all flush at every position"
    expected: "All four sticky-stack elements remain visible AND flush (no transparent gap, no overlap) at every scroll position"
    why_human: "Sticky layering regression requires real browser scroll; Phase 11's post-UAT stack contract must hold under the new --ph-h-driven calc()"

  - test: "Token contrast — open /coverage at 1440×900 and visually confirm text-text-tertiary is darker than prior; still distinguishable from text-text-secondary (sidebar section labels vs active item)"
    expected: "Sidebar WORKSPACE/OBSERVABILITY/ACCOUNT labels (tertiary) readable but lighter than the active item label (primary); tier hierarchy reads cleanly"
    why_human: "Color perception + tier hierarchy require live render at brand-target viewport"

  - test: "IMPECCABLE re-pass — run /impeccable critique on /coverage at 1440×900 (HEAD = phase 11.1 branch); record composite + per-heuristic score; deterministic detector MUST report zero low-contrast findings on text-text-tertiary"
    expected: "Composite ≥ 80 (calibration band — RECORD actual; do not gate); zero low-contrast findings; Nielsen #1 (System Status) ≥ 3 (toast); Nielsen #4 (Consistency) ≥ 3 (col-width lock)"
    why_human: "IMPECCABLE skill is human-invoked per D-10.5-02; produces calibration data point #3 for D-10.5-03"

  - test: "Regression smoke on /, /projects, /help, /settings — text-text-tertiary usage renders without visual artifact; PageHeader sticky-mode is opt-in only (default sticky=false routes unchanged)"
    expected: "No visual regression on any non-Coverage route from the token swap or PageHeader hook addition"
    why_human: "Cross-route regression validation requires manual walkthrough"
---

# Phase 11.1: Impeccable P1 Polish Bundle — Verification Report

**Phase Goal:** Lift the IMPECCABLE composite of `/coverage` from 76 (Phase 11 calibration data point #2) toward ~82 by closing the 4 inherited P1s surfaced in `11-IMPECCABLE.md`:

1. Column-width drift in `CoverageFamilySection` (locked via shared `<colgroup>` + `table-fixed` consuming `coverageColumns.ts` SoT).
2. Non-sticky `CoverageToolbar` (folded into `PageHeader` children slot; layering-stack constants switched to `calc(var(--ph-h) ± ...)` driven by new `usePageHeaderHeight` ResizeObserver hook).
3. Missing clipboard-write feedback (new `Toast` primitive — provider + portal + hook — wired at 6 call sites with contextual per-site wording).
4. `--color-text-tertiary` contrast failing WCAG AA (4.5:1) — swapped `#807A92` → `#75708A` (4.69:1 vs app-bg, 4.64:1 vs sidebar-bg) with a new `verify-contrast.test.ts` invariant lock.

Produces calibration data point #3 toward settling D-10.5-03 (provisional 87 floor recalibration).

## Verification matrix (filled during execution)

| Requirement | Must-haves | Evidence-1 (test) | Evidence-2 (visual/runtime) | Status |
|---|---|---|---|---|
| IMP-01 (column-width lock) | colgroup + table-fixed + COVERAGE_COL_WIDTHS SoT consumed by CoverageFamilySection + CoverageRow | TBD — Plan 02 commit refs | TBD — screenshots 02-col-widths-{0,400,800}.png | pending |
| IMP-02 (sticky toolbar) | CoverageToolbar inside sticky PageHeader subtree; family/column top- via calc(var(--ph-h)) | TBD — Plan 04 + 05 commit refs | TBD — screenshots 05-sticky-stack-{0,200,400,800,1200,1600}.png | pending |
| IMP-03 (toast feedback) | ToastProvider wraps AppShellV2; 6 call sites fire useToast().show() | TBD — Plan 03 + 06 commit refs | TBD — screenshots 06-toast-{site}.png × 6 | pending |
| IMP-04 (token swap) | --color-text-tertiary = #75708A with comment block update | TBD — Plan 01 commit refs | TBD — visual side-by-side before/after | pending |
| IMP-05 (contrast invariant) | verify-contrast.test.ts asserts all text-* tokens vs both bgs | TBD — Plan 01 commit refs | N/A (test IS the lock) | pending |

## Plan-level evidence (filled during execution)

- **Plan 01:** `[commit-sha]` `[commit-sha]` `[commit-sha]` `[commit-sha]` — Token swap + contrast lib + verify-contrast invariant
- **Plan 02:** `[commit-sha]` ... — coverageColumns SoT + CoverageFamilySection colgroup + CoverageRow track adoption
- **Plan 03:** `[commit-sha]` `[commit-sha]` — Toast primitive + tests
- **Plan 04:** `[commit-sha]` × 4 — usePageHeaderHeight hook + tokens.css default + PageHeader binding
- **Plan 05:** `[commit-sha]` × 4 — CoverageToolbar fold + sticky top- switch
- **Plan 06:** `[commit-sha]` × 6 — ToastProvider mount + 6 clipboard wirings

## Phase regression contract (must hold)

- Phase 11 post-UAT sticky stack: PageHeader + family-header + column-headers flush at scrollY {0, 200, 400, 800, 1200, 1600} at 1440×900. After Plan 05 lands, the same must remain true with the new combined-height PageHeader (toolbar inside) — only the `top-` values change, not the flush contract.
- Phase 11 `CoverageDriftBadge` rendering unchanged.
- Phase 11 row-refresh `opacity-30` unchanged.
- Phase 11 `CoveragePage` `sticky={true}` opt-in unchanged (still applies; PageHeader's hook publishes `--ph-h` regardless of sticky mode).
- All ~894 existing SPA tests still green after each plan.

## Out of scope (deferred — record in STATE.md "Pending Todos" after merge)

- P2 #5 column-header tooltips
- P2 #6 family-aggregate semantic redesign
- P2 #7 PageHeader subtitle `max-w-prose` (fold in only if Plan 05 finds it incidental; otherwise defer)
- P3 #8 collapse uniformly-missing columns
- `CodeBlock.tsx` + `MaskedToken.tsx` clipboard feedback (not on Coverage page)
- iPad touch target on row-refresh icon

## Calibration data point #3 (D-10.5-03)

`11.1-IMPECCABLE.md` will be produced by `/impeccable critique` AFTER execution.

Predicted composite band: 78-84 (Nielsen 24 → 30/40 via P1 closures; deterministic detector zero contrast findings; Phase-11-inherited issues not addressed remain).

If composite lands ≥ 85: opens Option A path on D-10.5-03 (keep 87 floor).
If composite lands 78-84: confirms Option B path (recalibrate floor to ~80 + waiver clause).
If composite lands < 78: confirms Option D path (per-surface waiver list — calibration is structurally broken).

ADR addendum `docs/decisions/NNNN-phase-11-1-impeccable-floor-recalibration.md` will be drafted post-IMPECCABLE based on the data.
