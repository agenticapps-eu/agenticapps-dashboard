---
phase: 11.1-impeccable-p1-polish-bundle
verified: 2026-05-18T14:35:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "--color-text-tertiary token value is #75708A"
    reason: "PD-11.1-09 deviation: real WCAG 2.1 math showed #75708A only reaches 4.38:1 on sidebar-bg (< AA floor). Executor selected #706B85 (4.86 / 4.71 on both backgrounds) which satisfies IMP-04 and IMP-05 more strictly than the plan-time approximation. Deviation is fully documented in 11.1-01-SUMMARY.md and tokens.css comment block."
    accepted_by: "gsd-verifier"
    accepted_at: "2026-05-18T14:35:00Z"
re_verification: false
human_verification:
  - test: "All 6 clipboard call sites surface a toast within 200ms of click — visually walk: IndexGitNexusButton, InstallGitNexusButton, CoverageEmptyState install link, CoverageFamilySection install hint, CoveragePage row-refresh wiki-compile entry, CoveragePage row-refresh workflow-update entry"
    expected: "Each click -> toast appears at top-16 right-4 with the contextual message (CLI command -> 'paste in terminal to ...'); fades out after 2.4s; no toast queue (rapid double-clicks replace)"
    why_human: "Visual feedback timing + per-site wording can only be verified live"

  - test: "Column-width lock at 1440x900 -- open /coverage, scroll the matrix, visually trace any column edge top-to-bottom across all 3 family sections; edges must align exactly"
    expected: "Repo column 288px, CLAUDE.md / Workflow 128px each, GitNexus 144px, Wiki 352px, Actions 32px -- identical across agenticapps, factiv, neuroflash"
    why_human: "Visual column-edge alignment cannot be programmatically asserted at the pixel level without a browser engine; structural assertion confirms colgroup classes match"

  - test: "Sticky toolbar at scrollY = 0, 200, 400, 800, 1200, 1600 -- PageHeader + CoverageToolbar (chips + search) + family-header + column-headers all flush at every position"
    expected: "All four sticky-stack elements remain visible AND flush (no transparent gap, no overlap) at every scroll position"
    why_human: "Sticky layering regression requires real browser scroll; Phase 11 post-UAT stack contract must hold under the new --ph-h-driven calc()"

  - test: "Token contrast -- open /coverage at 1440x900 and visually confirm text-text-tertiary is darker than prior; still distinguishable from text-text-secondary (sidebar section labels vs active item)"
    expected: "Sidebar WORKSPACE/OBSERVABILITY/ACCOUNT labels (tertiary) readable but lighter than the active item label (primary); tier hierarchy reads cleanly"
    why_human: "Color perception + tier hierarchy require live render at brand-target viewport"

  - test: "IMPECCABLE re-pass -- run /impeccable critique on /coverage at 1440x900 (HEAD = phase 11.1 branch); record composite + per-heuristic score; deterministic detector MUST report zero low-contrast findings on text-text-tertiary"
    expected: "Composite >= 80 (calibration band -- RECORD actual; do not gate); zero low-contrast findings; Nielsen #1 (System Status) >= 3 (toast); Nielsen #4 (Consistency) >= 3 (col-width lock)"
    why_human: "IMPECCABLE skill is human-invoked per D-10.5-02; produces calibration data point #3 for D-10.5-03"

  - test: "Regression smoke on /, /projects, /help, /settings -- text-text-tertiary usage renders without visual artifact; PageHeader sticky-mode is opt-in only (default sticky=false routes unchanged)"
    expected: "No visual regression on any non-Coverage route from the token swap or PageHeader hook addition"
    why_human: "Cross-route regression validation requires manual walkthrough"
---

# Phase 11.1: Impeccable P1 Polish Bundle -- Verification Report

**Phase Goal:** Lift the IMPECCABLE composite of `/coverage` from 76 (Phase 11 calibration data point #2) toward ~82 by closing the 4 inherited P1s surfaced in `11-IMPECCABLE.md`:

1. Column-width drift in `CoverageFamilySection` (locked via shared `<colgroup>` + `table-fixed` consuming `coverageColumns.ts` SoT).
2. Non-sticky `CoverageToolbar` (folded into `PageHeader` children slot; layering-stack constants switched to `calc(var(--ph-h) +/- ...)` driven by new `usePageHeaderHeight` ResizeObserver hook).
3. Missing clipboard-write feedback (new `Toast` primitive -- provider + portal + hook -- wired at 6 call sites with contextual per-site wording).
4. `--color-text-tertiary` contrast failing WCAG AA (4.5:1) -- swapped `#807A92` -> `#706B85` (4.86:1 vs app-bg, 4.71:1 vs sidebar-bg) via PD-11.1-09 deviation; new `verify-contrast.test.ts` invariant lock.

Produces calibration data point #3 toward settling D-10.5-03.

**Verified:** 2026-05-18T14:35:00Z
**Status:** human_needed (all structural must-haves VERIFIED; 6 human/visual/calibration items remain)
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 3 CoverageFamilySection tables render identical column widths via shared colgroup SoT | VERIFIED | `coverageColumns.ts` COVERAGE_COL_WIDTHS frozen object (6 keys); `CoverageFamilySection.tsx:175-183` has `<colgroup>` + `table-fixed`; `CoverageRow.tsx:104-152` stamps all 6 `<td>` with COVERAGE_COL_WIDTHS |
| 2 | CoverageToolbar is visible at every scrollY (sticky, inside PageHeader) | VERIFIED | `CoveragePage.tsx:265-288` renders `<CoverageToolbar>` as child of `<PageHeader sticky={true}>`; `CoverageFamilySection.tsx:124,186-191` uses `calc(var(--ph-h)-1.5rem)` / `calc(var(--ph-h)+1.5625rem)`; `usePageHeaderHeight.ts` publishes `--ph-h` via ResizeObserver; `tokens.css:83` has `--ph-h: 56px` default |
| 3 | Every clipboard write surfaces a toast at all 6 call sites | VERIFIED | `AppShellV2.tsx:32,57` wraps with `<ToastProvider>`; `IndexGitNexusButton.tsx:20,23,29`, `InstallGitNexusButton.tsx:18,21,27`, `CoverageEmptyState.tsx:19,34,65`, `CoverageFamilySection.tsx:37,92,157`, `CoveragePage.tsx:30,87,148,157` all import + call `useToast()` + `toast.show()`; PD-11.1-02 wording confirmed ("index your repos with GitNexus", not "45 repos") |
| 4 | --color-text-tertiary clears WCAG AA 4.5:1 on both backgrounds | VERIFIED | `tokens.css:41` value is `#706B85` (PD-11.1-09 deviation from plan's `#75708A`); computed 4.86:1 vs app-bg, 4.71:1 vs sidebar-bg; both clear AA floor; 6/6 verify-contrast.test.ts assertions pass |
| 5 | Token-contrast invariant locked in CI via verify-contrast.test.ts | VERIFIED | `packages/spa/src/styles/verify-contrast.test.ts` exists and asserts all 3 `text-*` tiers against `--color-app-bg` AND `--color-sidebar-bg` with per-tier floors (primary >=13, secondary >=4.5, tertiary >=4.5); reads tokens.css via readFileSync+regex; zero deps |

**Score:** 5/5 truths verified

### Token Deviation Note (IMP-04)

The VERIFICATION.md scaffold and plan specified `#75708A` as the target token value. PD-11.1-09 (documented in `11.1-01-SUMMARY.md` and `tokens.css` comment block) changed this to `#706B85` because real WCAG 2.1 math showed `#75708A` reaches only 4.38:1 on sidebar-bg (below 4.5:1 AA floor). `#706B85` achieves 4.86/4.71 on both backgrounds -- a stricter satisfaction of IMP-04. The deviation serves the requirement's intent more faithfully than the plan's literal value. Override applied.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/spa/src/lib/contrast.ts` | Pure-JS WCAG helpers (hexToRgb, relativeLuminance, contrastRatio) | VERIFIED | 35 LOC; 3 exports; zero deps; confirmed on disk |
| `packages/spa/src/lib/contrast.test.ts` | Unit tests against canonical WCAG pairs | VERIFIED | 13 assertions; confirmed on disk |
| `packages/spa/src/styles/verify-contrast.test.ts` | Token-pair invariant (6 assertions) | VERIFIED | 6/6 pass; reads tokens.css via readFileSync; confirmed on disk |
| `packages/spa/src/styles/tokens.css` | --color-text-tertiary swapped + --ph-h default added | VERIFIED | `#706B85` at line 41; `--ph-h: 56px` at line 83; comment block documents deviation |
| `packages/spa/src/components/panels/coverage/coverageColumns.ts` | COVERAGE_COL_WIDTHS frozen SoT (6 keys) | VERIFIED | Object.freeze + as const; 12 LOC; gitNexus bumped to w-36 per PD-11.1-06 |
| `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` | colgroup + table-fixed consuming SoT | VERIFIED | Import at line 36; `table-fixed` at line 175; `<colgroup>` lines 176-183 |
| `packages/spa/src/components/panels/coverage/CoverageRow.tsx` | All 6 td elements stamped with SoT widths | VERIFIED | Import at line 18; all 6 `<td>` classNames use COVERAGE_COL_WIDTHS.* |
| `packages/spa/src/components/ui/Toast.tsx` | ToastProvider + useToast + portal (91 LOC) | VERIFIED | Confirmed on disk; 5 exports |
| `packages/spa/src/components/ui/Toast.test.tsx` | 11 contract assertions | VERIFIED | Confirmed on disk |
| `packages/spa/src/components/ui/usePageHeaderHeight.ts` | ResizeObserver -> --ph-h publisher (22 LOC) | VERIFIED | Confirmed on disk; PD-11.1-01 cleanup on unmount |
| `packages/spa/src/components/ui/usePageHeaderHeight.test.tsx` | 5 hook contract assertions | VERIFIED | Confirmed on disk |
| `packages/spa/src/vitest.setup.ts` | Global MockResizeObserver for test harness | VERIFIED | Confirmed on disk; imported by test-setup.ts |
| `packages/spa/src/components/ui/PageHeader.tsx` | usePageHeaderHeight binding + data-testid | VERIFIED | Line 25 import; line 49 binding; line 51 testId logic; line 53 data-testid |
| `packages/spa/src/components/panels/coverage/CoveragePage.tsx` | CoverageToolbar inside PageHeader children | VERIFIED | Lines 282-288: CoverageToolbar as child of PageHeader |
| `packages/spa/src/components/AppShellV2.tsx` | Wrapped with ToastProvider | VERIFIED | Line 26 import; lines 32,57 provider wrapping |
| `packages/spa/src/components/panels/coverage/IndexGitNexusButton.tsx` | toast wired (call site 1) | VERIFIED | Lines 20,23,29: import + hook + show() |
| `packages/spa/src/components/panels/coverage/InstallGitNexusButton.tsx` | toast wired (call site 2) | VERIFIED | Lines 18,21,27: import + hook + show() |
| `packages/spa/src/components/panels/coverage/CoverageEmptyState.tsx` | toast wired (call site 3) | VERIFIED | Lines 19,34,65: import + hook + show() |
| `packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` | toast wired (call site 4) | VERIFIED | Lines 37,92,157: import + hook + show() |
| `packages/spa/src/components/panels/coverage/CoveragePage.tsx` | toast wired x2 (call sites 5+6) | VERIFIED | Lines 30,87,148,157: import + hook + show() x2 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CoverageFamilySection.tsx` | `coverageColumns.ts` | import + colgroup + table-fixed | WIRED | Import line 36; colgroup lines 176-183; table-fixed line 175 |
| `CoverageRow.tsx` | `coverageColumns.ts` | import + td className stamps | WIRED | Import line 18; all 6 td use COVERAGE_COL_WIDTHS.* |
| `CoveragePage.tsx` | `PageHeader` children slot | `<CoverageToolbar>` as JSX child | WIRED | Lines 282-288 confirmed |
| `PageHeader.tsx` | `usePageHeaderHeight` | useRef + hook call + --ph-h | WIRED | Lines 25,49; publishes to document.documentElement |
| `CoverageFamilySection.tsx` | `--ph-h` CSS var | `calc(var(--ph-h)-1.5rem)` / `calc(var(--ph-h)+1.5625rem)` | WIRED | Line 124 (family-header); lines 186-191 (column-headers) |
| `AppShellV2.tsx` | `ToastProvider` | wraps outermost return | WIRED | Lines 32,57; every authenticated route inherits context |
| All 6 clipboard call sites | `useToast().show()` | async onClick await + toast.show() | WIRED | All 6 files confirmed above |
| `verify-contrast.test.ts` | `tokens.css` | readFileSync + regex extract | WIRED | confirmed in test source |
| `verify-contrast.test.ts` | `contrast.ts` | import contrastRatio | WIRED | line 23 of test file |

### Data-Flow Trace (Level 4)

Level 4 data-flow trace is not applicable to this phase: all 5 must-haves are structural/token/primitive changes, not dynamic data pipelines. The artifacts that render data (CoverageFamilySection, CoveragePage) were already verified as data-flowing in Phase 10/11. Phase 11.1 changes only: CSS layout contracts, a CSS-var publisher, a toast primitive, and a token literal.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 1966 tests pass | `pnpm -r test` | shared: 234/234, agent: 746/746, spa: 955/955, meta-observer: 31/31 = 1966 total | PASS |
| Typecheck clean | `pnpm -r typecheck` | All 5 packages clean (agentlinter, shared, spa, agent, meta-observer) | PASS |
| Build clean | `pnpm -r build` | All packages build successfully (chunk-size warning is pre-existing, not a build error) | PASS |
| No hardcoded "45 repos" | `grep -rn "index 45 repos"` | Only found in test comment (IndexGitNexusButton.test.tsx line 4) -- NOT in implementation code | PASS |
| Token value is not old value | `grep "#807A92" tokens.css` | Only appears in historical comment block documenting v1.0.1 era value -- NOT as the active token | PASS |
| No new gitNexusInstalled boolean references | `grep -rn "gitNexusInstalled"` | 4 hits -- all in comments (JSDoc, test description text); zero in Phase 11.1 functional code | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| IMP-01 | Plan 02 | CoverageFamilySection columns identical pixel widths via colgroup SoT | SATISFIED | coverageColumns.ts + colgroup + table-fixed + CoverageRow td stamps -- all 5 plan-02 commits in git log |
| IMP-02 | Plans 04+05 | CoverageToolbar visible at every scrollTop (inside sticky PageHeader) | SATISFIED | CoverageToolbar inside PageHeader children; calc(var(--ph-h)) offsets; usePageHeaderHeight publishes --ph-h; --ph-h default in tokens.css |
| IMP-03 | Plans 03+06 | Every clipboard write surfaces visible toast (success/error) within 200ms | SATISFIED | Toast.tsx primitive; AppShellV2 wrapped with ToastProvider; all 6 call sites wired; PD-11.1-05 timing test (fireEvent.click + fake timers) in suite |
| IMP-04 | Plan 01 | --color-text-tertiary clears WCAG AA 4.5:1 on both backgrounds | SATISFIED | #706B85 (PD-11.1-09 deviation from #75708A); 4.86:1 vs app-bg, 4.71:1 vs sidebar-bg; both > 4.5:1 floor |
| IMP-05 | Plan 01 | Token-contrast invariant locked (verify-contrast.test.ts asserts all text-* tiers) | SATISFIED | verify-contrast.test.ts: 6 assertions, 6 passing; runs in CI; closes v1.0.1 -> v1.1 regression class |

### Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| `CoveragePage.tsx:242,257` | `<PageHeader title="Coverage" sticky={true} />` (self-closing, no toolbar) | INFO | Intentional -- loading/error/empty branches have no filter data to display; plan decision documented in 11.1-05-SUMMARY |
| `packages/spa` build output | Chunk size warning (cytoscape.esm, chunk-NNHCCRGN > 500KB) | INFO | Pre-existing; not introduced by Phase 11.1; not a build error |

No stub implementations found. No TODO/FIXME/placeholder patterns introduced. No hardcoded empty arrays/objects used as live data sources.

### Phase 11 Regression Contract (must hold)

| Phase 11 deliverable | Status | Evidence |
|---|---|---|
| Sticky PageHeader opt-in (PLI-01, D-11-09, default false) | VERIFIED | `PageHeader.tsx:50` sticky classes intact; `sticky={false}` default at line 46; data-testid logic at line 51 |
| CoverageRow row-refresh opacity-30 (PLI-02, D-11-10) | VERIFIED | `CoverageRow.tsx:158` `opacity-30` in className; `group-hover:opacity-100 focus-within:opacity-100 focus:opacity-100` bump intact |
| Option C single-ownership drift model | NOT CHECKED (pre-existing Phase 11 contract -- no Phase 11.1 touches to CoverageCell or useCoverageHistory) | Unchanged from Phase 11 pass |
| CoverageDriftBadge naming (NOT InlineDrift) | VERIFIED | `CoverageDriftBadge.tsx` exists on disk |
| Sidebar Skill drift as 2nd Observability entry (D-11-08) | VERIFIED | `Sidebar.tsx:66-80` shows Coverage + Skill drift peers under Observability section |
| /coverage sticky={true} opt-in unchanged | VERIFIED | `CoveragePage.tsx:268` `sticky={true}` still passed to PageHeader |

### Human Verification Required

**1. Toast visual feedback (all 6 call sites)**

**Test:** Open /coverage in browser; click each of the 6 clipboard CTAs: IndexGitNexusButton (top-right install CTA), InstallGitNexusButton, CoverageEmptyState install button, CoverageFamilySection install hint, CoveragePage row-refresh wiki-compile, CoveragePage row-refresh workflow-update.
**Expected:** Toast appears at top-16 right-4 with contextual message containing "paste in terminal to ..."; fades after 2.4s; rapid double-clicks replace (no stacking queue). Error variant (clipboard failure): "Copy failed -- open the help guide for the command."
**Why human:** Visual feedback timing and per-site wording confirmed by tests, but actual visual appearance + timing perception requires browser.

**2. Column-width lock visual (at 1440x900)**

**Test:** Open /coverage at 1440x900; scroll the matrix; visually trace any column edge top-to-bottom across agenticapps, factiv, neuroflash family sections.
**Expected:** Repo 288px, CLAUDE.md 128px, GitNexus 144px, Wiki 352px, Workflow 128px, Actions 32px -- identical across all 3 sections. No horizontal shift between families.
**Why human:** Pixel-level column edge alignment requires browser rendering; structural colgroup assertions confirm class assignment but not rendered pixel output.

**3. Sticky toolbar stack regression (Phase 11 + Phase 11.1)**

**Test:** Open /coverage; scroll through scrollY = 0, 200, 400, 800, 1200, 1600; confirm: PageHeader + CoverageToolbar (chips + search) + family-header + column-headers all flush at every position. Also test iPad portrait (768x1024).
**Expected:** All four sticky-stack elements remain visible and flush (no transparent gap, no overlap) at every scroll position. calc(var(--ph-h)) offsets correctly track the combined PageHeader+Toolbar height.
**Why human:** Sticky layering regression with ResizeObserver-driven offsets requires real browser scroll behavior.

**4. Token contrast visual tier hierarchy**

**Test:** Open /coverage at 1440x900; inspect sidebar WORKSPACE/OBSERVABILITY/ACCOUNT labels (text-tertiary = #706B85) vs active item labels (text-primary = #1F1B2E).
**Expected:** Tertiary labels readable but distinctly lighter than primary; tier hierarchy reads cleanly; no confusion between secondary and tertiary tiers.
**Why human:** Color perception and inter-tier readability require human judgment at live render.

**5. IMPECCABLE re-pass (calibration data point #3)**

**Test:** Run `/impeccable critique` on /coverage at 1440x900 against HEAD (Phase 11.1 branch); record composite + per-heuristic scores; produce `11.1-IMPECCABLE.md`.
**Expected:** Composite >= 80 (record actual; do not gate); zero low-contrast findings on text-text-tertiary; Nielsen #1 (System Status) >= 3 (toast feedback); Nielsen #4 (Consistency) >= 3 (col-width lock). This settles D-10.5-03 floor recalibration direction.
**Why human:** IMPECCABLE skill is human-invoked per D-10.5-02; produces the required calibration artifact.

**6. Cross-route regression smoke**

**Test:** Walk /, /projects, /help, /settings; confirm no visual regressions from the token swap (#706B85 on tertiary consumers) or PageHeader hook addition (--ph-h published but routes still use sticky=false by default).
**Expected:** No visual regression on any non-Coverage route.
**Why human:** Cross-route visual validation requires manual walkthrough.

### Gaps Summary

No structural gaps found. All 5 must-haves are verified against the actual codebase with direct evidence. The 1966-test suite passes clean; typecheck clean; build clean.

One intentional deviation noted (IMP-04 token value #706B85 vs plan's #75708A) -- fully documented, serves the requirement's intent more strictly, and is override-approved above.

Phase status is `human_needed` solely because 6 human verification items remain (visual/perceptual/calibration checks that cannot be automated).

---

## Verification matrix

| Requirement | Must-haves | Evidence | Status |
|---|---|---|---|
| IMP-01 (column-width lock) | colgroup + table-fixed + COVERAGE_COL_WIDTHS SoT consumed by CoverageFamilySection + CoverageRow | commits 8a28ac7 / e4c9527 / 328cfaa / 1b9ab1a / 8f0f75a; CoverageFamilySection.tsx:175-183; CoverageRow.tsx:104-152; coverageColumns.ts:3-10 | VERIFIED |
| IMP-02 (sticky toolbar) | CoverageToolbar inside sticky PageHeader subtree; family/column top- via calc(var(--ph-h)); usePageHeaderHeight publishes --ph-h; tokens.css --ph-h default | commits ad1bfd5 / 7ec18b9 / f4aa4f2 / 7f24f87 / 758b63a / 5c4e2f6 / cfd43cd / 84ed527 / 9e1d2c1; CoveragePage.tsx:282-288; CoverageFamilySection.tsx:124,186-191; usePageHeaderHeight.ts | VERIFIED |
| IMP-03 (toast feedback) | ToastProvider wraps AppShellV2; 6 call sites fire useToast().show() with contextual wording | commits 623aa7e / a6ad13e / 19e15b4 / 82eb362 / e26e86c / f84fdbb; AppShellV2.tsx:32,57; all 6 component files import + call useToast | VERIFIED |
| IMP-04 (token swap) | --color-text-tertiary = #706B85 (PD-11.1-09 deviation from plan's #75708A); clears AA on both bgs | commits 6dc50f9 / af0b4bf; tokens.css:41 | VERIFIED (override applied) |
| IMP-05 (contrast invariant) | verify-contrast.test.ts asserts all text-* tokens vs both bgs; 6/6 pass | commits e79ef1b / 46ef34c; verify-contrast.test.ts; all 6 assertions green in 1966-test suite | VERIFIED |

## Plan-level commits

- **Plan 01 (token + contrast):** `e79ef1b` `46ef34c` `6dc50f9` `af0b4bf`
- **Plan 02 (column-width lock):** `8a28ac7` `e4c9527` `328cfaa` `1b9ab1a` `8f0f75a`
- **Plan 03 (Toast primitive):** `d7d863d` `0cafc70`
- **Plan 04 (usePageHeaderHeight + --ph-h):** `ad1bfd5` `7ec18b9` `f4aa4f2` `7f24f87` `758b63a`
- **Plan 05 (sticky toolbar + calc stack):** `5c4e2f6` `cfd43cd` `84ed527` `9e1d2c1`
- **Plan 06 (ToastProvider + 6 clipboard wirings):** `623aa7e` `a6ad13e` `19e15b4` `82eb362` `e26e86c` `f84fdbb`

All commits verified in `git log` as of 2026-05-18.

---

_Verified: 2026-05-18T14:35:00Z_
_Verifier: Claude (gsd-verifier)_
