---
phase: 05-skills-health-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/skills.ts
  - packages/shared/src/schemas/agentlinter.ts
  - packages/shared/src/schemas/observability.ts
  - packages/shared/src/schemas/secrets.ts
  - packages/shared/src/schemas/integrations.ts
  - packages/shared/src/schemas/skills.test.ts
  - packages/shared/src/schemas/agentlinter.test.ts
  - packages/shared/src/schemas/observability.test.ts
  - packages/shared/src/schemas/secrets.test.ts
  - packages/shared/src/schemas/integrations.test.ts
  - packages/shared/src/index.ts
  - packages/meta-observer/package.json
  - packages/meta-observer/tsconfig.json
  - packages/meta-observer/vitest.config.ts
  - packages/meta-observer/SKILL.md
  - packages/meta-observer/hooks/session-end.mjs
  - packages/meta-observer/lib/projectRoot.ts
  - packages/meta-observer/lib/atomicWrite.ts
  - packages/meta-observer/lib/extractCommitment.ts
  - packages/meta-observer/lib/extractFirings.ts
  - packages/meta-observer/test/projectRoot.test.ts
  - packages/meta-observer/test/atomicWrite.test.ts
  - packages/meta-observer/test/extractCommitment.test.ts
  - packages/meta-observer/test/extractFirings.test.ts
  - packages/meta-observer/test/__fixtures__/sample-transcript.jsonl
  - packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
threats_addressed:
  - T-5-Meta-Write-Path
  - T-5-Meta-AtomicWrite
  - T-5-Meta-EndToEnd
  - T-5-Drift
must_haves:
  truths:
    - "All 5 wire-shape Zod schemas exist and pass round-trip tests"
    - "Meta-observer workspace package builds via pnpm and is picked up by `pnpm -r test`"
    - "Meta-observer SessionEnd hook reads transcript_path, extracts last `## Workflow commitment` block, atomic-writes `.md` + `.jsonl` to `<projectRoot>/.planning/skill-observations/`"
    - "Project-root resolver prefers CLAUDE_PROJECT_DIR env, falls back to CWD walk-up, returns null when no `.planning/.claude` marker found"
    - "Atomic write (.tmp + rename) — concurrent reader never sees a partial file"
    - "Wave 0 transcript-shape probe captured a real `~/.claude/projects/.../<session>.jsonl` line shape into a fixture"
    - "Wave 0 skill-scoped hook activation probe verified that a SessionEnd hook in skill frontmatter fires (or fallback path documented)"
  artifacts:
    - path: "packages/shared/src/schemas/skills.ts"
      provides: "SkillFrontmatterSchema, SkillEntrySchema, GlobalSkillsResponseSchema, LocalSkillsResponseSchema"
      contains: "GlobalSkillsResponseSchema"
    - path: "packages/shared/src/schemas/agentlinter.ts"
      provides: "AgentLinterSeveritySchema (3-value enum), AgentLinterDiagnosticSchema, AgentLinterReportSchema, AgentLinterResponseSchema (5-class discriminated union)"
      contains: "discriminatedUnion"
    - path: "packages/shared/src/schemas/observability.ts"
      provides: "ObservabilitySignalSchema, ObservabilityToolStateSchema, ObservabilityResponseSchema"
      contains: "ObservabilityResponseSchema"
    - path: "packages/shared/src/schemas/secrets.ts"
      provides: "SecretsResponseSchema (3-state discriminated union)"
      contains: "discriminatedUnion"
    - path: "packages/shared/src/schemas/integrations.ts"
      provides: "IntegrationStateSchema (3-value enum), IntegrationsResponseSchema"
      contains: "IntegrationsResponseSchema"
    - path: "packages/shared/src/index.ts"
      provides: "Re-exports for all 5 new schemas"
      contains: "skills.js"
    - path: "packages/meta-observer/package.json"
      provides: "workspace package manifest"
      contains: "@agenticapps/dashboard-meta-observer"
    - path: "packages/meta-observer/SKILL.md"
      provides: "Claude Code skill manifest with hooks: SessionEnd frontmatter"
      contains: "SessionEnd"
    - path: "packages/meta-observer/hooks/session-end.mjs"
      provides: "SessionEnd hook entry script reading transcript_path"
      contains: "transcript_path"
    - path: "packages/meta-observer/lib/projectRoot.ts"
      provides: "resolveProjectRoot — CLAUDE_PROJECT_DIR first, CWD walk-up fallback"
      contains: "CLAUDE_PROJECT_DIR"
    - path: "packages/meta-observer/lib/atomicWrite.ts"
      provides: "atomicWrite — .tmp + rename"
      contains: "rename"
    - path: "packages/meta-observer/lib/extractCommitment.ts"
      provides: "extractCommitment(transcript) → markdown | null"
      contains: "Workflow commitment"
    - path: "packages/meta-observer/lib/extractFirings.ts"
      provides: "extractFirings(transcript) → HookFiring[]"
      contains: "HookFiringSchema"
    - path: "packages/meta-observer/test/__fixtures__/sample-transcript.jsonl"
      provides: "Real transcript shape probe output"
      contains: "session_id"
  key_links:
    - from: "packages/meta-observer/hooks/session-end.mjs"
      to: "packages/meta-observer/lib/projectRoot.ts"
      via: "imports resolveProjectRoot"
      pattern: "resolveProjectRoot"
    - from: "packages/meta-observer/lib/extractFirings.ts"
      to: "packages/shared/src/schemas/observations.ts"
      via: "imports HookFiringSchema for emitted JSONL validation"
      pattern: "HookFiringSchema"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/schemas/{skills,agentlinter,observability,secrets,integrations}.ts"
      via: "re-export"
      pattern: "schemas/"
---

<objective>
Wave 1: ship the producer half of Phase 5's data plane.

1. Create the 5 new shared Zod schemas (skills, agentlinter, observability, secrets, integrations) so daemon routes and SPA panels in plans 02–05 can compile in parallel against frozen contracts.
2. Stand up `packages/meta-observer/` as a new pnpm workspace package — Claude Code SessionEnd hook + transcript reader + commitment + firings extractors + atomic writer. Closes the deferred DISC-01 / DISC-02 / DISC-03 data path Phase 4 left for "Phase 5+".
3. Run two Wave-0 probes BEFORE coding extractors: (a) sample a real transcript JSONL file shape into a fixture; (b) install a skeleton SessionEnd-hook skill and confirm it fires on a dormant session — fall back to `.claude/settings.json` hook entry if it doesn't.

Purpose: Schemas frozen here are imported by plans 02 (daemon routes) and 04/05 (SPA panels) — fixing the contracts in plan 01 lets Wave 2 work proceed in parallel against type-checked interfaces. Meta-observer ships in plan 01 too because it's mostly orthogonal to the right-column panels (closes the G1 deferral that's been carried since Phase 4) and shares zod via the catalog.

Output: 5 schemas + their tests + index re-export. New `packages/meta-observer/` workspace pkg with hook + 4 lib modules + 4 test files + 2 fixture files. Workspace recognises the new package via `pnpm-workspace.yaml`.

Per **D-5-08 (forward-only, no backfill)**: there is NO task that walks `~/.claude/projects/*/<session>.jsonl` history to retro-populate `.planning/skill-observations/`. The first real Claude session post-install seeds the directory; the dashboard's CommitmentBlock + HookFirings panels stay on their empty-state copy until then.

**Plan scope note:** at 5 tasks + 26 files modified this plan sits at the boundary of scope-sanity; splitting accepted as a tradeoff for Wave-1 parallelism vs Wave-0/extra-plan overhead. Tasks are independently scoped (Wave-0 probes → schemas → workspace bootstrap → extractors+hook → SKILL.md frontmatter); each has its own RED/GREEN cycle and verify gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-health-panels/05-CONTEXT.md
@.planning/phases/05-skills-health-panels/05-RESEARCH.md
@.planning/phases/05-skills-health-panels/05-VALIDATION.md

# Existing patterns to mimic
@packages/shared/src/schemas/observations.ts
@packages/shared/src/schemas/phaseDetail.ts
@packages/shared/src/index.ts
@packages/agent/src/lib/atomicWrite.ts
@packages/agent/src/lib/phaseDetail.ts

<interfaces>
Frozen contracts the rest of Phase 5 will consume. Names match 05-RESEARCH.md §Code Examples lines 707–830. Each schema MUST also export an inferred type (e.g. `export type GlobalSkillsResponse = z.infer<typeof GlobalSkillsResponseSchema>`).

skills.ts:
- SkillFrontmatterSchema: object { name: z.string(), description: z.string().optional(), version: z.string().optional() }.passthrough()
- SkillEntrySchema: SkillFrontmatterSchema.extend({ dir: z.string(), scope: z.enum(['global', 'local']) })
- GlobalSkillsResponseSchema: object { scope: z.literal('global'), skills: z.array(SkillEntrySchema) }
- LocalSkillsResponseSchema: object { scope: z.literal('local'), skills: z.array(SkillEntrySchema) }

agentlinter.ts:
- AgentLinterSeveritySchema: z.enum(['info', 'warning', 'error'])  — exactly 3 values, NOT 4
- AgentLinterDiagnosticSchema: object { severity, category: z.string(), rule: z.string(), file: z.string(), line: z.number().optional(), message: z.string(), fix: z.string().optional() }
- AgentLinterCategoryScoreSchema: object { name, score: z.number(), weight: z.number(), issues: z.number() }
- AgentLinterReportSchema: object { score: z.number(), categories: z.array(AgentLinterCategoryScoreSchema), diagnostics: z.array(AgentLinterDiagnosticSchema), files: z.array(z.string()), timestamp: z.string() }.passthrough()
- AgentLinterResponseSchema: z.discriminatedUnion('kind', [
    { kind: literal('ok'), report: AgentLinterReportSchema, cachedAt: z.string() },
    { kind: literal('not-installed') },
    { kind: literal('timeout') },
    { kind: literal('error'), exitCode: z.number(), stderr: z.string() },
    { kind: literal('unparseable'), exitCode: z.number(), rawStdout: z.string() }
  ])

observability.ts:
- ObservabilitySignalSchema: object { signal: z.enum(['sentry-sdk-dep', 'sentry-cli-script', 'sentryclirc', 'sentry-dsn-env', 'spotlight-dep', 'spotlight-dir', 'sentry-cli-binary', 'sentry-cli-ci']), evidence: z.string() }
- ObservabilityToolStateSchema: object { detected: z.boolean(), signals: z.array(ObservabilitySignalSchema) }
- ObservabilityResponseSchema: object { sentry: ObservabilityToolStateSchema, spotlight: ObservabilityToolStateSchema, sentryCli: ObservabilityToolStateSchema }

secrets.ts:
- SecretsResponseSchema: z.discriminatedUnion('state', [
    { state: literal('present-valid'), workspaceId: z.string(), defaultEnvironment: z.string().optional() },
    { state: literal('present-invalid'), reason: z.string() },
    { state: literal('absent') }
  ])

integrations.ts:
- IntegrationStateSchema: z.enum(['configured', 'present-but-not-configured', 'not-detected'])
- IntegrationsResponseSchema: object { sentry: IntegrationStateSchema, linear: IntegrationStateSchema, infisical: IntegrationStateSchema }

REUSED (do not modify) from packages/shared/src/schemas/observations.ts (Phase 4):
- HookFiringSchema: object { ts: z.string(), skill: z.string(), hook: z.string() }.passthrough()  — meta-observer JSONL output validates against this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 probes — transcript shape + skill-scoped hook activation</name>
  <read_first>
    - .planning/phases/05-skills-health-panels/05-RESEARCH.md §Open Questions 2 + 3 (lines 906–911 and 893)
    - .planning/phases/05-skills-health-panels/05-VALIDATION.md §Wave 0 probes (lines 116–119)
    - ~/.claude/projects/-Users-donald-Sourcecode-agenticapps-dashboard/ (real transcript dir — pick the most recent .jsonl)
  </read_first>
  <files>
    packages/meta-observer/test/__fixtures__/sample-transcript.jsonl,
    packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md
  </files>
  <action>
**Probe A — transcript shape (resolves RESEARCH §Open Question 3 / Assumption A3):**

1. Locate the most-recent transcript JSONL in `~/.claude/projects/-Users-donald-Sourcecode-agenticapps-dashboard/` via `ls -t *.jsonl | head -1`.
2. Read up to the first 50 lines plus any line containing the literal string `## Workflow commitment` (use `grep -n 'Workflow commitment'`).
3. Anonymise: replace any literal API keys, file paths under `/Users/`, or absolute home paths with `<redacted>` placeholders. Keep `session_id`, `transcript_path` shape, message role markers, tool_use/tool_result envelopes, hook event envelopes intact.
4. Write the anonymised excerpt to `packages/meta-observer/test/__fixtures__/sample-transcript.jsonl` (one JSON object per line; UTF-8; LF endings).
5. At the top of the fixture file (as a comment line `// transcript-shape probe — captured YYYY-MM-DD ...`) document the actual JSONL line shape: which top-level keys appear (`type`, `role`, `message`, `parentUuid`, `timestamp`, etc.), where the markdown body lives (e.g. `message.content[].text`), and whether tool calls are nested (`content[].input` for tool_use, `content[].content` for tool_result).

**Probe B — skill-scoped SessionEnd hook activation (resolves RESEARCH §Open Question 2 / Assumption A2):**

1. Create a skeleton SKILL.md at a temp dir (NOT under this repo's `.claude/skills/`) with frontmatter declaring `name: probe-meta-observer`, `disable-model-invocation: true`, and a `hooks: SessionEnd:` block whose command appends `fired-$(date -Iseconds)` to `/tmp/agentic-probe-skill-hook.log` via a bash one-liner with timeout: 10.
2. Install via `claude skill install <temp-dir>` (or copy to `~/.claude/skills/probe-meta-observer/SKILL.md`).
3. Run a one-shot Claude session that does NOT invoke this skill (e.g. `claude -p "echo hello"` or open + close an interactive session that asks an unrelated question). End the session.
4. Inspect `/tmp/agentic-probe-skill-hook.log`. If the file appears with a `fired-...` line → skill-scoped SessionEnd hooks fire on dormant skills (the desired path). If absent → fall back required.
5. Write `packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md` documenting the result. Required sections: `## Probe outcome` (`fires-when-dormant` | `does-not-fire-when-dormant`), `## Evidence` (paste the log line or a screenshot path), `## Implementation path locked` (one of: "skill frontmatter `hooks: SessionEnd` — primary path" | "fall back: declare hook in `.claude/settings.json` per project; SKILL.md becomes a docs marker only").
6. Uninstall the probe skill: `claude skill uninstall probe-meta-observer` (or `rm -rf ~/.claude/skills/probe-meta-observer`).

**Both probe outputs are inputs for Task 3 (extractors + hook script) and Task 4 (SKILL.md frontmatter format). Do NOT proceed to Tasks 1–4 without both fixtures committed.**

Per D-5-07: if Probe B's outcome is `does-not-fire-when-dormant`, downstream task 4 must declare the hook in `.claude/settings.json` instead of SKILL.md frontmatter; the install path becomes a per-project copy of `meta-observer/.claude/settings.json` rather than `claude skill install`.
  </action>
  <verify>
    <automated>test -f packages/meta-observer/test/__fixtures__/sample-transcript.jsonl &amp;&amp; test -f packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md &amp;&amp; grep -q 'transcript-shape probe' packages/meta-observer/test/__fixtures__/sample-transcript.jsonl &amp;&amp; grep -qE '## Probe outcome|## Implementation path locked' packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/meta-observer/test/__fixtures__/sample-transcript.jsonl` exists, contains at least 5 JSONL lines plus a leading `// transcript-shape probe` comment.
    - The fixture's leading comment names the markdown-body path (e.g. `message.content[].text`) — grep `'message.content'` OR `'content.*text'` succeeds.
    - File `packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md` exists with `## Probe outcome` heading containing one of `fires-when-dormant` OR `does-not-fire-when-dormant`.
    - The probe markdown also contains `## Implementation path locked` followed by a single sentence naming either `skill frontmatter` OR `.claude/settings.json` as the chosen path.
    - No API keys, no `/Users/donald/...` paths leak into either fixture (`grep -E 'sk-|/Users/donald'` returns no matches).
  </acceptance_criteria>
  <done>Both Wave-0 probe fixtures committed; subsequent task can read them to lock implementation choices for hook script + extractors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: 5 new shared schemas + their tests + index re-export</name>
  <read_first>
    - packages/shared/src/schemas/observations.ts (Phase 4 prior art — passthrough pattern + ObservationsRecentResponseSchema)
    - packages/shared/src/schemas/phaseDetail.ts (Phase 4 — discriminated union pattern)
    - packages/shared/src/index.ts (barrel re-export pattern)
    - .planning/phases/05-skills-health-panels/05-RESEARCH.md §Code Examples / Zod schemas (lines 707–830)
    - .planning/phases/05-skills-health-panels/05-CONTEXT.md §"Established Patterns" — Phase 0 D-04 catalog versions, Phase 0 D-06 single-source-of-truth, Phase 1 D-16 outbound parse
  </read_first>
  <files>
    packages/shared/src/schemas/skills.ts,
    packages/shared/src/schemas/skills.test.ts,
    packages/shared/src/schemas/agentlinter.ts,
    packages/shared/src/schemas/agentlinter.test.ts,
    packages/shared/src/schemas/observability.ts,
    packages/shared/src/schemas/observability.test.ts,
    packages/shared/src/schemas/secrets.ts,
    packages/shared/src/schemas/secrets.test.ts,
    packages/shared/src/schemas/integrations.ts,
    packages/shared/src/schemas/integrations.test.ts,
    packages/shared/src/index.ts
  </files>
  <behavior>
    - skills.test.ts: SkillFrontmatterSchema parses `{ name: 'foo' }` (only `name` required), preserves `paths` and `disable-model-invocation` via passthrough; SkillEntrySchema requires `dir` + `scope` ∈ `{global, local}`; GlobalSkillsResponseSchema rejects `scope: 'local'`; LocalSkillsResponseSchema rejects `scope: 'global'`.
    - agentlinter.test.ts: AgentLinterSeveritySchema accepts EXACTLY `info | warning | error` and rejects `medium`/`low`/`critical`; AgentLinterDiagnosticSchema requires severity+category+rule+file+message; AgentLinterReportSchema parses the verified live JSON shape from RESEARCH.md lines 668–702 (score/categories/diagnostics/files/timestamp); AgentLinterResponseSchema discriminates on `kind` — round-trips all 5 variants (`ok`, `not-installed`, `timeout`, `error`, `unparseable`).
    - observability.test.ts: ObservabilitySignalSchema accepts each of the 8 enumerated signal names (`sentry-sdk-dep`, `sentry-cli-script`, `sentryclirc`, `sentry-dsn-env`, `spotlight-dep`, `spotlight-dir`, `sentry-cli-binary`, `sentry-cli-ci`) and rejects unknowns; ObservabilityResponseSchema requires the three top-level keys `sentry/spotlight/sentryCli`.
    - secrets.test.ts: SecretsResponseSchema discriminates on `state`; `present-valid` requires `workspaceId: string`; `present-invalid` requires `reason: string`; `absent` rejects extra payload fields beyond `state`.
    - integrations.test.ts: IntegrationStateSchema accepts EXACTLY `configured | present-but-not-configured | not-detected`; IntegrationsResponseSchema requires the three keys `sentry/linear/infisical`.
    - All test files: at least one round-trip test (parse → JSON.stringify → JSON.parse → re-parse → deep-equal) per top-level response schema, asserting wire-shape stability.
  </behavior>
  <action>
RED: Write all 5 `.test.ts` files first (vitest). Each file imports its sibling `.ts` schema (file does not yet exist → import error) AND asserts the behaviours listed in `<behavior>`. Run `pnpm --filter @agenticapps/dashboard-shared test --run src/schemas/{skills,agentlinter,observability,secrets,integrations}.test.ts` and confirm all 5 fail with module-not-found OR assertion errors.

GREEN: Create the 5 schema `.ts` files using the EXACT signatures in the `<interfaces>` block above (cite 05-RESEARCH.md §Code Examples lines 707–830). Use `import { z } from 'zod'` (catalog version). For `AgentLinterReportSchema` use `.passthrough()` so future AgentLinter version bumps don't trigger schema drift (per D-4-06 philosophy reused). For `SkillFrontmatterSchema` use `.passthrough()` so frontmatter fields like `paths`, `disable-model-invocation`, `allowed-tools` survive validation untouched.

REFACTOR: Update `packages/shared/src/index.ts` to add five re-export blocks at the end, alphabetised after the existing exports. Each new schema module gets two re-export blocks (value re-export for the schemas, type re-export for the inferred types). Names exported (per the `<interfaces>` block):
- skills.js: SkillFrontmatterSchema, SkillEntrySchema, GlobalSkillsResponseSchema, LocalSkillsResponseSchema (+ corresponding types)
- agentlinter.js: AgentLinterSeveritySchema, AgentLinterDiagnosticSchema, AgentLinterCategoryScoreSchema, AgentLinterReportSchema, AgentLinterResponseSchema (+ corresponding types)
- observability.js: ObservabilitySignalSchema, ObservabilityToolStateSchema, ObservabilityResponseSchema (+ corresponding types)
- secrets.js: SecretsResponseSchema (+ SecretsResponse type)
- integrations.js: IntegrationStateSchema, IntegrationsResponseSchema (+ corresponding types)

Each schema file MUST also `export type X = z.infer<typeof XSchema>` for the response schema (and any caller-facing intermediate). Do NOT add new dependencies — `zod` ^3.25 is already in catalog (per RESEARCH.md §Standard Stack).

CRITICAL: Re-running `pnpm --filter @agenticapps/dashboard-shared typecheck && pnpm --filter @agenticapps/dashboard-shared test` MUST pass green at the end of this task, with exactly the 5 new test files contributing positive cases. Existing 822-test baseline stays green.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-shared test --run src/schemas/skills.test.ts src/schemas/agentlinter.test.ts src/schemas/observability.test.ts src/schemas/secrets.test.ts src/schemas/integrations.test.ts &amp;&amp; pnpm --filter @agenticapps/dashboard-shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Files `packages/shared/src/schemas/{skills,agentlinter,observability,secrets,integrations}.ts` all exist.
    - `grep -E "z\.discriminatedUnion\('kind'" packages/shared/src/schemas/agentlinter.ts` matches.
    - `grep -E "z\.enum\(\[.info., .warning., .error.\]" packages/shared/src/schemas/agentlinter.ts` matches.
    - `grep -E "z\.discriminatedUnion\('state'" packages/shared/src/schemas/secrets.ts` matches.
    - `grep -E "z\.enum\(\[.configured., .present-but-not-configured., .not-detected.\]" packages/shared/src/schemas/integrations.ts` matches.
    - `packages/shared/src/index.ts` re-exports all 5 schemas — `grep -c "from './schemas/\(skills\|agentlinter\|observability\|secrets\|integrations\)\.js'" packages/shared/src/index.ts` ≥ 10 (5 value + 5 type re-exports).
    - All 5 new test files exist with at least 4 `it(` blocks each.
    - `pnpm --filter @agenticapps/dashboard-shared test --run src/schemas/skills.test.ts` exits 0.
    - `pnpm --filter @agenticapps/dashboard-shared test --run src/schemas/agentlinter.test.ts` exits 0.
    - Full repo `pnpm -r typecheck` exits 0 (so plans 02–05 in Wave 2 can import from `@agenticapps/dashboard-shared`).
  </acceptance_criteria>
  <done>5 schemas + tests + index re-exports landed; downstream plans 02–05 can `import { GlobalSkillsResponseSchema } from '@agenticapps/dashboard-shared'` without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap `packages/meta-observer/` workspace package + projectRoot resolver + atomicWrite</name>
  <read_first>
    - pnpm-workspace.yaml (root file — confirm pattern for adding `packages/meta-observer`)
    - packages/agent/package.json (template for catalog deps + tsup setup)
    - packages/agent/tsconfig.json (TS strict + ESM-only template)
    - packages/agent/vitest.config.ts (vitest config template)
    - packages/agent/src/lib/atomicWrite.ts (existing pattern — REUSE the .tmp + rename approach)
    - packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md (Wave-0 outcome — informs SKILL.md frontmatter strategy)
    - .planning/phases/05-skills-health-panels/05-RESEARCH.md §Pattern 6 (lines 470–530) — projectRoot resolution + SKILL.md frontmatter shape
    - .planning/phases/05-skills-health-panels/05-CONTEXT.md D-5-03..D-5-09 (workspace pkg, SessionEnd hook, atomic write, walk-up, forward-only)
  </read_first>
  <files>
    pnpm-workspace.yaml,
    packages/meta-observer/package.json,
    packages/meta-observer/tsconfig.json,
    packages/meta-observer/vitest.config.ts,
    packages/meta-observer/lib/projectRoot.ts,
    packages/meta-observer/lib/atomicWrite.ts,
    packages/meta-observer/test/projectRoot.test.ts,
    packages/meta-observer/test/atomicWrite.test.ts
  </files>
  <behavior>
    - projectRoot.test.ts:
      • `resolveProjectRoot({ cwd })` returns `process.env.CLAUDE_PROJECT_DIR` when that env var is set AND the directory contains `.planning` or `.claude` (test sets the env, asserts result, then deletes the env).
      • Returns the deepest ancestor of `cwd` that contains `.planning` (test scaffolds tmp dir tree `<root>/.planning` and passes nested `<root>/a/b/c` as cwd).
      • Returns the deepest ancestor that contains `.claude` if `.planning` absent.
      • Returns `null` when neither marker found from cwd up to filesystem root.
      • Returns `null` when CLAUDE_PROJECT_DIR is set but the path doesn't have `.planning` or `.claude` (refuses silently per D-5-07).
    - atomicWrite.test.ts:
      • `atomicWrite(targetPath, contents)` writes contents to `<targetPath>.tmp` first, then renames to `<targetPath>` (assert via `fs.statSync` on intermediate state — use a stub fs OR check that the function performs `writeFile` then `rename` by spying on `fs.promises.rename`).
      • Concurrent reader race test: launch a parallel read loop while atomicWrite runs; assert the reader either gets the FULL contents OR `ENOENT`, NEVER a partial file.
      • Refuses to write a path outside `<projectRoot>/.planning/skill-observations/` (when given a sandboxRoot parameter): asserts that paths containing `..` or paths whose realpath escapes `<sandboxRoot>` reject with a `PathViolation`-shaped error.
  </behavior>
  <action>
**1. Add to pnpm-workspace.yaml.** Append `'packages/meta-observer'` to the `packages:` list (mirrors how Phase 0 D-04 added the existing 3 packages). Run `pnpm install` from repo root after editing to refresh the workspace index.

**2. Create `packages/meta-observer/package.json`** with:
- `name`: `@agenticapps/dashboard-meta-observer`
- `version`: `0.0.0-private`
- `private: true`
- `type: module`
- `exports`: { ".": "./lib/index.ts" }
- `scripts`: build (`tsc -p tsconfig.json`), test (`vitest --run`), typecheck (`tsc -p tsconfig.json --noEmit`)
- `dependencies`: `@agenticapps/dashboard-shared: workspace:*` ONLY
- `devDependencies`: `vitest: catalog:`, `typescript: catalog:`, `@types/node: catalog:`

NO native deps (INV-05 carries per D-5-03). NO transcript-parsing libraries — JSONL is line-by-line via Node's `node:readline`. Catalog versions only.

**3. Create `packages/meta-observer/tsconfig.json`:** copy `packages/agent/tsconfig.json` verbatim (TS strict + ESM-only + `exactOptionalPropertyTypes`). Adjust `outDir` and any package-relative paths to suit.

**4. Create `packages/meta-observer/vitest.config.ts`:** copy `packages/agent/vitest.config.ts` verbatim, adjust paths if needed.

**5. RED — write `packages/meta-observer/test/projectRoot.test.ts` and `test/atomicWrite.test.ts`** asserting the behaviours above. Use `vi.stubEnv('CLAUDE_PROJECT_DIR', ...)` (vitest 1.0+ API) for env stubbing in projectRoot tests. For atomicWrite race tests, use Node's `child_process.fork` OR a simple `Promise.all([atomicWrite(...), readLoop])` — the tests must demonstrate that no concurrent reader sees a partial file even when contents are >1MB.

Run `pnpm --filter @agenticapps/dashboard-meta-observer test` — both tests must fail with module-not-found.

**6. GREEN — write `packages/meta-observer/lib/projectRoot.ts`:** Implementation reads `process.env.CLAUDE_PROJECT_DIR` first; if set AND `existsSync(join(envRoot, '.planning'))` OR `existsSync(join(envRoot, '.claude'))`, returns `envRoot`. Otherwise walks up from `payload.cwd ?? process.cwd()` checking each ancestor for `.planning` or `.claude`. When `dirname(dir) === dir` (filesystem root reached), returns `null`. The `/` (POSIX root) and `C:\` (Windows root) edge cases both terminate via the dirname-equals-dir check. Use `node:fs.existsSync` and `node:path.{dirname,join}`.

**7. GREEN — write `packages/meta-observer/lib/atomicWrite.ts`:** REUSE the structure from `packages/agent/src/lib/atomicWrite.ts` (read it first). Augment with a `sandboxRoot` parameter that asserts the target path's realpath sits under the sandboxRoot. Steps:
  - `if (!isAbsolute(targetPath)) throw new PathViolation('targetPath must be absolute')`.
  - `if (targetPath.split(sep).includes('..')) throw new PathViolation('path traversal not allowed')`.
  - `const sandboxReal = await realpath(opts.sandboxRoot)`.
  - `await mkdir(dirname(targetPath), { recursive: true })`.
  - `const parentReal = await realpath(dirname(targetPath))`.
  - `if (parentReal !== sandboxReal && !parentReal.startsWith(sandboxReal + sep)) throw new PathViolation('target outside sandbox root')`.
  - `await writeFile(targetPath + '.tmp', contents, { encoding: 'utf8', mode: 0o644 })`.
  - `await rename(targetPath + '.tmp', targetPath)`.

`PathViolation` is a class extending `Error` with `name = 'PathViolation'` (mirror `paths.ts` pattern in `packages/agent`).

Run tests; both files green. Document in commit: `feat(05-01): meta-observer workspace pkg + projectRoot resolver + atomicWrite`.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @agenticapps/dashboard-meta-observer test --run test/projectRoot.test.ts test/atomicWrite.test.ts &amp;&amp; pnpm --filter @agenticapps/dashboard-meta-observer typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/meta-observer/package.json` exists; `grep '"name": "@agenticapps/dashboard-meta-observer"'` matches.
    - `pnpm-workspace.yaml` includes `packages/meta-observer` (grep `'packages/meta-observer'`).
    - `packages/meta-observer/lib/projectRoot.ts` contains `process.env.CLAUDE_PROJECT_DIR` (grep matches) and `dirname` (walk-up).
    - `packages/meta-observer/lib/atomicWrite.ts` contains both `'.tmp'` and `'rename'` (atomic pattern); contains `PathViolation` (sandbox enforcement); contains `sandboxRoot` (parameter name).
    - `pnpm --filter @agenticapps/dashboard-meta-observer test --run test/projectRoot.test.ts` exits 0.
    - `pnpm --filter @agenticapps/dashboard-meta-observer test --run test/atomicWrite.test.ts` exits 0.
    - Full `pnpm -r typecheck` exits 0.
    - Full `pnpm -r test` includes the meta-observer suite (search the output for `@agenticapps/dashboard-meta-observer`).
    - `packages/meta-observer/package.json` declares zero `dependencies` other than `@agenticapps/dashboard-shared` (grep that the dependencies block has only the workspace shared package — no native deps land).
  </acceptance_criteria>
  <done>Workspace package builds; projectRoot resolver + atomicWrite green per D-5-07 + D-5-06 contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: extractCommitment + extractFirings + SessionEnd hook entry script</name>
  <read_first>
    - packages/meta-observer/test/__fixtures__/sample-transcript.jsonl (Wave-0 probe output — KEY input shape)
    - packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md (locks frontmatter strategy)
    - packages/agent/src/lib/phaseDetail.ts lines 96–116 (parseCommitmentBlock — REUSE the regex pattern verbatim: `/^## Workflow commitment\s*$/gm`)
    - packages/shared/src/schemas/observations.ts (HookFiringSchema — must validate emitted lines)
    - .planning/phases/05-skills-health-panels/05-RESEARCH.md §Pitfall 5 (transcripts can be large — use streaming)
    - .planning/phases/05-skills-health-panels/05-CONTEXT.md D-5-04..D-5-06 (SessionEnd hook, .md + .jsonl, one-file-per-session naming)
  </read_first>
  <files>
    packages/meta-observer/lib/extractCommitment.ts,
    packages/meta-observer/lib/extractFirings.ts,
    packages/meta-observer/test/extractCommitment.test.ts,
    packages/meta-observer/test/extractFirings.test.ts,
    packages/meta-observer/hooks/session-end.mjs
  </files>
  <behavior>
    - extractCommitment.test.ts:
      • Returns `null` when transcript has no `## Workflow commitment` heading.
      • Returns the LAST `## Workflow commitment` block (greedy regex over all matches; mirror Phase 4 `phaseDetail.ts:101-115`) when transcript has multiple commitments.
      • Stops at the next `## ` heading OR at end-of-content.
      • Streams the transcript file via `node:readline` (no `readFileSync` of >1MB inputs) — assert via spying on `createReadStream` OR by structuring the API so the reader is injectable.
      • Tolerates partial / truncated transcripts (last line may be incomplete JSON — skip silently per Phase 4 D-22 pattern).
      • Walks the JSONL line shape captured in `sample-transcript.jsonl`: each line is JSON; markdown lives at the path documented in the probe fixture (e.g. `entry.message.content[].text` for assistant messages). Skip non-message entries (tool_use, tool_result, system_messages).
    - extractFirings.test.ts:
      • Emits an array of `HookFiring`-shaped objects (`{ ts, skill, hook }` plus passthrough fields).
      • Each emitted object validates against `HookFiringSchema.parse(...)` from `@agenticapps/dashboard-shared` — assert by parsing every output element through the schema.
      • Empty input → empty array.
      • Tool_use/tool_result events become firings with `skill: <tool name>`, `hook: 'PostToolUse'` (or whatever the captured shape demands — driven by Wave-0 probe).
      • Lines that don't parse as JSON are skipped silently (no throw).
  </behavior>
  <action>
**RED:** Author both test files. Each test file must include at least one fixture-driven case that reads `packages/meta-observer/test/__fixtures__/sample-transcript.jsonl` and asserts the extractor produces a non-empty result with the shape declared in `<behavior>`. Run tests — both must fail with module-not-found.

**GREEN — `packages/meta-observer/lib/extractCommitment.ts`:**

The function `extractCommitment(transcriptPath: string): Promise<string | null>` opens the transcript via `createReadStream(transcriptPath, { encoding: 'utf8' })`, wraps with `createInterface({ input: stream, crlfDelay: Infinity })`, then iterates `for await (const line of rl)`. For each line, trim → skip empty → `JSON.parse` (try/catch, skip on throw) → call `extractTextBody(parsed)` → if non-null, push to `bodies: string[]`. After the loop, `joined = bodies.join('\n\n')`. Then run a global regex `/^## Workflow commitment\s*$/gm` over `joined`, tracking the LAST match index. Slice tail from that index, find next `\n## ` heading, slice block accordingly, trim, return non-empty string or null.

`extractTextBody(entry)` handles the Wave-0-probe-documented shape: typically `{ type: 'user'|'assistant', message: { content: Array<{ type: 'text', text: string } | { type: 'tool_use', ... }> } }`. Concatenate all `text`-type content blocks with `\n`. Returns null for non-message entries.

Mirror the LAST-occurrence semantics of Phase 4 `phaseDetail.ts:101-115` so a session with multiple commitments retains only the most recent.

**GREEN — `packages/meta-observer/lib/extractFirings.ts`:**

The function `extractFirings(transcriptPath: string): Promise<HookFiring[]>` opens via the same streaming reader. For each line: trim → skip empty → JSON.parse (skip on throw) → call `entryToFiring(parsed)` → if non-null, validate via `HookFiringSchema.safeParse(firing)`, push validated only.

`entryToFiring(entry)` covers two shapes (driven by Wave-0 probe):
- **Shape A:** explicit hook firing record `{ hook_event_name: string, timestamp: string, skill?: string, ... }` → return `{ ts: timestamp, skill: skill ?? 'session', hook: hook_event_name }`.
- **Shape B:** assistant message with tool_use content blocks → for each `content[i]` with `type: 'tool_use', name: string`, return `{ ts: entry.timestamp, skill: name, hook: 'PostToolUse' }`. Take the FIRST tool_use only per entry (or extend to multiple via the test fixture's verified ordering).

If the Wave-0 probe reveals a third shape, add a third branch with an explicit comment citing the fixture line that motivated it.

Import: `import { HookFiringSchema, type HookFiring } from '@agenticapps/dashboard-shared'`.

**GREEN — `packages/meta-observer/hooks/session-end.mjs`:**

Implements the SessionEnd hook entry script. Steps in `main()`:
1. Shebang `#!/usr/bin/env node` first line.
2. Read stdin via `readFileSync(0, 'utf8')` (fd 0). Try/catch — on parse failure, write to stderr, `process.exit(0)` (never block session).
3. `const cwd = typeof payload?.cwd === 'string' ? payload.cwd : process.cwd()`.
4. `const root = resolveProjectRoot({ cwd })`. If null → stderr message, `process.exit(0)` (D-5-07 silent skip).
5. `const transcriptPath = typeof payload?.transcript_path === 'string' ? payload.transcript_path : null`. If null → stderr, `exit(0)`.
6. `const sessionId = typeof payload?.session_id === 'string' ? payload.session_id : 'unknown'`.
7. Filename pattern (D-5-06): `const stamp = new Date().toISOString().replaceAll(':', '-').replace(/\..+$/, '')` → `2026-05-07T17-55-12`. `const baseName = ${stamp}--${sessionId}`. `const obsDir = join(root, '.planning', 'skill-observations')`.
8. Try block 1 (per **D-5-05** — dual `.md` + `.jsonl` outputs per session, written independently so a parse failure on one side does not lose the other): `const commitment = await extractCommitment(transcriptPath)`. If non-null: `await atomicWrite(join(obsDir, ${baseName}.md), '## Workflow commitment\n\n' + commitment + '\n', { sandboxRoot: obsDir })`. Catch any throw → stderr, continue.
9. Try block 2 (the second half of D-5-05): `const firings = await extractFirings(transcriptPath)`. If `firings.length > 0`: build JSONL by mapping `JSON.stringify` and joining with `\n` (trailing newline). `await atomicWrite(join(obsDir, ${baseName}.jsonl), jsonl, { sandboxRoot: obsDir })`. Catch any throw → stderr, continue.
10. Final `process.exit(0)`.

Top-level `main().catch((e) => { stderr; process.exit(0) })` so an unhandled async rejection still exits 0.

Mark executable: `chmod +x packages/meta-observer/hooks/session-end.mjs`.

The script must NEVER throw a non-zero exit code — failing a SessionEnd hook is silent UX death. All errors → stderr + exit 0.

REFACTOR: confirm no test sees a partial `.md` or `.jsonl` (atomicWrite tests cover); confirm the script's only writable path is `<projectRoot>/.planning/skill-observations/` (atomicWrite's sandboxRoot enforces).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-meta-observer test --run test/extractCommitment.test.ts test/extractFirings.test.ts &amp;&amp; pnpm --filter @agenticapps/dashboard-meta-observer typecheck &amp;&amp; test -x packages/meta-observer/hooks/session-end.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/meta-observer/lib/extractCommitment.ts` contains `Workflow commitment` regex literal.
    - File `packages/meta-observer/lib/extractFirings.ts` imports `HookFiringSchema` from `@agenticapps/dashboard-shared` (grep matches `HookFiringSchema`).
    - File `packages/meta-observer/hooks/session-end.mjs` exists, has shebang `#!/usr/bin/env node` (grep first line), and is executable (`test -x`).
    - The hook script imports `resolveProjectRoot`, `atomicWrite`, `extractCommitment`, `extractFirings` from `../lib/` (4 imports total).
    - The hook script reads stdin (`readFileSync(0` or `process.stdin.fd 0`).
    - The hook script calls `process.exit(0)` on every error path (grep `process.exit(0)` count ≥ 4).
    - `pnpm --filter @agenticapps/dashboard-meta-observer test` exits 0.
    - extractCommitment test asserts streaming via `createReadStream` (grep test file for `createReadStream`).
    - extractFirings tests assert every emitted firing parses against `HookFiringSchema.safeParse` (grep test file for `HookFiringSchema`).
    - The hook script is ≤ 100 lines (manageable footprint — `wc -l < packages/meta-observer/hooks/session-end.mjs` ≤ 100).
  </acceptance_criteria>
  <done>Meta-observer SessionEnd hook can be invoked locally with a synthetic payload + a fixture transcript and writes valid `.md` + `.jsonl` to a sandbox dir; all extractor tests green.</done>
</task>

<task type="auto">
  <name>Task 4: SKILL.md frontmatter — wire to Wave-0 probe outcome</name>
  <read_first>
    - packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md (THE locking artefact for this task)
    - .planning/phases/05-skills-health-panels/05-RESEARCH.md §Pattern 6 (lines 512–530, frontmatter shape)
    - .planning/phases/05-skills-health-panels/05-CONTEXT.md D-5-04 + D-5-09 (SessionEnd hook, manual install)
    - .claude/skills/agenticapps-workflow/skill/SKILL.md (existing project-installed skill — reference layout)
  </read_first>
  <files>
    packages/meta-observer/SKILL.md
  </files>
  <action>
**Read `packages/meta-observer/test/__fixtures__/skeleton-skill-probe.md` FIRST and parse the `## Implementation path locked` line.**

**If "skill frontmatter" path locked (preferred — Probe B `fires-when-dormant`):**

Write `packages/meta-observer/SKILL.md` with frontmatter:
- `name: meta-observer`
- `description: |` followed by an indented multi-line description ending with the dashboard-panel-population rationale (3-4 lines)
- `disable-model-invocation: true`
- `hooks:` block with a `SessionEnd:` array, each entry containing `hooks:` array with `type: command, command: node ${CLAUDE_SKILL_DIR}/hooks/session-end.mjs, timeout: 30`

Body sections after frontmatter:
- `# meta-observer` heading
- One paragraph explaining: silent persister, no user-facing skill body required, SessionEnd hook runs `node hooks/session-end.mjs` once per Claude session
- `## Install` section: `claude skill install <path-to-this-package>` (per-project, D-5-09)
- `## What it writes` section: example filenames `2026-05-07T17-55-12--abc123.md` and `.jsonl` per D-5-06
- `## Privacy` section: reads local transcript file only; writes only inside resolved `<projectRoot>/.planning/skill-observations/`; no network, no telemetry

**If ".claude/settings.json" fallback path locked (Probe B `does-not-fire-when-dormant`):**

Write SKILL.md without the `hooks:` frontmatter block (just `name` + `description` + `disable-model-invocation`) and add a `## Install` section that documents:
1. `claude skill install <path-to-this-package>` (so the skill files land in `.claude/skills/meta-observer/`).
2. Append the following block to the project's `.claude/settings.json` `hooks.SessionEnd` array:
   - `hooks: [{ type: command, command: node ${CLAUDE_PROJECT_DIR}/.claude/skills/meta-observer/hooks/session-end.mjs, timeout: 30 }]`

In EITHER path, the SKILL.md MUST set `disable-model-invocation: true` so Claude never auto-invokes this skill in chat (it's a silent persister).

**File ending:** Single trailing newline. Markdown linting friendly.
  </action>
  <verify>
    <automated>test -f packages/meta-observer/SKILL.md &amp;&amp; grep -q '^name: meta-observer' packages/meta-observer/SKILL.md &amp;&amp; grep -q 'disable-model-invocation: true' packages/meta-observer/SKILL.md &amp;&amp; grep -qE '(SessionEnd|settings\.json)' packages/meta-observer/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/meta-observer/SKILL.md` exists with valid frontmatter (file starts with `---\n`).
    - Frontmatter contains `name: meta-observer` (literal line).
    - Frontmatter contains `disable-model-invocation: true` (literal line).
    - File contains either `SessionEnd:` (skill-frontmatter path) OR a `settings.json` install instruction (fallback path) — `grep -qE 'SessionEnd:|settings\\.json'` matches.
    - File contains `## Install` section.
    - The implementation choice (frontmatter vs settings.json) is consistent with the `## Implementation path locked` line in `test/__fixtures__/skeleton-skill-probe.md`.
    - File length ≤ 80 lines (concise).
  </acceptance_criteria>
  <done>Meta-observer skill is installable in either path; install hint copy in Phase 4 DISC-04 (`claude skill install meta-observer`) still resolves correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code session → meta-observer hook stdin | Untrusted JSON payload (transcript_path, cwd) crosses here |
| meta-observer hook → user's project FS | Hook writes to `<projectRoot>/.planning/skill-observations/` — must stay inside |
| Transcript JSONL → extractCommitment markdown body | Untrusted markdown is later rendered by Phase 4 CommitmentBlock SPA panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-Meta-Write-Path | T (Tampering) | atomicWrite | mitigate | `sandboxRoot` parameter required; realpath-after-realpath check; reject any path whose parent's realpath isn't `===` sandboxRoot or starts with sandboxRoot+sep. Test asserts `atomicWrite('/etc/passwd', ..., { sandboxRoot: tmpDir })` rejects via PathViolation. |
| T-05-01-Meta-AtomicWrite | T (Tampering) | atomicWrite | mitigate | Write-to-`.tmp` + rename pattern (POSIX-atomic on same FS). Race test asserts no concurrent reader sees partial file. |
| T-05-01-Meta-Skip-On-No-Marker | T (Tampering) | resolveProjectRoot | mitigate | Returns null when no `.planning/.claude` found. Hook script exits 0 silently — never writes outside a real project root. |
| T-05-01-Schema-Drift | I (Information Disclosure) | shared schemas | mitigate | All 5 new schemas use `.passthrough()` on response envelopes where future fields are likely; discriminated unions on `kind` / `state` lock the contract surface. Daemon `outbound()` (Phase 1 D-16) will catch parse failures; tests assert round-trip stability. |
| T-05-01-Markdown-Injection | I (Information Disclosure) | extractCommitment + downstream Phase 4 CommitmentBlock | mitigate | extractCommitment returns plain string; Phase 4 CommitmentBlock already renders via React text children with default escaping (Phase 4 pattern in HookFirings panel). No raw HTML injection introduced in this plan. |
| T-05-01-Hook-Crash-Loop | D (Denial of Service) | session-end.mjs | mitigate | Every error path exits 0; never throws to surface; transcript-large-file pitfall (Pitfall 5) addressed by streaming via `node:readline` (no full-file readFileSync). |
| T-05-01-Subprocess-Inj | T (Tampering) | session-end.mjs | accept | Hook script accepts no shell-string commands — its only inputs are stdin JSON (parsed via JSON.parse) and `process.env.CLAUDE_PROJECT_DIR` (treated as a string path). No subprocess spawn calls in this plan. |
</threat_model>

<verification>
- All 5 schema tests + meta-observer's 4 lib tests pass green.
- `pnpm -r typecheck` passes — schemas exported and consumable from `@agenticapps/dashboard-shared` import.
- `pnpm -r test` passes — full repo regression including new package.
- `node packages/meta-observer/hooks/session-end.mjs < /dev/null` exits 0 (silent skip when no payload).
- `echo '{"cwd":"/tmp/no-such","transcript_path":"/tmp/nope.jsonl","session_id":"test"}' | node packages/meta-observer/hooks/session-end.mjs` exits 0 (silent skip when no project root marker).
- Wave-0 fixture files captured + committed.
</verification>

<success_criteria>
- `pnpm --filter @agenticapps/dashboard-shared test` green (5 new test files + existing).
- `pnpm --filter @agenticapps/dashboard-meta-observer test` green (4 new test files).
- Schema barrel re-exports usable: `import { GlobalSkillsResponseSchema, AgentLinterResponseSchema, ObservabilityResponseSchema, SecretsResponseSchema, IntegrationsResponseSchema } from '@agenticapps/dashboard-shared'` typechecks.
- `pnpm-workspace.yaml` recognises `packages/meta-observer`.
- Both Wave-0 probe fixtures (`sample-transcript.jsonl` + `skeleton-skill-probe.md`) are committed and inform Tasks 3 + 4.
- `packages/meta-observer/SKILL.md` declares the SessionEnd hook OR documents the settings.json fallback path consistently with the probe outcome.
- Atomic-write race test demonstrates no partial-file visibility.
- INV-05 holds — meta-observer has zero native deps.
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-health-panels/05-01-SUMMARY.md` capturing:
- Probe outcomes (transcript shape, hook activation path locked).
- Final extractor field-mapping (Shape A/B/etc. resolved against the real fixture).
- Any deviations from the planned schemas (passthrough additions, etc.).
- File counts + test counts added.
- Any blocking issue surfaced for plans 02–05 (e.g. if AgentLinter live JSON shape differs from RESEARCH §Code Examples).
</output>
</content>
</invoke>