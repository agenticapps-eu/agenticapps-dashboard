---
phase: DASH-15-capability-level-engine-fleet-matrix
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx
  - packages/spa/src/components/panels/coverage/LevelBadgeCell.test.tsx
  - packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx
  - packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.test.tsx
autonomous: true
requirements: [CML-08, CML-09]
nyquist_compliant: true

must_haves:
  truths:
    - "LevelBadgeCell renders a keyboard-activatable button with the L{level} label and the correct semantic token color for each level"
    - "LevelBadgeCell renders the always-visible inferred ~ sigil when eval.inferred is true, and renders an em-dash with NO button when evalData is undefined (degraded)"
    - "ClaudeMdLevelDrawer renders all six rungs with a pass (Check) / fail (Circle) indicator, evidence strings, and the L5 inferred sigil + always-visible callout"
    - "The drawer renders the nextSteps list when present and the 'Fully adaptive' copy when nextSteps is empty; it closes on Esc and backdrop click with focus restoration"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx"
      provides: "L-level badge cell with degraded + inferred states"
      contains: "LevelBadgeCell"
      min_lines: 30
    - path: "packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx"
      provides: "native-dialog per-rung drawer"
      contains: "ClaudeMdLevelDrawer"
      min_lines: 50
    - path: "packages/spa/src/components/panels/coverage/LevelBadgeCell.test.tsx"
      provides: "badge render + degraded + inferred + onClick tests"
      contains: "LevelBadgeCell"
    - path: "packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.test.tsx"
      provides: "rung list + nextSteps + close tests"
      contains: "ClaudeMdLevelDrawer"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx"
      to: "@agenticapps/dashboard-shared"
      via: "ClaudeMdEval type import"
      pattern: "ClaudeMdEval"
    - from: "packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx"
      to: "native <dialog> showModal pattern"
      via: "RegisterModal focus management"
      pattern: "showModal"
---

<objective>
Build the two new presentational SPA surfaces (CML-08, CML-09): the `LevelBadgeCell` (the
clickable L-level badge `<td>` content) and the `ClaudeMdLevelDrawer` (the native-`<dialog>`
per-rung drawer). Both are self-contained components with their own tests — they are NOT yet
wired into the coverage table (Plan 04 does the wiring). This isolation lets this plan run in
parallel with the daemon scanner (Plan 02).

Purpose: These components render the `ClaudeMdEval` payload. They depend only on the Plan 01
shared type. Plan 04 then imports and mounts them.
Output: `LevelBadgeCell.tsx`, `ClaudeMdLevelDrawer.tsx`, and their `.test.tsx` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-UI-SPEC.md
@.planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md

<interfaces>
<!-- Contracts these components consume. Source: UI-SPEC §Component Specifications + PATTERNS.md -->
import type { ClaudeMdEval } from '@agenticapps/dashboard-shared'   // from Plan 01

interface LevelBadgeCellProps {
  evalData: ClaudeMdEval | undefined   // undefined = pre-DASH-15 daemon / scanner degraded
  repoName: string                     // for aria-label
  onClick: () => void                  // triggers drawer open
}

interface ClaudeMdLevelDrawerProps {
  isOpen: boolean
  onClose: () => void
  repoName: string
  evalData: ClaudeMdEval               // always defined when drawer is open
}
</interfaces>

<styling_contract>
<!-- D-5.1-10 — NON-NEGOTIABLE. Source: UI-SPEC §Design System + §Color. -->
- NO cn()/clsx/CVA; NO hex literals; NO shadcn aliases (bg-background/text-foreground).
- Inline className strings; state→token mapping via a frozen LEVEL_TOKEN_MAP record (mirror CoverageCell.tsx STATE_TOKEN_MAP).
- Two font weights only: font-normal (body/evidence) + font-semibold (labels/headings/badge/sigil).

LEVEL_TOKEN_MAP (exact, from UI-SPEC §L-level Badge Color Mapping):
  0: bg-text-tertiary/10  + text-text-tertiary   (Absent — muted)
  1: bg-status-warning/10 + text-status-warning  (Basic — amber)
  2: bg-status-warning/10 + text-status-warning  (Scoped — amber)
  3: bg-status-info/10    + text-status-info      (Structured — blue)
  4: bg-status-info/10    + text-status-info      (Abstracted — blue)
  5: bg-status-success/10 + text-status-success   (Maintained — green)
  6: bg-status-success/10 + text-status-success   (Adaptive — green)

Inferred sigil (badge + drawer, ALWAYS visible — never hover-only):
  <span class="ml-1 text-[10px] font-semibold bg-accent-bg text-accent rounded px-1">~</span>
Inferred-L5 drawer callout (always visible when L5 rung.inferred): exact copy from UI-SPEC §Copywriting.
</styling_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LevelBadgeCell component + tests</name>
  <files>packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx, packages/spa/src/components/panels/coverage/LevelBadgeCell.test.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/CoverageCell.tsx (STATE_TOKEN_MAP lookup-table pattern + token usage — the styling analog)
    - packages/spa/src/components/panels/coverage/UnderstandCopyPill.tsx (cell-component role analog)
    - packages/spa/src/components/panels/coverage/UnderstandCopyPill.test.tsx (EXACT test-infra analog — render/screen/fireEvent/cleanup)
    - packages/spa/src/components/panels/coverage/CoverageRow.tsx (the `row.understand` undefined em-dash fallback at lines ~208-217 — mirror for degraded state)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-UI-SPEC.md §"1. LevelBadgeCell" + §"2. Inferred-L5 Marker" + §"Copywriting Contract" (exact aria-label strings)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md §"LevelBadgeCell.tsx" + §"LevelBadgeCell.test.tsx" (lines 441-563)
  </read_first>
  <behavior>
    - Renders a <button role="button"> whose text contains "L4" for an L4 eval
    - When evalData is undefined: renders <span> em-dash "—", NO button (queryByRole('button') is null)
    - When eval.inferred true: button text contains the "~" sigil
    - Clicking the button calls onClick exactly once
    - aria-label is "CLAUDE.md level for {repoName}: {levelLabel}. Click for details." (normal), includes ", inferred" when inferred, and is "...: not available" when degraded
  </behavior>
  <action>
    Create `LevelBadgeCell.tsx` exporting a named `LevelBadgeCell` with the props from
    `<interfaces>`. Define a frozen `LEVEL_TOKEN_MAP` record using the EXACT token pairs in
    `<styling_contract>`. Degraded branch (evalData === undefined): return a `<span
    className="text-text-tertiary text-xs">—</span>` with the "not available" aria-label, NO button.
    Otherwise render a `<button type="button">` with `w-full h-full flex items-center justify-center
    focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md`, the exact
    aria-label (including ", inferred" when `evalData.inferred`), and inside it a pill `<span>` with
    `inline-flex items-center rounded-md px-2 py-1 text-xs font-semibold ${tokens.bg} ${tokens.text}`
    showing `L{evalData.level}`. When `evalData.inferred`, append the inferred sigil span from
    `<styling_contract>` (always rendered, not hover-gated). Wire `onClick`. Write the matching
    test file using the UnderstandCopyPill test harness covering all five `<behavior>` cases. Use
    `.js` ESM import extensions for local imports. No cn()/clsx/hex/shadcn aliases.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test LevelBadgeCell</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @agenticapps/dashboard-spa test LevelBadgeCell` green
    - Source assertion: `grep -c "LEVEL_TOKEN_MAP" packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx` returns >= 1
    - Source assertion (no banned styling): `grep -Ec "cn\(|clsx|#[0-9a-fA-F]{3,6}|bg-background|text-foreground" packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx` returns 0
    - Behavior: degraded render produces no button; inferred render contains "~"
  </acceptance_criteria>
  <done>LevelBadgeCell renders all 5 states correctly with token-only styling; its test file is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClaudeMdLevelDrawer component + tests</name>
  <files>packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx, packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.test.tsx</files>
  <read_first>
    - packages/spa/src/components/RegisterModal.tsx (EXACT analog — native <dialog>, dialogRef/previouslyFocused focus management lines 57-105, backdrop-click handler, onCancel Esc wiring, header layout)
    - packages/spa/src/components/panels/coverage/UnderstandCopyPill.test.tsx (test-infra analog)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-UI-SPEC.md §"3. ClaudeMdLevelDrawer" (Sections A/B/C/D — full JSX contract) + §"2. Inferred-L5 Marker" + §"Copywriting Contract"
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md §"ClaudeMdLevelDrawer.tsx" (lines 567-655)
  </read_first>
  <behavior>
    - Renders all 6 rung rows; each passed:true shows a Check icon, passed:false shows a Circle icon
    - Renders each rung's evidence strings as a list under the rung name
    - For the L5 rung with inferred:true: renders the "~" sigil after the name AND the always-visible "Inferred — not hard-verified..." callout
    - When nextSteps.length > 0: renders a "Next steps" heading + each step with a "→" prefix
    - When nextSteps.length === 0: renders "Fully adaptive — all six rungs complete."
    - Backdrop click calls onClose; Esc (onCancel) calls onClose
  </behavior>
  <action>
    Create `ClaudeMdLevelDrawer.tsx` exporting a named `ClaudeMdLevelDrawer` with the props from
    `<interfaces>`. Import `Check, Circle` from `lucide-react` and `ClaudeMdEval` type. Copy the
    RegisterModal focus-management `useEffect` (dialogRef + previouslyFocused, showModal/close)
    verbatim, plus `handleBackdropClick` and the `onCancel={(e)=>{e.preventDefault();onClose()}}`
    wiring. `<dialog>` className: `bg-card-bg border border-border-subtle rounded-lg p-0 max-w-lg
    w-full mx-4 dark:shadow-none shadow-card backdrop:bg-text-primary/50`. Inner `<div className="p-6">`
    with Section A header (repo name `text-xl font-semibold`, sub-label "CLAUDE.md capability level",
    a decorative headline level badge `L{level} — {levelLabel}` using the same LEVEL_TOKEN_MAP +
    inferred sigil, and a "×" close button with aria-label "Close capability level drawer").
    Section B: `<ul role="list">` mapping `evalData.rungs` to Check/Circle + `L{rung} — {label}` +
    inferred sigil (when rung.inferred) + an evidence `<ul>` + the always-visible L5 callout (when
    `rung.rung===5 && rung.inferred`). Section C: nextSteps list with "→" prefix when length>0.
    Section D: the "Fully adaptive" success copy when length===0. Use EXACT copy strings from
    UI-SPEC §Copywriting. Define LEVEL_TOKEN_MAP locally (or share via a small module — executor's
    discretion, but no cross-package leak). Write the matching test file covering all six
    `<behavior>` cases (jsdom; the test may assert rendered text/icons and call the backdrop/onCancel
    handlers). No cn()/clsx/hex/shadcn aliases.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test ClaudeMdLevelDrawer</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @agenticapps/dashboard-spa test ClaudeMdLevelDrawer` green
    - Source assertion: `grep -c "showModal" packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx` returns >= 1
    - Source assertion: `grep -c "Inferred — not hard-verified" packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx` returns 1
    - Source assertion: `grep -c "Fully adaptive" packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx` returns 1
    - Source assertion (no banned styling): `grep -Ec "cn\(|clsx|#[0-9a-fA-F]{3,6}|bg-background|text-foreground" packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx` returns 0
  </acceptance_criteria>
  <done>The drawer renders the 6-rung list, evidence, the inferred-L5 sigil+callout, nextSteps/Fully-adaptive branches, and closes on Esc/backdrop with focus restoration; its test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon JSON → React render | The `ClaudeMdEval` payload is rendered as text/structure in the browser; React escapes interpolated strings by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Information Disclosure | Evidence strings rendered in the drawer | mitigate | Evidence strings are daemon-produced human-readable summaries (never raw file content, enforced in Plan 02 T-15-02-04); React text interpolation escapes them — no dangerouslySetInnerHTML |
| T-15-03-02 | Elevation of Privilege | XSS via interpolated repoName / evidence | mitigate | No `dangerouslySetInnerHTML` anywhere in these components; all dynamic values go through React's default-escaped `{value}` interpolation |
| T-15-03-03 | Tampering | Drawer accessibility/focus-trap regression | mitigate | Native `<dialog>.showModal()` provides the focus trap; `previouslyFocused` restoration mirrors the audited RegisterModal pattern (Pitfall 8) |
| T-15-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (UI-SPEC §Registry Safety — no new registries, all primitives from the existing workspace); no install task |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test LevelBadgeCell ClaudeMdLevelDrawer` green
- `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0
- Grep confirms zero banned styling primitives in both new components
</verification>

<success_criteria>
- CML-08 (badge render) and CML-09 (drawer render) component-level behavior is implemented and unit-tested in isolation, ready for Plan 04 to wire into the table.
</success_criteria>

<output>
Create `.planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-03-SUMMARY.md` when done.
</output>
