---
phase: DASH-15-capability-level-engine-fleet-matrix
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - packages/spa/src/components/panels/coverage/coverageColumns.ts
  - packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts
  - packages/spa/src/components/panels/coverage/CoverageRow.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx
  - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-IMPECCABLE.md
autonomous: false
requirements: [CML-08, CML-09]
nyquist_compliant: true

must_haves:
  truths:
    - "The Coverage Matrix table shows a new 'Level' column between claudeMd and gitNexus for every registered repo"
    - "Clicking a row's level badge opens the drawer with that repo's per-rung list, evidence, and next steps; degraded rows show an em-dash and no drawer"
    - "The table has no horizontal scroll at 1440x900 (total ~1200px within the 1440px viewport)"
    - "A 15-IMPECCABLE.md artifact records composite >= 80 for the /coverage route with the drawer both closed and open"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      provides: "level column width registration"
      contains: "level:"
    - path: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      provides: "level <td> + inline drawer + local drawerOpen state"
      contains: "LevelBadgeCell"
    - path: "packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx"
      provides: "colgroup <col> + Level <th> header"
      contains: "coverageColumnTooltips.level"
    - path: ".planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-IMPECCABLE.md"
      provides: "impeccable:critique artifact for /coverage (composite >= 80)"
      contains: "composite"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      to: "packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx"
      via: "level <td> renders LevelBadgeCell with row.eval"
      pattern: "LevelBadgeCell"
    - from: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      to: "packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx"
      via: "inline drawer gated on row.eval !== undefined"
      pattern: "ClaudeMdLevelDrawer"
    - from: "packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx"
      to: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      via: "COVERAGE_COL_WIDTHS.level in colgroup"
      pattern: "COVERAGE_COL_WIDTHS.level"
---

<objective>
Wire the Plan 03 components into the live Coverage Matrix table (CML-08, CML-09): register the
`level` column width + tooltip, add the `<td>` + inline drawer + local state to `CoverageRow`,
add the `<col>` + `<th>` to `CoverageFamilySection` (and the mobile equivalent), and produce the
mandatory `15-IMPECCABLE.md` visual-quality artifact (composite >= 80) for the `/coverage` route
with the drawer both closed and open.

Purpose: This is the integration + gate plan. It depends on Plan 02 (the `eval` field now arrives
on the wire) and Plan 03 (the badge cell + drawer components exist). It is the only plan that edits
the shared coverage-table files, so it owns them exclusively in Wave 3.
Output: 5 modified table files + the IMPECCABLE artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-UI-SPEC.md
@.planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md

<interfaces>
<!-- Components + schema this plan wires together (from Plans 01 + 03). -->
import { LevelBadgeCell } from './LevelBadgeCell.js'        // Plan 03
import { ClaudeMdLevelDrawer } from './ClaudeMdLevelDrawer.js'   // Plan 03
// row.eval: ClaudeMdEval | undefined  — now present on the wire from Plan 02

LevelBadgeCell props: { evalData: ClaudeMdEval | undefined; repoName: string; onClick: () => void }
ClaudeMdLevelDrawer props: { isOpen: boolean; onClose: () => void; repoName: string; evalData: ClaudeMdEval }
</interfaces>

<column_layout>
<!-- Source: UI-SPEC §Table Width Impact + PATTERNS.md §coverageColumns.ts. -->
Column order (insert `level` after claudeMd, before gitNexus):
  repo(w-72,288) claudeMd(w-32,128) [level(w-20,80) NEW] gitNexus(w-36,144)
  wiki(w-60,240) workflow(w-32,128) understand(w-36,144) actions(w-12,48)
Total: ~1200px < 1440px viewport — 240px slack, NO existing widths change.
Column header tooltip copy (coverageColumnTooltips.level):
  "CLAUDE.md capability level (L0–L6). Click for rung details."
</column_layout>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register level column width + tooltip</name>
  <files>packages/spa/src/components/panels/coverage/coverageColumns.ts, packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/coverageColumns.ts (the file being modified — read COVERAGE_COL_WIDTHS to place `level` after `claudeMd`)
    - packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts (the file being modified — read the existing tooltip object)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md §"coverageColumns.ts" + §"coverageColumnTooltips.ts" (lines 680-713)
  </read_first>
  <action>
    In `coverageColumns.ts`: add `level: 'w-20',` to the frozen `COVERAGE_COL_WIDTHS` object,
    positioned AFTER `claudeMd` and BEFORE `gitNexus` (preserving the column order in `<column_layout>`).
    Do NOT change any existing width. In `coverageColumnTooltips.ts`: add a `level:` entry with the
    EXACT copy `'CLAUDE.md capability level (L0–L6). Click for rung details.'`, also placed after
    `claudeMd`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "level: 'w-20'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - Source assertion: `grep -c "CLAUDE.md capability level" packages/spa/src/components/panels/coverage/coverageColumnTooltips.ts` returns 1
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0
  </acceptance_criteria>
  <done>The `level` column is registered with width `w-20` and its header tooltip copy; no existing column width changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire level <td> + inline drawer into CoverageRow, and <col>/<th> into the family sections</name>
  <files>packages/spa/src/components/panels/coverage/CoverageRow.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/coverage/CoverageRow.tsx (the file being modified — read the existing claudeMd/understand `<td>` cells + the local useState pattern + the understand undefined fallback to mirror placement)
    - packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx (the file being modified — read the existing `<colgroup>` `<col>` list + the sticky `<th>` header pattern)
    - packages/spa/src/components/panels/coverage/CoverageFamilySectionMobile.tsx (the file being modified — read the COLUMN_LABELS/key-columns mobile layout)
    - packages/spa/src/components/panels/coverage/LevelBadgeCell.tsx (Plan 03 — the component being mounted)
    - packages/spa/src/components/panels/coverage/ClaudeMdLevelDrawer.tsx (Plan 03 — the drawer being mounted)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-PATTERNS.md §"CoverageRow.tsx" §"CoverageFamilySection.tsx" §"CoverageFamilySectionMobile.tsx" (lines 717-803)
    - .planning/phases/DASH-15-capability-level-engine-fleet-matrix/DASH-15-UI-SPEC.md §"Responsive Behavior" (mobile inclusion is planner discretion)
  </read_first>
  <behavior>
    - CoverageRow renders a level <td> containing LevelBadgeCell with evalData={row.eval} repoName={row.repo}
    - Clicking the badge sets local drawerOpen=true; the drawer renders only when row.eval !== undefined
    - The drawer closes (drawerOpen=false) on its onClose
    - CoverageFamilySection has a <col> and a sticky <th> "Level" header (with the tooltip) in the correct position
    - A degraded row (row.eval undefined) renders the em-dash cell and mounts no drawer
  </behavior>
  <action>
    In `CoverageRow.tsx`: import `LevelBadgeCell` and `ClaudeMdLevelDrawer` (`.js` extensions). Add
    `const [drawerOpen, setDrawerOpen] = useState(false)` alongside the existing local state. Insert
    a new `<td className={\`${COVERAGE_COL_WIDTHS.level} px-2 py-2\`}>` containing `<LevelBadgeCell
    evalData={row.eval} repoName={row.repo} onClick={() => setDrawerOpen(true)} />` AFTER the claudeMd
    `<td>` and BEFORE the gitNexus `<td>`. Render the drawer inline (local-state approach), gated:
    `{row.eval !== undefined && <ClaudeMdLevelDrawer isOpen={drawerOpen} onClose={() =>
    setDrawerOpen(false)} repoName={row.repo} evalData={row.eval} />}`. Reference `row.eval` only as
    property access (never bare `eval`). In `CoverageFamilySection.tsx`: add a `<col
    className={COVERAGE_COL_WIDTHS.level} />` after the claudeMd col, and a sticky `<th>` matching the
    existing header pattern wrapping a `<Tooltip content={coverageColumnTooltips.level}>Level</Tooltip>`
    after the CLAUDE.md `<th>`. In `CoverageFamilySectionMobile.tsx`: add `level: 'Level'` to the mobile
    column labels and render the badge tile alongside the claudeMd tile (planner decision: INCLUDE in
    mobile, treated like the claudeMd cell-block); if the existing mobile structure cannot cleanly host
    a button tile, fall back to omitting level from mobile only (does not affect the 1440x900 gate) and
    note the choice in the SUMMARY. Add/extend a CoverageRow test asserting the level `<td>` mounts
    LevelBadgeCell and that an undefined `row.eval` mounts no drawer.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test coverage && pnpm --filter @agenticapps/dashboard-spa typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "LevelBadgeCell" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 1
    - Source assertion: `grep -c "ClaudeMdLevelDrawer" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 1
    - Source assertion: `grep -c "COVERAGE_COL_WIDTHS.level" packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` returns >= 1
    - Source assertion: `grep -c "coverageColumnTooltips.level" packages/spa/src/components/panels/coverage/CoverageFamilySection.tsx` returns 1
    - `pnpm --filter @agenticapps/dashboard-spa test coverage` green and `typecheck` exits 0
    - Behavior: a row with undefined `row.eval` mounts no drawer (drawer gated on `row.eval !== undefined`)
  </acceptance_criteria>
  <done>The level column renders in the table for every row; clicking a badge opens the drawer; degraded rows show em-dash with no drawer; the family-section header + colgroup include the new column; coverage tests + typecheck are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: IMPECCABLE visual-quality gate for /coverage (composite >= 80)</name>
  <files>.planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-IMPECCABLE.md</files>
  <what-built>
    The Coverage Matrix now has a new L-level badge column and a per-rung drawer. Per the
    project's hard constraint, every frontend-touching phase commits a `15-IMPECCABLE.md`
    artifact scoring the affected route at 1440×900 (composite floor >= 80, ratified
    D-10.5-03.calibration-2). The route to score is `/coverage`, in BOTH the drawer-closed and
    drawer-open states (two screenshots, per UI-SPEC §Table Width Impact).
  </what-built>
  <how-to-verify>
    1. Start the SPA dev server: `pnpm --filter @agenticapps/dashboard-spa dev` (localhost:5174) and ensure the local daemon is running so `/coverage` populates real rows (or use the established fixture/mock path used by prior coverage IMPECCABLE runs).
    2. Run the `impeccable:critique` skill against `/coverage` at 1440×900 with the drawer CLOSED, then OPEN a row's drawer and capture the open state.
    3. Confirm there is NO horizontal scroll at 1440px (table total ~1200px) and the new Level column sits between CLAUDE.md and GitNexus.
    4. Confirm the inferred `~` sigil is visible in both badge and drawer for any inferred-L5 repo, and the inferred-L5 callout is present and not collapsed.
    5. Write `.planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-IMPECCABLE.md` recording the composite + per-heuristic scores + findings + any persona red flags. Composite MUST be >= 80; if a route is structurally below the floor, invoke the per-phase structural-debt waiver clause explicitly in the artifact (per VERIFICATION precedent).
  </how-to-verify>
  <action>Run the `impeccable:critique` skill against `/coverage` at 1440×900 (drawer closed + open), then write `15-IMPECCABLE.md` with composite + per-heuristic scores + findings. This is a human-verified gate per <how-to-verify>; do not auto-approve.</action>
  <verify>
    <human-check>15-IMPECCABLE.md exists with composite >= 80 (or an explicit structural-debt waiver), covering the /coverage route in both drawer-closed and drawer-open states.</human-check>
  </verify>
  <done>`15-IMPECCABLE.md` committed with composite >= 80 (or explicit waiver); no horizontal scroll at 1440×900; inferred sigil + L5 callout visible.</done>
  <resume-signal>Type "approved" once 15-IMPECCABLE.md exists with composite >= 80 (or an explicit waiver), or describe the findings to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon JSON → coverage table render | `row.eval` is rendered per-row in the matrix and drawer; same escaped-interpolation boundary as Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Information Disclosure | Per-row eval rendered in the live matrix | mitigate | Reuses the Plan 03 components which render daemon summaries via React default-escaped interpolation; no raw content and no dangerouslySetInnerHTML introduced in the wiring |
| T-15-04-02 | Denial of Service | A degraded/undefined `row.eval` crashing the table render | mitigate | The drawer is gated on `row.eval !== undefined` and the cell renders an em-dash fallback — a missing/degraded eval yields a degraded cell, never a render crash (mirrors `understand` back-compat) |
| T-15-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (UI-SPEC §Registry Safety); no install task in this plan |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test` green (coverage + new wiring tests)
- `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0
- `pnpm --filter @agenticapps/dashboard-spa lint` clean (CI enforces eslint — run before ship per MEMORY)
- `15-IMPECCABLE.md` exists with composite >= 80 (or explicit structural-debt waiver)
- No horizontal scroll at 1440×900; Level column between claudeMd and gitNexus
</verification>

<success_criteria>
- CML-08 satisfied: the Coverage Matrix shows a per-repo L-level badge column.
- CML-09 satisfied: clicking a row opens the drawer with the per-rung ✓/○ list (+ evidence) and the next-steps list.
- The phase's mandatory `15-IMPECCABLE.md` gate is met at composite >= 80.
</success_criteria>

<output>
Create `.planning/phases/DASH-15-capability-level-engine-fleet-matrix/15-04-SUMMARY.md` when done.
</output>
