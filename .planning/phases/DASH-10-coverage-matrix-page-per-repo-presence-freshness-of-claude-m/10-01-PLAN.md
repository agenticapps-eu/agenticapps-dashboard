---
phase: 10
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/schemas/coverage.ts
  - packages/shared/src/schemas/coverage.test.ts
  - packages/shared/src/index.ts
  - packages/agent/src/lib/scanners/claudeMdScanner.test.ts
  - packages/agent/src/lib/scanners/gitNexusScanner.test.ts
  - packages/agent/src/lib/scanners/wikiScanner.test.ts
  - packages/agent/src/lib/scanners/workflowVersionScanner.test.ts
  - packages/agent/src/lib/scanners/overrideSentinelScanner.test.ts
  - packages/agent/src/lib/repoDiscovery.test.ts
  - packages/agent/src/lib/coverageCache.test.ts
  - packages/agent/src/lib/coverageScan.test.ts
  - packages/agent/src/lib/coverageSpawn.test.ts
  - packages/agent/src/routes/coverage.test.ts
  - packages/spa/src/lib/coverageQueries.test.ts
  - packages/spa/src/components/panels/coverage/CoveragePage.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageToolbar.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageGitNexusBanner.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageCell.test.tsx
  - packages/spa/src/components/panels/coverage/OverrideChip.test.tsx
  - packages/spa/src/components/panels/coverage/CoverageEmptyState.test.tsx
autonomous: true
tdd: true
requirements:
  - COV-01
  - COV-11
must_haves:
  truths:
    - "CoverageResponseSchema, CoverageRowSchema, CoverageRefreshRequestSchema, CoverageRefreshResponseSchema exist as Zod schemas in @agenticapps/dashboard-shared and are exported from packages/shared/src/index.ts"
    - "All freshness state values match the 4-state vocabulary: 'fresh' | 'stale' | 'missing' | 'not-applicable'"
    - "Every Phase 10 production module (5 scanners + repoDiscovery + coverageCache + coverageScan + coverageSpawn + coverage route + coverageQueries + 8 SPA panels) has a RED-state test file committed in this plan"
  artifacts:
    - path: "packages/shared/src/schemas/coverage.ts"
      provides: "Zod schemas for /api/coverage response + /api/coverage/refresh request/response"
      contains: "CoverageResponseSchema"
    - path: "packages/shared/src/schemas/coverage.test.ts"
      provides: "12+ schema validation tests covering happy + 5 invalid shapes per schema"
      contains: "describe('CoverageResponseSchema'"
    - path: "packages/shared/src/index.ts"
      provides: "Barrel re-export of all 4 schemas + types"
      contains: "export { CoverageResponseSchema"
    - path: "packages/agent/src/lib/scanners/claudeMdScanner.test.ts"
      provides: "RED stub test for CLAUDE.md/AGENTS.md presence detection"
      contains: "describe('scanClaudeMd'"
    - path: "packages/agent/src/lib/scanners/gitNexusScanner.test.ts"
      provides: "RED stub for ~/.gitnexus/registry.json array-shape parsing + not-installed + canonical-path matching"
    - path: "packages/agent/src/lib/scanners/wikiScanner.test.ts"
      provides: "RED stub for .wiki-compiler.json source-reference + .compile-state.json freshness"
    - path: "packages/agent/src/lib/scanners/workflowVersionScanner.test.ts"
      provides: "RED stub for head detection from migrations/*.md + per-repo dual-layout SKILL.md probe (5 cases)"
    - path: "packages/agent/src/lib/scanners/overrideSentinelScanner.test.ts"
      provides: "RED stub for sentinel discovery + git log -1 timestamp + mtime fallback"
    - path: "packages/agent/src/lib/repoDiscovery.test.ts"
      provides: "RED stub for one-level-deep walk of 3 family roots + dotfile/node_modules skip"
    - path: "packages/agent/src/lib/coverageCache.test.ts"
      provides: "RED stub for 30s TTL memo: hit-within-ttl, miss-after-ttl, invalidate-on-refresh"
    - path: "packages/agent/src/lib/coverageScan.test.ts"
      provides: "RED stub for orchestrator that fans out 5 scanners and assembles CoverageResponse"
    - path: "packages/agent/src/lib/coverageSpawn.test.ts"
      provides: "RED stub for spawnGitNexusAnalyze + 3 clipboard-string builders (wiki, claude-md, workflow)"
    - path: "packages/agent/src/routes/coverage.test.ts"
      provides: "RED stub for GET /api/coverage + POST /api/coverage/refresh integration tests"
    - path: "packages/spa/src/lib/coverageQueries.test.ts"
      provides: "RED stub for useCoverage + useCoverageRefresh hooks with schema-drift case"
    - path: "packages/spa/src/components/panels/coverage/CoveragePage.test.tsx"
      provides: "RED stub for page-level rendering + empty/error states"
    - path: "packages/spa/src/components/panels/coverage/CoverageToolbar.test.tsx"
      provides: "RED stub for chip multi-select + search debounce + URL round-trip"
    - path: "packages/spa/src/components/panels/coverage/CoverageGitNexusBanner.test.tsx"
      provides: "RED stub for banner copy + clipboard copy-install button"
    - path: "packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx"
      provides: "RED stub for sticky header + aggregate counts + collapse-persistence"
    - path: "packages/spa/src/components/panels/coverage/CoverageRow.test.tsx"
      provides: "RED stub for 4-cell rendering + conditional override chip + refresh popover"
    - path: "packages/spa/src/components/panels/coverage/CoverageCell.test.tsx"
      provides: "RED stub for 4-state icon+label+token rendering (4 states × 4 columns)"
    - path: "packages/spa/src/components/panels/coverage/OverrideChip.test.tsx"
      provides: "RED stub for conditional render (count>0) + expand/collapse + ARIA"
    - path: "packages/spa/src/components/panels/coverage/CoverageEmptyState.test.tsx"
      provides: "RED stub for 4 empty-state branches"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/schemas/coverage.ts"
      via: "re-export"
      pattern: "export \\{ CoverageResponseSchema"
    - from: "packages/agent/src/lib/coverageScan.ts (created in Plan 02)"
      to: "@agenticapps/dashboard-shared"
      via: "import { CoverageResponseSchema }"
      pattern: "import.*CoverageResponseSchema.*from '@agenticapps/dashboard-shared'"
verification_test: "pnpm --filter @agenticapps/dashboard-shared test -- coverage.test.ts (Zod schema validation: 12+ cases, all GREEN); other 20 test files exist and are RED (suite reports failing tests for missing implementations)"
---

<objective>
Establish the wire contract for Phase 10 by creating Zod schemas in @agenticapps/dashboard-shared and committing RED-state test stubs for every production module the subsequent plans will create. This is Wave 0 of the Phase 10 TDD pyramid.

Purpose: Lock the daemon ↔ SPA contract before either side implements anything. Subsequent plans (02-07) will turn each RED test GREEN by implementing the production modules. No production code is written in this plan except the schemas themselves.

Output: 4 Zod schemas (CoverageRowSchema, CoverageResponseSchema, CoverageRefreshRequestSchema, CoverageRefreshResponseSchema) + 21 RED-state test files spanning daemon scanners, daemon orchestrator + cache + spawn, daemon route, SPA query hooks, and SPA component panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CONTEXT.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-RESEARCH.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-VALIDATION.md
@packages/shared/src/index.ts
@packages/shared/src/schemas/overview.ts
@packages/shared/src/schemas/skills.ts

<interfaces>
<!-- Schema files this plan creates that downstream plans (02-07) will import -->

From packages/shared/src/schemas/coverage.ts (NEW):
```typescript
// 4-state freshness vocabulary (UI-SPEC §4 + COV-11)
export const CoverageStateSchema = z.enum(['fresh', 'stale', 'missing', 'not-applicable'])
export type CoverageState = z.infer<typeof CoverageStateSchema>

// 3 family identifiers (D-10-05)
export const CoverageFamilySchema = z.enum(['agenticapps', 'factiv', 'neuroflash'])
export type CoverageFamily = z.infer<typeof CoverageFamilySchema>

// Per-column state with optional detail subtext (e.g. "stale 22d", "1.7.0", "never compiled")
export const CoverageColumnStateSchema = z.object({
  state: CoverageStateSchema,
  label: z.string().optional(),     // human-readable detail e.g. "1.7.0", "stale 22d", "never compiled"
  daysSince: z.number().optional(), // for gitNexus + wiki only
})
export type CoverageColumnState = z.infer<typeof CoverageColumnStateSchema>

// Sentinel entry surfaced by override chip
export const OverrideEntrySchema = z.object({
  phaseSlug: z.string(),
  sinceIso: z.string().optional(),
  source: z.enum(['git-log', 'mtime']),
})

// Per-row matrix entry
export const CoverageRowSchema = z.object({
  family: CoverageFamilySchema,
  repo: z.string(),                  // dirname (e.g. "agenticapps-dashboard")
  absPath: z.string(),               // absolute path on disk (e.g. "/Users/.../agenticapps/agenticapps-dashboard"); used as cache key, never displayed
  claudeMd: CoverageColumnStateSchema,
  gitNexus: CoverageColumnStateSchema,
  wiki: CoverageColumnStateSchema,
  workflowVersion: CoverageColumnStateSchema,
  overrideCount: z.number().int().nonnegative(),
  overrides: z.array(OverrideEntrySchema),   // empty array when overrideCount === 0
})
export type CoverageRow = z.infer<typeof CoverageRowSchema>

// Full /api/coverage response
export const CoverageResponseSchema = z.object({
  schemaVersion: z.literal(1),
  generatedAtIso: z.string(),
  gitNexusInstalled: z.boolean(),    // global signal — drives banner per COV-10
  workflowHeadVersion: z.string().nullable(),
  rows: z.array(CoverageRowSchema),
})
export type CoverageResponse = z.infer<typeof CoverageResponseSchema>

// POST /api/coverage/refresh request body
export const CoverageRefreshActionSchema = z.enum(['gitnexus-analyze'])
// Note: 'wiki-compile', 'claude-md-add', 'workflow-update' are CLIPBOARD-ONLY per D-10-09;
// the daemon never spawns them, so they are not valid action values on this endpoint.

export const CoverageRefreshRequestSchema = z.object({
  family: CoverageFamilySchema,
  repo: z.string(),
  action: CoverageRefreshActionSchema,
})
export type CoverageRefreshRequest = z.infer<typeof CoverageRefreshRequestSchema>

export const CoverageRefreshResponseSchema = z.object({
  ok: z.boolean(),
  kind: z.enum(['ok', 'not-installed', 'timeout', 'error']),
  exitCode: z.number().int().optional(),
  stderr: z.string().optional(),
  updatedRow: CoverageRowSchema.optional(),  // present when kind === 'ok'
})
export type CoverageRefreshResponse = z.infer<typeof CoverageRefreshResponseSchema>
```

From packages/shared/src/index.ts (EDIT — append):
```typescript
export {
  CoverageStateSchema,
  CoverageFamilySchema,
  CoverageColumnStateSchema,
  OverrideEntrySchema,
  CoverageRowSchema,
  CoverageResponseSchema,
  CoverageRefreshActionSchema,
  CoverageRefreshRequestSchema,
  CoverageRefreshResponseSchema,
} from './schemas/coverage.js'
export type {
  CoverageState,
  CoverageFamily,
  CoverageColumnState,
  CoverageRow,
  CoverageResponse,
  CoverageRefreshRequest,
  CoverageRefreshResponse,
} from './schemas/coverage.js'
```
</interfaces>

<security_invariants>
- INV-01: Schemas never carry secret material — only enumerated states + absolute paths (which are NOT exposed to SPA components beyond cache-key use; SPA renders `family + repo`, not `absPath`).
- INV-04: Both daemon (outbound) and SPA (apiFetch) parse these schemas. Drift surfaces as `<SchemaDriftState>` per Phase 4 pattern.
- INV-05: No new native deps — only Zod (already in catalog).
</security_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CoverageStateSchema + CoverageRowSchema + CoverageResponseSchema in packages/shared with 12+ unit tests (GREEN by end of task)</name>
  <files>packages/shared/src/schemas/coverage.ts, packages/shared/src/schemas/coverage.test.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/schemas/coverage.ts (does not exist yet — confirms absence)
    - packages/shared/src/index.ts (existing barrel — read full file to find the append point)
    - packages/shared/src/schemas/overview.ts (REFERENCE — closest existing per-family-style schema pattern)
    - packages/shared/src/schemas/skills.ts (REFERENCE — discriminated state pattern)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CONTEXT.md (D-10-09 amendment: wiki refresh is CLIPBOARD-ONLY, do NOT include 'wiki-compile' in CoverageRefreshActionSchema)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-RESEARCH.md (Schemas section, lines around the "## Architecture Patterns" + workflowVersionScanner code example — confirms 5 workflow-version cases shape)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md §4 (state token mapping for 4 states)
  </read_first>
  <behavior>
    - Test: `CoverageStateSchema.parse('fresh')` accepts all 4 enum values, rejects 'unknown' / 'red' / arbitrary strings.
    - Test: `CoverageFamilySchema.parse('agenticapps')` accepts the 3 enum values, rejects 'personal' / 'archive' / 'shared'.
    - Test: `CoverageColumnStateSchema.parse({ state: 'fresh' })` succeeds; `{ state: 'fresh', label: '1.7.0' }` succeeds; `{ state: 'fresh', daysSince: 7 }` succeeds.
    - Test: `OverrideEntrySchema.parse({ phaseSlug: '01-foo', sinceIso: '2026-05-01T00:00:00Z', source: 'git-log' })` succeeds; `source: 'mtime'` succeeds; `source: 'foo'` fails.
    - Test: `CoverageRowSchema.parse({ family: 'agenticapps', repo: 'dashboard', absPath: '/Users/x/Sourcecode/agenticapps/dashboard', claudeMd: {state:'fresh'}, gitNexus: {state:'not-applicable'}, wiki: {state:'missing'}, workflowVersion: {state:'fresh', label:'1.7.0'}, overrideCount: 0, overrides: [] })` succeeds with all 4 columns + zero overrides.
    - Test: `CoverageRowSchema.parse(...)` with `overrideCount: 2, overrides: [{phaseSlug, source}, {phaseSlug, source}]` succeeds.
    - Test: `CoverageResponseSchema.parse({ schemaVersion: 1, generatedAtIso: '...', gitNexusInstalled: false, workflowHeadVersion: null, rows: [] })` succeeds with empty rows.
    - Test: `CoverageResponseSchema.parse({ schemaVersion: 2, ... })` fails (schemaVersion is `z.literal(1)`).
    - Test: `CoverageRefreshActionSchema.parse('gitnexus-analyze')` succeeds; `parse('wiki-compile')` FAILS (D-10-09 — wiki is clipboard-only, never a valid action).
    - Test: `CoverageRefreshRequestSchema.parse({ family: 'agenticapps', repo: 'dashboard', action: 'gitnexus-analyze' })` succeeds.
    - Test: `CoverageRefreshResponseSchema.parse({ ok: true, kind: 'ok', updatedRow: {...} })` succeeds with valid row.
    - Test: `CoverageRefreshResponseSchema.parse({ ok: false, kind: 'not-installed' })` succeeds (no updatedRow when not ok).
  </behavior>
  <action>
Create three files exactly as specified — the interface block in `<context>` above is the complete and authoritative source for `coverage.ts` content. Copy it verbatim into `packages/shared/src/schemas/coverage.ts` and prefix with the import `import { z } from 'zod'` (catalog version, already a dep of @agenticapps/dashboard-shared).

Write `packages/shared/src/schemas/coverage.test.ts` mirroring the style of `packages/shared/src/schemas/overview.test.ts` and `packages/shared/src/schemas/skills.test.ts` (`describe()` per schema, `it()` per case, use `expect(() => Schema.parse(...)).toThrow()` for negative cases, `expect(Schema.parse(...)).toEqual(...)` for positive). Include exactly the 12 test cases enumerated in `<behavior>` above. Use vitest's `describe` + `it` + `expect` API as the rest of the shared package does.

Append the export block from the interface section to `packages/shared/src/index.ts`. Place it after the existing `SkillFrontmatterSchema` re-export block (which is currently the last export) — keep barrel exports grouped by schema file.

Verify with: `pnpm --filter @agenticapps/dashboard-shared test -- coverage.test.ts` → must pass all 12+ assertions (GREEN). Then `pnpm --filter @agenticapps/dashboard-shared typecheck` → must pass.

Per D-10-01: `CoverageResponseSchema` carries `generatedAtIso` so the SPA can render "last refreshed N seconds ago" without a separate clock-skew workaround.
Per D-10-04 + D-10-09: `CoverageRefreshActionSchema` enum is ONLY `'gitnexus-analyze'` — wiki-compile, claude-md-add, and workflow-update are clipboard-only and are NEVER valid action values on the refresh endpoint.
Per COV-11: `CoverageStateSchema` enumerates exactly 4 values — no `'unknown'`, no `'pending'`, no extras.
  </action>
  <acceptance_criteria>
    - File `packages/shared/src/schemas/coverage.ts` exists and `grep -c "export const " packages/shared/src/schemas/coverage.ts` returns >= 8 (8 schemas + types).
    - File `packages/shared/src/schemas/coverage.test.ts` exists and `grep -c "^\s*it(" packages/shared/src/schemas/coverage.test.ts` returns >= 12.
    - `grep -c "CoverageResponseSchema" packages/shared/src/index.ts` returns exactly 2 (one in `export {`, one in `export type {`).
    - `grep "CoverageStateSchema = z.enum(\['fresh', 'stale', 'missing', 'not-applicable'\])" packages/shared/src/schemas/coverage.ts` returns 1 hit (exact 4-state vocab — COV-11).
    - `grep "CoverageRefreshActionSchema = z.enum(\['gitnexus-analyze'\])" packages/shared/src/schemas/coverage.ts` returns 1 hit (D-10-09: no wiki-compile).
    - `grep -E "schemaVersion: z.literal\(1\)" packages/shared/src/schemas/coverage.ts` returns 1 hit.
    - `pnpm --filter @agenticapps/dashboard-shared test -- coverage.test.ts` exits 0 with all assertions passing.
    - `pnpm --filter @agenticapps/dashboard-shared typecheck` exits 0.
    - `pnpm --filter @agenticapps/dashboard-shared build` exits 0 (dist contains `schemas/coverage.js` + `.d.ts`).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-shared test -- coverage.test.ts && pnpm --filter @agenticapps/dashboard-shared typecheck && pnpm --filter @agenticapps/dashboard-shared build</automated>
  </verify>
  <done>3 files created/edited, 12+ schema tests passing, types exported from barrel, build emits dist artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 10 RED-state daemon test stub files (5 scanners + repoDiscovery + coverageCache + coverageScan + coverageSpawn + coverage.route)</name>
  <files>packages/agent/src/lib/scanners/claudeMdScanner.test.ts, packages/agent/src/lib/scanners/gitNexusScanner.test.ts, packages/agent/src/lib/scanners/wikiScanner.test.ts, packages/agent/src/lib/scanners/workflowVersionScanner.test.ts, packages/agent/src/lib/scanners/overrideSentinelScanner.test.ts, packages/agent/src/lib/repoDiscovery.test.ts, packages/agent/src/lib/coverageCache.test.ts, packages/agent/src/lib/coverageScan.test.ts, packages/agent/src/lib/coverageSpawn.test.ts, packages/agent/src/routes/coverage.test.ts</files>
  <read_first>
    - packages/agent/src/lib/skillsScan.test.ts (REFERENCE — fixture-tmpdir pattern + parseFrontmatter calls)
    - packages/agent/src/lib/projectMetadataScan.test.ts (REFERENCE — multi-scanner test orchestration)
    - packages/agent/src/lib/agentLinterRunner.test.ts (REFERENCE — execa mocking pattern; coverageSpawn.test.ts mirrors this)
    - packages/agent/src/lib/agentLinterCache.test.ts (REFERENCE — TTL memo testing pattern; coverageCache.test.ts mirrors this)
    - packages/agent/src/lib/overviewCache.test.ts (REFERENCE — lazy-expiry single-key memo)
    - packages/agent/src/routes/observability.test.ts (REFERENCE — Hono route integration test pattern with createApp + auth header)
    - packages/agent/src/lib/__fixtures__/ (directory of existing fixture builders)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-RESEARCH.md (Code Examples section — each scanner has a verbatim signature)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-VALIDATION.md (Wave 0 Requirements section — enumerates exact test files)
  </read_first>
  <behavior>
    Each test file lists empty `describe()` blocks with `it.todo()` placeholders for the GREEN-state behaviors that Plans 02-04 will implement. The suite RUNS GREEN (it.todo doesn't fail) but reports pending tests. This is intentional — the RED→GREEN transition happens in Plans 02-04 where each `it.todo` becomes `it` with a real assertion that fails until the production module is written.

    Required `it.todo()` entries (planner-counted minimums — executor can add more, never fewer):

    - **claudeMdScanner.test.ts** (≥3): "scans CLAUDE.md present → fresh via CLAUDE.md", "scans AGENTS.md fallback → fresh via AGENTS.md", "scans neither → missing via none"
    - **gitNexusScanner.test.ts** (≥6): "returns installed=false when ~/.gitnexus absent", "returns installed=true with empty entries when registry.json absent under existing ~/.gitnexus/", "parses RegistryEntry top-level array (NOT { repos: [] })", "rates repo fresh when indexedAt within 14d", "rates repo stale when indexedAt > 14d", "rates repo missing when not in entries", "tries realpath fallback for path canonicalization"
    - **wikiScanner.test.ts** (≥5): "returns missing+'wiki not linked' when .wiki-compiler.json absent", "returns missing+'repo not in sources' when config exists but no source matches", "returns stale+'never compiled' when config has repo but .compile-state.json absent", "returns fresh when last_compiled within 7d", "returns stale when last_compiled > 7d", "returns stale+'compile-state.json invalid' on JSON parse failure"
    - **workflowVersionScanner.test.ts** (≥7): "readWorkflowHeadVersion picks highest-numbered migration's to_version", "returns null when migrations dir absent", "scanWorkflowVersionForRepo: equal=fresh", "behind=stale with installedVersion+headVersion populated", "ahead=fresh with detail='ahead'", "missing-skill=missing", "version field absent → stale with detail='version-unknown' (Pitfall 3 — dashboard's own repo case)", "probes all 4 CANDIDATE_PATHS (canonical+canonical, canonical+bundle, divergent+bundle, divergent+canonical)"
    - **overrideSentinelScanner.test.ts** (≥4): "returns empty array when .planning/phases absent (every clean repo)", "returns empty array when no sentinels exist (verified-current state)", "discovers one sentinel and uses execFileSync git log -1 timestamp when git succeeds", "falls back to mtime when git log fails"
    - **repoDiscovery.test.ts** (≥5): "walks all 3 family roots one level deep", "skips families whose root doesn't exist (existsSync gate)", "skips directories starting with .", "skips node_modules", "accepts both .git/ directory AND .git file (worktree case)", "returns {family, name, absPath} tuples"
    - **coverageCache.test.ts** (≥3 per VALIDATION map): "hit-within-ttl returns same instance", "miss-after-ttl recomputes", "invalidate-on-refresh clears cache so next get re-scans"
    - **coverageScan.test.ts** (≥4): "fans out 5 scanners across N discovered repos", "assembles CoverageResponse passing CoverageResponseSchema.safeParse", "sets gitNexusInstalled=false when scanner reports not-applicable globally (COV-10)", "sets workflowHeadVersion from highest migration"
    - **coverageSpawn.test.ts** (≥5 per VALIDATION map): "spawnGitNexusAnalyze invokes execa with argv-array (not shell string)", "spawnGitNexusAnalyze returns kind=not-installed when binary absent from PATH", "spawnGitNexusAnalyze NEVER calls 'npx gitnexus' (grep assertion against spawn args)", "buildWikiCompileClipboardString returns 'cd ~/Sourcecode/<family> && claude /wiki-compile' (D-10-09: clipboard only, never spawned)", "buildClaudeMdHelpString returns deep-link to /help/operations/install", "buildWorkflowUpdateClipboardString returns '/update-agenticapps-workflow' or equivalent"
    - **coverage.route.test.ts** (≥6): "GET /api/coverage requires bearer token (401 without)", "GET /api/coverage returns 200 with CoverageResponseSchema-conformant body", "GET /api/coverage second call within 30s returns same generatedAtIso (cache hit)", "POST /api/coverage/refresh with body {family,repo,action:'gitnexus-analyze'} routes to spawnGitNexusAnalyze and returns CoverageRefreshResponseSchema", "POST /api/coverage/refresh with action:'wiki-compile' returns 400 (D-10-09 — not a valid action)", "POST /api/coverage/refresh clears the coverage cache so next GET re-scans"

    Total it.todo across 10 files: ≥48 placeholders. These become GREEN tests in Plans 02-04.
  </behavior>
  <action>
For each of the 10 test files, write a minimal vitest stub. Pattern (use literally):

```typescript
// packages/agent/src/lib/scanners/claudeMdScanner.test.ts
import { describe, it } from 'vitest'

describe('claudeMdScanner', () => {
  it.todo('scans CLAUDE.md present → fresh via CLAUDE.md')
  it.todo('scans AGENTS.md fallback → fresh via AGENTS.md')
  it.todo('scans neither → missing via none')
})
```

Each file MUST:
1. Use `describe()` blocks scoped to the module name (e.g. `describe('gitNexusScanner', ...)`)
2. Use `it.todo('...')` for every behavior listed in `<behavior>` above
3. Import nothing from production code (those files don't exist yet)
4. Be runnable via `pnpm --filter @agenticapps/dashboard-agent test -- <filename>` and report N pending (NOT failures)

Create parent directory `packages/agent/src/lib/scanners/` with `mkdir -p` if absent.

Per D-10-09: coverageSpawn.test.ts and coverage.route.test.ts MUST include the explicit assertion that `'wiki-compile'` is REJECTED as an action value. This locks the contract that wiki refresh is clipboard-only.
Per D-5-21 (Phase 5 CSO finding): coverageSpawn.test.ts MUST include the assertion that `'npx gitnexus'` is NEVER in the spawn argv (grep test against captured args).
  </action>
  <acceptance_criteria>
    - All 10 test files exist: `find packages/agent/src/lib/scanners packages/agent/src/lib packages/agent/src/routes -name '*.test.ts' -newer packages/agent/src/lib/skillsScan.test.ts | sort` returns at least the 10 paths.
    - Each scanner test file count: `grep -c "it.todo(" packages/agent/src/lib/scanners/claudeMdScanner.test.ts` ≥ 3; gitNexusScanner ≥ 6; wikiScanner ≥ 5; workflowVersionScanner ≥ 7; overrideSentinelScanner ≥ 4.
    - `grep -c "it.todo(" packages/agent/src/lib/repoDiscovery.test.ts` ≥ 5.
    - `grep -c "it.todo(" packages/agent/src/lib/coverageCache.test.ts` ≥ 3.
    - `grep -c "it.todo(" packages/agent/src/lib/coverageScan.test.ts` ≥ 4.
    - `grep -c "it.todo(" packages/agent/src/lib/coverageSpawn.test.ts` ≥ 5.
    - `grep -c "it.todo(" packages/agent/src/routes/coverage.test.ts` ≥ 6.
    - `grep "wiki-compile.*\(rejected\|not.*valid\|400\|throws\)" packages/agent/src/routes/coverage.test.ts` returns at least 1 hit (D-10-09 contract lock).
    - `grep "npx" packages/agent/src/lib/coverageSpawn.test.ts` returns at least 1 hit asserting `npx` is NEVER in spawn argv (D-5-21).
    - `pnpm --filter @agenticapps/dashboard-agent test -- coverage` exits 0 and the reporter shows ≥48 pending (it.todo) entries.
    - Total pending across the 10 files ≥ 48: `pnpm --filter @agenticapps/dashboard-agent test -- coverage 2>&1 | grep -E "Pending|todo" | head -1` shows a count ≥ 48.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test -- coverage 2>&1 | tee /tmp/agent-cov-todo.log && grep -c "it.todo" packages/agent/src/lib/scanners/*.test.ts packages/agent/src/lib/repoDiscovery.test.ts packages/agent/src/lib/coverageCache.test.ts packages/agent/src/lib/coverageScan.test.ts packages/agent/src/lib/coverageSpawn.test.ts packages/agent/src/routes/coverage.test.ts | awk -F: '{sum+=$2} END {if (sum>=48) exit 0; else exit 1}'</automated>
  </verify>
  <done>10 daemon test files exist with ≥48 it.todo placeholders, suite runs without errors, D-10-09 wiki-rejection assertion + D-5-21 no-npx assertion present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 9 RED-state SPA test stub files (1 lib + 8 panel components)</name>
  <files>packages/spa/src/lib/coverageQueries.test.ts, packages/spa/src/components/panels/coverage/CoveragePage.test.tsx, packages/spa/src/components/panels/coverage/CoverageToolbar.test.tsx, packages/spa/src/components/panels/coverage/CoverageGitNexusBanner.test.tsx, packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx, packages/spa/src/components/panels/coverage/CoverageRow.test.tsx, packages/spa/src/components/panels/coverage/CoverageCell.test.tsx, packages/spa/src/components/panels/coverage/OverrideChip.test.tsx, packages/spa/src/components/panels/coverage/CoverageEmptyState.test.tsx</files>
  <read_first>
    - packages/spa/src/lib/projectQueries.test.ts (REFERENCE — useQuery hook test pattern with mock fetch + drift case)
    - packages/spa/src/components/panels/InstalledSkills.test.tsx (REFERENCE — panel rendering test with empty/loaded/error states)
    - packages/spa/src/components/panels/IntegrationsHealth.test.tsx (REFERENCE — 3-state visual rendering)
    - packages/spa/src/components/panels/SkillHealth.test.tsx (REFERENCE — row expand/collapse + retry button)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md §3 (component composition tree), §5 (interaction states), §6 (empty states), §11 (test coverage contract)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    Each test file lists `describe()` + `it.todo()` placeholders that Plans 05-06 will turn GREEN. Required minimums:

    - **coverageQueries.test.ts** (≥4): "useCoverage fetches /api/coverage and parses CoverageResponseSchema", "useCoverage surfaces schema drift via parseOrDrift", "useCoverageRefresh POSTs to /api/coverage/refresh with body {family,repo,action}", "useCoverage stale-time matches 30s daemon TTL"
    - **CoveragePage.test.tsx** (≥5 per UI-SPEC §11): "renders PageHeader + CoverageToolbar + 3 CoverageFamilySection", "renders CoverageGitNexusBanner when gitNexusInstalled=false", "does NOT render banner when gitNexusInstalled=true", "renders CoverageEmptyState when rows array is empty", "renders SchemaDriftState when drift surfaces"
    - **CoverageToolbar.test.tsx** (≥6 per UI-SPEC §5 + COV-06): "all chip selected by default", "toggling missing chip deselects all", "toggling all 4 non-all chips off auto-selects all", "search input debounces 200ms", "search composes with chip filters via AND semantics", "URL query params (?status=stale&q=neuro) round-trip via validateSearch"
    - **CoverageGitNexusBanner.test.tsx** (≥3 per COV-10): "renders 'GitNexus is not installed' copy", "renders Copy install command button", "click button writes 'npm install -g gitnexus' to clipboard"
    - **CoverageFamilySection.test.tsx** (≥5 per COV-05 + UI-SPEC §5): "renders sticky family header with name", "aggregate counts (✕ N ⚠ N ✓ N) reflect FILTERED rows", "collapse toggle hides body", "collapse state persists to localStorage key coverage:section-collapsed:<family>", "section restored from localStorage on mount"
    - **CoverageRow.test.tsx** (≥5): "renders 4 cells in order: claudeMd, gitNexus, wiki, workflowVersion", "renders OverrideChip ONLY when overrideCount > 0", "renders refresh action button on hover/focus", "click refresh button opens popover with row-specific actions (stale columns drive options)", "keyboard Esc dismisses popover"
    - **CoverageCell.test.tsx** (≥5 per COV-11 + UI-SPEC §4): "renders ✓ icon + bg-status-success/10 token for state=fresh", "renders ⚠ icon + bg-status-warning/10 token for state=stale", "renders ✕ icon + bg-status-error/10 token for state=missing", "renders ⚪ icon + bg-text-tertiary/10 token for state=not-applicable", "renders optional label as subtext (e.g. '1.7.0', 'stale 22d', 'never compiled')"
    - **OverrideChip.test.tsx** (≥5 per COV-07 + UI-SPEC §5): "renders nothing when count === 0", "renders '⚠ 1 override' singular for count=1", "renders '⚠ N overrides' plural for count >= 2", "click toggles aria-expanded between false and true", "expanded body lists OverrideEntry rows formatted as '<phaseSlug> — sentinel since <ISO-date>'"
    - **CoverageEmptyState.test.tsx** (≥4 per UI-SPEC §6): "renders 'No repos match your filters' + Clear filters button for filter-empty case", "renders 'GitNexus is not installed' variant when no-gitnexus", "renders 'Coverage scan failed' + Retry button when daemon-error", "renders 'No git repos found' when zero discovered repos"

    Total it.todo across 9 SPA files: ≥42 placeholders.
  </behavior>
  <action>
For each of the 9 SPA test files, write a minimal vitest stub mirroring the agent-side pattern. Example:

```typescript
// packages/spa/src/components/panels/coverage/CoverageCell.test.tsx
import { describe, it } from 'vitest'

describe('CoverageCell', () => {
  it.todo('renders ✓ icon + bg-status-success/10 token for state=fresh')
  it.todo('renders ⚠ icon + bg-status-warning/10 token for state=stale')
  it.todo('renders ✕ icon + bg-status-error/10 token for state=missing')
  it.todo('renders ⚪ icon + bg-text-tertiary/10 token for state=not-applicable')
  it.todo('renders optional label as subtext')
})
```

Create directory `packages/spa/src/components/panels/coverage/` with `mkdir -p`.

For component tests (.tsx files), no imports from production code (those modules don't exist yet) — pure `it.todo` blocks compile cleanly with no module-resolution errors.

Per UI-SPEC §4: CoverageCell.test.tsx asserts the EXACT Tailwind class tokens (`bg-status-success/10`, `bg-status-warning/10`, `bg-status-error/10`, `bg-text-tertiary/10`) — these are non-negotiable Phase 05.1 tokens. Never use hex literals, shadcn aliases (`bg-foreground`), or new tokens.
Per COV-06: CoverageToolbar.test.tsx URL-roundtrip case uses TanStack Router `validateSearch` (see Phase 7 router.tsx pattern).
Per COV-07: OverrideChip.test.tsx tests STRICTLY that count===0 renders NOTHING (not an empty state) — verifies the "no '0 overrides' pollution" pitfall (RESEARCH Pitfall 5).
  </action>
  <acceptance_criteria>
    - All 9 SPA test files exist: `find packages/spa/src/lib packages/spa/src/components/panels/coverage -name 'coverage*.test.*' -o -name 'CoverageCell.test.*' -o -name 'OverrideChip.test.*' | wc -l` returns ≥ 9.
    - `grep -c "it.todo(" packages/spa/src/lib/coverageQueries.test.ts` ≥ 4.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoveragePage.test.tsx` ≥ 5.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageToolbar.test.tsx` ≥ 6.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageGitNexusBanner.test.tsx` ≥ 3.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageFamilySection.test.tsx` ≥ 5.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageRow.test.tsx` ≥ 5.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageCell.test.tsx` ≥ 5.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/OverrideChip.test.tsx` ≥ 5.
    - `grep -c "it.todo(" packages/spa/src/components/panels/coverage/CoverageEmptyState.test.tsx` ≥ 4.
    - Total ≥ 42: `grep -c "it.todo" packages/spa/src/lib/coverageQueries.test.ts packages/spa/src/components/panels/coverage/*.test.tsx | awk -F: '{sum+=$2} END {if (sum>=42) exit 0; else exit 1}'`.
    - `grep "bg-status-success/10\|bg-status-warning/10\|bg-status-error/10\|bg-text-tertiary/10" packages/spa/src/components/panels/coverage/CoverageCell.test.tsx | wc -l` ≥ 4 (Phase 05.1 token lock).
    - `pnpm --filter @agenticapps/dashboard-spa test -- coverage` exits 0 with all entries pending.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- coverage 2>&1 | tee /tmp/spa-cov-todo.log && grep -c "it.todo" packages/spa/src/lib/coverageQueries.test.ts packages/spa/src/components/panels/coverage/*.test.tsx | awk -F: '{sum+=$2} END {if (sum>=42) exit 0; else exit 1}'</automated>
  </verify>
  <done>9 SPA test files exist with ≥42 it.todo placeholders, vitest reports them as pending, Phase 05.1 token assertions baked in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA ↔ daemon | Schema parses on both sides (CoverageResponseSchema). Drift surfaces via SchemaDriftState. No path strings leave the daemon. |
| Daemon ↔ filesystem | Read-only on registered project FS. The 4 new external roots (`~/.gitnexus`, `~/Sourcecode/{agenticapps,factiv,neuroflash}`) go through `resolveAllowedNamed` only — added in Plan 03 (this plan just locks the schema shape). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | CoverageRefreshActionSchema | mitigate | Action enum is ONLY `'gitnexus-analyze'`; `'wiki-compile'`, `'workflow-update'`, `'claude-md-add'` are NEVER valid action values (D-10-09). Schema rejects them at request-body parse time. |
| T-10-01-02 | Information Disclosure | CoverageRowSchema | mitigate | `absPath` field is internal to daemon (used as cache key); SPA components render `family + repo` only, never absolute path. CoverageRowSchema permits absPath because daemon needs it for refresh routing, but SPA UI MUST NOT display it (verified in Plan 06 component tests). |
| T-10-01-03 | Spoofing | CoverageResponseSchema.schemaVersion | mitigate | `z.literal(1)` — any schemaVersion drift (future bump) surfaces as schema-drift on both ends, never silently parses. |
| T-10-01-04 | Repudiation | OverrideEntrySchema.source | accept | `source: 'git-log' | 'mtime'` is signal-of-signal — the trustworthy source is `git-log` (commit timestamp). Plan 02 scanner prefers git-log; mtime is fallback only. Forgery via touching the sentinel file is intentional (the override mechanism IS user-controlled). |
</threat_model>

<verification>
- All schemas parse correctly (12+ tests GREEN).
- Barrel export wired (downstream plans can `import { CoverageResponseSchema } from '@agenticapps/dashboard-shared'`).
- Total ≥ 90 it.todo placeholders across 21 test files (48 daemon + 42 SPA).
- Build artifacts emit (`dist/schemas/coverage.{js,d.ts}`).
- TypeScript strict-mode passes for shared package.
</verification>

<success_criteria>
- COV-01 anchored: CoverageResponseSchema exists and is the wire contract for `GET /api/coverage`.
- COV-11 anchored: CoverageStateSchema enumerates the exact 4-state vocabulary.
- D-10-09 anchored: CoverageRefreshActionSchema rejects `'wiki-compile'` at schema parse — locked in test.
- D-5-21 anchored: coverageSpawn.test.ts asserts no `npx` in spawn argv (locks the CSO contract).
- Wave 0 complete per 10-VALIDATION.md Sampling Rate.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-01-SUMMARY.md` documenting schemas + the test stubs created. Note the total it.todo count and confirm D-10-09 + D-5-21 contracts are encoded in tests.
</output>
