/**
 * Plan 07-02 placeholder — 07-03 owns this file in its worktree.
 * @see ./RepoTopologyMap.stub.tsx for full context.
 */
export default function GatePickerPlaceholder(): React.JSX.Element {
  return <div>GatePicker stub (Plan 07-02 placeholder; 07-03 ships canonical)</div>
}
