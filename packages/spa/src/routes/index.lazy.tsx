import { createLazyRoute } from '@tanstack/react-router'

// TODO Plan 05: index placeholder — replace with multi-project home view
export const Route = createLazyRoute('/')({
  component: () => <p>TODO Plan 05: index placeholder</p>,
})
