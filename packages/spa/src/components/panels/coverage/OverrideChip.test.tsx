/**
 * Test scaffold for OverrideChip.tsx — conditional chip for override sentinel entries.
 * Plan 06 implements; Plan 01 provides the it.todo placeholders.
 */

import { describe, it } from 'vitest'

describe('OverrideChip', () => {
  it.todo('renders nothing when overrideCount === 0 (conditional render guard)')
  it.todo('renders chip with count badge when overrideCount > 0')
  it.todo('clicking the chip expands to show the list of override entries with phaseSlug + sinceIso')
  it.todo('clicking the chip again collapses the expanded list')
  it.todo(
    'has correct ARIA attributes: aria-expanded on toggle button, accessible list role on entries'
  )
})
