---
phase: 14-understand-anything-integration-daemon-hosted-knowledge-grap
plan: 04
type: tdd
wave: 2
depends_on: [14-01]
files_modified:
  - packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx
  - packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.test.tsx
  - packages/spa/src/routes/code-intelligence.lazy.tsx
  - packages/spa/src/router.tsx
  - packages/spa/src/components/ui/Sidebar.tsx
  - packages/spa/src/components/ui/Sidebar.test.tsx
autonomous: true
requirements: [D-14-02, D-14-06, D-14-07]

must_haves:
  truths:
    - "A new 'Code Intelligence' sidebar section (between Observability and ACCOUNT) contains a 'Knowledge graphs' item routing to /code-intelligence (D-14-06, user's sidebar-architecture preference: section with growth room)"
    - "The page lists analyzed projects (understand state fresh or stale) with staleness indication and new-tab viewer links (D-14-06, D-14-07)"
    - "When the viewer is not installed, the page shows an install hint naming `agentic-dashboard install-understand-viewer`; when health.understand.updateAvailable is true it shows an update hint with the same command (D-14-02)"
  artifacts:
    - path: "packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx"
      provides: "Analyzed-projects listing page"
      min_lines: 60
    - path: "packages/spa/src/routes/code-intelligence.lazy.tsx"
      provides: "Lazy route module"
      contains: "createLazyRoute"
  key_links:
    - from: "packages/spa/src/components/ui/Sidebar.tsx"
      to: "/code-intelligence"
      via: "SidebarSection + SidebarItem"
      pattern: "code-intelligence"
    - from: "packages/spa/src/router.tsx"
      to: "packages/spa/src/routes/code-intelligence.lazy.tsx"
      via: "createRoute().lazy(import)"
      pattern: "code-intelligence.lazy"
    - from: "packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx"
      to: "useCoverage + useHealth"
      via: "TanStack Query hooks"
      pattern: "useCoverage"
---

<objective>
Add the Code Intelligence sidebar section and the /code-intelligence page: a listing of analyzed projects (from coverage rows' understand column) with viewer links, staleness labels, and viewer-install/update hints from /health.

Purpose: D-14-06 — dedicated discoverability surface for knowledge graphs, built as a section with growth room (future GitNexus explorer); D-14-02 update-available hint.
Output: new page + lazy route + router entry + sidebar section.

Naming (Claude's discretion resolved): section label "Code Intelligence", item label "Knowledge graphs", route path `/code-intelligence`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-CONTEXT.md
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-PATTERNS.md

<interfaces>
From packages/shared (plan 14-01, read shipped source):
- `CoverageRow.understand?` block — state 'fresh'|'stale'|'missing'|'not-applicable', viewerToken?, lastAnalyzedAt?, analyzedFiles?
- `HealthResponse.understand?` — { viewerInstalled, viewerVersion, pluginVersion, updateAvailable }

From packages/spa (verified):
- Sidebar sections at packages/spa/src/components/ui/Sidebar.tsx lines ~48 (WORKSPACE), ~75 (Observability), ~93 (ACCOUNT) — insert the new section between Observability and ACCOUNT
- `SidebarSection` / `SidebarItem` primitives (use SidebarItem, NOT SidebarSubItem, per user's recorded sidebar preference)
- Route analog: packages/spa/src/routes/observability.skill-drift.lazy.tsx + router.tsx route definitions (~lines 125-141) + routeTree.addChildren (~lines 179-192)
- Page analog: packages/spa/src/components/panels/skill-drift/SkillDriftPage.tsx (PageHeader, loading/error/empty states)
- Hooks: useCoverage (coverageQueries), useHealth (healthQueries), getPairing (pairing.ts)
- D-5.1-10: NO cn()/clsx/CVA, NO hex literals
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CodeIntelligencePage component</name>
  <files>packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx, packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.test.tsx</files>
  <read_first>
    - packages/spa/src/components/panels/skill-drift/SkillDriftPage.tsx (full page anatomy: PageHeader, query states, table/list layout, EmptyState)
    - packages/spa/src/lib/coverageQueries.ts and packages/spa/src/lib/healthQueries.ts (hook signatures + query keys)
    - packages/spa/src/lib/pairing.ts (getPairing)
    - 14-03-SUMMARY.md if present (whether buildViewerUrl was lifted to packages/spa/src/lib/understandViewerUrl.ts — import it if so, else define the same construction locally)
  </read_first>
  <behavior>
    - Test 1: coverage rows with understand.state 'fresh' or 'stale' are listed (repo name, family, analyzedFiles, lastAnalyzedAt, staleness label for stale); rows with 'missing'/'not-applicable'/undefined are excluded
    - Test 2: each listed row with a viewerToken renders an "Open viewer" link, target="_blank" rel="noopener noreferrer", href `{agentUrl}/understand/{family}/{repo}/?token={encodeURIComponent(viewerToken)}` (D-14-07)
    - Test 3: health.understand.viewerInstalled === false → page shows install hint containing the exact string `agentic-dashboard install-understand-viewer`; viewer links suppressed (daemon would 503 on assets)
    - Test 4: health.understand.updateAvailable === true → update hint rendered containing `agentic-dashboard install-understand-viewer` and both versions (viewerVersion → pluginVersion) (D-14-02)
    - Test 5: zero analyzed repos → EmptyState explaining how to analyze (mentions the `/understand` skill / copy command from coverage)
    - Test 6: loading and error query states render the established skeleton/error patterns
  </behavior>
  <action>
    Create CodeIntelligencePage following SkillDriftPage anatomy: PageHeader (title "Knowledge graphs", subtitle describing understand-anything graphs; sticky per the page-level convention if SkillDriftPage opts in — match it), then hint banners (install hint when !viewerInstalled, update hint when updateAvailable), then the analyzed-projects list grouped or flat-sorted by family/repo, each row: repo label, family, `analyzedFiles` count, `lastAnalyzedAt` (human-readable), stale badge when state==='stale', and the new-tab viewer link. Data: useCoverage rows filtered to understand fresh/stale + useHealth for the understand block. URL construction identical to 14-03 Task 3 (`/understand/{family}/{repo}/?token=`). Handle health.understand undefined (old daemon) as viewer-not-installed.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- --run src/components/panels/code-intelligence/CodeIntelligencePage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior tests green
    - `grep -c "install-understand-viewer" packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx` ≥ 1
    - `grep -cE "clsx|cn\(|#[0-9a-fA-F]{3,6}" packages/spa/src/components/panels/code-intelligence/CodeIntelligencePage.tsx` returns 0
    - Negative assertion in tests: pairing.token fixture absent from all hrefs
  </acceptance_criteria>
  <done>Page lists analyzed projects with viewer links + install/update hints, all states covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy route + router entry + Code Intelligence sidebar section</name>
  <files>packages/spa/src/routes/code-intelligence.lazy.tsx, packages/spa/src/router.tsx, packages/spa/src/components/ui/Sidebar.tsx, packages/spa/src/components/ui/Sidebar.test.tsx</files>
  <read_first>
    - packages/spa/src/routes/observability.skill-drift.lazy.tsx (exact lazy-route file shape to copy)
    - packages/spa/src/router.tsx (createRoute definitions ~lines 125-141, appShellLayoutRoute parent, routeTree.addChildren ~lines 179-192)
    - packages/spa/src/components/ui/Sidebar.tsx (SidebarSection/SidebarItem usage; section order WORKSPACE → Observability → ACCOUNT; lucide icon imports at top)
    - packages/spa/src/components/ui/Sidebar.test.tsx if present (section/item assertions to extend)
  </read_first>
  <behavior>
    - Test 1: Sidebar renders a "Code Intelligence" section between Observability and ACCOUNT, containing a "Knowledge graphs" item linking to /code-intelligence
    - Test 2: navigating to /code-intelligence renders CodeIntelligencePage (router-level test, or lazy-route export shape assertion matching the skill-drift precedent)
  </behavior>
  <action>
    Create routes/code-intelligence.lazy.tsx: `createLazyRoute('/code-intelligence')({ component: CodeIntelligencePage })` importing from '../components/panels/code-intelligence/CodeIntelligencePage.js'. In router.tsx add `codeIntelligenceRoute = createRoute({ getParentRoute: () => appShellLayoutRoute, path: '/code-intelligence' }).lazy(() => import('./routes/code-intelligence.lazy.js').then((m) => m.Route))` and append to routeTree.addChildren after conformanceRoute with comment `Phase 14 D-14-06`. In Sidebar.tsx import `Network` from lucide-react, insert `<SidebarSection label="Code Intelligence">` with `<SidebarItem to="/code-intelligence" icon={<Network size={16} aria-hidden="true" />} label="Knowledge graphs" />` between the Observability and ACCOUNT sections. Match the existing label-casing convention used by the Observability section (it renders "Observability", not all-caps — mirror exactly what the source shows).
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test -- --run src/components/ui/Sidebar.test.tsx && pnpm --filter @agenticapps/dashboard-spa typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar test asserts section ordering: Code Intelligence appears after Observability and before ACCOUNT
    - `grep -c "code-intelligence" packages/spa/src/router.tsx` ≥ 2 (route path + lazy import)
    - `pnpm --filter @agenticapps/dashboard-spa build` succeeds (lazy chunk resolves)
  </acceptance_criteria>
  <done>Route reachable from the sidebar; section built with growth room for future GitNexus explorer entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → new tab | Viewer URLs with scoped token enter browser history (same posture as 14-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Information Disclosure (token in URL) | viewer links | mitigate (residual accepted per D-14-03) | Scoped repo-bound read-only token only; bearer token negative-asserted in tests |
| T-14-04-02 | Tampering (reverse tabnabbing) | viewer links | mitigate | rel="noopener noreferrer" asserted by test |
| T-14-04-03 | Information Disclosure | page renders graph metadata | accept | Only counts/timestamps from the bearer-authed coverage payload; no file paths rendered |
| T-14-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-spa test` green
- `pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build` green
</verification>

<success_criteria>
- /code-intelligence reachable via the new sidebar section; lists analyzed projects with viewer links; install/update hints driven by /health understand block
</success_criteria>

<output>
Create `.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-04-SUMMARY.md` when done.
</output>
