---
phase: 14-understand-anything-integration-daemon-hosted-knowledge-grap
plan: 06
type: tdd
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - packages/agent/src/lib/scanners/understandScanner.ts
  - packages/agent/src/lib/scanners/understandScanner.test.ts
  - packages/agent/src/lib/coverageScan.ts
  - packages/agent/src/lib/coverageScan.test.ts
  - packages/agent/src/routes/health.ts
  - packages/agent/src/routes/health.test.ts
autonomous: true
requirements: [D-14-02, D-14-03, D-14-08, D-14-09]

must_haves:
  truths:
    - "Every coverage row carries understand state: fresh when meta.json gitCommitHash equals the repo HEAD, stale when it differs, missing when .understand-anything/meta.json is absent or unparseable (D-14-08)"
    - "Fresh/stale rows carry a per-repo scoped viewerToken minted via the 14-02 HMAC helper (D-14-03 link carrier)"
    - "/health exposes understand: { viewerInstalled, viewerVersion, pluginVersion, updateAvailable } (D-14-02 detect + hint)"
    - "Detection is pure FS reads — no subprocess (Phase 10.6 'detection without execution' discipline)"
  artifacts:
    - path: "packages/agent/src/lib/scanners/understandScanner.ts"
      provides: "readRepoHeadSha() + scanUnderstandForRepo()"
      exports: ["readRepoHeadSha", "scanUnderstandForRepo"]
      min_lines: 50
    - path: "packages/agent/src/lib/coverageScan.ts"
      provides: "understand column in buildRow() Promise.allSettled fan-out"
      contains: "scanUnderstandForRepo"
    - path: "packages/agent/src/routes/health.ts"
      provides: "understand block in HealthResponse payload"
      contains: "viewerInstalled"
  key_links:
    - from: "packages/agent/src/lib/coverageScan.ts"
      to: "packages/agent/src/lib/viewerToken.ts"
      via: "mintViewerToken(repoId) for fresh/stale rows"
      pattern: "mintViewerToken"
    - from: "packages/agent/src/routes/health.ts"
      to: "packages/agent/src/lib/viewerInstall.ts"
      via: "getInstalledViewerVersion / getNewestPluginCacheVersion"
      pattern: "getInstalledViewerVersion"
---

<objective>
Wire understand-anything status detection into the coverage scan and expose viewer install/version state on /health.

Purpose: D-14-08 staleness semantics feed the SPA column (14-03) and listing page (14-04); D-14-02 drift detection feeds the install/update hints.
Output: understandScanner module, coverageScan integration, health extension — all TDD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-CONTEXT.md
@.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-PATTERNS.md

<interfaces>
From plan 14-01 (shared, shipped): `CoverageRow.understand` optional block — kind 'basic', state CoverageStateSchema ('fresh'|'stale'|'missing'|'not-applicable'), lastAnalyzedAt?, analyzedCommit?, analyzedFiles?, viewerToken?, degraded?, degradedReason?. `HealthResponse.understand` — { viewerInstalled, viewerVersion: string|null, pluginVersion: string|null, updateAvailable } .strict().optional().

From plan 14-02 (agent, shipped): `mintViewerToken(repoId)`, `getInstalledViewerPath/Version()`, `getNewestPluginCacheVersion()`.

Live meta.json schema (verified at ~/Sourcecode/agenticapps/claude-workflow/.understand-anything/meta.json):
`{ "lastAnalyzedAt": ISO8601, "gitCommitHash": <full 40-char SHA>, "version": "1.0.0", "analyzedFiles": 110 }`

14-PATTERNS.md note 5 (verified): NO existing coverage scanner reads git HEAD as a full SHA — readRepoHeadSha() is new, pure-FS (.git/HEAD → refs/heads/<branch> → packed-refs fallback).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: understandScanner.ts — readRepoHeadSha + scanUnderstandForRepo</name>
  <files>packages/agent/src/lib/scanners/understandScanner.ts, packages/agent/src/lib/scanners/understandScanner.test.ts</files>
  <read_first>
    - packages/agent/src/lib/scanners/gitNexusScanner.ts (scanner module conventions: state types, pure-FS probes, test fixture style in gitNexusScanner.test.ts)
    - 14-PATTERNS.md §understandScanner (readRepoHeadSha sketch: ref-form, detached-HEAD form, packed-refs fallback)
  </read_first>
  <behavior>
    - Test 1 (readRepoHeadSha): fixture .git with HEAD `ref: refs/heads/main` + refs/heads/main file → returns the full SHA; detached HEAD (raw SHA in HEAD) → returns it; ref present only in packed-refs → returns the packed SHA; no .git → null
    - Test 2 (scan fresh): meta.json gitCommitHash === head SHA → { state: 'fresh', lastAnalyzedAt, analyzedCommit: <7-char short>, analyzedFiles }
    - Test 3 (scan stale): hash differs → state 'stale' with same metadata
    - Test 4 (scan missing): no .understand-anything/meta.json → { state: 'missing' }; malformed JSON → { state: 'missing' } (no throw); head SHA null but meta present → 'stale' (conservative: cannot prove freshness)
  </behavior>
  <action>
    Create understandScanner.ts exporting `readRepoHeadSha(repoRoot): string | null` (pure FS per the 14-PATTERNS.md sketch — .git/HEAD trim, 'ref: ' prefix branch, refs file, packed-refs line scan; try/catch → null) and `scanUnderstandForRepo(repoRoot, currentHeadSha: string | null): UnderstandScanResult` where UnderstandScanResult is `{ state: 'fresh' | 'stale' | 'missing'; lastAnalyzedAt?: string; analyzedCommit?: string; analyzedFiles?: number }` — note 'fresh' not 'present' (matches the shared CoverageStateSchema enum). Comparison per D-14-08: strict full-SHA equality; null head ⇒ stale. analyzedCommit = gitCommitHash.slice(0, 7). No subprocess anywhere in the module.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test -- --run src/lib/scanners/understandScanner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behavior groups green including packed-refs and detached-HEAD paths
    - `grep -cE "execa|execSync|spawn" packages/agent/src/lib/scanners/understandScanner.ts` returns 0
    - Malformed meta.json case does not throw (test proves)
  </acceptance_criteria>
  <done>Deterministic 3-state detection from pure FS reads, vocabulary aligned with the wire enum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coverageScan.ts integration — understand column + viewerToken mint</name>
  <files>packages/agent/src/lib/coverageScan.ts, packages/agent/src/lib/coverageScan.test.ts</files>
  <read_first>
    - packages/agent/src/lib/coverageScan.ts (buildRow() Promise.allSettled fan-out ~lines 163-260; the gitNexus column construction + degraded-row branch is the exact template; AGREED-2 degraded pattern)
    - packages/agent/src/lib/coverageScan.test.ts (row fixture + scanner stub conventions)
    - packages/agent/src/lib/viewerToken.ts (mintViewerToken — needs the secret loaded; check how tests seed it via ensureViewerSecretFile with tmp path)
  </read_first>
  <behavior>
    - Test 1: repo fixture with fresh meta → row.understand = { kind: 'basic', state: 'fresh', lastAnalyzedAt, analyzedCommit, analyzedFiles, viewerToken: <verifies back to this row's family/repo id> }
    - Test 2: stale fixture → state 'stale' WITH viewerToken (stale rows keep their link per D-14-10)
    - Test 3: missing fixture → { kind: 'basic', state: 'missing' } with NO viewerToken key
    - Test 4: scanner rejection (allSettled rejected branch) → { kind: 'basic', state: 'missing', degraded: true, degradedReason } and the row's degraded list gains an 'understand: …' entry — full payload still parses against CoverageResponseSchema
  </behavior>
  <action>
    In buildRow(), add a sixth entry to the Promise.allSettled array: `scanUnderstandForRepo(repoAbsPath, readRepoHeadSha(repoAbsPath))`. Build the understand column mirroring the gitNexus column construction: fulfilled ⇒ spread scan result into the wire shape, adding `viewerToken: mintViewerToken(repoId)` only when state is 'fresh' or 'stale' (repoId is the row's `family/repo` id already computed in scanCoverageInternal); rejected ⇒ degraded missing-state object per AGREED-2 with reason pushed to rowDegraded. Ensure the response still parses via the existing outbound CoverageResponseSchema path (the optional understand field from 14-01 makes this additive). Do NOT cache tokens; mint is a cheap HMAC.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test -- --run src/lib/coverageScan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 round-trips the row's viewerToken through verifyViewerToken back to the exact row repoId (cross-module proof of D-14-03 binding)
    - Missing rows have no viewerToken property (Test 3 asserts absence, not undefined-value)
    - Full pre-existing coverageScan suite green (no regression in the 5 existing columns)
  </acceptance_criteria>
  <done>/api/coverage rows carry understand state + repo-bound viewer tokens; degraded path proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /health understand block</name>
  <files>packages/agent/src/routes/health.ts, packages/agent/src/routes/health.test.ts</files>
  <read_first>
    - packages/agent/src/routes/health.ts (payload assembly + gitnexus block + outbound() usage — the exact extension point)
    - packages/agent/src/routes/health.test.ts (existing assertions to extend)
    - packages/agent/src/lib/viewerInstall.ts (helper signatures)
  </read_first>
  <behavior>
    - Test 1: viewer installed (fixture dir) + plugin cache has same version → understand: { viewerInstalled: true, viewerVersion: '2.7.6', pluginVersion: '2.7.6', updateAvailable: false }
    - Test 2: plugin cache has newer version than installed → updateAvailable: true (D-14-02 hint trigger)
    - Test 3: nothing installed → { viewerInstalled: false, viewerVersion: null, pluginVersion: <null or version> , updateAvailable: false }
    - Test 4: response still parses through outbound(HealthResponseSchema) — and contains NO token field (negative assertion)
  </behavior>
  <action>
    Extend the health payload after the gitnexus block: `understand: { viewerInstalled, viewerVersion, pluginVersion, updateAvailable: viewerVersion !== null && pluginVersion !== null && viewerVersion !== pluginVersion }` sourced from getInstalledViewerVersion()/getNewestPluginCacheVersion() (call per-request like the gitnexus fields, or compute at module init mirroring whichever pattern health.ts currently uses for gitnexus — match it). Keep outbound(HealthResponseSchema...) as the response path. Comment-tag `Phase 14 D-14-02: detect + hint, manual re-run — no auto-rebuild`. Use the viewerInstall helpers' injectable baseDir for tests.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-agent test -- --run src/routes/health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behavior tests green; updateAvailable truth-table covered (same-version false, newer-plugin true, not-installed false)
    - Negative assertion: serialized health response contains no 'viewerToken' / token material
    - `pnpm --filter @agenticapps/dashboard-agent test` fully green
  </acceptance_criteria>
  <done>/health drives the SPA install/update hints with zero token exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo .git + .understand-anything files → daemon | Untrusted repo-controlled file contents parsed by the scanner |
| daemon → SPA wire | Tokens + metadata cross to the browser over bearer-authed routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Denial of Service | scanner parsing hostile meta.json/.git files | mitigate | try/catch → 'missing'/null on every parse; allSettled degraded-row isolation (Task 1 Test 4, Task 2 Test 4); no subprocess |
| T-14-06-02 | Information Disclosure | viewerToken in coverage rows | mitigate (residual accepted) | Coverage route stays behind main bearer auth; tokens are repo-bound read-only scope; minted only for fresh/stale rows |
| T-14-06-03 | Information Disclosure | token in /health | mitigate | understand block carries versions only; negative assertion test (Task 3 Test 4) |
| T-14-06-04 | Spoofing | wrong-repo token on a row | mitigate | Task 2 Test 1 round-trip: each row's token verifies back to that row's repoId |
| T-14-06-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `pnpm --filter @agenticapps/dashboard-agent test` fully green
- `pnpm -r typecheck` green
- 14-VALIDATION.md D-14-08 family (fresh/stale/missing) fully covered by understandScanner.test.ts + coverageScan.test.ts
</verification>

<success_criteria>
- Coverage rows + health block live with D-14-08 semantics and D-14-03 token plumbing, zero subprocess additions
</success_criteria>

<output>
Create `.planning/phases/DASH-14-understand-anything-integration-daemon-hosted-knowledge-grap/14-06-SUMMARY.md` when done.
</output>
