---
phase: 11.2
plan: "04"
subsystem: spa/coverage
tags:
  - coverage
  - touch-target
  - apple-hig
  - a11y
  - tdd
  - p2
dependency_graph:
  requires:
    - "11.2-02: CoverageRow pending/non-pending className branches (p-0.5 replaced)"
    - "11.2-03: coverageColumns.ts wiki value (w-72 preserved)"
  provides:
    - "COVERAGE_COL_WIDTHS.actions widened to w-12 (48px)"
    - "Refresh button 44×44px hit area in both pending + idle states"
    - "Apple HIG touch target compliance for the per-row refresh action"
  affects:
    - "CoverageFamilySection <colgroup> (via SoT — automatic)"
    - "CoverageRow <td> actions cell (via SoT — automatic)"
    - "Plan 05, Plan 06 (Wave 1) — inherit the widened actions column + button"
tech_stack:
  added: []
  patterns:
    - "TDD red-green-refactor with atomic commits"
    - "Single source of truth (coverageColumns.ts) for column widths"
    - "Apple HIG 44px touch target: min-w/h + padding math (2×15px + 14px = 44px)"
    - "Comment chain history pattern (PD-11.2-02) from Plan 03 precedent"
key_files:
  created: []
  modified:
    - packages/spa/src/components/panels/coverage/coverageColumns.ts
    - packages/spa/src/components/panels/coverage/coverageColumns.test.ts
    - packages/spa/src/components/panels/coverage/CoverageRow.tsx
    - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
decisions:
  - "D-11.2-11: refresh button min-w-[44px] min-h-[44px] + p-[15px]; icon stays size={14} for visual density"
  - "D-11.2-12: actions column w-8 (32px) → w-12 (48px); 44px button + 4px breathing room; net table delta with Plan 03: −48px"
metrics:
  duration: "~10 minutes"
  completed: "2026-05-18"
  tasks_completed: 2
  files_modified: 4
---

# Phase 11.2 Plan 04: Actions Column Touch Target Summary

**One-liner:** Refresh button widened to 44×44px Apple HIG minimum (p-[15px] + min-w/h-[44px]) with actions column bumped from w-8 to w-12 to accommodate, closing P2 #4 from 11.1-IMPECCABLE.md.

## What Was Built

Plan 04 closes P2 #4 from `11.1-IMPECCABLE.md`: the per-row refresh button had a ~18×18px hit area (padding `p-0.5` = 2px each side + 14px icon), well below Apple HIG's 44px minimum. "iPad-Donald" persona red flag.

Two coordinated changes in the single-writer pattern:

### Task 1 — Actions column width: w-8 → w-12

**`packages/spa/src/components/panels/coverage/coverageColumns.ts`**
- Changed `actions: 'w-8'` to `actions: 'w-12'` (32px → 48px)
- Added 4-line comment chain entry (PD-11.2-02) referencing D-11.2-12, Apple HIG 44px touch target, the button geometry (min-w/h + p-[15px]), and net table delta
- The wiki value `w-72` (Plan 03) is preserved exactly

**`packages/spa/src/components/panels/coverage/coverageColumns.test.ts`**
- Updated the actions assertion from `toBe('w-8')` to `toBe('w-12')` (D-11.2-12)
- All 6 regression-lock assertions remain (repo, claudeMd, gitNexus, wiki, workflow, actions)

### Task 2 — Refresh button hit area: ~18px → 44px

**`packages/spa/src/components/panels/coverage/CoverageRow.tsx`**
- Both pending and non-pending className branches updated: `p-0.5` replaced with `min-w-[44px] min-h-[44px] p-[15px]`
- Padding math: `2 × 15px + 14px (icon) = 44px` — meets HIG minimum
- Icon size stays `size={14}` — visual density preserved (D-11.2-11)
- `animate-spin`, `aria-busy`, `disabled`, `opacity-100` (Plan 02 contracts) all retained

**`packages/spa/src/components/panels/coverage/CoverageRow.test.tsx`**
- New `describe('refresh button touch target (D-11.2-11)')` block with 3 tests:
  1. `min-w-[44px]` and `min-h-[44px]` present in idle state className
  2. `p-[15px]` present, `p-0.5` absent in idle state className
  3. Both branches retain `min-w/h-[44px]`, `p-[15px]`, `opacity-100`, `animate-spin` in pending state

## TDD Commits

| Step | Commit | Message |
|------|--------|---------|
| Task 1 RED  | fed79c2 | `test(11.2): RED — actions column width regression lock (w-8 → w-12)` |
| Task 1 GREEN | df9e017 | `feat(11.2): GREEN — actions column w-8 → w-12 (PD-11.2-02, HIG 44px)` |
| Task 2 RED  | 5ce06fe | `test(11.2): RED — refresh button 44×44px touch target (HIG)` |
| Task 2 GREEN | 962e856 | `feat(11.2): GREEN — refresh button 44×44px hit area (p-[15px] + min-w/h-[44px])` |

## Verification Results

| Check | Result |
|-------|--------|
| `actions: 'w-12'` in coverageColumns.ts | 1 occurrence |
| `actions: 'w-8'` in coverageColumns.ts | 0 (fully replaced) |
| Comment chain PD-11.2-02 / D-11.2-12 / Apple HIG 44px | Present |
| `wiki: 'w-72'` preserved (Plan 03) | Confirmed |
| All 6 column width assertions in test | Pass |
| `min-w-[44px]` in CoverageRow.tsx | 2 (both branches) |
| `min-h-[44px]` in CoverageRow.tsx | 2 (both branches) |
| `p-[15px]` in CoverageRow.tsx | 2 (both branches) |
| `p-0.5` in CoverageRow.tsx | 0 (fully replaced) |
| `size={14}` preserved | Confirmed |
| `animate-spin` preserved | Confirmed |
| CoverageRow.test.tsx (23 tests: 20 existing + 3 new) | All pass |
| coverageColumns.test.ts (10 tests) | All pass |
| Full SPA suite (114 files, 996 tests) | All pass |
| Typecheck | Clean |

## Plan 02 Regression Contract

All 6 pending-state tests from Plan 02 continue to pass:
- `animate-spin` in pending state — confirmed
- `aria-busy="true"` in pending state — confirmed
- `disabled` in pending state — confirmed
- `opacity-100` (no `opacity-30`) in pending state — confirmed

## Deviations from Plan

None — plan executed exactly as written.

The full SPA suite had pre-existing failures due to `@agenticapps/dashboard-shared` not being built in this worktree. Building the shared package resolved all failures; all 996 tests pass. This was an environment setup issue, not caused by Plan 04 changes.

## Known Stubs

None. The `w-12` and `min-w-[44px]` values are live Tailwind classes consumed by the table layout and button rendering — no stubbed values or placeholder text.

## Threat Flags

None. CSS className changes only; no new code paths, data flows, or trust boundaries. T-11.2-06 accepted per plan threat model.

## Self-Check: PASSED

- [x] `packages/spa/src/components/panels/coverage/coverageColumns.ts` — exists, `actions: 'w-12'`, comment chain present, `wiki: 'w-72'` preserved
- [x] `packages/spa/src/components/panels/coverage/coverageColumns.test.ts` — exists, actions assertion updated to `w-12`
- [x] `packages/spa/src/components/panels/coverage/CoverageRow.tsx` — exists, both className branches have `min-w-[44px] min-h-[44px] p-[15px]`, `p-0.5` absent
- [x] `packages/spa/src/components/panels/coverage/CoverageRow.test.tsx` — exists, 3 new touch-target tests present
- [x] Commit fed79c2 (Task 1 RED) — exists
- [x] Commit df9e017 (Task 1 GREEN) — exists
- [x] Commit 5ce06fe (Task 2 RED) — exists
- [x] Commit 962e856 (Task 2 GREEN) — exists
- [x] Full SPA suite green (996 tests, 114 files)
- [x] Typecheck clean
