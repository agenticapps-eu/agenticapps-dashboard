---
phase: 11.2
plan: 04
type: tdd
wave: 3
depends_on: ["11.2-02", "11.2-03"]
files_modified:
  - packages/spa/src/components/panels/coverage/coverageColumns.ts
  - packages/spa/src/components/panels/coverage/coverageColumns.test.ts
  - packages/spa/src/components/panels/coverage/CoverageRow.tsx
  - packages/spa/src/components/panels/coverage/CoverageRow.test.tsx
autonomous: true
requirements: []
tags:
  - coverage
  - touch-target
  - apple-hig
  - a11y
  - tdd
  - p2

must_haves:
  truths:
    - "coverageColumns.ts actions entry value is 'w-12' (was 'w-8')"
    - "coverageColumns.ts actions entry has a new comment chain line referencing PD-11.2-02 + D-11.2-12 + Apple HIG 44px touch target"
    - "CoverageRow.tsx refresh button has className containing 'min-w-[44px]' and 'min-h-[44px]' AND 'p-[15px]' (replaces 'p-0.5')"
    - "CoverageRow.tsx refresh button retains the existing focus-visible:ring-2 focus-visible:ring-accent ring (the visible focus ring is drawn against the button at its rendered 44px hit area)"
    - "The pending and non-pending className branches added in Plan 02 both adopt the new touch-target dimensions consistently"
    - "Icon size remains size={14} (visual density preserved per D-11.2-11)"
    - "Regression: COVERAGE_COL_WIDTHS.wiki is still 'w-72' (Plan 03 result preserved); the other 4 entries (repo, claudeMd, gitNexus, workflow) are unchanged"
  artifacts:
    - path: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      provides: "actions width bumped to w-12 (48px); comment chain entry added"
      contains: "actions:  'w-12'"
    - path: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      provides: "Refresh button hit area widened to 44×44px while icon stays 14px"
      contains: "min-w-[44px] min-h-[44px]"
  key_links:
    - from: "packages/spa/src/components/panels/coverage/CoverageRow.tsx"
      to: "packages/spa/src/components/panels/coverage/coverageColumns.ts"
      via: "consumes COVERAGE_COL_WIDTHS.actions for the <td> width"
      pattern: "COVERAGE_COL_WIDTHS\\.actions"
---

<objective>
Close the iPad touch-target gap by widening the refresh button to 44×44px (Apple HIG minimum) while keeping the icon at 14px so visual density is preserved. The actions column widens correspondingly from `w-8` (32px) to `w-12` (48px) — 44px button + 4px breathing room.

Purpose: Close P2 #4 from `11.1-IMPECCABLE.md` ("iPad-Donald" persona red flag: ~18×18px hit area is well below Apple HIG). Net horizontal impact: −48px (Plan 03's wiki tightening freed 64px; this plan reclaims 16px; net table is 48px narrower than Phase 11.1).

Output: 2 modified files (coverageColumns.ts — actions value + comment; CoverageRow.tsx — button className + padding) + their test files.

Implements: D-11.2-11 (button min-w/h 44px + p-[15px]), D-11.2-12 (actions: w-8 → w-12).

**Depends on Plan 02 (Wave 2)** because both modify `CoverageRow.tsx` (and specifically the refresh button className strings — Plan 02 added the pending/non-pending branch with `p-0.5`; this plan replaces `p-0.5` with `p-[15px]` and adds the `min-w/h-[44px]` in both branches).

**Depends on Plan 03 (Wave 1)** because both write to `coverageColumns.ts` (Plan 03 edited the `wiki` line; this plan edits the `actions` line — single-writer pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md
@.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md

# Existing files this plan modifies — read POST Plan 02 + Plan 03 state
@packages/spa/src/components/panels/coverage/coverageColumns.ts
@packages/spa/src/components/panels/coverage/coverageColumns.test.ts
@packages/spa/src/components/panels/coverage/CoverageRow.tsx
@packages/spa/src/components/panels/coverage/CoverageRow.test.tsx

<interfaces>
After Plan 02 lands, the refresh button in `CoverageRow.tsx` looks like (pending-aware):

```tsx
<button
  type="button"
  aria-label={`Refresh actions for ${row.repo}`}
  onClick={() => setPopoverOpen((o) => !o)}
  disabled={pending}
  {...(pending ? { 'aria-busy': true } : {})}
  className={
    pending
      ? "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-100 p-0.5"
      : "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-30 group-hover:opacity-100 focus-within:opacity-100 focus:opacity-100 p-0.5"
  }
>
  <RefreshCw size={14} aria-hidden="true" className={pending ? 'animate-spin' : ''} />
</button>
```

After Plan 03 lands, the coverageColumns.ts looks like:

```typescript
export const COVERAGE_COL_WIDTHS = Object.freeze({
  repo:     'w-72',       // 288px
  claudeMd: 'w-32',       // 128px
  gitNexus: 'w-36',       // 144px
  wiki:     'w-72',       // 288px — Plan 03 update
  workflow: 'w-32',       // 128px
  actions:  'w-8',        //  32px — Plan 04 will widen to w-12
} as const)
```

This plan transforms BOTH files into their Plan 04 final state.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump actions column width to w-12 in coverageColumns.ts (RED then GREEN)</name>
  <files>packages/spa/src/components/panels/coverage/coverageColumns.test.ts, packages/spa/src/components/panels/coverage/coverageColumns.ts</files>

  <read_first>
    - `packages/spa/src/components/panels/coverage/coverageColumns.ts` (POST-Plan 03 state — `wiki` should already be `w-72`)
    - `packages/spa/src/components/panels/coverage/coverageColumns.test.ts` (POST-Plan 03 state — confirm the actions assertion currently locks `w-8`)
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md` §D-11.2-12 — actions: w-8 → w-12 (48px) rationale
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md` §"5. Actions column + touch target" — exact comment chain text

    PRECONDITION VERIFICATION (executor must run before editing):
    - `grep -c "wiki:.*'w-72'" packages/spa/src/components/panels/coverage/coverageColumns.ts` MUST return 1 (Plan 03 has landed)
    - `grep -c "actions:.*'w-8'" packages/spa/src/components/panels/coverage/coverageColumns.ts` MUST return 1 (Plan 04 has not yet landed)
    If either grep fails, ABORT — Plan 03 has not landed yet or Plan 04 has already been applied.
  </read_first>

  <behavior>
    **Step 1a (RED) — update coverageColumns.test.ts:**

    Change the actions assertion to expect `w-12`:

    ```typescript
    it('actions column width is w-12 (48px) — widened from w-8 for 44×44px button per D-11.2-12', () => {
      expect(COVERAGE_COL_WIDTHS.actions).toBe('w-12')
    })
    ```

    (Replace the existing `actions === 'w-8'` assertion from Plan 03.)

    Run the test — confirm actions assertion FAILS. Other 5 assertions still pass. Commit:
    `test(11.2): RED — actions column width regression lock (w-8 → w-12)`

    **Step 1b (GREEN) — modify coverageColumns.ts:**

    Change the `actions` line + add comment chain entry per D-11.2-12 / UI-SPEC §5:

    BEFORE (post-Plan 03 state):
    ```typescript
      workflow: 'w-32',       // 128px
      actions:  'w-8',        //  32px — refresh icon only (Plan 04 widens to w-12 for HIG 44px touch target)
    } as const)
    ```

    AFTER:
    ```typescript
      workflow: 'w-32',       // 128px
      // PD-11.2-02: w-8 (32px) → w-12 (48px) — D-11.2-12, Apple HIG 44px touch target.
      //   Button is min-w-[44px] min-h-[44px] + p-[15px] in CoverageRow.tsx;
      //   48px col = 44px button + 4px breathing room. Net table delta with Plan 03: −48px.
      actions:  'w-12',       //  48px — refresh icon (14px) + p-[15px] = 44px hit area
    } as const)
    ```

    Run tests — all 6 width assertions pass. Run full suite — confirm no regressions. Commit:
    `feat(11.2): GREEN — actions column w-8 → w-12 (PD-11.2-02, HIG 44px)`
  </behavior>

  <action>
    Execute RED test update → GREEN source edit → verify. The wiki value (set by Plan 03) MUST remain `w-72` — do NOT touch it. Only the `actions` line + its comment chain block change.
  </action>

  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/coverageColumns.test.ts 2>&1 | tail -15</automated>
    All 6 width assertions pass.
    <automated>pnpm --filter @agenticapps/dashboard-spa test 2>&1 | tail -10</automated>
    Full SPA suite green.
  </verify>

  <acceptance_criteria>
    - `grep -c "actions:.*'w-12'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1
    - `grep -c "actions:.*'w-8'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 0
    - `grep -c "PD-11.2-02\\|D-11.2-12\\|Apple HIG 44px" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns >= 1
    - `grep -c "wiki:.*'w-72'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1 (Plan 03 result preserved)
    - `grep -c "repo:.*'w-72'" packages/spa/src/components/panels/coverage/coverageColumns.ts` returns 1 (unchanged)
    - `pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/coverageColumns.test.ts` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa test` exits 0
    - Two commits landed in TDD order
  </acceptance_criteria>

  <done>coverageColumns.ts actions value swapped to 'w-12' with comment chain entry; wiki value (Plan 03) preserved; full SPA suite green; two TDD commits landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen refresh button hit area to 44×44px in CoverageRow.tsx (RED then GREEN)</name>
  <files>packages/spa/src/components/panels/coverage/CoverageRow.test.tsx, packages/spa/src/components/panels/coverage/CoverageRow.tsx</files>

  <read_first>
    - `packages/spa/src/components/panels/coverage/CoverageRow.tsx` (POST-Plan 02 state — confirm the refresh button has the pending/non-pending conditional className with `p-0.5`)
    - `packages/spa/src/components/panels/coverage/CoverageRow.test.tsx` (POST-Plan 02 state — confirm the 6 pending-state tests are present)
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-CONTEXT.md` §D-11.2-11 — exact button geometry (44×44 + p-[15px])
    - `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-UI-SPEC.md` §"3. Per-row in-flight spinner" — exact className strings for the pending and non-pending branches WITH the touch-target geometry

    PRECONDITION VERIFICATION (executor must run before editing):
    - `grep -c "pending?:\\s*boolean" packages/spa/src/components/panels/coverage/CoverageRow.tsx` MUST return >= 1 (Plan 02 has landed)
    - `grep -c "p-0.5" packages/spa/src/components/panels/coverage/CoverageRow.tsx` MUST return >= 1 (touch-target widening not yet applied)
    - `grep -c "min-w-\\[44px\\]" packages/spa/src/components/panels/coverage/CoverageRow.tsx` MUST return 0 (Plan 04 has not yet landed)
    If preconditions fail, ABORT.
  </read_first>

  <behavior>
    **Step 2a (RED) — append to CoverageRow.test.tsx:**

    Add 3 new tests under a new `describe('refresh button touch target (D-11.2-11)')` block:

    1. `refresh button className contains min-w-[44px] and min-h-[44px] (idle state)`:
       - Render a row WITHOUT pending
       - Query the refresh button (`getByRole('button', { name: /refresh actions/i })`)
       - Assert `button.className` contains `min-w-[44px]`
       - Assert `button.className` contains `min-h-[44px]`

    2. `refresh button className contains p-[15px] (idle state)`:
       - Same render
       - Assert `button.className` contains `p-[15px]`
       - Assert `button.className` does NOT contain `p-0.5` (the old value)

    3. `refresh button className keeps min-w/h 44px AND p-[15px] in pending state`:
       - Render with `pending={true}`
       - Assert button className contains `min-w-[44px]`, `min-h-[44px]`, `p-[15px]`
       - Assert button className still contains `opacity-100` (Plan 02's pending visual)
       - Assert button className still contains `animate-spin` somewhere in the icon's SVG className (Plan 02 contract preserved)

    Run tests — confirm 3 new failures (button currently has `p-0.5`, no `min-w/h-[44px]`). Existing CoverageRow tests (including Plan 02's 6 pending tests) still pass. Commit:
    `test(11.2): RED — refresh button 44×44px touch target (HIG)`

    **Step 2b (GREEN) — modify CoverageRow.tsx:**

    Replace the refresh button's conditional className from Plan 02. Both branches gain `min-w-[44px] min-h-[44px]` and swap `p-0.5` → `p-[15px]`.

    BEFORE (POST-Plan 02 state):
    ```tsx
    <button
      type="button"
      aria-label={`Refresh actions for ${row.repo}`}
      onClick={() => setPopoverOpen((o) => !o)}
      disabled={pending}
      {...(pending ? { 'aria-busy': true } : {})}
      className={
        pending
          ? "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-100 p-0.5"
          : "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-30 group-hover:opacity-100 focus-within:opacity-100 focus:opacity-100 p-0.5"
      }
    >
      <RefreshCw size={14} aria-hidden="true" className={pending ? 'animate-spin' : ''} />
    </button>
    ```

    AFTER (Plan 04 final):
    ```tsx
    <button
      type="button"
      aria-label={`Refresh actions for ${row.repo}`}
      onClick={() => setPopoverOpen((o) => !o)}
      disabled={pending}
      {...(pending ? { 'aria-busy': true } : {})}
      className={
        pending
          ? "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-100 min-w-[44px] min-h-[44px] p-[15px]"
          : "text-text-tertiary hover:text-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-md opacity-30 group-hover:opacity-100 focus-within:opacity-100 focus:opacity-100 min-w-[44px] min-h-[44px] p-[15px]"
      }
    >
      <RefreshCw size={14} aria-hidden="true" className={pending ? 'animate-spin' : ''} />
    </button>
    ```

    Notes:
    - Icon size stays `size={14}` — visual density preserved (D-11.2-11).
    - Padding math: `2 * 15px + 14px = 44px` — meets HIG minimum.
    - The popover positioning (`<div className="absolute right-0 top-6 z-20 ..."` at line 164) MAY need a tiny adjustment if the popover anchor visually shifts. Check the visual after the edit; if it shifts down by ~14px, adjust `top-6` to `top-[44px]` or similar. Otherwise leave untouched.

    Run tests — all 3 new tests pass + all existing CoverageRow tests (including the 6 from Plan 02) still pass. Commit:
    `feat(11.2): GREEN — refresh button 44×44px hit area (p-[15px] + min-w/h-[44px])`
  </behavior>

  <action>
    Execute the RED → GREEN cycle. Both conditional branches (pending=true and pending=false) MUST adopt the new geometry; the `min-w-[44px] min-h-[44px] p-[15px]` triple is required in BOTH for the assertions to pass. Use grep to confirm both branches were edited (`grep -c "min-w-\\[44px\\]" packages/spa/src/components/panels/coverage/CoverageRow.tsx` must return >= 2).

    The icon size remains 14px. Do NOT change it. If the popover anchor (line 164) visually shifts after the padding edit, tighten its top offset; otherwise leave it alone.
  </action>

  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/CoverageRow.test.tsx 2>&1 | tail -25</automated>
    All CoverageRow tests pass (existing + Plan 02's 6 pending tests + Plan 04's 3 new touch-target tests).
    <automated>pnpm --filter @agenticapps/dashboard-spa test 2>&1 | tail -10</automated>
    Full SPA suite green.
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck 2>&1 | tail -5</automated>
    Typecheck clean.
  </verify>

  <acceptance_criteria>
    - `grep -c "min-w-\\[44px\\]" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 2 (both conditional branches)
    - `grep -c "min-h-\\[44px\\]" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 2
    - `grep -c "p-\\[15px\\]" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 2
    - `grep -c "p-0.5" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns 0 (old padding fully replaced)
    - `grep -c "size={14}" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 1 (icon size unchanged)
    - `grep -c "animate-spin" packages/spa/src/components/panels/coverage/CoverageRow.tsx` returns >= 1 (Plan 02 contract preserved)
    - `grep -c "min-w-\\[44px\\]\\|p-\\[15px\\]" packages/spa/src/components/panels/coverage/CoverageRow.test.tsx` returns >= 3 (3 new tests reference the new className tokens)
    - `pnpm --filter @agenticapps/dashboard-spa test packages/spa/src/components/panels/coverage/CoverageRow.test.tsx` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa test` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exits 0
    - Two commits landed in TDD order
  </acceptance_criteria>

  <done>Refresh button hit area now 44×44px in both pending and idle states; icon stays 14px; full SPA suite green including Plan 02's pending tests; two TDD commits landed.</done>
</task>

</tasks>

<threat_model>
- New attack surface: none. CSS class name changes only. No new code paths, no new state, no new data flow.
- Data flows touched: none.
- Trust boundaries crossed: none.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-06 | N/A | CoverageRow refresh button | accept | No security surface. Click-area / CSS only. No new event handlers, no input validation surface, no new prop types that admit untrusted data. |

Mitigations: existing CSP / bearer-token auth / read-only filesystem constraints preserved unchanged.
</threat_model>

<verification>
- All 4 coverageColumns.test.ts assertions for the affected columns pass (wiki=w-72 preserved, actions=w-12 new).
- All 9 CoverageRow tests pass (6 from Plan 02 + 3 new from Plan 04).
- Full SPA suite green.
- Typecheck clean.
- Regression: IMP-01 column-width identity preserved (`<colgroup>` and `<td>` still read from COVERAGE_COL_WIDTHS).
- Regression: Plan 02's pending visual signal preserved (animate-spin, aria-busy, disabled, opacity-100 all retained in the pending className branch).
- Regression: D-11.2-15 §IMP-04 — `--color-text-tertiary` unchanged (no token edit in this plan); `verify-contrast.test.ts` still passes.
</verification>

<success_criteria>
- Plan 04 closes D-11.2-11 + D-11.2-12 fully per the decision-coverage matrix.
- Four commits landed (2 RED + 2 GREEN).
- Wave 3 sequential: depends_on [11.2-02, 11.2-03] correctly enforces the single-writer pattern on CoverageRow.tsx + coverageColumns.ts.
- After this plan: D-11.2-01..14 all delivered (Plan 05 closes D-11.2-13 in parallel Wave 1; Plan 06 closes D-11.2-14 in parallel Wave 1).
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-04-SUMMARY.md` matching the Phase 11.1 plan-summary convention. Confirm the 4 commits, the 3 new touch-target tests, and the regression contract on Plan 02's pending behavior + Plan 03's wiki width.
</output>
