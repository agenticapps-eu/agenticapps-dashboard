---
phase: 07
plan: 02
type: execute
wave: 1
title: "Wave 1 shell components: HelpLayout + HelpWidget + HelpHook + ComingSoon + MermaidBlock"
depends_on: ["07-01"]
autonomous: true
requirements:
  - HELP-03
  - HELP-04
  - HELP-05
files_modified:
  - packages/spa/src/help/components/HelpLayout.tsx
  - packages/spa/src/help/components/HelpLayout.test.tsx
  - packages/spa/src/help/components/HelpWidget.tsx
  - packages/spa/src/help/components/HelpWidget.test.tsx
  - packages/spa/src/help/components/HelpHook.tsx
  - packages/spa/src/help/components/HelpHook.test.tsx
  - packages/spa/src/help/components/ComingSoon.tsx
  - packages/spa/src/help/components/ComingSoon.test.tsx
  - packages/spa/src/help/components/MermaidBlock.tsx
  - packages/spa/src/help/components/MermaidBlock.test.tsx
  - packages/spa/src/help/topicToUrl.ts
  - packages/spa/src/help/topicToUrl.test.ts
  - packages/spa/src/help/mdxComponents.ts
must_haves:
  truths:
    - "HelpLayout renders sidebar (sticky on desktop, hidden+toggleable on mobile) with all 5 NAV sections + the new D-7-13 stubs + the new HELP-06 reference/shortcuts entry"
    - "HelpWidget dispatches the 8 named widgets via React.lazy + Suspense; unknown widget renders bordered error"
    - "HelpHook renders an inline (?) button with tooltip; topicToUrl is a pure function with table-driven coverage"
    - "ComingSoon renders title + section + back-link; operations section back-links to /help/operations/install (not overview)"
    - "MermaidBlock is StrictMode-safe (data-processed guard + cancellation flag); renders <pre class=\"mermaid\"> slot in vitest; SVG render deferred to e2e"
    - "Zero hex literals leak into any component file (tokenSourceOfTruth.test.ts passes)"
    - "All components use @tanstack/react-router (Link, useNavigate, useRouterState) — NO react-router-dom imports"
    - "All shadcn token classes are translated to dashboard tokens.css names per the COMPLETE table inline below"
  artifacts:
    - path: "packages/spa/src/help/components/HelpLayout.tsx"
      provides: "Sidebar+main shell, NAV table (5 sections), mobile drawer toggle, search-disabled placeholder"
      min_lines: 150
      exports: ["HelpLayout"]
    - path: "packages/spa/src/help/components/HelpWidget.tsx"
      provides: "Lazy widget dispatch via React.lazy; Suspense fallback; unknown-widget bordered error"
      exports: ["HelpWidget"]
    - path: "packages/spa/src/help/components/HelpHook.tsx"
      provides: "Inline (?) icon button with tooltip; onClick calls TanStack useNavigate"
      exports: ["HelpHook"]
    - path: "packages/spa/src/help/components/ComingSoon.tsx"
      provides: "Stub placeholder with back-link; operations section fallback to install"
      exports: ["ComingSoon"]
    - path: "packages/spa/src/help/components/MermaidBlock.tsx"
      provides: "StrictMode-safe lazy Mermaid runtime renderer; not-prose wrapper; warm-paper theme variables"
      exports: ["MermaidBlock"]
    - path: "packages/spa/src/help/topicToUrl.ts"
      provides: "Pure function: 'workflow.gates' → '/help/workflow/gates'; 'observability.scan#high' → '/help/observability/scan#high'"
      exports: ["topicToUrl"]
  key_links:
    - from: "packages/spa/src/help/components/HelpLayout.tsx"
      to: "@tanstack/react-router Link + useRouterState"
      via: "navigation links and active-route detection"
      pattern: "from '@tanstack/react-router'"
    - from: "packages/spa/src/help/components/HelpWidget.tsx"
      to: "packages/spa/src/help/widgets/*.stub.tsx (Plan 07-03)"
      via: "React.lazy dynamic imports"
      pattern: "lazy\\(\\(\\) => import\\("
    - from: "packages/spa/src/help/components/MermaidBlock.tsx"
      to: "mermaid package (lazy dynamic import)"
      via: "useEffect on mount; data-processed guard"
      pattern: "import\\('mermaid'\\)"
    - from: "packages/spa/src/help/mdxComponents.ts"
      to: "HelpWidget + HelpHook + MermaidBlock + ComingSoon exports"
      via: "components map passed to MDXProvider"
      pattern: "HelpWidget"
---

<objective>
Author the 5 shell components — `HelpLayout`, `HelpWidget`, `HelpHook`, `ComingSoon`, `MermaidBlock` — by translating each from `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/` to `packages/spa/src/help/components/`. Apply the COMPLETE token translation table from RESEARCH.md (NOT just the subset in CONTEXT.md D-7-11). Swap `react-router-dom` for `@tanstack/react-router`. Author exhaustive unit tests per VALIDATION.md row spec. Ship a StrictMode-safe Mermaid renderer with the data-processed guard. Extract `topicToUrl` as a pure function for table-driven testing. Wire the 4 components into `mdxComponents.ts` so MDX pages can reference them without explicit imports.

Purpose: HELP-03 / HELP-04 / HELP-05 met at the component level. Plan 07-04 consumes these in MDX content. Plan 07-05 mounts them via the router.

Output: 13 files (5 component + 5 component tests + 1 pure-fn + 1 pure-fn test + 1 mdxComponents update). Estimated context cost: 40-45%. Multiple atomic TDD commits per task.

**R2 disjoint-set resolution:** Plan 07-02 declares the `HelpWidget` dispatch table at line 22-31 of the source — but the imports point to Plan 07-03's `../widgets/*.stub` files. Plan 07-02's HelpWidget test mocks those lazy imports via vitest's `vi.mock()` so the test runs even before 07-03's stubs exist. Real integration (lazy-loaded stub renders in the browser) is exercised in Plan 07-05's e2e walkthrough.
</objective>

<read_first>
Plan-level reference list:

- `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` — D-7-01 (replace /help), D-7-06 (TanStack routing), D-7-11 (token translation), D-7-12 (peer layout), D-7-13 (extra stubs), D-7-14 (8 widget stubs), D-7-15 (test surface), D-7-16 (5-plan decomposition with parallel 07-02 + 07-03).
- `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` — §"Complete token translation table" (the EXHAUSTIVE map), §Pitfalls (P1 Mermaid StrictMode, P4 tokenSourceOfTruth, P5 anchor IDs, P9 not-prose on MermaidBlock).
- `.planning/phases/07-help-docs-v1-0/07-VALIDATION.md` §"Per-Task Verification Map" (rows to fill after authoring).
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/HelpLayout.tsx` — canonical source for HelpLayout (188 lines).
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/HelpWidget.tsx` — canonical source for HelpWidget.
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/HelpHook.tsx` — canonical source for HelpHook (incl. topicToUrl).
- `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/ComingSoon.tsx` — canonical source for ComingSoon.
- `packages/spa/src/styles/tokens.css` — token names available (locked Phase 5.1).
- `packages/spa/src/components/ui/KbdHint.tsx` — pattern reference (no cn/clsx, simple ternaries).
- `packages/spa/src/router.tsx` — TanStack route patterns + how Phase 5.1 imports Link / useRouterState.
- `packages/spa/src/help/mdxComponents.ts` (from Plan 07-01) — stub to populate.
</read_first>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/agenticapps-workflow/skill/SKILL.md
@$HOME/.claude/skills/superpowers/test-driven-development/SKILL.md
@$HOME/.claude/skills/superpowers/verification-before-completion/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-help-docs-v1-0/07-CONTEXT.md
@.planning/phases/07-help-docs-v1-0/07-RESEARCH.md
@.planning/phases/07-help-docs-v1-0/07-VALIDATION.md

<token_translation>
<!--
COMPLETE token translation table — copied from RESEARCH.md §"Complete token translation table".
DO NOT skip any row. Every shadcn class in the source files maps to the dashboard token here.
After translation, `grep -rE '(bg|text|border)-(background|foreground|primary|secondary|destructive|muted|accent|card)\b' packages/spa/src/help/` MUST return zero matches.
-->

Backgrounds:
- bg-background → bg-app-bg
- bg-card → bg-card-bg
- bg-accent (chrome hover, NOT purple) → bg-card-bg-hover
- bg-accent/50 → bg-card-bg-hover/60
- bg-muted (chip) → bg-sidebar-bg
- bg-muted/30 → bg-sidebar-bg/60
- bg-muted/60 → bg-sidebar-bg
- bg-destructive/5 (error wash) → bg-status-error/10
- bg-primary → bg-accent
- bg-primary/90 (hover) → bg-accent-hover
- bg-primary/10 (accent wash) → bg-accent-bg
- bg-foreground (inverted tooltip) → bg-text-primary

Gradients (judgment call):
- bg-gradient-to-br from-muted/30 to-muted/60 → bg-sidebar-bg (flat — warm-paper doesn't carry gradients)

Text colors:
- text-foreground → text-text-primary
- text-foreground/70 (inactive nav) → text-text-secondary
- text-muted-foreground (default) → text-text-secondary
- text-muted-foreground (uppercase section labels) → text-text-tertiary
- text-primary (badge) → text-accent
- text-primary-foreground (button on accent) → text-card-bg
- text-accent-foreground (active nav) → text-accent
- text-destructive → text-status-error
- text-background (tooltip inverted) → text-app-bg

Borders:
- border (default) → border-border-subtle
- border-r (sidebar) → border-r border-border-subtle
- border-b (mobile header) → border-b border-border-subtle
- border-destructive/40 → border-status-error/40
- border-2 border-dashed border-muted → border-2 border-dashed border-border-subtle

Focus ring:
- focus:ring-1 focus:ring-ring → focus:ring-1 focus:ring-accent
- focus:outline-none → unchanged

Typography prose:
- prose prose-slate dark:prose-invert max-w-none → unchanged (Tailwind v4 typography plugin owns these; prose-slate may be dropped to plain prose if visual review fails — defer to /browse)
- not-prose → unchanged (escape hatch)
- ADD not-prose to MermaidBlock wrapper (Pitfall 9)
</token_translation>

<router_translation>
<!--
react-router-dom → @tanstack/react-router mapping
-->

| Source (react-router-dom) | Target (@tanstack/react-router) | Notes |
|---|---|---|
| `import { Outlet } from "react-router-dom"` | `import { Outlet } from '@tanstack/react-router'` | Same name. |
| `import { NavLink } from "react-router-dom"` | `import { Link, useRouterState } from '@tanstack/react-router'` | TanStack has no NavLink — use Link + manual active-detection via useRouterState().location.pathname. |
| `import { Link } from "react-router-dom"` | `import { Link } from '@tanstack/react-router'` | Same name; TanStack Link uses `to` prop. |
| `import { useNavigate } from "react-router-dom"` | `import { useNavigate } from '@tanstack/react-router'` | Returns navigate fn; call signature `navigate({ to: '/help/...' })`. |
| `<NavLink to={path} className={({ isActive }) => ...}>` | Custom: `const pathname = useRouterState({ select: (s) => s.location.pathname }); const isActive = pathname === path; <Link to={path} className={isActive ? 'active' : 'inactive'}>` | See HelpLayout Task below. |
| `<Navigate to="/help/X" replace />` | route definition `beforeLoad: () => { throw redirect({ to: '/help/X' }) }` | Done in Plan 07-05 router wiring, NOT here. |
</router_translation>
</context>

<tasks>

<task id="07-02-T1" type="auto" tdd="true">
  <name>Task 1: Extract topicToUrl pure function (RED→GREEN with table-driven cases)</name>
  <files>packages/spa/src/help/topicToUrl.ts, packages/spa/src/help/topicToUrl.test.ts</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/HelpHook.tsx` lines 74-81 — current `topicToUrl` definition.
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-15 — table-driven test spec.
    - `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §Pitfall 5 — anchor IDs note (topicToUrl returns the URL; anchor resolution at the page is v1.1).
  </read_first>
  <behavior>
    - `topicToUrl('workflow.gates')` returns `/help/workflow/gates`
    - `topicToUrl('observability.scan#high-confidence')` returns `/help/observability/scan#high-confidence`
    - `topicToUrl('workflow')` (single segment) returns `/help/workflow`
    - `topicToUrl('a.b.c.d')` returns `/help/a/b/c/d` (arbitrary depth)
    - `topicToUrl('a.b#anchor.with.dots')` returns `/help/a/b#anchor.with.dots` (only the FIRST `#` splits; segment dots are converted to slashes only in the path portion)
    - `topicToUrl('')` returns `/help/` (degenerate but no crash)
  </behavior>
  <action>
**RED commit first.**

Create `packages/spa/src/help/topicToUrl.test.ts`:

```ts
/**
 * Plan 07-02 Task 1 — topicToUrl pure function.
 *
 * Extracts the URL builder from HelpHook so it can be table-driven tested
 * without rendering. Source: ~/Documents/.../HelpHook.tsx lines 74-81.
 */
import { describe, it, expect } from 'vitest'

import { topicToUrl } from './topicToUrl'

describe('topicToUrl', () => {
  it.each([
    ['workflow.gates', '/help/workflow/gates'],
    ['repos.core', '/help/repos/core'],
    ['observability.scan', '/help/observability/scan'],
    ['observability.scan#high-confidence', '/help/observability/scan#high-confidence'],
    ['workflow', '/help/workflow'],
    ['a.b.c.d', '/help/a/b/c/d'],
    ['a.b#anchor.with.dots', '/help/a/b#anchor.with.dots'],
    ['reference.shortcuts', '/help/reference/shortcuts'],
    ['', '/help/'],
  ])('topicToUrl(%j) === %j', (topic, expected) => {
    expect(topicToUrl(topic)).toBe(expected)
  })
})
```

Run: `pnpm --filter @agenticapps/dashboard-spa test src/help/topicToUrl.test.ts`. MUST fail (`Cannot find module './topicToUrl'`). Commit as `test(07-02): RED topicToUrl table-driven cases`.

**GREEN commit.**

Create `packages/spa/src/help/topicToUrl.ts`:

```ts
/**
 * topicToUrl — pure function mapping dot-separated topic paths to /help/* URLs.
 *
 * Plan 07-02 Task 1 — extracted from HelpHook.tsx so it can be tested without
 * rendering. Only the first `#` splits the anchor; dots inside the anchor are
 * preserved (e.g. "a.b#anchor.with.dots" → "/help/a/b#anchor.with.dots").
 *
 * @example
 *   topicToUrl('workflow.gates') === '/help/workflow/gates'
 *   topicToUrl('observability.scan#high-confidence') === '/help/observability/scan#high-confidence'
 */
export function topicToUrl(topic: string): string {
  const hashIdx = topic.indexOf('#')
  const path = hashIdx === -1 ? topic : topic.slice(0, hashIdx)
  const anchor = hashIdx === -1 ? '' : topic.slice(hashIdx + 1)
  const segments = path.split('.')
  const url = `/help/${segments.join('/')}`
  return anchor ? `${url}#${anchor}` : url
}
```

Re-run test: 9 cases pass. Commit as `feat(07-02): GREEN topicToUrl pure function`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/topicToUrl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Git log shows `test(07-02):` then `feat(07-02):` for this task
    - `test -f packages/spa/src/help/topicToUrl.ts` exit 0
    - `test -f packages/spa/src/help/topicToUrl.test.ts` exit 0
    - `grep -q "export function topicToUrl" packages/spa/src/help/topicToUrl.ts` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/topicToUrl.test.ts` exit 0, reports 9 cases pass (one for each it.each row)
  </acceptance_criteria>
  <done>Pure function extracted and exhaustively tested.</done>
</task>

<task id="07-02-T2" type="auto" tdd="true">
  <name>Task 2: HelpHook component (RED→GREEN with tooltip + navigate + topicToUrl integration)</name>
  <files>packages/spa/src/help/components/HelpHook.tsx, packages/spa/src/help/components/HelpHook.test.tsx</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/HelpHook.tsx` — full source (81 lines).
    - `packages/spa/src/help/topicToUrl.ts` (just created) — reuse it; do not re-implement.
    - Token translation table above (this plan's `<token_translation>` block).
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-15 — HelpHook test spec.
  </read_first>
  <behavior>
    - Renders `<button aria-label={label}>` containing `<HelpCircle size={14}>`.
    - On mouseEnter/focus, sets tooltip visible; on mouseLeave/blur, hides it.
    - Tooltip is `<span role="tooltip">{label}</span>` positioned absolute.
    - On click, calls `navigate({ to: topicToUrl(topic) })`.
    - If `panel: true` (v1.1+), `console.warn(...)` is called and `navigate` still fires (current v1.0 fall-through).
    - Default `label = 'Learn more'`.
    - All tokens translated per the table above.
  </behavior>
  <action>
**RED commit.**

Create `packages/spa/src/help/components/HelpHook.test.tsx`:

```tsx
/**
 * Plan 07-02 Task 2 — HelpHook unit tests.
 *
 * Source: ~/Documents/.../HelpHook.tsx
 * @see .planning/phases/07-help-docs-v1-0/07-CONTEXT.md D-7-15
 */
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi, beforeEach } from 'vitest'

const mockNavigate = vi.fn()

vi.mock('@tanstack/react-router', () => ({
  useNavigate: () => mockNavigate,
}))

import { HelpHook } from './HelpHook'

describe('HelpHook', () => {
  beforeEach(() => {
    mockNavigate.mockReset()
  })

  it('renders a button with the default "Learn more" aria-label', () => {
    render(<HelpHook topic="workflow.gates" />)
    expect(screen.getByRole('button', { name: 'Learn more' })).toBeInTheDocument()
  })

  it('uses the provided label override on aria-label', () => {
    render(<HelpHook topic="workflow.gates" label="What's a gate?" />)
    expect(screen.getByRole('button', { name: "What's a gate?" })).toBeInTheDocument()
  })

  it('shows tooltip on hover and hides on unhover', async () => {
    const user = userEvent.setup()
    render(<HelpHook topic="workflow.gates" label="Click me" />)
    const btn = screen.getByRole('button')
    expect(screen.queryByRole('tooltip')).not.toBeInTheDocument()
    await user.hover(btn)
    expect(screen.getByRole('tooltip')).toHaveTextContent('Click me')
    await user.unhover(btn)
    expect(screen.queryByRole('tooltip')).not.toBeInTheDocument()
  })

  it('shows tooltip on focus and hides on blur', async () => {
    const user = userEvent.setup()
    render(<HelpHook topic="workflow.gates" />)
    const btn = screen.getByRole('button')
    btn.focus()
    expect(screen.getByRole('tooltip')).toBeInTheDocument()
    await user.tab() // blur
    expect(screen.queryByRole('tooltip')).not.toBeInTheDocument()
  })

  it('navigates to topicToUrl(topic) on click', async () => {
    const user = userEvent.setup()
    render(<HelpHook topic="observability.scan#high-confidence" />)
    await user.click(screen.getByRole('button'))
    expect(mockNavigate).toHaveBeenCalledWith({
      to: '/help/observability/scan#high-confidence',
    })
  })

  it('with panel=true, console.warns AND still navigates (v1.0 fall-through)', async () => {
    const user = userEvent.setup()
    const warnSpy = vi.spyOn(console, 'warn').mockImplementation(() => {})
    render(<HelpHook topic="workflow.gates" panel={true} />)
    await user.click(screen.getByRole('button'))
    expect(warnSpy).toHaveBeenCalledWith(
      expect.stringContaining('HelpHook panel mode not yet implemented'),
    )
    expect(mockNavigate).toHaveBeenCalledWith({ to: '/help/workflow/gates' })
    warnSpy.mockRestore()
  })
})
```

Run: test fails (module not found). Commit `test(07-02): RED HelpHook unit tests`.

**GREEN commit.**

Create `packages/spa/src/help/components/HelpHook.tsx`:

```tsx
/**
 * HelpHook — contextual deep-link from a dashboard feature page into /help.
 *
 * Source: ~/Documents/.../HelpHook.tsx (translated from react-router-dom
 * useNavigate → @tanstack/react-router useNavigate, with all shadcn tokens
 * translated to tokens.css names per Plan 07-02 token translation table).
 *
 * v1.0 — component ships but NOT yet wired into dashboard pages (HELP-05).
 * panel={true} mode falls through to navigation; real side-panel is v1.1+.
 */
import { useState } from 'react'
import { useNavigate } from '@tanstack/react-router'
import { HelpCircle } from 'lucide-react'

import { topicToUrl } from '../topicToUrl.js'

export interface HelpHookProps {
  /** Dot-separated topic path, e.g. "workflow.gates" or "observability.scan#high". */
  topic: string
  /** Optional label override; defaults to "Learn more". */
  label?: string
  /** If true, opens in a side panel instead of navigating. v1.1+; falls through to navigate today. */
  panel?: boolean
}

export function HelpHook({ topic, label = 'Learn more', panel = false }: HelpHookProps): React.JSX.Element {
  const [showTooltip, setShowTooltip] = useState(false)
  const navigate = useNavigate()
  const url = topicToUrl(topic)

  function handleClick(): void {
    if (panel) {
      console.warn('HelpHook panel mode not yet implemented; navigating instead.')
    }
    void navigate({ to: url })
  }

  return (
    <span className="relative inline-flex items-center">
      <button
        type="button"
        aria-label={label}
        onClick={handleClick}
        onMouseEnter={() => setShowTooltip(true)}
        onMouseLeave={() => setShowTooltip(false)}
        onFocus={() => setShowTooltip(true)}
        onBlur={() => setShowTooltip(false)}
        className="inline-flex items-center justify-center rounded-full p-0.5 text-text-secondary hover:text-text-primary hover:bg-card-bg-hover focus:outline-none focus:ring-1 focus:ring-accent"
      >
        <HelpCircle size={14} />
      </button>
      {showTooltip && (
        <span
          role="tooltip"
          className="pointer-events-none absolute left-1/2 top-full z-50 mt-1 -translate-x-1/2 whitespace-nowrap rounded bg-text-primary text-app-bg text-xs px-2 py-1 shadow-md"
        >
          {label}
        </span>
      )}
    </span>
  )
}
```

NOTE on `void navigate(...)`: TanStack's `useNavigate` returns a promise-returning function; `void` quiets the `@typescript-eslint/no-floating-promises` rule consistent with `useGlobalShortcuts.ts:64`.

Run test: 6 cases pass. Commit `feat(07-02): GREEN HelpHook component`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpHook.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/components/HelpHook.tsx` exit 0
    - `grep -q "from '@tanstack/react-router'" packages/spa/src/help/components/HelpHook.tsx` exit 0
    - `! grep -q "react-router-dom" packages/spa/src/help/components/HelpHook.tsx` exit 0
    - `! grep -qE "(bg|text|border)-(background|foreground|primary|secondary|destructive|muted|accent|card)\\b" packages/spa/src/help/components/HelpHook.tsx` exit 0 (no shadcn token leakage — but `bg-card-bg`, `text-text-primary` etc must NOT match; the regex specifically targets shadcn forms without the `-bg`/`-primary` qualifier — verify by running `grep -oE "(bg|text|border)-[a-z]+(/[0-9]+)?" packages/spa/src/help/components/HelpHook.tsx | sort -u` and inspecting output is all tokens.css names)
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpHook.test.tsx` exit 0, reports 6 cases pass
    - Git log shows `test(07-02):` then `feat(07-02):` for HelpHook
  </acceptance_criteria>
  <done>HelpHook + tests in place; 6 unit tests passing; TanStack useNavigate wired.</done>
</task>

<task id="07-02-T3" type="auto" tdd="true">
  <name>Task 3: ComingSoon component (RED→GREEN with section/title/back-link + operations special-case)</name>
  <files>packages/spa/src/help/components/ComingSoon.tsx, packages/spa/src/help/components/ComingSoon.test.tsx</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/ComingSoon.tsx` — full source.
    - Token translation table above.
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-15 — ComingSoon test spec (table-driven incl. operations fallback).
  </read_first>
  <behavior>
    - Renders Construction icon + title + paragraph + Back-link.
    - Back-link `to` prop:
      - `section: 'workflow'` → `/help/workflow/overview`
      - `section: 'repos'` → `/help/repos/overview`
      - `section: 'observability'` → `/help/observability/overview`
      - `section: 'operations'` → `/help/operations/install` (SPECIAL CASE — operations has no overview)
      - `section: 'reference'` → `/help/reference/overview` (note: reference has no overview yet in v1.0, but the fallback URL is still computed; the resulting `/help/reference/overview` is itself a stub — rendered ComingSoon, no crash)
    - Footer with GitHub issues link + contributing link.
    - All tokens translated.
  </behavior>
  <action>
**RED commit.**

Create `packages/spa/src/help/components/ComingSoon.test.tsx`:

```tsx
/**
 * Plan 07-02 Task 3 — ComingSoon unit tests.
 *
 * Table-driven across sections to verify the operations special-case.
 * Source: ~/Documents/.../ComingSoon.tsx
 */
import { render, screen } from '@testing-library/react'
import { createMemoryHistory } from '@tanstack/history'
import { describe, it, expect, vi } from 'vitest'

// Mock @tanstack/react-router Link so we can assert on `to` prop without a real router.
vi.mock('@tanstack/react-router', async (importActual) => {
  const actual = await importActual<typeof import('@tanstack/react-router')>()
  return {
    ...actual,
    Link: ({ to, children, className }: { to: string; children: React.ReactNode; className?: string }) => (
      <a href={to} data-to={to} className={className}>
        {children}
      </a>
    ),
  }
})

import { ComingSoon } from './ComingSoon'

describe('ComingSoon', () => {
  it.each([
    ['workflow', 'Gates', '/help/workflow/overview'],
    ['repos', 'core repo', '/help/repos/overview'],
    ['observability', 'Scan', '/help/observability/overview'],
    ['operations', 'Update', '/help/operations/install'], // SPECIAL CASE
    ['reference', 'Glossary', '/help/reference/overview'],
  ])(
    'section=%j title=%j back-links to %j',
    (section, title, expectedBackTo) => {
      render(<ComingSoon section={section} title={title} />)
      expect(screen.getByRole('heading', { name: title })).toBeInTheDocument()
      const backLink = screen.getByRole('link', { name: new RegExp(`back to ${section}`, 'i') })
      expect(backLink).toHaveAttribute('data-to', expectedBackTo)
    },
  )

  it('renders the GitHub issues link to the project repo issues page', () => {
    render(<ComingSoon section="workflow" title="Gates" />)
    const link = screen.getByRole('link', { name: /github issues/i })
    expect(link).toHaveAttribute(
      'href',
      expect.stringContaining('agenticapps-dashboard/issues'),
    )
  })

  it('renders the Contributing link to /help/reference/contributing', () => {
    render(<ComingSoon section="workflow" title="Gates" />)
    const link = screen.getByRole('link', { name: /^contributing$/i })
    expect(link).toHaveAttribute('data-to', '/help/reference/contributing')
  })
})
```

(Note: `@tanstack/history` is bundled into `@tanstack/react-router` — already available.)

Run: fails. Commit `test(07-02): RED ComingSoon unit tests`.

**GREEN commit.**

Create `packages/spa/src/help/components/ComingSoon.tsx`:

```tsx
/**
 * ComingSoon — fallback for v1.0 stub pages.
 *
 * Source: ~/Documents/.../ComingSoon.tsx (translated to @tanstack/react-router Link
 * + tokens.css names).
 */
import { Link } from '@tanstack/react-router'
import { ArrowLeft, Construction } from 'lucide-react'

export interface ComingSoonProps {
  title: string
  section: string
}

export function ComingSoon({ title, section }: ComingSoonProps): React.JSX.Element {
  // Operations has no /overview — fall back to /install. All other sections use /overview.
  const fallbackPath =
    section === 'operations' ? '/help/operations/install' : `/help/${section}/overview`

  return (
    <div className="my-12">
      <div className="rounded-lg border-2 border-dashed border-border-subtle bg-sidebar-bg/60 px-8 py-12 text-center">
        <Construction size={40} className="mx-auto mb-4 text-text-secondary" />
        <h1 className="text-2xl font-semibold mb-2 text-text-primary">{title}</h1>
        <p className="text-text-secondary mb-6 max-w-md mx-auto">
          This page is on the roadmap but hasn&apos;t been written yet. The
          v1.0 docs site ships with five anchor pages; deeper topic
          coverage lands in v1.1.
        </p>
        <Link
          to={fallbackPath}
          className="inline-flex items-center gap-2 rounded-md bg-accent px-4 py-2 text-sm font-medium text-card-bg hover:bg-accent-hover"
        >
          <ArrowLeft size={14} />
          Back to {section} overview
        </Link>
      </div>

      <p className="mt-6 text-center text-xs text-text-secondary">
        Track progress on{' '}
        <a
          href={`https://github.com/agenticapps-eu/agenticapps-dashboard/issues?q=is%3Aissue+label%3Adocs+${encodeURIComponent(title)}`}
          className="underline hover:text-text-primary"
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub issues
        </a>
        {' '}or contribute via{' '}
        <Link to="/help/reference/contributing" className="underline hover:text-text-primary">
          Contributing
        </Link>
        .
      </p>
    </div>
  )
}
```

Run test: 7 cases pass. Commit `feat(07-02): GREEN ComingSoon component`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/components/ComingSoon.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/components/ComingSoon.tsx` exit 0
    - `grep -q "section === 'operations'" packages/spa/src/help/components/ComingSoon.tsx` exit 0
    - `grep -q "/help/operations/install" packages/spa/src/help/components/ComingSoon.tsx` exit 0
    - `grep -q "from '@tanstack/react-router'" packages/spa/src/help/components/ComingSoon.tsx` exit 0
    - `! grep -q "react-router-dom" packages/spa/src/help/components/ComingSoon.tsx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/components/ComingSoon.test.tsx` exit 0, 7 cases pass
    - Git log shows RED→GREEN for ComingSoon
  </acceptance_criteria>
  <done>ComingSoon ships with operations special-case verified.</done>
</task>

<task id="07-02-T4" type="auto" tdd="true">
  <name>Task 4: HelpWidget component (RED→GREEN with 8 lazy stubs + unknown-widget bordered error)</name>
  <files>packages/spa/src/help/components/HelpWidget.tsx, packages/spa/src/help/components/HelpWidget.test.tsx</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/components/HelpWidget.tsx` — full source.
    - Token translation table.
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-14 (8 widget stubs) + D-7-15 (HelpWidget test spec).
    - **R2 resolution above:** Plan 07-03 owns the 8 `.stub.tsx` files; this plan's HelpWidget.tsx imports them. Tests mock the lazy imports.
  </read_first>
  <behavior>
    - Renders `<div class="not-prose ...">` wrapper.
    - Dispatch table: 8 named keys → `lazy(() => import('../widgets/<Name>.stub'))`.
    - On `name` match: renders the lazy component inside `<Suspense fallback={<Loader />}>`.
    - On `name` miss (TypeScript-narrowed; reachable only via MDX authoring error): renders bordered error box with `<code>{name}</code>` and instruction to update the dispatch table.
    - All tokens translated.
  </behavior>
  <action>
**RED commit.**

Create `packages/spa/src/help/components/HelpWidget.test.tsx`:

```tsx
/**
 * Plan 07-02 Task 4 — HelpWidget unit tests.
 *
 * R2 resolution: Plan 07-03 owns ../widgets/*.stub files. This test mocks the
 * 8 lazy imports so the test runs in parallel with 07-03 (or before 07-03
 * lands physical files).
 *
 * Source: ~/Documents/.../HelpWidget.tsx
 */
import { render, screen, waitFor } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'

// Mock all 8 widget stubs that HelpWidget imports lazily.
vi.mock('../widgets/RepoTopologyMap.stub', () => ({
  default: () => <div data-testid="stub-RepoTopologyMap">RepoTopologyMap stub</div>,
}))
vi.mock('../widgets/WorkflowStateMachine.stub', () => ({
  default: () => <div data-testid="stub-WorkflowStateMachine">WorkflowStateMachine stub</div>,
}))
vi.mock('../widgets/GatePicker.stub', () => ({
  default: () => <div data-testid="stub-GatePicker">GatePicker stub</div>,
}))
vi.mock('../widgets/TraceVisualizer.stub', () => ({
  default: () => <div data-testid="stub-TraceVisualizer">TraceVisualizer stub</div>,
}))
vi.mock('../widgets/ScanReportPlayground.stub', () => ({
  default: () => <div data-testid="stub-ScanReportPlayground">ScanReportPlayground stub</div>,
}))
vi.mock('../widgets/ApplyConsentSimulator.stub', () => ({
  default: () => <div data-testid="stub-ApplyConsentSimulator">ApplyConsentSimulator stub</div>,
}))
vi.mock('../widgets/MigrationDryRun.stub', () => ({
  default: () => <div data-testid="stub-MigrationDryRun">MigrationDryRun stub</div>,
}))
vi.mock('../widgets/SlashCommandCatalog.stub', () => ({
  default: () => <div data-testid="stub-SlashCommandCatalog">SlashCommandCatalog stub</div>,
}))

import { HelpWidget } from './HelpWidget'

const KNOWN_NAMES = [
  'RepoTopologyMap',
  'WorkflowStateMachine',
  'GatePicker',
  'TraceVisualizer',
  'ScanReportPlayground',
  'ApplyConsentSimulator',
  'MigrationDryRun',
  'SlashCommandCatalog',
] as const

describe('HelpWidget', () => {
  it.each(KNOWN_NAMES)('dispatches known widget %s via React.lazy', async (name) => {
    // Cast: WidgetName is the union of KNOWN_NAMES — TypeScript narrows in source code.
    render(<HelpWidget name={name as never} />)
    await waitFor(() => {
      expect(screen.getByTestId(`stub-${name}`)).toBeInTheDocument()
    })
  })

  it('renders bordered error for an unknown widget name', () => {
    // @ts-expect-error - intentionally passing a name outside the WidgetName union to exercise the runtime branch
    render(<HelpWidget name={'NotARealWidget'} />)
    expect(screen.getByText(/unknown widget/i)).toBeInTheDocument()
    const code = screen.getByText('NotARealWidget', { selector: 'code' })
    expect(code).toBeInTheDocument()
  })

  it('wraps known widget in not-prose container so MDX prose styles do not bleed in', async () => {
    const { container } = render(<HelpWidget name={'RepoTopologyMap' as never} />)
    await waitFor(() => {
      expect(screen.getByTestId('stub-RepoTopologyMap')).toBeInTheDocument()
    })
    // The outermost wrapper has not-prose
    expect(container.querySelector('.not-prose')).toBeInTheDocument()
  })
})
```

Run: fails. Commit `test(07-02): RED HelpWidget unit tests (8 names + unknown + not-prose)`.

**GREEN commit.**

Create `packages/spa/src/help/components/HelpWidget.tsx`:

```tsx
/**
 * HelpWidget — render slot for interactive components inside MDX pages.
 *
 * Pages reference widgets by name:
 *   <HelpWidget name="RepoTopologyMap" />
 *
 * The dispatch table maps each name to its component. v1.0 ships stubbed
 * widgets (Plan 07-03). v1.2 replaces stubs with real implementations.
 *
 * Source: ~/Documents/.../HelpWidget.tsx (translated tokens + lazy imports
 * land at ../widgets/<Name>.stub per Plan 07-03 D-7-14).
 */
import { lazy, Suspense } from 'react'
import { Loader2 } from 'lucide-react'

const widgets = {
  // v1.0 — stubs (Plan 07-03); replace with real components in v1.2.
  RepoTopologyMap: lazy(() => import('../widgets/RepoTopologyMap.stub.js')),
  WorkflowStateMachine: lazy(() => import('../widgets/WorkflowStateMachine.stub.js')),
  GatePicker: lazy(() => import('../widgets/GatePicker.stub.js')),
  TraceVisualizer: lazy(() => import('../widgets/TraceVisualizer.stub.js')),
  ScanReportPlayground: lazy(() => import('../widgets/ScanReportPlayground.stub.js')),
  ApplyConsentSimulator: lazy(() => import('../widgets/ApplyConsentSimulator.stub.js')),
  MigrationDryRun: lazy(() => import('../widgets/MigrationDryRun.stub.js')),
  SlashCommandCatalog: lazy(() => import('../widgets/SlashCommandCatalog.stub.js')),
} as const

export type WidgetName = keyof typeof widgets

export interface HelpWidgetProps {
  name: WidgetName
}

export function HelpWidget({ name }: HelpWidgetProps): React.JSX.Element {
  const W = widgets[name]
  if (!W) {
    return (
      <div className="my-6 not-prose rounded-md border border-status-error/40 bg-status-error/10 px-4 py-3 text-sm text-status-error">
        Unknown widget: <code>{name}</code>. Add it to{' '}
        <code>HelpWidget.tsx</code>.
      </div>
    )
  }

  return (
    <div className="my-8 not-prose rounded-lg border border-border-subtle bg-card-bg p-1 shadow-card">
      <Suspense
        fallback={
          <div className="flex items-center justify-center gap-2 px-4 py-12 text-sm text-text-secondary">
            <Loader2 size={14} className="animate-spin" />
            Loading {name}…
          </div>
        }
      >
        <W />
      </Suspense>
    </div>
  )
}
```

NOTE: `.js` extension on lazy imports is the dashboard's ESM convention (NodeNext module resolution). Same pattern as `router.tsx:79`.

Run test: 10 cases pass (8 dispatches + 1 unknown + 1 not-prose). Commit `feat(07-02): GREEN HelpWidget component`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpWidget.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/components/HelpWidget.tsx` exit 0
    - `grep -c "lazy(() => import" packages/spa/src/help/components/HelpWidget.tsx` returns 8
    - `grep -c "\.stub\.js" packages/spa/src/help/components/HelpWidget.tsx` returns 8
    - All 8 widget names present: `for n in RepoTopologyMap WorkflowStateMachine GatePicker TraceVisualizer ScanReportPlayground ApplyConsentSimulator MigrationDryRun SlashCommandCatalog; do grep -q "$n: lazy" packages/spa/src/help/components/HelpWidget.tsx; done` exit 0 each
    - `grep -q "Unknown widget" packages/spa/src/help/components/HelpWidget.tsx` exit 0
    - `grep -q "not-prose" packages/spa/src/help/components/HelpWidget.tsx` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpWidget.test.tsx` exit 0, reports 10 cases pass
  </acceptance_criteria>
  <done>HelpWidget dispatch table for 8 stubs + unknown-widget path verified.</done>
</task>

<task id="07-02-T5" type="auto" tdd="true">
  <name>Task 5: MermaidBlock component (RED→GREEN with StrictMode-safe lazy mermaid + data-processed guard)</name>
  <files>packages/spa/src/help/components/MermaidBlock.tsx, packages/spa/src/help/components/MermaidBlock.test.tsx</files>
  <read_first>
    - `.planning/phases/07-help-docs-v1-0/07-RESEARCH.md` §Pitfall 1 (Mermaid StrictMode), §Pitfall 6 (syntax error catch + console.warn vs error), §Pitfall 9 (not-prose wrapper).
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-10 (Mermaid runtime), D-7-15 (MermaidBlock smoke spec — do NOT assert SVG in vitest).
    - OQ-7-A from RESEARCH (theme variables — minimal set: primaryColor, primaryTextColor, primaryBorderColor, lineColor).
  </read_first>
  <behavior>
    - Renders `<pre class="mermaid not-prose">{code}</pre>` inside a `not-prose` wrapper.
    - On mount, dynamically imports `mermaid`, initializes once with the warm-paper theme variables, and calls `mermaid.run({ nodes: [ref.current] })`.
    - StrictMode guard: skips re-render if `node.dataset.processed === 'true'`.
    - Cancellation flag prevents stale-mount renders.
    - `mermaid.run()` rejection: `console.warn` (NOT `console.error`) — preserves "no console errors" invariant (Pitfall 6).
    - Default props: `code: string`.
    - Vitest smoke test asserts the `<pre class="mermaid">` slot exists; does NOT assert SVG (jsdom doesn't run mermaid; deferred to Plan 07-05 Playwright).
  </behavior>
  <action>
**RED commit.**

Create `packages/spa/src/help/components/MermaidBlock.test.tsx`:

```tsx
/**
 * Plan 07-02 Task 5 — MermaidBlock smoke + StrictMode-safety unit tests.
 *
 * Per D-7-15: smoke only — renders <pre class="mermaid"> slot.
 * SVG assertion deferred to Plan 07-05 Playwright e2e (jsdom doesn't run mermaid).
 *
 * @see .planning/phases/07-help-docs-v1-0/07-RESEARCH.md §Pitfall 1 + §Pitfall 9
 */
import { render, screen } from '@testing-library/react'
import { StrictMode } from 'react'
import { describe, it, expect, vi } from 'vitest'

// Mock the mermaid dynamic import; vitest's vi.doMock applies to the whole module.
vi.mock('mermaid', () => ({
  default: {
    initialize: vi.fn(),
    run: vi.fn().mockResolvedValue(undefined),
  },
}))

import { MermaidBlock } from './MermaidBlock'

const SAMPLE_CODE = 'flowchart LR\n  A --> B'

describe('MermaidBlock', () => {
  it('renders <pre class="mermaid"> with the raw code as text content', () => {
    render(<MermaidBlock code={SAMPLE_CODE} />)
    const pre = screen.getByText(/A --> B/)
    expect(pre.tagName).toBe('PRE')
    expect(pre.className).toContain('mermaid')
  })

  it('wraps the <pre> in a not-prose container so MDX prose styles do not bleed in', () => {
    const { container } = render(<MermaidBlock code={SAMPLE_CODE} />)
    expect(container.querySelector('.not-prose')).toBeInTheDocument()
  })

  it('survives StrictMode double-mount without throwing (data-processed guard)', () => {
    expect(() => {
      render(
        <StrictMode>
          <MermaidBlock code={SAMPLE_CODE} />
        </StrictMode>,
      )
    }).not.toThrow()
  })

  it('renders inline (mermaid library import is async; SVG appearance is deferred to e2e)', () => {
    render(<MermaidBlock code={SAMPLE_CODE} />)
    // We do NOT assert on SVG — jsdom doesn't run the renderer. The Playwright
    // walking checklist (Plan 07-05) asserts SVG visibility after networkidle.
    expect(screen.getByText(/A --> B/)).toBeInTheDocument()
  })
})
```

Run: fails. Commit `test(07-02): RED MermaidBlock smoke + StrictMode test`.

**GREEN commit.**

Create `packages/spa/src/help/components/MermaidBlock.tsx`:

```tsx
/**
 * MermaidBlock — runtime Mermaid renderer for MDX pages.
 *
 * Strategy (per RESEARCH P1):
 *   1. Render <pre class="mermaid">{code}</pre> immediately so users see raw
 *      code while the mermaid bundle (~600 KB) lazy-loads (RESEARCH bundle perf).
 *   2. On mount, dynamic `import('mermaid')` resolves; we run `mermaid.run({ nodes: [ref] })`
 *      to replace the <pre> contents with SVG.
 *   3. StrictMode guard: skip re-run if data-processed="true" is already set
 *      (mermaid sets this on success).
 *   4. Cancellation flag: if the component unmounts before mermaid finishes
 *      loading, the run is skipped.
 *   5. Render errors: console.warn (NOT console.error) so the Playwright
 *      walking-checklist "no console errors" assertion stays green even when
 *      mermaid rejects on bad syntax (RESEARCH P6).
 *
 * Theme: 'base' with warm-paper themeVariables (OQ-7-A defaults):
 *   primaryColor:       --color-accent-bg  (#F2EBFA)
 *   primaryTextColor:   --color-text-primary (#1F1B2E)
 *   primaryBorderColor: --color-accent      (#6B46C1)
 *   lineColor:          --color-text-tertiary (#9C95A8)
 */
import { useEffect, useRef } from 'react'

export interface MermaidBlockProps {
  /** Raw mermaid source. */
  code: string
}

const MERMAID_THEME_VARIABLES = {
  primaryColor: '#F2EBFA',
  primaryTextColor: '#1F1B2E',
  primaryBorderColor: '#6B46C1',
  lineColor: '#9C95A8',
} as const

export function MermaidBlock({ code }: MermaidBlockProps): React.JSX.Element {
  const ref = useRef<HTMLPreElement>(null)

  useEffect(() => {
    const node = ref.current
    if (!node || node.dataset.processed === 'true') return

    let cancelled = false

    void import('mermaid').then(({ default: mermaid }) => {
      if (cancelled || !node || node.dataset.processed === 'true') return
      try {
        mermaid.initialize({
          startOnLoad: false,
          theme: 'base',
          themeVariables: MERMAID_THEME_VARIABLES,
          securityLevel: 'loose',
        })
        void mermaid.run({ nodes: [node] }).catch((err: unknown) => {
          // Per RESEARCH P6: console.warn (not console.error) so Playwright
          // "no console errors" stays green on bad mermaid syntax.
          console.warn('MermaidBlock render failed:', err)
        })
      } catch (err) {
        console.warn('MermaidBlock initialize failed:', err)
      }
    })

    return () => {
      cancelled = true
    }
  }, [code])

  return (
    <div className="not-prose my-6">
      <pre ref={ref} className="mermaid">
        {code}
      </pre>
    </div>
  )
}
```

NOTE: The hex literals `#F2EBFA`, `#1F1B2E`, `#6B46C1`, `#9C95A8` ARE intentional and will FAIL `tokenSourceOfTruth.test.ts` once it scans `src/help/**` (Plan 07-01 Task 6). **Two options:**

- **Option A (recommended):** Move `MERMAID_THEME_VARIABLES` to `packages/spa/src/styles/tokens.css` as exported CSS vars and read them via `getComputedStyle(document.documentElement).getPropertyValue('--color-accent')` at runtime. Cost: ~5 lines. Benefit: invariant stays clean.

- **Option B:** Add MermaidBlock.tsx to the test's allow-list (extend the `walk` function's filter to skip this file). Faster but weakens the invariant.

**Choose Option A:** at the top of `MermaidBlock.tsx`, replace `MERMAID_THEME_VARIABLES` constant with a function:

```ts
function getMermaidThemeVariables(): Record<string, string> {
  if (typeof window === 'undefined') return {}
  const root = document.documentElement
  const v = (name: string): string =>
    getComputedStyle(root).getPropertyValue(name).trim()
  return {
    primaryColor: v('--color-accent-bg'),
    primaryTextColor: v('--color-text-primary'),
    primaryBorderColor: v('--color-accent'),
    lineColor: v('--color-text-tertiary'),
  }
}
```

And inside the useEffect, `themeVariables: getMermaidThemeVariables()`.

This keeps zero hex literals in the .tsx file → `tokenSourceOfTruth.test.ts` passes.

Run vitest: 4 cases pass. Commit `feat(07-02): GREEN MermaidBlock with StrictMode guard + tokens.css theme vars`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/components/MermaidBlock.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/components/MermaidBlock.tsx` exit 0
    - `! grep -qE '#[0-9a-fA-F]{3,8}\b' packages/spa/src/help/components/MermaidBlock.tsx` exit 0 (NO hex literals — must use Option A above)
    - `grep -q "getMermaidThemeVariables\|MERMAID_THEME_VARIABLES" packages/spa/src/help/components/MermaidBlock.tsx` exit 0
    - `grep -q "dataset.processed" packages/spa/src/help/components/MermaidBlock.tsx` exit 0 (StrictMode guard)
    - `grep -q "cancelled = false" packages/spa/src/help/components/MermaidBlock.tsx` exit 0 (cancellation flag)
    - `grep -q "console.warn" packages/spa/src/help/components/MermaidBlock.tsx` exit 0
    - `! grep -q "console.error" packages/spa/src/help/components/MermaidBlock.tsx` exit 0
    - `grep -q "not-prose" packages/spa/src/help/components/MermaidBlock.tsx` exit 0
    - `grep -q "import('mermaid')" packages/spa/src/help/components/MermaidBlock.tsx` exit 0 (lazy)
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/components/MermaidBlock.test.tsx` exit 0, 4 cases pass
    - `pnpm --filter @agenticapps/dashboard-spa test src/styles/tokenSourceOfTruth.test.ts` exit 0 (invariant still passes — proves no hex leak)
  </acceptance_criteria>
  <done>MermaidBlock ships with StrictMode safety + console.warn-on-error + tokens.css theme vars.</done>
</task>

<task id="07-02-T6" type="auto" tdd="true">
  <name>Task 6: HelpLayout component (RED→GREEN with NAV table + TanStack Link + mobile drawer)</name>
  <files>packages/spa/src/help/components/HelpLayout.tsx, packages/spa/src/help/components/HelpLayout.test.tsx</files>
  <read_first>
    - `~/Documents/Claude/Projects/agentic-workflow/dashboard-help-pages/_shell/HelpLayout.tsx` — 188-line source.
    - Token translation table (this plan's `<token_translation>` block).
    - Router translation table (this plan's `<router_translation>` block).
    - D-7-13: extra workflow stubs (`rationalization-table`, `red-flags`) appended to Workflow NAV with status "stub".
    - HELP-06: `Keyboard shortcuts` entry as FIRST item under Reference, status "ready", path `/help/reference/shortcuts`.
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-15 HelpLayout test spec.
  </read_first>
  <behavior>
    - NAV table: 5 sections (Workflow, Repositories, Observability, Operations, Reference), with the original migration items PLUS:
      - Workflow gains `rationalization-table` (status: 'stub') and `red-flags` (status: 'stub') AT THE END of the existing items list.
      - Reference is REORDERED with `Keyboard shortcuts` (status: 'ready', path '/help/reference/shortcuts') as the FIRST entry; Glossary, ADR index, Changelog, Contributing follow as 'stub'.
    - Mobile header: shown only at `md:hidden`; toggle button toggles drawer; clicking a nav link closes the drawer.
    - Desktop sidebar: sticky top, w-72, scrollable, sticky on `md:` breakpoint.
    - Active-link detection: via `useRouterState({ select: (s) => s.location.pathname })` — `isActive = pathname === item.path`.
    - Stub items show `(soon)` label.
    - Disabled search input with `aria-label="Search documentation (coming in v1.1)"`.
    - Main: `<article className="prose prose-slate dark:prose-invert max-w-none">{<Outlet />}</article>`.
    - All tokens translated.
  </behavior>
  <action>
**RED commit.**

Create `packages/spa/src/help/components/HelpLayout.test.tsx`:

```tsx
/**
 * Plan 07-02 Task 6 — HelpLayout unit tests.
 *
 * Asserts:
 *   - All 5 NAV sections render.
 *   - Workflow contains the 2 NEW D-7-13 stubs (rationalization-table, red-flags).
 *   - Reference contains the NEW HELP-06 entry "Keyboard shortcuts" as first item.
 *   - Active-link styling fires on current path.
 *   - Mobile drawer toggles via the toggle button.
 *   - Stub entries render "(soon)" label.
 *
 * @see .planning/phases/07-help-docs-v1-0/07-CONTEXT.md D-7-13 + D-7-15 + HELP-06
 */
import { render, screen, within } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi } from 'vitest'

// Mock TanStack router primitives.
let currentPathname = '/help'
vi.mock('@tanstack/react-router', () => ({
  Link: ({ to, children, className, onClick }: {
    to: string
    children: React.ReactNode
    className?: string
    onClick?: () => void
  }) => (
    <a href={to} data-to={to} className={className} onClick={onClick}>
      {children}
    </a>
  ),
  Outlet: () => <div data-testid="outlet">Outlet content</div>,
  useRouterState: ({ select }: { select: (s: { location: { pathname: string } }) => string }) =>
    select({ location: { pathname: currentPathname } }),
}))

import { HelpLayout } from './HelpLayout'

describe('HelpLayout', () => {
  it('renders all 5 NAV section headings', () => {
    currentPathname = '/help'
    render(<HelpLayout />)
    for (const label of ['Workflow', 'Repositories', 'Observability', 'Operations', 'Reference']) {
      expect(screen.getAllByText(label).length).toBeGreaterThan(0)
    }
  })

  it('Workflow section includes the 2 new D-7-13 stubs', () => {
    currentPathname = '/help'
    render(<HelpLayout />)
    expect(screen.getAllByRole('link', { name: /rationalization table/i }).length).toBeGreaterThan(0)
    expect(screen.getAllByRole('link', { name: /red flags/i }).length).toBeGreaterThan(0)
  })

  it('Reference section has Keyboard shortcuts as a "ready" entry pointing to /help/reference/shortcuts', () => {
    currentPathname = '/help'
    render(<HelpLayout />)
    const shortcuts = screen.getAllByRole('link', { name: /keyboard shortcuts/i })
    expect(shortcuts.length).toBeGreaterThan(0)
    expect(shortcuts[0]).toHaveAttribute('data-to', '/help/reference/shortcuts')
    // "ready" entries do NOT render "(soon)"
    expect(within(shortcuts[0]).queryByText(/\(soon\)/i)).not.toBeInTheDocument()
  })

  it('stub entries render "(soon)" label', () => {
    currentPathname = '/help'
    render(<HelpLayout />)
    const stub = screen.getAllByRole('link', { name: /rationalization table/i })[0]
    expect(within(stub).getByText(/\(soon\)/i)).toBeInTheDocument()
  })

  it('current path receives active styling (text-accent or font-medium)', () => {
    currentPathname = '/help/workflow/overview'
    render(<HelpLayout />)
    const link = screen.getAllByRole('link', { name: /^overview$/i })[0]
    expect(link.className).toMatch(/(text-accent|font-medium|bg-accent-bg)/)
  })

  it('mobile drawer toggles open/close on the toggle button', async () => {
    const user = userEvent.setup()
    currentPathname = '/help'
    render(<HelpLayout />)
    const aside = screen.getByLabelText('Help navigation')
    // Drawer starts hidden on mobile (hidden class) — md:block always on desktop.
    expect(aside.className).toMatch(/\bhidden\b/)
    await user.click(screen.getByRole('button', { name: /toggle navigation/i }))
    expect(aside.className).toMatch(/\bblock\b/)
  })

  it('renders the Outlet for the active route', () => {
    currentPathname = '/help'
    render(<HelpLayout />)
    expect(screen.getByTestId('outlet')).toBeInTheDocument()
  })

  it('search input is disabled with the v1.1 aria-label', () => {
    render(<HelpLayout />)
    const search = screen.getByLabelText('Search documentation (coming in v1.1)')
    expect(search).toBeDisabled()
  })

  it('main content is wrapped in an article.prose with prose-slate dark:prose-invert max-w-none', () => {
    const { container } = render(<HelpLayout />)
    const article = container.querySelector('article')
    expect(article?.className).toContain('prose')
    expect(article?.className).toContain('max-w-none')
  })
})
```

Run: fails. Commit `test(07-02): RED HelpLayout 9 unit tests (NAV + D-7-13 stubs + HELP-06 + drawer + active + Outlet)`.

**GREEN commit.**

Create `packages/spa/src/help/components/HelpLayout.tsx`:

```tsx
/**
 * HelpLayout — chrome for every /help/* page.
 *
 * Sidebar (sections + pages) + main content area. Mobile collapses sidebar
 * to a drawer toggled by the header hamburger.
 *
 * Source: ~/Documents/.../HelpLayout.tsx (translated: react-router-dom NavLink/Outlet
 * → @tanstack/react-router Link+useRouterState+Outlet, with all shadcn tokens
 * translated to tokens.css names per Plan 07-02 token translation table).
 *
 * NAV deltas vs the source migration:
 *   - Workflow adds 2 stubs at the end: rationalization-table, red-flags (D-7-13).
 *   - Reference adds "Keyboard shortcuts" as the FIRST entry, status: ready,
 *     path: /help/reference/shortcuts (HELP-06).
 */
import { useState } from 'react'
import { Link, Outlet, useRouterState } from '@tanstack/react-router'
import { Menu, Search, X } from 'lucide-react'

interface NavItem {
  label: string
  path: string
  status?: 'ready' | 'stub'
}

interface NavSection {
  label: string
  emoji: string
  items: NavItem[]
}

const NAV: NavSection[] = [
  {
    label: 'Workflow',
    emoji: '📐',
    items: [
      { label: 'Overview', path: '/help/workflow/overview', status: 'ready' },
      { label: 'Commitment ritual', path: '/help/workflow/commitment-ritual', status: 'stub' },
      { label: 'Gates', path: '/help/workflow/gates', status: 'stub' },
      { label: 'Superpowers', path: '/help/workflow/superpowers', status: 'stub' },
      { label: 'GSD', path: '/help/workflow/gsd', status: 'stub' },
      { label: 'gstack', path: '/help/workflow/gstack', status: 'stub' },
      { label: 'Impeccable', path: '/help/workflow/impeccable', status: 'stub' },
      { label: 'Two-stage review', path: '/help/workflow/two-stage-review', status: 'stub' },
      { label: 'Verification', path: '/help/workflow/verification', status: 'stub' },
      { label: 'ADRs', path: '/help/workflow/adrs', status: 'stub' },
      // D-7-13 additions
      { label: 'Rationalization table', path: '/help/workflow/rationalization-table', status: 'stub' },
      { label: 'Red flags', path: '/help/workflow/red-flags', status: 'stub' },
    ],
  },
  {
    label: 'Repositories',
    emoji: '📦',
    items: [
      { label: 'Overview', path: '/help/repos/overview', status: 'ready' },
      { label: 'core', path: '/help/repos/core', status: 'stub' },
      { label: 'claude-workflow', path: '/help/repos/claude', status: 'stub' },
      { label: 'pi-workflow', path: '/help/repos/pi', status: 'stub' },
      { label: 'codex-workflow', path: '/help/repos/codex', status: 'stub' },
      { label: 'dashboard', path: '/help/repos/dashboard', status: 'stub' },
      { label: 'Client projects', path: '/help/repos/projects', status: 'stub' },
    ],
  },
  {
    label: 'Observability',
    emoji: '📡',
    items: [
      { label: 'Overview', path: '/help/observability/overview', status: 'ready' },
      { label: 'Spec §10', path: '/help/observability/spec-section-10', status: 'stub' },
      { label: 'Stack templates', path: '/help/observability/stacks', status: 'stub' },
      { label: 'Install', path: '/help/observability/install', status: 'stub' },
      { label: 'Scan', path: '/help/observability/scan', status: 'stub' },
      { label: 'Apply', path: '/help/observability/apply', status: 'stub' },
      { label: 'Policy', path: '/help/observability/policy', status: 'stub' },
      { label: 'Pi & Codex status', path: '/help/observability/pi-codex-status', status: 'stub' },
    ],
  },
  {
    label: 'Operations',
    emoji: '⚙️',
    items: [
      { label: 'Install', path: '/help/operations/install', status: 'ready' },
      { label: 'Update', path: '/help/operations/update', status: 'stub' },
      { label: 'Slash commands', path: '/help/operations/slash-commands', status: 'stub' },
      { label: 'Troubleshooting', path: '/help/operations/troubleshooting', status: 'stub' },
      { label: 'Migration runbook', path: '/help/operations/migration-runbook', status: 'stub' },
    ],
  },
  {
    label: 'Reference',
    emoji: '📚',
    items: [
      // HELP-06: Keyboard shortcuts moved from old /help into docs site; first entry, ready.
      { label: 'Keyboard shortcuts', path: '/help/reference/shortcuts', status: 'ready' },
      { label: 'Glossary', path: '/help/reference/glossary', status: 'stub' },
      { label: 'ADR index', path: '/help/reference/adr-index', status: 'stub' },
      { label: 'Changelog', path: '/help/reference/changelog', status: 'stub' },
      { label: 'Contributing', path: '/help/reference/contributing', status: 'stub' },
    ],
  },
]

export function HelpLayout(): React.JSX.Element {
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const pathname = useRouterState({ select: (s) => s.location.pathname })

  return (
    <div className="min-h-screen bg-app-bg text-text-primary">
      {/* Mobile top bar */}
      <header className="md:hidden sticky top-0 z-40 flex items-center justify-between border-b border-border-subtle bg-app-bg px-4 py-3">
        <Link to="/help" className="font-semibold text-lg text-text-primary">
          AgenticApps
        </Link>
        <button
          type="button"
          aria-label="Toggle navigation"
          onClick={() => setSidebarOpen((s) => !s)}
          className="rounded-md p-2 hover:bg-card-bg-hover"
        >
          {sidebarOpen ? <X size={20} /> : <Menu size={20} />}
        </button>
      </header>

      <div className="mx-auto flex max-w-7xl">
        {/* Sidebar */}
        <aside
          className={`${sidebarOpen ? 'block' : 'hidden'} md:block fixed md:sticky inset-y-0 md:top-0 left-0 z-30 h-screen w-72 shrink-0 overflow-y-auto border-r border-border-subtle bg-app-bg px-4 py-6`}
          aria-label="Help navigation"
        >
          <div className="hidden md:flex items-center gap-2 mb-6">
            <Link to="/help" className="font-semibold text-xl text-text-primary">
              AgenticApps
            </Link>
            <span className="text-xs text-text-tertiary rounded bg-sidebar-bg px-1.5 py-0.5">
              help
            </span>
          </div>

          {/* Search placeholder — wires to MiniSearch in v1.1 */}
          <div className="mb-4 relative">
            <Search size={14} className="absolute left-2 top-2 text-text-tertiary" />
            <input
              type="search"
              placeholder="Search docs…"
              className="w-full rounded-md border border-border-subtle bg-app-bg py-1.5 pl-7 pr-2 text-sm placeholder:text-text-tertiary focus:outline-none focus:ring-1 focus:ring-accent"
              disabled
              aria-label="Search documentation (coming in v1.1)"
            />
          </div>

          <nav className="space-y-6 text-sm">
            {NAV.map((section) => (
              <div key={section.label}>
                <h3 className="mb-2 text-xs font-semibold uppercase tracking-wider text-text-tertiary">
                  <span className="mr-1.5">{section.emoji}</span>
                  {section.label}
                </h3>
                <ul className="space-y-0.5">
                  {section.items.map((item) => {
                    const isActive = pathname === item.path
                    const className = `block rounded-md px-2 py-1 ${
                      isActive
                        ? 'bg-accent-bg text-accent font-medium'
                        : 'text-text-secondary hover:bg-card-bg-hover/60 hover:text-text-primary'
                    }`
                    return (
                      <li key={item.path}>
                        <Link to={item.path} className={className} onClick={() => setSidebarOpen(false)}>
                          {item.label}
                          {item.status === 'stub' && (
                            <span className="ml-1.5 text-[10px] text-text-tertiary">(soon)</span>
                          )}
                        </Link>
                      </li>
                    )
                  })}
                </ul>
              </div>
            ))}
          </nav>
        </aside>

        {/* Main content */}
        <main className="flex-1 px-4 py-8 md:px-12 md:py-12 max-w-3xl">
          <article className="prose prose-slate dark:prose-invert max-w-none">
            <Outlet />
          </article>
        </main>
      </div>
    </div>
  )
}
```

Run tests: 9 cases pass. Commit `feat(07-02): GREEN HelpLayout component (5 NAV sections + D-7-13 stubs + HELP-06 entry + TanStack router translation)`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `grep -q "from '@tanstack/react-router'" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `! grep -q "react-router-dom" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `grep -q "/help/workflow/rationalization-table" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `grep -q "/help/workflow/red-flags" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `grep -q "/help/reference/shortcuts" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `grep -A1 "Reference" packages/spa/src/help/components/HelpLayout.tsx | grep -q "Keyboard shortcuts"` (Keyboard shortcuts appears in Reference block) — alternative: `awk '/label: .Reference./{a=1} a==1 && /Keyboard shortcuts/{print; exit}' packages/spa/src/help/components/HelpLayout.tsx | grep -q "Keyboard shortcuts"` exit 0
    - `grep -q "useRouterState" packages/spa/src/help/components/HelpLayout.tsx` exit 0
    - `! grep -qE '#[0-9a-fA-F]{3,8}\b' packages/spa/src/help/components/HelpLayout.tsx` exit 0 (no hex literals)
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/components/HelpLayout.test.tsx` exit 0, reports 9 cases pass
    - `pnpm --filter @agenticapps/dashboard-spa test src/styles/tokenSourceOfTruth.test.ts` exit 0
  </acceptance_criteria>
  <done>HelpLayout ships with all required NAV deltas + TanStack routing + tests passing.</done>
</task>

<task id="07-02-T7" type="auto" tdd="false">
  <name>Task 7: Populate mdxComponents.ts with the 4 MDX-callable components</name>
  <files>packages/spa/src/help/mdxComponents.ts</files>
  <read_first>
    - `packages/spa/src/help/mdxComponents.ts` (created in Plan 07-01 as empty stub).
    - `.planning/phases/07-help-docs-v1-0/07-CONTEXT.md` D-7-05 — components map content.
    - All 4 components just authored in this plan.
  </read_first>
  <behavior>
    - `mdxComponents` exports `{ HelpWidget, HelpHook, ComingSoon, MermaidBlock }`.
    - Importable from `'./help/mdxComponents.js'` per the dashboard's ESM convention.
    - When `<MDXProvider components={mdxComponents}>` renders MDX content, JSX references like `<HelpWidget name="..." />` and `<HelpHook topic="..." />` resolve without explicit MDX import.
    - **`KbdHint` is NOT added here yet** — that's Plan 07-05 Task (finalising the map for reference/shortcuts.mdx).
    - **`pre: MermaidPreOrDefault` mapping is NOT added here yet** — that's Plan 07-05 (MDX ` ```mermaid ` fence support). Plan 07-04's anchor MDX pages embed Mermaid via `<MermaidBlock code="..."/>` JSX (NOT fence) until Plan 07-05 wires the `pre` mapping. **NOTE for Plan 07-04 executor:** when copying anchor `.md` to `.mdx`, you must convert ` ```mermaid ... ``` ` fence blocks to `<MermaidBlock code={`...`} />` JSX OR wait until Plan 07-05 wires the `pre` mapping. Plan 07-04's action explicitly addresses this.
  </behavior>
  <action>
Replace `packages/spa/src/help/mdxComponents.ts` contents with EXACTLY:

```ts
/**
 * mdxComponents — global component map passed to <MDXProvider>.
 *
 * Plan 07-01: empty stub.
 * Plan 07-02 (this update): adds { HelpWidget, HelpHook, ComingSoon, MermaidBlock }.
 * Plan 07-05: adds { KbdHint, pre: MermaidPreOrDefault } to finalise the map.
 *
 * MDX pages can reference these components by name without explicit imports:
 *   <HelpWidget name="RepoTopologyMap" />
 *   <HelpHook topic="workflow.gates" />
 *
 * Keep this file lean — main.tsx imports it on module load.
 */
import type { MDXComponents } from 'mdx/types'

import { ComingSoon } from './components/ComingSoon.js'
import { HelpHook } from './components/HelpHook.js'
import { HelpWidget } from './components/HelpWidget.js'
import { MermaidBlock } from './components/MermaidBlock.js'

export const mdxComponents: MDXComponents = {
  HelpWidget,
  HelpHook,
  ComingSoon,
  MermaidBlock,
}
```
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/mdx-smoke.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "HelpWidget" packages/spa/src/help/mdxComponents.ts` exit 0
    - `grep -q "HelpHook" packages/spa/src/help/mdxComponents.ts` exit 0
    - `grep -q "ComingSoon" packages/spa/src/help/mdxComponents.ts` exit 0
    - `grep -q "MermaidBlock" packages/spa/src/help/mdxComponents.ts` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` exit 0
    - `pnpm --filter @agenticapps/dashboard-spa test src/help/__tests__/mdx-smoke.test.tsx` exit 0 (Plan 07-01 smoke still green with the updated map)
  </acceptance_criteria>
  <done>mdxComponents map has 4 entries; smoke test still green.</done>
</task>

<task id="07-02-T8" type="auto" tdd="false">
  <name>Task 8: Final sweep — full SPA test suite + tokenSourceOfTruth + dev server smoke</name>
  <files>(verification only; no file changes)</files>
  <read_first>
    - All files modified in this plan.
  </read_first>
  <behavior>
    - Full SPA test suite passes (existing Phase 5.1 + 5 + 06.1 tests must NOT regress).
    - `tokenSourceOfTruth.test.ts` passes — proves zero hex leaks across new `src/help/` files.
    - `pnpm --filter @agenticapps/dashboard-spa typecheck` passes.
    - `pnpm --filter @agenticapps/dashboard-spa build` succeeds.
    - Dev server boots: `pnpm --filter @agenticapps/dashboard-spa dev &` then `curl -s http://localhost:5174/` returns the SPA HTML; ZERO console errors in the browser console (verified manually via /browse — Plan 07-05 e2e automates this).
  </behavior>
  <action>
1. Run `pnpm --filter @agenticapps/dashboard-spa test` (full SPA suite). Expect zero failures. If any pre-Phase-7 test fails, the token translation or NAV change broke something — fix and re-run.

2. Run `pnpm --filter @agenticapps/dashboard-spa typecheck`. Expect exit 0.

3. Run `pnpm --filter @agenticapps/dashboard-spa build`. Expect exit 0; chunk report should NOT mention mermaid in the main chunk (it's lazy-loaded).

4. Run `pnpm --filter @agenticapps/dashboard-spa exec playwright test --list`. Expect exit 0; still zero specs (added in Plan 07-05).

5. Verify the 5 shell components are exportable: run `node --input-type=module -e "import('./packages/spa/src/help/mdxComponents.ts').then(m => console.log(Object.keys(m.mdxComponents)))"` from the repo root. Expect output `[ 'HelpWidget', 'HelpHook', 'ComingSoon', 'MermaidBlock' ]`. (TS import via tsx if pure Node fails — but our test in T7 already covers this.)

6. Optional dev-server smoke (recommended per project hooks but not strictly required at plan boundary): boot `pnpm --filter @agenticapps/dashboard-spa dev` in a sub-shell, hit `curl -s http://localhost:5174/help` (returns SPA HTML — route resolution is Plan 07-05, so the SPA may 404 the `/help` route currently — that's expected). Kill the dev server.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test && pnpm --filter @agenticapps/dashboard-spa typecheck && pnpm --filter @agenticapps/dashboard-spa build</automated>
  </verify>
  <acceptance_criteria>
    - Full SPA test suite exits 0
    - SPA typecheck exits 0
    - SPA build exits 0
    - tokenSourceOfTruth test passes (no hex literals in `src/help/`)
    - `pnpm -r typecheck && pnpm -r test && pnpm lint` exit 0 (workspace-level)
  </acceptance_criteria>
  <done>All shell components landed, tested, typechecked, linted, built; ready for Plan 07-04 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX → JSX render | MDX pages embed `<HelpWidget>` and `<HelpHook>` JSX; we control all MDX source. |
| `<HelpHook>` → router navigation | `topicToUrl(topic)` produces a URL; if `topic` were user-supplied (it is not in v1.0), this would be an open-redirect surface. Today the topic is hardcoded in MDX. |
| `MermaidBlock` → dynamic import | `import('mermaid')` resolves to a pnpm-locked version; no runtime configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | MermaidBlock `securityLevel: 'loose'` | mitigate | `loose` is the Mermaid default for in-app rendering (not `antiscript`-style). All mermaid source comes from our MDX files; no user input. Re-evaluate if v1.1 ever accepts user-supplied diagrams. |
| T-07-02-02 | Information Disclosure | HelpHook tooltip text rendered with React (not innerHTML) | mitigate | `{label}` is a React expression; React auto-escapes. No XSS surface. |
| T-07-02-03 | Tampering | HelpWidget dispatch table | accept | Compile-time `as const` plus TypeScript `WidgetName` union narrows valid names; runtime branch is the bordered-error fallback. Unknown names cannot reach `<W />` invocation. |
| T-07-02-04 | Open Redirect | topicToUrl producing arbitrary URLs | mitigate | `topicToUrl` always prefixes `/help/`; an attacker cannot produce an off-site URL. v1.0 topics are hardcoded in MDX (never user-supplied). |
| T-07-02-05 | Spoofing | Stub mock in HelpWidget tests vs real Plan 07-03 stubs | accept | Vitest mock is test-time only; production code imports the real stubs from `../widgets/*.stub.js`. Plan 07-05 e2e exercises the real integration. |
| T-07-02-06 | Tampering | Mermaid hex theme variables read from CSS at runtime | mitigate | `getComputedStyle(document.documentElement)` reads only `--color-*` CSS vars defined in `tokens.css` (controlled by us). Cannot be overridden at runtime by an attacker. |
| T-07-02-07 | DoS | Mermaid render-failure infinite-retry | mitigate | `console.warn` is fire-and-forget; useEffect runs once per `code` change; `data-processed` guard prevents re-runs. No retry loop. |

**Block-on policy:** `high` severity blocks. None of T-07-02-01..07 are `high`.
</threat_model>

<verification>
- 9 task-level vitest test files exist (4 shell components × 1 + topicToUrl × 1 + HelpLayout × 1 + MermaidBlock × 1 = 8 files; plus the existing mdx-smoke from 07-01 still green). Full SPA test count grows by ~40 tests.
- `pnpm --filter @agenticapps/dashboard-spa test` exit 0.
- `pnpm --filter @agenticapps/dashboard-spa typecheck` exit 0.
- `pnpm --filter @agenticapps/dashboard-spa build` exit 0.
- `pnpm --filter @agenticapps/dashboard-spa test src/styles/tokenSourceOfTruth.test.ts` exit 0 — confirms zero hex leaks in `src/help/`.
- `grep -rE '(bg|text|border)-(background|foreground|primary|secondary|destructive|muted|accent|card)\b' packages/spa/src/help/components/` returns ONLY tokens.css names (no shadcn token-name leaks — verify by eye).
- `grep -r "react-router-dom" packages/spa/src/help/` returns zero matches (TanStack only).
- `mdxComponents` exports 4 keys: HelpWidget, HelpHook, ComingSoon, MermaidBlock.
- Git log shows ≥ 6 atomic RED/GREEN commit pairs (one pair per TDD task).
</verification>

<success_criteria>
- HELP-03 component-level evidence: HelpLayout renders + sidebar drawer toggles + active styling + 9 unit tests green.
- HELP-04 component-level evidence: HelpWidget dispatches 8 stubs + unknown-widget error + 10 unit tests green.
- HELP-05 component-level evidence: HelpHook component compiles + topicToUrl pure-fn + 15 unit tests green.
- All migration source files translated: `react-router-dom` → `@tanstack/react-router`; all tokens → tokens.css names.
- `_helpLayout` mount comes in Plan 07-05; this plan just ships HelpLayout the component.
- VALIDATION.md per-task verification map filled with 8 task rows (orchestrator updates after this plan ships).
- Zero hex literals in `src/help/components/**` (Mermaid theme vars read from CSS at runtime).
- Zero console.error invocations from any component (MermaidBlock uses console.warn per RESEARCH P6).
</success_criteria>

<output>
After completion, create `.planning/phases/07-help-docs-v1-0/07-02-SUMMARY.md` describing:
- File-by-file delta (5 components + 1 pure fn + 5 test files + mdxComponents update).
- Token translation audit summary (paste output of `grep -roE "(bg|text|border)-[a-z]+(-[a-z]+)*(/[0-9]+)?" packages/spa/src/help/components/ | sort -u` to prove all classes match tokens.css names).
- TDD commit pairs (RED/GREEN log per task).
- Final test counts: vitest passed N, typecheck green, build green, tokenSourceOfTruth green.
- Coverage matrix: D-7-11 ✓ (full table applied), D-7-13 ✓ (workflow stubs added to NAV), D-7-14 ✓ (8 widget dispatch entries reference 07-03), D-7-15 ✓ (test surface per spec), HELP-06 ✓ (Keyboard shortcuts NAV entry), RESEARCH Pitfall 1 ✓ (StrictMode guard), Pitfall 4 ✓ (no hex), Pitfall 6 ✓ (console.warn), Pitfall 9 ✓ (not-prose wrapper).
</output>