---
phase: 10
plan: 09
type: execute
wave: 6
depends_on: [07, 08]
files_modified:
  - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md
  - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md
  - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md
  - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md
  - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md
  - refs/coverage-1440x900.png
  - refs/coverage-768.png
autonomous: false
tdd: false
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
  - COV-05
  - COV-06
  - COV-07
  - COV-08
  - COV-09
  - COV-10
  - COV-11
  - COV-12
must_haves:
  truths:
    - "Stage 1 review (`/review`) executed against the Phase 10 diff with spec compliance against dashboard-prompt.md + REQUIREMENTS.md COV-01..12 + the 10-REVIEWS.md amendments"
    - "Stage 2 review (`superpowers:requesting-code-review`) executed by an independent reviewer (fresh-context Claude session)"
    - "`/cso` audit covers the new cross-repo filesystem trust boundary (~/.gitnexus + 3 family roots) plus subprocess spawn surface (gitnexus analyze) plus the per-repo refresh lock"
    - "`/qa` live walkthrough against a dev server (localhost:5174 + daemon) clicks every filter chip, opens an override chip (if any sentinel exists), triggers a refresh popover, exercises Refresh-all-stale"
    - "impeccable:critique score ≥ 90 on /coverage at 1440x900 AND ≥ 87 at 768 (mobile is desktop-first non-blocker but must not be broken)"
    - "10-HUMAN-UAT.md scaffold exists with 6 acceptance scenarios derived from VALIDATION.md Manual-Only Verifications + the 4 user-named constraints in CONTEXT.md"
    - "After all gates: COV-01..COV-12 ticked in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md"
      provides: "Two-stage review document (Stage 1 spec-compliance + Stage 2 code-quality) with <finding severity=...> blocks per Phase 6 D-6-12"
    - path: ".planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md"
      provides: "Cross-repo filesystem trust boundary audit (security)"
    - path: ".planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md"
      provides: "Live /coverage walkthrough findings"
    - path: ".planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md"
      provides: "impeccable:critique scores at 1440x900 and 768"
    - path: ".planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md"
      provides: "6 acceptance scenarios + sign-off checklist"
    - path: "refs/coverage-1440x900.png"
      provides: "Screenshot for impeccable critique reference"
    - path: "refs/coverage-768.png"
      provides: "Screenshot at narrow breakpoint"
  key_links:
    - from: "10-REVIEW.md"
      to: "Stage 1 + Stage 2 finding lists"
      via: "two-stage review protocol (D-6-12)"
    - from: "10-IMPECCABLE.md"
      to: "POLISH-04 gate (Phase 6)"
      via: "≥ 90 composite at lg per Phase 6 closure ritual"
verification_test: "Phase 10 closing ritual — all 5 gate artifacts (10-REVIEW.md / 10-CSO.md / 10-QA.md / 10-IMPECCABLE.md / 10-HUMAN-UAT.md) exist with their respective contents committed. REQUIREMENTS.md COV-01..12 all ticked. PR can open against main."
---

<objective>
Execute the mandatory post-phase gates that 10-REVIEWS.md (CODEX finding #11) flagged as unscheduled. Without this plan, the project workflow rules (Stage 1 + Stage 2 review, `/cso`, `/qa`, impeccable critique ≥ 90, HUMAN-UAT scaffold) would be skipped under deadline pressure.

This plan is intentionally SEPARATE from 10-08 (workflow-version artefacts) and from 10-07 (route mount) because:
1. It runs LAST — after every other plan has produced its artefacts.
2. Multiple of its tasks are `autonomous: false` — they require human review, dev-server interaction, and screenshot eyeballing.
3. Splitting it out gives `/gsd-execute-phase` a clean Wave 6 dedicated to post-phase gates, after `10-08` (Wave 5) lands the migration + ADR.

Output: 5 gate documents + 2 screenshot artefacts + REQUIREMENTS.md tick-marks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEWS.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CONTEXT.md
@.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-VALIDATION.md
@.planning/REQUIREMENTS.md
@docs/spec/dashboard-prompt.md
@.planning/phases/06-polish-service-install-acceptance/06-07-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /cso audit ↔ Phase 10 diff | Reads the Phase 10 file changes to identify cross-repo filesystem trust boundary expansions. |
| /qa session ↔ local dev server | Browser interactions; bearer-token auth in localStorage; daemon on loopback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Repudiation | Gates skipped under deadline pressure | mitigate | This plan converts the gates into executable tasks with concrete acceptance criteria. CI / plan-checker will refuse to mark Phase 10 complete if any gate artifact is missing (acceptance criteria below). |
| T-10-09-02 | Tampering | Stage 1 + Stage 2 reviewers are the same Claude session | mitigate | Stage 2 task is `autonomous: false` and the human-action prompt explicitly instructs the user to run Stage 2 in a fresh-context `/clear`'d session via `superpowers:requesting-code-review`. |
| T-10-09-03 | Information Disclosure | impeccable:critique screenshots leak local paths | mitigate | Screenshots are of the SPA at /coverage; the SPA never displays absPath (CODEX HIGH-1). Screenshots saved to refs/ which is repo-tracked. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run `/review` Stage 1 — spec compliance audit vs REQUIREMENTS.md COV-01..12 + 10-REVIEWS.md amendments</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (COV-01..12 — read each requirement carefully; tick each off after verification)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEWS.md (every CODEX HIGH/MED/LOW + AGREED finding — Stage 1 confirms each amendment landed)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CONTEXT.md (D-10-01..D-10-11 — verify each locked decision is honored)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md (visual contract)
    - docs/spec/dashboard-prompt.md (the spec — the ultimate authority when in doubt)
    - .planning/phases/06-polish-service-install-acceptance/06-07-PLAN.md (REFERENCE — two-stage review format)
  </read_first>
  <action>
Run `/review` (the gstack review skill) against the Phase 10 diff. Document findings in `10-REVIEW.md` under a `## Stage 1: /review` heading.

The reviewer (Claude in the current session) walks the diff and verifies:

**A. REQUIREMENTS.md coverage (COV-01..12):**
For each requirement, locate the implementing file(s) and confirm the behaviour. Tick the box in REQUIREMENTS.md after verification.

**B. 10-REVIEWS.md amendment closure:**
For each finding (AGREED-1, AGREED-2, AGREED-3, AGREED-4, CODEX HIGH-1..6, CODEX MED-12..17, CODEX LOW-18..20), verify the corresponding code/test landed.

**C. CONTEXT.md decision fidelity (D-10-01..11):**
For each locked decision, verify the implementation matches the decision exactly. Flag any drift.

**D. Architectural invariants (INV-01..05):**
- INV-01: No daemon writes to project filesystems (Phase 10 spawn is `gitnexus analyze` only; gitnexus writes to ~/.gitnexus, not registered projects).
- INV-02: ~/.agenticapps/dashboard/ permissions still 0600.
- INV-04: Schema validation at both ends.
- INV-05: No native deps added.

Findings format (per Phase 6 D-6-12):
```markdown
<finding severity="error" req="COV-04" file="packages/agent/src/routes/coverage.ts">
  Title: <one-line summary>
  Detail: <explanation>
  Fix: <concrete remediation>
</finding>
```

Severities: error (blocks merge), warning (should fix), info (note for future).

After review, if ZERO errors and ≤ 5 warnings: Stage 1 PASSES. Otherwise, the plan-checker will REJECT and a remediation plan is needed.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md` exists with a `## Stage 1: /review` section.
    - `grep -c "<finding severity=" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md` ≥ 0 (zero findings = clean Stage 1; non-zero = explicit findings; either is acceptable as long as severity="error" count is 0).
    - `grep "<finding severity=\"error\"" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md | wc -l` returns 0 (no blocking findings).
    - REQUIREMENTS.md: all 12 COV-XX items have their `[ ]` flipped to `[x]` for the items that are complete.
    - All 17 review findings in 10-REVIEWS.md are addressed (explicitly noted as Closed-by-Plan-NN in the review doc).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md && grep -q "## Stage 1: /review" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md && test "$(grep -c '<finding severity=\"error\"' .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md)" = "0"</automated>
  </verify>
  <done>Stage 1 review document exists, zero error-severity findings, REQUIREMENTS.md updated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run Stage 2 — `superpowers:requesting-code-review` from a FRESH `/clear`'d session for independent reviewer perspective</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md</files>
  <read_first>
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md (Stage 1 findings — Stage 2 should NOT see this until after Stage 2 completes; the user runs Stage 2 from a fresh context)
    - .planning/phases/06-polish-service-install-acceptance/06-07-PLAN.md (Phase 6 closure ritual — Stage 2 protocol)
  </read_first>
  <what-built>
    The Phase 10 diff (all changes from Plans 01-08) is ready for an independent reviewer. The reviewer is a fresh-context Claude session (the user runs `/clear` then invokes the superpowers skill).
  </what-built>
  <how-to-verify>
    1. `/clear` the current context.
    2. Run `superpowers:requesting-code-review --target packages/agent packages/spa packages/shared --since main`.
    3. The skill prompts a fresh-context Claude to read the diff and produce findings.
    4. Append the Stage 2 findings to `10-REVIEW.md` under a `## Stage 2: superpowers:requesting-code-review` section.
    5. Confirm Stage 2 produces ZERO error-severity findings, OR negotiate fixes via follow-up plan.
  </how-to-verify>
  <resume-signal>Type "stage 2 review complete with N errors, M warnings" — N MUST be 0 to proceed. If non-zero, additional remediation plans are needed.</resume-signal>
  <action>
This task is `autonomous: false` because the user must explicitly `/clear` the context to get an independent reviewer. The instructions above are the operational protocol.
  </action>
  <verify>
    <automated>grep -q "## Stage 2: superpowers:requesting-code-review" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md && test "$(grep -c '<finding severity=\"error\"' .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md)" = "0"</automated>
  </verify>
  <done>Stage 2 section appended to 10-REVIEW.md; zero error-severity findings across BOTH stages combined; user has confirmed via resume-signal.</done>
</task>

<task type="auto">
  <name>Task 3: Run `/cso` — cross-repo filesystem trust boundary audit covering Phase 10's new attack surface</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md</files>
  <read_first>
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEWS.md (CODEX HIGH-2 symlink escape; CODEX HIGH-3 resolver-everywhere; CODEX MED-14 refresh lock — `/cso` confirms these landed)
    - packages/agent/src/lib/paths.ts (COVERAGE_ROOTS additions)
    - packages/agent/src/lib/coverageResolver.ts (Plan 03 — single point of FS entry)
    - packages/agent/src/lib/repoDiscovery.ts (Plan 02 — symlink escape guard)
    - packages/agent/src/lib/scanners/ (all 5 — confirm resolver injection)
    - packages/agent/src/lib/coverageSpawn.ts (gitnexus argv-array, no npx)
    - packages/agent/src/routes/coverage.ts (refresh: discoverRepos + realpath + per-repo lock)
    - .planning/phases/05-skills-health-panels/05-CONTEXT.md (D-5-21 NO npx CSO contract — must still hold)
  </read_first>
  <action>
Run the `/cso` skill to audit the Phase 10 changes. Document findings in `10-CSO.md`.

The audit covers:

**A. Filesystem trust boundary expansion:**
1. The 4 new allowed roots (`~/.gitnexus`, `~/Sourcecode/{agenticapps,factiv,neuroflash}`) — are they reachable ONLY via the dedicated scanners (not via /api/projects/:id/read)?
2. Does every scanner route reads through `coverageResolver` (CODEX HIGH-3)?
3. Does `repoDiscovery` reject symlinks escaping the family root (CODEX HIGH-2)?

**B. Subprocess spawn surface:**
1. `spawnGitNexusAnalyze` resolves the binary via PATH lookup (not npx)?
2. argv-array form only (no shell-string interpolation)?
3. POST /refresh re-canonicalises absPath via realpath before invoking spawn (TOCTOU mitigation)?

**C. Concurrent-request hygiene:**
1. Per-repo refresh lock (CODEX MED-14) — second concurrent POST against the same {family,repo} awaits the first?
2. Different repos refresh in parallel (no global lock)?

**D. Schema-bound disclosure:**
1. Public CoverageResponse contains NO absPath (CODEX HIGH-1)?
2. SPA components do NOT reference absPath anywhere in JSX or imports?

Findings format same as Task 1 (Stage 1 review).

After audit: ZERO error-severity findings → CSO PASSES.
  </action>
  <acceptance_criteria>
    - File `10-CSO.md` exists with sections A–D covered.
    - `grep "<finding severity=\"error\"" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md | wc -l` returns 0.
    - `grep -E "CODEX HIGH-2|CODEX HIGH-3|CODEX MED-14|D-5-21|CODEX HIGH-1" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md | wc -l` ≥ 5 (every audit-relevant finding cross-referenced).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md && test "$(grep -c '<finding severity=\"error\"' .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md)" = "0"</automated>
  </verify>
  <done>CSO audit document exists; zero error findings; CODEX HIGH-1/2/3 + MED-14 + D-5-21 explicitly cross-referenced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Run `/qa` — live /coverage walkthrough on localhost:5174 with daemon running</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md</files>
  <read_first>
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md §5 (interaction states — the QA walks each)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-UI-SPEC.md §11 (test coverage contract — UI behaviours to verify live)
  </read_first>
  <what-built>
    The /coverage route is mounted (Plan 07), all components implemented (Plans 06 + 06-Task-3a), useCoverage hook wired (Plan 05), daemon routes responding (Plan 04). A user can:
    - Visit localhost:5174/coverage
    - See 3 family sections
    - Filter via chip clicks (URL updates)
    - Search by repo name (URL updates after 200ms debounce)
    - Expand an override chip (if any sentinel exists)
    - Open the refresh popover on a row
    - Click Refresh-all-stale and observe the "Refreshing N of M…" indicator
  </what-built>
  <how-to-verify>
    1. Start the daemon: `pnpm --filter @agenticapps/dashboard-agent build && agentic-dashboard start`.
    2. Start the SPA: `pnpm --filter @agenticapps/dashboard-spa dev`.
    3. Open `http://localhost:5174` in browser; complete pair flow if needed.
    4. Navigate to `/coverage` (via sidebar → Observability → Coverage).
    5. Execute the QA checklist:
       - [ ] Page renders 3 family sections (agenticapps, factiv, neuroflash).
       - [ ] Each family header shows aggregate counts (worst-state-wins).
       - [ ] GitNexus column shows ⚪ Not installed for every row + per-family install hint inside header (current dev machine — gitnexus absent).
       - [ ] Click [✕ missing] chip → URL becomes `/coverage?status=missing`; matrix filters.
       - [ ] Type "agent" in search → URL becomes `?status=missing&q=agent` after 200ms; matrix filters further.
       - [ ] Click [all] chip → URL clears status; matrix unfilters.
       - [ ] Hover a row → refresh icon appears; click → popover with available remediations.
       - [ ] Click an option (e.g. "Copy /wiki-compile") → clipboard receives the string; toast appears (if implemented).
       - [ ] Press Esc → popover dismisses.
       - [ ] Click Refresh-all-stale → confirm dialog appears; click confirm → button text becomes "Refreshing 1 of N…", "Refreshing 2 of N…", etc.
       - [ ] Press Tab from page-header → focus reaches the toolbar → first family section → first row → override chip (if any) → refresh action.
       - [ ] No console errors during the walkthrough.
    6. Append findings to `10-QA.md`.
  </how-to-verify>
  <resume-signal>Type "QA walkthrough complete: all checks pass" OR list failing checks with line/file references.</resume-signal>
  <action>
This task is `autonomous: false` (checkpoint:human-verify) — the user runs the dev server + daemon, exercises the page in a browser, and documents the result.
  </action>
  <verify>
    <automated>test -f .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md && grep -q "QA checklist" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md</automated>
  </verify>
  <done>QA walkthrough document exists; all checklist items ticked; user has confirmed via resume-signal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run `impeccable:critique` on /coverage at 1440x900 + 768; capture screenshots; assert ≥ 90 composite at lg</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md, refs/coverage-1440x900.png, refs/coverage-768.png</files>
  <read_first>
    - .planning/phases/06-polish-service-install-acceptance/06-06-PLAN.md (REFERENCE — impeccable gate at Phase 6)
    - .planning/phases/06.1-typography-layout-impeccable-lift/06.1-06-PLAN.md (REFERENCE — impeccable re-measure)
    - .github/workflows/impeccable.yml (existing CI gate)
    - scripts/screenshot.mjs (Phase 6 — captures /coverage at viewport)
  </read_first>
  <what-built>
    /coverage is fully implemented and renders with Phase 05.1 tokens. impeccable:critique can be run against it the same way as any other route.
  </what-built>
  <how-to-verify>
    1. Start dev server + daemon (as in Task 4).
    2. Run `node scripts/screenshot.mjs --route /coverage --viewport 1440x900 --out refs/coverage-1440x900.png`.
    3. Run `node scripts/screenshot.mjs --route /coverage --viewport 768x1024 --out refs/coverage-768.png`.
    4. Run `npx impeccable critique --image refs/coverage-1440x900.png` — capture composite + sub-scores (Color, Typography, Layout).
    5. Run `npx impeccable critique --image refs/coverage-768.png` — same capture.
    6. Write findings to `10-IMPECCABLE.md` with the score breakdown.
    7. **Gate: composite ≥ 90 at 1440x900.** If < 90 at lg, additional polish work is required (separate plan; do not silently lower the gate).
    8. 768 is informational — desktop-first stance per UI-SPEC §8. Note any sub-87 score for future follow-up.
  </how-to-verify>
  <resume-signal>Type "impeccable composite at 1440x900 = NN" — NN MUST be ≥ 90 to proceed.</resume-signal>
  <action>
This task is `autonomous: false` because impeccable scoring + screenshot review involve visual judgement.

If the impeccable tool drift v1.0.1 follow-up (deferred from Phase 7) is unresolved, document the workaround used (e.g. pinning to last critique-capable version OR migrating to `detect` per the follow-up note in STATE.md).
  </action>
  <verify>
    <automated>test -f .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md && test -f refs/coverage-1440x900.png && test -f refs/coverage-768.png && grep -E "composite.*9[0-9]|composite.*100" .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md</automated>
  </verify>
  <done>impeccable critique document exists; both screenshots committed under refs/; composite ≥ 90 at 1440x900 confirmed; user has confirmed via resume-signal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Scaffold 10-HUMAN-UAT.md with 6 acceptance scenarios derived from VALIDATION.md Manual-Only Verifications + CONTEXT.md user-named constraints</name>
  <files>.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-VALIDATION.md (Manual-Only Verifications section — INV-01 git-status check, INV-02 permissions check, override-chip git-log accuracy, etc.)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CONTEXT.md (D-10-01..04 — 4 user-named constraints; each gets a UAT scenario)
    - .planning/phases/06-polish-service-install-acceptance/06-07-PLAN.md (REFERENCE — Phase 6 HUMAN-UAT format)
  </read_first>
  <what-built>
    The phase is functionally complete. The user (Donald) needs an explicit UAT scaffold with 6 acceptance scenarios to walk through before merging.
  </what-built>
  <how-to-verify>
    Write `10-HUMAN-UAT.md` containing the following six scenarios (one section each):

    **Scenario 1: Cross-family read happens daemon-side; no project FS writes during scan/refresh (INV-01)**
    Steps:
    1. Stage a clean checkout of any registered project (e.g. agenticapps-dashboard itself).
    2. Open /coverage in the dashboard.
    3. Trigger Refresh-all-stale (or any per-row refresh).
    4. Run `git status` in each registered project's root.
    5. Expected: 0 changes in ALL projects. The daemon only writes to ~/.gitnexus (gitnexus's own writes during analysis).

    **Scenario 2: ~/.agenticapps/dashboard/ permissions remain 0600 post-Phase-10 (INV-02)**
    Steps:
    1. After phase 10 lands, run `stat -f %p ~/.agenticapps/dashboard/auth.json`.
    2. Expected: last 3 digits = 600.

    **Scenario 3: D-10-01 pull-with-30s-cache UX feels right**
    Steps:
    1. Visit /coverage.
    2. Note the page-header "last refreshed N seconds ago" indicator.
    3. Reload — confirm the cache (sub-1s response). After 30s, reload again — confirm a real scan triggers.

    **Scenario 4: D-10-02 per-row refresh + clipboard for unsafe (D-10-09 amendment)**
    Steps:
    1. Find a row with wiki=stale (or simulate).
    2. Click refresh icon → popover.
    3. Click "Copy /wiki-compile command" → clipboard receives `cd ~/Sourcecode/<family> && claude /wiki-compile`.
    4. Paste into terminal, confirm it's valid.
    5. Find a row with gitNexus=stale (after gitnexus is installed) → "Run gitnexus analyze" → daemon spawns; row updates after invalidation.

    **Scenario 5: D-10-03 grouped sections + filter-aware aggregate counts**
    Steps:
    1. Click [✕ missing] chip.
    2. Confirm each family header's aggregate counts reflect the filtered view (not unfiltered total).
    3. Toggle [⚠ stale] on; counts update to union.
    4. Family-collapse toggle works; state persists across page reload.

    **Scenario 6: D-10-04 override chip — graceful empty state when no sentinels exist (Pitfall 5)**
    Steps:
    1. On dev machine (current state: 0 sentinels), confirm NO override chip renders on any row (NOT "0 overrides" text — literally nothing).
    2. Manually create a sentinel: `mkdir -p <some-repo>/.planning/phases/01-foo && touch <some-repo>/.planning/phases/01-foo/multi-ai-review-skipped`.
    3. `git add` + commit in that repo.
    4. Reload /coverage; the override chip appears with "⚠ 1 override".
    5. Click the chip; the expanded list shows `01-foo — sentinel since <commit ISO date>`.
    6. The date MUST match `git log -1 --format=%aI -- .planning/phases/01-foo/multi-ai-review-skipped` (NOT the file mtime).

    Each scenario has a `## Result` heading; the user fills in `Pass / Fail / Notes` after walking it.

    Bottom of file: sign-off table with 6 rows, one per scenario.
  </how-to-verify>
  <resume-signal>Type "10-HUMAN-UAT.md scaffold reviewed and 6 scenarios captured" — the user can defer the actual UAT walkthrough (it happens before merge, not before plan-checker).</resume-signal>
  <action>
This task is `autonomous: false` because the scenario sign-off requires the user. The plan executor writes the scaffold; the user later walks through and signs off before opening the merge PR.

The scaffold itself can be authored by the executor; only the sign-off requires human-in-the-loop.
  </action>
  <verify>
    <automated>test -f .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md && test "$(grep -c '^## Scenario' .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md)" -ge 6</automated>
  </verify>
  <done>10-HUMAN-UAT.md scaffold exists with all 6 scenarios; user has confirmed the scaffold is captured (actual walkthrough happens before merge).</done>
</task>

<task type="auto">
  <name>Task 7: Tick REQUIREMENTS.md COV-01..12 + update STATE.md with Phase 10 completion record</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (COV-01..12 — flip `[ ]` to `[x]` after Tasks 1-6 succeed)
    - .planning/STATE.md (Current Position / Performance Metrics / Accumulated Context sections)
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-REVIEW.md
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-CSO.md
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-QA.md
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-IMPECCABLE.md
    - .planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-HUMAN-UAT.md
  </read_first>
  <action>
1. Open `.planning/REQUIREMENTS.md`. For each of `COV-01` through `COV-12`, flip `- [ ]` to `- [x]` ONLY for items where the corresponding gate document confirms completion. If any item is partially complete, leave it as `[ ]` and document the remaining work in `STATE.md`.

2. Update `.planning/STATE.md`:
   - `stopped_at`: "Phase 10 complete — all 9 plans landed, all 12 COV requirements ticked, 5 gate artifacts authored, two-stage review passed."
   - `last_activity`: today's date + "Phase 10 closed".
   - `progress.completed_plans`: increment by 9.
   - Add a `## Decisions` entry: "[Phase 10]: Coverage matrix page shipped. Migration 0008 bumps workflow head to 1.8.0. CODEX HIGH-1 (absPath strip), HIGH-3 (resolver-everywhere), HIGH-5 (refresh contract pin), HIGH-6 (per-family install hint) and 13 other review findings all landed via the --reviews replan."

3. Commit (already part of the gsd-execute-phase wrap-up; this task just produces the diff).

Note: this is the LAST autonomous task in the phase. After it, the only remaining work is the optional Task 8 (PR open) and the user's HUMAN-UAT walkthrough.
  </action>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*COV-" .planning/REQUIREMENTS.md` is exactly 12 (all 12 COV items ticked).
    - `grep "Phase 10 complete" .planning/STATE.md | wc -l` ≥ 1.
    - `grep "1.8.0" .planning/STATE.md | wc -l` ≥ 1 (workflow head bump recorded).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*COV-' .planning/REQUIREMENTS.md)" = "12" && grep -q "Phase 10 complete" .planning/STATE.md</automated>
  </verify>
  <done>REQUIREMENTS.md COV-01..12 all ticked; STATE.md reflects Phase 10 closure.</done>
</task>

</tasks>

<verification>
- Stage 1 review (`/review`) executed: 10-REVIEW.md exists with zero error-severity findings.
- Stage 2 review (`superpowers:requesting-code-review`) executed from fresh-context Claude session.
- `/cso` audit (10-CSO.md) confirms CODEX HIGH-1/2/3 + MED-14 + D-5-21 fixes all landed.
- `/qa` live walkthrough (10-QA.md) confirms /coverage works on localhost:5174.
- impeccable:critique ≥ 90 at 1440x900 (10-IMPECCABLE.md).
- 10-HUMAN-UAT.md scaffold with 6 acceptance scenarios authored.
- REQUIREMENTS.md COV-01..12 all ticked.
- STATE.md updated.
</verification>

<success_criteria>
- All 5 gate documents (REVIEW, CSO, QA, IMPECCABLE, HUMAN-UAT) exist.
- ZERO error-severity findings across Stage 1 + Stage 2 + CSO.
- impeccable composite ≥ 90 at 1440x900.
- COV-01..12 ticked.
- Phase 10 ready for merge PR.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-10-coverage-matrix-page-per-repo-presence-freshness-of-claude-m/10-09-SUMMARY.md` documenting:
- Gate artifact paths (5).
- Final composite impeccable score at 1440x900 + 768.
- Stage 1 + Stage 2 finding counts.
- COV-01..12 tick status.
- Any deferred follow-ups (e.g. impeccable tool drift from v1.0.1, mobile breakpoint polish).
- A one-line summary that confirms Phase 10 is ready for merge.
</output>
