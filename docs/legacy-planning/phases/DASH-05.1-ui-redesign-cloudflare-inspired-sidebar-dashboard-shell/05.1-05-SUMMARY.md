---
phase: "05.1"
plan: "05"
subsystem: "spa-tokens"
tags: ["token-migration", "tailwind4", "repalette", "alias-elimination", "ui-redesign"]
dependency_graph:
  requires: ["05.1-01", "05.1-02", "05.1-03", "05.1-04"]
  provides: ["zero-legacy-alias-tokens", "wave5-precondition"]
  affects: ["packages/spa/src/styles/global.css", "packages/spa/src/styles/tokens.css", "all-spa-components"]
tech_stack:
  added: ["Tailwind 4 @theme token block (tokens.css)", "PageHeader UI component"]
  patterns: ["CSS-first token generation via @theme", "V2-gated feature flag pattern (VITE_APPSHELL_V2=1)", "namespaced Tailwind token classes"]
key_files:
  created:
    - "packages/spa/src/styles/tokens.css"
    - "packages/spa/src/components/ui/PageHeader.tsx"
  modified:
    - "packages/spa/src/styles/global.css"
    - "packages/spa/src/routes/settings.lazy.tsx"
    - "packages/spa/src/routes/help.lazy.tsx"
    - "packages/spa/src/components/ManualPairForm.tsx"
    - "packages/spa/src/components/ThemeToggle.tsx"
    - "packages/spa/src/components/ThemeChip.tsx"
    - "packages/spa/src/components/CodeBlock.tsx"
    - "packages/spa/src/components/RepairBanner.tsx"
    - "packages/spa/src/components/CommandPalette.tsx"
    - "packages/spa/src/components/SchemaDriftState.tsx"
    - "packages/spa/src/components/DaemonUnreachableState.tsx"
    - "packages/spa/src/components/CardContextMenu.tsx"
    - "packages/spa/src/components/RegisterModal.tsx"
    - "packages/spa/src/components/RenameTagsForms.tsx"
    - "packages/spa/src/components/Header.tsx"
    - "packages/spa/src/components/AppShell.tsx"
    - "packages/spa/src/components/panels/PanelContainer.tsx"
    - "packages/spa/src/components/OnboardingHero.tsx"
    - "packages/spa/src/components/HomeToolbar.tsx"
    - "packages/spa/src/components/MultiProjectHome.tsx"
    - "packages/spa/src/components/ProjectCard.tsx"
    - "packages/spa/src/components/ProjectHeader.tsx"
    - "packages/spa/src/components/RegisterButtonCard.tsx"
    - "packages/spa/src/routes/pair-error.tsx"
    - "packages/spa/src/routes/pair.lazy.tsx"
    - "packages/spa/src/components/panels/*.tsx (13 panels)"
    - "packages/spa/src/components/*.test.tsx (11 test files)"
    - "packages/spa/src/components/panels/*.test.tsx (7 test files)"
decisions:
  - "Batch-migrated 24 files beyond plan's 8 explicit targets — worktree was pre-05.1-01 state, so AppShell/panels/routes still had legacy aliases; treated as sub-task additions per plan NOTE"
  - "Test assertion strings updated to new namespaced token names; test description strings and JSDoc comments also cleaned for consistency"
  - "Wave 5 precondition confirmed: zero [--*] patterns in packages/spa/src/ after both tasks"
metrics:
  duration: "resumed session (~3 hours total)"
  completed_date: "2026-05-09"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 47
---

# Phase 5.1 Plan 05: Alias Migration Final — Complete Summary

**One-liner:** Full workspace alias elimination — Tailwind 4 @theme token block + backwards-compat alias layer + PageHeader V2 gate + repalette of all 32 SPA components (planned 8 + 24 discovered) to namespaced tokens; zero `[--*]` usage remaining.

## Tasks Completed

| Task | Description | Commit | Files |
|------|-------------|--------|-------|
| 1 | PageHeader V2-gate for /settings + /help; repalette ManualPairForm, ThemeToggle, ThemeChip, CodeBlock; create tokens.css + global.css alias layer | `ea4bf01` | 10 files |
| 2 | Repalette RepairBanner (animation removed), CommandPalette, SchemaDriftState, DaemonUnreachableState, CardContextMenu, RegisterModal, RenameTagsForms, Header; batch-migrate 24 additional files; update 18 test assertion files | `9f9545d` | 45 files |

## What Was Built

### Token Infrastructure (Task 1 prerequisite)

**`packages/spa/src/styles/tokens.css`** — Tailwind 4 `@theme` block locking Phase 5.1 palette:
- `--color-app-bg: #FAFAF7`, `--color-card-bg: #FFFFFF`, `--color-card-bg-hover: #F5F5F0`
- `--color-border-subtle: #E8E8E3`, `--color-text-primary: #1A1A18`, `--color-text-secondary: #6B6B63`, `--color-text-tertiary: #9B9B93`
- `--color-accent: #6B46C1`, `--color-accent-hover: #5A389F`
- `--color-status-error: #B53D3D`, `--color-status-warning: #C17E3D`, `--color-status-success: #2D7A4F`
- Shadow, radius, and z-index ladder tokens

**`packages/spa/src/styles/global.css`** — Backwards-compat alias layer pointing legacy `[--*]` CSS vars to new `--color-*` vars (Wave 5 will delete this layer).

### PageHeader Component (Task 1)

**`packages/spa/src/components/ui/PageHeader.tsx`** — Per-route heading component with `title`, `helper`, `actions`, and `children` props. Used in `/settings` and `/help` routes when `VITE_APPSHELL_V2 === '1'`.

### Token Migration Coverage

Complete migration of all 32 affected SPA components from `[--legacy-alias]` to namespaced Tailwind classes:

| Legacy alias | New class |
|---|---|
| `bg-[--surface]` | `bg-card-bg` |
| `bg-[--surface-elevated]` | `bg-card-bg-hover` |
| `text-[--text]` | `text-text-primary` |
| `text-[--text-muted]` | `text-text-secondary` |
| `text-[--text-subtle]` | `text-text-tertiary` |
| `text-[--danger]` | `text-status-error` |
| `text-[--warning]` | `text-status-warning` |
| `text-[--success]` | `text-status-success` |
| `border-[--border]` | `border-border-subtle` |
| `bg-[--accent]` | `bg-accent` |
| `border-[--accent]` | `border-accent` |
| `text-[--accent]` | `text-accent` |
| `focus-visible:ring-[--ring]` | `focus-visible:ring-accent` |
| `focus-visible:ring-offset-[--bg]` | `focus-visible:ring-offset-app-bg` |

### D-5.1-10 Compliance (Anti-AI-slop)

RepairBanner mount animation (`motion-safe:animate-[slideInDown_150ms_ease-out]`) removed per decision D-5.1-10.

## Verification

- **Tests:** 515 passed, 0 failed (56 test files)
- **Typecheck:** 0 errors across all packages
- **Lint:** 0 errors (35 pre-existing import/order warnings, none introduced by this plan)
- **Alias audit:** `grep -rEn '\[--[a-z]' packages/spa/src/` → ZERO matches
- **PageHeader in settings:** `grep -c "PageHeader" packages/spa/src/routes/settings.lazy.tsx` → 2
- **PageHeader in help:** `grep -c "PageHeader" packages/spa/src/routes/help.lazy.tsx` → 2
- **RepairBanner animation:** `grep -c "motion-safe:animate" RepairBanner.tsx` → 0

## Deviations from Plan

### Auto-added Scope Expansion (Rule 2 / Plan NOTE)

**[Rule 2 - Missing Critical Functionality] Batch-migrated 24 additional files beyond the 8 explicit Task 2 targets**
- **Found during:** Post-Task 2 workspace-wide alias audit
- **Issue:** This worktree is based on the pre-05.1 branch state (before plans 01-04 were applied), so AppShell, PanelContainer, OnboardingHero, HomeToolbar, MultiProjectHome, ProjectCard, ProjectHeader, RegisterButtonCard, all 13 panel files, pair-error.tsx, and pair.lazy.tsx still contained legacy alias tokens
- **Fix:** Python batch script applied all token translations across 24 additional files, achieving the plan's stated goal of zero workspace-wide aliases
- **Files modified:** 24 additional files beyond plan scope
- **Commit:** `9f9545d` (included in same Task 2 commit)

**Plan NOTE:** The plan explicitly noted this worktree discrepancy and authorized treating additional migrations as sub-task additions.

### Infrastructure Prerequisites (Rule 2)

**[Rule 2 - Missing Critical Functionality] Created tokens.css and global.css alias layer as prerequisite**
- **Found during:** Task 1 start
- **Issue:** Worktree had no `tokens.css` file and `global.css` still used pre-05.1 raw hex values; the Tailwind 4 @theme infrastructure required by the entire plan was absent
- **Fix:** Created `packages/spa/src/styles/tokens.css` with the locked Phase 5.1 palette, rewrote `global.css` to import it and provide backwards-compat aliases
- **Commit:** `ea4bf01`

### Test Assertion Updates (Rule 1 - Bug)

**[Rule 1 - Bug] Updated 18 test files asserting legacy `[--*]` class names**
- **Found during:** Post-migration test run (18 failing assertions)
- **Issue:** Test assertions checked for `text-[--text]`, `border-[--border]`, etc. which no longer matched migrated source class names
- **Fix:** Updated all `expect(...).toContain('text-[--*]')` assertions, `document.querySelector('.text-\\[--*\\]')` calls, and test/JSDoc description strings to use new namespaced token names
- **Files modified:** 11 test files in `src/components/` + 7 test files in `src/components/panels/`
- **Commit:** `9f9545d`

## Known Stubs

None. All token migrations are complete — no hardcoded empty values or placeholder data introduced.

## Threat Flags

None. This plan only modified CSS class names in existing components; no new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| `packages/spa/src/styles/tokens.css` | FOUND |
| `packages/spa/src/components/ui/PageHeader.tsx` | FOUND |
| `05.1-05-SUMMARY.md` | FOUND |
| Commit `ea4bf01` (Task 1) | FOUND |
| Commit `9f9545d` (Task 2) | FOUND |
| Legacy `[--*]` alias matches in packages/spa/src/ | 0 (CLEAN) |
