---
phase: 05.1
plan: 05
type: execute
wave: 5
depends_on: [05.1-04]
files_modified:
  - packages/spa/src/routes/settings.lazy.tsx
  - packages/spa/src/routes/help.lazy.tsx
  - packages/spa/src/components/ManualPairForm.tsx
  - packages/spa/src/components/ThemeToggle.tsx
  - packages/spa/src/components/ThemeChip.tsx
  - packages/spa/src/components/CodeBlock.tsx
  - packages/spa/src/components/RepairBanner.tsx
  - packages/spa/src/components/CommandPalette.tsx
  - packages/spa/src/components/SchemaDriftState.tsx
  - packages/spa/src/components/DaemonUnreachableState.tsx
  - packages/spa/src/components/CardContextMenu.tsx
  - packages/spa/src/components/RegisterModal.tsx
  - packages/spa/src/components/RenameTagsForms.tsx
  - packages/spa/src/components/Header.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings route renders inside AppShellV2 with PageHeader (title 'Settings'); ManualPairForm + ThemeToggle repaletted to new tokens"
    - "Help route renders inside AppShellV2 with PageHeader (title 'Help'); CodeBlock repaletted to new tokens"
    - "All remaining shell-adjacent components (RepairBanner, CommandPalette, SchemaDriftState, DaemonUnreachableState, CardContextMenu, RegisterModal, RenameTagsForms, ThemeChip, Header) migrated to Tailwind 4 namespaced tokens"
    - "After this plan, a global grep over `packages/spa/src/` returns ZERO matches for `[--text]`, `[--surface]`, `[--border]`, `[--accent]`, `[--ring]`, `[--danger]`, `[--warning]`, `[--success]`, `[--bg]`, `[--text-muted]`, `[--text-subtle]` (precondition for Wave 5 alias deletion)"
    - "Header.tsx is migrated for the legacy-mode flag-OFF rendering path; AppShellV2's TopBar is the V2-mode replacement"
    - "1117-test baseline + new V2-mode tests pass; pnpm lint green"
  artifacts:
    - path: "packages/spa/src/routes/settings.lazy.tsx"
      provides: "Settings route with PageHeader wrapper (V2-gated)"
      contains: "PageHeader"
    - path: "packages/spa/src/routes/help.lazy.tsx"
      provides: "Help route with PageHeader wrapper (V2-gated)"
      contains: "PageHeader"
    - path: "packages/spa/src/components/CodeBlock.tsx"
      provides: "Repaletted code block — bg-card-bg-hover + text-text-primary + border-border-subtle"
      contains: "bg-card-bg-hover"
  key_links:
    - from: "packages/spa/src/routes/settings.lazy.tsx"
      to: "packages/spa/src/components/ui/PageHeader.tsx"
      via: "<PageHeader title=\"Settings\" /> wrap"
      pattern: "PageHeader.*Settings"
    - from: "packages/spa/src/routes/help.lazy.tsx"
      to: "packages/spa/src/components/ui/PageHeader.tsx"
      via: "<PageHeader title=\"Help\" /> wrap"
      pattern: "PageHeader.*Help"
---

<objective>
Wave 4 — Migrate `/settings` and `/help` routes; repalette every remaining shell-adjacent component (RepairBanner, CommandPalette, SchemaDriftState, DaemonUnreachableState, CardContextMenu, RegisterModal, RenameTagsForms, ManualPairForm, ThemeToggle, ThemeChip, CodeBlock, Header) to Tailwind 4 namespaced tokens. After this plan, the entire SPA is alias-free; the precondition for Wave 5 (alias deletion + flag flip) is met.

Purpose: Plans 02-04 migrated the most-visible content; this plan sweeps the remaining surface area. The Wave 5 alias deletion would break any file still using `text-[--text]` form, so this plan is the no-aliases-remaining checkpoint.

Output: 12 components + 2 routes repaletted; full grep audit returns empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-CONTEXT.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-RESEARCH.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-04-SUMMARY.md
@CLAUDE.md
@packages/spa/src/components/CodeBlock.tsx
@packages/spa/src/components/RepairBanner.tsx
@packages/spa/src/components/CommandPalette.tsx
@packages/spa/src/components/SchemaDriftState.tsx
@packages/spa/src/components/DaemonUnreachableState.tsx
@packages/spa/src/components/CardContextMenu.tsx
@packages/spa/src/components/RegisterModal.tsx
@packages/spa/src/components/RenameTagsForms.tsx
@packages/spa/src/components/ManualPairForm.tsx
@packages/spa/src/components/ThemeToggle.tsx
@packages/spa/src/components/ThemeChip.tsx
@packages/spa/src/components/Header.tsx
@packages/spa/src/routes/settings.lazy.tsx
@packages/spa/src/routes/help.lazy.tsx
@packages/spa/src/components/ui/PageHeader.tsx

<token_translation_table>
SAME table as Plan 04. Repeated here for executor convenience:

| Legacy alias usage          | Namespaced replacement       |
|------------------------------|------------------------------|
| `bg-[--surface]`             | `bg-card-bg`                 |
| `bg-[--surface-elevated]`    | `bg-card-bg-hover`           |
| `border-[--border]`          | `border-border-subtle`       |
| `border-[--border-strong]`   | `border-border-subtle`       |
| `text-[--text]`              | `text-text-primary`          |
| `text-[--text-muted]`        | `text-text-secondary`        |
| `text-[--text-subtle]`       | `text-text-tertiary`         |
| `bg-[--accent]`              | `bg-accent`                  |
| `bg-[--accent-hover]`        | `bg-accent-hover`            |
| `text-[--accent]`            | `text-accent`                |
| `text-[--accent-fg]`         | `text-white`                 |
| `bg-[--ring]`                | `bg-accent`                  |
| `focus-visible:ring-[--ring]`| `focus-visible:ring-accent`  |
| `text-[--danger]`            | `text-status-error`          |
| `bg-[--danger-surface]`      | `bg-card-bg-hover`           |
| `border-l-[--danger]`        | `border-l-status-error`      |
| `text-[--warning]`           | `text-status-warning`        |
| `bg-[--warning-surface]`     | `bg-card-bg-hover`           |
| `text-[--success]`           | `text-status-success`        |
| `text-[--bg]`                | `text-app-bg`                |
| `bg-[--bg]`                  | `bg-app-bg`                  |
| `divide-[--border]`          | `divide-border-subtle`       |
</token_translation_table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap /settings + /help routes in PageHeader (V2-gated) + repalette ManualPairForm, ThemeToggle, ThemeChip, CodeBlock</name>
  <files>
    packages/spa/src/routes/settings.lazy.tsx,
    packages/spa/src/routes/help.lazy.tsx,
    packages/spa/src/components/ManualPairForm.tsx,
    packages/spa/src/components/ThemeToggle.tsx,
    packages/spa/src/components/ThemeChip.tsx,
    packages/spa/src/components/CodeBlock.tsx
  </files>
  <read_first>
    - packages/spa/src/routes/settings.lazy.tsx (current — wraps ManualPairForm + ThemeToggle in space-y-12)
    - packages/spa/src/routes/help.lazy.tsx (current — single section with text content)
    - packages/spa/src/components/ManualPairForm.tsx (8-state SPA-04 machine — DO NOT change behavior, only chrome)
    - packages/spa/src/components/ThemeToggle.tsx
    - packages/spa/src/components/ThemeChip.tsx
    - packages/spa/src/components/CodeBlock.tsx (used by HookFirings, OnboardingHero, /help — must migrate carefully because OnboardingHero is OUTSIDE V2 mode but still resolves through aliases when alias layer is removed in Wave 5)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §1 ("Settings + Help"), §7 (PageHeader)
    - <token_translation_table>
  </read_first>
  <behavior>
    Existing test files for ManualPairForm, ThemeToggle, ThemeChip, CodeBlock MUST pass without modification.

    settings.lazy.tsx and help.lazy.tsx are simple route components — no test files exist beyond e2e routing tests; no test changes required. The routes get a V2-gated PageHeader wrap.
  </behavior>
  <action>
    Step 1 — packages/spa/src/routes/settings.lazy.tsx:
    ```typescript
    import { useEffect } from 'react'
    import { createLazyRoute } from '@tanstack/react-router'

    import { ManualPairForm } from '../components/ManualPairForm.js'
    import { ThemeToggle } from '../components/ThemeToggle.js'
    import { PageHeader } from '../components/ui/PageHeader.js'

    export const Route = createLazyRoute('/settings')({ component: SettingsPage })

    function SettingsPage(): React.JSX.Element {
      useEffect(() => { document.title = 'AgenticApps Dashboard — Settings' }, [])
      const useV2 = import.meta.env.VITE_APPSHELL_V2 === '1'
      return (
        <div className="space-y-12">
          {useV2 && <PageHeader title="Settings" helper="Pair the daemon and configure the dashboard." />}
          <ManualPairForm />
          <ThemeToggle />
        </div>
      )
    }
    ```

    Step 2 — packages/spa/src/routes/help.lazy.tsx:
    ```typescript
    import { useEffect } from 'react'
    import { createLazyRoute } from '@tanstack/react-router'

    import { PageHeader } from '../components/ui/PageHeader.js'

    export const Route = createLazyRoute('/help')({ component: HelpPage })

    function HelpPage(): React.JSX.Element {
      useEffect(() => { document.title = 'AgenticApps Dashboard — Help' }, [])
      const useV2 = import.meta.env.VITE_APPSHELL_V2 === '1'
      return (
        <div className="space-y-6">
          {useV2 && <PageHeader title="Help" helper="Reference and troubleshooting." />}
          <section className="rounded-card bg-card-bg p-6 shadow-card">
            <h2 className="text-lg font-semibold leading-snug text-text-primary">Help</h2>
            <p className="mt-3 text-base leading-relaxed text-text-secondary">
              Detailed help arrives in Phase 6. For now, see the README at{' '}
              <code className="font-mono text-sm">github.com/agenticapps-eu/agenticapps-dashboard</code>.
            </p>
          </section>
        </div>
      )
    }
    ```

    Step 3 — packages/spa/src/components/ManualPairForm.tsx — apply <token_translation_table>. ManualPairForm has 8 states (SPA-04 state machine); each state has visual chrome that uses aliases. NO state-machine changes; ONLY className token swaps.

    Step 4 — packages/spa/src/components/ThemeToggle.tsx — apply <token_translation_table>.

    Step 5 — packages/spa/src/components/ThemeChip.tsx — apply <token_translation_table>. (Used inside TopBar AND legacy Header.)

    Step 6 — packages/spa/src/components/CodeBlock.tsx — apply <token_translation_table>. CodeBlock is used in: HookFirings (DISC-04 install hint), OnboardingHero (/onboarding — outside V2), /help. Migrating CodeBlock once propagates to all three.

    Step 7 — Pre-commit audit on these 6 files:
    ```bash
    grep -nE '\[--text\b|\[--surface|\[--border|\[--accent|\[--ring|\[--danger|\[--warning|\[--success|\[--text-muted|\[--text-subtle|\[--bg\b' \
      packages/spa/src/routes/settings.lazy.tsx \
      packages/spa/src/routes/help.lazy.tsx \
      packages/spa/src/components/ManualPairForm.tsx \
      packages/spa/src/components/ThemeToggle.tsx \
      packages/spa/src/components/ThemeChip.tsx \
      packages/spa/src/components/CodeBlock.tsx
    ```
    Output empty.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/ManualPairForm.test.tsx src/components/ThemeToggle.test.tsx src/components/ThemeChip.test.tsx src/components/CodeBlock.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/routes/settings.lazy.tsx packages/spa/src/routes/help.lazy.tsx packages/spa/src/components/ManualPairForm.tsx packages/spa/src/components/ThemeToggle.tsx packages/spa/src/components/ThemeChip.tsx packages/spa/src/components/CodeBlock.tsx` returns NO matches
    - `grep -c "PageHeader" packages/spa/src/routes/settings.lazy.tsx` returns ≥ 1
    - `grep -c "PageHeader" packages/spa/src/routes/help.lazy.tsx` returns ≥ 1
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/ManualPairForm.test.tsx src/components/ThemeToggle.test.tsx src/components/ThemeChip.test.tsx src/components/CodeBlock.test.tsx` exits 0
    - `pnpm -r test` exits 0
  </acceptance_criteria>
  <done>
    Settings + Help routes wrap content in PageHeader on V2; 4 components alias-free.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repalette remaining shell-adjacent components (RepairBanner, CommandPalette, SchemaDriftState, DaemonUnreachableState, CardContextMenu, RegisterModal, RenameTagsForms, Header)</name>
  <files>
    packages/spa/src/components/RepairBanner.tsx,
    packages/spa/src/components/CommandPalette.tsx,
    packages/spa/src/components/SchemaDriftState.tsx,
    packages/spa/src/components/DaemonUnreachableState.tsx,
    packages/spa/src/components/CardContextMenu.tsx,
    packages/spa/src/components/RegisterModal.tsx,
    packages/spa/src/components/RenameTagsForms.tsx,
    packages/spa/src/components/Header.tsx
  </files>
  <read_first>
    - packages/spa/src/components/RepairBanner.tsx (animation: `motion-safe:animate-[slideInDown_150ms_ease-out]` — CONSIDER removing per D-5.1-10 anti-AI-slop "no animations on route entry" — see action step 1)
    - packages/spa/src/components/CommandPalette.tsx (153 LOC — palette dialog chrome)
    - packages/spa/src/components/SchemaDriftState.tsx
    - packages/spa/src/components/DaemonUnreachableState.tsx
    - packages/spa/src/components/CardContextMenu.tsx
    - packages/spa/src/components/RegisterModal.tsx (largest — 23kB / ~600 LOC; many states)
    - packages/spa/src/components/RenameTagsForms.tsx
    - packages/spa/src/components/Header.tsx (legacy-mode top bar; KEPT through Wave 5 for flag-OFF mode)
    - All corresponding .test.tsx files
    - <token_translation_table>
  </read_first>
  <behavior>
    All test files for these components MUST pass without modification.

    RepairBanner animation decision: D-5.1-10 forbids "animations on route entry, sidebar open, or panel mount." The `motion-safe:animate-[slideInDown_150ms_ease-out]` is a banner mount animation. The decision is to REMOVE it (anti-AI-slop conformance). Existing RepairBanner.test.tsx asserts banner appearance/dismissal but not the animation class — verify, then remove the className.
  </behavior>
  <action>
    For each of the 8 files, apply <token_translation_table> mechanically.

    Special action items:
    - RepairBanner.tsx: REMOVE `motion-safe:animate-[slideInDown_150ms_ease-out]` from the className (D-5.1-10). The banner appears on state change, not on route entry, but the animation is decorative-only and conflicts with anti-AI-slop discipline.
    - RepairBanner.tsx: change `border-l-[--danger]` → `border-l-status-error`; `bg-[--danger-surface]` → `bg-card-bg-hover` with a `text-status-error` accent; `text-[--accent]` on the Re-pair button → `text-accent`; etc.
    - CommandPalette.tsx: dialog chrome migrates. The `bg-[--surface] border-[--border-strong]` → `bg-card-bg border-border-subtle`. The listbox option highlighted state `bg-[--surface-elevated]` → `bg-card-bg-hover`. The ⌘K hint chip: `bg-[--surface-elevated] border-[--border]` → `bg-card-bg-hover border-border-subtle`. Search icon `text-[--text-muted]` → `text-text-secondary`.
    - RegisterModal.tsx: large file with form fields, buttons, error states. Mechanical token-swap pass. Pay attention to button accent states (`bg-[--accent] text-[--accent-fg]` → `bg-accent text-white`).
    - Header.tsx (legacy mode): apply translation table. Note `bg-[--surface]` → `bg-card-bg` (flag-OFF rendering keeps the legacy header but with new tokens).

    Pre-commit audit on these 8 files: same grep, output empty.

    Final-final audit (THIS plan's main acceptance criterion) — workspace-wide:
    ```bash
    grep -rEn '\[--text\b|\[--surface|\[--border|\[--accent|\[--ring|\[--danger|\[--warning|\[--success|\[--text-muted|\[--text-subtle|\[--bg\b' \
      packages/spa/src/ --include="*.tsx" --include="*.ts" \
      | grep -v ".test.tsx" | grep -v ".test.ts"
    ```
    Output MUST be empty (modulo OnboardingHero — see note below).

    NOTE on OnboardingHero.tsx (NOT in this plan's files_modified):
    - OnboardingHero is rendered on /onboarding which is OUTSIDE V2 (D-5.1-03)
    - It uses CodeBlock which IS migrated in Task 1
    - OnboardingHero's own className aliases (`bg-[--bg]` etc.) MUST also migrate so the audit passes
    - ADD OnboardingHero.tsx to this task's file list if grep shows residual aliases (likely needs the same translation-table pass)

    NOTE on pair.lazy.tsx, pair-error.tsx, onboarding.lazy.tsx, index.lazy.tsx, projects.$projectId.lazy.tsx — these are route-level components. If they use legacy aliases, they must also migrate. The audit grep will reveal them; treat any positive-match file as a sub-task addition.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa test --run src/components/RepairBanner.test.tsx src/components/CommandPalette.test.tsx src/components/SchemaDriftState.test.tsx src/components/DaemonUnreachableState.test.tsx src/components/CardContextMenu.test.tsx src/components/RegisterModal.test.tsx src/components/RenameTagsForms.test.tsx src/components/Header.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Workspace-wide audit: `grep -rEn '\\[--text\\b|\\[--surface|\\[--border|\\[--accent|\\[--ring|\\[--danger|\\[--warning|\\[--success|\\[--text-muted|\\[--text-subtle|\\[--bg\\b' packages/spa/src/ --include="*.tsx" --include="*.ts" | grep -v ".test.tsx" | grep -v ".test.ts"` returns NO matches (Wave 5 alias-deletion precondition met)
    - `grep -c "motion-safe:animate" packages/spa/src/components/RepairBanner.tsx` returns 0 (D-5.1-10 conformance)
    - `pnpm --filter @agenticapps/dashboard-spa test --run src/components/` exits 0
    - `pnpm -r typecheck` exits 0
    - `pnpm -r test` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    Every component file in packages/spa/src/components/ uses Tailwind 4 namespaced tokens. Workspace-wide alias grep is empty. RepairBanner animation removed (anti-AI-slop). Wave 5 alias-deletion precondition met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 4 is mechanical token migration on settings/help and remaining components. No new network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-05-01 | T (Tampering) | ManualPairForm 8-state machine className regression | mitigate | Existing test coverage preserves state transitions; only className strings change. |
| T-05.1-05-02 | I (Information disclosure) | RegisterModal form field rendering token migration | mitigate | Token swap is invisible at the test-assertion level (text + aria); form fields still render with same accessibility tree. |
| T-05.1-05-03 | T (Tampering) | RepairBanner animation removal | accept | Banner functionality (display + dismiss) preserved; only the slide-in motion is removed. D-5.1-10 anti-AI-slop conformance gain outweighs minor UX shift. |
</threat_model>

<verification>
- pnpm -r typecheck exits 0
- pnpm -r test exits 0 (1117 baseline + cumulative V2-mode tests)
- pnpm lint exits 0
- Manual smoke (V2 mode): visit /settings, /help — PageHeader visible at top; pair form looks correct; help section uses bg-card-bg.
- Manual smoke (legacy mode): all routes still render via the legacy AppShell with new-token-driven aliases; visual regression minimal (the alias map shifted colors slightly but layout is unchanged).
- The workspace-wide audit grep for legacy aliases returns empty — Wave 5 can safely delete the alias layer.
</verification>

<success_criteria>
1. /settings and /help routes have PageHeader on V2 mode.
2. ManualPairForm, ThemeToggle, ThemeChip, CodeBlock alias-free.
3. RepairBanner, CommandPalette, SchemaDriftState, DaemonUnreachableState, CardContextMenu, RegisterModal, RenameTagsForms, Header alias-free.
4. RepairBanner animation removed (D-5.1-10).
5. Workspace-wide audit grep returns NO legacy aliases (Wave 5 precondition).
6. 1117-test baseline + new V2-mode tests pass.
7. `pnpm lint` green.
</success_criteria>

<output>
After completion, create `.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-05-SUMMARY.md` documenting:
- Confirmation workspace-wide audit empty
- The RepairBanner animation removal (anti-AI-slop)
- Note for Wave 5: alias layer can now be safely deleted; AppShell, Header (legacy use only), HomeLayout, ProjectLayout, appShellWidth.ts can be deleted as part of the flag flip
- Confirmation OnboardingHero.tsx migrated (if it needed it)
</output>
