---
phase: 00-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .nvmrc
  - .gitattributes
  - .editorconfig
  - .gitignore
  - eslint.config.mjs
  - prettier.config.mjs
  - vitest.config.ts
  - .github/workflows/ci.yml
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/vitest.config.ts
  - packages/shared/src/index.ts
  - packages/shared/src/schemas/health.ts
  - packages/shared/src/schemas/health.test.ts
  - packages/agent/package.json
  - packages/agent/tsconfig.json
  - packages/agent/vitest.config.ts
  - packages/agent/src/index.ts
  - packages/agent/src/index.test.ts
  - packages/spa/package.json
  - packages/spa/tsconfig.json
  - packages/spa/vitest.config.ts
  - packages/spa/src/index.ts
  - packages/spa/src/index.test.ts
autonomous: true
requirements: [BOOT-01, BOOT-02]
requirements_addressed: [BOOT-01, BOOT-02]
threat_refs: [T-00-01, T-00-02]

must_haves:
  truths:
    - "`pnpm install --frozen-lockfile` from a fresh checkout exits 0 and produces a single root `pnpm-lock.yaml`"
    - "All three packages (`@agenticapps/dashboard-shared`, `@agenticapps/dashboard-agent`, `@agenticapps/dashboard-spa`) resolve via pnpm workspace and import each other through the `workspace:*` protocol"
    - "`pnpm lint`, `pnpm typecheck`, `pnpm test --run`, and `pnpm build` each exit 0"
    - "GitHub Actions `ci.yml` runs all five gates (install / lint / typecheck / test / build) on push and pull_request"
    - "HealthResponseSchema is defined in `packages/shared` and parses correctly"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace declaration + pnpm catalog"
      contains: "catalog:"
    - path: "package.json"
      provides: "Root scripts + engines pin"
      contains: "lint"
    - path: "tsconfig.base.json"
      provides: "Strict TypeScript baseline"
      contains: "\"strict\": true"
    - path: ".nvmrc"
      provides: "Node 20 LTS pin"
      contains: "20"
    - path: "vitest.config.ts"
      provides: "Root vitest with test.projects"
      contains: "projects"
    - path: "eslint.config.mjs"
      provides: "ESLint flat config (typescript-eslint + prettier)"
      contains: "typescript-eslint"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline (push + PR)"
      contains: "pnpm test"
    - path: "packages/shared/src/schemas/health.ts"
      provides: "HealthResponseSchema (Zod)"
      contains: "HealthResponseSchema"
    - path: "packages/shared/src/schemas/health.test.ts"
      provides: "Schema unit tests"
      contains: "HealthResponseSchema"
  key_links:
    - from: "packages/agent/package.json"
      to: "@agenticapps/dashboard-shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: "packages/spa/package.json"
      to: "@agenticapps/dashboard-shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: ".github/workflows/ci.yml"
      to: "pnpm scripts"
      via: "lint/typecheck/test/build steps"
      pattern: "pnpm (lint|typecheck|test|build)"
---

<objective>
Wire the entire pnpm workspace end-to-end in a single smoke plan: root config, three package skeletons, root + per-package Vitest configs, ESLint/Prettier, root TS config, the one shared `HealthResponseSchema`, and a green CI workflow. After this plan, `pnpm install && pnpm lint && pnpm typecheck && pnpm test --run && pnpm build` all pass from a clean checkout, and the same five gates run green on `push` and `pull_request` in GitHub Actions.

Purpose: Catch workspace resolution / catalog / Vitest projects / TS path failures before any per-package plan adds substantive content. RESEARCH.md §"Sequencing Recommendation" makes this case explicitly.

Output: A complete, minimal-but-real workspace skeleton. Every subsequent Phase 0 plan (02–07) builds inside this skeleton without touching the root wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/agenticapps-workflow/skill/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-bootstrap/00-CONTEXT.md
@.planning/phases/00-bootstrap/00-RESEARCH.md
@.planning/phases/00-bootstrap/00-VALIDATION.md
@docs/spec/dashboard-prompt.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contracts this plan creates that downstream Phase 0 plans (02–04) consume. -->

```typescript
// packages/shared/src/schemas/health.ts
import { z } from 'zod'

export const HealthResponseSchema = z.object({
  ok: z.boolean(),
  version: z.string(),
  message: z.string().optional(),
})

export type HealthResponse = z.infer<typeof HealthResponseSchema>
```

```typescript
// packages/shared/src/index.ts
export { HealthResponseSchema } from './schemas/health.js'
export type { HealthResponse } from './schemas/health.js'
```

```yaml
# pnpm-workspace.yaml
packages:
  - 'packages/*'

catalog:
  zod: ^3.24.0
  typescript: ^6.0.0
  vitest: ^4.1.5
  '@vitest/coverage-v8': ^4.1.5
  eslint: ^10.3.0
  typescript-eslint: ^8.59.1
  prettier: ^3.8.3
  'eslint-config-prettier': ^10.1.8
  'eslint-plugin-import': ^2.32.0
```

Package names (locked):
- `@agenticapps/dashboard-shared` — `private: true`
- `@agenticapps/dashboard-agent` — publishable (Plan 03 fills, Plan 05 publishes)
- `@agenticapps/dashboard-spa` — `private: true`

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace skeleton + root config + ESLint/Prettier</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    tsconfig.base.json,
    .nvmrc,
    .gitattributes,
    .editorconfig,
    .gitignore,
    eslint.config.mjs,
    prettier.config.mjs,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts,
    packages/agent/package.json,
    packages/agent/tsconfig.json,
    packages/agent/src/index.ts,
    packages/spa/package.json,
    packages/spa/tsconfig.json,
    packages/spa/src/index.ts
  </files>
  <read_first>
    - .planning/phases/00-bootstrap/00-RESEARCH.md (§"Standard Stack", §"Architecture Patterns" Patterns 1, 6, 9; §"Code Examples" — Root package.json scripts; §"Common Pitfalls" Pitfalls 2 + 4)
    - .planning/phases/00-bootstrap/00-CONTEXT.md (D-01..D-05, D-13..D-15)
    - docs/spec/dashboard-prompt.md (§"Tech stack", §"Repo")
    - CLAUDE.md (hard architectural constraints; current repo state — confirm `package.json` does NOT yet exist)
    - .gitignore (current contents — extend, do not overwrite generic patterns)
  </read_first>
  <behavior>
    - Test 1 (manual on completion via Task 3 verify): `pnpm install --frozen-lockfile` exits 0 from a clean clone (lockfile committed in this task).
    - Test 2: `pnpm lint` exits 0 with zero warnings on the placeholder index.ts files.
    - Test 3: `pnpm typecheck` exits 0 — every package's tsconfig.json extends ../../tsconfig.base.json with strict: true.
    - Test 4: `pnpm build` exits 0 — root delegates via `pnpm -r build`; placeholder packages have a no-op build script that exits 0.
    - Test 5: `node -e "require('./package.json').engines.node"` prints `>=20.0.0`.
  </behavior>
  <action>
Create the entire workspace skeleton in one task. NOTE: this is config + skeleton, but it MUST be reproducible — every file goes in with concrete contents below. NO placeholder TODO comments in committed files.

**1. `pnpm-workspace.yaml`** (per RESEARCH §Pattern 1) — **FULL catalog for the entire phase**. Plans 02 and 03 will not modify this file; they reference these catalog entries directly:
```yaml
packages:
  - 'packages/*'

catalog:
  # Cross-cutting (used by ≥2 packages)
  zod: ^3.24.0
  typescript: ^6.0.0
  vitest: ^4.1.5
  '@vitest/coverage-v8': ^4.1.5
  # Lint + format
  eslint: ^10.3.0
  typescript-eslint: ^8.59.1
  prettier: ^3.8.3
  eslint-config-prettier: ^10.1.8
  eslint-plugin-import: ^2.32.0
  eslint-plugin-react: ^7.37.5
  eslint-plugin-react-hooks: ^7.1.1
  # SPA stack (consumed by Plan 03)
  react: ^18.3.1
  react-dom: ^18.3.1
  '@types/react': ^19.2.14
  '@types/react-dom': ^19.2.3
  vite: ^8.0.10
  '@vitejs/plugin-react': ^6.0.1
  tailwindcss: ^4.2.4
  '@tailwindcss/vite': ^4.2.4
  '@tanstack/react-query': ^5.100.8
  'lucide-react': ^1.14.0
  jsdom: ^29.1.1
  '@testing-library/react': ^16.3.2
  '@testing-library/jest-dom': ^6.4.0
  # Agent stack (consumed by Plan 02)
  commander: ^14.0.0
  tsx: ^4.20.0
```

Rationale: shipping the FULL catalog in Plan 01 makes Plans 02 and 03 parallel-safe. They reference `catalog:` specifiers directly without touching `pnpm-workspace.yaml`, eliminating the same-wave file-conflict that would force them into different waves.

**2. Root `package.json`** (per RESEARCH §"Code Examples"):
```json
{
  "name": "agenticapps-dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.5.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "typescript-eslint": "catalog:",
    "eslint-config-prettier": "catalog:",
    "eslint-plugin-import": "catalog:",
    "prettier": "catalog:"
  }
}
```

**3. `tsconfig.base.json`** (per RESEARCH §Pattern 6):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  }
}
```

**4. `.nvmrc`** — single line `20` (no shebang, no comments).

**5. `.editorconfig`**:
```
root = true

[*]
end_of_line = lf
charset = utf-8
indent_style = space
indent_size = 2
trim_trailing_whitespace = true
insert_final_newline = true

[*.md]
trim_trailing_whitespace = false
```

**6. `.gitattributes`**:
```
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
pnpm-lock.yaml linguist-generated=true
```

**7. `.gitignore`** — preserve any existing entries, add (idempotent — only append missing lines):
```
node_modules
dist
.turbo
.cache
*.tsbuildinfo
.DS_Store
coverage
.vite
```

**8. `prettier.config.mjs`**:
```js
/** @type {import('prettier').Config} */
export default {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  arrowParens: 'always',
}
```

**9. `eslint.config.mjs`** (per RESEARCH §Pattern 9) — **FULL config including SPA React glob and agent CLI overrides**. Plans 02 and 03 will not modify this file; their globs already match here. The SPA glob simply matches no files until Plan 03 lands; the agent glob matches no files until Plan 02 lands. Both lint clean during Plan 01.

Add `eslint-plugin-react` and `eslint-plugin-react-hooks` to the root `package.json` `devDependencies` (alongside the existing entries in step 2):
```json
"eslint-plugin-react": "catalog:",
"eslint-plugin-react-hooks": "catalog:"
```

Final `eslint.config.mjs`:
```js
import tseslint from 'typescript-eslint'
import eslintConfigPrettier from 'eslint-config-prettier'
import pluginImport from 'eslint-plugin-import'
import pluginReact from 'eslint-plugin-react'
import pluginReactHooks from 'eslint-plugin-react-hooks'

export default tseslint.config(
  {
    ignores: ['**/node_modules/**', '**/dist/**', '**/coverage/**', '**/.vite/**'],
  },
  ...tseslint.configs.recommended,
  {
    plugins: { import: pluginImport },
    rules: {
      'import/order': ['warn', { 'newlines-between': 'always' }],
      'import/no-duplicates': 'error',
    },
  },
  // SPA React rules (Plan 03 fills out the matching files)
  {
    files: ['packages/spa/**/*.{ts,tsx}'],
    plugins: {
      react: pluginReact,
      'react-hooks': pluginReactHooks,
    },
    settings: { react: { version: 'detect' } },
    rules: {
      ...pluginReact.configs.recommended.rules,
      ...pluginReactHooks.configs.recommended.rules,
      'react/react-in-jsx-scope': 'off',
      'react/jsx-uses-react': 'off',
    },
  },
  // Agent CLI overrides — process.exit + console.log are intentional in CLI binaries (Plan 02 fills out the matching files)
  {
    files: ['packages/agent/src/**/*.ts'],
    rules: {
      'no-console': 'off',
      'no-process-exit': 'off',
    },
  },
  // Prettier MUST be last — disables conflicting rules
  eslintConfigPrettier,
)
```

Rationale: shipping the full ESLint config (including not-yet-matched globs) in Plan 01 makes Plans 02 and 03 parallel-safe. The empty globs are inert until those plans add code; lint passes trivially in Plan 01 with no .tsx files and no agent CLI source.

**10. Per-package skeletons** — for each of `packages/{shared,agent,spa}`:

`packages/shared/package.json`:
```json
{
  "name": "@agenticapps/dashboard-shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'shared: source-only, no build' && exit 0"
  },
  "dependencies": {
    "zod": "catalog:"
  }
}
```

`packages/shared/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "noEmit": true
  },
  "include": ["src"]
}
```

`packages/shared/src/index.ts` — empty placeholder for now (Task 2 fills it):
```ts
export {}
```

`packages/agent/package.json`:
```json
{
  "name": "@agenticapps/dashboard-agent",
  "version": "0.0.1-alpha.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'agent: build added in Plan 03' && exit 0"
  },
  "dependencies": {
    "@agenticapps/dashboard-shared": "workspace:*"
  }
}
```
(Note: `private: true` for now; Plan 05 flips to `false` AFTER `bin` and `files` fields are wired in Plan 03.)

`packages/agent/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "Node16",
    "moduleResolution": "Node16",
    "lib": ["ES2022"],
    "outDir": "./dist"
  },
  "include": ["src"]
}
```

`packages/agent/src/index.ts`:
```ts
export {}
```

`packages/spa/package.json`:
```json
{
  "name": "@agenticapps/dashboard-spa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'spa: build added in Plan 04' && exit 0"
  },
  "dependencies": {
    "@agenticapps/dashboard-shared": "workspace:*"
  }
}
```

`packages/spa/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true
  },
  "include": ["src"]
}
```

`packages/spa/src/index.ts`:
```ts
export {}
```

**11. Run `pnpm install`** to generate `pnpm-lock.yaml`. Stage the lockfile.

**Per D-04 + RESEARCH Pitfall 2:** the lockfile MUST be committed; CI uses `--frozen-lockfile`.
**Per D-15:** the executor MUST emit the Workflow commitment block before any tool call (covered by execute-plan workflow, but verify it happens).
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps-dashboard && pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `pnpm-workspace.yaml` AND `grep -q "^catalog:" pnpm-workspace.yaml`
    - File exists: `package.json` AND `node -p "require('./package.json').engines.node" | grep -q ">=20.0.0"`
    - File exists: `package.json` AND `node -p "require('./package.json').type" | grep -q "module"`
    - File exists: `tsconfig.base.json` AND `grep -q "\"strict\": true" tsconfig.base.json`
    - File contents: `cat .nvmrc` outputs exactly `20\n`
    - File exists: `.gitattributes` AND `grep -q "eol=lf" .gitattributes`
    - File exists: `.editorconfig` AND `grep -q "indent_size = 2" .editorconfig`
    - File exists: `eslint.config.mjs` AND `grep -q "typescript-eslint" eslint.config.mjs`
    - File exists: `prettier.config.mjs` AND `grep -q "singleQuote: true" prettier.config.mjs`
    - All three package.json files exist with correct names: `node -p "require('./packages/shared/package.json').name"` outputs `@agenticapps/dashboard-shared` (and same pattern for agent + spa).
    - Each package.json declares `"type": "module"`.
    - `packages/agent/package.json` and `packages/spa/package.json` both contain `"@agenticapps/dashboard-shared": "workspace:*"` (verify with `grep`).
    - `pnpm-lock.yaml` exists at repo root.
    - Command `pnpm install --frozen-lockfile` exits 0.
    - Command `pnpm lint` exits 0 with no errors and no warnings (placeholders are empty `export {}` files, must lint clean).
    - Command `pnpm typecheck` exits 0.
    - Command `pnpm build` exits 0 (placeholder builds echo + exit 0).
  </acceptance_criteria>
  <done>
Workspace skeleton committed; `pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm build` all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HealthResponseSchema in shared + Vitest workspace wiring</name>
  <files>
    vitest.config.ts,
    packages/shared/vitest.config.ts,
    packages/agent/vitest.config.ts,
    packages/spa/vitest.config.ts,
    packages/shared/src/schemas/health.ts,
    packages/shared/src/schemas/health.test.ts,
    packages/shared/src/index.ts,
    packages/agent/src/index.test.ts,
    packages/spa/src/index.test.ts,
    packages/spa/package.json,
    packages/agent/package.json,
    packages/shared/package.json
  </files>
  <read_first>
    - .planning/phases/00-bootstrap/00-RESEARCH.md (§Pattern 3 — Vitest test.projects; §"`packages/shared` Export Strategy"; §"Code Examples" — HealthResponseSchema smoke test; §Pitfall 4)
    - .planning/phases/00-bootstrap/00-CONTEXT.md (D-02, D-06)
    - .planning/phases/00-bootstrap/00-VALIDATION.md (Wave 0 Requirements section)
    - packages/shared/src/index.ts (current placeholder — replace)
    - packages/shared/package.json (Task 1 output — preserve, add devDependencies)
    - Existing `vitest.config.ts` (none yet — confirm before creating)
  </read_first>
  <behavior>
    RED commit (single batch acceptable since this is Wave 0 / scaffolding — but each test must initially fail):
    - Test 1 (`packages/shared/src/schemas/health.test.ts`): `HealthResponseSchema.parse({ ok: true, version: '0.0.1-alpha.0' })` does NOT throw.
    - Test 2 (same file): `HealthResponseSchema.parse({ version: '0.0.1' })` (missing `ok`) DOES throw with a Zod error.
    - Test 3 (same file): `HealthResponseSchema.parse({ ok: true, version: '1.0.0', message: 'hi' })` does NOT throw and the parsed `message` equals `'hi'`.
    - Test 4 (`packages/agent/src/index.test.ts`): can import `HealthResponseSchema` from `@agenticapps/dashboard-shared` and parse a valid payload (proves agent → shared workspace resolution).
    - Test 5 (`packages/spa/src/index.test.ts`): same as Test 4 but in SPA (jsdom env, proves Vite/Vitest resolves `.ts` source from shared).

    GREEN commit: implement `HealthResponseSchema` in `packages/shared/src/schemas/health.ts`; export from `packages/shared/src/index.ts`; tests pass.
  </behavior>
  <action>
**1. Add Vitest devDependencies to per-package package.json files** (D-02, RESEARCH §Standard Stack):

`packages/shared/package.json` — add `devDependencies`:
```json
"devDependencies": {
  "typescript": "catalog:",
  "vitest": "catalog:"
}
```
Replace existing `scripts.test` (if any) with `"test": "vitest run"`.

`packages/agent/package.json` — add:
```json
"devDependencies": {
  "typescript": "catalog:",
  "vitest": "catalog:"
}
```
Add `"test": "vitest run"` to scripts.

`packages/spa/package.json` — add:
```json
"devDependencies": {
  "typescript": "catalog:",
  "vitest": "catalog:",
  "jsdom": "catalog:"
}
```
Add `"test": "vitest run"` to scripts.

**2. Root `vitest.config.ts`** (per RESEARCH §Pattern 3):
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    projects: ['packages/*'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      include: ['packages/*/src/**'],
      exclude: ['packages/*/src/**/*.test.{ts,tsx}'],
    },
  },
})
```

**3. Per-package `vitest.config.ts` — every project MUST have a unique name (RESEARCH §Pitfall 4):**

`packages/shared/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: 'shared',
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

`packages/agent/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: 'agent',
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

`packages/spa/vitest.config.ts` (jsdom for SPA tests; full Vite + Tailwind plugin wiring is added in Plan 04 — for now Vitest alone is sufficient):
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: 'spa',
    environment: 'jsdom',
    include: ['src/**/*.test.{ts,tsx}'],
  },
})
```

**4. RED step — write tests first.**

`packages/shared/src/schemas/health.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { HealthResponseSchema } from '../index.js'

describe('HealthResponseSchema', () => {
  it('accepts a valid health response', () => {
    const valid = { ok: true, version: '0.0.1-alpha.0' }
    expect(() => HealthResponseSchema.parse(valid)).not.toThrow()
  })

  it('rejects missing ok field', () => {
    expect(() => HealthResponseSchema.parse({ version: '0.0.1' })).toThrow()
  })

  it('accepts optional message', () => {
    const valid = { ok: true, version: '1.0.0', message: 'hi' }
    const parsed = HealthResponseSchema.parse(valid)
    expect(parsed.message).toBe('hi')
  })
})
```

`packages/agent/src/index.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { HealthResponseSchema } from '@agenticapps/dashboard-shared'

describe('agent → shared workspace resolution', () => {
  it('imports HealthResponseSchema from shared package', () => {
    const result = HealthResponseSchema.parse({ ok: true, version: '0.0.1-alpha.0' })
    expect(result.ok).toBe(true)
  })
})
```

`packages/spa/src/index.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { HealthResponseSchema } from '@agenticapps/dashboard-shared'

describe('spa → shared workspace resolution (jsdom)', () => {
  it('imports HealthResponseSchema from shared package', () => {
    const result = HealthResponseSchema.parse({ ok: true, version: '0.0.1-alpha.0' })
    expect(result.ok).toBe(true)
  })
})
```

Run `pnpm test --run` — MUST fail (no `HealthResponseSchema` export yet). Commit as:
`test(00-01): add HealthResponseSchema + workspace-resolution tests`

**5. GREEN step — implement.**

`packages/shared/src/schemas/health.ts`:
```typescript
import { z } from 'zod'

export const HealthResponseSchema = z.object({
  ok: z.boolean(),
  version: z.string(),
  message: z.string().optional(),
})

export type HealthResponse = z.infer<typeof HealthResponseSchema>
```

`packages/shared/src/index.ts`:
```typescript
export { HealthResponseSchema } from './schemas/health.js'
export type { HealthResponse } from './schemas/health.js'
```

Run `pnpm install` (catalog updates require regenerating lockfile — confirm + commit lockfile changes).
Run `pnpm test --run` — MUST pass for all three projects (shared, agent, spa). Commit as:
`feat(00-01): implement HealthResponseSchema in @agenticapps/dashboard-shared`

**Key constraints:**
- `import ... from '../index.js'` (with `.js` extension) is correct for `moduleResolution: Node16` even when the source is `.ts`. TypeScript rewrites these on emit.
- `import ... from '@agenticapps/dashboard-shared'` (no path, no extension) works because shared's `exports` field maps `.` to `./src/index.ts` directly.
- Per A1 in RESEARCH assumptions log: this exercises the source-only shared pattern. If it fails, surface immediately — falling back means adding a build step in shared (not in scope for Phase 0).
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps-dashboard && pnpm install --frozen-lockfile && pnpm test --run</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `vitest.config.ts` AND `grep -q "projects:" vitest.config.ts`
    - All three per-package vitest.config.ts files exist AND each contains a unique `name:` value (`'shared'`, `'agent'`, `'spa'`).
    - File exists: `packages/shared/src/schemas/health.ts` AND `grep -q "export const HealthResponseSchema" packages/shared/src/schemas/health.ts` AND `grep -q "z.boolean()" packages/shared/src/schemas/health.ts`
    - File exists: `packages/shared/src/index.ts` AND `grep -q "export { HealthResponseSchema" packages/shared/src/index.ts`
    - File exists: `packages/shared/src/schemas/health.test.ts` AND contains all three describe blocks (rejects, accepts, message).
    - File exists: `packages/agent/src/index.test.ts` AND `grep -q "@agenticapps/dashboard-shared" packages/agent/src/index.test.ts`
    - File exists: `packages/spa/src/index.test.ts` AND `grep -q "@agenticapps/dashboard-shared" packages/spa/src/index.test.ts`
    - Command `pnpm test --run` exits 0 and reports tests for all three projects (shared, agent, spa) in the output.
    - Git log shows two commits: a `test(00-01):` commit AND a `feat(00-01):` commit (RED → GREEN).
    - Verifies via `git log --oneline -n 5 | grep -cE "^[a-f0-9]+ (test|feat)\(00-01\)"` returns ≥ 2.
    - Command `pnpm typecheck` still exits 0.
  </acceptance_criteria>
  <done>
HealthResponseSchema exists in shared, parses correctly, and is importable from both agent and spa via `workspace:*`. RED→GREEN commits in git history.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GitHub Actions CI workflow (push + pull_request)</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .planning/phases/00-bootstrap/00-RESEARCH.md (§Pattern 7 — GitHub Actions CI Workflow; §"Standard Stack" pnpm/action-setup version)
    - .planning/phases/00-bootstrap/00-CONTEXT.md (D-10)
    - .planning/REQUIREMENTS.md (BOOT-02 success criterion)
    - package.json (Task 1 output — confirms `lint`/`typecheck`/`test`/`build` scripts exist)
    - Any existing `.github/workflows/` directory contents (none yet — confirm)
  </read_first>
  <behavior>
    Test (verified by pushing the branch in Phase 0 verify-work, NOT in this task — unit-testable behavior is YAML structure):
    - `actionlint` (offline check) parses the YAML without errors.
    - YAML contains all five gates: install, lint, typecheck, test, build.
    - YAML triggers on both `push` and `pull_request`.
    - YAML uses `setup-node` with `node-version-file: '.nvmrc'`.
    - YAML uses `pnpm/action-setup@v6`.
  </behavior>
  <action>
Create `.github/workflows/ci.yml` exactly per RESEARCH §Pattern 7. Use `pnpm/action-setup@v6`, `actions/setup-node@v4`, `actions/checkout@v6` (see RESEARCH A4 — fall back to `@v4` only if v6 fails at execution time).

```yaml
name: CI

on:
  push:
    branches: [main, 'feat/**', 'fix/**', 'chore/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js (from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck

      - name: Test
        run: pnpm test --run

      - name: Build
        run: pnpm build
```

Notes:
- D-10 specifies a single sequential job; do not split.
- `permissions: contents: read` is the principle-of-least-privilege baseline (T-00-02 mitigation — see threat_model). Release workflow (Plan 05) needs `id-token: write`; this one does not.
- `--frozen-lockfile` is REQUIRED — RESEARCH Pitfall 2.
- `pnpm test --run` (not `pnpm test`) — ensures non-watch mode in CI per VALIDATION.md sampling rate rule.

Optional (don't add yet): if the user later wants `actionlint` validation, add a separate workflow in Phase 6 polish.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps-dashboard && test -f .github/workflows/ci.yml && grep -q "pnpm/action-setup@v6" .github/workflows/ci.yml && grep -q "actions/setup-node@v4" .github/workflows/ci.yml && grep -q "node-version-file: '.nvmrc'" .github/workflows/ci.yml && grep -q "pnpm install --frozen-lockfile" .github/workflows/ci.yml && grep -q "pnpm lint" .github/workflows/ci.yml && grep -q "pnpm typecheck" .github/workflows/ci.yml && grep -q "pnpm test --run" .github/workflows/ci.yml && grep -q "pnpm build" .github/workflows/ci.yml && grep -E "^on:" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.github/workflows/ci.yml`
    - File contains `pnpm/action-setup@v6` (verbatim).
    - File contains `actions/setup-node@v4` (verbatim).
    - File contains `actions/checkout@v6` (verbatim).
    - File contains `node-version-file: '.nvmrc'` (verbatim).
    - File contains `pnpm install --frozen-lockfile`.
    - File contains all five gate commands: `pnpm lint`, `pnpm typecheck`, `pnpm test --run`, `pnpm build`.
    - File triggers on both `push` (main + feat/** branches) AND `pull_request` to main — verified by `grep -A1 "^on:" .github/workflows/ci.yml | grep -E "push|pull_request"`.
    - File contains `permissions: contents: read`.
    - YAML is parseable (no syntax errors): `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (Python ships on macOS; alternative: `node -e "require('js-yaml')"` IF js-yaml is in node_modules; if neither, fall back to manual visual confirmation in execute-plan).
    - Manual verification (deferred to phase verify-work, NOT this task): pushing this branch triggers a CI run that goes green.
  </acceptance_criteria>
  <done>
`.github/workflows/ci.yml` committed; the file references the exact action versions specified in RESEARCH; all five gates and both triggers are present.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local checkout → npm registry | pnpm install pulls dependencies from public registry; supply-chain risk |
| Repo → GitHub Actions runner | Workflow YAML is the source of truth for what runs in CI; injection / over-privileged tokens |
| Repo `~/.agenticapps/dashboard/` (NOT IN SCOPE) | Phase 0 has no daemon; INV-02 honored vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | T (Tampering) | npm dependency tree (catalog deps + lockfile) | mitigate | (a) Use `--frozen-lockfile` in CI so transitive resolution cannot drift; (b) commit `pnpm-lock.yaml`; (c) pin catalog versions to `^X.Y.Z` (no `*`); (d) Plan 05 will add `--provenance` for the agent's outbound publish. |
| T-00-02 | E (Elevation of Privilege) | GitHub Actions `ci.yml` token scope | mitigate | Set `permissions: contents: read` at the workflow level. CI does not need write tokens; release workflow (Plan 05) gets `id-token: write` only. |
| T-00-03 | I (Information Disclosure) | Public repo settings (currently private — D-13 deferral) | accept | Repo is private until Phase 6 per D-13. No secrets in committed files. Re-evaluate at Phase 8 public flip. |

</threat_model>

<verification>
- `pnpm install --frozen-lockfile` from clean checkout exits 0.
- `pnpm lint && pnpm typecheck && pnpm test --run && pnpm build` all exit 0 locally.
- Pushing the branch triggers `ci.yml` and the run goes green (verified at phase verify-work, not in-task).
- HealthResponseSchema is shared and verifiable from all three packages via Vitest.
- Git log contains both `test(00-01):` and `feat(00-01):` commits in Task 2 (TDD evidence).
</verification>

<success_criteria>
1. Fresh `git clone` + `pnpm install --frozen-lockfile` resolves all three packages with no errors. (BOOT-01)
2. `pnpm lint && pnpm typecheck && pnpm test --run && pnpm build` exits 0 from a clean state. (BOOT-01, BOOT-02)
3. `.github/workflows/ci.yml` runs on push + PR with all five gates. (BOOT-02)
4. `HealthResponseSchema` is consumed by both placeholder agent and SPA via `workspace:*` import — proves the cross-package contract end-to-end. (D-06, BOOT-01)
5. RED→GREEN commit pair exists in git for the schema work. (D-15 / TDD discipline)
</success_criteria>

<output>
After completion, create `.planning/phases/00-bootstrap/00-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. Include:
- Commit SHAs for the RED + GREEN pair in Task 2.
- Output of `pnpm test --run` (test counts per project).
- Confirmation that `pnpm install --frozen-lockfile` exits 0 from a clean tree (re-run after wiping node_modules).
- Note: CI green-status verification is deferred to phase verify-work step; this plan only proves local + YAML correctness.
</output>
