---
phase: 13-gitnexus-scoped-scan-actions-coverage-matrix
plan: 04
type: execute
wave: 4
depends_on: [00, 01, 02, 03]
files_modified:
  - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md
  - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md
  - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md
  - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md
  - .planning/REQUIREMENTS.md  # tick off implementation behaviours; Phase 13 has no REQ-IDs
autonomous: false
requirements: []
tags: [gates, cso, review, qa, impeccable, calibration]
must_haves:
  truths:
    - "/cso audit on the new subprocess-exec surface (daemon route + spawn + ~/.gitnexus/ write-boundary carve-out) produced a written audit at 13-CSO.md with 0 unresolved HIGH findings"
    - "Stage 1 /review on the phase diff produced findings; any HIGH severity findings are addressed before Stage 2"
    - "Stage 2 superpowers:requesting-code-review (independent reviewer) produced an independent assessment captured in 13-REVIEW.md"
    - "/qa walkthrough at 1440×900 covered the 6 scenarios from 13-VALIDATION.md §Manual-Only Verifications; results captured in 13-UAT.md"
    - "impeccable:critique skill ran on /coverage at 1440×900 producing 13-IMPECCABLE.md with composite + per-heuristic scores + findings + persona red flags; composite ≥ 87 (D-10.5-03 floor — calibration data point #6)"
    - "pnpm -r typecheck, pnpm -r test, pnpm --filter @agenticapps/dashboard-spa build all green on HEAD"
  artifacts:
    - path: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md"
      provides: "Composite + per-heuristic scores + findings + persona red flags from impeccable:critique"
      contains: "Composite"
    - path: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md"
      provides: "Stage 1 (gstack /review) + Stage 2 (superpowers:requesting-code-review) findings; explicit two-stage section headers"
      contains: "Stage 1"
    - path: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md"
      provides: "/cso audit on the new subprocess-exec surface + ~/.gitnexus carve-out + bind-mode refusal"
      contains: "subprocess"
    - path: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md"
      provides: "/qa walkthrough results covering the 6 manual-only scenarios from 13-VALIDATION.md"
      contains: "Scenario"
  key_links:
    - from: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md"
      to: ".planning/phases/DASH-10.5-impeccable-skill-driven-gate/10.5-DECISIONS.md"
      via: "composite ≥ 87 floor (D-10.5-03) — calibration data point #6"
      pattern: "calibration"
    - from: ".planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md"
      to: "packages/agent/src/routes/gitnexusScan.ts + packages/agent/src/lib/gitnexusScan.ts"
      via: "audit subject — subprocess spawn + bindMode refusal + ~/.gitnexus/ carve-out"
      pattern: "gitnexusScan"
---

<objective>
Run all Phase 13 closing gates. This plan is **autonomous: false** because every step is a human-verified checkpoint or a skill invocation that requires running infrastructure (dev server, real browser, fresh-context review session).

Five gates in sequential order — none can be skipped, none can be collapsed:

1. **`/cso` audit** — REQUIRED per CLAUDE.md (new subprocess-exec surface + new daemon route + explicit `~/.gitnexus/` write-boundary carve-out). The audit captures the 9 STRIDE patterns from 13-RESEARCH.md "Known Threat Patterns" + verifies the global scan-serialisation lock + verifies error responses do not leak stderr/file paths.

2. **Stage 1 `/review`** (gstack) on the phase diff — surfaces in-tree style, conventions, dead code, contract drift. Findings written to 13-REVIEW.md "Stage 1" section.

3. **Stage 2 `superpowers:requesting-code-review`** — independent reviewer in a fresh context window. CRITICAL: do NOT collapse with Stage 1. Findings written to 13-REVIEW.md "Stage 2" section.

4. **`/qa` walkthrough** — manual UAT covering the 6 scenarios from 13-VALIDATION.md §"Manual-Only Verifications" — scan one repo, scan a family, bind to tailscale and verify Scan pills disable with tooltip, rename gitnexus binary aside and verify InstallGitNexusButton fallback, double-click same row and verify 409 toast, use stub-gitnexus-failing.sh and verify partial-success toast.

5. **`impeccable:critique` skill** on `/coverage` at 1440×900 → `13-IMPECCABLE.md`. Composite ≥ 87 (D-10.5-03 floor). Calibration data point #6 for the floor recalibration policy.

Purpose: close the phase against every gate spelled out in CLAUDE.md + ROADMAP §"Phase 13 Sub-tracks > Gates".
Output: 4 gate artifacts + REQUIREMENTS.md acceptance-proxy tick-off + a phase-ready-to-merge state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CONTEXT.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-RESEARCH.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-VALIDATION.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-00-PLAN.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-01-PLAN.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-02-PLAN.md
@.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-03-PLAN.md
@.planning/phases/DASH-10.5-impeccable-skill-driven-gate/10.5-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /cso audit on the new subprocess-exec surface and write 13-CSO.md</name>
  <files>.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md</files>
  <read_first>
    - packages/agent/src/routes/gitnexusScan.ts (the new route — primary audit target)
    - packages/agent/src/lib/gitnexusScan.ts (job registry + global lock + spawn orchestration)
    - packages/agent/src/lib/gitnexusFamilyScan.ts (sequential family orchestrator)
    - packages/agent/src/__tests__/gitnexusScan.integration.test.ts (the integration tests that already exercise threat-model invariants)
    - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-RESEARCH.md §"Security Domain" + §"Known Threat Patterns" (9-row table)
    - .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-02-PLAN.md §"threat_model" (9-entry register)
    - CLAUDE.md (hard architectural constraints — re-read the "daemon writes confined to ~/.agenticapps/dashboard/" carve-out language)
  </read_first>
  <action>
    1. Invoke the `/cso` slash command (or `cso` skill) against the Phase 13 changeset. Scope: the new daemon route + lib modules. The auditor should review against ASVS L1 + the project's standard STRIDE checklist.
    2. The audit MUST explicitly cover:
       - **The `~/.gitnexus/` write-boundary carve-out**: CLAUDE.md says "Daemon writes confined to `~/.agenticapps/dashboard/`". `~/.gitnexus/` is outside that. The daemon does NOT write there — the spawned `gitnexus` subprocess does. Auditor confirms this distinction is sound and that the global scan-serialisation lock (D-13-EXT-01) adequately mitigates the verified gitnexus@1.6.4 read-modify-write race on `registry.json`.
       - **bindMode refusal precedence**: 403 BIND_REFUSED is returned BEFORE rate-limit and BEFORE body parse. Auditor confirms this minimises attack surface.
       - **Argument injection structural impossibility**: execa argv-array form via `spawnGitNexusAnalyze(repoAbsPath)`. The repo path comes from the registry, never from POST body. The POST body's `target` is Zod-regex-validated.
       - **Stderr leak invariant**: GET response carries only `{state, error?:{code, message}}` where `message` is mapped from a fixed enum. Integration test asserts the daemon does not echo back probe strings from STUB_GITNEXUS_STDERR.
       - **Rate limit + per-repo lock + global lock composition**: bounds CPU/disk/memory blast radius.
       - **Scan-id entropy**: 122 bits via `node:crypto.randomUUID()`. Bearer-auth still required.
    3. Write `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md` with the following sections:
       - **Audit summary** — pass/fail per category (Authentication, Authorization, Input Validation, Cryptography, Subprocess Execution, Error Handling, Logging, Concurrency)
       - **Threats reviewed** — table cross-referencing the 9 patterns from 13-RESEARCH.md "Known Threat Patterns" with verified mitigations + the carve-out
       - **Findings** — `<finding severity="...">` blocks (severity ∈ critical/high/medium/low/info). Each finding includes file:line + suggested mitigation.
       - **Disposition decisions** — for any "accept" disposition, record explicit rationale (especially the `~/.gitnexus/` carve-out)
       - **Verification commands** — copy-pasteable commands the user can run to spot-check (e.g. `pnpm --filter @agenticapps/dashboard-agent test -- gitnexusScan.integration --run`)
    4. If any HIGH or CRITICAL finding surfaces, STOP and surface to the user — do not proceed to Stage 1 review until HIGH+ findings are mitigated or explicitly accepted (with documented rationale).
    5. Commit: `docs(13-04): /cso audit on gitnexus scan surface`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md && grep -q "subprocess" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md && grep -q "~/.gitnexus" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md && grep -q "globalScanLock\\|global scan" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-CSO.md</automated>
  </verify>
  <done>
    - `13-CSO.md` exists and is committed to git
    - The file contains explicit coverage of: subprocess exec, `~/.gitnexus/` carve-out, global scan lock, bindMode refusal precedence, argument-injection impossibility, stderr leak invariant
    - The audit contains at least one `<finding ...>` block (even if severity=info — proves the auditor walked the surface)
    - Zero unresolved CRITICAL or HIGH findings (or, if present, each has explicit accept-with-rationale disposition)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Stage 1 /review (gstack) on the phase diff and capture findings</name>
  <files>.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md</files>
  <read_first>
    - git diff main...HEAD (or main..feat/phase-13-gitnexus-scan branch if applicable) — full phase changeset
    - all four prior plans (00/01/02/03 SUMMARY.md files if they exist)
    - .planning/phases/DASH-11.2-impeccable-p2-polish-bundle/11.2-REVIEW.md (recent Stage-1+2 structure precedent — copy the section layout)
  </read_first>
  <action>
    1. Invoke `/review` (gstack) on the phase diff. Scope: all files modified/added/deleted across Plans 00, 01, 02, 03.
    2. The review should surface:
       - Style/convention violations (matching the project's eslint + prettier config)
       - Dead code / unused exports
       - Contract drift (especially between shared schemas and daemon + SPA consumers)
       - Test gaps (any production code path lacking a test)
       - Subtle bugs (off-by-one, race conditions, missing await, mis-typed enums)
       - Documentation completeness (JSDoc on public exports, file-header purpose comments)
    3. **Output**: open `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md` and write the **Stage 1** section ONLY. Use this structure:
       ```markdown
       # Phase 13 Code Review

       **Branch:** feat/phase-13-... (or current HEAD)
       **Reviewers:** Stage 1 = /review (gstack); Stage 2 = superpowers:requesting-code-review (independent, fresh context)

       ---

       ## Stage 1 — gstack /review

       **Date:** {YYYY-MM-DD}
       **Diff scope:** {N} files changed, +{M} -{K} lines

       ### Findings

       <finding severity="high|medium|low|info" id="S1-01" file="path/to/file.ts" lines="LL-LL">
       {description, suggested fix}
       </finding>

       ### Disposition

       | Finding | Status | Action |
       |---------|--------|--------|
       | S1-01 | addressed | commit {sha} |
       | S1-02 | accepted-with-rationale | {reason} |

       ---

       ## Stage 2 — superpowers:requesting-code-review

       *To be filled in Task 3 — DO NOT collapse with Stage 1.*
       ```
    4. **Address any HIGH severity findings** before moving on. Either fix them (and record the commit SHA in the Disposition table) or accept-with-rationale (and document why deferring is safe).
    5. Commit: `docs(13-04): Stage 1 /review findings`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md && grep -q "## Stage 1" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md && grep -q "<finding severity=" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md</automated>
  </verify>
  <done>
    - `13-REVIEW.md` exists with a "## Stage 1" section
    - At least one `<finding ...>` block exists (even severity=info)
    - "## Stage 2" placeholder is present but NOT filled in
    - All HIGH severity findings have a Disposition row in the table
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Run Stage 2 superpowers:requesting-code-review in a FRESH context window</name>
  <what-built>
    The Stage 2 reviewer must run in an independent context — typically a new `/clear` then `superpowers:requesting-code-review` invocation. This is NOT something the executor agent should attempt in-line, because:
    1. The independence of Stage 2 is contractual (CLAUDE.md: "Two-stage review (gstack /review + superpowers:requesting-code-review) before merging any phase. Stages do not collapse.")
    2. Fresh context prevents the same agent from rubber-stamping its own implementation.

    The user runs Stage 2 themselves; this task captures the human handoff.
  </what-built>
  <files>.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md</files>
  <action>This is a checkpoint task — the executor agent pauses and waits for the user to perform the Stage 2 review in a fresh context. See <how-to-verify> for the full procedure. Do NOT collapse with Stage 1. Resume only when the resume-signal is given.</action>
  <verify>
    <automated>grep -q "## Stage 2" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md && grep -c "<finding severity=" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md | awk '$1 >= 2'</automated>
  </verify>
  <done>
    - 13-REVIEW.md has BOTH "## Stage 1" AND "## Stage 2" section headers
    - Stage 2 section contains at least one <finding> block
    - User has typed the resume-signal
  </done>
  <how-to-verify>
    1. Run `/clear` to drop all current context.
    2. Open a fresh session in the same project.
    3. Invoke `superpowers:requesting-code-review` against the Phase 13 diff (main...HEAD).
    4. The reviewer should produce independent findings — orthogonal observations, not Stage 1 re-runs.
    5. Append the findings to `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-REVIEW.md` under the "## Stage 2" section using the same `<finding>` block format.
    6. Address any HIGH severity findings before merging.
    7. Once 13-REVIEW.md has both Stage 1 AND Stage 2 sections populated, type `approved` to resume.
  </how-to-verify>
  <resume-signal>Type "approved" once Stage 2 findings are appended to 13-REVIEW.md and all HIGH severity findings are addressed. If Stage 2 found something blocking, type the issue summary and the executor will pause for remediation.</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: /qa walkthrough — execute the 6 scenarios from 13-VALIDATION.md §"Manual-Only Verifications"</name>
  <what-built>
    The full Phase 13 surface needs a real-browser, real-daemon manual walkthrough. The 6 scenarios from 13-VALIDATION.md must be exercised in order. This is the user's final functional check before merge.
  </what-built>
  <files>.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md</files>
  <action>This is a checkpoint task — the executor pauses while the user runs the /qa walkthrough manually against a live daemon + SPA dev server. See <how-to-verify> for the 6 scenarios. Resume on uat-pass.</action>
  <verify>
    <automated>test -f .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md && grep -cE "^(##|###)? *Scenario [1-6]" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md | awk '$1 >= 6'</automated>
  </verify>
  <done>
    - 13-UAT.md exists and is committed
    - All 6 scenarios documented with pass/fail + timestamp
    - User has typed "uat-pass" (or routed failed scenarios to gap closure)
  </done>
  <how-to-verify>
    Prerequisites:
    1. `pnpm --filter @agenticapps/dashboard-agent build` then `node packages/agent/dist/cli.js start --bind 127.0.0.1` — daemon up on loopback.
    2. `pnpm --filter @agenticapps/dashboard-spa dev` — SPA at `http://localhost:5174/`.
    3. Browser open at `http://localhost:5174/coverage` (must be paired).

    Scenarios (mark each in 13-UAT.md as pass/fail with timestamp):

    **Scenario 1 — Scan one repo (happy path):**
    - Pick a row in the GitNexus column showing the Scan pill (state: not-installed or installed-no-registry).
    - Click Scan. Expect: pill → spinner + "Scanning…" within 200ms.
    - Wait for completion (typically 10s-2min). Expect: success toast "Indexed {family}/{repo}", cell flips from ✗ → ✓.

    **Scenario 2 — Scan a family:**
    - Click the Scan family button in a family header.
    - Expect: button shows progress (e.g. "Scanning X/Y"); rows light up sequentially as each completes.
    - At completion: if all succeed, toast "Scanned N repos in {family}". If partial: toast "C/N scanned, F failed — retry failed?".

    **Scenario 3 — Tailscale bind refusal:**
    - Stop the daemon. Restart with `--bind tailscale` (or `--bind 0.0.0.0` if Tailscale unavailable locally).
    - Reload `/coverage`. Expect: every Scan pill (per-row AND per-family) is rendered disabled with hover/focus tooltip "Connect from the host device to scan".
    - Open devtools and POST manually to `/api/gitnexus/scan` with a valid bearer + body. Expect: 403 BIND_REFUSED.
    - Stop, restart with `--bind 127.0.0.1` before proceeding.

    **Scenario 4 — Binary missing fallback:**
    - Rename the actual `gitnexus` binary aside (e.g. `mv $(which gitnexus) $(which gitnexus).bak`).
    - Restart the daemon. Reload `/coverage`. Expect: per-row Scan pills are gone; per-family Scan button gone; existing InstallGitNexusButton family-hint visible (Phase 10.6 behaviour).
    - Restore the binary (`mv {path}.bak {path}`); restart daemon; verify Scan pills return.

    **Scenario 5 — Same-repo collision (409):**
    - Click Scan on a row that takes >2s. While the spinner is showing, open devtools console and POST a second `/api/gitnexus/scan` with the same body via fetch (or click Scan a second time on a re-rendered button somehow).
    - Expect: 409 SCAN_IN_FLIGHT with toast "A scan is already running for this repo".

    **Scenario 6 — Partial-success family scan:**
    - Set PATH to prepend the test-fixtures dir so `which gitnexus` resolves to `stub-gitnexus-failing.sh`. Set STUB_FAIL_ON_INVOCATION=2.
    - Restart daemon. Trigger a family scan (3+ repos).
    - Expect: family completes with toast "C/N scanned, F failed — retry failed?". Verify perRepoResults via daemon log shows error.code=SCAN_FAILED on the 2nd entry.

    Capture results to `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-UAT.md` with screenshots (or a written log) for each scenario.
  </how-to-verify>
  <resume-signal>Type "uat-pass" if all 6 scenarios pass. If any scenario fails, type "uat-fail" + scenario number + description; the executor will route to gap-closure.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 5: Run impeccable:critique skill on /coverage at 1440×900 → 13-IMPECCABLE.md (calibration point #6)</name>
  <what-built>
    Phase 13 ends with the canonical IMPECCABLE artifact at `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md`. This is the D-10.5-03 floor gate (composite ≥ 87) and the calibration data point #6 for the floor recalibration policy.
  </what-built>
  <files>.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md</files>
  <action>This is a checkpoint task — the executor pauses while the user runs the impeccable:critique skill against /coverage at 1440x900. See <how-to-verify> for the artifact requirements. Resume on impeccable-done.</action>
  <verify>
    <automated>test -f .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md && grep -qi "composite" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md && grep -qi "1440" .planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md</automated>
  </verify>
  <done>
    - 13-IMPECCABLE.md exists and is committed
    - Composite score + per-heuristic scores recorded
    - Viewport 1440x900 recorded in the artifact metadata
    - User has typed "impeccable-done"
  </done>
  <how-to-verify>
    1. Ensure the dev server is running (`pnpm --filter @agenticapps/dashboard-spa dev` at `localhost:5174`) and the daemon is bound to loopback so Scan affordances are enabled.
    2. Invoke the `impeccable:critique` skill against `/coverage` at viewport 1440×900.
    3. The skill produces a structured Markdown artifact with:
       - **Composite score** (0-100)
       - **Per-heuristic scores** — Typography / Color / Layout / Hierarchy / Affordance / Motion / Feedback / Empty states / Errors / Loading / Density / a11y / Microcopy / Brand / Performance
       - **Findings** — P1 / P2 / P3 severity blocks
       - **Persona red flags** — Director / Maker / Editor reactions
       - **Phase 13.x candidate carry-overs** — items deferred to a follow-up polish phase
    4. Write to `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-IMPECCABLE.md`.
    5. Per D-10.5-03: composite floor is ≥ 87. If the score is below 87:
       - Document the gap explicitly in the artifact
       - Check Phase 11/11.1/11.2/12 calibration data (composite scores in their IMPECCABLE artifacts) — if the score is in the empirical band 74-82 set by prior calibration points, this is data point #6 informing the recalibration ADR (D-10.5-03.calibration-2). Surface the result to the user; do NOT auto-fail the phase if the gap is consistent with prior calibration.
       - If clearly below the empirical band, propose a Phase 13.x polish bundle.
    6. The artifact MUST also note:
       - Calibration point # (this is #6)
       - Whether this data point supports the prior recalibration trend (Phase 10=74, 11=76, 11.1=82, 11.2=85-87 target, 12=?)
       - Any inherited P1s from prior phases that are still visible on the Coverage page surface
    7. Type `impeccable-done` once 13-IMPECCABLE.md is committed.
  </how-to-verify>
  <resume-signal>Type "impeccable-done" once 13-IMPECCABLE.md exists, is committed, and the composite score + per-heuristic scores + findings + persona red flags are populated.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Update REQUIREMENTS.md acceptance proxies + final regression sweep + commit phase closure</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (verify there is no "## Phase 13" section to update — phase has phase_req_ids=null. Add a brief implementation-behaviours note instead.)
    - .planning/STATE.md (verify the current status/last_activity fields; we'll update)
    - All 4 prior plan SUMMARYs (00/01/02/03) to confirm they exist
    - 13-IMPECCABLE.md, 13-REVIEW.md, 13-CSO.md, 13-UAT.md (from Tasks 1-5 of this plan)
  </read_first>
  <action>
    1. **Append a Phase 13 acceptance-proxy block to REQUIREMENTS.md** below the Phase 12 section. Since Phase 13 has no REQ-IDs (phase_req_ids: null), use the must_haves from 13-VALIDATION.md as the acceptance contract. Use this format:
       ```markdown
       ### GitNexus scoped scan actions (Phase 13)

       **Minted:** {date} during `/gsd-plan-phase 13`. Phase 13 has NO REQ-IDs (subtractive feature with implementation-behaviours as acceptance proxies — see 13-VALIDATION.md).

       Acceptance behaviours (proxies, no REQ-ID):
       - [ ] **B-13-01**: Page-header IndexGitNexusButton removed (D-13-06) — file deleted; zero references in packages/spa/src/
       - [ ] **B-13-02**: Per-family Scan button mounted in CoverageFamilySection header (gated on canScan)
       - [ ] **B-13-03**: Per-repo Scan pill replaces ✗ cell on not-installed / installed-no-registry rows (D-13-08; gated on canScan)
       - [ ] **B-13-04**: New daemon route POST /api/gitnexus/scan accepting {scope, target}; spawns gitnexus via execa argv-array; returns scanId
       - [ ] **B-13-05**: New daemon route GET /api/gitnexus/scan/:id returns job state for polling
       - [ ] **B-13-06**: Daemon refuses scan routes when bindMode !== 'loopback' (403 BIND_REFUSED; D-13-11)
       - [ ] **B-13-07**: Per-repo lock (409 SCAN_IN_FLIGHT) + global scan-serialisation lock (D-13-EXT-01) both enforced
       - [ ] **B-13-08**: Health response exposes gitnexus: { installed, canScan }
       - [ ] **B-13-09**: SPA invalidates ['coverage'] AND ['conformance'] queries on scan terminal state (D-13-09)
       - [ ] **B-13-10**: Family scan sequential per-repo with partial-success toast (D-13-04, D-13-05)
       - [ ] **B-13-11**: Stub gitnexus fixture + integration tests cover happy path, concurrency 409, bind refusal 403, partial-success family scan
       - [ ] **B-13-12**: 13-IMPECCABLE.md artifact at 1440×900 with composite ≥ 87 (or documented calibration data point)
       - [ ] **B-13-13**: Threat model includes 9 STRIDE patterns + explicit ~/.gitnexus carve-out; /cso audit complete
       ```
       After running through 13-UAT.md scenarios + verifying the must_haves, change each `[ ]` to `[x]`.
    2. Add Phase 13 rows to the **Traceability** table at the bottom of REQUIREMENTS.md (B-13-01 through B-13-13 → Phase 13 → Complete).
    3. **Update STATE.md**:
       - `stopped_at`: "Phase 13 shipped — gates passed"
       - `last_activity`: "{today's date} -- Phase 13 closed"
       - `progress.completed_phases`: bump by 1 (verify the current count)
       - `progress.completed_plans`: bump by 4 (plans 13-00 through 13-03; 13-04 is the gates plan itself)
       - Add a new "Recent decisions" line: `- [Phase 13]: <2-line summary of what shipped + IMPECCABLE composite + any deferred items>`
    4. **Update ROADMAP.md** Phase 13 entry: mark plans as `[x]` complete; add a closing-status block noting the IMPECCABLE composite score + any v1.3.x deferred items pulled out of the IMPECCABLE artifact.
    5. **Final regression sweep**:
       - `pnpm -r typecheck` exits 0
       - `pnpm -r test --run` exits 0
       - `pnpm --filter @agenticapps/dashboard-spa build` exits 0
       - `pnpm --filter @agenticapps/dashboard-agent build` exits 0
       - `pnpm --filter @agenticapps/dashboard-spa lint` exits 0
       - `git status` clean (or only the planning artifacts staged)
    6. **Commit phase closure**: `docs(13-04): close Phase 13 — gates passed, REQUIREMENTS + STATE updated`. If the user is on a feature branch, this is the final commit before opening the PR.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm -r test --run && pnpm --filter @agenticapps/dashboard-spa build && pnpm --filter @agenticapps/dashboard-agent build && grep -q "Phase 13" .planning/REQUIREMENTS.md && grep -q "B-13-01" .planning/REQUIREMENTS.md && grep -q "Phase 13 shipped\\|Phase 13 closed" .planning/STATE.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md contains a "GitNexus scoped scan actions (Phase 13)" section with B-13-01 through B-13-13 all marked `[x]`
    - REQUIREMENTS.md Traceability table includes rows for B-13-01 through B-13-13 mapped to Phase 13 = Complete
    - STATE.md `stopped_at` reads "Phase 13 shipped — gates passed"
    - STATE.md "Recent decisions" includes a Phase 13 line
    - ROADMAP.md Phase 13 entry has every plan marked `[x]`
    - `pnpm -r typecheck` exits 0
    - `pnpm -r test --run` exits 0
    - `pnpm --filter @agenticapps/dashboard-spa build` exits 0
    - `pnpm --filter @agenticapps/dashboard-agent build` exits 0
    - Closing commit is in git log
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Plan 13-04 is gates + documentation. No new code is written. Threat surface is owned by Plan 13-02 (daemon) and Plan 13-03 (SPA). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Repudiation | Gate artifacts (`13-CSO.md`, `13-REVIEW.md`, `13-UAT.md`, `13-IMPECCABLE.md`) could be tampered with or back-dated | mitigate | All four artifacts are committed to git with author + timestamp + signed commit history. The two-stage review specifically uses an independent reviewer in a fresh context (Task 3) to provide a second opinion that cannot be silently aligned with Stage 1. |
| T-13-04-02 | Information Disclosure | `/cso` audit could fail to surface the `~/.gitnexus/` carve-out, leaving the architectural exception undocumented | mitigate | Task 1's `<done>` criteria explicitly require `grep -q "~/.gitnexus" 13-CSO.md` and `grep -q "globalScanLock\\|global scan" 13-CSO.md`. The audit cannot complete without those grep checks passing. |
| T-13-04-03 | Tampering | `impeccable:critique` could be invoked at the wrong viewport, producing a score not comparable to prior calibration points | mitigate | Task 5's `<how-to-verify>` step 2 explicitly requires 1440×900. The IMPECCABLE artifact's metadata records the viewport. If a future re-run finds drift, it can be detected by viewport mismatch. |
</threat_model>

<verification>
- `13-CSO.md`, `13-REVIEW.md`, `13-UAT.md`, `13-IMPECCABLE.md` all exist and are committed
- `13-REVIEW.md` has BOTH `## Stage 1` AND `## Stage 2` sections populated (independence enforced)
- `13-IMPECCABLE.md` records composite + per-heuristic scores + findings + persona red flags
- `13-CSO.md` covers the 9 STRIDE patterns + the `~/.gitnexus/` carve-out + the global scan lock
- `13-UAT.md` documents all 6 manual scenarios from 13-VALIDATION.md with pass/fail outcomes
- REQUIREMENTS.md has B-13-01..13 ticked off
- STATE.md reflects Phase 13 shipped
- ROADMAP.md Phase 13 plan list is fully checked
- Full regression sweep passes: typecheck + test + build + lint
</verification>

<success_criteria>
1. **`/cso` artifact**: `13-CSO.md` exists, covers the 9 STRIDE patterns + the explicit `~/.gitnexus/` write-boundary carve-out + the bind-mode refusal + the global scan-serialisation lock. Zero unresolved HIGH/CRITICAL findings.
2. **Stage 1 review artifact**: `13-REVIEW.md` has a populated `## Stage 1` section with at least one `<finding>` block. HIGH severity findings addressed.
3. **Stage 2 review artifact**: `13-REVIEW.md` has a populated `## Stage 2` section (independent reviewer, fresh context). HIGH severity findings addressed. **Stages are NOT collapsed.**
4. **`/qa` UAT artifact**: `13-UAT.md` documents all 6 scenarios from 13-VALIDATION.md with pass/fail outcomes + timestamps + (optionally) screenshots.
5. **IMPECCABLE artifact**: `13-IMPECCABLE.md` exists with composite + per-heuristic scores + findings + persona red flags. Composite ≥ 87 OR documented as calibration data point #6 with reference to the empirical band trend.
6. **REQUIREMENTS.md**: Phase 13 acceptance proxies B-13-01..13 all ticked, Traceability rows added.
7. **STATE.md**: stopped_at + last_activity + Recent decisions updated for Phase 13 closure.
8. **ROADMAP.md**: Phase 13 plan list checkboxes all `[x]`; closing-status block added with IMPECCABLE score + any v1.3.x deferred items.
9. **Regression sweep**: `pnpm -r typecheck && pnpm -r test --run && pnpm --filter @agenticapps/dashboard-spa build && pnpm --filter @agenticapps/dashboard-agent build` all exit 0.
10. **Phase ready to merge**: feature branch (or HEAD) is in a clean state; one closing commit exists.
</success_criteria>

<output>
After completion, create `.planning/phases/13-gitnexus-scoped-scan-actions-coverage-matrix/13-04-SUMMARY.md` documenting:
- All 4 gate artifacts created (paths + commit SHAs)
- Stage 1 vs Stage 2 review findings counts by severity
- IMPECCABLE composite score + how it compares to prior calibration data points (Phase 10=74, 11=76, 11.1=82, 11.2 target=85-87, 12=?)
- Whether D-10.5-03.calibration-2 ADR can now be ratified (i.e. if the empirical band has stabilised across 6 data points)
- Any v1.3.x deferred items pulled out of 13-IMPECCABLE.md
- Final test counts (unit + integration + SPA + shared)
- Commit SHA of the phase-closing commit
- Whether the PR is ready to open
</output>
