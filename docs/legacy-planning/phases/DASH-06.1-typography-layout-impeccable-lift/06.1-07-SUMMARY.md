---
phase: 06.1
plan: 07
title: Targeted closure polish — 6 surgical edits to lift ≥ 90 gate
status: complete
gate: REMAINING DELTA
completed_at: 2026-05-10
---

# Plan 06.1-07 — Closure Polish SUMMARY

## Objective

Close the remaining 1-5 point gap to ≥ 90 composite across all 6 v1.0 routes
at 1440x900 per D-6-09 + D-6-21. Six surgical edits, one per route.

## Commits

| # | Commit | Route | Change |
|---|--------|-------|--------|
| 1 | 77e85af | /settings | ThemeToggle wrapped in Card container matching ManualPairForm |
| 2 | 713b8a0 | /onboarding | Step circles: bg-card-bg-hover → bg-accent/10 + text-accent |
| 3 | 7fdb3d8 | / | max-w-[60ch] on MultiProjectHome empty-state + ProjectCard no-.planning span |
| 4 | 21b3d79 | /help | "Common tasks" section added (3 cards fill viewport) |
| 5 | 30959db | /projects/:id | Integrations not-detected paragraphs trimmed to single sentence |
| 6 | 6239a5a | /pair | mx-auto max-w-[60ch] on pairing-state ol |

## Gate result

`node scripts/check-impeccable-score.mjs .playwright-mcp/scores/imp-all-061-07.json` **exit 1** (REMAINING DELTA).

| Route | Post-06.1-06 | Post-06.1-07 | Δ | Pass? |
|-------|-------------|-------------|---|-------|
| /settings | 89 | 90 | +1 | ✓ |
| /help | 87 | 90 | +3 | ✓ |
| /onboarding | 89 | 90 | +1 | ✓ |
| / | 88 | 89 | +1 | ✗ (−1) |
| /pair | 87 | 88 | +1 | ✗ (−2) |
| /projects/:id | 85 | 87 | +2 | ✗ (−3) |

3/6 routes cleared the gate. 3/6 remain below 90.

## Self-Check

| Criterion | Status |
|-----------|--------|
| 6 route commits made | ✓ |
| SPA test suite green (627 tests) | ✓ |
| Workspace typecheck clean | ✓ |
| tokens.css byte-identical | ✓ |
| No transition/animate/motion/@keyframes/skeleton/glassmorphism added | ✓ |
| refs/post-061-07-impeccable.md committed | ✓ |
| Gate cleared (≥ 90 all routes) | ✗ — REMAINING DELTA, checkpoint fired |

## Checkpoint:decision required

Per plan hard rule: "If gate STILL fails after this plan, STOP and return a checkpoint:decision."

**3 options for orchestrator:**

**A. Accept REMAINING DELTA** — Override D-6-09 (≥ 90 gate). Accept that:
- / at 89 (−1), /pair at 88 (−2), /projects/:id at 87 (−3) are good enough.
- Plans 01-07 together lifted the composite range from 81-87 → 87-90.
- Gate infrastructure is correctly wired and will enforce ≥ 90 on future PRs.
- All 4 D-6.1 decisions are confirmed implemented.

**B. Lower gate threshold to 88** — Update `scripts/check-impeccable-score.mjs` default threshold from 90 to 88.
- All 6 routes would pass at 88.
- Gate still provides meaningful floor without infinite polish loops.
- Requires D-6-09 amendment.

**C. One more micro-polish pass (06.1-08)** — Target the 3 remaining routes:
- `/` (+1 needed): Add `max-w-[60ch]` to the card subtitle `<span>` (the `client · tags` line) to constrain even when populated.
- `/pair` (+2 needed): Add `bg-card-bg p-6 rounded-card shadow-card` to the failed-state section (currently unstyled `rounded-md border`). Also constrain body text to 60ch.
- `/projects/:id` (+3 needed): This is the hardest — needs structural work on the Commitment block (line-clamp or max-h scroll) which may conflict with D-5-20 read-only constraints.
  Estimated: ~1h, high confidence on / and /pair; /projects/:id is uncertain.

## Open questions

- D-6-09 override: acceptable to ship Phase 06.1 with REMAINING DELTA given +6 to +9 point overall lift?
- /projects/:id at 87: the Commitment block monospace content is data-driven (read from filesystem), so its density can't be controlled by polish alone — is the score anchored structurally?
