---
phase: 06.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/spa/src/routes/help.lazy.tsx
  - packages/spa/src/components/OnboardingHero.tsx
  - packages/spa/src/components/OnboardingHero.test.tsx
  - packages/spa/src/routes/__tests__/help.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Multi-sentence prose paragraphs in /help and /onboarding never exceed ~75 characters per line at 1440x900."
    - "All headings, code blocks, and KbdHint chips on those routes are NOT constrained by the 75ch cap."
    - "Tests assert max-w-[75ch] is present on the targeted prose containers and absent from headings/code/kbd."
  artifacts:
    - path: "packages/spa/src/routes/help.lazy.tsx"
      provides: "Help route prose containers carrying max-w-[75ch] on every <p> and on the keyboard-shortcuts table description column."
      contains: "max-w-[75ch]"
    - path: "packages/spa/src/components/OnboardingHero.tsx"
      provides: "Onboarding hero subtitle, step-3 instruction paragraph, and 'Why local-only' details paragraph all carrying max-w-[75ch]."
      contains: "max-w-[75ch]"
    - path: "packages/spa/src/components/OnboardingHero.test.tsx"
      provides: "Updated test asserting max-w-[75ch] on subtitle/step paragraph/details body and ABSENT from h1/h2/code blocks."
      contains: "max-w-\\[75ch\\]"
  key_links:
    - from: "/help route render"
      to: "max-w-[75ch] Tailwind utility"
      via: "className concatenation on <p> elements only"
      pattern: "className=\"[^\"]*max-w-\\[75ch\\]"
    - from: "/onboarding step bodies"
      to: "max-w-[75ch] cap"
      via: "className on `<p>` and details body"
      pattern: "max-w-\\[75ch\\]"
---

<objective>
Apply D-6.1-01 line-length policy (`max-w-[75ch]`) to multi-sentence prose paragraphs on /help and /onboarding routes — the two routes whose Typography sub-score (84 and 86 respectively) sits below the 90 gate and whose primary remediation is line-length per `06-06-SUMMARY.md` "What would close the gap" recommendations.

Purpose: lifts Typography sub-score on /help and /onboarding by capping prose readability at ~75 characters per line. EXCLUDES headings, code blocks, KbdHint chips, table-cell metadata, and form/panel prose (panels are Plan 03's scope; ManualPairForm is Plan 05's scope).

Output: two route/component files modified, one test file updated, plus zero new primitives — D-6.1 Claude's Discretion explicitly allows inline `max-w-[75ch]` over a `<Prose>` primitive when usage is concentrated in 2-3 sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-CONTEXT.md
@.planning/phases/06-polish-service-install-acceptance/06-06-SUMMARY.md
@.planning/phases/06-polish-service-install-acceptance/refs/post-polish-impeccable.md
@.planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md
@packages/spa/src/routes/help.lazy.tsx
@packages/spa/src/components/OnboardingHero.tsx
@packages/spa/src/components/OnboardingHero.test.tsx

<interfaces>
<!-- Tailwind 4 arbitrary-value syntax (already supported by the project; no token addition needed). -->
<!-- The cap is applied as className="... max-w-[75ch]" on the prose container. -->

D-6.1-01 (verbatim from CONTEXT):
  Targets: multi-sentence prose paragraphs in card bodies, all `<p>` content
    in /help shortcut explanations, /onboarding hero subtitle and steps,
    /settings descriptions and field hints.
  Excludes: card metadata (counts, IDs, status pills, timestamps), all headings
    (h1-h6), code blocks (`<pre>`, `<code>` blocks; inline `<code>` inherits
    container), kbd hints.

Phase 5.1 typography invariants (`tokens.css`):
  --leading-relaxed: 1.65;     /* used on prose paragraphs */
  --text-base: 13px;           /* default body */
  Inter Variable, font-feature-settings 'tnum' 1 on numerics.

Anti-AI-slop (D-43, D-5.1-10): NO new colors, NO transitions, NO animation,
NO Prose primitive (planner choice — usage is concentrated in 2 files).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap onboarding hero prose at 75ch and update tests</name>
  <files>
    packages/spa/src/components/OnboardingHero.tsx,
    packages/spa/src/components/OnboardingHero.test.tsx
  </files>
  <read_first>
    - packages/spa/src/components/OnboardingHero.tsx (the component being modified)
    - packages/spa/src/components/OnboardingHero.test.tsx (the test file being modified)
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-CONTEXT.md (D-6.1-01 source of truth)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §3 Typography Scale (so cap respects 13px body + 1.65 line-height contract)
    - packages/spa/src/components/OnboardingHero.tsx — confirm current `max-w-3xl` outer wrapper is preserved (D-6.1-01 caps prose only, NOT the layout container)
  </read_first>
  <behavior>
    - Subtitle paragraph "Nothing leaves your machine." MUST carry className containing `max-w-[75ch]`.
    - Step-3 explanation paragraph (the one starting "When the agent prints a pair URL like...") MUST carry className containing `max-w-[75ch]`.
    - "Why local-only" details body paragraph (starting "Your `.planning/`...") MUST carry className containing `max-w-[75ch]` (this also REPLACES the existing `max-w-prose` on the parent <details> — see action below for rationale).
    - h1 ("One local daemon. Every device.") MUST NOT carry `max-w-[75ch]`.
    - h2 step-headers ("Install the agent", "Register and start", "Click the pair URL") MUST NOT carry `max-w-[75ch]`.
    - All `<CodeBlock>` invocations remain unchanged (code excluded from D-6.1-01).
    - The outer wrapper `mx-auto max-w-3xl px-6 py-12 md:px-8 md:pt-16` is layout, NOT prose — REMAINS unchanged.
    - Tests assert presence of `max-w-[75ch]` on the 3 prose elements and ABSENCE on h1, all h2s, and any element rendered by `<CodeBlock>`.
  </behavior>
  <action>
    1. RED — In `packages/spa/src/components/OnboardingHero.test.tsx`, add a new `describe('D-6.1-01 line-length cap', () => { ... })` block with the following named test cases (commit as failing first):

       a. `it('caps subtitle paragraph at 75ch', () => { ... getByText('Nothing leaves your machine.').closest('p')?.className.includes('max-w-[75ch]') === true })`
       b. `it('caps step-3 instruction paragraph at 75ch', () => { ... getByText(/When the agent prints a pair URL/).closest('p')?.className.includes('max-w-[75ch]') === true })`
       c. `it('caps "Why local-only" details body at 75ch', () => { ... getByText(/Your.*\.planning.*are sensitive/).closest('p')?.className.includes('max-w-[75ch]') === true })`
       d. `it('does NOT cap the h1 heading', () => { ... getByRole('heading', { level: 1 }).className.includes('max-w-[75ch]') === false })`
       e. `it('does NOT cap any h2 heading', () => { const h2s = getAllByRole('heading', { level: 2 }); h2s.forEach((h) => expect(h.className.includes('max-w-[75ch]')).toBe(false)) })`

       Confirm RED by running:
       `pnpm --filter @agenticapps/dashboard-spa exec vitest run src/components/OnboardingHero.test.tsx`
       Expect 5 new failing tests. Commit: `test(06.1-01): RED for D-6.1-01 line-length cap on OnboardingHero`.

    2. GREEN — In `packages/spa/src/components/OnboardingHero.tsx`:

       Subtitle (line 10) — change:
         `<p className="mt-2 text-base leading-relaxed text-text-secondary">`
         to:
         `<p className="mt-2 max-w-[75ch] text-base leading-relaxed text-text-secondary">`

       Step-3 instruction paragraph (line 51) — change:
         `<p className="text-base leading-relaxed text-text-secondary">`
         to:
         `<p className="max-w-[75ch] text-base leading-relaxed text-text-secondary">`

       "Why local-only" details body (line 66) — change:
         `<p className="mt-3 text-base leading-relaxed text-text-secondary">`
         to:
         `<p className="mt-3 max-w-[75ch] text-base leading-relaxed text-text-secondary">`

       Also REMOVE `max-w-prose` from the parent `<details>` element (line 62) since the inner `<p>` now carries the explicit cap. New className for the `<details>`:
         `<details className="mt-12">`

       Headings (h1 line 7, h2 lines 21/34/50) and `<CodeBlock>` calls remain unchanged.

       Re-run the test command — expect 5 passing.

    3. REFACTOR (only if needed) — no class deduplication required; no new helper extracted (per D-6.1-01 Claude's Discretion: inline application chosen over `<Prose>` primitive).

    4. Commit GREEN: `feat(06.1-01): apply max-w-[75ch] to OnboardingHero prose per D-6.1-01`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa exec vitest run src/components/OnboardingHero.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-w-\[75ch\]' packages/spa/src/components/OnboardingHero.tsx` returns >= 3.
    - `grep -E '<h[12][^>]*max-w-\[75ch\]' packages/spa/src/components/OnboardingHero.tsx` returns no matches (no headings carry the cap).
    - `grep 'max-w-prose' packages/spa/src/components/OnboardingHero.tsx` returns no matches (replaced by explicit child cap).
    - `pnpm --filter @agenticapps/dashboard-spa exec vitest run src/components/OnboardingHero.test.tsx` exits 0 with all D-6.1-01 named cases passing.
  </acceptance_criteria>
  <done>
    OnboardingHero.tsx prose paragraphs carry `max-w-[75ch]`; headings and CodeBlocks do not; OnboardingHero.test.tsx asserts both presence and absence; vitest passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cap /help prose at 75ch (placeholder body + table description column)</name>
  <files>
    packages/spa/src/routes/help.lazy.tsx,
    packages/spa/src/routes/__tests__/help.test.tsx
  </files>
  <read_first>
    - packages/spa/src/routes/help.lazy.tsx (the route being modified)
    - packages/spa/src/components/ui/KbdHint.tsx (confirm KbdHint shape so test selector is correct)
    - .planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-CONTEXT.md (D-6.1-01 — kbd hints are EXCLUDED from cap)
    - .planning/phases/DASH-05.1-ui-redesign-cloudflare-inspired-sidebar-dashboard-shell/05.1-UI-SPEC.md §8 Card Anatomy (Card body 13px regular, 1.5 line-height)
    - packages/spa/src/routes/__tests__/ — confirm no existing help test file (this task creates the first one)
  </read_first>
  <behavior>
    - Placeholder card body paragraph "See the README at github.com/...for full documentation." MUST carry `max-w-[75ch]`.
    - The keyboard-shortcuts table's `<td>` description cells (e.g., "Refresh the current view's data") MUST carry `max-w-[75ch]` so multi-word labels wrap at the same column boundary as prose.
    - The kbd-bearing `<td>` cells (the left column with `<KbdHint />`) MUST NOT carry `max-w-[75ch]` (kbd hints are tabular metadata, not prose).
    - h2 ("Help" placeholder card heading and "Keyboard shortcuts" table heading) MUST NOT carry `max-w-[75ch]`.
    - The `<code>` element inside the placeholder body REMAINS unchanged (inline code inherits the parent container's cap, per D-6.1-01).
  </behavior>
  <action>
    1. RED — Create `packages/spa/src/routes/__tests__/help.test.tsx` with imports:
       ```ts
       import { describe, it, expect } from 'vitest'
       import { render } from '@testing-library/react'
       import { createRouter, createMemoryHistory, RouterProvider, createRootRoute, createRoute, Outlet } from '@tanstack/react-router'
       import { Route as HelpLazyRoute } from '../help.lazy.tsx'
       ```

       Use the same TanStack Router test harness pattern already used in `packages/spa/src/routes/projects.$projectId.test.tsx` (read that file first to copy the exact harness shape — specifically the `createRouter` + `createMemoryHistory` + `createRootRoute` + `createRoute` setup mounting the lazy route at path `/help`).

       Add a `describe('D-6.1-01 line-length cap on /help', () => { ... })` block:

       a. `it('caps the placeholder body paragraph at 75ch', () => { /* render at /help; getByText that contains 'See the README'; closest('p').className includes max-w-[75ch] */ })`
       b. `it('caps each shortcut-description <td> at 75ch', () => { /* getAllByRole('cell') filtered to descriptions (those without role='presentation' KbdHint); each .className includes max-w-[75ch] */ })`
       c. `it('does NOT cap the kbd <td> cells', () => { /* the cell WRAPPING <KbdHint> elements MUST NOT include max-w-[75ch] */ })`
       d. `it('does NOT cap any h2 heading', () => { getAllByRole('heading', { level: 2 }).forEach((h) => expect(h.className.includes('max-w-[75ch]')).toBe(false)) })`

       Confirm RED:
       `pnpm --filter @agenticapps/dashboard-spa exec vitest run src/routes/__tests__/help.test.tsx`
       Commit: `test(06.1-01): RED for D-6.1-01 line-length cap on /help`.

    2. GREEN — In `packages/spa/src/routes/help.lazy.tsx`:

       Placeholder body `<p>` (line 21) — change:
         `<p className="mt-3 text-base leading-relaxed text-text-secondary">`
         to:
         `<p className="mt-3 max-w-[75ch] text-base leading-relaxed text-text-secondary">`

       Description `<td>` cells (lines 33, 37, 41, 48 — all the `<td className="py-2 text-text-secondary">` cells) — change each to:
         `<td className="py-2 max-w-[75ch] text-text-secondary">`

       The kbd-bearing `<td>` cells (the ones whose body is `<KbdHint keys="..."/>`, lines 32/36/40/45 — including the wider `<td className="py-2 w-24">`) REMAIN unchanged.

       Both `<h2>` headings ("Help" line 20, "Keyboard shortcuts" line 28) REMAIN unchanged.

       Re-run the test command — expect all 4 named cases passing.

    3. Commit GREEN: `feat(06.1-01): apply max-w-[75ch] to /help prose per D-6.1-01`.
  </action>
  <verify>
    <automated>pnpm --filter @agenticapps/dashboard-spa exec vitest run src/routes/__tests__/help.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-w-\[75ch\]' packages/spa/src/routes/help.lazy.tsx` returns >= 5 (one for the placeholder paragraph + four for the description `<td>` cells).
    - `grep -E '<h[1-6][^>]*max-w-\[75ch\]' packages/spa/src/routes/help.lazy.tsx` returns no matches.
    - `grep -B1 'KbdHint' packages/spa/src/routes/help.lazy.tsx | grep -c 'max-w-\[75ch\]'` returns 0 (no kbd-cell parents carry the cap).
    - `pnpm --filter @agenticapps/dashboard-spa exec vitest run src/routes/__tests__/help.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    /help route prose paragraph and shortcut-description `<td>`s carry `max-w-[75ch]`; kbd-cells and h2 headings do not; new help.test.tsx asserts both directions; vitest passes.
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm --filter @agenticapps/dashboard-spa exec vitest run src/components/OnboardingHero.test.tsx src/routes/__tests__/help.test.tsx` — all D-6.1-01 named cases pass.
2. Run `pnpm --filter @agenticapps/dashboard-spa typecheck` — clean (no TS regressions from new test file).
3. Inspect grep counts:
   - `grep -rEc 'max-w-\[75ch\]' packages/spa/src/components/OnboardingHero.tsx packages/spa/src/routes/help.lazy.tsx` returns >= 8 total across both files.
4. Visually walk both routes at 1440x900 in the dev server (`pnpm --filter @agenticapps/dashboard-spa dev`):
   - /onboarding subtitle, step-3 paragraph, and "Why local-only" details body wrap at ~75ch.
   - /help placeholder paragraph and shortcut-description cells wrap at ~75ch; kbd column is untouched.
   - Headings and code blocks are unchanged in width.
</verification>

<success_criteria>
- All tasks complete and committed (RED + GREEN per task).
- Vitest exit 0 on the two affected test files.
- Typecheck clean across the workspace.
- No `<h1>`, `<h2>`, `<h3>`, `<pre>`, `<code>`, or `<KbdHint>` parent in the modified files carries `max-w-[75ch]` (per D-6.1-01 exclusions).
- Cap applied via Tailwind 4 arbitrary-value syntax — NO new tokens, NO new primitive.
- Anti-AI-slop discipline preserved: zero animations, transitions, or new color additions in this plan.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DOM | Prose containers carry width caps; cap is purely presentational, no user-input crosses this boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01-01 | I (Information disclosure) | OnboardingHero pair URL example, /help README link | accept | The URL `https://agenticapps-dashboard.pages.dev/pair?...` is a public marketing example, not a real user token; the GitHub README link is public. No new disclosure surface. |
| T-06.1-01-02 | T (Tampering) | Prose className concatenation | accept | Strings are static literals in TSX; no user input is interpolated. No injection surface. |
| T-06.1-01-03 | D (DoS via accessibility tree) | Test file using `getAllByRole('heading')` against /help | mitigate | Test file is render-only; no `aria-live` regions added in this plan (those are Plan 04). No screen-reader DoS path. |
</threat_model>

<output>
After completion, create `.planning/phases/DASH-06.1-typography-layout-impeccable-lift/06.1-01-SUMMARY.md` recording:
- Files modified with `max-w-[75ch]` count per file.
- Test cases added (5 in OnboardingHero.test.tsx, 4 in new help.test.tsx).
- Confirmation that headings and CodeBlocks were NOT modified.
- Anti-AI-slop self-check (no animations/transitions/new tokens added).
</output>
</output>
