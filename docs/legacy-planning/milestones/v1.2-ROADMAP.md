# Milestone v1.2: Optional integrations & fleet-conformance follow-through

**Status:** ✅ SHIPPED 2026-06-12 (merged to `main` via PR #58 + #59; tagged `v1.2`)
**Phases:** 8, 12 (close-out), 12.1, 13 (close-out), 14.1
**Total Plans:** 6 (Phase 8) + 4 carry-over close-out/lift phases

## Overview

Mostly carry-over close-out of already-shipped v1.1 work; only **Phase 8** is net-new feature work. Landed the held optional-integration panels (Sentry · Linear · Infisical, unblocked by the completed `secrets-platform` cparx pilot) and cleared the conformance debt deferred from v1.1: the Phase 12 close-out gate, a chart-legibility follow-up (12.1), the Phase 13 gate confirmation, and the Phase 14.1 `/code-intelligence` IMPECCABLE lift that retired the outstanding structural-debt waiver.

Phase directories were preserved (no `phases.clear`); numbering continued from existing.

## Phases

### Phase 8: Optional Integration Panels

**Goal:** Read-only Sentry, Linear, and Infisical panels that surface live data when their env vars are configured and show graceful "configure to enable" empty states when they are not — without making the dashboard depend on any of them. Source: spec §"Optional integrations: the contract" (lines 508–544), §"Optional integration routes" (lines 354–369).
**Depends on:** Phase 6 (complete dashboard baseline). Independent of the v1.2 close-out phases.
**Plans:** 6 plans (4 waves)

Plans:

- [x] 08-01: Shared Zod schemas (Sentry, Linear, env, INFI-03 scope) [wave 1]
- [x] 08-02: Agent libs — outboundFetch (timeout/last-good/classify) + envFile (0600) + ENV_FILE [wave 2]
- [x] 08-03: Sentry route — slug resolution + /sentry/recent (60s cache, last-good, token-safe) [wave 3]
- [x] 08-04: env set/list/unset CLI + boot loadEnvFile + INFI-03 scope reflection [wave 3]
- [x] 08-05: Linear route — branch+log detection + /linear/issues + mount both routes [wave 4]
- [x] 08-06: SPA SentryPanel + LinearPanel + query hooks + SingleProjectView wiring [wave 4]

**Details:**
Requirements SENTRY-01..03, LINEAR-01..03, INFI-01..03 all delivered. With zero integration env vars set, all three panels render "configure to enable" copy and the dashboard remains fully functional (INV-03). With tokens set, Sentry and Linear panels show live data; Infisical status reflects `.infisical.json` presence. API failures show "unreachable — using cached data from {time}" rather than crashing. No native dependencies added to `packages/agent/` (INV-02); secrets handling honors the `0600` constraint (INV-05); all new daemon↔SPA wire shapes validate against shared Zod schemas (INV-04). Collapsed Sentry/Linear panels signal "not configured" at a glance (IMPECCABLE lift); `08-IMPECCABLE.md` composite 78, structural-debt WAIVED per the per-phase waiver clause. Security: 28/28 threats CLOSED.

### Phase 12: Observability Conformance Surface — gate close-out (carry-over)

**Goal:** Run the deferred `12-06` close-out gate for implementation shipped in v1.1.
**Plans:** 7/7 (gate only — impl 12-00..05 shipped in v1.1)

Plans:

- [x] 12-06: close-out gate (retrospective via GSD retro-tools)

**Details:**
GATE-12-01..03 closed: `12-REVIEW.md` (0 crit) + `12-SECURITY.md` (SECURED 27/27) + `12-HUMAN-UAT.md` (4/4 PASS) + `12-IMPECCABLE.md` (composite 80, at ratified ≥80 floor) + `12-VERIFICATION.md` with 1:1 gate evidence. 2 P1 chart-legibility gaps routed to Phase 12.1.

### Phase 12.1: Conformance chart legibility (INSERTED)

**Goal:** Make `/observability/conformance` fleet trend chart legible without hover.
**Depends on:** Phase 12 close-out (emerged from 12-IMPECCABLE).
**Plans:** 1 plan

Plans:

- [x] 12.1-01: persistent legend + 70/90 threshold labels in `FleetTrendChart.tsx` (TDD)

**Details:**
IMPV-12.1-01 closed. Added persistent legend (4 series → exact stroke-color swatches) + labeled the 70 (floor) / 90 (target) threshold lines. Re-critique composite **84** (≥83 target met). Closed code-review IN-06.

### Phase 13: GitNexus scoped scan actions — gate close-out (carry-over)

**Goal:** Confirm the `13-04` gate ritual complete for implementation shipped in v1.1.
**Plans:** 4/4 (gate only — impl 13-00..03 shipped in v1.1)

Plans:

- [x] 13-04: gates ritual (confirmed complete + verification written)

**Details:**
GATE-13-01 closed: `13-CSO.md` (PASS, 0 HIGH), `13-REVIEW.md` (both stages; HIGH S1-01 addressed), `13-IMPECCABLE.md` (composite 84), `13-UAT.md` (resolved/pass), and `13-VERIFICATION.md` (PASS). gitnexusScan 45 tests green. Phase 13 flipped ⚠️ → ✅.

### Phase 14.1: /code-intelligence IMPECCABLE lift (INSERTED)

**Goal:** Lift `/code-intelligence` composite to the ≥80 floor and retire the structural-debt waiver.
**Depends on:** Phase 14 (v1.1).
**Plans:** 1 plan

Plans:

- [x] 14.1-01: error recovery + communicative cells + relative time + header consistency + real status-pill tokens (TDD)

**Details:**
IMPV-01 closed. Lifted composite **74 → 81** (Nielsen 24→30/40; cog-load 2→0). `14.1-IMPECCABLE.md` committed; structural-debt waiver retired.

---

## Milestone Summary

**Decimal/inserted phases:**

- Phase 12.1: Conformance chart legibility (inserted after Phase 12 close-out; emerged from 12-IMPECCABLE)
- Phase 14.1: `/code-intelligence` IMPECCABLE lift (inserted after Phase 14; retires the structural-debt waiver)

**Key Decisions:**

- `./daemon` subpath export in `shared` — avoids rootDir violation; `env.ts` absent from browser-facing `index.ts` (T-08-01 / INV-05).
- Linear cache keyed `projectId:issueId` — same issueId across two projects must not share cache entries.
- Linear auth uses raw API key header (no `Bearer` prefix) — `lin_api_*` keys differ from OAuth Bearer tokens.
- LinearPanel renders identifier in link only (not a separate span) — avoids duplicate DOM text that breaks RTL `getByText`.
- Collapsed-panel "not configured" signal at `text-sm` to match sibling panel states (IMPECCABLE lift).

**Issues Resolved:**

- Phase 12 formally closed (gate `12-06` run retrospectively; `12-VERIFICATION.md` written).
- Phase 13 gate `13-04` confirmed complete; `13-VERIFICATION.md` written.
- `/code-intelligence` structural-debt waiver retired (composite 74 → 81).
- Two v1.1-deferred close-out items (Phase 12 gate, Phase 13 gate) resolved.

**Issues Deferred:**

- Historical human-UAT / verification sign-offs on Phases 00–06, 05.1, 10, 11.1, 11.2 (acknowledged at v1.2 close; see STATE.md "Deferred Items").
- 2 open debug sessions (`family-scan-no-ui-feedback`, `per-row-scan-repo-not-registered`).
- Tailscale second-device viewer access (D-14-04) — infra-gated.
- Phase 01 HUMAN-UAT (2 pending scenarios).

**Technical Debt Incurred:**

- `08-IMPECCABLE.md` composite 78 below the ≥80 floor — accepted under the per-phase structural-debt waiver clause (D-10.5-03.calibration-2).
- `claude-workflow` migration 0008 drift — 2 known-pending smoke-test failures on every dashboard CI run (pre-existing).

---

_For current project status, see .planning/ROADMAP.md_
